/* SPDX-License-Identifier: MPL-2.0 */

/**
 * @surea11y/core -- standalone browser bundle.
 *
 * Auto-generated by scripts/build-browser.js from src/core.js. Do not edit
 * this file by hand -- change the rule sources under src/checks and run
 * "npm run build" instead.
 *
 * Loading this file directly (e.g. via a <script> tag, with no bundler or
 * module loader) defines a single global, window.a11ycore, exposing the
 * engine's in-page scan function. See README.md's "Standalone browser
 * bundle" section and docs/INTEGRATION.md for usage and its one limitation
 * (no cross-frame scanning -- use the npm package directly for that).
 */
(function (global) {
  'use strict';

function runa11yCoreInPage(pageUrl, contextSelector, engineOptions, runOnly) {
  const ENGINE_TAG = "a11ycore";
  const SCHEMA_VERSION = "1.0.0";

  // Rule catalog (data only)
  const CHECK_DEFS = [
  {
    "ruleId": "accesskeys",
    "title": "accesskey values must be unique",
    "description": "Checks that no two elements on the page share the same accesskey attribute value.",
    "i18n": {
      "titleKey": "accesskeys_title",
      "descriptionKey": "accesskeys_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "keyboard",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "area-alt-decorative",
    "title": "<area> with alt=\"\" must be decorative (manual review)",
    "description": "Flags <area> elements with empty alt for human review that they are decorative/non-informative.",
    "i18n": {
      "titleKey": "area_altDecorative_title",
      "descriptionKey": "area_altDecorative_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "images",
      "imagemap",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "area-alt-present",
    "title": "&lt;area&gt; must have an alt attribute",
    "description": "Checks that &lt;area&gt; elements provide an alt attribute to support a text alternative mechanism.",
    "i18n": {
      "titleKey": "area_altPresent_title",
      "descriptionKey": "area_altPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "images",
      "imagemap",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "area-alt-attr-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "area-alt-quality",
    "title": "<area> alt text must be appropriate (manual review)",
    "description": "Flags <area> elements with non-empty alt text for human review of appropriateness.",
    "i18n": {
      "titleKey": "area_altQuality_title",
      "descriptionKey": "area_altQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "images",
      "imagemap",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-allowed-attr",
    "title": "aria-* attributes must be permitted for the element’s role",
    "description": "Checks that every recognized aria-* attribute present on an element with an explicit role is either globally supported or supported by that role.",
    "i18n": {
      "titleKey": "ariaAllowedAttr_title",
      "descriptionKey": "ariaAllowedAttr_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "medium",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-attr-allowed-for-role"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-allowed-role",
    "title": "Explicit role must be permitted for its host element",
    "description": "Checks that an explicit role=\"\" attribute is one of the roles the ARIA-in-HTML specification permits for the host element (e.g. role=\"tab\" is not permitted on <nav>).",
    "i18n": {
      "titleKey": "ariaAllowedRole_title",
      "descriptionKey": "ariaAllowedRole_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-role-allowed-for-element"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-braille-equivalent",
    "title": "aria-braillelabel/aria-brailleroledescription must have a non-braille equivalent",
    "description": "Checks that elements using aria-braillelabel also have a regular accessible name, and elements using aria-brailleroledescription also have aria-roledescription.",
    "i18n": {
      "titleKey": "ariaBrailleEquivalent_title",
      "descriptionKey": "ariaBrailleEquivalent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-braille-equivalent"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-checked-state-mismatch",
    "title": "Native checkbox/radio aria-checked should match its actual state",
    "description": "Flags a native <input type=\"checkbox\">/<input type=\"radio\"> whose explicit aria-checked value disagrees with its actual checked/indeterminate state, for manual review.",
    "i18n": {
      "titleKey": "ariaCheckedStateMismatch_title",
      "descriptionKey": "ariaCheckedStateMismatch_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "forms",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-checked-state-mismatch"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-conditional-attr",
    "title": "aria-errormessage requires aria-invalid to be set to a non-false value",
    "description": "Checks that elements with aria-errormessage also have aria-invalid set to \"true\", \"grammar\", or \"spelling\" — otherwise the error message is dropped from the accessibility tree.",
    "i18n": {
      "titleKey": "ariaConditionalAttr_title",
      "descriptionKey": "ariaConditionalAttr_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-conditional-attr"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-deprecated-role",
    "title": "role attribute should not use a deprecated or author-discouraged ARIA role",
    "description": "Checks that an explicit role=\"\" attribute does not use a role deprecated by the WAI-ARIA specification, or one reserved for user-agent-internal use (e.g. role=\"generic\").",
    "i18n": {
      "titleKey": "ariaDeprecatedRole_title",
      "descriptionKey": "ariaDeprecatedRole_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-role-not-deprecated"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-hidden-body",
    "title": "The document <body> must not be aria-hidden",
    "description": "Checks that <body> does not have aria-hidden=\"true\", which would remove the entire page from the accessibility tree.",
    "i18n": {
      "titleKey": "ariaHiddenBody_title",
      "descriptionKey": "ariaHiddenBody_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "wcag412",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1",
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "critical",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "aria-hidden-body-absent"
        ],
        "4.1.2": [
          "aria-hidden-body-absent"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-hidden-focus",
    "title": "ARIA hidden elements must not be focusable",
    "description": "Checks that aria-hidden=\"true\" elements are not focusable and do not contain focusable descendants.",
    "i18n": {
      "titleKey": "ariaHidden_focus_title",
      "descriptionKey": "ariaHidden_focus_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag2aa",
      "wcag412",
      "structure",
      "aria",
      "focus",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "2.4.7",
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.7",
        "title": "Focus Visible",
        "conformanceLevel": "AA"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-hidden-focusable"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-prohibited-attr",
    "title": "ARIA naming attributes must not be used on roles that prohibit them",
    "description": "Checks that aria-label/aria-labelledby are not present on WAI-ARIA roles whose specification explicitly prohibits ARIA naming (e.g. generic, emphasis, strong, paragraph).",
    "i18n": {
      "titleKey": "ariaProhibitedAttr_title",
      "descriptionKey": "ariaProhibitedAttr_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-attr-not-prohibited"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-prohibited-children",
    "title": "Container roles must not own a child with a disallowed role",
    "description": "Checks that every accessible-tree-owned child of a container role (list, listbox, menu, menubar, radiogroup, rowgroup, table, grid, treegrid, tablist, tree, row) has one of that role's allowed owned roles — the same set as its required owned roles.",
    "i18n": {
      "titleKey": "ariaProhibitedChildren_title",
      "descriptionKey": "ariaProhibitedChildren_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "medium",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-role-owned-children-allowed"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-required-attr",
    "title": "Roles with a required ARIA state/property must carry it",
    "description": "Checks that elements with an explicit role carry every unambiguous, context-independent required aria-* state/property for that role (e.g. role=\"checkbox\" must have aria-checked).",
    "i18n": {
      "titleKey": "ariaRequiredAttr_title",
      "descriptionKey": "ariaRequiredAttr_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-attr-required-for-role"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-required-children",
    "title": "Container roles must own at least one required child role",
    "description": "Checks that container roles with a documented \"required owned elements\" entry (list, listbox, menu, radiogroup, table, grid, tablist, tree, row, ...) contain at least one descendant or aria-owns-referenced element with an acceptable owned role.",
    "i18n": {
      "titleKey": "ariaRequiredChildren_title",
      "descriptionKey": "ariaRequiredChildren_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "medium",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-role-required-owned-children"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-required-parent",
    "title": "Roles requiring a specific context role must be in that context",
    "description": "Checks that roles with a documented \"required context role\" entry (listitem, option, tab, treeitem, row, cell, ...) have an ancestor or aria-owns owner with an acceptable context role.",
    "i18n": {
      "titleKey": "ariaRequiredParent_title",
      "descriptionKey": "ariaRequiredParent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "medium",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-role-required-context-parent"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-role-name-present",
    "title": "ARIA widget/container roles have an accessible name",
    "description": "Checks that selected ARIA widget/container roles expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "ariaRoleNamePresent_title",
      "descriptionKey": "ariaRoleNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "navigation",
      "atomic",
      "automatic",
      "name",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-role-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-roles-valid",
    "title": "role attribute must be a valid, non-abstract ARIA role",
    "description": "Checks that an explicit role=\"\" attribute resolves to a real, non-abstract WAI-ARIA role.",
    "i18n": {
      "titleKey": "ariaRolesValid_title",
      "descriptionKey": "ariaRolesValid_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-role-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-text",
    "title": "role=\"text\" elements should have no focusable descendants",
    "description": "Checks that elements with role=\"text\" contain no focusable descendant (link, button, form control, tabindex, iframe, or contenteditable).",
    "i18n": {
      "titleKey": "ariaText_title",
      "descriptionKey": "ariaText_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "aria",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-valid-attr",
    "title": "aria-* attributes must be real, defined ARIA attributes",
    "description": "Checks that every aria-* attribute name present in the DOM is a real attribute defined by the WAI-ARIA specification.",
    "i18n": {
      "titleKey": "ariaValidAttr_title",
      "descriptionKey": "ariaValidAttr_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-attr-name-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "aria-valid-attr-value",
    "title": "aria-* attribute values must match their declared type",
    "description": "Checks that every recognized aria-* attribute has a value conforming to its WAI-ARIA-declared value type (boolean, tristate, token, integer, number, or ID reference).",
    "i18n": {
      "titleKey": "ariaValidAttrValue_title",
      "descriptionKey": "ariaValidAttrValue_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "aria-attr-value-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "autocomplete-valid",
    "title": "autocomplete attribute must be a valid autofill value",
    "description": "Checks that a non-empty autocomplete attribute is \"on\"/\"off\" or a well-formed autofill detail token list.",
    "i18n": {
      "titleKey": "autocompleteValid_title",
      "descriptionKey": "autocompleteValid_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag21aa",
      "wcag135",
      "forms",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.5"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.5",
        "title": "Identify Input Purpose",
        "conformanceLevel": "AA"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.5": [
          "autocomplete-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "avoid-inline-spacing",
    "title": "Inline style must not force text spacing below the WCAG metric",
    "description": "Checks that where inline style forces line-height, letter-spacing or word-spacing with !important, the value already meets WCAG 1.4.12, so the user has nothing left to override.",
    "i18n": {
      "titleKey": "avoidInlineSpacing_title",
      "descriptionKey": "avoidInlineSpacing_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag21aa",
      "wcag1412",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.4.12"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.4.12",
        "title": "Text Spacing",
        "conformanceLevel": "AA"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.4.12": [
          "avoid-inline-spacing"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "binary-control-name-present",
    "title": "Binary controls have an accessible name",
    "description": "Checks that checkbox, radio, and switch controls expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "binaryControlNamePresent_title",
      "descriptionKey": "binaryControlNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "name",
      "checkbox",
      "radio",
      "switch",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "checkbox-name-present",
          "radio-name-present",
          "switch-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "button-name-present",
    "title": "Buttons have an accessible name",
    "description": "Checks that buttons expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "buttonNamePresent_title",
      "descriptionKey": "buttonNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "buttons",
      "name",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "button-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "bypass-blocks-present",
    "title": "Page must provide a way to bypass repeated blocks",
    "description": "Checks that the page has at least one recognized WCAG 2.4.1 bypass-blocks mechanism: a main landmark, a working same-page anchor link, or a heading.",
    "i18n": {
      "titleKey": "bypassBlocksPresent_title",
      "descriptionKey": "bypassBlocksPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag241",
      "navigation",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "2.4.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.1",
        "title": "Bypass Blocks",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "2.4.1": [
          "bypass-blocks-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "canvas-text-alternative-present",
    "title": "<canvas> must provide a text alternative",
    "description": "Checks that <canvas> elements provide a text alternative via fallback content or an accessible name.",
    "i18n": {
      "titleKey": "canvas_textAltPresent_title",
      "descriptionKey": "canvas_textAltPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "canvas",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "canvas-text-alternative-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "canvas-text-alternative-quality",
    "title": "<canvas> text alternative must be appropriate (manual review)",
    "description": "Flags <canvas> elements with a detected text alternative for human review of equivalence and appropriateness.",
    "i18n": {
      "titleKey": "canvas_textAltQuality_title",
      "descriptionKey": "canvas_textAltQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "canvas",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "combobox-name-present",
    "title": "Comboboxes have an accessible name",
    "description": "Checks that elements with role=\"combobox\" expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "comboboxNamePresent_title",
      "descriptionKey": "comboboxNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "name",
      "combobox",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "combobox-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "contrast-computable",
    "title": "Color contrast is computable for rendered text",
    "description": "Determines whether sufficient information is available to compute WCAG color contrast for visible text (e.g., no gradients/images/blend modes that make background indeterminate).",
    "i18n": {
      "titleKey": "contrastComputable_title",
      "descriptionKey": "contrastComputable_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2aa",
      "wcag2aaa",
      "wcag143",
      "wcag146",
      "contrast",
      "color",
      "structure",
      "atomic",
      "automatic",
      "dom",
      "a11ycore"
    ],
    "wcagSc": [
      "1.4.3",
      "1.4.6"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.4.3",
        "title": "Contrast (Minimum)",
        "conformanceLevel": "AA"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.4.6",
        "title": "Contrast (Enhanced)",
        "conformanceLevel": "AAA"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.4.3": [
          "contrast-computability-143"
        ],
        "1.4.6": [
          "contrast-computability-146"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "contrast-enhanced",
    "title": "Text must meet the enhanced color contrast ratio",
    "description": "Checks that visible text has a contrast ratio of at least 7:1 (normal) or 4.5:1 (large), when contrast is computable from CSS.",
    "i18n": {
      "titleKey": "contrastEnhanced_title",
      "descriptionKey": "contrastEnhanced_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2aaa",
      "wcag146",
      "contrast",
      "color",
      "structure",
      "atomic",
      "automatic",
      "dom",
      "a11ycore"
    ],
    "wcagSc": [
      "1.4.6"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.4.6",
        "title": "Contrast (Enhanced)",
        "conformanceLevel": "AAA"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.4.6": [
          "contrast-enhanced-text"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "contrast-minimum",
    "title": "Text must meet the minimum color contrast ratio",
    "description": "Checks that visible text has a contrast ratio of at least 4.5:1 (normal) or 3:1 (large), when contrast is computable from CSS.",
    "i18n": {
      "titleKey": "contrastMinimum_title",
      "descriptionKey": "contrastMinimum_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2aa",
      "wcag143",
      "contrast",
      "color",
      "structure",
      "atomic",
      "automatic",
      "dom",
      "a11ycore"
    ],
    "wcagSc": [
      "1.4.3"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.4.3",
        "title": "Contrast (Minimum)",
        "conformanceLevel": "AA"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.4.3": [
          "contrast-minimum-text"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "css-hidden-focus",
    "title": "Focusable elements must not be visually hidden",
    "description": "Checks that keyboard-focusable elements are not visually hidden by CSS techniques that can leave them in the tab order.",
    "i18n": {
      "titleKey": "cssHidden_focus_title",
      "descriptionKey": "cssHidden_focus_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2aa",
      "wcag247",
      "navigation",
      "focus",
      "css",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "2.4.7"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.7",
        "title": "Focus Visible",
        "conformanceLevel": "AA"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "2.4.7": [
          "css-hidden-focusable"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "css-orientation-lock",
    "title": "CSS must not lock the page to a single orientation",
    "description": "Checks that no @media (orientation: portrait|landscape) rule sets a transform: rotate(...) on the page, a known technique for defeating device orientation.",
    "i18n": {
      "titleKey": "cssOrientationLock_title",
      "descriptionKey": "cssOrientationLock_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag21aa",
      "wcag134",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.4"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.4",
        "title": "Orientation",
        "conformanceLevel": "AA"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.4": [
          "css-orientation-lock"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "definition-list-children-valid",
    "title": "Description lists must be structured correctly",
    "description": "Checks that <dl> elements only directly contain <dt>/<dd> groups (optionally wrapped in one <div>), <script>, <template>, or <style>.",
    "i18n": {
      "titleKey": "definitionListChildrenValid_title",
      "descriptionKey": "definitionListChildrenValid_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "automatic",
      "list",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "definition-list-children-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "deprecated-elements-not-used",
    "title": "Obsolete non-stoppable elements (<blink>, <marquee>) must not be used",
    "description": "Checks that deprecated, non-standard HTML elements whose blinking/scrolling content cannot be paused, stopped, or hidden by the user (<blink>, <marquee>) are not present.",
    "i18n": {
      "titleKey": "deprecatedElements_title",
      "descriptionKey": "deprecatedElements_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag222",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "2.2.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.2.2",
        "title": "Pause, Stop, Hide",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "2.2.2": [
          "deprecated-non-stoppable-elements-absent"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "dialog-name-present",
    "title": "Dialogs have an accessible name",
    "description": "Checks that elements with role=\"dialog\" or role=\"alertdialog\" expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "dialogNamePresent_title",
      "descriptionKey": "dialogNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "structure",
      "atomic",
      "automatic",
      "name",
      "dialog",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "dialog-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "dlitem-parent-valid",
    "title": "Description-list items must be inside a description list",
    "description": "Checks that <dt>/<dd> elements are contained by a <dl>, directly or via one wrapping <div>.",
    "i18n": {
      "titleKey": "dlitemParentValid_title",
      "descriptionKey": "dlitemParentValid_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "automatic",
      "list",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "dlitem-parent-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "duplicate-id-aria",
    "title": "IDs referenced by ARIA must be unique",
    "description": "Checks that any id value referenced by an ARIA ID-reference attribute (aria-labelledby, aria-describedby, aria-owns, aria-controls, aria-activedescendant, aria-flowto, aria-errormessage, aria-details) is unique in the document.",
    "i18n": {
      "titleKey": "duplicateIdAria_title",
      "descriptionKey": "duplicateIdAria_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "aria",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "duplicate-id-aria"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "embed-text-alternative-present",
    "title": "<embed> must provide a text alternative",
    "description": "Checks that <embed> elements provide a text alternative via an accessible name.",
    "i18n": {
      "titleKey": "embed_textAltPresent_title",
      "descriptionKey": "embed_textAltPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "embed",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "embed-text-alternative-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "embed-text-alternative-quality",
    "title": "<embed> text alternative must be appropriate (manual review)",
    "description": "Flags <embed> elements with a detected name for human review of appropriateness.",
    "i18n": {
      "titleKey": "embed_textAltQuality_title",
      "descriptionKey": "embed_textAltQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "embed",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "empty-heading",
    "title": "Headings must not be empty",
    "description": "Checks that heading elements (<h1>-<h6> or role=\"heading\") have a non-empty accessible name.",
    "i18n": {
      "titleKey": "emptyHeading_title",
      "descriptionKey": "emptyHeading_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "headings",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "empty-table-header",
    "title": "Table header cells must not be empty",
    "description": "Checks that table header cells (<th>, or any element with role=\"columnheader\"/\"rowheader\") have visible text content — a header named only via aria-label/aria-labelledby is also flagged, since real screen-reader/browser support for that is inconsistent.",
    "i18n": {
      "titleKey": "emptyTableHeader_title",
      "descriptionKey": "emptyTableHeader_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "tables",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "focus-order-semantics",
    "title": "Elements added to the tab order should have interactive semantics",
    "description": "Flags elements with tabindex >= 0 whose explicit role is a non-interactive structural/document role (e.g. heading, list, region, presentation), for manual review.",
    "i18n": {
      "titleKey": "focusOrderSemantics_title",
      "descriptionKey": "focusOrderSemantics_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "aria",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "form-control-programmatic-label-present",
    "title": "Form controls must have a programmatic label",
    "description": "Checks that form controls have a programmatic label via <label>, aria-label, aria-labelledby, title, or placeholder.",
    "i18n": {
      "titleKey": "formControl_programmaticLabelPresent_title",
      "descriptionKey": "formControl_programmaticLabelPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "wcag332",
      "wcag412",
      "forms",
      "labels",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1",
      "3.3.2",
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "3.3.2",
        "title": "Labels or Instructions",
        "conformanceLevel": "A"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "medium",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "form-control-programmatic-label-present"
        ],
        "3.3.2": [
          "form-control-labels-or-instructions-present"
        ],
        "4.1.2": [
          "form-control-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "form-control-programmatic-label-quality",
    "title": "Form controls should not rely on placeholder or title as the primary label",
    "description": "Flags form controls whose computed accessible name relies on placeholder or title as the primary labeling method. Prefer <label> or aria-labelledby.",
    "i18n": {
      "titleKey": "formControl_programmaticLabelQuality_title",
      "descriptionKey": "formControl_programmaticLabelQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "labels",
      "quality",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "form-control-name-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "form-control-single-label",
    "title": "Form controls must not have multiple labels",
    "description": "Checks that a form control is associated with at most one <label> (by wrapping or by label[for]).",
    "i18n": {
      "titleKey": "formControlSingleLabel_title",
      "descriptionKey": "formControlSingleLabel_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag332",
      "forms",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "3.3.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "3.3.2",
        "title": "Labels or Instructions",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "3.3.2": [
          "form-control-single-label"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "understandable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "heading-order",
    "title": "Heading levels must not skip a level",
    "description": "Checks that heading levels increase by at most one at a time in document order.",
    "i18n": {
      "titleKey": "headingOrder_title",
      "descriptionKey": "headingOrder_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "headings",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "html-lang-attr-present",
    "title": "Page language is declared",
    "description": "Checks that the default language of the page is programmatically declared.",
    "i18n": {
      "titleKey": "html_lang_attr_title",
      "descriptionKey": "html_lang_attr_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag311",
      "structure",
      "language",
      "automatic",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "3.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "3.1.1",
        "title": "Language of Page",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "3.1.1": [
          "html-lang-attr-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "understandable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "html-xml-lang-mismatch",
    "title": "lang and xml:lang must not disagree",
    "description": "Checks that the <html> element's lang and xml:lang attributes declare the same primary language, when both are present.",
    "i18n": {
      "titleKey": "htmlXmlLangMismatch_title",
      "descriptionKey": "htmlXmlLangMismatch_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag311",
      "structure",
      "language",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "3.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "3.1.1",
        "title": "Language of Page",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "3.1.1": [
          "html-xml-lang-mismatch"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "understandable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "identical-links-same-purpose",
    "title": "Links with the same accessible name should lead to the same destination",
    "description": "Flags groups of links that share the same accessible name but resolve to more than one distinct destination, for manual review of whether they serve the same purpose.",
    "i18n": {
      "titleKey": "identicalLinksSamePurpose_title",
      "descriptionKey": "identicalLinksSamePurpose_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2aaa",
      "wcag249",
      "navigation",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "2.4.9"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.9",
        "title": "Link Purpose (Link Only)",
        "conformanceLevel": "AAA"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "2.4.9": [
          "identical-links-same-purpose-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "iframe-focusable-content",
    "title": "Frames with tabindex=\"-1\" must not contain focusable content",
    "description": "Checks that same-origin <iframe>/<frame> elements with tabindex=\"-1\" do not contain focusable content, since browsers do not propagate that restriction into the frame’s embedded document.",
    "i18n": {
      "titleKey": "iframeFocusableContent_title",
      "descriptionKey": "iframeFocusableContent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag211",
      "structure",
      "atomic",
      "automatic",
      "keyboard",
      "iframe",
      "a11ycore"
    ],
    "wcagSc": [
      "2.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.1.1",
        "title": "Keyboard",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "2.1.1": [
          "iframe-tabindex-negative-content-not-focusable"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "iframe-name-present",
    "title": "Frames have an accessible name",
    "description": "Checks that <iframe>/<frame> elements expose a non-empty accessible name via aria-label, aria-labelledby, or the title attribute.",
    "i18n": {
      "titleKey": "iframeNamePresent_title",
      "descriptionKey": "iframeNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "structure",
      "atomic",
      "automatic",
      "name",
      "iframe",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "iframe-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "iframe-title-unique",
    "title": "Frame titles must be unique",
    "description": "Checks that no two <iframe>/<frame> elements in scope share the same title attribute value.",
    "i18n": {
      "titleKey": "iframeTitleUnique_title",
      "descriptionKey": "iframeTitleUnique_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "structure",
      "atomic",
      "automatic",
      "name",
      "iframe",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "iframe-title-unique"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "image-redundant-alt",
    "title": "Image alt text must not duplicate adjacent visible text",
    "description": "Checks that an <img> alt text is not identical to other visible text already present in its immediate parent element.",
    "i18n": {
      "titleKey": "imageRedundantAlt_title",
      "descriptionKey": "imageRedundantAlt_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "images",
      "nontext",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "img-alt-decorative",
    "title": "<img> with alt=\"\" must be decorative (manual review)",
    "description": "Flags <img> elements with empty alt for human review that they are purely decorative.",
    "i18n": {
      "titleKey": "img_altDecorative_title",
      "descriptionKey": "img_altDecorative_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "images",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "img-alt-present",
    "title": "<img> must have an alt attribute",
    "description": "Checks that <img> elements provide an alt attribute to support a text alternative mechanism.",
    "i18n": {
      "titleKey": "img_altPresent_title",
      "descriptionKey": "img_altPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "images",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "img-alt-attr-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "img-alt-quality",
    "title": "<img> alt text must be appropriate (manual review)",
    "description": "Flags <img> elements with non-empty alt text for human review of appropriateness.",
    "i18n": {
      "titleKey": "img_altQuality_title",
      "descriptionKey": "img_altQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "images",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "input-image-alt-decorative",
    "title": "<input type=\"image\"> with alt=\"\" must be appropriate (manual review)",
    "description": "Flags <input type=\"image\"> elements with empty alt for human review (usually not appropriate for functional controls).",
    "i18n": {
      "titleKey": "inputImage_altDecorative_title",
      "descriptionKey": "inputImage_altDecorative_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "forms",
      "images",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "input-image-alt-present",
    "title": "<input type=\"image\"> must have an alt attribute",
    "description": "Checks that <input type=\"image\"> elements provide an alt attribute to support a text alternative mechanism.",
    "i18n": {
      "titleKey": "inputImage_altPresent_title",
      "descriptionKey": "inputImage_altPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "images",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "input-image-alt-attr-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "input-image-alt-quality",
    "title": "<input type=\"image\"> alt text must be appropriate (manual review)",
    "description": "Flags <input type=\"image\"> elements with non-empty alt text for human review of appropriateness.",
    "i18n": {
      "titleKey": "inputImage_altQuality_title",
      "descriptionKey": "inputImage_altQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "forms",
      "images",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "label-in-name",
    "title": "Label in Name: accessible name contains visible text",
    "description": "Checks that when a control has a visible text label, the accessible name (from aria-label/aria-labelledby) contains that visible label text (WCAG 2.5.3).",
    "i18n": {
      "titleKey": "labelInName_title",
      "descriptionKey": "labelInName_description"
    },
    "helpUrl": "https://www.w3.org/WAI/WCAG22/Understanding/label-in-name.html",
    "tags": [
      "wcag21a",
      "wcag253",
      "forms",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "2.5.3"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.5.3",
        "title": "Label in Name",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "2.5.3": [
          "label-in-name"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "label-title-only",
    "title": "Form controls should not use title as their only label",
    "description": "Checks that a form control with a title attribute also has a real label (label element, aria-label, or aria-labelledby).",
    "i18n": {
      "titleKey": "labelTitleOnly_title",
      "descriptionKey": "labelTitleOnly_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "forms",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "understandable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "landmark-banner-is-top-level",
    "title": "Banner landmark must be top-level",
    "description": "Checks that the banner landmark (role=\"banner\" or a non-nested <header>) is not nested inside another landmark region.",
    "i18n": {
      "titleKey": "landmarkBannerIsTopLevel_title",
      "descriptionKey": "landmarkBannerIsTopLevel_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "landmark-contentinfo-is-top-level",
    "title": "Contentinfo landmark must be top-level",
    "description": "Checks that the contentinfo landmark (role=\"contentinfo\" or a non-nested <footer>) is not nested inside another landmark region.",
    "i18n": {
      "titleKey": "landmarkContentinfoIsTopLevel_title",
      "descriptionKey": "landmarkContentinfoIsTopLevel_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "landmark-main-is-top-level",
    "title": "Main landmark must be top-level",
    "description": "Checks that the main landmark (role=\"main\" or <main>) is not nested inside another landmark region.",
    "i18n": {
      "titleKey": "landmarkMainIsTopLevel_title",
      "descriptionKey": "landmarkMainIsTopLevel_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "landmark-no-duplicate-banner",
    "title": "Page must not have more than one banner landmark",
    "description": "Checks that at most one banner landmark (role=\"banner\" or a non-nested <header>) exists on the page.",
    "i18n": {
      "titleKey": "landmarkNoDuplicateBanner_title",
      "descriptionKey": "landmarkNoDuplicateBanner_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "landmark-no-duplicate-contentinfo",
    "title": "Page must not have more than one contentinfo landmark",
    "description": "Checks that at most one contentinfo landmark (role=\"contentinfo\" or a non-nested <footer>) exists on the page.",
    "i18n": {
      "titleKey": "landmarkNoDuplicateContentinfo_title",
      "descriptionKey": "landmarkNoDuplicateContentinfo_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "landmark-no-duplicate-main",
    "title": "Page must not have more than one main landmark",
    "description": "Checks that at most one main landmark (role=\"main\" or <main>) exists on the page.",
    "i18n": {
      "titleKey": "landmarkNoDuplicateMain_title",
      "descriptionKey": "landmarkNoDuplicateMain_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "landmark-one-main",
    "title": "Page should have a main landmark",
    "description": "Checks that the page has at least one main landmark (role=\"main\" or <main>).",
    "i18n": {
      "titleKey": "landmarkOneMain_title",
      "descriptionKey": "landmarkOneMain_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "landmark-unique",
    "title": "Landmarks with the same role must have unique names",
    "description": "Checks that when two or more landmarks share the same role, each has a distinct accessible name.",
    "i18n": {
      "titleKey": "landmarkUnique_title",
      "descriptionKey": "landmarkUnique_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "link-in-text-block",
    "title": "Links in text blocks must be distinguishable from surrounding text without relying on color alone",
    "description": "Checks that a link inside a run of text is visually distinguishable from the surrounding text by underline, a font-weight/style difference, or a sufficient (>=3:1) color-contrast difference — not by color alone.",
    "i18n": {
      "titleKey": "linkInTextBlock_title",
      "descriptionKey": "linkInTextBlock_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag141",
      "navigation",
      "color",
      "links",
      "contrast",
      "atomic",
      "automatic",
      "dom",
      "a11ycore"
    ],
    "wcagSc": [
      "1.4.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.4.1",
        "title": "Use of Color",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.4.1": [
          "link-in-text-block"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "link-name-present",
    "title": "Links have an accessible name",
    "description": "Checks that links expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "linkNamePresent_title",
      "descriptionKey": "linkNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "navigation",
      "atomic",
      "automatic",
      "links",
      "name",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "link-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "link-name-quality",
    "title": "Link text should be descriptive, not generic",
    "description": "Flags links whose full accessible name is a known non-descriptive phrase (e.g. \"click here\", \"read more\", \"more\"), for manual review of whether the purpose is clear without additional context.",
    "i18n": {
      "titleKey": "linkNameQuality_title",
      "descriptionKey": "linkNameQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag244",
      "navigation",
      "quality",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "2.4.4"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.4",
        "title": "Link Purpose (In Context)",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "2.4.4": [
          "link-text-descriptive-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "list-children-valid",
    "title": "Lists must only directly contain list items",
    "description": "Checks that <ul>/<ol> elements only have <li>, <script>, or <template> as direct children.",
    "i18n": {
      "titleKey": "listChildrenValid_title",
      "descriptionKey": "listChildrenValid_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "automatic",
      "list",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "list-children-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "listbox-name-present",
    "title": "Accessible name is present",
    "description": "Checks that elements expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "listboxNamePresent_title",
      "descriptionKey": "listboxNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "name",
      "listbox",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "listbox-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "listitem-parent-valid",
    "title": "List items must be inside a list container",
    "description": "Checks that <li> elements are contained by <ul>, <ol>, or an element with role=\"list\".",
    "i18n": {
      "titleKey": "listitemParentValid_title",
      "descriptionKey": "listitemParentValid_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "automatic",
      "list",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "listitem-parent-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "manual-review",
    "title": "Manual review: keyboard navigation and focus order",
    "description": "Flags that a manual review of keyboard navigation and focus order is required.",
    "i18n": {
      "titleKey": "manualReview_title",
      "descriptionKey": "manualReview_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag2aa",
      "wcag211",
      "wcag243",
      "wcag247",
      "nontext",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "2.1.1",
      "2.4.3",
      "2.4.7"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.1.1",
        "title": "Keyboard",
        "conformanceLevel": "A",
        "url": "https://www.w3.org/TR/WCAG22/#keyboard"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.3",
        "title": "Focus Order",
        "conformanceLevel": "A",
        "url": "https://www.w3.org/TR/WCAG22/#focus-order"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.7",
        "title": "Focus Visible",
        "conformanceLevel": "AA",
        "url": "https://www.w3.org/TR/WCAG22/#focus-visible"
      },
      {
        "standard": "EN 301 549",
        "version": "V3.2.1",
        "requirement": "9.2.1.1",
        "title": "Keyboard"
      },
      {
        "standard": "EN 301 549",
        "version": "V3.2.1",
        "requirement": "9.2.4.3",
        "title": "Focus Order"
      },
      {
        "standard": "EN 301 549",
        "version": "V3.2.1",
        "requirement": "9.2.4.7",
        "title": "Focus Visible"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "type": "Understanding",
        "requirement": "2.1.1",
        "title": "Understanding Keyboard",
        "url": "https://www.w3.org/WAI/WCAG22/Understanding/keyboard.html"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "type": "Understanding",
        "requirement": "2.4.3",
        "title": "Understanding Focus Order",
        "url": "https://www.w3.org/WAI/WCAG22/Understanding/focus-order.html"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "type": "Understanding",
        "requirement": "2.4.7",
        "title": "Understanding Focus Visible",
        "url": "https://www.w3.org/WAI/WCAG22/Understanding/focus-visible.html"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": null,
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "media-alternative-transcript-evidence",
    "title": "Time-based media: transcript / media alternative evidence",
    "description": "Finds <audio>/<video> elements where a transcript or other text alternative is not strongly evidenced in-page. This rule is conservative and returns cantTell when evidence is missing or unverified.",
    "i18n": {
      "titleKey": "mediaTranscriptPresent_title",
      "descriptionKey": "mediaTranscriptPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag121",
      "timebasedmedia",
      "media",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "1.2.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.2.1",
        "title": "Audio-only and Video-only (Prerecorded)",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.2.1": [
          "transcript-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "menuitem-name-present",
    "title": "Menu items have an accessible name",
    "description": "Checks that menu items (role=\"menuitem*\", including checkbox/radio variants) expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "menuitemNamePresent_title",
      "descriptionKey": "menuitemNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "navigation",
      "atomic",
      "automatic",
      "name",
      "menuitem",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "menuitem-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "meta-refresh-no-exceptions",
    "title": "Page must not use a meta refresh at all (AAA)",
    "description": "Checks that <meta http-equiv=\"refresh\"> is not present at all, regardless of delay — the stricter AAA-level counterpart of the A-level positive-delay-only check.",
    "i18n": {
      "titleKey": "metaRefreshNoExceptions_title",
      "descriptionKey": "metaRefreshNoExceptions_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2aaa",
      "wcag224",
      "wcag325",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "2.2.4",
      "3.2.5"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.2.4",
        "title": "Interruptions",
        "conformanceLevel": "AAA"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "3.2.5",
        "title": "Change on Request",
        "conformanceLevel": "AAA"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "2.2.4": [
          "meta-refresh-no-exceptions"
        ],
        "3.2.5": [
          "meta-refresh-no-exceptions"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "meta-refresh-timing-absent",
    "title": "Page must not use a timed meta refresh",
    "description": "Checks that <meta http-equiv=\"refresh\"> does not impose a positive delay of 20 hours or less.",
    "i18n": {
      "titleKey": "metaRefreshTimingAbsent_title",
      "descriptionKey": "metaRefreshTimingAbsent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag221",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "2.2.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.2.1",
        "title": "Timing Adjustable",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "2.2.1": [
          "meta-refresh-timing-absent"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "meta-viewport-large",
    "title": "Viewport meta tag should allow zooming up to 500%",
    "description": "Checks that <meta name=\"viewport\"> does not set user-scalable=no or maximum-scale below 5 (500%).",
    "i18n": {
      "titleKey": "metaViewportLarge_title",
      "descriptionKey": "metaViewportLarge_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "meta-viewport-zoom-enabled",
    "title": "Viewport meta tag must not disable zoom",
    "description": "Checks that <meta name=\"viewport\"> does not set user-scalable=no or maximum-scale below 2 (200%).",
    "i18n": {
      "titleKey": "metaViewportZoomEnabled_title",
      "descriptionKey": "metaViewportZoomEnabled_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2aa",
      "wcag144",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.4.4"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.4.4",
        "title": "Resize Text",
        "conformanceLevel": "AA"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.4.4": [
          "meta-viewport-zoom-enabled"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "meter-name-present",
    "title": "Meters have an accessible name",
    "description": "Checks that elements with role=\"meter\" expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "meterNamePresent_title",
      "descriptionKey": "meterNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "atomic",
      "automatic",
      "name",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "meter-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "mouse-only-event-handlers",
    "title": "Pointer-only inline event handlers should have a keyboard-reachable equivalent",
    "description": "Flags elements with an inline pointer-only event handler (onmouseover, onmouseout, onmousedown, onmouseup, ondblclick, onmousemove, onmouseenter, onmouseleave) and no keyboard-reachable equivalent (onkeydown/onkeyup/onkeypress/onfocus/onblur), for manual review.",
    "i18n": {
      "titleKey": "mouseOnlyEventHandlers_title",
      "descriptionKey": "mouseOnlyEventHandlers_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag211",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "2.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.1.1",
        "title": "Keyboard",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "2.1.1": [
          "mouse-only-event-handlers-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "nested-interactive-controls-absent",
    "title": "Interactive controls must not be nested",
    "description": "Checks that an interactive control (link, button, form control, or ARIA widget role) does not contain another interactive control.",
    "i18n": {
      "titleKey": "nestedInteractiveControlsAbsent_title",
      "descriptionKey": "nestedInteractiveControlsAbsent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "nested-interactive-controls-absent"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "no-autoplay-audio",
    "title": "Autoplaying audio should provide a pause/stop or volume-control mechanism",
    "description": "Flags <audio>/<video> elements that autoplay unmuted with no native controls attribute, for manual review against the 3-second exemption in WCAG 1.4.2.",
    "i18n": {
      "titleKey": "noAutoplayAudio_title",
      "descriptionKey": "noAutoplayAudio_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag142",
      "media",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "1.4.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.4.2",
        "title": "Audio Control",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.4.2": [
          "no-autoplay-audio-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "object-text-alternative-present",
    "title": "<object> must provide a text alternative",
    "description": "Checks that <object> elements provide a text alternative via fallback content or an accessible name.",
    "i18n": {
      "titleKey": "object_textAltPresent_title",
      "descriptionKey": "object_textAltPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "object",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "object-text-alternative-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "object-text-alternative-quality",
    "title": "<object> text alternative must be appropriate (manual review)",
    "description": "Flags <object> elements with detected fallback or name for human review of equivalence and appropriateness.",
    "i18n": {
      "titleKey": "object_textAltQuality_title",
      "descriptionKey": "object_textAltQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "object",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "option-name-present",
    "title": "Accessible name is present",
    "description": "Checks that elements expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "optionNamePresent_title",
      "descriptionKey": "optionNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "name",
      "option",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "option-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "p-as-heading",
    "title": "A <p> styled to look like a heading should probably be a real heading",
    "description": "Flags short <p> elements whose entire text is bold and rendered at >=18px, for manual review of whether a real heading element should be used instead.",
    "i18n": {
      "titleKey": "pAsHeading_title",
      "descriptionKey": "pAsHeading_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "p-as-heading-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "page-has-heading-one",
    "title": "Page should have a level-one heading",
    "description": "Checks that the page has at least one level-one heading (<h1> or role=\"heading\" with aria-level=\"1\").",
    "i18n": {
      "titleKey": "pageHasHeadingOne_title",
      "descriptionKey": "pageHasHeadingOne_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "headings",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "page-title-patterns",
    "title": "Page title patterns that may indicate low descriptiveness",
    "description": "Flags page titles that are likely too generic or templated as review signals (WCAG 2.2 SC 2.4.2). This rule is conservative and does not fail based on patterns alone.",
    "i18n": {
      "titleKey": "pageTitlePatterns_title",
      "descriptionKey": "pageTitlePatterns_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag242",
      "titles",
      "atomic",
      "navigation",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "2.4.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.2",
        "title": "Page Titled",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "2.4.2": [
          "page-title-patterns"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "page-title-present",
    "title": "Page title is present and non-empty",
    "description": "Checks that the document has a non-empty <title> element (WCAG 2.2 SC 2.4.2).",
    "i18n": {
      "titleKey": "pageTitlePresent_title",
      "descriptionKey": "pageTitlePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag242",
      "titles",
      "atomic",
      "navigation",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "2.4.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.4.2",
        "title": "Page Titled",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "2.4.2": [
          "page-title-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "presentation-role-conflict",
    "title": "Presentational role must not conflict with a global ARIA attribute or focusability",
    "description": "Checks that role=\"presentation\"/\"none\" (including an <img alt=\"\"> implicit presentation role) is not combined with a global ARIA attribute (aria-label, aria-hidden, aria-describedby, ...) or focusability (tabindex/native).",
    "i18n": {
      "titleKey": "presentationRoleConflict_title",
      "descriptionKey": "presentationRoleConflict_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "aria",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "progressbar-name-present",
    "title": "Progress bars have an accessible name",
    "description": "Checks that elements with role=\"progressbar\" expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "progressbarNamePresent_title",
      "descriptionKey": "progressbarNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "atomic",
      "automatic",
      "name",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "progressbar-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "region",
    "title": "Page content should be inside a landmark region",
    "description": "Checks that content under <body> is contained within a landmark region.",
    "i18n": {
      "titleKey": "region_title",
      "descriptionKey": "region_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "landmarks",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "role-img-text-alternative-present",
    "title": "[role=\"img\"] must have an accessible text alternative",
    "description": "Checks that elements with role=\"img\" provide an accessible text alternative via aria-label, aria-labelledby, or a title attribute.",
    "i18n": {
      "titleKey": "roleImg_textAlternativePresent_title",
      "descriptionKey": "roleImg_textAlternativePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "images",
      "aria",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "role-img-text-alternative-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "scope-attr-valid",
    "title": "scope attribute must have a valid value",
    "description": "Checks that scope=\"...\" is one of row, col, rowgroup, or colgroup.",
    "i18n": {
      "titleKey": "scopeAttrValid_title",
      "descriptionKey": "scopeAttrValid_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "tables",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "scrollable-region-focusable",
    "title": "Scrollable regions with no focusable content should be keyboard-focusable",
    "description": "Flags elements whose CSS declares overflow:auto/scroll, contain no focusable descendant, and are not themselves keyboard-focusable, for manual review of whether their content actually overflows and needs keyboard scroll access.",
    "i18n": {
      "titleKey": "scrollableRegionFocusable_title",
      "descriptionKey": "scrollableRegionFocusable_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag2aaa",
      "wcag211",
      "wcag213",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "2.1.1",
      "2.1.3"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.1.1",
        "title": "Keyboard",
        "conformanceLevel": "A"
      },
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.1.3",
        "title": "Keyboard (No Exception)",
        "conformanceLevel": "AAA"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "2.1.1": [
          "scrollable-region-focusable-evidence"
        ],
        "2.1.3": [
          "scrollable-region-focusable-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "searchbox-name-present",
    "title": "Accessible name is present",
    "description": "Checks that elements expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "searchboxNamePresent_title",
      "descriptionKey": "searchboxNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "name",
      "searchbox",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "searchbox-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "server-side-image-map-absent",
    "title": "Images must not use a server-side image map",
    "description": "Checks that <img> elements do not carry the ismap attribute (server-side image maps have no keyboard-operable equivalent).",
    "i18n": {
      "titleKey": "serverSideImageMapAbsent_title",
      "descriptionKey": "serverSideImageMapAbsent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag211",
      "structure",
      "atomic",
      "automatic",
      "keyboard",
      "a11ycore"
    ],
    "wcagSc": [
      "2.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.1.1",
        "title": "Keyboard",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "2.1.1": [
          "server-side-image-map-absent"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "skip-link",
    "title": "Skip link must have a resolvable, usable target",
    "description": "Checks that a \"skip to ...\" link's href fragment resolves to a real, currently usable element in the document.",
    "i18n": {
      "titleKey": "skipLink_title",
      "descriptionKey": "skipLink_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "keyboard",
      "navigation",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "slider-name-present",
    "title": "Sliders have an accessible name",
    "description": "Checks that sliders (input[type=\"range\"] and role=\"slider\") expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "sliderNamePresent_title",
      "descriptionKey": "sliderNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "name",
      "slider",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "slider-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "spinbutton-name-present",
    "title": "Accessible name is present",
    "description": "Checks that elements expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "spinbuttonNamePresent_title",
      "descriptionKey": "spinbuttonNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "name",
      "spinbutton",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "spinbutton-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "summary-name-present",
    "title": "Summary elements have an accessible name",
    "description": "Checks that <summary> elements expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "summaryNamePresent_title",
      "descriptionKey": "summaryNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "navigation",
      "atomic",
      "automatic",
      "name",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "summary-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "svg-image-text-alternative-present",
    "title": "SVG <image> must have a text alternative",
    "description": "Checks that SVG <image> elements provide a text alternative via <title>/<desc> or an ARIA accessible name.",
    "i18n": {
      "titleKey": "svgImage_textAltPresent_title",
      "descriptionKey": "svgImage_textAltPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "svg",
      "image",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "medium",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "svg-image-text-alt-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "svg-text-alternative-present",
    "title": "<svg> must provide a text alternative",
    "description": "Checks that inline <svg> elements provide a text alternative via a <title> element or an ARIA name (a <desc> element alone does not count — it only contributes to the accessible description, not the name).",
    "i18n": {
      "titleKey": "svg_textAltPresent_title",
      "descriptionKey": "svg_textAltPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "svg",
      "nontext",
      "images",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "svg-text-alternative-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "svg-text-alternative-quality",
    "title": "<svg> text alternative must be appropriate (manual review)",
    "description": "Flags applicable <svg> graphics with a detected text alternative for human review of appropriateness.",
    "i18n": {
      "titleKey": "svg_textAltQuality_title",
      "descriptionKey": "svg_textAltQuality_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "nontext",
      "svg",
      "manual",
      "atomic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "text-alternative-quality"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "tab-name-present",
    "title": "Tabs have an accessible name",
    "description": "Checks that elements with role=\"tab\" expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "tabNamePresent_title",
      "descriptionKey": "tabNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "navigation",
      "atomic",
      "automatic",
      "name",
      "tab",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "tab-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "tabindex",
    "title": "tabindex should not be greater than 0",
    "description": "Checks that tabindex values are 0 or negative, not a positive number.",
    "i18n": {
      "titleKey": "tabindex_title",
      "descriptionKey": "tabindex_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "keyboard",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "table-duplicate-name",
    "title": "Table caption must not duplicate its summary attribute",
    "description": "Checks that a <table>'s <caption> text is not identical to its (deprecated) summary attribute.",
    "i18n": {
      "titleKey": "tableDuplicateName_title",
      "descriptionKey": "tableDuplicateName_description"
    },
    "helpUrl": "",
    "tags": [
      "best-practice",
      "tables",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [],
    "normativeMappings": [],
    "defaultSeverity": "minor",
    "defaultConfidence": "medium",
    "type": "manual",
    "coverage": {},
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "table-fake-caption",
    "title": "A table's first row should not stand in for a real <caption>",
    "description": "Flags tables with no <caption> whose first row has a single non-empty cell while other rows have multiple cells, for manual review of whether that cell is acting as a fake caption.",
    "i18n": {
      "titleKey": "tableFakeCaption_title",
      "descriptionKey": "tableFakeCaption_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "minor",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "table-fake-caption-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "table-headers-attr-valid",
    "title": "Table cell \"headers\" attribute must reference valid header cells",
    "description": "Checks that each id in a <td>/<th> headers attribute resolves to a <th> element within the same table (not missing, not a non-th element, not itself).",
    "i18n": {
      "titleKey": "tableHeadersAttrValid_title",
      "descriptionKey": "tableHeadersAttrValid_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "automatic",
      "table",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "table-headers-attr-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "table-th-has-data-cells",
    "title": "<th> elements must describe at least one data cell",
    "description": "Checks that a table containing <th> elements also contains at least one <td> data cell for those headers to describe.",
    "i18n": {
      "titleKey": "tableThHasDataCells_title",
      "descriptionKey": "tableThHasDataCells_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "automatic",
      "table",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "table-th-has-data-cells"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "target-size-minimum",
    "title": "Pointer targets meet minimum size (AA)",
    "description": "Checks that pointer-operable targets have an effective hit region of at least 24 by 24 CSS pixels, or meet an allowed exception (e.g. sufficient spacing).",
    "i18n": {
      "titleKey": "targetSizeMinimum_title",
      "descriptionKey": "targetSizeMinimum_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag22aa",
      "wcag258",
      "navigation",
      "operable",
      "pointer",
      "target-size",
      "atomic",
      "automatic",
      "dom",
      "a11ycore"
    ],
    "wcagSc": [
      "2.5.8"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "2.5.8",
        "title": "Target Size (Minimum)",
        "conformanceLevel": "AA"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "medium",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "2.5.8": [
          "target-size-minimum-pointer"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "operable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "td-has-header",
    "title": "Data cells in large tables must have an associated header",
    "description": "Checks that every <td> in a large, simple (no colspan/rowspan) table has an associated header — via a headers attribute, an implicit column <th> above it, or an implicit row <th> to its left.",
    "i18n": {
      "titleKey": "tdHasHeader_title",
      "descriptionKey": "tdHasHeader_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag131",
      "structure",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.3.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.3.1",
        "title": "Info and Relationships",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.3.1": [
          "td-has-header"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "textbox-name-present",
    "title": "Accessible name is present",
    "description": "Checks that elements expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "textboxNamePresent_title",
      "descriptionKey": "textboxNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "forms",
      "atomic",
      "automatic",
      "name",
      "textbox",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "textbox-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "tooltip-name-present",
    "title": "Tooltips have an accessible name",
    "description": "Checks that elements with role=\"tooltip\" expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "tooltipNamePresent_title",
      "descriptionKey": "tooltipNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "navigation",
      "atomic",
      "automatic",
      "name",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "tooltip-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "treeitem-name-present",
    "title": "Accessible name is present",
    "description": "Checks that elements expose a non-empty accessible name.",
    "i18n": {
      "titleKey": "treeitemNamePresent_title",
      "descriptionKey": "treeitemNamePresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag412",
      "navigation",
      "atomic",
      "automatic",
      "name",
      "treeitem",
      "a11ycore"
    ],
    "wcagSc": [
      "4.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "4.1.2",
        "title": "Name, Role, Value",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "4.1.2": [
          "treeitem-name-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "robust",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "valid-lang",
    "title": "Element lang attribute must be syntactically valid",
    "description": "Checks that any element (other than the root <html>) with a non-empty lang attribute uses a syntactically valid language tag.",
    "i18n": {
      "titleKey": "validLang_title",
      "descriptionKey": "validLang_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2aa",
      "wcag312",
      "structure",
      "language",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "3.1.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "3.1.2",
        "title": "Language of Parts",
        "conformanceLevel": "AA"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "high",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "3.1.2": [
          "element-lang-valid"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "understandable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "video-caption",
    "title": "Prerecorded video should provide a captions track",
    "description": "Flags <video> elements with no <track kind=\"captions\"|\"subtitles\"> child, for manual review of whether the video has an audio track that needs captions.",
    "i18n": {
      "titleKey": "videoCaption_title",
      "descriptionKey": "videoCaption_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag122",
      "media",
      "timebasedmedia",
      "atomic",
      "manual",
      "a11ycore"
    ],
    "wcagSc": [
      "1.2.2"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.2.2",
        "title": "Captions (Prerecorded)",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "moderate",
    "defaultConfidence": "low",
    "type": "manual",
    "coverage": {
      "facetsBySc": {
        "1.2.2": [
          "video-captions-track-evidence"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  },
  {
    "ruleId": "video-poster-text-alternative-present",
    "title": "<video> poster must have a text alternative",
    "description": "Checks that <video> elements with a poster image provide a text alternative (accessible name).",
    "i18n": {
      "titleKey": "videoPoster_textAltPresent_title",
      "descriptionKey": "videoPoster_textAltPresent_description"
    },
    "helpUrl": "",
    "tags": [
      "wcag2a",
      "wcag111",
      "media",
      "video",
      "atomic",
      "automatic",
      "a11ycore"
    ],
    "wcagSc": [
      "1.1.1"
    ],
    "normativeMappings": [
      {
        "standard": "WCAG",
        "version": "2.2",
        "requirement": "1.1.1",
        "title": "Non-text Content",
        "conformanceLevel": "A"
      }
    ],
    "defaultSeverity": "serious",
    "defaultConfidence": "medium",
    "type": "automatic",
    "coverage": {
      "facetsBySc": {
        "1.1.1": [
          "video-poster-text-alt-present"
        ]
      }
    },
    "data": null,
    "ruleInterfaceVersion": "1.0.0",
    "ruleVersion": "0.0.0",
    "normative": true,
    "atomic": true,
    "deprecated": false,
    "deprecation": null,
    "category": "perceivable",
    "standard": null,
    "applicability": "",
    "expectation": "",
    "references": [],
    "requirements": null,
    "mappings": null
  }
];

  // Tests catalog (alias of CHECK_DEFS; tests are the atomic executable units)
  const TEST_DEFS = CHECK_DEFS;

  // Composite rules catalog (data only)
  const COMPOSITE_RULES = [
  {
    "id": "wcag-1.1.1-non-text-content",
    "checksIds": [
      "area-alt-present",
      "img-alt-decorative",
      "img-alt-quality",
      "area-alt-decorative",
      "area-alt-quality",
      "canvas-text-alternative-quality",
      "canvas-text-alternative-present",
      "embed-text-alternative-present",
      "img-alt-present",
      "input-image-alt-present",
      "object-text-alternative-present",
      "role-img-text-alternative-present",
      "svg-image-text-alternative-present",
      "svg-text-alternative-present",
      "video-poster-text-alternative-present",
      "embed-text-alternative-quality",
      "input-image-alt-decorative",
      "input-image-alt-quality",
      "object-text-alternative-quality",
      "svg-text-alternative-quality",
      "meter-name-present",
      "progressbar-name-present"
    ],
    "meta": {
      "title": "Non-text content: text alternatives",
      "description": "Rollup of checks ensuring non-text content has an appropriate text alternative.",
      "wcagSc": [
        "1.1.1"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-1.2.1-audio-only-video-only-prerecorded",
    "checksIds": [
      "media-alternative-transcript-evidence"
    ],
    "meta": {
      "title": "Audio-only and video-only (prerecorded): transcript",
      "description": "Rollup of checks for transcript availability for prerecorded audio-only/video-only media.",
      "wcagSc": [
        "1.2.1"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-1.2.2-captions-prerecorded",
    "checksIds": [
      "video-caption"
    ],
    "meta": {
      "title": "Captions (Prerecorded)",
      "description": "Rollup of checks for captions-track evidence on prerecorded video.",
      "wcagSc": [
        "1.2.2"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-1.3.1-info-and-relationships",
    "checksIds": [
      "table-headers-attr-valid",
      "table-th-has-data-cells",
      "aria-hidden-body",
      "list-children-valid",
      "listitem-parent-valid",
      "definition-list-children-valid",
      "dlitem-parent-valid",
      "form-control-programmatic-label-present",
      "p-as-heading",
      "table-fake-caption",
      "td-has-header"
    ],
    "meta": {
      "title": "Info and Relationships",
      "description": "Rollup of checks ensuring information, structure, and relationships conveyed through presentation are programmatically determinable.",
      "wcagSc": [
        "1.3.1"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-1.4.1-use-of-color",
    "checksIds": [
      "link-in-text-block"
    ],
    "meta": {
      "title": "Use of Color",
      "description": "Rollup of checks ensuring color is not used as the only visual means of conveying information.",
      "wcagSc": [
        "1.4.1"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-1.4.2-audio-control",
    "checksIds": [
      "no-autoplay-audio"
    ],
    "meta": {
      "title": "Audio Control",
      "description": "Rollup of checks for a pause/stop or volume-control mechanism on autoplaying audio.",
      "wcagSc": [
        "1.4.2"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-1.4.3-contrast-minimum",
    "checksIds": [
      "contrast-computable",
      "contrast-minimum"
    ],
    "meta": {
      "title": "Contrast: minimum",
      "description": "Rollup of checks for minimum text contrast.",
      "wcagSc": [
        "1.4.3"
      ],
      "level": "AA"
    }
  },
  {
    "id": "wcag-1.4.6-contrast-enhanced",
    "checksIds": [
      "contrast-computable",
      "contrast-enhanced"
    ],
    "meta": {
      "title": "Contrast: enhanced",
      "description": "Rollup of checks for enhanced text contrast.",
      "wcagSc": [
        "1.4.6"
      ],
      "level": "AAA"
    }
  },
  {
    "id": "wcag-2.1.1-keyboard",
    "checksIds": [
      "manual-review",
      "iframe-focusable-content",
      "server-side-image-map-absent",
      "scrollable-region-focusable",
      "mouse-only-event-handlers"
    ],
    "meta": {
      "title": "Keyboard",
      "description": "Rollup of checks ensuring functionality is operable through a keyboard interface.",
      "wcagSc": [
        "2.1.1"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-2.1.3-keyboard-no-exception",
    "checksIds": [
      "scrollable-region-focusable"
    ],
    "meta": {
      "title": "Keyboard (No Exception)",
      "description": "Rollup of checks ensuring functionality is operable through a keyboard interface with no exceptions (AAA).",
      "wcagSc": [
        "2.1.3"
      ],
      "level": "AAA"
    }
  },
  {
    "id": "wcag-2.2.2-pause-stop-hide",
    "checksIds": [
      "deprecated-elements-not-used"
    ],
    "meta": {
      "title": "Pause, Stop, Hide",
      "description": "Rollup of checks ensuring moving, blinking, or auto-scrolling content can be paused, stopped, or hidden.",
      "wcagSc": [
        "2.2.2"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-2.4.1-bypass-blocks",
    "checksIds": [
      "bypass-blocks-present"
    ],
    "meta": {
      "title": "Bypass Blocks",
      "description": "Rollup of checks ensuring the page provides a way to bypass repeated blocks of content.",
      "wcagSc": [
        "2.4.1"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-2.4.2-page-titled",
    "checksIds": [
      "page-title-patterns",
      "page-title-present"
    ],
    "meta": {
      "title": "Page titled",
      "description": "Rollup of checks ensuring documents have a meaningful page title.",
      "wcagSc": [
        "2.4.2"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-2.4.3-focus-order",
    "checksIds": [
      "manual-review"
    ],
    "meta": {
      "title": "Focus order",
      "description": "Rollup of checks ensuring focus moves through content in a meaningful order.",
      "wcagSc": [
        "2.4.3"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-2.4.4-link-purpose-in-context",
    "checksIds": [
      "link-name-quality"
    ],
    "meta": {
      "title": "Link Purpose (In Context)",
      "description": "Rollup of checks flagging links whose text alone is a known non-descriptive/generic phrase.",
      "wcagSc": [
        "2.4.4"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-2.4.7-focus-visible",
    "checksIds": [
      "aria-hidden-focus",
      "css-hidden-focus",
      "manual-review"
    ],
    "meta": {
      "title": "Focus visible",
      "description": "Rollup of checks ensuring keyboard focus is not hidden and remains perceivable.",
      "wcagSc": [
        "2.4.7"
      ],
      "level": "AA"
    }
  },
  {
    "id": "wcag-2.4.9-link-purpose-link-only",
    "checksIds": [
      "identical-links-same-purpose"
    ],
    "meta": {
      "title": "Link Purpose (Link Only)",
      "description": "Rollup of checks ensuring links with the same accessible name serve the same purpose (AAA).",
      "wcagSc": [
        "2.4.9"
      ],
      "level": "AAA"
    }
  },
  {
    "id": "wcag-2.5.3-label-in-name",
    "checksIds": [
      "label-in-name"
    ],
    "meta": {
      "title": "Label in name",
      "description": "Rollup of checks ensuring that when a control has a visible text label, the accessible name contains that visible label text.",
      "wcagSc": [
        "2.5.3"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-2.5.8-target-size-minimum",
    "checksIds": [
      "target-size-minimum"
    ],
    "meta": {
      "title": "Target size: minimum",
      "description": "Rollup of checks ensuring pointer targets meet minimum size requirements.",
      "wcagSc": [
        "2.5.8"
      ],
      "level": "AA"
    }
  },
  {
    "id": "wcag-3.1.1-language-of-page",
    "checksIds": [
      "html-lang-attr-present",
      "html-xml-lang-mismatch"
    ],
    "meta": {
      "title": "Language of page",
      "description": "Rollup of checks ensuring the page language is specified.",
      "wcagSc": [
        "3.1.1"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-3.1.2-language-of-parts",
    "checksIds": [
      "valid-lang"
    ],
    "meta": {
      "title": "Language of Parts",
      "description": "Rollup of checks ensuring elements whose language differs from the page default declare it correctly.",
      "wcagSc": [
        "3.1.2"
      ],
      "level": "AA"
    }
  },
  {
    "id": "wcag-1.3.4-orientation",
    "checksIds": [
      "css-orientation-lock"
    ],
    "meta": {
      "title": "Orientation",
      "description": "Rollup of checks ensuring content does not restrict its view to a single display orientation.",
      "wcagSc": [
        "1.3.4"
      ],
      "level": "AA"
    }
  },
  {
    "id": "wcag-1.3.5-identify-input-purpose",
    "checksIds": [
      "autocomplete-valid"
    ],
    "meta": {
      "title": "Identify Input Purpose",
      "description": "Rollup of checks ensuring the autocomplete attribute correctly identifies input purpose.",
      "wcagSc": [
        "1.3.5"
      ],
      "level": "AA"
    }
  },
  {
    "id": "wcag-1.4.12-text-spacing",
    "checksIds": [
      "avoid-inline-spacing"
    ],
    "meta": {
      "title": "Text Spacing",
      "description": "Rollup of checks ensuring inline styles do not block user text-spacing overrides.",
      "wcagSc": [
        "1.4.12"
      ],
      "level": "AA"
    }
  },
  {
    "id": "wcag-2.2.4-interruptions",
    "checksIds": [
      "meta-refresh-no-exceptions"
    ],
    "meta": {
      "title": "Interruptions",
      "description": "Rollup of checks ensuring automatic context changes only happen at the user's request (AAA).",
      "wcagSc": [
        "2.2.4"
      ],
      "level": "AAA"
    }
  },
  {
    "id": "wcag-3.2.5-change-on-request",
    "checksIds": [
      "meta-refresh-no-exceptions"
    ],
    "meta": {
      "title": "Change on Request",
      "description": "Rollup of checks ensuring context changes only happen at the user's request (AAA).",
      "wcagSc": [
        "3.2.5"
      ],
      "level": "AAA"
    }
  },
  {
    "id": "wcag-4.1.2-name",
    "checksIds": [
      "aria-role-name-present",
      "binary-control-name-present",
      "button-name-present",
      "combobox-name-present",
      "dialog-name-present",
      "form-control-programmatic-label-present",
      "iframe-name-present",
      "iframe-title-unique",
      "link-name-present",
      "listbox-name-present",
      "menuitem-name-present",
      "option-name-present",
      "searchbox-name-present",
      "slider-name-present",
      "spinbutton-name-present",
      "tab-name-present",
      "textbox-name-present",
      "treeitem-name-present",
      "aria-hidden-focus",
      "aria-hidden-body",
      "form-control-programmatic-label-quality",
      "summary-name-present",
      "tooltip-name-present"
    ],
    "meta": {
      "title": "Name, role, value: accessible name",
      "description": "Rollup of checks that common interactive elements expose a non-empty accessible name.",
      "wcagSc": [
        "4.1.2"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-4.1.2-aria-validity",
    "checksIds": [
      "aria-roles-valid",
      "aria-deprecated-role",
      "aria-valid-attr",
      "aria-valid-attr-value",
      "aria-allowed-attr",
      "aria-prohibited-attr",
      "aria-required-attr",
      "aria-allowed-role",
      "aria-required-children",
      "aria-prohibited-children",
      "aria-required-parent",
      "duplicate-id-aria",
      "nested-interactive-controls-absent",
      "aria-braille-equivalent",
      "aria-conditional-attr",
      "aria-checked-state-mismatch"
    ],
    "meta": {
      "title": "Name, role, value: ARIA validity",
      "description": "Rollup of checks that ARIA role and attribute usage conforms to the WAI-ARIA specification (valid roles, valid attributes, valid values, required attributes/relationships, unique ARIA-referenced ids).",
      "wcagSc": [
        "4.1.2"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-1.4.4-resize-text",
    "checksIds": [
      "meta-viewport-zoom-enabled"
    ],
    "meta": {
      "title": "Resize Text",
      "description": "Rollup of checks ensuring the viewport meta tag does not prevent users from zooming text up to 200%.",
      "wcagSc": [
        "1.4.4"
      ],
      "level": "AA"
    }
  },
  {
    "id": "wcag-2.2.1-timing-adjustable",
    "checksIds": [
      "meta-refresh-timing-absent"
    ],
    "meta": {
      "title": "Timing Adjustable",
      "description": "Rollup of checks ensuring the page does not impose a timed refresh the user cannot control.",
      "wcagSc": [
        "2.2.1"
      ],
      "level": "A"
    }
  },
  {
    "id": "wcag-3.3.2-labels-or-instructions",
    "checksIds": [
      "form-control-single-label",
      "form-control-programmatic-label-present"
    ],
    "meta": {
      "title": "Labels or Instructions",
      "description": "Rollup of checks ensuring form controls have unambiguous labeling.",
      "wcagSc": [
        "3.3.2"
      ],
      "level": "A"
    }
  }
];

  const RULE_IMPLS = {
    "accesskeys": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[accesskey]')
    : helpers.queryAll('[accesskey]');

  const groups = new Map(); // normalized key -> elements[]
  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const raw = String(el.getAttribute('accesskey') || '').trim();
    if (!raw) continue;
    const key = raw.toLowerCase();
    const list = groups.get(key) || [];
    list.push(el);
    groups.set(key, list);
  }

  const occurrences = [];
  for (const [key, els] of groups) {
    if (els.length <= 1) continue;
    for (const el of els) {
      const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';

      occurrences.push({
        selector: stableSelector,
        html,
        summary: "This element's accesskey is shared with another element on the page.",
        hint: 'Make each accesskey value unique across the page.',
        i18n: {
          summaryKey: 'accesskeys_summary_cantTell',
          hintKey: 'accesskeys_hint_cantTell',
          params: { accesskey: key, duplicateCount: String(els.length) }
        },
        data: {
          details: { reasonCode: 'ACCESSKEY_DUPLICATE', accesskey: key, duplicateCount: els.length }
        }
      });
    }
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "area-alt-decorative": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const __accEligCache = new WeakMap();
  function accEligibleCached(node) {
    if (!isAccTreeEligible) return { eligible: true, reasons: [] };
    if (!node || typeof node !== 'object') return { eligible: true, reasons: [] };
    const cached = __accEligCache.get(node);
    if (cached) return cached;
    let res;
    try {
      res = isAccTreeEligible(node, ctx);
    } catch {
      res = { eligible: true, reasons: [] };
    }
    res = res && typeof res === 'object' ? res : { eligible: !!res, reasons: [] };
    __accEligCache.set(node, res);
    return res;
  }

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  // --- image-map semantics (rule-local; match automatic <area> applicability) ---
  function normUsemap(val) {
    try {
      const t = String(val || '').trim();
      if (!t) return '';
      return t[0] === '#' ? t.slice(1).trim().toLowerCase() : t.toLowerCase();
    } catch {
      return '';
    }
  }

  function getMapName(mapEl) {
    try {
      if (!mapEl || !mapEl.getAttribute) return '';
      const n = String(mapEl.getAttribute('name') || mapEl.getAttribute('id') || '').trim();
      return n ? n.toLowerCase() : '';
    } catch {
      return '';
    }
  }

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  const els = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('area') : queryAll('area')) || []);
    } catch {
      return queryAll('area');
    }
  })();

  const __usemapIndex = new Map(); // mapName -> img (first in document order)
  try {
    const imgs = Array.from(document.querySelectorAll('img[usemap]'));
    for (const img of imgs) {
      const u = normUsemap(img.getAttribute('usemap'));
      if (!u) continue;
      if (!__usemapIndex.has(u)) __usemapIndex.set(u, img); // keep first match only
    }
  } catch {}

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    // Must belong to a *used* image map (referenced by an <img usemap>). If unused, not applicable.
    let img;
    try {
      const map = el.closest && el.closest('map');
      const mapName = map ? getMapName(map) : '';
      img = mapName ? __usemapIndex.get(mapName) || null : null;
    } catch {
      img = null;
    }
    if (!img) continue;

    // The referencing <img> must be eligible in the accessibility tree.
    if (isAccTreeEligible) {
      const imgElig = accEligibleCached(img);
      if (imgElig && imgElig.eligible === false) continue;
    }

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    // Rule-specific applicability (only elements that already have a text alternative mechanism)
    let alt;
    try {
      alt = el.getAttribute('alt');
    } catch {
      alt = null;
    }
    if (alt === null) continue;
    if (String(alt).trim() !== '') continue; // only alt=""

    applicableCount += 1;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Review whether <area> is decorative (alt="").',
      hint: 'Confirm the area does not convey information or function. If it is interactive or meaningful, provide meaningful alt text.',
      i18n: {
        summaryKey: 'area_altDecorative_summary_cantTell',
        hintKey: 'area_altDecorative_hint_cantTell',
        params: { element: (el.tagName || '').toLowerCase() }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: null
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "area-alt-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;

  // --- image-map semantics (rule-local) ---

  function normUsemap(val) {
    try {
      const s = String(val || '').trim();
      if (!s) return '';
      return s[0] === '#' ? s.slice(1).trim().toLowerCase() : s.toLowerCase();
    } catch {
      return '';
    }
  }

  function getMapName(mapEl) {
    try {
      if (!mapEl || !mapEl.getAttribute) return '';
      const n = String(mapEl.getAttribute('name') || mapEl.getAttribute('id') || '').trim();
      return n ? n.toLowerCase() : '';
    } catch {
      return '';
    }
  }

  // Cache mapName -> first referencing <img> in document order (deterministic)
  const __usemapIndex = (() => {
    const idx = new Map();
    try {
      const imgs =
        document && document.querySelectorAll ? document.querySelectorAll('img[usemap]') : [];
      for (const img of imgs) {
        if (!img || !img.getAttribute) continue;
        const u = normUsemap(img.getAttribute('usemap'));
        if (!u) continue;
        if (!idx.has(u)) idx.set(u, img); // first in document order wins
      }
    } catch {
      // ignore
    }
    return idx;
  })();

  function getReferencingImgForArea(areaEl) {
    try {
      if (!areaEl || !areaEl.closest) return null;
      const map = areaEl.closest('map');
      if (!map) return null;

      const mapName = getMapName(map);
      if (!mapName) return null;

      return __usemapIndex.get(mapName) || null;
    } catch {}
    return null;
  }

  const areas = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('area') : queryAll('area')) || []);
    } catch {
      return queryAll('area');
    }
  })();

  if (!areas.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of areas) {
    if (!el || !el.getAttribute) continue;

    // 0) Must belong to a *used* image map: an <img usemap> must reference its <map>.
    // If not used, <area> is not applicable (matches your observed focus behavior).
    const img = getReferencingImgForArea(el);
    if (!img) continue;

    // 1) The referencing <img> must itself be eligible in the acc tree.
    // This is the "visibility of map/area doesn't matter; the image does" policy.
    if (isAccTreeEligible) {
      const imgElig = (() => {
        try {
          return isAccTreeEligible(img, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (imgElig && imgElig.eligible === false) continue;
    }

    // 2) The <area> itself must be eligible (aria-hidden/inert exceptions handled by helper).
    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    // From here: applicable
    applicableCount += 1;

    const hasAlt = el.getAttribute('alt') !== null;
    if (hasAlt) continue;

    // aria-label / aria-labelledby is also a valid, standards-recognized
    // text-alternative mechanism for <area> (HTML-AAM accessible name
    // computation includes ARIA naming before falling back to alt).
    if (getAriaNameInfo) {
      let ariaName;
      try {
        ariaName = getAriaNameInfo(el, ctx);
      } catch {
        ariaName = null;
      }
      if (ariaName && ariaName.present) continue;
    }

    // A non-empty title attribute is HTML-AAM's own next fallback naming
    // source once alt is entirely absent. Same gap img-alt-present handles
    // for <img title="..."> with no alt.
    const titleRaw = (() => {
      try {
        return el.getAttribute('title');
      } catch {
        return null;
      }
    })();
    if (titleRaw !== null && String(titleRaw).trim()) continue;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      // Leave selector/html empty so the engine can fill them from __node.
      selector: '',
      html: '',
      summary: 'Missing alt attribute on &lt;area&gt;.',
      hint: 'Add an alt attribute (use alt="" only for decorative areas).',
      i18n: {
        summaryKey: 'area_altPresent_summary_fail',
        hintKey: 'area_altPresent_hint_fail',
        params: { element: 'area' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      // Never compute selector/snippet in the rule.
      occurrences.push({ ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "area-alt-quality": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const __accEligCache = new WeakMap();
  function accEligibleCached(node) {
    if (!isAccTreeEligible) return { eligible: true, reasons: [] };
    if (!node || typeof node !== 'object') return { eligible: true, reasons: [] };
    const c = __accEligCache.get(node);
    if (c) return c;
    let r;
    try {
      r = isAccTreeEligible(node, ctx);
    } catch {
      r = { eligible: true, reasons: [] };
    }
    r = r && typeof r === 'object' ? r : { eligible: !!r, reasons: [] };
    __accEligCache.set(node, r);
    return r;
  }

  // --- image-map semantics (rule-local; match automatic <area> applicability) ---
  function normUsemap(val) {
    try {
      const t = String(val || '').trim();
      if (!t) return '';
      return t[0] === '#' ? t.slice(1).trim().toLowerCase() : t.toLowerCase();
    } catch {
      return '';
    }
  }
  function getMapName(mapEl) {
    try {
      if (!mapEl || !mapEl.getAttribute) return '';
      const n = String(mapEl.getAttribute('name') || mapEl.getAttribute('id') || '').trim();
      return n ? n.toLowerCase() : '';
    } catch {
      return '';
    }
  }

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  const els = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('area') : queryAll('area')) || []);
    } catch {
      return queryAll('area');
    }
  })();

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  const __usemapIndex = new Map(); // mapName -> img (first in document order)
  try {
    const imgs = Array.from(document.querySelectorAll('img[usemap]'));
    for (const img of imgs) {
      const u = normUsemap(img.getAttribute('usemap'));
      if (!u) continue;
      if (!__usemapIndex.has(u)) __usemapIndex.set(u, img);
    }
  } catch {}

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    // Must belong to a *used* image map (referenced by an <img usemap>). If unused, not applicable.
    let img;
    try {
      const map = el.closest && el.closest('map');
      const mapName = map ? getMapName(map) : '';
      img = mapName ? __usemapIndex.get(mapName) || null : null;
    } catch {
      img = null;
    }
    if (!img) continue;

    // The referencing <img> must be eligible in the accessibility tree.
    if (isAccTreeEligible) {
      const imgElig = accEligibleCached(img);
      if (imgElig && imgElig.eligible === false) continue;
    }

    if (isAccTreeEligible) {
      const elig = accEligibleCached(el);
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    // Rule-specific applicability (only elements that already have a text alternative mechanism)
    let alt;
    try {
      alt = el.getAttribute('alt');
    } catch {
      alt = null;
    }
    if (alt === null) continue;
    if (String(alt).trim() === '') continue; // only non-empty alt is applicable here

    applicableCount += 1;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    let altVal;
    try {
      altVal = String(el.getAttribute('alt') || '');
    } catch {
      altVal = '';
    }

    const baseOccurrence = {
      summary: 'Review alt text on <area> for accuracy and appropriateness.',
      hint: 'Ensure the alt text identifies the destination/action of the image map area in context.',
      i18n: {
        summaryKey: 'area_altQuality_summary_cantTell',
        hintKey: 'area_altQuality_hint_cantTell',
        params: { element: (el.tagName || '').toLowerCase() }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: { alt: altVal.trim() } // optional but useful for manual review
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "aria-allowed-attr": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  // Global ARIA states/properties supported on (almost) any element,
  // regardless of role, per the WAI-ARIA "Global States and Properties" list.
  // Declared inside runInPage (rather than at module scope) because the
  // build inlines only this function's own source text — see
  // scripts/build-core.js header ("runInPage MUST be self-contained").
  // <generated:aria-global-attrs>
  const GLOBAL_ATTRS = [
    'aria-atomic',
    'aria-braillelabel',
    'aria-brailleroledescription',
    'aria-busy',
    'aria-controls',
    'aria-current',
    'aria-describedby',
    'aria-description',
    'aria-details',
    'aria-dropeffect',
    'aria-flowto',
    'aria-grabbed',
    'aria-hidden',
    'aria-keyshortcuts',
    'aria-label',
    'aria-labelledby',
    'aria-live',
    'aria-owns',
    'aria-relevant',
    'aria-roledescription'
  ];
  // </generated:aria-global-attrs>

  // Per-role supported (non-global) states/properties. Deliberately
  // conservative — see src/core/aria-helpers.js file header for the same
  // confidence-scoping rationale; only well-established, unambiguous
  // role/attribute pairings from the WAI-ARIA role definitions are listed.
  // <generated:aria-implicit-roles>
  const IMPLICIT_ROLE_BY_ELEMENT = {
    article: 'article',
    blockquote: 'blockquote',
    button: 'button',
    caption: 'caption',
    code: 'code',
    dd: 'definition',
    del: 'deletion',
    details: 'group',
    dfn: 'term',
    dialog: 'dialog',
    dt: 'term',
    em: 'emphasis',
    fieldset: 'group',
    figure: 'figure',
    h1: 'heading',
    h2: 'heading',
    h3: 'heading',
    h4: 'heading',
    h5: 'heading',
    h6: 'heading',
    hr: 'separator',
    ins: 'insertion',
    main: 'main',
    mark: 'mark',
    menu: 'list',
    meter: 'meter',
    nav: 'navigation',
    ol: 'list',
    optgroup: 'group',
    option: 'option',
    output: 'status',
    p: 'paragraph',
    progress: 'progressbar',
    strong: 'strong',
    sub: 'subscript',
    sup: 'superscript',
    textarea: 'textbox',
    time: 'time',
    ul: 'list',
    'input[type=text]': 'textbox',
    'input[type=tel]': 'textbox',
    'input[type=url]': 'textbox',
    'input[type=email]': 'textbox',
    'input[type=password]': 'textbox',
    'input[type=search]': 'searchbox',
    'input[type=number]': 'spinbutton',
    'input[type=range]': 'slider',
    'input[type=checkbox]': 'checkbox',
    'input[type=radio]': 'radio',
    'input[type=button]': 'button',
    'input[type=submit]': 'button',
    'input[type=reset]': 'button',
    'input[type=image]': 'button'
  };
  const NON_GLOBAL_ARIA_ATTR_SELECTOR =
    '[aria-activedescendant], [aria-autocomplete], [aria-checked], [aria-colcount], [aria-colindex], [aria-colspan], [aria-disabled], [aria-errormessage], [aria-expanded], [aria-haspopup], [aria-invalid], [aria-level], [aria-modal], [aria-multiline], [aria-multiselectable], [aria-orientation], [aria-placeholder], [aria-posinset], [aria-pressed], [aria-readonly], [aria-required], [aria-rowcount], [aria-rowindex], [aria-rowspan], [aria-selected], [aria-setsize], [aria-sort], [aria-valuemax], [aria-valuemin], [aria-valuenow], [aria-valuetext]';
  // </generated:aria-implicit-roles>

  // <generated:aria-role-attrs>
  const SUPPORTED_ATTRS_BY_ROLE = {
    alert: [],
    alertdialog: ['aria-modal'],
    application: [
      'aria-activedescendant',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    article: ['aria-posinset', 'aria-setsize'],
    banner: [],
    blockquote: [],
    button: ['aria-disabled', 'aria-expanded', 'aria-haspopup', 'aria-pressed'],
    caption: [],
    cell: ['aria-colindex', 'aria-colspan', 'aria-rowindex', 'aria-rowspan'],
    checkbox: [
      'aria-checked',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-invalid',
      'aria-readonly',
      'aria-required'
    ],
    code: [],
    columnheader: [
      'aria-colindex',
      'aria-colspan',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-readonly',
      'aria-required',
      'aria-rowindex',
      'aria-rowspan',
      'aria-selected',
      'aria-sort'
    ],
    combobox: [
      'aria-activedescendant',
      'aria-autocomplete',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-readonly',
      'aria-required'
    ],
    complementary: [],
    contentinfo: [],
    definition: [],
    deletion: [],
    dialog: ['aria-modal'],
    directory: [],
    'doc-abstract': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-acknowledgments': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-afterword': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-appendix': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-backlink': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-biblioentry': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-level',
      'aria-posinset',
      'aria-setsize'
    ],
    'doc-bibliography': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-biblioref': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-chapter': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-colophon': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-conclusion': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-cover': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-credit': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-credits': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-dedication': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-endnote': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-level',
      'aria-posinset',
      'aria-setsize'
    ],
    'doc-endnotes': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-epigraph': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-epilogue': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-errata': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-example': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-footnote': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-foreword': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-glossary': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-glossref': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-index': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-introduction': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-noteref': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-notice': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-pagebreak': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-orientation',
      'aria-valuemax',
      'aria-valuemin',
      'aria-valuenow',
      'aria-valuetext'
    ],
    'doc-pagefooter': ['aria-disabled', 'aria-errormessage', 'aria-haspopup', 'aria-invalid'],
    'doc-pageheader': ['aria-disabled', 'aria-errormessage', 'aria-haspopup', 'aria-invalid'],
    'doc-pagelist': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-part': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-preface': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-prologue': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-pullquote': [],
    'doc-qna': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-subtitle': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-tip': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'doc-toc': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    document: [],
    emphasis: [],
    feed: [],
    figure: [],
    form: [],
    generic: [],
    'graphics-document': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'graphics-object': [
      'aria-activedescendant',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    'graphics-symbol': [
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid'
    ],
    grid: [
      'aria-activedescendant',
      'aria-colcount',
      'aria-disabled',
      'aria-multiselectable',
      'aria-readonly',
      'aria-rowcount'
    ],
    gridcell: [
      'aria-colindex',
      'aria-colspan',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-readonly',
      'aria-required',
      'aria-rowindex',
      'aria-rowspan',
      'aria-selected'
    ],
    group: ['aria-activedescendant', 'aria-disabled'],
    heading: ['aria-level'],
    img: [],
    insertion: [],
    link: ['aria-disabled', 'aria-expanded', 'aria-haspopup'],
    list: [],
    listbox: [
      'aria-activedescendant',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-invalid',
      'aria-multiselectable',
      'aria-orientation',
      'aria-readonly',
      'aria-required'
    ],
    listitem: ['aria-level', 'aria-posinset', 'aria-setsize'],
    log: [],
    main: [],
    mark: [],
    marquee: [],
    math: [],
    menu: ['aria-activedescendant', 'aria-disabled', 'aria-orientation'],
    menubar: ['aria-activedescendant', 'aria-disabled', 'aria-orientation'],
    menuitem: ['aria-disabled', 'aria-expanded', 'aria-haspopup', 'aria-posinset', 'aria-setsize'],
    menuitemcheckbox: [
      'aria-checked',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-posinset',
      'aria-readonly',
      'aria-required',
      'aria-setsize'
    ],
    menuitemradio: [
      'aria-checked',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-posinset',
      'aria-readonly',
      'aria-required',
      'aria-setsize'
    ],
    meter: ['aria-valuemax', 'aria-valuemin', 'aria-valuenow', 'aria-valuetext'],
    navigation: [],
    none: [],
    note: [],
    option: ['aria-checked', 'aria-disabled', 'aria-posinset', 'aria-selected', 'aria-setsize'],
    paragraph: [],
    presentation: [],
    progressbar: ['aria-valuemax', 'aria-valuemin', 'aria-valuenow', 'aria-valuetext'],
    radio: ['aria-checked', 'aria-disabled', 'aria-posinset', 'aria-setsize'],
    radiogroup: [
      'aria-activedescendant',
      'aria-disabled',
      'aria-errormessage',
      'aria-invalid',
      'aria-orientation',
      'aria-readonly',
      'aria-required'
    ],
    region: [],
    row: [
      'aria-activedescendant',
      'aria-colindex',
      'aria-disabled',
      'aria-expanded',
      'aria-level',
      'aria-posinset',
      'aria-rowindex',
      'aria-selected',
      'aria-setsize'
    ],
    rowgroup: [],
    rowheader: [
      'aria-colindex',
      'aria-colspan',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-haspopup',
      'aria-invalid',
      'aria-readonly',
      'aria-required',
      'aria-rowindex',
      'aria-rowspan',
      'aria-selected',
      'aria-sort'
    ],
    scrollbar: [
      'aria-disabled',
      'aria-orientation',
      'aria-valuemax',
      'aria-valuemin',
      'aria-valuenow',
      'aria-valuetext'
    ],
    search: [],
    searchbox: [
      'aria-activedescendant',
      'aria-autocomplete',
      'aria-disabled',
      'aria-errormessage',
      'aria-haspopup',
      'aria-invalid',
      'aria-multiline',
      'aria-placeholder',
      'aria-readonly',
      'aria-required'
    ],
    separator: [
      'aria-disabled',
      'aria-orientation',
      'aria-valuemax',
      'aria-valuemin',
      'aria-valuenow',
      'aria-valuetext'
    ],
    slider: [
      'aria-disabled',
      'aria-errormessage',
      'aria-haspopup',
      'aria-invalid',
      'aria-orientation',
      'aria-readonly',
      'aria-valuemax',
      'aria-valuemin',
      'aria-valuenow',
      'aria-valuetext'
    ],
    spinbutton: [
      'aria-activedescendant',
      'aria-disabled',
      'aria-errormessage',
      'aria-invalid',
      'aria-readonly',
      'aria-required',
      'aria-valuemax',
      'aria-valuemin',
      'aria-valuenow',
      'aria-valuetext'
    ],
    status: [],
    strong: [],
    subscript: [],
    superscript: [],
    switch: [
      'aria-checked',
      'aria-disabled',
      'aria-errormessage',
      'aria-expanded',
      'aria-invalid',
      'aria-readonly',
      'aria-required'
    ],
    tab: [
      'aria-disabled',
      'aria-expanded',
      'aria-haspopup',
      'aria-posinset',
      'aria-selected',
      'aria-setsize'
    ],
    table: ['aria-colcount', 'aria-rowcount'],
    tablist: [
      'aria-activedescendant',
      'aria-disabled',
      'aria-level',
      'aria-multiselectable',
      'aria-orientation'
    ],
    tabpanel: [],
    term: [],
    textbox: [
      'aria-activedescendant',
      'aria-autocomplete',
      'aria-disabled',
      'aria-errormessage',
      'aria-haspopup',
      'aria-invalid',
      'aria-multiline',
      'aria-placeholder',
      'aria-readonly',
      'aria-required'
    ],
    time: [],
    timer: [],
    toolbar: ['aria-activedescendant', 'aria-disabled', 'aria-orientation'],
    tooltip: [],
    tree: [
      'aria-activedescendant',
      'aria-disabled',
      'aria-errormessage',
      'aria-invalid',
      'aria-multiselectable',
      'aria-orientation',
      'aria-required'
    ],
    treegrid: [
      'aria-activedescendant',
      'aria-colcount',
      'aria-disabled',
      'aria-errormessage',
      'aria-invalid',
      'aria-multiselectable',
      'aria-orientation',
      'aria-readonly',
      'aria-required',
      'aria-rowcount'
    ],
    treeitem: [
      'aria-checked',
      'aria-disabled',
      'aria-expanded',
      'aria-haspopup',
      'aria-level',
      'aria-posinset',
      'aria-selected',
      'aria-setsize'
    ]
  };
  // </generated:aria-role-attrs>

  const globalSet = new Set(GLOBAL_ATTRS);
  // [role] keeps the explicit-role path; the attribute selector brings in
  // elements judged by their implicit role. Only non-global attributes can be
  // disallowed, so nothing else needs visiting.
  const selector = '[role], ' + NON_GLOBAL_ARIA_ATTR_SELECTOR;
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  const failOccurrences = [];
  const cantTellOccurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.attributes) continue;

    // ACT 5c01ea scopes the rule to any element carrying an ARIA attribute, so
    // an element with no role attribute is judged against its implicit role.
    // Only elements whose implicit role is context-free are covered; the
    // generator lists what is excluded and why.
    const explicitRole = ariaHelpers.getExplicitRole(el);
    let role = explicitRole;
    if (!role) {
      const tag = String(el.tagName || '').toLowerCase();
      const key =
        tag === 'input'
          ? 'input[type=' + String(el.getAttribute('type') || 'text').toLowerCase() + ']'
          : tag;
      role = Object.prototype.hasOwnProperty.call(IMPLICIT_ROLE_BY_ELEMENT, key)
        ? IMPLICIT_ROLE_BY_ELEMENT[key]
        : '';
    }
    if (!role || !ariaHelpers.isValidConcreteRole(role)) continue; // aria-roles-valid's concern

    // Presentational role conflict resolution drops role="none"/"presentation"
    // when the element is focusable or carries global ARIA, so the implicit
    // role decides which attributes are supported. Judging against the
    // presentational role would flag valid markup such as
    // <button role="none" aria-pressed="false">; presentation-role-conflict
    // owns this case.
    if (role === 'none' || role === 'presentation') continue;

    // Roles absent from the generated table are unknown to ARIA, so there is
    // nothing to judge them against.
    const roleSupported = SUPPORTED_ATTRS_BY_ROLE[role];
    if (!roleSupported) continue;
    const roleSupportedSet = new Set(roleSupported);

    let disallowed = null;
    const attrs = el.attributes;
    for (let i = 0; i < attrs.length; i++) {
      const name = String(attrs[i].name || '').toLowerCase();
      if (name.slice(0, 5) !== 'aria-') continue;
      if (!ariaHelpers.isValidAriaAttrName(name)) continue; // aria-valid-attr's concern

      applicableCount += 1;

      if (globalSet.has(name) || roleSupportedSet.has(name)) continue;

      if (!disallowed) disallowed = [];
      disallowed.push(name);
    }

    if (!disallowed || !disallowed.length) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    for (const name of disallowed) {
      // A property ARIA deprecated (rather than prohibited) on this role is
      // still allowed — surfaced as cantTell for the author to decide, not a
      // not-allowed fail.
      const deprecated =
        typeof ariaHelpers.isDeprecatedAttr === 'function' &&
        ariaHelpers.isDeprecatedAttr(name, role);
      if (deprecated) {
        cantTellOccurrences.push({
          selector: stableSelector,
          html,
          summary: 'This ARIA attribute is deprecated for this element’s role.',
          hint: 'It is still allowed but discouraged; remove it or use a role that supports it, as a future ARIA version may disallow it.',
          occurrenceOutcome: 'cantTell',
          i18n: {
            summaryKey: 'ariaAllowedAttr_summary_cantTell',
            hintKey: 'ariaAllowedAttr_hint_cantTell',
            params: { attr: name, role }
          },
          data: {
            details: { reasonCode: 'ARIA_ATTR_DEPRECATED', attr: name, role }
          }
        });
        continue;
      }
      failOccurrences.push({
        selector: stableSelector,
        html,
        summary: 'This ARIA attribute is not permitted for this element’s role.',
        hint: 'Remove this attribute, or use a role that supports it.',
        occurrenceOutcome: 'fail',
        i18n: {
          summaryKey: 'ariaAllowedAttr_summary_fail',
          hintKey: 'ariaAllowedAttr_hint_fail',
          params: { attr: name, role }
        },
        data: {
          details: { reasonCode: 'ARIA_ATTR_NOT_ALLOWED', attr: name, role }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const resolved = helpers.resolveTieredOutcome(
    failOccurrences,
    cantTellOccurrences,
    rule.defaultSeverity || 'moderate'
  );
  return { ruleId: rule.ruleId, ...resolved };
}), applicability: null },
    "aria-allowed-role": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role]')
    : helpers.queryAll('[role]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const role = ariaHelpers.getExplicitRole(el);
    if (!role || !ariaHelpers.isValidConcreteRole(role)) continue; // aria-roles-valid's concern

    const info = ariaHelpers.isRoleAllowedOnElement(el, role);
    if (!info.constrained) continue; // no asserted constraint for this element

    applicableCount += 1;

    if (info.allowed) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const tag = (el.tagName || '').toLowerCase();

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This role is not permitted on this element.',
      hint: 'Use a role permitted for this element, or change the host element.',
      i18n: {
        summaryKey: 'ariaAllowedRole_summary_fail',
        hintKey: 'ariaAllowedRole_hint_fail',
        params: { role, element: tag }
      },
      data: {
        details: { reasonCode: 'ARIA_ROLE_NOT_ALLOWED_FOR_ELEMENT', role, element: tag }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-braille-equivalent": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  function getConservativeSubtreeText(container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[aria-braillelabel], [aria-brailleroledescription]')
    : helpers.queryAll('[aria-braillelabel], [aria-brailleroledescription]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const brailleLabel = trim(el.getAttribute('aria-braillelabel'));
    const brailleRoleDesc = trim(el.getAttribute('aria-brailleroledescription'));
    if (!brailleLabel && !brailleRoleDesc) continue;

    applicableCount += 1;

    const missing = [];

    if (brailleLabel) {
      const nameInfo = helpers.getAccessibleNameInfo
        ? helpers.getAccessibleNameInfo(el, ctx)
        : null;
      const programmaticName = trim(
        nameInfo && typeof nameInfo.value === 'string' ? nameInfo.value : ''
      );
      const name = programmaticName || getConservativeSubtreeText(el);
      if (!name) missing.push({ attr: 'aria-braillelabel', requires: 'an accessible name' });
    }

    if (brailleRoleDesc) {
      const roleDesc = trim(el.getAttribute('aria-roledescription'));
      if (!roleDesc)
        missing.push({ attr: 'aria-brailleroledescription', requires: 'aria-roledescription' });
    }

    if (!missing.length) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const tag = (el.tagName || '').toLowerCase();

    for (const m of missing) {
      occurrences.push({
        selector: stableSelector,
        html,
        summary: `This element has ${m.attr} but no ${m.requires}, its non-braille equivalent.`,
        hint: `${m.attr} is a Braille-specific supplement, not a replacement — also provide ${m.requires}.`,
        i18n: {
          summaryKey: 'ariaBrailleEquivalent_summary_fail',
          hintKey: 'ariaBrailleEquivalent_hint_fail',
          params: { element: tag, attr: m.attr, requires: m.requires }
        },
        data: {
          details: {
            reasonCode: 'BRAILLE_ATTR_WITHOUT_EQUIVALENT',
            attr: m.attr,
            requires: m.requires
          }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-checked-state-mismatch": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(
        'input[type="checkbox"][aria-checked], input[type="radio"][aria-checked]'
      )
    : helpers.queryAll('input[type="checkbox"][aria-checked], input[type="radio"][aria-checked]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const type = trim(el.getAttribute('type')).toLowerCase();
    const isCheckbox = type === 'checkbox';
    const isRadio = type === 'radio';
    if (!isCheckbox && !isRadio) continue;

    const rawAriaChecked = trim(el.getAttribute('aria-checked')).toLowerCase();
    if (!rawAriaChecked) continue;

    applicableCount += 1;

    let normalizedAriaChecked;
    if (isCheckbox) {
      normalizedAriaChecked =
        rawAriaChecked === 'mixed' || rawAriaChecked === 'true' ? rawAriaChecked : 'false';
    } else {
      normalizedAriaChecked = rawAriaChecked === 'true' ? 'true' : 'false';
    }

    let actualState;
    if (isCheckbox && el.indeterminate) {
      actualState = 'mixed';
    } else {
      actualState = el.checked ? 'true' : 'false';
    }

    if (normalizedAriaChecked === actualState) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary:
        'This element’s aria-checked value does not match its actual checked/indeterminate state.',
      hint: 'Set aria-checked to match the element’s real state, or remove it — a native checkbox/radio already exposes this state without it.',
      i18n: {
        summaryKey: 'ariaCheckedStateMismatch_summary_cantTell',
        hintKey: 'ariaCheckedStateMismatch_hint_cantTell',
        params: { ariaChecked: normalizedAriaChecked, actualState, type }
      },
      data: {
        details: {
          reasonCode: 'ARIA_CHECKED_STATE_MISMATCH',
          ariaChecked: normalizedAriaChecked,
          actualState,
          type
        }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-conditional-attr": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  const TRUTHY_INVALID_VALUES = new Set(['true', 'grammar', 'spelling']);

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[aria-errormessage]')
    : helpers.queryAll('[aria-errormessage]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const errorMessageRef = trim(el.getAttribute('aria-errormessage'));
    if (!errorMessageRef) continue;

    applicableCount += 1;

    const invalidValue = trim(el.getAttribute('aria-invalid')).toLowerCase();
    if (TRUTHY_INVALID_VALUES.has(invalidValue)) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const tag = (el.tagName || '').toLowerCase();

    occurrences.push({
      selector: stableSelector,
      html,
      summary:
        'This element has aria-errormessage but aria-invalid is missing or "false", so the error message is not exposed.',
      hint: 'Set aria-invalid to "true" (or "grammar"/"spelling") whenever aria-errormessage should be exposed to assistive technology.',
      i18n: {
        summaryKey: 'ariaConditionalAttr_summary_fail',
        hintKey: 'ariaConditionalAttr_hint_fail',
        params: { element: tag, ariaInvalid: invalidValue || '(absent)' }
      },
      data: {
        details: {
          reasonCode: 'ARIA_ERRORMESSAGE_WITHOUT_TRUTHY_INVALID',
          ariaInvalid: invalidValue
        }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-deprecated-role": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role]')
    : helpers.queryAll('[role]');

  const failOccurrences = [];
  const cantTellOccurrences = [];
  let applicableCount = 0;

  // A role on an element hidden from assistive technology has no effect, so
  // ACT 674b10 does not apply to it.
  function isHidden(el) {
    try {
      if (typeof helpers.isDomVisibleEligible === 'function') {
        if (!helpers.isDomVisibleEligible(el, ctx)) return true;
      }
      for (let n = el; n && n.getAttribute; n = n.parentElement) {
        if (String(n.getAttribute('aria-hidden') || '').toLowerCase() === 'true') return true;
      }
    } catch {
      return false;
    }
    return false;
  }

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    if (isHidden(el)) continue;

    const role = ariaHelpers.getExplicitRole(el);
    if (!role) continue;

    // Only applicable to roles that are otherwise valid + concrete;
    // invalid/abstract usage is aria-roles-valid's concern.
    if (!ariaHelpers.isValidConcreteRole(role)) continue;

    applicableCount += 1;

    const deprecated = ariaHelpers.isDeprecatedRole(role);
    const discouraged =
      typeof ariaHelpers.isAuthorDiscouragedRole === 'function' &&
      ariaHelpers.isAuthorDiscouragedRole(role);
    const prohibited =
      typeof ariaHelpers.isAuthorProhibitedRole === 'function' &&
      ariaHelpers.isAuthorProhibitedRole(role);
    if (!deprecated && !discouraged && !prohibited) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const guidance = ariaHelpers.getDeprecatedRoleGuidance
      ? ariaHelpers.getDeprecatedRoleGuidance(role)
      : 'Replace the deprecated role with its recommended replacement.';

    if (prohibited) {
      // Author MUST NOT: the usage is non-conforming, not merely discouraged.
      failOccurrences.push({
        selector: stableSelector,
        html,
        summary: `This element uses role="${role}", which authors must not explicitly declare.`,
        hint: guidance,
        occurrenceOutcome: 'fail',
        i18n: {
          summaryKey: 'ariaDeprecatedRole_summary_fail',
          hintKey: 'ariaDeprecatedRole_hint_fail',
          params: { role, guidance }
        },
        data: {
          details: { reasonCode: 'ARIA_ROLE_AUTHOR_PROHIBITED', role, guidance }
        }
      });
    } else if (discouraged) {
      // Reserved for user-agent-internal use, at SHOULD NOT strength.
      cantTellOccurrences.push({
        selector: stableSelector,
        html,
        summary: `This element uses role="${role}", which is reserved for user agents (still valid, but discouraged).`,
        hint: guidance,
        occurrenceOutcome: 'cantTell',
        i18n: {
          summaryKey: 'ariaDeprecatedRole_summary_cantTell_discouraged',
          hintKey: 'ariaDeprecatedRole_hint_cantTell',
          params: { role, guidance }
        },
        data: {
          details: { reasonCode: 'ARIA_ROLE_AUTHOR_DISCOURAGED', role, guidance }
        }
      });
    } else {
      // Deprecated but still valid: surfaced for the author to decide.
      cantTellOccurrences.push({
        selector: stableSelector,
        html,
        summary: `This element uses role="${role}", which is deprecated in WAI-ARIA.`,
        hint: guidance,
        occurrenceOutcome: 'cantTell',
        i18n: {
          summaryKey: 'ariaDeprecatedRole_summary_cantTell',
          hintKey: 'ariaDeprecatedRole_hint_cantTell',
          params: { role, guidance }
        },
        data: {
          details: { reasonCode: 'ARIA_ROLE_DEPRECATED', role, guidance }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const resolved = helpers.resolveTieredOutcome(
    failOccurrences,
    cantTellOccurrences,
    rule.defaultSeverity || 'moderate'
  );
  return { ruleId: rule.ruleId, ...resolved };
}), applicability: null },
    "aria-hidden-body": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const body = document && document.body ? document.body : null;
  if (!body) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const raw = body.getAttribute ? body.getAttribute('aria-hidden') : null;
  const isHidden = raw != null && String(raw).trim().toLowerCase() === 'true';

  if (!isHidden) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  const stableSelector = helpers.buildSelector ? helpers.buildSelector(body) : 'body';
  const html = helpers.getOuterHtmlSnippet
    ? helpers.getOuterHtmlSnippet(body)
    : (body.outerHTML || '').slice(0, 200);

  const occurrences = [
    {
      selector: stableSelector,
      html,
      summary:
        'The document body has aria-hidden="true", which hides the entire page from assistive technologies.',
      hint: 'Remove aria-hidden from <body>. Hide specific elements instead, if that was the intent.',
      i18n: {
        summaryKey: 'ariaHiddenBody_summary_fail',
        hintKey: 'ariaHiddenBody_hint_fail',
        params: {}
      },
      data: {
        details: { reasonCode: 'ARIA_HIDDEN_BODY' }
      }
    }
  ];

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'critical',
    occurrences
  };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "aria-hidden-focus": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  // Used only to exclude non-rendered elements; we explicitly DO NOT exclude opacity:0.
  const isDomVisibleEligible =
    helpers && typeof helpers.isDomVisibleEligible === 'function'
      ? helpers.isDomVisibleEligible
      : null;

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  // Prefer helper occurrence wrapper so the engine can attach selector/snippet later.
  const reportOccurrence =
    helpers && typeof helpers.reportOccurrence === 'function' ? helpers.reportOccurrence : null;

  const trim = (v) => (v == null ? '' : String(v)).trim();
  const lower = (v) => trim(v).toLowerCase();

  function qAll(sel) {
    try {
      if (queryAllSmart) {
        const r = queryAllSmart(sel);
        if (Array.isArray(r)) return r;
        return Array.from(r || []);
      }
    } catch {
      // fall through
    }
    try {
      if (safeRoot && typeof safeRoot.querySelectorAll === 'function')
        return Array.from(safeRoot.querySelectorAll(sel));
    } catch {
      // fall through
    }
    return [];
  }

  // Flat-tree ancestor walk (assignedSlot wins over parentNode, then shadow
  // host) — shared with every other rule via ctx.helpers.composedParent
  // (src/core/dom-helpers.js), not reimplemented here, so a fix to the one
  // canonical definition can't drift out of sync with this rule's copy.
  const composedParent =
    helpers && typeof helpers.composedParent === 'function'
      ? helpers.composedParent
      : function (n) {
          return n && n.parentElement ? n.parentElement : null;
        };

  function closestAriaHiddenTrue(node) {
    let cur = node;
    let guard = 0;
    while (cur && guard++ < 200) {
      try {
        if (cur.getAttribute) {
          const v = cur.getAttribute('aria-hidden');
          if (v != null && lower(v) === 'true') return cur;
        }
      } catch {
        // ignore
      }
      cur = composedParent(cur);
    }
    return null;
  }

  function isWithinComposedSubtree(node, ancestor) {
    let cur = node;
    let guard = 0;
    while (cur && guard++ < 400) {
      if (cur === ancestor) return true;
      cur = composedParent(cur);
    }
    return false;
  }

  function getDeepActiveElement() {
    let cur = document && document.activeElement ? document.activeElement : null;
    let guard = 0;
    while (cur && cur.shadowRoot && cur.shadowRoot.activeElement && guard++ < 20) {
      cur = cur.shadowRoot.activeElement;
    }
    return cur;
  }

  function focusElementSafe(el) {
    if (!el || typeof el.focus !== 'function') return false;
    try {
      el.focus({ preventScroll: true });
      return true;
    } catch {
      try {
        el.focus();
        return true;
      } catch {
        return false;
      }
    }
  }

  function buildNodeRef(node, rootEl) {
    if (!node) return null;
    const tag = (() => {
      try {
        return lower(node.tagName || '');
      } catch {
        return '';
      }
    })();
    const idVal = (() => {
      try {
        return trim(node.getAttribute && node.getAttribute('id'));
      } catch {
        return '';
      }
    })();
    return {
      tag: tag || null,
      id: idVal || null,
      withinRoot: !!(rootEl && isWithinComposedSubtree(node, rootEl))
    };
  }

  function runFocusObservationWindow(fn) {
    const w =
      document && document.defaultView
        ? document.defaultView
        : typeof window !== 'undefined'
          ? window
          : null;
    if (!w || typeof fn !== 'function') return;

    const originalSetTimeout = typeof w.setTimeout === 'function' ? w.setTimeout.bind(w) : null;
    const originalRequestAnimationFrame =
      typeof w.requestAnimationFrame === 'function' ? w.requestAnimationFrame.bind(w) : null;
    const originalQueueMicrotask =
      typeof w.queueMicrotask === 'function' ? w.queueMicrotask.bind(w) : null;

    const queuedMicrotasks = [];
    const queuedRaf = [];
    const queuedTimers = [];
    let fakeTimerId = 1;

    const patchedSetTimeout = function (cb, delay) {
      const d = Number.isFinite(Number(delay)) ? Number(delay) : 0;
      if (typeof cb === 'function' && d <= 200) {
        const args = [];
        for (let i = 2; i < arguments.length; i++) args.push(arguments[i]);
        queuedTimers.push({ delay: d, cb: () => cb.apply(w, args) });
        return fakeTimerId++;
      }
      if (originalSetTimeout) return originalSetTimeout.apply(w, arguments);
      return fakeTimerId++;
    };

    const patchedRaf = function (cb) {
      if (typeof cb === 'function') {
        queuedRaf.push(cb);
        return fakeTimerId++;
      }
      if (originalRequestAnimationFrame) return originalRequestAnimationFrame.apply(w, arguments);
      return fakeTimerId++;
    };

    const patchedQueueMicrotask = function (cb) {
      if (typeof cb === 'function') queuedMicrotasks.push(cb);
    };

    try {
      if (originalSetTimeout) w.setTimeout = patchedSetTimeout;
      if (originalRequestAnimationFrame) w.requestAnimationFrame = patchedRaf;
      if (originalQueueMicrotask) w.queueMicrotask = patchedQueueMicrotask;
      fn();

      // Deterministic mini-window: microtasks -> rAF -> short timers (<=200ms).
      let guard = 0;
      while (
        (queuedMicrotasks.length || queuedRaf.length || queuedTimers.length) &&
        guard++ < 100
      ) {
        while (queuedMicrotasks.length) {
          const mt = queuedMicrotasks.shift();
          try {
            mt();
          } catch {}
        }
        while (queuedRaf.length) {
          const rf = queuedRaf.shift();
          try {
            rf(16);
          } catch {}
        }
        if (queuedTimers.length) {
          queuedTimers.sort((a, b) => a.delay - b.delay);
          const tt = queuedTimers.shift();
          try {
            tt.cb();
          } catch {}
        }
      }
    } finally {
      if (originalSetTimeout) w.setTimeout = originalSetTimeout;
      if (originalRequestAnimationFrame) w.requestAnimationFrame = originalRequestAnimationFrame;
      if (originalQueueMicrotask) w.queueMicrotask = originalQueueMicrotask;
    }
  }

  function probeImmediateFocusRedirect(entry) {
    // Conservative downgrade gate:
    // - only probe simple single-offender roots
    // - only downgrade when focus moves immediately OUTSIDE the aria-hidden subtree
    if (!entry || entry.count !== 1 || !entry.probeCandidates || !entry.probeCandidates.length)
      return null;
    const candidate = entry.probeCandidates[0];
    if (!candidate) return null;

    let focusedByEvent = false;
    const onFocusCapture = () => {
      focusedByEvent = true;
    };
    try {
      candidate.addEventListener('focus', onFocusCapture, true);
    } catch {
      // ignore
    }

    const focusTrace = [];
    const onFocusInCapture = (ev) => {
      const n = ev && ev.target ? ev.target : null;
      const ref = buildNodeRef(n, entry.rootEl);
      if (ref) focusTrace.push(ref);
    };
    try {
      document.addEventListener('focusin', onFocusInCapture, true);
    } catch {
      // ignore
    }

    const before = getDeepActiveElement();
    const beforeRef = buildNodeRef(before, entry.rootEl);
    if (beforeRef) focusTrace.push(beforeRef);

    let focused = false;
    runFocusObservationWindow(() => {
      focused = focusElementSafe(candidate);
    });

    try {
      document.removeEventListener('focusin', onFocusInCapture, true);
    } catch {
      // ignore
    }

    try {
      candidate.removeEventListener('focus', onFocusCapture, true);
    } catch {
      // ignore
    }
    if (!focused || !focusedByEvent) return null;

    const after = getDeepActiveElement();
    const afterRef = buildNodeRef(after, entry.rootEl);
    if (afterRef) focusTrace.push(afterRef);

    // Best-effort restore to reduce side effects across checks.
    if (before && before !== after) {
      focusElementSafe(before);
    }

    if (!after || after === candidate) return null;
    if (isWithinComposedSubtree(after, entry.rootEl)) return null;

    const redirectedTag = (() => {
      try {
        return lower(after.tagName || '');
      } catch {
        return '';
      }
    })();
    const redirectedId = (() => {
      try {
        return trim(after.getAttribute && after.getAttribute('id'));
      } catch {
        return '';
      }
    })();

    return {
      redirected: true,
      redirectedToTag: redirectedTag || null,
      redirectedToId: redirectedId || null,
      focusTrace
    };
  }

  // Lightweight "invisible but still focusable" hints.
  // Only computed for a capped set of offenders per aria-hidden root.
  function getVisibilityHints(el) {
    const out = [];
    if (!el) return out;

    let cs;
    try {
      const w =
        document && document.defaultView
          ? document.defaultView
          : typeof window !== 'undefined'
            ? window
            : null;
      cs = w && w.getComputedStyle ? w.getComputedStyle(el) : null;
    } catch {
      cs = null;
    }

    // opacity:0 (still focusable)
    try {
      const rawOp = cs && cs.opacity != null ? String(cs.opacity).trim() : '';
      const op = rawOp ? Number.parseFloat(rawOp) : 1;
      if (Number.isFinite(op) && op <= 0.0001) out.push('opacityZero');
    } catch {
      // ignore
    }

    // clip / clip-path (still focusable)
    try {
      const clip = cs && cs.clip != null ? String(cs.clip).trim() : '';
      const clipPath = cs && cs.clipPath != null ? String(cs.clipPath).trim() : '';
      const clipLow = (clip || '').toLowerCase();
      const clipPathLow = (clipPath || '').toLowerCase();

      // Common visually-hidden patterns
      if (clipLow && clipLow !== 'auto') {
        // Examples: rect(0px, 0px, 0px, 0px) / rect(0,0,0,0)
        if (clipLow.indexOf('rect(') !== -1 && clipLow.replace(/\s+/g, '').indexOf('rect(0') !== -1)
          out.push('clipped');
      }
      if (clipPathLow && clipPathLow !== 'none') {
        // Examples: inset(100%) / inset(50%)
        if (
          clipPathLow.indexOf('inset(') !== -1 &&
          (clipPathLow.indexOf('100%') !== -1 || clipPathLow.indexOf('50%') !== -1)
        ) {
          out.push('clipped');
        }
      }
    } catch {
      // ignore
    }

    // zero-size + overflow hidden/clip (still focusable)
    try {
      const wv = cs && cs.width != null ? String(cs.width).trim() : '';
      const hv = cs && cs.height != null ? String(cs.height).trim() : '';
      const ov = cs && cs.overflow != null ? String(cs.overflow).trim().toLowerCase() : '';
      const isZeroW = wv === '0px' || wv === '0';
      const isZeroH = hv === '0px' || hv === '0';
      const hidesOverflow = ov === 'hidden' || ov === 'clip';
      if ((isZeroW || isZeroH) && hidesOverflow) out.push('zeroSizeOverflowHidden');
    } catch {
      // ignore
    }

    // Off-screen heuristic (still focusable)
    // Deterministic string parsing; does not require layout geometry.
    try {
      const pos = cs && cs.position != null ? String(cs.position).trim().toLowerCase() : '';
      const left = cs && cs.left != null ? String(cs.left).trim().toLowerCase() : '';
      const top = cs && cs.top != null ? String(cs.top).trim().toLowerCase() : '';
      const ti = cs && cs.textIndent != null ? String(cs.textIndent).trim().toLowerCase() : '';

      const parsePx = (s) => {
        if (!s || s === 'auto') return null;
        const m = String(s).match(/-?\d+(\.\d+)?/);
        if (!m) return null;
        const n = Number.parseFloat(m[0]);
        return Number.isFinite(n) ? n : null;
      };

      const l = parsePx(left);
      const t = parsePx(top);
      const ind = parsePx(ti);

      if (pos === 'absolute' || pos === 'fixed') {
        if ((l != null && l <= -5000) || (t != null && t <= -5000)) out.push('offscreen');
      }
      // Common text-indent technique
      if (ind != null && ind <= -5000) out.push('offscreen');
    } catch {
      // ignore
    }

    // Dedup while preserving stable order
    const seen = new Set();
    const uniq = [];
    for (const k of out) {
      const kk = String(k);
      if (!seen.has(kk)) {
        seen.add(kk);
        uniq.push(kk);
      }
    }
    return uniq;
  }

  // DOM-visibility gate to avoid false positives:
  // Exclude structural/CSS hidden cases that prevent focus (display:none, visibility:hidden, hidden attr, etc.).
  // IMPORTANT: Do NOT exclude opacity-based invisibility; opacity:0 remains in-scope.
  function isActuallyFocusable(el) {
    if (!el || !el.getAttribute) return false;

    // Hard blockers that should always win (even if fallback logic would say "focusable")
    if (hasInertAncestor(el)) return false;
    if (isDisabledFormControl(el)) return false;

    // An explicit negative tabindex removes the element from the keyboard
    // tab sequence entirely, regardless of tag — the standard, WAI-
    // recommended technique for safely hiding focusable content behind
    // aria-hidden (e.g. <button tabindex="-1"> / <a tabindex="-1"> inside
    // an aria-hidden container). This cares about tabbability, not raw
    // focusability. Such an element is still programmatically focusable
    // (script could call .focus()), but that's not what "no focusable
    // content behind aria-hidden" cares about.
    const explicitTabindex = trim(el.getAttribute('tabindex'));
    if (
      explicitTabindex !== '' &&
      !Number.isNaN(Number(explicitTabindex)) &&
      Number(explicitTabindex) < 0
    ) {
      return false;
    }

    // 1) "DOM focusability" check (ignore aria-hidden)
    // Prefer helper for broad coverage, but do not let aria-hidden flip focusable->false.
    let helperInfo = null;
    if (getFocusableInfo) {
      try {
        helperInfo = getFocusableInfo(el, ctx);
      } catch {
        helperInfo = null;
      }
    }

    // Local fallback that does NOT care about aria-hidden
    const tag = lower(el.tagName || '');
    let fallbackFocusable = false;

    if (tag === 'a' || tag === 'area') {
      const href = trim(el.getAttribute('href'));
      fallbackFocusable = !!href;
    } else if (tag === 'button' || tag === 'select' || tag === 'textarea' || tag === 'summary') {
      fallbackFocusable = true;
    } else if (tag === 'input') {
      const type = lower(el.getAttribute('type') || '');
      fallbackFocusable = type !== 'hidden';
    } else if (tag === 'iframe') {
      fallbackFocusable = true;
    } else if (
      (tag === 'audio' || tag === 'video') &&
      el.hasAttribute &&
      el.hasAttribute('controls')
    ) {
      fallbackFocusable = true;
    } else if (el.hasAttribute && el.hasAttribute('contenteditable')) {
      // contenteditable="false" explicitly disables the editing host and
      // does not by itself add the element to the tab order; only treat
      // presence/""/"true"/"plaintext-only" as focus-enabling.
      const ceVal = lower(trim(el.getAttribute('contenteditable')));
      fallbackFocusable = ceVal !== 'false';
    } else {
      const ti = el.getAttribute('tabindex');
      const s = trim(ti);
      if (ti != null && s !== '' && !Number.isNaN(Number(s))) {
        fallbackFocusable = true; // tabindex makes it programmatically focusable
      }
    }

    // Use helper focusable if it says true; otherwise use fallback focusable.
    // This specifically prevents aria-hidden from suppressing "self focusable".
    const focusable = (helperInfo && helperInfo.focusable === true) || fallbackFocusable;
    if (!focusable) return false;

    // 2) exclude non-rendered / non-visible-by-style blockers
    // IMPORTANT: Do NOT exclude opacity-based invisibility; opacity:0 remains in-scope.
    if (isDomVisibleEligible) {
      try {
        const vis = isDomVisibleEligible(el, ctx, {
          visibilityMode: 'styleOnly',
          disableGeometry: true
        });
        if (vis && vis.eligible === false) {
          const rs = Array.isArray(vis.reasons) ? vis.reasons : [];
          const nonOpacity = rs.filter((r) => String(r) !== 'opacityZero');
          if (nonOpacity.length) return false;
        }
      } catch {
        // ignore
      }
    }

    return true;
  }

  function hasInertAncestor(el) {
    let cur = el;
    let guard = 0;
    while (cur && guard++ < 200) {
      try {
        if (cur.nodeType === 1 && cur.hasAttribute && cur.hasAttribute('inert')) return true;
      } catch {
        // ignore
      }
      cur = composedParent(cur);
    }
    return false;
  }

  function isDisabledFormControl(el) {
    try {
      // Covers button/input/select/textarea/option/optgroup/fieldset etc.
      if (typeof el.disabled === 'boolean' && el.disabled) return true;
    } catch {
      // ignore
    }
    try {
      const tag = lower(el.tagName || '');
      if (
        (tag === 'button' ||
          tag === 'input' ||
          tag === 'select' ||
          tag === 'textarea' ||
          tag === 'option' ||
          tag === 'optgroup') &&
        el.hasAttribute &&
        el.hasAttribute('disabled')
      ) {
        return true;
      }
    } catch {
      // ignore
    }
    return false;
  }

  // 1) Find aria-hidden="true" roots.
  const ariaHiddenRoots = qAll('[aria-hidden="true"]');
  if (!ariaHiddenRoots.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  // 2) Find focusable candidates once (performance) and bucket those inside aria-hidden.
  // Keep selector fairly small to avoid huge candidate sets while still covering the reference-engine cases.
  const focusableCandidates = qAll(
    'a[href],area[href],button,input,select,textarea,summary,iframe,audio[controls],video[controls],[tabindex],[contenteditable]'
  );

  const bucket = new Map(); // ariaHiddenRoot -> { rootEl, count, offenders: [], hints:Set, rootIsFocusable, probeCandidates: [] }
  const maxOffendersPerRoot = 5;
  const maxProbeCandidatesPerRoot = 3;

  for (let i = 0; i < focusableCandidates.length; i++) {
    const el = focusableCandidates[i];
    if (!el || !el.getAttribute) continue;

    // Cheap check first: a plain ancestor-attribute walk with no CSS
    // computation, vs. isActuallyFocusable's getComputedStyle-per-ancestor
    // cost. Both conditions are required (AND), so checking whichever is
    // cheaper first cannot change which elements end up in the bucket —
    // it only skips the expensive check for the (typically vast) majority
    // of focusable candidates that were never inside an aria-hidden root
    // in the first place. On pages with many focusable candidates and a
    // large stylesheet this is a big speedup, and a pure ordering change,
    // not a behavior change.
    const rootEl = closestAriaHiddenTrue(el);
    if (!rootEl) continue;

    if (!isActuallyFocusable(el)) continue;

    let entry = bucket.get(rootEl);
    if (!entry) {
      entry = {
        rootEl,
        count: 0,
        offenders: [],
        hints: new Set(),
        rootIsFocusable: false,
        probeCandidates: []
      };
      bucket.set(rootEl, entry);
    }
    entry.count += 1;
    if (entry.probeCandidates.length < maxProbeCandidatesPerRoot) entry.probeCandidates.push(el);

    // Capture a small, deterministic offender summary + visibility hints.
    if (entry.offenders.length < maxOffendersPerRoot) {
      let tag;
      let ti;
      let href;
      let type;

      try {
        tag = lower(el.tagName || '');
      } catch {
        tag = '';
      }
      try {
        ti = el.getAttribute('tabindex');
      } catch {
        ti = null;
      }
      try {
        href = tag === 'a' || tag === 'area' ? trim(el.getAttribute('href')) : null;
      } catch {
        href = null;
      }
      try {
        type = tag === 'input' ? lower(el.getAttribute('type') || '') : null;
      } catch {
        type = null;
      }

      const hints = getVisibilityHints(el);
      for (const h of hints) entry.hints.add(h);

      entry.offenders.push({
        tag: tag || null,
        tabindex: ti == null ? null : String(ti),
        href: href || null,
        type: type || null,
        visibilityHints: hints
      });
    }
  }

  if (!bucket.size) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  // Ensure deterministic root focusability computation (done once per failing root).
  for (const [, entry] of bucket) {
    const el = entry.rootEl;
    entry.rootIsFocusable = isActuallyFocusable(el);
  }

  // 3) Report one occurrence per aria-hidden root that contains focusable content.
  const failOccurrences = [];
  const uncertainOccurrences = [];
  for (const [, entry] of bucket) {
    const el = entry.rootEl;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;

    // stable visibility hint ordering
    const hintOrder = ['opacityZero', 'offscreen', 'clipped', 'zeroSizeOverflowHidden'];
    const hintsArr = [];
    for (const k of hintOrder) if (entry.hints.has(k)) hintsArr.push(k);

    const tagName = (() => {
      try {
        return lower(el.tagName || '');
      } catch {
        return '';
      }
    })();

    // Split counts deterministically
    const selfFocusable = !!entry.rootIsFocusable;
    const totalFocusable = entry.count;
    const descendantFocusable = selfFocusable ? Math.max(0, totalFocusable - 1) : totalFocusable;

    const summaryKey = selfFocusable
      ? descendantFocusable > 0
        ? 'ariaHidden_focus_summary_fail_self_and_desc'
        : 'ariaHidden_focus_summary_fail_self'
      : 'ariaHidden_focus_summary_fail_desc';

    const reasonCode = selfFocusable
      ? descendantFocusable > 0
        ? 'ariaHiddenSelfAndDescendantsFocusable'
        : 'ariaHiddenSelfFocusable'
      : 'ariaHiddenContainsFocusable';

    const summaryText = selfFocusable
      ? descendantFocusable > 0
        ? `aria-hidden ${tagName} is focusable and contains ${descendantFocusable} focusable descendant(s) (${totalFocusable} focusable element(s) total).`
        : `aria-hidden ${tagName} is focusable (${totalFocusable} focusable element(s)).`
      : `aria-hidden ${tagName} contains ${totalFocusable} focusable element(s).`;

    const runtimeProbe = probeImmediateFocusRedirect(entry);
    const downgradedToCantTell = !!(runtimeProbe && runtimeProbe.redirected);

    const cantTellSummary = `aria-hidden ${tagName} received focus but focus moved immediately to another element. Verify sentinel/focus-trap behavior.`;

    const baseOccurrence = {
      summary: downgradedToCantTell ? cantTellSummary : summaryText,
      hint: downgradedToCantTell
        ? 'Verify this is an intentional focus sentinel/focus-trap handoff and that keyboard users never remain on hidden focus targets.'
        : 'Remove focusability from descendants or remove aria-hidden; ensure focus and accessibility trees stay aligned.',
      occurrenceOutcome: downgradedToCantTell ? 'cantTell' : 'fail',
      i18n: {
        summaryKey: downgradedToCantTell
          ? 'ariaHidden_focus_summary_cantTell_redirect'
          : summaryKey,
        hintKey: downgradedToCantTell
          ? 'ariaHidden_focus_hint_cantTell_redirect'
          : 'ariaHidden_focus_hint_fail',
        params: {
          element: tagName,
          focusableCount: String(totalFocusable),
          descendantFocusableCount: String(descendantFocusable),
          redirectedToTag:
            runtimeProbe && runtimeProbe.redirectedToTag ? runtimeProbe.redirectedToTag : '',
          redirectedToId:
            runtimeProbe && runtimeProbe.redirectedToId ? runtimeProbe.redirectedToId : ''
        }
      },
      data: {
        details: {
          reasonCode: downgradedToCantTell
            ? 'ariaHiddenFocusable_runtimeRedirect_needsReview'
            : reasonCode,
          metrics: {
            focusableTotal: totalFocusable,
            focusableDescendants: descendantFocusable,
            rootIsFocusable: selfFocusable,
            offendersCaptured: entry.offenders.length,
            visibilityHints: hintsArr.slice(0)
          },
          runtimeProbe: runtimeProbe || null,
          offenders: entry.offenders.slice(0)
        },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    };

    const occurrence = reportOccurrence
      ? reportOccurrence(el, baseOccurrence)
      : { __node: el, selector: '', html: '', ...baseOccurrence };

    if (downgradedToCantTell) uncertainOccurrences.push(occurrence);
    else failOccurrences.push(occurrence);
  }

  // See helpers.resolveTieredOutcome's own header comment (src/core/dom-helpers.js):
  // a fail-tier finding never silently discards cantTell-tier findings from
  // the same run — both are returned together when the outcome is 'fail'.
  const resolved = helpers.resolveTieredOutcome(
    failOccurrences,
    uncertainOccurrences,
    rule.defaultSeverity || 'minor'
  );
  return { ruleId: rule.ruleId, ...resolved };
}), applicability: null },
    "aria-prohibited-attr": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  // Roles whose WAI-ARIA 1.2 definition lists a "Prohibited ARIA States and
  // Properties" entry for naming attributes (these roles must never carry an
  // accessible name). Declared inside runInPage (rather than at module
  // scope) because the build inlines only this function's own source text
  // — see scripts/build-core.js header ("runInPage MUST be self-contained").
  const ROLES_PROHIBITING_NAME = new Set([
    'caption',
    'code',
    'deletion',
    'emphasis',
    'generic',
    'insertion',
    'mark',
    'none',
    'paragraph',
    'presentation',
    'strong',
    'subscript',
    'suggestion',
    'superscript',
    'time'
  ]);

  const PROHIBITED_NAMING_ATTRS = ['aria-label', 'aria-labelledby'];

  const failOccurrences = [];
  const cantTellOccurrences = [];
  let applicableCount = 0;

  // --- Tier 1: explicit, valid role from the naming-prohibited set ---

  const roleNodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role]')
    : helpers.queryAll('[role]');

  for (const el of roleNodes) {
    if (!el || !el.getAttribute) continue;

    const role = ariaHelpers.getExplicitRole(el);
    if (!role || !ROLES_PROHIBITING_NAME.has(role)) continue;

    applicableCount += 1;

    const present = [];
    for (const attr of PROHIBITED_NAMING_ATTRS) {
      const v = el.getAttribute(attr);
      if (v != null && String(v).trim() !== '') present.push(attr);
    }

    if (!present.length) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    for (const attr of present) {
      failOccurrences.push({
        selector: stableSelector,
        html,
        occurrenceOutcome: 'fail',
        summary: 'This attribute is prohibited on this element’s role.',
        hint: 'Remove this attribute; this role must not carry an accessible name.',
        i18n: {
          summaryKey: 'ariaProhibitedAttr_summary_fail',
          hintKey: 'ariaProhibitedAttr_hint_fail',
          params: { attr, role }
        },
        data: {
          details: { reasonCode: 'ARIA_ATTR_PROHIBITED', attr, role }
        }
      });
    }
  }

  // --- Tier 2: no role at all (see header comment for the full rationale
  // and how ROLELESS_NATIVE_TAGS/WIDGET_TYPE_ROLES were derived) ---

  // Small, curated set of native tags verified to carry no explicit or
  // implicit ARIA role. Deliberately excludes <section>/<form>/<a> — all
  // conditionally roleless too, but already handled with more nuance
  // elsewhere in this engine (see header comment).
  const ROLELESS_NATIVE_TAGS = new Set([
    'p',
    'b',
    'i',
    'em',
    'strong',
    'span',
    'div',
    'code',
    'mark',
    'time',
    'ins',
    'del',
    'small',
    'sub',
    'sup',
    'abbr',
    'cite',
    'q',
    'kbd',
    'samp',
    'var',
    'address',
    'blockquote',
    'pre',
    'figcaption',
    'picture',
    'template',
    'hgroup',
    'wbr',
    'br',
    'legend'
  ]);

  // WAI-ARIA roles typed as "widget" (the role set the roleless-branch
  // exemption below branches on, not the six-category WAI-ARIA taxonomy).
  const WIDGET_TYPE_ROLES = new Set([
    'alert',
    'alertdialog',
    'button',
    'checkbox',
    'combobox',
    'dialog',
    'gridcell',
    'link',
    'listbox',
    'log',
    'marquee',
    'menuitem',
    'menuitemcheckbox',
    'menuitemradio',
    'option',
    'progressbar',
    'radio',
    'scrollbar',
    'searchbox',
    'slider',
    'spinbutton',
    'status',
    'switch',
    'tab',
    'tabpanel',
    'textbox',
    'timer',
    'treeitem'
  ]);

  const getComposedParent =
    helpers && typeof helpers.composedParent === 'function'
      ? helpers.composedParent
      : function (n) {
          return n && n.parentElement ? n.parentElement : null;
        };

  // Nearest ancestor's real role (explicit-if-valid, else native/implicit),
  // skipping roleless/presentation/none ancestors — used only to check
  // whether that role is a "widget"-type one (the roleless-branch
  // exemption). Not the same helper as aria-required-parent's containment
  // walk: this one also accepts non-required-context roles.
  function getNearestAncestorRole(el) {
    let cur = getComposedParent(el);
    let guard = 0;
    while (cur && guard++ < 200) {
      if (cur.nodeType !== 1) {
        cur = getComposedParent(cur);
        continue;
      }
      const explicit = ariaHelpers.getExplicitRole(cur);
      const role =
        explicit && ariaHelpers.isValidConcreteRole(explicit)
          ? explicit
          : ariaHelpers.getNativeRoleForElement(cur);
      if (!role || role === 'presentation' || role === 'none') {
        cur = getComposedParent(cur);
        continue;
      }
      return role;
    }
    return '';
  }

  // A small, spec-reserved set of hyphenated tag names that are NOT
  // autonomous custom elements despite containing a hyphen (legacy SVG/
  // MathML tags predating the Custom Elements spec) — see
  // https://html.spec.whatwg.org/#valid-custom-element-name's own
  // exclusion list. Excluded so this doesn't misclassify them as
  // always-roleless the same way a real custom element is.
  const RESERVED_HYPHENATED_TAGS = new Set([
    'annotation-xml',
    'color-profile',
    'font-face',
    'font-face-src',
    'font-face-uri',
    'font-face-format',
    'font-face-name',
    'missing-glyph'
  ]);

  // An autonomous custom element (author-defined tag, always containing a
  // hyphen per the Custom Elements spec's naming grammar) has no implicit
  // ARIA role at all -- unlike native tags, there is no conditional-role
  // nuance to worry about here (a native <a>/<section>/<form> can gain an
  // implicit role depending on other attributes, which is exactly why
  // ROLELESS_NATIVE_TAGS is a hand-verified allowlist rather than a
  // blanket rule; a custom element has no such spec-defined conditional
  // role logic whatsoever). Covers e.g. a `<play-button aria-label="...">`
  // or `<app-carousel aria-label="...">` with no other name source, which
  // the fixed native-tag allowlist below would otherwise skip.
  function isRolelessCustomElementTag(tag) {
    return tag.includes('-') && !RESERVED_HYPHENATED_TAGS.has(tag);
  }

  const namingSelector = '[aria-label],[aria-labelledby]';
  const namingNodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(namingSelector)
    : helpers.queryAll(namingSelector);

  for (const el of namingNodes) {
    if (!el || !el.getAttribute) continue;

    const tag = String(el.tagName || '').toLowerCase();
    if (!ROLELESS_NATIVE_TAGS.has(tag) && !isRolelessCustomElementTag(tag)) continue;
    const explicitRole = ariaHelpers.getExplicitRole(el);
    if (explicitRole && ariaHelpers.isValidConcreteRole(explicitRole)) continue; // has a real, recognized role — Tier 1's concern (if in ROLES_PROHIBITING_NAME) or a role this rule has no opinion on. An INVALID role token (e.g. a typo) is ignored per spec, same as no role attribute at all, and must still fall through to this branch.
    if (ariaHelpers.getNativeRoleForElement(el)) continue; // has a real implicit role after all — not this branch's concern

    const present = [];
    for (const attr of PROHIBITED_NAMING_ATTRS) {
      const v = el.getAttribute(attr);
      if (v != null && String(v).trim() !== '') present.push(attr);
    }
    if (!present.length) continue;

    applicableCount += 1;

    const ancestorRole = getNearestAncestorRole(el);
    if (ancestorRole && WIDGET_TYPE_ROLES.has(ancestorRole)) continue; // roleless helper node inside a real widget — not flagged

    const nameInfo = helpers.getContentNameInfo ? helpers.getContentNameInfo(el, ctx) : null;
    const hasContentFallback = !!(
      nameInfo &&
      nameInfo.present &&
      String(nameInfo.value || '').trim() !== ''
    );

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    for (const attr of present) {
      if (hasContentFallback) {
        cantTellOccurrences.push({
          selector: stableSelector,
          html,
          occurrenceOutcome: 'cantTell',
          summary: `This ${tag} has no role, so ${attr} may not be exposed as its accessible name by assistive technology — but the element's own content already provides one.`,
          hint: 'Verify whether the existing text content already serves as this element’s label; if so the naming attribute is redundant, otherwise give the element a role that supports naming (e.g. role="img").',
          i18n: {
            summaryKey: 'ariaProhibitedAttr_summary_cantTell_roleless',
            hintKey: 'ariaProhibitedAttr_hint_cantTell_roleless',
            params: { attr, element: tag }
          },
          data: {
            details: {
              reasonCode: 'ARIA_ATTR_PROHIBITED_ROLELESS_NEEDS_REVIEW',
              attr,
              role: null,
              element: tag
            }
          }
        });
      } else {
        failOccurrences.push({
          selector: stableSelector,
          html,
          occurrenceOutcome: 'fail',
          summary: `This ${tag} has no role and no other accessible-name source, so ${attr} is not reliably exposed to assistive technology.`,
          hint: 'Give this element a role that supports an accessible name (e.g. role="img"/"button"), or remove this attribute if it serves no purpose without one.',
          i18n: {
            summaryKey: 'ariaProhibitedAttr_summary_fail_roleless',
            hintKey: 'ariaProhibitedAttr_hint_fail_roleless',
            params: { attr, element: tag }
          },
          data: {
            details: { reasonCode: 'ARIA_ATTR_PROHIBITED_ROLELESS', attr, role: null, element: tag }
          }
        });
      }
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  // See helpers.resolveTieredOutcome's own header comment (src/core/dom-helpers.js):
  // a fail-tier finding never silently discards cantTell-tier findings from
  // the same run — both are returned together when the outcome is 'fail'.
  const resolved = helpers.resolveTieredOutcome(
    failOccurrences,
    cantTellOccurrences,
    rule.defaultSeverity || 'moderate'
  );
  return { ruleId: rule.ruleId, ...resolved };
}), applicability: null },
    "aria-prohibited-children": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  function isEligibleAcc(el) {
    const fn =
      helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // The WAI-ARIA "Global States and Properties" set (same list as
  // aria-allowed-attr.js's GLOBAL_ATTRS — duplicated, not imported, since
  // runInPage must be self-contained per scripts/build-core.js). A
  // roleless descendant carrying any of these is a real accessible-tree
  // node, not a transparent wrapper.
  const GLOBAL_ARIA_ATTRS = [
    'aria-atomic',
    'aria-braillelabel',
    'aria-brailleroledescription',
    'aria-busy',
    'aria-controls',
    'aria-current',
    'aria-describedby',
    'aria-description',
    'aria-details',
    'aria-disabled',
    'aria-dropeffect',
    'aria-errormessage',
    'aria-flowto',
    'aria-grabbed',
    'aria-haspopup',
    'aria-hidden',
    'aria-invalid',
    'aria-keyshortcuts',
    'aria-label',
    'aria-labelledby',
    'aria-live',
    'aria-owns',
    'aria-relevant',
    'aria-roledescription'
  ];

  function getGlobalAriaAttr(el) {
    for (const attr of GLOBAL_ARIA_ATTRS) {
      const v = el.getAttribute ? el.getAttribute(attr) : null;
      if (v != null) return attr;
    }
    return null;
  }

  const MAX_DEPTH = 40;

  // Collects this container's owned-role entries, pruning role="none"/
  // "presentation" and required-matching "group"/"rowgroup" wrappers as
  // transparent (recursing through them), and stopping at the first
  // non-transparent role boundary otherwise — see header comment. A
  // roleless descendant is ALSO a non-transparent boundary (an owned
  // entry with role: null, which can never satisfy a required-role set)
  // when it carries a global aria-* attribute or is focusable.
  // kidRole comes from getContainmentRole, not getExplicitRole: "roleless"
  // here means neither an explicit role="" NOR one of the native
  // containment tags (li, tr, td, ...), so a bare <li>/<tr>/... is a real
  // listitem/row boundary, not a transparent wrapper the walk should pass
  // through.
  function collectOwnedRoles(el, requiredSet, out, depth) {
    if (depth > MAX_DEPTH) return;
    const kids = el.children ? Array.prototype.slice.call(el.children) : [];
    for (const kid of kids) {
      if (!kid || kid.nodeType !== 1) continue;
      if (!isEligibleAcc(kid)) continue;

      const kidRole = ariaHelpers.getContainmentRole(kid);
      const isPresentational = kidRole === 'presentation' || kidRole === 'none';
      const isTransparentGroup =
        (kidRole === 'group' || kidRole === 'rowgroup') && requiredSet.has(kidRole);

      if (!kidRole && !isPresentational) {
        const globalAttr = getGlobalAriaAttr(kid);
        let mechanism;
        try {
          const fi =
            helpers && typeof helpers.getFocusableInfo === 'function'
              ? helpers.getFocusableInfo(kid, ctx)
              : null;
          mechanism = (fi && fi.focusable && fi.mechanism) || 'none';
        } catch {
          mechanism = 'none';
        }
        if (globalAttr || mechanism !== 'none') {
          // `mechanism` distinguishes an actual tabindex="" attribute from
          // native focusability (e.g. <a href>, <button>, <input>) — these
          // are different facts and must not be reported as the same
          // "carries tabindex" claim (a native anchor with no tabindex
          // attribute at all is not "carrying tabindex").
          const attr = globalAttr || (mechanism === 'tabindex' ? 'tabindex' : 'nativeFocusable');
          out.push({ el: kid, role: null, attr });
          continue; // real accessible-tree node: stop here, do not recurse further
        }
      }

      if (!kidRole || isPresentational || isTransparentGroup) {
        collectOwnedRoles(kid, requiredSet, out, depth + 1);
        continue;
      }

      if (!ariaHelpers.isValidConcreteRole(kidRole)) continue; // aria-roles-valid's concern

      out.push({ el: kid, role: kidRole });
      // Stop here: a real, non-transparent role is its own semantic unit.
    }
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role]')
    : helpers.queryAll('[role]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const role = ariaHelpers.getExplicitRole(el);
    if (!role || !ariaHelpers.isValidConcreteRole(role)) continue;

    const requiredOwned = ariaHelpers.getRequiredOwnedRoles(role);
    if (!requiredOwned || !requiredOwned.length) continue;

    if (!isEligibleAcc(el)) continue; // not currently exposed to the accessibility tree

    applicableCount += 1;

    const requiredSet = new Set(requiredOwned);
    const owned = [];
    collectOwnedRoles(el, requiredSet, owned, 0);

    for (const entry of owned) {
      if (entry.role && requiredSet.has(entry.role)) continue;

      const stableSelector = helpers.buildSelector ? helpers.buildSelector(entry.el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(entry.el)
        : entry.el.outerHTML || '';
      const containerSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';

      const isRoleless = !entry.role;
      const isNativeFocusable = entry.attr === 'nativeFocusable';

      let summary;
      let hint;
      let summaryKey;
      let hintKey;
      if (isNativeFocusable) {
        summary = `This element has no explicit role but is natively focusable, making it a real accessible-tree node that is not an allowed owned child of the enclosing role="${role}" container.`;
        hint = `Give this element role="presentation"/"none", remove its native focusability (e.g. drop the href/tabindex-granting attribute), or move it outside the ${role} container.`;
        summaryKey = 'ariaProhibitedChildren_summary_fail_native_focusable';
        hintKey = 'ariaProhibitedChildren_hint_fail_native_focusable';
      } else if (isRoleless) {
        summary = `This element has no explicit role but carries ${entry.attr}, making it a real accessible-tree node that is not an allowed owned child of the enclosing role="${role}" container.`;
        hint = `Remove ${entry.attr} (or the role="${role}" container ownership), or give this element role="presentation"/"none" if it isn't meant to be its own accessible-tree node.`;
        summaryKey = 'ariaProhibitedChildren_summary_fail_roleless';
        hintKey = 'ariaProhibitedChildren_hint_fail_roleless';
      } else {
        summary = `This element has role="${entry.role}", which is not an allowed owned child of the enclosing role="${role}" container.`;
        hint = `Remove or change this role so it matches one of the container's allowed owned roles (${requiredOwned.join(', ')}), or move this element outside the ${role} container.`;
        summaryKey = 'ariaProhibitedChildren_summary_fail';
        hintKey = 'ariaProhibitedChildren_hint_fail';
      }

      occurrences.push({
        selector: stableSelector,
        html,
        summary,
        hint,
        i18n: {
          summaryKey,
          hintKey,
          params: isRoleless
            ? { attr: entry.attr, containerRole: role }
            : { childRole: entry.role, containerRole: role, allowedRoles: requiredOwned.join(', ') }
        },
        data: {
          details: {
            reasonCode: isRoleless ? 'ARIA_PROHIBITED_CHILD_ROLELESS' : 'ARIA_PROHIBITED_CHILD',
            childRole: entry.role,
            attr: entry.attr,
            containerRole: role,
            containerSelector,
            allowedOwnedRoles: requiredOwned
          }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-required-attr": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  function isEligibleAcc(el) {
    const fn =
      helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  function isMarkedBusy(el) {
    const v = el.getAttribute('aria-busy');
    return v != null && String(v).trim().toLowerCase() === 'true';
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role]')
    : helpers.queryAll('[role]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const role = ariaHelpers.getExplicitRole(el);
    if (!role || !ariaHelpers.isValidConcreteRole(role)) continue; // aria-roles-valid's concern

    const required = ariaHelpers.getRequiredAttrsForRole(role);
    if (!required.length) continue;

    if (!isEligibleAcc(el)) continue; // not currently exposed to the accessibility tree
    if (isMarkedBusy(el)) continue; // author has signaled transient incompleteness per WAI-ARIA

    applicableCount += 1;

    const missing = [];
    for (const attr of required) {
      const v = el.getAttribute(attr);
      if (v == null || String(v).trim() === '') missing.push(attr);
    }

    if (!missing.length) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    for (const attr of missing) {
      occurrences.push({
        selector: stableSelector,
        html,
        summary: 'This attribute is required for this element’s role, but is missing.',
        hint: 'Add this attribute with a valid value for this role.',
        i18n: {
          summaryKey: 'ariaRequiredAttr_summary_fail',
          hintKey: 'ariaRequiredAttr_hint_fail',
          params: { attr, role }
        },
        data: {
          details: { reasonCode: 'ARIA_ATTR_REQUIRED_MISSING', attr, role }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-required-children": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  function isEligibleAcc(el) {
    const fn =
      helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  function isMarkedBusy(el) {
    const v = el.getAttribute('aria-busy');
    return v != null && String(v).trim().toLowerCase() === 'true';
  }

  // Candidate selector for descendant scanning: explicit role attributes,
  // plus every native tag ariaHelpers.getContainmentRole() recognizes
  // (kept in sync with aria-helpers.js NATIVE_CONTAINMENT_ROLE_BY_ELEMENT).
  // Declared inside runInPage — see scripts/build-core.js header
  // ("runInPage MUST be self-contained").
  const CANDIDATE_SELECTOR =
    '[role], li, option, tr, td, th, thead, tbody, tfoot, ul, ol, table, select, input[type="radio"]';

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role]')
    : helpers.queryAll('[role]');

  const occurrences = [];
  let applicableCount = 0;

  // Composed-tree descendant walk: el.querySelectorAll only sees the raw
  // light-DOM subtree, so a container whose real owned children are
  // distributed via <slot> (e.g. a shadow-DOM role="list" wrapping
  // <slot></slot>, with the actual role="listitem" elements living in the
  // light DOM and projected in) would never find them there — same class
  // of bug as aria-required-parent's ancestor search, just in the opposite
  // (descendant) direction.
  //
  // Deliberately scoped to slot expansion only — does NOT separately
  // descend into an unrelated nested custom element's own shadow root
  // (e.g. a <my-widget> child with no <slot> involvement at all). That's a
  // qualitatively different question (does an arbitrary component's own
  // internal structure count as this container's "owned children"?) with
  // no known case driving it yet; slot projection is the shape that
  // actually comes up.
  function collectComposedDescendants(node, out, seen, limit) {
    if (!node || !node.children) return;
    for (const child of Array.from(node.children)) {
      if (out.length >= limit) return;
      if (seen.has(child)) continue;

      if (
        (child.tagName || '').toLowerCase() === 'slot' &&
        typeof child.assignedElements === 'function'
      ) {
        let assigned;
        try {
          assigned = child.assignedElements({ flatten: true }) || [];
        } catch {
          assigned = [];
        }
        for (const a of assigned) {
          if (seen.has(a)) continue;
          seen.add(a);
          out.push(a);
          if (out.length >= limit) return;
          collectComposedDescendants(a, out, seen, limit);
          if (out.length >= limit) return;
        }
        continue; // a <slot>'s own childNodes are unrendered fallback content once something is assigned
      }

      seen.add(child);
      out.push(child);
      collectComposedDescendants(child, out, seen, limit);
    }
  }

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const role = ariaHelpers.getExplicitRole(el);
    if (!role || !ariaHelpers.isValidConcreteRole(role)) continue; // aria-roles-valid's concern

    const requiredOwned = ariaHelpers.getRequiredOwnedRoles(role);
    if (!requiredOwned || !requiredOwned.length) continue;

    if (!isEligibleAcc(el)) continue; // not currently exposed to the accessibility tree
    if (isMarkedBusy(el)) continue; // author has signaled transient incompleteness per WAI-ARIA

    applicableCount += 1;

    const ownedSet = new Set(requiredOwned);
    let found = false;

    // Fast path first: native querySelectorAll over the curated candidate
    // selector, exactly as before this fix — covers the overwhelming
    // majority of containers (no shadow DOM involved at all) with zero
    // added cost.
    let descendants;
    try {
      descendants = el.querySelectorAll(CANDIDATE_SELECTOR);
    } catch {
      descendants = [];
    }
    for (const cand of descendants) {
      const candRole = ariaHelpers.getContainmentRole(cand);
      if (candRole && ownedSet.has(candRole)) {
        found = true;
        break;
      }
    }

    // Slow path only when the fast path found nothing AND there's an actual
    // <slot> somewhere in the subtree to expand — bounds the extra cost to
    // exactly the containers that could possibly need it.
    if (!found) {
      let hasSlot;
      try {
        hasSlot = !!el.querySelector('slot');
      } catch {
        hasSlot = false;
      }
      if (hasSlot) {
        const composed = [];
        try {
          collectComposedDescendants(el, composed, new Set(), 5000);
        } catch {
          // fall through with whatever was collected before the error
        }
        for (const cand of composed) {
          if (!cand || !cand.getAttribute) continue;
          const candRole = ariaHelpers.getContainmentRole(cand);
          if (candRole && ownedSet.has(candRole)) {
            found = true;
            break;
          }
        }
      }
    }

    if (!found) {
      const ownsAttr = el.getAttribute('aria-owns');
      if (ownsAttr && helpers.resolveIdRefs) {
        const resolved = helpers.resolveIdRefs(ownsAttr, ctx, { maxRefs: 50 });
        for (const ownedEl of resolved.refs || []) {
          const candRole = ariaHelpers.getContainmentRole(ownedEl);
          if (candRole && ownedSet.has(candRole)) {
            found = true;
            break;
          }
        }
      }
    }

    if (found) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This container role has no owned child with a required role.',
      hint: 'Add a descendant (or aria-owns-referenced element) with one of the required owned roles.',
      i18n: {
        summaryKey: 'ariaRequiredChildren_summary_fail',
        hintKey: 'ariaRequiredChildren_hint_fail',
        params: { role, requiredRoles: requiredOwned.join(', ') }
      },
      data: {
        details: {
          reasonCode: 'ARIA_REQUIRED_CHILD_MISSING',
          role,
          requiredOwnedRoles: requiredOwned
        }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-required-parent": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  function isEligibleAcc(el) {
    const fn =
      helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // Roles that may host a nested listitem/treeitem group without breaking
  // the required-context chain — the group role is transparent for exactly
  // these two roles.
  const GROUP_TRANSPARENT_FOR_ROLES = new Set(['listitem', 'treeitem']);

  // A real ancestor role — not "no role at all" and not the two roles that
  // strip an element from the accessibility tree's parent/child chain
  // entirely (presentation/none) — stops the search. This is stricter than
  // "any ancestor with the right role anywhere up the tree": the
  // required-context relationship is about the accessibility tree's actual
  // PARENT, so an intervening ancestor with its OWN distinct real role
  // (e.g. a plain <li>'s native "listitem" role) blocks the search even if
  // a further-up ancestor has the correct role. E.g. a <button role="tab">
  // inside a plain <li> (native listitem) inside <ul role="tablist"> fails:
  // the tablist is never the tab's accessible-tree parent, the listitem is.
  function getRealContextRole(el) {
    const role = ariaHelpers.getContainmentRole(el);
    if (!role || role === 'presentation' || role === 'none') return '';
    return role;
  }

  // Flat-tree ancestor walk (ctx.helpers.composedParent — assignedSlot wins
  // over parentNode, then shadow host). A slotted light-DOM element's real
  // rendered ancestor is whatever the shadow tree wraps its <slot> in (e.g.
  // a role="list" container), not its own light-DOM parentElement.
  // composedParent can return a non-Element node (a ShadowRoot, nodeType
  // 11) when climbing out of a shadow tree that has no further light-DOM
  // parent — skip those and keep climbing rather than treating them as a
  // (roleless) context.
  const getComposedParent =
    helpers && typeof helpers.composedParent === 'function'
      ? helpers.composedParent
      : function (n) {
          return n && n.parentElement ? n.parentElement : null;
        };

  function hasAcceptableAncestorContext(el, acceptableRoles, ownRole) {
    const allowsGroup = acceptableRoles.has('group');
    let cur = getComposedParent(el);
    let guard = 0;
    // Mutable working copy: passing a transparent "group" ancestor also
    // makes the element's OWN role an acceptable context from that point on
    // (a nested treeitem-under-group-under-treeitem chain is a normal,
    // arbitrarily-deep ARIA tree/list, not just one level). Cloned lazily
    // so the caller's Set (built once per element in runInPage) is never
    // mutated.
    let roles = acceptableRoles;
    while (cur && guard++ < 200) {
      if (cur.nodeType !== 1) {
        cur = getComposedParent(cur);
        continue;
      }
      const role = getRealContextRole(cur);
      if (!role) {
        cur = getComposedParent(cur);
        continue;
      }
      if (role === 'group' && allowsGroup && GROUP_TRANSPARENT_FOR_ROLES.has(ownRole)) {
        if (roles === acceptableRoles) roles = new Set(acceptableRoles);
        roles.add(ownRole);
        cur = getComposedParent(cur);
        continue;
      }
      return roles.has(role);
    }
    return false;
  }

  function hasAcceptableOwnerContext(el, acceptableRoles) {
    const elId = el.getAttribute('id');
    const idTok = elId && String(elId).trim();
    if (!idTok) return false;

    const owners = helpers.queryAllSmart
      ? helpers.queryAllSmart('[aria-owns]')
      : helpers.queryAll('[aria-owns]');
    for (const owner of owners) {
      if (!owner || !owner.getAttribute) continue;
      const ownsAttr = owner.getAttribute('aria-owns') || '';
      const tokens = ownsAttr.split(/\s+/).filter(Boolean);
      if (tokens.indexOf(idTok) === -1) continue;

      const role = ariaHelpers.getContainmentRole(owner);
      if (role && acceptableRoles.has(role)) return true;
    }
    return false;
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role]')
    : helpers.queryAll('[role]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const role = ariaHelpers.getExplicitRole(el);
    if (!role || !ariaHelpers.isValidConcreteRole(role)) continue; // aria-roles-valid's concern

    const requiredContext = ariaHelpers.getRequiredContextRoles(role);
    if (!requiredContext || !requiredContext.length) continue; // no entry, or explicitly unconstrained

    if (!isEligibleAcc(el)) continue; // not currently exposed to the accessibility tree

    applicableCount += 1;

    const acceptableRoles = new Set(requiredContext);
    const hasContext =
      hasAcceptableAncestorContext(el, acceptableRoles, role) ||
      hasAcceptableOwnerContext(el, acceptableRoles);

    if (hasContext) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This role requires a specific ancestor/owner context role, which was not found.',
      hint: 'Place this element inside (or aria-owns-reference it from) an element with an acceptable context role.',
      i18n: {
        summaryKey: 'ariaRequiredParent_summary_fail',
        hintKey: 'ariaRequiredParent_hint_fail',
        params: { role, requiredRoles: requiredContext.join(', ') }
      },
      data: {
        details: {
          reasonCode: 'ARIA_REQUIRED_PARENT_MISSING',
          role,
          requiredContextRoles: requiredContext
        }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-role-name-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel, rt) => {
          try {
            const scope = rt || safeRoot;
            return scope && scope.querySelectorAll ? Array.from(scope.querySelectorAll(sel)) : [];
          } catch {
            return [];
          }
        };

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const getAriaLabelledByInfo =
    helpers && typeof helpers.getAriaLabelledByInfo === 'function'
      ? helpers.getAriaLabelledByInfo
      : null;

  const normalizeWs = (s) =>
    String(s || '')
      .replace(/\s+/g, ' ')
      .trim();

  const getAttr = (el, name) => {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  };

  // aria-labelledby resolution: delegate to the shared helper (used by
  // button-name-present et al.), which correctly includes hidden/aria-hidden
  // referenced nodes per the Accessible Name and Description Computation
  // spec (a hidden node directly referenced by aria-labelledby still
  // supplies its text — this is a standard visually-hidden-label pattern).
  const resolveLabelledby = (el, maxRefs) => {
    if (!getAriaLabelledByInfo) return '';
    try {
      const info = getAriaLabelledByInfo(el, ctx, { maxRefs: Math.max(1, maxRefs || 8) });
      return info && info.present ? normalizeWs(info.value) : '';
    } catch {
      return '';
    }
  };

  const isEligible = (el) => {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  };

  // Frozen allowlist of roles to check in this generic rule.
  // (Keep this small, explicit, and standards-based for determinism.)
  const roleSet = new Set([
    'scrollbar',
    'toolbar',
    'tablist',
    'radiogroup',
    'tree',
    'grid',
    'menu',
    'menubar',
    'meter',
    'progressbar'
  ]);

  const selector =
    '[role="scrollbar"],[role="toolbar"],[role="tablist"],[role="radiogroup"],[role="tree"],[role="grid"],[role="menu"],[role="menubar"],[role="meter"],[role="progressbar"]';

  const nodes = (() => {
    try {
      return (
        (queryAllSmart ? queryAllSmart(selector, safeRoot) : queryAll(selector, safeRoot)) || []
      );
    } catch {
      return queryAll(selector, safeRoot);
    }
  })();

  if (!nodes.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    // Role normalization + allowlist check (defensive).
    const role = (() => {
      try {
        return normalizeWs(el.getAttribute('role')).toLowerCase();
      } catch {
        return '';
      }
    })();
    if (!roleSet.has(role)) continue;

    if (!isEligible(el)) continue;

    applicableCount += 1;

    const ariaLabel = getAttr(el, 'aria-label');
    const labelled = ariaLabel ? '' : resolveLabelledby(el, 8);

    const title = ariaLabel || labelled ? '' : getAttr(el, 'title');

    const ok = !!(ariaLabel || labelled || title);
    if (ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;

    const baseOccurrence = {
      summary: 'This element has no accessible name.',
      hint: 'Provide aria-label or aria-labelledby (preferred), or a non-empty title attribute.',
      i18n: {
        summaryKey: 'ariaRoleNamePresent_summary_fail',
        hintKey: 'ariaRoleNamePresent_hint_fail',
        params: { role }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: {
          reasonCode: 'name_missing',
          role,
          nameSourcesChecked: ['aria-label', 'aria-labelledby', 'title']
        }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "aria-roles-valid": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role]')
    : helpers.queryAll('[role]');

  const occurrences = [];
  let applicableCount = 0;

  // Programmatically hidden per the ACT glossary: display:none, visibility not
  // visible, or aria-hidden on the element or an ancestor.
  function isHidden(el) {
    try {
      if (typeof helpers.isDomVisibleEligible === 'function') {
        if (!helpers.isDomVisibleEligible(el, ctx)) return true;
      }
      for (let n = el; n && n.getAttribute; n = n.parentElement) {
        if (String(n.getAttribute('aria-hidden') || '').toLowerCase() === 'true') return true;
      }
    } catch {
      return false;
    }
    return false;
  }

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    // ACT 674b10 is not applicable to a programmatically hidden element.
    if (isHidden(el)) continue;

    // role takes a fallback list and the first token the browser recognises
    // wins, so role="searchfield searchbox" resolves to searchbox. The rule
    // fails only when no token names a concrete role.
    const tokens =
      typeof ariaHelpers.getAllRoleTokens === 'function'
        ? ariaHelpers.getAllRoleTokens(el)
        : [ariaHelpers.getExplicitRole(el)].filter(Boolean);
    if (!tokens.length) continue; // role="" or whitespace-only: not this rule's concern

    applicableCount += 1;

    const usable = tokens.find((t) => ariaHelpers.isKnownRole(t) && !ariaHelpers.isAbstractRole(t));
    if (usable) continue;

    const role = tokens[0];
    const isKnown = tokens.some((t) => ariaHelpers.isKnownRole(t));
    const reasonCode = !isKnown ? 'ARIA_ROLE_INVALID' : 'ARIA_ROLE_ABSTRACT';
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: !isKnown
        ? 'The role attribute value is not a recognized ARIA role.'
        : 'The role attribute value is an abstract ARIA role, which must not be used directly.',
      hint: !isKnown
        ? 'Use a valid ARIA role token, or remove the role attribute if none applies.'
        : 'Replace this abstract role with a concrete role appropriate for the widget/structure.',
      i18n: {
        summaryKey: !isKnown ? 'ariaRolesValid_summary_invalid' : 'ariaRolesValid_summary_abstract',
        hintKey: !isKnown ? 'ariaRolesValid_hint_invalid' : 'ariaRolesValid_hint_abstract',
        params: { role }
      },
      data: {
        details: { reasonCode, role }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-text": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const FOCUSABLE_DESCENDANT_SELECTOR =
    'a[href], button, input, select, textarea, [tabindex], iframe, [contenteditable]:not([contenteditable="false"])';

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role="text"]')
    : helpers.queryAll('[role="text"]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.querySelector) continue;

    applicableCount += 1;

    let focusableDescendant;
    try {
      focusableDescendant = el.querySelector(FOCUSABLE_DESCENDANT_SELECTOR);
    } catch {
      focusableDescendant = null;
    }
    if (!focusableDescendant) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    const baseOccurrence = {
      selector: stableSelector,
      html,
      summary: 'This role="text" element contains a focusable descendant.',
      hint: 'Remove role="text" (or remove the focusable descendant) — a "plain text" region should not contain focusable content.',
      i18n: {
        summaryKey: 'ariaText_summary_cantTell',
        hintKey: 'ariaText_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'ROLE_TEXT_HAS_FOCUSABLE_DESCENDANT' }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push(baseOccurrence);
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }

  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-valid-attr": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const nodes = helpers.queryAllSmart ? helpers.queryAllSmart('*') : helpers.queryAll('*');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.attributes) continue;

    let invalidNames = null;
    const attrs = el.attributes;
    for (let i = 0; i < attrs.length; i++) {
      const name = String(attrs[i].name || '').toLowerCase();
      if (name.slice(0, 5) !== 'aria-') continue;

      applicableCount += 1;

      if (!ariaHelpers.isValidAriaAttrName(name)) {
        if (!invalidNames) invalidNames = [];
        invalidNames.push(name);
      }
    }

    if (!invalidNames || !invalidNames.length) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    for (const name of invalidNames) {
      occurrences.push({
        selector: stableSelector,
        html,
        summary: 'This element has an attribute that is not a recognized ARIA attribute.',
        hint: 'Correct the attribute name (check for typos), or remove it if not needed.',
        i18n: {
          summaryKey: 'ariaValidAttr_summary_fail',
          hintKey: 'ariaValidAttr_hint_fail',
          params: { attr: name }
        },
        data: {
          details: { reasonCode: 'ARIA_ATTR_INVALID', attr: name }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "aria-valid-attr-value": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const ariaHelpers = helpers && helpers.aria ? helpers.aria : null;
  if (!ariaHelpers) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const nodes = helpers.queryAllSmart ? helpers.queryAllSmart('*') : helpers.queryAll('*');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.attributes || !el.getAttribute) continue;

    let invalid = null;
    const attrs = el.attributes;
    for (let i = 0; i < attrs.length; i++) {
      const name = String(attrs[i].name || '').toLowerCase();
      if (name.slice(0, 5) !== 'aria-') continue;
      if (!ariaHelpers.isValidAriaAttrName(name)) continue; // aria-valid-attr's concern

      applicableCount += 1;

      const rawValue = el.getAttribute(name);
      const result = ariaHelpers.validateAttrValue(name, rawValue);
      if (!result.valid) {
        if (!invalid) invalid = [];
        invalid.push({
          name,
          value: rawValue == null ? '' : String(rawValue),
          reason: result.reason
        });
      }
    }

    if (!invalid || !invalid.length) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    for (const item of invalid) {
      occurrences.push({
        selector: stableSelector,
        html,
        summary: 'This element has an ARIA attribute with an invalid value.',
        hint: 'Use a value that matches the attribute’s expected type (see the WAI-ARIA specification for this attribute).',
        i18n: {
          summaryKey: 'ariaValidAttrValue_summary_fail',
          hintKey: 'ariaValidAttrValue_hint_fail',
          params: { attr: item.name, value: item.value }
        },
        data: {
          details: {
            reasonCode: 'ARIA_ATTR_VALUE_INVALID',
            attr: item.name,
            value: item.value,
            valueReason: item.reason
          }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "autocomplete-valid": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  // Declared inside runInPage — see scripts/build-core.js header
  // ("runInPage MUST be self-contained").
  const FIELD_NAMES = new Set([
    'name',
    'honorific-prefix',
    'given-name',
    'additional-name',
    'family-name',
    'honorific-suffix',
    'nickname',
    'username',
    'new-password',
    'current-password',
    'one-time-code',
    'organization-title',
    'organization',
    'street-address',
    'address-line1',
    'address-line2',
    'address-line3',
    'address-level4',
    'address-level3',
    'address-level2',
    'address-level1',
    'country',
    'country-name',
    'postal-code',
    'cc-name',
    'cc-given-name',
    'cc-additional-name',
    'cc-family-name',
    'cc-number',
    'cc-exp',
    'cc-exp-month',
    'cc-exp-year',
    'cc-csc',
    'cc-type',
    'transaction-currency',
    'transaction-amount',
    'language',
    'bday',
    'bday-day',
    'bday-month',
    'bday-year',
    'sex',
    'tel',
    'tel-country-code',
    'tel-national',
    'tel-area-code',
    'tel-local',
    'tel-extension',
    'email',
    'impp',
    'url',
    'photo'
  ]);
  const CONTACT_MODALITY = new Set(['home', 'work', 'mobile', 'fax', 'pager', 'impp']);

  function isValidAutocomplete(raw) {
    const tokens = raw.trim().toLowerCase().split(/\s+/).filter(Boolean);
    if (!tokens.length) return false;
    if (tokens.length === 1 && (tokens[0] === 'on' || tokens[0] === 'off')) return true;

    let i = 0;
    if (tokens[i] && tokens[i].startsWith('section-') && tokens[i].length > 'section-'.length)
      i += 1;
    if (tokens[i] === 'shipping' || tokens[i] === 'billing') i += 1;
    // A contact modality token is only allowed when the field that follows is
    // a contact field, so "work photo" is invalid while "work email" is not.
    if (CONTACT_MODALITY.has(tokens[i])) {
      const next = tokens[i + 1];
      const isContactField =
        next === 'email' || next === 'impp' || next === 'tel' || (next || '').startsWith('tel-');
      if (!isContactField) return false;
      i += 1;
    }

    let end = tokens.length;
    if (tokens[end - 1] === 'webauthn') end -= 1;

    const remaining = tokens.slice(i, end);
    if (remaining.length !== 1) return false;
    return FIELD_NAMES.has(remaining[0]);
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('input, select, textarea')
    : helpers.queryAll('input, select, textarea');

  const occurrences = [];
  let applicableCount = 0;

  // ACT 73f2c2 exempts controls where the attribute cannot describe an input
  // purpose: the on/off toggle, disabled controls, input types with a fixed
  // value, and controls that take no input.
  const FIXED_VALUE_TYPES = new Set([
    'button',
    'checkbox',
    'file',
    'image',
    'radio',
    'reset',
    'submit'
  ]);

  function isExempt(el) {
    const tag = String(el.tagName || '').toLowerCase();
    if (tag === 'input') {
      const type = String(el.getAttribute('type') || 'text').toLowerCase();
      if (FIXED_VALUE_TYPES.has(type)) return true;
    }
    if (el.hasAttribute && el.hasAttribute('disabled')) return true;
    if (String(el.getAttribute('aria-disabled') || '').toLowerCase() === 'true') return true;
    return false;
  }

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const raw = String(el.getAttribute('autocomplete') || '').trim();
    if (!raw) continue;

    const tokens = raw.toLowerCase().split(/\s+/).filter(Boolean);
    if (tokens.length === 1 && (tokens[0] === 'on' || tokens[0] === 'off')) continue;
    if (isExempt(el)) continue;

    applicableCount += 1;

    if (isValidAutocomplete(raw)) continue;

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This autocomplete attribute value is not a valid autofill value.',
      hint: 'Use "on"/"off", or a valid autofill token list (e.g. "shipping street-address", "cc-number").',
      i18n: {
        summaryKey: 'autocompleteValid_summary_fail',
        hintKey: 'autocompleteValid_hint_fail',
        params: { element: tag, value: raw }
      },
      data: {
        details: { reasonCode: 'AUTOCOMPLETE_VALUE_INVALID', element: tag, value: raw }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "avoid-inline-spacing": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const SPACING_PROPS = ['line-height', 'letter-spacing', 'word-spacing'];
  // WCAG 1.4.12's own metrics, as multiples of the font size.
  const MIN_RATIO = { 'line-height': 1.5, 'letter-spacing': 0.12, 'word-spacing': 0.16 };
  // Only these two take the value from the parent, leaving this declaration
  // specifying no spacing of its own. `initial` and `revert` resolve to a
  // concrete value (`normal` for all three properties), so they stay in scope.
  const INHERITED_KEYWORDS = ['inherit', 'unset'];
  // No user agent's `normal` line height reaches 1.5, so a forced `normal`
  // always falls short of the metric.
  const NORMAL_LINE_HEIGHT_RATIO = 1.2;
  const CAMEL = {
    'line-height': 'lineHeight',
    'letter-spacing': 'letterSpacing',
    'word-spacing': 'wordSpacing'
  };

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[style]')
    : helpers.queryAll('[style]');

  const occurrences = [];
  let applicableCount = 0;

  // Within one declaration block, importance wins over order, so the last
  // important declaration is the one that takes effect. Passed Example 5 of ACT
  // 78fd32 turns on this and Passed Example 6 on the non-important half.
  function effectiveDeclaration(raw, prop) {
    const re = new RegExp('(?:^|;)\\s*' + prop + '\\s*:\\s*([^;]*)', 'gi');
    let chosen = null;
    let m;
    while ((m = re.exec(raw))) {
      const value = String(m[1] || '').trim();
      const important = /!\s*important\s*$/i.test(value);
      const clean = value.replace(/!\s*important\s*$/i, '').trim();
      if (!clean) continue;
      if (!chosen || important || !chosen.important) {
        if (chosen && chosen.important && !important) continue;
        chosen = { value: clean, important };
      }
    }
    return chosen;
  }

  function hasVisibleTextChild(el) {
    let kids;
    try {
      kids = el.childNodes ? Array.from(el.childNodes) : [];
    } catch {
      return false;
    }
    return kids.some((n) => n && n.nodeType === 3 && String(n.nodeValue || '').trim() !== '');
  }

  function isRendered(el) {
    if (helpers.isDomVisibleEligible) {
      try {
        return !!helpers.isDomVisibleEligible(el, ctx, { targetSet: 'dom' }).eligible;
      } catch {
        return true;
      }
    }
    return true;
  }

  const px = (v) => {
    const n = parseFloat(v);
    return Number.isFinite(n) && /px\s*$/.test(String(v)) ? n : null;
  };

  function computedStyleOf(el) {
    if (helpers && typeof helpers.computedStyle === 'function') {
      try {
        const cs = helpers.computedStyle(el);
        if (cs) return cs;
      } catch {
        // fall through to the realm's own view
      }
    }
    try {
      const view = el.ownerDocument && el.ownerDocument.defaultView;
      if (view && typeof view.getComputedStyle === 'function') return view.getComputedStyle(el);
    } catch {
      // no computed style available
    }
    return null;
  }

  // ACT scopes these rules to text visible on screen, and text pushed far off
  // canvas is the one hidden shape the shared eligibility check keeps eligible.
  function isOffScreen(el) {
    if (!helpers.getVisibilityHintsInfo) return false;
    try {
      const info = helpers.getVisibilityHintsInfo(el, ctx, {});
      return !!(info && Array.isArray(info.hints) && info.hints.indexOf('offscreen') !== -1);
    } catch {
      return false;
    }
  }

  // A realm that does not lay the document out reports font-size as the CSS
  // absolute-size keyword rather than a length. Resolving those keeps px-valued
  // spacing checkable there, and cannot manufacture a failure: a font size
  // larger than assumed only makes a px ratio smaller.
  const ABSOLUTE_FONT_SIZES = {
    'xx-small': 9,
    'x-small': 10,
    small: 13,
    medium: 16,
    large: 18,
    'x-large': 24,
    'xx-large': 32
  };

  function fontSizeOf(cs) {
    if (!cs) return null;
    const direct = px(cs.fontSize);
    if (direct !== null) return direct;
    const keyword = String(cs.fontSize || '')
      .trim()
      .toLowerCase();
    return ABSOLUTE_FONT_SIZES[keyword] || null;
  }

  /**
   * The spacing as a multiple of the font size, or null when it cannot be
   * resolved. Computed style is preferred because it already applies the
   * cascade and unit resolution; the declared value is only a fallback for
   * environments that do not lay the document out.
   */
  function spacingRatio(el, prop, declared) {
    const cs = computedStyleOf(el);
    const fontSize = fontSizeOf(cs);
    if (cs && fontSize) {
      const used = px(cs[CAMEL[prop]]);
      if (used !== null) return used / fontSize;
    }

    const v = String(declared || '')
      .trim()
      .toLowerCase();
    // `normal`, and the keywords that resolve to it, add nothing for the two
    // spacing properties and stay under the metric for line height.
    if (v === 'normal' || v === 'initial' || v === 'revert' || v === 'revert-layer') {
      return prop === 'line-height' ? NORMAL_LINE_HEIGHT_RATIO : 0;
    }
    if (/^[0-9.]+$/.test(v)) return parseFloat(v);
    if (/em$/.test(v)) return parseFloat(v);
    if (/%$/.test(v)) return parseFloat(v) / 100;
    const asPx = px(v);
    if (asPx !== null && fontSize) return asPx / fontSize;
    return null;
  }

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const raw = String(el.getAttribute('style') || '');
    if (!raw.trim()) continue;

    const lower = raw.toLowerCase();
    const hasAnySpacingProp = SPACING_PROPS.some((p) => lower.includes(p));
    if (!hasAnySpacingProp) continue;

    // The rule is about text the user needs to re-space, so an element with no
    // text of its own, or none that renders, is not a target.
    if (!hasVisibleTextChild(el) || !isRendered(el) || isOffScreen(el)) continue;

    const flagged = [];
    let inScope = false;
    for (const prop of SPACING_PROPS) {
      const decl = effectiveDeclaration(raw, prop);
      if (!decl || !decl.important) continue;
      if (INHERITED_KEYWORDS.indexOf(decl.value.toLowerCase()) !== -1) continue;
      inScope = true;
      const ratio = spacingRatio(el, prop, decl.value);
      // Unresolvable spacing is left alone: this engine reserves fail for
      // high-confidence violations.
      if (ratio === null) continue;
      if (ratio < MIN_RATIO[prop]) flagged.push(prop);
    }

    if (inScope) applicableCount += 1;
    if (!flagged.length) continue;

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: `This element's inline style forces ${flagged.join(', ')} with !important below the WCAG text-spacing metric, so the user cannot raise it.`,
      hint: 'Remove !important from line-height/letter-spacing/word-spacing in inline styles, or set a value that already meets the metric (line-height 1.5, letter-spacing 0.12em, word-spacing 0.16em).',
      i18n: {
        summaryKey: 'avoidInlineSpacing_summary_fail',
        hintKey: 'avoidInlineSpacing_hint_fail',
        params: { element: tag, properties: flagged.join(', ') }
      },
      data: {
        details: { reasonCode: 'INLINE_SPACING_IMPORTANT', element: tag, properties: flagged }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "binary-control-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function buildLabelForMap(doc) {
    const map = new Map(); // id -> label element (first)
    try {
      const labels = doc && doc.getElementsByTagName ? doc.getElementsByTagName('label') : [];
      for (let i = 0; i < labels.length; i += 1) {
        const lab = labels[i];
        if (!lab || !lab.getAttribute) continue;
        const f = normalizeWs(lab.getAttribute('for'));
        if (!f) continue;
        if (!map.has(f)) map.set(f, lab);
      }
    } catch {}
    return map;
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  // A <label> contributes a name via its own aria-label/aria-labelledby
  // (checked first, same ARIA-over-content precedence any element's
  // accessible name gives — e.g. <label aria-label="Search"><svg
  // aria-hidden="true">...</svg></label> names its control "Search" even
  // though the label's only child content is aria-hidden) or, failing
  // that, its rendered content (getConservativeSubtreeText).
  function getLabelText(lab) {
    if (helpers.getAriaNameInfo) {
      try {
        const aria = helpers.getAriaNameInfo(lab, ctx);
        if (aria && aria.present && aria.value) return normalizeWs(aria.value);
      } catch {}
    }
    const content = getConservativeSubtreeText(document, lab);
    if (content) return content;
    // Final fallback per the general accname text-alternative algorithm,
    // which applies to any element being asked for its name regardless of
    // why (own aria-label, an aria-labelledby reference, or — here — native
    // <label for> association): title, when nothing else yields a name.
    // Purely additive (only fills in a name where there was none before),
    // so it carries no false-positive risk — see dialog-name-present.js's
    // identical <iframe>-title-fallback fix for the concrete real-world
    // trigger this same accname step covers elsewhere.
    return getAttr(lab, 'title');
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  // Native checkbox/radio without an explicit role belongs to
  // form-control-programmatic-label-present.
  const selector = '[role="checkbox"], [role="radio"], [role="switch"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  // Precompute label[for] associations once for speed/determinism.
  const labelForMap = buildLabelForMap(document);

  function getNativeLabelText(el) {
    // 1) labels API
    try {
      if ('labels' in el && el.labels && el.labels.length) {
        // concatenate conservative text of all associated labels (cap at 4 for determinism)
        const parts = [];
        const max = Math.min(4, el.labels.length);
        for (let i = 0; i < max; i += 1) {
          const lab = el.labels[i];
          const t = lab ? getLabelText(lab) : '';
          if (t) parts.push(t);
        }
        const joined = normalizeWs(parts.join(' '));
        if (joined) return joined;
      }
    } catch {}

    // 2) wrapped by <label>
    try {
      if (el.closest) {
        const wrap = el.closest('label');
        if (wrap) {
          const t = getLabelText(wrap);
          if (t) return t;
        }
      }
    } catch {}

    // 3) label[for=id]
    try {
      const idAttr = getAttr(el, 'id');
      if (idAttr && labelForMap.has(idAttr)) {
        const lab = labelForMap.get(idAttr);
        const t = lab ? getLabelText(lab) : '';
        if (t) return t;
      }
    } catch {}

    return '';
  }

  function evaluate(el, controlType) {
    // Programmatic mechanisms
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // Native input label association (for input[type=checkbox|radio])
    if (controlType === 'checkbox' || controlType === 'radio') {
      const lab = getNativeLabelText(el);
      if (lab) return { ok: true, method: 'label' };
    }

    // ARIA roles (checkbox/radio/switch) can use content as name
    if (
      controlType === 'switch' ||
      controlType === 'aria-checkbox' ||
      controlType === 'aria-radio'
    ) {
      const t = getConservativeSubtreeText(document, el);
      if (t) return { ok: true, method: 'content' };
    }

    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    // Determine control type
    const tag = (el.tagName || '').toLowerCase();
    const type = getAttr(el, 'type').toLowerCase();
    const role = getAttr(el, 'role').toLowerCase();

    let controlType;
    if (tag === 'input' && type === 'checkbox') controlType = 'checkbox';
    else if (tag === 'input' && type === 'radio') controlType = 'radio';
    else if (role === 'checkbox') controlType = 'aria-checkbox';
    else if (role === 'radio') controlType = 'aria-radio';
    else if (role === 'switch') controlType = 'switch';
    else continue;

    applicableCount += 1;

    const res = evaluate(el, controlType);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This control has no accessible name.',
      hint: 'Provide a label, aria-label, aria-labelledby, or other accessible-name mechanism so assistive technologies can identify the control.',
      i18n: {
        summaryKey: 'binaryControlNamePresent_summary_fail',
        hintKey: 'binaryControlNamePresent_hint_fail',
        params: { controlType }
      },
      data: {
        details: {
          reasonCode: 'name_missing',
          controlType,
          methodTried: res.method
        },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "button-name-present": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const occurrences = [];
  let applicableCount = 0;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getConservativeSubtreeText(container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (covers the common "<a><img alt='...'></a>" logo-link /
    // "<button><img alt='...'></button>" icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  function getInputButtonValueName(el) {
    try {
      const type = normalizeWs(el.getAttribute ? el.getAttribute('type') : '').toLowerCase();
      if (type !== 'button' && type !== 'submit' && type !== 'reset') return '';
      const vAttr = el.getAttribute ? el.getAttribute('value') : '';
      return normalizeWs(vAttr != null ? vAttr : typeof el.value === 'string' ? el.value : '');
    } catch {
      return '';
    }
  }

  const selector =
    'button, input[type="button"], input[type="submit"], input[type="reset"], [role="button"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  for (const el of nodes) {
    // isAccTreeEligible returns { eligible, reasons }, not a boolean.
    // Naming rules apply only to elements included in the accessibility tree
    // (ACT c487ae), which excludes focusable aria-hidden content;
    // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
    const eligResult = helpers.isIncludedInAccessibilityTree
      ? helpers.isIncludedInAccessibilityTree(el, ctx)
      : helpers.isAccTreeEligible
        ? helpers.isAccTreeEligible(el, ctx)
        : true;
    const eligible =
      typeof eligResult === 'boolean' ? eligResult : !!(eligResult && eligResult.eligible);
    if (!eligible) continue;

    applicableCount += 1;

    const tag = (el.tagName || '').toLowerCase();
    const role = el.getAttribute ? el.getAttribute('role') : null;
    const roleNorm = normalizeWs(role).toLowerCase();

    const nameInfo = helpers.getAccessibleNameInfo ? helpers.getAccessibleNameInfo(el, ctx) : null;

    // getAccessibleNameInfo only resolves programmatic mechanisms (aria-labelledby,
    // aria-label, native <label> association, title) — it never falls back to
    // subtree content — so it's safe to trust directly whenever present.
    const trustedProgrammaticName = normalizeWs(
      nameInfo && nameInfo.present && typeof nameInfo.value === 'string' ? nameInfo.value : ''
    );
    const explicitProg = !!trustedProgrammaticName;

    let inputValueName = '';
    if (!trustedProgrammaticName && tag === 'input') {
      inputValueName = getInputButtonValueName(el);
    }

    // ARIA 1.2 "Name From: author, contents". Every other known role is
    // name-from-author-only: <button role="combobox">List</button> exposes a
    // value, not a label. An unknown role falls back to the implicit role.
    // <generated:aria-name-from-content>
    const NAME_FROM_CONTENT_ROLES = [
      'button',
      'cell',
      'checkbox',
      'columnheader',
      'doc-backlink',
      'doc-biblioref',
      'doc-glossref',
      'doc-noteref',
      'graphics-object',
      'gridcell',
      'heading',
      'link',
      'menuitem',
      'menuitemcheckbox',
      'menuitemradio',
      'option',
      'radio',
      'row',
      'rowgroup',
      'rowheader',
      'switch',
      'tab',
      'tooltip',
      'treeitem'
    ];
    // </generated:aria-name-from-content>
    const isKnownRoleToken =
      helpers && helpers.aria && typeof helpers.aria.isKnownRole === 'function'
        ? (() => {
            try {
              return !!helpers.aria.isKnownRole(roleNorm);
            } catch {
              return false;
            }
          })()
        : false;
    const isContentNameCandidate =
      (tag === 'button' || role === 'button') &&
      (!roleNorm || !isKnownRoleToken || NAME_FROM_CONTENT_ROLES.includes(roleNorm));
    const contentName =
      !trustedProgrammaticName && !inputValueName && isContentNameCandidate
        ? getConservativeSubtreeText(el)
        : '';

    const finalName = normalizeWs(trustedProgrammaticName || inputValueName || contentName);

    if (!finalName) {
      // Only compute the richer eligibility-info payload (used solely for
      // the occurrence's visibilityFilter) once we know an occurrence is
      // actually being built, rather than for every applicable element.
      const eligInfo = helpers.getEligibilityInfo
        ? helpers.getEligibilityInfo(el, ctx, { targetSet: 'acc' })
        : null;

      const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';

      occurrences.push({
        selector: stableSelector,
        html,
        summary: 'This button has no accessible name.',
        hint: 'Provide visible button text or a programmatic accessible-name mechanism (for example aria-label) so assistive technologies can identify the button.',
        i18n: {
          summaryKey: 'buttonNamePresent_summary_fail',
          hintKey: 'buttonNamePresent_hint_fail',
          params: { element: tag }
        },
        data: {
          visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
          details: {
            reasonCode: 'name_missing',
            metrics: {
              trustedProgrammaticNameLength: trustedProgrammaticName.length,
              inputValueNameLength: inputValueName.length,
              contentNameLength: contentName.length,
              explicitProgrammatic: explicitProg ? 1 : 0
            },
            refs: { accessibleName: nameInfo || null }
          }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "bypass-blocks-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const body = document && document.body ? document.body : null;
  if (!body) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isExposedToAt(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  function queryAll(selector) {
    try {
      return helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart(selector)
        : document.querySelectorAll(selector);
    } catch {
      return [];
    }
  }

  // Filters through isAccTreeEligible (hidden/aria-hidden/display:none/inert
  // don't count as "a bypass mechanism is present") -- see
  // page-has-heading-one-manual.js for the identical real-world trigger
  // (e.g. a page whose only <h1> sits inside a display:none ancestor,
  // unreachable by sighted and screen reader users alike). A fully
  // non-rendered <main>/heading must not be credited here, since that would
  // wrongly treat a page with zero currently-exposed bypass mechanisms as
  // having one.
  function hasMainLandmark() {
    for (const el of queryAll('main, [role="main"]')) {
      if (el && isExposedToAt(el)) return true;
    }
    return false;
  }

  // Resolve a fragment id (or legacy <a name>) inside a specific root node
  // (a Document or a ShadowRoot). Both expose getElementById; querySelector
  // is used for the legacy anchor-name fallback.
  function resolveInRoot(root, fragment) {
    if (!root) return null;
    let target;
    try {
      target = typeof root.getElementById === 'function' ? root.getElementById(fragment) : null;
    } catch {
      target = null;
    }
    if (target) return target;
    try {
      target =
        typeof root.querySelector === 'function'
          ? root.querySelector('a[name="' + fragment.replace(/"/g, '\\"') + '"]')
          : null;
    } catch {
      target = null;
    }
    return target;
  }

  // Shadow-DOM-aware: gather anchors via queryAllSmart (pierces shadow roots
  // when includeShadowDom is enabled, and drops hard-hidden links), and
  // resolve each fragment in the link's own root before falling back to the
  // document. This credits a skip link encapsulated in a web component the
  // same way as one authored in the light DOM.
  function hasWorkingAnchorLink() {
    let links;
    try {
      links =
        helpers && typeof helpers.queryAllSmart === 'function'
          ? helpers.queryAllSmart('a[href]')
          : document.querySelectorAll('a[href]');
    } catch {
      links = [];
    }
    for (const a of links) {
      if (!a || !a.getAttribute) continue;
      const href = String(a.getAttribute('href') || '').trim();
      if (href.length < 2 || href.charAt(0) !== '#') continue;
      let fragment = href.slice(1);
      try {
        fragment = decodeURIComponent(fragment);
      } catch {
        // keep raw fragment if it isn't a valid percent-encoded string
      }
      fragment = fragment.trim();
      if (!fragment) continue;

      let root = document;
      try {
        if (typeof a.getRootNode === 'function') {
          const r = a.getRootNode();
          if (r) root = r;
        }
      } catch {
        root = document;
      }

      let target = resolveInRoot(root, fragment);
      if (!target && root !== document) {
        target = resolveInRoot(document, fragment);
      }
      if (target) return true;
    }
    return false;
  }

  function hasHeading() {
    for (const el of queryAll('h1, h2, h3, h4, h5, h6, [role="heading"]')) {
      if (el && isExposedToAt(el)) return true;
    }
    return false;
  }

  const mainLandmark = hasMainLandmark();
  const anchorLink = mainLandmark ? false : hasWorkingAnchorLink();
  const heading = mainLandmark || anchorLink ? false : hasHeading();

  // A recognized mechanism is present -> nothing to review on this page.
  if (mainLandmark || anchorLink || heading) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const stableSelector = helpers.buildSelector ? helpers.buildSelector(body) : 'body';
  const html = helpers.getOuterHtmlSnippet
    ? helpers.getOuterHtmlSnippet(body)
    : (body.outerHTML || '').slice(0, 200);

  const occurrences = [
    {
      selector: stableSelector,
      html,
      summary:
        'No recognized way to bypass repeated blocks of content was detected on this page — verify a bypass mechanism exists.',
      hint: 'Confirm the page offers a bypass mechanism: a main landmark (<main> or role="main"), a working "skip to content" link, or heading elements that assistive technology can use to jump past repeated content. (A mechanism may be temporarily hidden — e.g. while a modal dialog makes the page inert — or provided on a per-site basis; this needs human confirmation.)',
      i18n: {
        summaryKey: 'bypassBlocksPresent_summary_cantTell',
        hintKey: 'bypassBlocksPresent_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'BYPASS_MECHANISM_ABSENT' },
        visibilityFilter: { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    }
  ];

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'moderate',
    occurrences
  };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "canvas-text-alternative-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  // aria-hidden removes the canvas from the accessibility tree, so a text
  // alternative on it cannot reach anyone; aria-hidden-focus reports a
  // focusable one. Matches the other non-text-content rules.
  const isEligibleHelper =
    helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
      ? helpers.isIncludedInAccessibilityTree
      : helpers && typeof helpers.isAccTreeEligible === 'function'
        ? helpers.isAccTreeEligible
        : null;

  const getTextAlternativeInfo =
    helpers && typeof helpers.getTextAlternativeInfo === 'function'
      ? helpers.getTextAlternativeInfo
      : null;

  const canvases = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('canvas') : queryAll('canvas')) || []);
    } catch {
      return queryAll('canvas');
    }
  })();

  if (!canvases.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of canvases) {
    if (!el) continue;

    // Applicability: included in the accessibility tree.
    if (isEligibleHelper) {
      const elig = (() => {
        try {
          return isEligibleHelper(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig === false) continue;
      if (elig && elig.eligible === false) continue;
    }

    applicableCount += 1;

    // Expectation: must provide a text alternative.
    const ti = getTextAlternativeInfo
      ? (() => {
          try {
            return getTextAlternativeInfo(el, ctx);
          } catch {
            return null;
          }
        })()
      : null;

    const hasTextAlt = !!(ti && ti.present);

    if (hasTextAlt) continue;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      selector: '',
      html: '',
      summary: 'Missing text alternative for <canvas>.',
      hint: 'Provide fallback text inside <canvas> or an accessible name (e.g., aria-label/aria-labelledby).',
      i18n: {
        summaryKey: 'canvas_textAltPresent_summary_fail',
        hintKey: 'canvas_textAltPresent_hint_fail',
        params: { element: 'canvas' }
      },
      data: {
        // Always log eligibility/filter info (engine contract for targetSet=acc checks)
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        // Debuggable, deterministic helper facts (non-verdict)
        textAlternative: ti || null
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      // Never compute selector/snippet in the rule.
      occurrences.push({ ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "canvas-text-alternative-quality": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const __accEligCache = new WeakMap();
  function accEligibleCached(node) {
    if (!isAccTreeEligible) return { eligible: true, reasons: [] };
    if (!node || typeof node !== 'object') return { eligible: true, reasons: [] };
    const c = __accEligCache.get(node);
    if (c) return c;
    let r;
    try {
      r = isAccTreeEligible(node, ctx);
    } catch {
      r = { eligible: true, reasons: [] };
    }
    r = r && typeof r === 'object' ? r : { eligible: !!r, reasons: [] };
    __accEligCache.set(node, r);
    return r;
  }

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      let tabindex;
      try {
        tabindex = el.getAttribute('tabindex');
      } catch {
        tabindex = null;
      }
      const t = tabindex == null ? '' : String(tabindex).trim();
      focusable = t !== '' && !Number.isNaN(Number(t));
    }
    return !focusable;
  }

  const els = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('canvas') : queryAll('canvas')) || []);
    } catch {
      return queryAll('canvas');
    }
  })();

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = accEligibleCached(el);
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    const getTextAlternativeInfo =
      helpers && typeof helpers.getTextAlternativeInfo === 'function'
        ? helpers.getTextAlternativeInfo
        : null;

    const trim = (v) => (v == null ? '' : String(v)).trim();

    // Rule-specific applicability (only elements that already have a text alternative mechanism)
    let textAltInfo = null;
    if (getTextAlternativeInfo) {
      try {
        textAltInfo = getTextAlternativeInfo(el, ctx);
      } catch {
        textAltInfo = null;
      }
    }

    const hasTextAlt = !!(textAltInfo && textAltInfo.present);
    if (!hasTextAlt) continue;

    applicableCount += 1;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Review text alternative for <canvas> for equivalence and appropriateness.',
      hint: 'Confirm the fallback text or accessible name conveys the same information/function as the canvas content.',
      i18n: {
        summaryKey: 'canvas_textAltQuality_summary_cantTell',
        hintKey: 'canvas_textAltQuality_hint_cantTell',
        params: { element: (el.tagName || '').toLowerCase() }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: textAltInfo || null
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "combobox-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function buildLabelForMap(doc) {
    const map = new Map(); // id -> label element (first)
    try {
      const labels = doc && doc.getElementsByTagName ? doc.getElementsByTagName('label') : [];
      for (let i = 0; i < labels.length; i += 1) {
        const lab = labels[i];
        if (!lab || !lab.getAttribute) continue;
        const f = normalizeWs(lab.getAttribute('for'));
        if (!f) continue;
        if (!map.has(f)) map.set(f, lab);
      }
    } catch {}
    return map;
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  // A <label> contributes a name via its own aria-label/aria-labelledby
  // (checked first, same ARIA-over-content precedence any element's
  // accessible name gives — e.g. <label aria-label="Search"><svg
  // aria-hidden="true">...</svg></label> names its control "Search" even
  // though the label's only child content is aria-hidden) or, failing
  // that, its rendered content (getConservativeSubtreeText).
  function getLabelText(lab) {
    if (helpers.getAriaNameInfo) {
      try {
        const aria = helpers.getAriaNameInfo(lab, ctx);
        if (aria && aria.present && aria.value) return normalizeWs(aria.value);
      } catch {}
    }
    const content = getConservativeSubtreeText(document, lab);
    if (content) return content;
    // Final fallback per the general accname text-alternative algorithm,
    // which applies to any element being asked for its name regardless of
    // why (own aria-label, an aria-labelledby reference, or — here — native
    // <label for> association): title, when nothing else yields a name.
    // Purely additive (only fills in a name where there was none before),
    // so it carries no false-positive risk — see dialog-name-present.js's
    // identical <iframe>-title-fallback fix for the concrete real-world
    // trigger this same accname step covers elsewhere.
    return getAttr(lab, 'title');
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="combobox"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  // Precompute label[for] map for combobox elements that are labelable
  // native form controls (e.g. <input role="combobox">).
  const labelForMap = buildLabelForMap(document);

  function getNativeLabelText(el) {
    try {
      if ('labels' in el && el.labels && el.labels.length) {
        const parts = [];
        const max = Math.min(4, el.labels.length);
        for (let i = 0; i < max; i += 1) {
          const lab = el.labels[i];
          const t = lab ? getLabelText(lab) : '';
          if (t) parts.push(t);
        }
        const joined = normalizeWs(parts.join(' '));
        if (joined) return joined;
      }
    } catch {}
    try {
      if (el.closest) {
        const wrap = el.closest('label');
        if (wrap) {
          const t = getLabelText(wrap);
          if (t) return t;
        }
      }
    } catch {}
    try {
      const idAttr = getAttr(el, 'id');
      if (idAttr && labelForMap.has(idAttr)) {
        const lab = labelForMap.get(idAttr);
        const t = lab ? getLabelText(lab) : '';
        if (t) return t;
      }
    } catch {}
    return '';
  }

  function evaluate(el, controlType) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // Native <label> association (e.g. <input role="combobox">).
    const lab = getNativeLabelText(el);
    if (lab) return { ok: true, method: 'label' };

    // role="combobox" is name-from-author-only per WAI-ARIA: it must NOT
    // fall back to subtree content. Visible text near/inside a custom
    // combobox widget is not reliably exposed as its accessible name.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    const role = getAttr(el, 'role').toLowerCase();
    if (role !== 'combobox') continue;

    applicableCount += 1;

    const res = evaluate(el, 'combobox');
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This combobox has no accessible name.',
      hint: "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this combobox's accessible name.",
      i18n: {
        summaryKey: 'comboboxNamePresent_summary_fail',
        hintKey: 'comboboxNamePresent_hint_fail',
        params: {}
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'combobox', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "contrast-computable": { run: (function runInPage(ctx) {
  const { helpers, rule, engineOptions } = ctx;

  const __contrastSharedCache =
    helpers && helpers.contrast && helpers.contrast.sharedCache
      ? helpers.contrast.sharedCache
      : null;

  const contrast =
    engineOptions && typeof engineOptions.contrast === 'object' && engineOptions.contrast
      ? engineOptions.contrast
      : {};

  const mode = contrast.mode === 'auditorAssist' ? 'auditorAssist' : 'strictConformance';

  const rootCanvasFallback =
    typeof contrast.rootCanvasFallback === 'string' && contrast.rootCanvasFallback.trim()
      ? contrast.rootCanvasFallback.trim()
      : '#ffffff';

  const MAX_OCCURRENCES = 50;

  const occurrences = [];
  let eligibleTextCount = 0;
  let cantTellCount = 0;
  let assumptionsCount = 0;
  const assumptionsAppliedSet = new Set();

  const seenFailEls = new Set();

  function pushCantTellOccurrence(el, reasonCode, extraDetails) {
    try {
      if (!el || seenFailEls.has(el)) return;
      if (occurrences.length >= MAX_OCCURRENCES) return;
      seenFailEls.add(el);

      const rc = String(reasonCode || 'UNKNOWN');

      // Match test contract: choose a specific summaryKey per computability blocker.
      // Fall back to the generic notComputable key.
      let summaryKey = 'contrastComputable_cantTell_notComputable';
      if (rc === 'BACKGROUND_IMAGE_OR_GRADIENT') {
        const t =
          extraDetails && typeof extraDetails === 'object'
            ? String(extraDetails.backgroundFillType || '')
            : '';

        if (t === 'image') summaryKey = 'contrastComputable_cantTell_bgImage';
        else if (t === 'gradient') summaryKey = 'contrastComputable_cantTell_bgGradient';
        else if (t === 'imageAndGradient')
          summaryKey = 'contrastComputable_cantTell_bgImageAndGradient';
        else summaryKey = 'contrastComputable_cantTell_bgImageOrGradient';
      } else if (rc === 'MIX_BLEND_MODE') summaryKey = 'contrastComputable_cantTell_mixBlendMode';
      else if (rc === 'BACKGROUND_FILTER_OR_BACKDROP_FILTER') {
        const bp =
          extraDetails && typeof extraDetails === 'object'
            ? String(extraDetails.blockerProperty || '')
            : '';
        if (bp === 'filter') summaryKey = 'contrastComputable_cantTell_filter';
        else if (bp === 'backdrop-filter')
          summaryKey = 'contrastComputable_cantTell_backdropFilter';
        else summaryKey = 'contrastComputable_cantTell_filterOrBackdropFilter';
      } else if (rc === 'BACKGROUND_NOT_OPAQUE_AT_ROOT')
        summaryKey = 'contrastComputable_cantTell_rootNotOpaque';

      const details = Object.assign(
        { reasonCode: rc },
        extraDetails && typeof extraDetails === 'object' ? extraDetails : {}
      );

      const occBase = {
        selector: '',
        html: '',
        summary: '',
        hint: '',
        i18n: {
          summaryKey,
          hintKey: '',
          params: Object.assign(
            { reasonCode: rc },
            details && typeof details === 'object'
              ? {
                  blockerProperty: details.blockerProperty ? String(details.blockerProperty) : '',
                  blockerValue: details.blockerValue ? String(details.blockerValue) : '',
                  backgroundAlpha:
                    details.backgroundAlpha !== undefined && details.backgroundAlpha !== null
                      ? String(details.backgroundAlpha)
                      : ''
                }
              : {}
          )
        },
        data: { details }
      };

      if (helpers && typeof helpers.reportOccurrence === 'function') {
        occurrences.push(helpers.reportOccurrence(el, occBase));
      } else {
        // Never compute selector/snippet in the rule.
        occurrences.push({ ...occBase });
      }
    } catch {
      // no-throw
    }
  }

  // perf: cache per-element analysis so multiple text nodes in same element don't repeat expensive work
  const __elBlockerCache = __contrastSharedCache
    ? __contrastSharedCache.__elBlockerCache ||
      (__contrastSharedCache.__elBlockerCache = new WeakMap())
    : new WeakMap(); // Element -> { ok:boolean, reasonCode, blockerSelector, blockerProperty, blockerValue }
  const __elBgCache = __contrastSharedCache
    ? __contrastSharedCache.__elBgCache || (__contrastSharedCache.__elBgCache = new WeakMap())
    : new WeakMap(); // Element -> { ok, rgba, alpha, reasonCode } (no stack)
  const __elFgCache = __contrastSharedCache
    ? __contrastSharedCache.__elFgCache || (__contrastSharedCache.__elFgCache = new WeakMap())
    : new WeakMap(); // Element -> { rgba, alpha, opacityProduct }

  // perf: fast-path memo for self-opaque background (common) to avoid ancestor walk
  const __elBgSelfOpaqueCache = __contrastSharedCache
    ? __contrastSharedCache.__elBgSelfOpaqueCache ||
      (__contrastSharedCache.__elBgSelfOpaqueCache = new WeakMap())
    : new WeakMap(); // Element -> { ok, rgba, alpha, reasonCode }

  // Shared deterministic text scan (computed once per run and reused across contrast checks)
  let scan;
  try {
    scan =
      helpers && helpers.contrast && typeof helpers.contrast.getTextScan === 'function'
        ? helpers.contrast.getTextScan(ctx, helpers, engineOptions)
        : null;
  } catch {
    scan = null;
  }

  // Walk eligible visible text nodes (counted per text node), but compute blockers/bg/fg once per element.
  if (scan && scan.elements && Array.isArray(scan.elements)) {
    try {
      eligibleTextCount = Number(scan.eligibleTextCount) || 0;

      for (const rec of scan.elements) {
        // If we're already capped on occurrences and already know the final outcome is cantTell,
        // stop scanning to avoid wasting time. Deterministic: we don't randomize; we just stop
        // once further work cannot change the output (cantTell + capped occurrences).
        if (cantTellCount > 0 && occurrences.length >= MAX_OCCURRENCES) break;

        const el = rec && rec.el;
        const textCount = rec && Number(rec.textCount) ? Number(rec.textCount) : 0;
        if (!el || textCount <= 0) continue;

        // 1) Blockers in ancestor chain (blend/filter/bg-image/gradient)
        let blocker = __elBlockerCache.get(el);
        if (!blocker) {
          blocker = helpers.contrast.getComputabilityBlocker(el);
          // Normalize to stable shape
          blocker = blocker || {
            ok: true,
            reasonCode: null,
            blockerSelector: '',
            blockerProperty: '',
            blockerValue: ''
          };
          __elBlockerCache.set(el, blocker);
        }
        if (blocker && blocker.ok === false) {
          cantTellCount += textCount;
          // occurrence is deduped per element (seenFailEls)
          pushCantTellOccurrence(el, blocker.reasonCode, {
            blockerProperty: blocker.blockerProperty,
            blockerValue: blocker.blockerValue,
            blockerSelector: blocker.blockerSelector,
            backgroundFillType: blocker.backgroundFillType
          });
          continue;
        }

        // 2) Background resolution (CSS-only), strict vs. reference-engine-compatible mode
        let bg = __elBgCache.get(el);
        if (!bg) {
          // 2a) Fast-path: if the element itself paints an opaque background-color and opacity is 1,
          // then the background behind its text is computable without walking ancestors.
          // This is a strict subset of the full algorithm, so it is behavior-preserving:
          // if it doesn't match, we fall back to computeEffectiveBackground.
          let bgSelfOpaque = __elBgSelfOpaqueCache.get(el);
          if (bgSelfOpaque === undefined) {
            bgSelfOpaque = null;
            try {
              if (
                helpers &&
                typeof helpers.getComputedStyle === 'function' &&
                helpers.contrast &&
                typeof helpers.contrast.parseCssColorToRgba === 'function'
              ) {
                const cs = helpers.getComputedStyle(el);
                const op = Number.parseFloat(cs && cs.opacity != null ? cs.opacity : '1');
                // Only treat as self-opaque if opacity is exactly 1 (string '1' or number 1)
                // to avoid float/serialization quirks and preserve determinism.
                if (Number.isFinite(op) && op === 1) {
                  const c = helpers.contrast.parseCssColorToRgba(cs && cs.backgroundColor);
                  if (c && typeof c.a === 'number' && c.a === 1) {
                    bgSelfOpaque = {
                      ok: true,
                      rgba: { r: c.r, g: c.g, b: c.b, a: 1 },
                      alpha: 1,
                      reasonCode: null
                    };
                  }
                }
              }
            } catch {
              bgSelfOpaque = null;
            }
            __elBgSelfOpaqueCache.set(el, bgSelfOpaque);
          }

          if (bgSelfOpaque && bgSelfOpaque.ok === true) {
            bg = bgSelfOpaque;
          } else {
            bg = helpers.contrast.computeEffectiveBackground(el, {
              contrast: { mode, rootCanvasFallback },
              collectStack: false
            });
            bg = bg || { ok: false, reasonCode: 'BACKGROUND_NOT_COMPUTABLE', rgba: null, alpha: 0 };
          }

          __elBgCache.set(el, bg);
        }
        if (!bg || bg.ok === false) {
          cantTellCount += textCount;
          pushCantTellOccurrence(el, (bg && bg.reasonCode) || 'BACKGROUND_NOT_COMPUTABLE', {
            background: bg && bg.rgba ? helpers.contrast.rgbaToString(bg.rgba) : '',
            backgroundAlpha:
              bg && typeof bg.alpha === 'number' ? helpers.contrast.round2(bg.alpha) : ''
          });
          continue;
        }

        // Track any explicit assumptions applied by background resolution.
        try {
          if (bg && Array.isArray(bg.assumptionsApplied) && bg.assumptionsApplied.length) {
            assumptionsCount += textCount;
            for (const a of bg.assumptionsApplied) assumptionsAppliedSet.add(String(a));
          }
        } catch {
          /* no-throw */
        }

        // 3) Foreground parsability (computed color should be rgb/rgba)
        let fg = __elFgCache.get(el);
        if (!fg) {
          fg = helpers.contrast.computeEffectiveForeground(el);
          fg = fg || { rgba: null, alpha: 0, opacityProduct: 1 };
          __elFgCache.set(el, fg);
        }
        if (!fg || !fg.rgba) {
          cantTellCount += textCount;
          pushCantTellOccurrence(el, 'FOREGROUND_UNPARSABLE', {
            background: bg && bg.rgba ? helpers.contrast.rgbaToString(bg.rgba) : '',
            backgroundAlpha:
              bg && typeof bg.alpha === 'number' ? helpers.contrast.round2(bg.alpha) : ''
          });
          continue;
        }

        // If we got here, this element's eligible text is computable.
        // (We intentionally do not compute contrast ratio in this gatekeeper rule.)
      }
    } catch {
      // If scan processing fails unexpectedly, keep determinism: treat as cantTell with one occurrence
      return {
        ruleId: rule.ruleId,
        outcome: 'cantTell',
        severity: rule.defaultSeverity || 'serious',
        confidence: rule.defaultConfidence || 'high',
        occurrences: [
          {
            selector: '',
            summary: '',
            hint: '',
            html: '',
            i18n: {
              summaryKey: 'contrastComputable_cantTell_engineFailure',
              hintKey: '',
              params: { reasonCode: 'ENGINE_EXCEPTION' }
            },
            data: { details: { reasonCode: 'ENGINE_EXCEPTION' } }
          }
        ]
      };
    }
  }

  if (eligibleTextCount === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: rule.defaultSeverity || 'serious',
      confidence: rule.defaultConfidence || 'high',
      occurrences: []
    };
  }

  if (cantTellCount > 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'serious',
      confidence: rule.defaultConfidence || 'high',
      occurrences
    };
  }

  // All eligible text is computable
  return {
    ruleId: rule.ruleId,
    outcome: 'pass',
    severity: rule.defaultSeverity || 'serious',
    confidence: rule.defaultConfidence || 'high',
    occurrences: [
      {
        selector: '',
        summary: '',
        hint: '',
        html: '',
        i18n: {
          summaryKey: 'contrastComputable_pass_allComputable',
          hintKey: '',
          params: {
            eligibleTextCount: String(eligibleTextCount)
          }
        },
        data: {
          details: {
            eligibleTextCount,
            assumptionsCount,
            assumptionsApplied: Array.from(assumptionsAppliedSet).sort()
          }
        }
      }
    ]
  };
}), applicability: null },
    "contrast-enhanced": { run: (function runInPage(ctx) {
  const { helpers, rule, engineOptions } = ctx;

  function toElement(node) {
    try {
      if (!node) return null;
      if (node.nodeType === 1) return node; // ELEMENT_NODE
      if (node.nodeType === 3) return node.parentElement || null; // TEXT_NODE
      return null;
    } catch {
      return null;
    }
  }

  const __contrastSharedCache =
    helpers && helpers.contrast && helpers.contrast.sharedCache
      ? helpers.contrast.sharedCache
      : null;
  const __elBlockerCache = __contrastSharedCache
    ? __contrastSharedCache.__elBlockerCache ||
      (__contrastSharedCache.__elBlockerCache = new WeakMap())
    : null;

  const __elBgCache = __contrastSharedCache
    ? __contrastSharedCache.__elBgCache || (__contrastSharedCache.__elBgCache = new WeakMap())
    : null;

  const __elFgCache = __contrastSharedCache
    ? __contrastSharedCache.__elFgCache || (__contrastSharedCache.__elFgCache = new WeakMap())
    : null;

  const __elFontCache = __contrastSharedCache
    ? __contrastSharedCache.__elFontCache || (__contrastSharedCache.__elFontCache = new WeakMap())
    : new WeakMap();

  function safeComputedStyle(el) {
    try {
      if (!el || el.nodeType !== 1) return null;

      // Prefer engine helper (matches checks/engine behavior + may be cached)
      if (helpers && typeof helpers.computedStyle === 'function') {
        const cs = helpers.computedStyle(el);
        if (cs) return cs;
      }

      const view =
        el.ownerDocument && el.ownerDocument.defaultView ? el.ownerDocument.defaultView : null;

      if (view && typeof view.getComputedStyle === 'function') return view.getComputedStyle(el);
    } catch {}
    return null;
  }

  function getFontInfo(el) {
    try {
      if (!el || el.nodeType !== 1) {
        return {
          fontSizePx: 0,
          fontSizePt: '',
          fontWeightNum: 400,
          fontWeight: 'normal',
          isBold: false,
          isLarge: false,
          isLargeText: false
        };
      }

      const cached = __elFontCache.get(el);
      if (cached) return cached;

      const cs = safeComputedStyle(el);
      const fontSizePx = cs ? helpers.contrast.parsePx(cs.fontSize) : null;
      const fontWeightNum = cs ? helpers.contrast.normalizeFontWeight(cs.fontWeight) : 400;

      const sizePx = Number.isFinite(fontSizePx) ? fontSizePx : 0;
      const isBold = Number.isFinite(fontWeightNum) && fontWeightNum >= 700;
      const isLarge = helpers.contrast.isLargeText(sizePx, fontWeightNum);

      const out = {
        fontSizePx: sizePx,
        fontSizePt: helpers.contrast.pxToPt(sizePx),
        fontWeightNum,
        fontWeight: helpers.contrast.fontWeightLabel(fontWeightNum),
        isBold,
        isLarge,
        isLargeText: isLarge
      };

      __elFontCache.set(el, out);
      return out;
    } catch {
      return {
        fontSizePx: 0,
        fontSizePt: '',
        fontWeightNum: 400,
        fontWeight: 'normal',
        isBold: false,
        isLarge: false,
        isLargeText: false
      };
    }
  }

  const contrast =
    engineOptions && typeof engineOptions.contrast === 'object' && engineOptions.contrast
      ? engineOptions.contrast
      : {};

  const mode = contrast.mode === 'auditorAssist' ? 'auditorAssist' : 'strictConformance';

  const rootCanvasFallback =
    typeof contrast.rootCanvasFallback === 'string' && contrast.rootCanvasFallback.trim()
      ? contrast.rootCanvasFallback.trim()
      : '#ffffff';

  const MAX_OCCURRENCES = 50;

  const occurrences = [];
  let eligibleTextCount = 0;
  let computableTextCount = 0;
  let failCount = 0;

  const seenFailEls = new Set();

  function pushPassOccurrence(eligibleCount, computableCount) {
    try {
      occurrences.push({
        selector: '',
        summary: 'All computable text meets the enhanced (AAA) contrast threshold.',
        hint: '',
        html: '',
        i18n: {
          summaryKey: 'contrastEnhanced_pass_allAboveThreshold',
          hintKey: '',
          params: {
            eligibleTextCount: String(Number(eligibleCount) || 0),
            computableTextCount: String(Number(computableCount) || 0)
          }
        },
        data: {
          details: {
            reasonCode: 'ALL_ABOVE_THRESHOLD',
            eligibleTextCount: Number(eligibleCount) || 0,
            computableTextCount: Number(computableCount) || 0,
            metrics: {
              eligibleTextCount: Number(eligibleCount) || 0,
              computableTextCount: Number(computableCount) || 0
            }
          }
        }
      });
    } catch {
      // no-throw
    }
  }

  function pushFailOccurrence(el, params, details) {
    try {
      if (!el || seenFailEls.has(el)) return;
      if (occurrences.length >= MAX_OCCURRENCES) return;

      seenFailEls.add(el);

      const det = details && typeof details === 'object' ? details : { reasonCode: 'UNKNOWN' };

      const occBase = {
        selector: '',
        html: '',
        summary: '',
        hint: '',
        i18n: {
          summaryKey: 'contrastEnhanced_fail_belowThreshold',
          hintKey: '',
          params: params && typeof params === 'object' ? params : {}
        },
        data: { details: det }
      };

      // Bind the occurrence to an element deterministically, but still let the engine
      // attach canonical selector + HTML snippet via reportOccurrence when available.
      let nodeSelector = '';
      try {
        const elementId =
          el && typeof el.getAttribute === 'function' ? el.getAttribute('id') || '' : '';
        if (elementId) nodeSelector = `#${elementId}`;
      } catch {
        // no-throw
      }
      const tagName = el && el.tagName ? String(el.tagName).toLowerCase() : 'element';

      let occ = { ...occBase };

      // IMPORTANT: this is what populates occ.selector and occ.html (snippet)
      if (helpers && typeof helpers.reportOccurrence === 'function') {
        try {
          const reported = helpers.reportOccurrence(el, occBase);
          if (reported && typeof reported === 'object') occ = reported;
        } catch {
          // no-throw
        }
      }

      occ.selector = occ.selector || nodeSelector || '';

      occ.data = occ.data || {};
      occ.data.details = occ.data.details || {};
      occ.data.details.node = { selector: nodeSelector, tagName };

      occurrences.push(occ);
    } catch {
      // no-throw
    }
  }

  // perf: per-element analysis cache (per run)
  let __elAnalysisCache = new WeakMap();
  if (__contrastSharedCache) {
    try {
      if (!__contrastSharedCache.__elAnalysisCacheAAA)
        __contrastSharedCache.__elAnalysisCacheAAA = new WeakMap();
      __elAnalysisCache = __contrastSharedCache.__elAnalysisCacheAAA;
    } catch {
      __elAnalysisCache = new WeakMap();
    }
  }

  // Shared deterministic text scan (computed once per run and reused across contrast checks)
  let scan;
  try {
    scan =
      helpers && helpers.contrast && typeof helpers.contrast.getTextScan === 'function'
        ? helpers.contrast.getTextScan(ctx, helpers, engineOptions)
        : null;
  } catch {
    scan = null;
  }

  // Walk eligible visible text nodes (counted per text node), but compute expensive analysis once per element.
  if (scan && scan.elements && Array.isArray(scan.elements)) {
    try {
      eligibleTextCount = Number(scan.eligibleTextCount) || 0;

      for (const rec of scan.elements) {
        const el = toElement(rec && rec.el);
        const textCount = rec && Number(rec.textCount) ? Number(rec.textCount) : 0;
        if (!el || textCount <= 0) continue;

        // Computability + contrast analysis (cached per element)
        let analysis = __elAnalysisCache.get(el);
        if (!analysis) {
          // Computability gate (do NOT emit cantTell here; Rule 1 is responsible for that)
          let blocker = __elBlockerCache ? __elBlockerCache.get(el) : null;
          if (!blocker) {
            blocker = helpers.contrast.getComputabilityBlocker(el);
            blocker = blocker || {
              ok: true,
              reasonCode: null,
              blockerSelector: '',
              blockerProperty: '',
              blockerValue: ''
            };
            if (__elBlockerCache) __elBlockerCache.set(el, blocker);
          }

          if (blocker && blocker.ok === false) {
            analysis = { computable: false };
          } else {
            let bg = __elBgCache ? __elBgCache.get(el) : null;
            if (!bg) {
              bg = helpers.contrast.computeEffectiveBackground(el, {
                contrast: { mode, rootCanvasFallback },
                collectStack: false
              });
              bg = bg || {
                ok: false,
                reasonCode: 'BACKGROUND_NOT_COMPUTABLE',
                rgba: null,
                alpha: 0
              };
              if (__elBgCache) __elBgCache.set(el, bg);
            }

            const bgAssumptionsApplied =
              bg && Array.isArray(bg.assumptionsApplied) && bg.assumptionsApplied.length
                ? bg.assumptionsApplied.slice(0)
                : null;
            const bgAssumedRootCanvasColor =
              bg && typeof bg.assumedRootCanvasColor === 'string'
                ? bg.assumedRootCanvasColor
                : null;

            let fg = __elFgCache ? __elFgCache.get(el) : null;
            if (!fg) {
              fg = helpers.contrast.computeEffectiveForeground(el);
              fg = fg || { rgba: null, alpha: 0, opacityProduct: 1 };
              if (__elFgCache) __elFgCache.set(el, fg);
            }

            if (!bg || bg.ok === false || !bg.rgba || !fg || !fg.rgba) {
              analysis = { computable: false };
            } else {
              // Compose FG over BG if FG has alpha (effective fg may be < 1 due to opacity chain)
              const fgOpaque =
                fg.rgba.a != null && fg.rgba.a < 1
                  ? helpers.contrast.compositeRgba(fg.rgba, bg.rgba)
                  : { r: fg.rgba.r, g: fg.rgba.g, b: fg.rgba.b, a: 1 };

              const bgOpaque = { r: bg.rgba.r, g: bg.rgba.g, b: bg.rgba.b, a: 1 };

              const ratio = helpers.contrast.contrastRatio(fgOpaque, bgOpaque);

              const font = getFontInfo(el);
              const threshold = helpers.contrast.requiredRatio('AAA', font.isLargeText);

              analysis = {
                computable: true,
                ratio,
                bgAssumptionsApplied,
                bgAssumedRootCanvasColor,
                ratioStr: helpers.contrast.round2(ratio),
                threshold,
                thresholdStr: `${threshold}`,
                font,
                fgOpaque,
                bgOpaque
              };
            }
          }
          __elAnalysisCache.set(el, analysis);
        }

        if (!analysis || analysis.computable !== true) continue;

        computableTextCount += textCount;

        const ratio = analysis.ratio;
        const font = analysis.font;
        const threshold = analysis.threshold;
        const ratioStr = analysis.ratioStr;
        const thresholdStr = analysis.thresholdStr;
        const fgOpaque = analysis.fgOpaque;
        const bgOpaque = analysis.bgOpaque;

        if (!(ratio >= threshold)) {
          failCount += textCount;

          const fgHex = helpers.contrast.rgbToHex ? helpers.contrast.rgbToHex(fgOpaque) : '';

          const bgHex = helpers.contrast.rgbToHex ? helpers.contrast.rgbToHex(bgOpaque) : '';

          const fontPxNum = font.fontSizePx ? parseFloat(font.fontSizePx) : NaN;

          const fontPtStr =
            helpers.contrast.pxToPt && Number.isFinite(fontPxNum)
              ? helpers.contrast.pxToPt(fontPxNum)
              : '';

          const fwNum = Number(font.fontWeightNum);
          const fwLabel = helpers.contrast.fontWeightLabel
            ? helpers.contrast.fontWeightLabel(fwNum)
            : font.isBold
              ? 'bold'
              : 'normal';

          const fgRgbaStr = helpers.contrast.rgbaToString(fgOpaque);
          const bgRgbaStr = helpers.contrast.rgbaToString(bgOpaque);

          const params = {
            reasonCode: 'BELOW_THRESHOLD',

            foreground: fgRgbaStr,
            background: bgRgbaStr,

            foregroundHex: fgHex,
            backgroundHex: bgHex,
            fontSizePt: fontPtStr,
            fontWeightLabel: fwLabel,

            ratio: ratioStr,
            threshold: thresholdStr,

            fontSizePx: font.fontSizePx,
            fontWeight: font.fontWeight,
            isBold: font.isBold,
            isLargeText: font.isLargeText
          };

          const assumptionsApplied =
            analysis &&
            Array.isArray(analysis.bgAssumptionsApplied) &&
            analysis.bgAssumptionsApplied.length
              ? analysis.bgAssumptionsApplied.slice(0)
              : null;
          const assumedRootCanvasColor =
            analysis && typeof analysis.bgAssumedRootCanvasColor === 'string'
              ? analysis.bgAssumedRootCanvasColor
              : null;

          const details = {
            reasonCode: 'BELOW_THRESHOLD',
            metrics: { ratio, threshold },
            typography: {
              fontSizePx: Number.isFinite(fontPxNum) ? fontPxNum : null,
              fontSizePt: Number.isFinite(fontPxNum) ? fontPxNum * 0.75 : null,
              fontWeight: Number.isFinite(fwNum) ? fwNum : null,
              fontWeightLabel: fwLabel,
              isBold: !!font.isBold,
              isLargeText: !!font.isLargeText
            },
            colors: {
              foregroundHex: fgHex,
              backgroundHex: bgHex,
              foregroundRgba: fgRgbaStr,
              backgroundRgba: bgRgbaStr
            },
            assumptionsApplied: assumptionsApplied,
            assumedRootCanvasColor: assumedRootCanvasColor
          };

          pushFailOccurrence(el, params, details);

          if (occurrences.length >= MAX_OCCURRENCES) break;
        }
      }
    } catch {
      // No-throw: deterministic cantTell on internal failure
      return {
        ruleId: rule.ruleId,
        outcome: 'cantTell',
        severity: rule.defaultSeverity || 'serious',
        confidence: rule.defaultConfidence || 'high',
        occurrences: [
          {
            selector: '',
            summary: '',
            hint: '',
            html: '',
            i18n: {
              summaryKey: 'contrastEnhanced_cantTell_engineFailure',
              hintKey: '',
              params: { reasonCode: 'ENGINE_EXCEPTION' }
            },
            data: { details: { reasonCode: 'ENGINE_EXCEPTION' } }
          }
        ]
      };
    }
  }
  if (eligibleTextCount === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: rule.defaultSeverity || 'serious',
      confidence: rule.defaultConfidence || 'high',
      occurrences: []
    };
  }

  // If eligible text exists but none is computable, stay notApplicable; Rule 1 reports cantTell.
  if (computableTextCount === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: rule.defaultSeverity || 'serious',
      confidence: rule.defaultConfidence || 'high',
      occurrences: [
        {
          selector: '',
          summary: '',
          hint: '',
          html: '',
          i18n: {
            summaryKey: 'contrastEnhanced_notApplicable_noComputableText',
            hintKey: '',
            params: { eligibleTextCount: String(eligibleTextCount) }
          },
          data: { details: { eligibleTextCount, computableTextCount } }
        }
      ]
    };
  }

  if (failCount > 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      confidence: rule.defaultConfidence || 'high',
      occurrences
    };
  }

  // All computable text passed
  if (!occurrences.length) {
    pushPassOccurrence(eligibleTextCount, computableTextCount);
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'pass',
    severity: rule.defaultSeverity || 'serious',
    confidence: rule.defaultConfidence || 'high',
    occurrences
  };
}), applicability: null },
    "contrast-minimum": { run: (function runInPage(ctx) {
  const { helpers, rule, engineOptions } = ctx;

  function toElement(node) {
    try {
      if (!node) return null;
      if (node.nodeType === 1) return node; // ELEMENT_NODE
      if (node.nodeType === 3) return node.parentElement || null; // TEXT_NODE
      return null;
    } catch {
      return null;
    }
  }

  const __contrastSharedCache =
    helpers && helpers.contrast && helpers.contrast.sharedCache
      ? helpers.contrast.sharedCache
      : null;
  const __elBlockerCache = __contrastSharedCache
    ? __contrastSharedCache.__elBlockerCache ||
      (__contrastSharedCache.__elBlockerCache = new WeakMap())
    : null;

  const __elBgCache = __contrastSharedCache
    ? __contrastSharedCache.__elBgCache || (__contrastSharedCache.__elBgCache = new WeakMap())
    : null;

  const __elFgCache = __contrastSharedCache
    ? __contrastSharedCache.__elFgCache || (__contrastSharedCache.__elFgCache = new WeakMap())
    : null;

  const __elFontCache = __contrastSharedCache
    ? __contrastSharedCache.__elFontCache || (__contrastSharedCache.__elFontCache = new WeakMap())
    : new WeakMap();

  function safeComputedStyle(el) {
    try {
      if (!el || el.nodeType !== 1) return null;

      if (helpers && typeof helpers.computedStyle === 'function') {
        const cs = helpers.computedStyle(el);
        if (cs) return cs;
      }
      const view =
        el.ownerDocument && el.ownerDocument.defaultView ? el.ownerDocument.defaultView : null;
      if (view && typeof view.getComputedStyle === 'function') return view.getComputedStyle(el);
    } catch {}
    return null;
  }

  function getFontInfo(el) {
    try {
      if (!el || el.nodeType !== 1) {
        return {
          fontSizePx: 0,
          fontSizePt: '',
          fontWeightNum: 400,
          fontWeight: 'normal',
          isBold: false,
          isLarge: false,
          isLargeText: false
        };
      }

      const cached = __elFontCache.get(el);
      if (cached) return cached;

      const cs = safeComputedStyle(el);
      const fontSizePx = cs ? helpers.contrast.parsePx(cs.fontSize) : null;
      const fontWeightNum = cs ? helpers.contrast.normalizeFontWeight(cs.fontWeight) : 400;

      const sizePx = Number.isFinite(fontSizePx) ? fontSizePx : 0;
      const isBold = Number.isFinite(fontWeightNum) && fontWeightNum >= 700;
      const isLarge = helpers.contrast.isLargeText(sizePx, fontWeightNum);

      const out = {
        fontSizePx: sizePx,
        fontSizePt: helpers.contrast.pxToPt(sizePx),
        fontWeightNum,
        fontWeight: helpers.contrast.fontWeightLabel(fontWeightNum),
        isBold,
        isLarge,
        isLargeText: isLarge
      };

      __elFontCache.set(el, out);
      return out;
    } catch {
      return {
        fontSizePx: 0,
        fontSizePt: '',
        fontWeightNum: 400,
        fontWeight: 'normal',
        isBold: false,
        isLarge: false,
        isLargeText: false
      };
    }
  }

  const contrast =
    engineOptions && typeof engineOptions.contrast === 'object' && engineOptions.contrast
      ? engineOptions.contrast
      : {};

  const mode = contrast.mode === 'auditorAssist' ? 'auditorAssist' : 'strictConformance';

  const rootCanvasFallback =
    typeof contrast.rootCanvasFallback === 'string' && contrast.rootCanvasFallback.trim()
      ? contrast.rootCanvasFallback.trim()
      : '#ffffff';

  const MAX_OCCURRENCES = 50;

  const occurrences = [];
  let eligibleTextCount = 0;
  let computableTextCount = 0;
  let failCount = 0;

  const seenFailEls = new Set();

  function pushPassOccurrence(eligibleCount, computableCount) {
    try {
      occurrences.push({
        selector: '',
        summary: 'All computable text meets the minimum (AA) contrast threshold.',
        hint: '',
        html: '',
        i18n: {
          summaryKey: 'contrastMinimum_pass_allAboveThreshold',
          hintKey: '',
          params: {
            eligibleTextCount: String(Number(eligibleCount) || 0),
            computableTextCount: String(Number(computableCount) || 0)
          }
        },
        data: {
          details: {
            reasonCode: 'ALL_ABOVE_THRESHOLD',
            eligibleTextCount: Number(eligibleCount) || 0,
            computableTextCount: Number(computableCount) || 0,
            metrics: {
              eligibleTextCount: Number(eligibleCount) || 0,
              computableTextCount: Number(computableCount) || 0
            }
          }
        }
      });
    } catch {
      // no-throw
    }
  }

  function pushFailOccurrence(el, params, details) {
    try {
      if (!el || seenFailEls.has(el)) return;
      if (occurrences.length >= MAX_OCCURRENCES) return;

      seenFailEls.add(el);

      const det = details && typeof details === 'object' ? details : { reasonCode: 'UNKNOWN' };

      const occBase = {
        selector: '',
        html: '',
        summary: '',
        hint: '',
        i18n: {
          summaryKey: 'contrastMinimum_fail_belowThreshold',
          hintKey: '',
          params: params && typeof params === 'object' ? params : {}
        },
        data: { details: det }
      };

      // Bind the occurrence to an element deterministically, but still let the engine
      // attach canonical selector + HTML snippet via reportOccurrence when available.
      let nodeSelector = '';
      try {
        const elementId =
          el && typeof el.getAttribute === 'function' ? el.getAttribute('id') || '' : '';
        if (elementId) nodeSelector = `#${elementId}`;
      } catch {
        // no-throw
      }
      const tagName = el && el.tagName ? String(el.tagName).toLowerCase() : 'element';

      let occ = { ...occBase };

      // IMPORTANT: this populates occ.selector and occ.html (snippet) when available
      if (helpers && typeof helpers.reportOccurrence === 'function') {
        try {
          const reported = helpers.reportOccurrence(el, occBase);
          if (reported && typeof reported === 'object') occ = reported;
        } catch {
          // no-throw
        }
      }

      occ.selector = occ.selector || nodeSelector || '';

      occ.data = occ.data || {};
      occ.data.details = occ.data.details || {};
      occ.data.details.node = { selector: nodeSelector, tagName };

      occurrences.push(occ);
    } catch {
      // no-throw
    }
  }

  // perf: per-element analysis cache (per run)
  let __elAnalysisCache = new WeakMap();
  if (__contrastSharedCache) {
    try {
      if (!__contrastSharedCache.__elAnalysisCacheAA)
        __contrastSharedCache.__elAnalysisCacheAA = new WeakMap();
      __elAnalysisCache = __contrastSharedCache.__elAnalysisCacheAA;
    } catch {
      __elAnalysisCache = new WeakMap();
    }
  }

  // Shared deterministic text scan (computed once per run and reused across contrast checks)
  let scan;
  try {
    scan =
      helpers && helpers.contrast && typeof helpers.contrast.getTextScan === 'function'
        ? helpers.contrast.getTextScan(ctx, helpers, engineOptions)
        : null;
  } catch {
    scan = null;
  }

  // Walk eligible visible text nodes (counted per text node), but compute expensive analysis once per element.
  if (scan && scan.elements && Array.isArray(scan.elements)) {
    try {
      eligibleTextCount = Number(scan.eligibleTextCount) || 0;

      for (const rec of scan.elements) {
        const el = toElement(rec && rec.el);
        const textCount = rec && Number(rec.textCount) ? Number(rec.textCount) : 0;
        if (!el || textCount <= 0) continue;

        // Computability + contrast analysis (cached per element)
        let analysis = __elAnalysisCache.get(el);
        if (!analysis) {
          // Computability gate (do NOT emit cantTell here; Rule 1 is responsible for that)
          let blocker = __elBlockerCache ? __elBlockerCache.get(el) : null;
          if (!blocker) {
            blocker = helpers.contrast.getComputabilityBlocker(el);
            blocker = blocker || {
              ok: true,
              reasonCode: null,
              blockerSelector: '',
              blockerProperty: '',
              blockerValue: ''
            };
            if (__elBlockerCache) __elBlockerCache.set(el, blocker);
          }

          if (blocker && blocker.ok === false) {
            analysis = { computable: false };
          } else {
            let bg = __elBgCache ? __elBgCache.get(el) : null;
            if (!bg) {
              bg = helpers.contrast.computeEffectiveBackground(el, {
                contrast: { mode, rootCanvasFallback },
                collectStack: false
              });
              bg = bg || {
                ok: false,
                reasonCode: 'BACKGROUND_NOT_COMPUTABLE',
                rgba: null,
                alpha: 0
              };
              if (__elBgCache) __elBgCache.set(el, bg);
            }

            const bgAssumptionsApplied =
              bg && Array.isArray(bg.assumptionsApplied) && bg.assumptionsApplied.length
                ? bg.assumptionsApplied.slice(0)
                : null;
            const bgAssumedRootCanvasColor =
              bg && typeof bg.assumedRootCanvasColor === 'string'
                ? bg.assumedRootCanvasColor
                : null;

            let fg = __elFgCache ? __elFgCache.get(el) : null;
            if (!fg) {
              fg = helpers.contrast.computeEffectiveForeground(el);
              fg = fg || { rgba: null, alpha: 0, opacityProduct: 1 };
              if (__elFgCache) __elFgCache.set(el, fg);
            }

            if (!bg || bg.ok === false || !bg.rgba || !fg || !fg.rgba) {
              analysis = { computable: false };
            } else {
              // Compose FG over BG if FG has alpha (effective fg may be < 1 due to opacity chain)
              const fgOpaque =
                fg.rgba.a != null && fg.rgba.a < 1
                  ? helpers.contrast.compositeRgba(fg.rgba, bg.rgba)
                  : { r: fg.rgba.r, g: fg.rgba.g, b: fg.rgba.b, a: 1 };

              const bgOpaque = { r: bg.rgba.r, g: bg.rgba.g, b: bg.rgba.b, a: 1 };

              const ratio = helpers.contrast.contrastRatio(fgOpaque, bgOpaque);

              const font = getFontInfo(el);
              const threshold = helpers.contrast.requiredRatio('AA', font.isLargeText);

              analysis = {
                computable: true,
                ratio,
                bgAssumptionsApplied,
                bgAssumedRootCanvasColor,
                ratioStr: helpers.contrast.round2(ratio),
                threshold,
                thresholdStr: `${threshold}`,
                font,
                fgOpaque,
                bgOpaque
              };
            }
          }
          __elAnalysisCache.set(el, analysis);
        }

        if (!analysis || analysis.computable !== true) continue;

        computableTextCount += textCount;

        const ratio = analysis.ratio;
        const font = analysis.font;
        const threshold = analysis.threshold;
        const ratioStr = analysis.ratioStr;
        const thresholdStr = analysis.thresholdStr;
        const fgOpaque = analysis.fgOpaque;
        const bgOpaque = analysis.bgOpaque;

        if (!(ratio >= threshold)) {
          failCount += textCount;

          const fgHex = helpers.contrast.rgbToHex ? helpers.contrast.rgbToHex(fgOpaque) : '';

          const bgHex = helpers.contrast.rgbToHex ? helpers.contrast.rgbToHex(bgOpaque) : '';

          const fontPxNum = font.fontSizePx ? parseFloat(font.fontSizePx) : NaN;

          const fontPtStr =
            helpers.contrast.pxToPt && Number.isFinite(fontPxNum)
              ? helpers.contrast.pxToPt(fontPxNum)
              : '';

          const fwNum = Number(font.fontWeightNum);
          const fwLabel = font.fontWeight || (font.isBold ? 'bold' : 'normal');

          const fgRgbaStr = helpers.contrast.rgbaToString(fgOpaque);
          const bgRgbaStr = helpers.contrast.rgbaToString(bgOpaque);

          const params = {
            reasonCode: 'BELOW_THRESHOLD',

            foreground: fgRgbaStr,
            background: bgRgbaStr,

            foregroundHex: fgHex,
            backgroundHex: bgHex,
            fontSizePt: fontPtStr,
            fontWeightLabel: fwLabel,

            ratio: ratioStr,
            threshold: thresholdStr,

            fontSizePx: font.fontSizePx,
            fontWeight: font.fontWeight,
            isBold: font.isBold,
            isLargeText: font.isLargeText
          };

          const assumptionsApplied =
            analysis &&
            Array.isArray(analysis.bgAssumptionsApplied) &&
            analysis.bgAssumptionsApplied.length
              ? analysis.bgAssumptionsApplied.slice(0)
              : null;
          const assumedRootCanvasColor =
            analysis && typeof analysis.bgAssumedRootCanvasColor === 'string'
              ? analysis.bgAssumedRootCanvasColor
              : null;

          const details = {
            reasonCode: 'BELOW_THRESHOLD',
            metrics: { ratio, threshold },
            typography: {
              fontSizePx: Number.isFinite(fontPxNum) ? fontPxNum : null,
              fontSizePt: Number.isFinite(fontPxNum) ? fontPxNum * 0.75 : null,
              fontWeight: Number.isFinite(fwNum) ? fwNum : null,
              fontWeightLabel: fwLabel,
              isBold: !!font.isBold,
              isLargeText: !!font.isLargeText
            },
            colors: {
              foregroundHex: fgHex,
              backgroundHex: bgHex,
              foregroundRgba: fgRgbaStr,
              backgroundRgba: bgRgbaStr
            },
            assumptionsApplied: assumptionsApplied,
            assumedRootCanvasColor: assumedRootCanvasColor
          };

          pushFailOccurrence(el, params, details);

          if (occurrences.length >= MAX_OCCURRENCES) break;
        }
      }
    } catch {
      // No-throw: deterministic cantTell on internal failure
      return {
        ruleId: rule.ruleId,
        outcome: 'cantTell',
        severity: rule.defaultSeverity || 'serious',
        confidence: rule.defaultConfidence || 'high',
        occurrences: [
          {
            selector: '',
            summary: '',
            hint: '',
            html: '',
            i18n: {
              summaryKey: 'contrastMinimum_cantTell_engineFailure',
              hintKey: '',
              params: { reasonCode: 'ENGINE_EXCEPTION' }
            },
            data: { details: { reasonCode: 'ENGINE_EXCEPTION' } }
          }
        ]
      };
    }
  }

  if (eligibleTextCount === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: rule.defaultSeverity || 'serious',
      confidence: rule.defaultConfidence || 'high',
      occurrences: []
    };
  }

  // If there was eligible text but nothing computable, this rule stays notApplicable
  // because Rule 1 (computability) is responsible for cantTell.
  if (computableTextCount === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: rule.defaultSeverity || 'serious',
      confidence: rule.defaultConfidence || 'high',
      occurrences: [
        {
          selector: '',
          summary: '',
          hint: '',
          html: '',
          i18n: {
            summaryKey: 'contrastMinimum_notApplicable_noComputableText',
            hintKey: '',
            params: { eligibleTextCount: String(eligibleTextCount) }
          },
          data: { details: { eligibleTextCount, computableTextCount } }
        }
      ]
    };
  }

  if (failCount > 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      confidence: rule.defaultConfidence || 'high',
      occurrences
    };
  }

  // All computable text passed
  if (!occurrences.length) {
    pushPassOccurrence(eligibleTextCount, computableTextCount);
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'pass',
    severity: rule.defaultSeverity || 'serious',
    confidence: rule.defaultConfidence || 'high',
    occurrences
  };
}), applicability: null },
    "css-hidden-focus": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;
  const isDomVisibleEligible =
    helpers && typeof helpers.isDomVisibleEligible === 'function'
      ? helpers.isDomVisibleEligible
      : null;
  const reportOccurrence =
    helpers && typeof helpers.reportOccurrence === 'function' ? helpers.reportOccurrence : null;

  const trim = (v) => (v == null ? '' : String(v)).trim();
  const lower = (v) => trim(v).toLowerCase();

  function qAll(sel) {
    try {
      if (queryAllSmart) {
        const r = queryAllSmart(sel);
        if (Array.isArray(r)) return r;
        return Array.from(r || []);
      }
    } catch {
      // fall through
    }
    try {
      if (safeRoot && typeof safeRoot.querySelectorAll === 'function')
        return Array.from(safeRoot.querySelectorAll(sel));
    } catch {
      // fall through
    }
    return [];
  }

  function getComputedStyleSafe(el) {
    try {
      const w =
        document && document.defaultView
          ? document.defaultView
          : typeof window !== 'undefined'
            ? window
            : null;
      return w && w.getComputedStyle ? w.getComputedStyle(el) : null;
    } catch {
      return null;
    }
  }

  // Returns deterministic "visually hidden but can remain focusable" hints.
  function getVisibilityHints(el) {
    const out = [];
    if (!el) return out;
    const cs = getComputedStyleSafe(el);

    // opacity:0
    try {
      const rawOp = cs && cs.opacity != null ? String(cs.opacity).trim() : '';
      const op = rawOp ? Number.parseFloat(rawOp) : 1;
      if (Number.isFinite(op) && op <= 0.0001) out.push('opacityZero');
    } catch {}

    // clip / clip-path
    try {
      const clip = cs && cs.clip != null ? String(cs.clip).trim() : '';
      const clipPath = cs && cs.clipPath != null ? String(cs.clipPath).trim() : '';
      const clipLow = (clip || '').toLowerCase();
      const clipPathLow = (clipPath || '').toLowerCase();

      if (clipLow && clipLow !== 'auto') {
        if (clipLow.indexOf('rect(') !== -1 && clipLow.replace(/\s+/g, '').indexOf('rect(0') !== -1)
          out.push('clipped');
      }
      if (clipPathLow && clipPathLow !== 'none') {
        if (
          clipPathLow.indexOf('inset(') !== -1 &&
          (clipPathLow.indexOf('100%') !== -1 || clipPathLow.indexOf('50%') !== -1)
        ) {
          out.push('clipped');
        }
      }
    } catch {}

    // zero-size + overflow hidden/clip
    try {
      const wv = cs && cs.width != null ? String(cs.width).trim() : '';
      const hv = cs && cs.height != null ? String(cs.height).trim() : '';
      const ov = cs && cs.overflow != null ? String(cs.overflow).trim().toLowerCase() : '';
      const isZeroW = wv === '0px' || wv === '0';
      const isZeroH = hv === '0px' || hv === '0';
      const hidesOverflow = ov === 'hidden' || ov === 'clip';
      if ((isZeroW || isZeroH) && hidesOverflow) out.push('zeroSizeOverflowHidden');
    } catch {}

    // off-screen heuristic (absolute/fixed + left/top <= -5000 OR text-indent <= -5000)
    try {
      const pos = cs && cs.position != null ? String(cs.position).trim().toLowerCase() : '';
      const left = cs && cs.left != null ? String(cs.left).trim().toLowerCase() : '';
      const top = cs && cs.top != null ? String(cs.top).trim().toLowerCase() : '';
      const ti = cs && cs.textIndent != null ? String(cs.textIndent).trim().toLowerCase() : '';

      const parsePx = (s) => {
        if (!s || s === 'auto') return null;
        const m = String(s).match(/-?\d+(\.\d+)?/);
        if (!m) return null;
        const n = Number.parseFloat(m[0]);
        return Number.isFinite(n) ? n : null;
      };

      const l = parsePx(left);
      const t = parsePx(top);
      const ind = parsePx(ti);

      if (pos === 'absolute' || pos === 'fixed') {
        if ((l != null && l <= -5000) || (t != null && t <= -5000)) out.push('offscreen');
      }
      if (ind != null && ind <= -5000) out.push('offscreen');
    } catch {}

    // Dedup
    const seen = new Set();
    const uniq = [];
    for (const k of out) {
      const kk = String(k);
      if (!seen.has(kk)) {
        seen.add(kk);
        uniq.push(kk);
      }
    }
    return uniq;
  }

  function getFocusableInfoSafe(el) {
    if (!getFocusableInfo) return null;
    try {
      return getFocusableInfo(el, ctx);
    } catch {
      return null;
    }
  }

  function getDeepActiveElement(docRef) {
    let cur = docRef && docRef.activeElement ? docRef.activeElement : null;
    let guard = 0;
    while (cur && cur.shadowRoot && cur.shadowRoot.activeElement && guard++ < 20) {
      cur = cur.shadowRoot.activeElement;
    }
    return cur;
  }

  function focusElementSafe(el) {
    if (!el || typeof el.focus !== 'function') return false;
    try {
      el.focus({ preventScroll: true });
      return true;
    } catch {
      try {
        el.focus();
        return true;
      } catch {
        return false;
      }
    }
  }

  function runFocusObservationWindow(docRef, fn) {
    const w =
      docRef && docRef.defaultView
        ? docRef.defaultView
        : typeof window !== 'undefined'
          ? window
          : null;
    if (!w || typeof fn !== 'function') return;

    const originalSetTimeout = typeof w.setTimeout === 'function' ? w.setTimeout.bind(w) : null;
    const originalRequestAnimationFrame =
      typeof w.requestAnimationFrame === 'function' ? w.requestAnimationFrame.bind(w) : null;
    const originalQueueMicrotask =
      typeof w.queueMicrotask === 'function' ? w.queueMicrotask.bind(w) : null;

    const queuedMicrotasks = [];
    const queuedRaf = [];
    const queuedTimers = [];
    let fakeTimerId = 1;

    const patchedSetTimeout = function (cb, delay) {
      const d = Number.isFinite(Number(delay)) ? Number(delay) : 0;
      if (typeof cb === 'function' && d <= 200) {
        const args = [];
        for (let i = 2; i < arguments.length; i++) args.push(arguments[i]);
        queuedTimers.push({ delay: d, cb: () => cb.apply(w, args) });
        return fakeTimerId++;
      }
      if (originalSetTimeout) return originalSetTimeout.apply(w, arguments);
      return fakeTimerId++;
    };

    const patchedRaf = function (cb) {
      if (typeof cb === 'function') {
        queuedRaf.push(cb);
        return fakeTimerId++;
      }
      if (originalRequestAnimationFrame) return originalRequestAnimationFrame.apply(w, arguments);
      return fakeTimerId++;
    };

    const patchedQueueMicrotask = function (cb) {
      if (typeof cb === 'function') queuedMicrotasks.push(cb);
    };

    try {
      if (originalSetTimeout) w.setTimeout = patchedSetTimeout;
      if (originalRequestAnimationFrame) w.requestAnimationFrame = patchedRaf;
      if (originalQueueMicrotask) w.queueMicrotask = patchedQueueMicrotask;
      fn();

      let guard = 0;
      while (
        (queuedMicrotasks.length || queuedRaf.length || queuedTimers.length) &&
        guard++ < 100
      ) {
        while (queuedMicrotasks.length) {
          const mt = queuedMicrotasks.shift();
          try {
            mt();
          } catch {}
        }
        while (queuedRaf.length) {
          const rf = queuedRaf.shift();
          try {
            rf(16);
          } catch {}
        }
        if (queuedTimers.length) {
          queuedTimers.sort((a, b) => a.delay - b.delay);
          const tt = queuedTimers.shift();
          try {
            tt.cb();
          } catch {}
        }
      }
    } finally {
      if (originalSetTimeout) w.setTimeout = originalSetTimeout;
      if (originalRequestAnimationFrame) w.requestAnimationFrame = originalRequestAnimationFrame;
      if (originalQueueMicrotask) w.queueMicrotask = originalQueueMicrotask;
    }
  }

  function probeImmediateFocusRedirect(candidate) {
    if (!candidate || typeof candidate.addEventListener !== 'function') return null;

    let focusedByEvent = false;
    const onFocusCapture = () => {
      focusedByEvent = true;
    };
    try {
      candidate.addEventListener('focus', onFocusCapture, true);
    } catch {}

    const before = getDeepActiveElement(document);
    let focused = false;
    runFocusObservationWindow(document, () => {
      focused = focusElementSafe(candidate);
    });

    try {
      candidate.removeEventListener('focus', onFocusCapture, true);
    } catch {}
    if (!focused || !focusedByEvent) return null;

    const after = getDeepActiveElement(document);

    if (before && before !== after) {
      focusElementSafe(before);
    }

    if (!after || after === candidate) return null;
    const redirectedTag = (() => {
      try {
        return lower(after.tagName || '');
      } catch {
        return '';
      }
    })();
    const redirectedId = (() => {
      try {
        return trim(after.getAttribute && after.getAttribute('id'));
      } catch {
        return '';
      }
    })();
    return {
      redirected: true,
      redirectedToTag: redirectedTag || null,
      redirectedToId: redirectedId || null
    };
  }

  function isTabbable(el, info) {
    const f = info || getFocusableInfoSafe(el);
    return !!(f && f.focusable && f.tabbable);
  }

  // Exclude elements that are not rendered / not in the visual rendering tree.
  function isRendered(el) {
    if (!isDomVisibleEligible) return true;
    try {
      const vis = isDomVisibleEligible(el, ctx, {
        visibilityMode: 'styleOnly',
        disableGeometry: true
      });
      if (vis && vis.eligible === false) {
        const rs = Array.isArray(vis.reasons) ? vis.reasons : [];
        // We *include* opacityZero in this rule. Everything else is treated as not rendered for this purpose.
        const nonOpacity = rs.filter((r) => String(r) !== 'opacityZero');
        if (nonOpacity.length) return false;
      }
    } catch {
      // If in doubt, keep deterministic behavior and consider it rendered.
    }
    return true;
  }
  const candidates = qAll(
    'a[href],area[href],button,input,select,textarea,summary,[tabindex],[contenteditable]'
  );
  if (!candidates.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  const maxRuntimeProbeCount = 3;
  let runtimeProbeCount = 0;

  for (const el of candidates) {
    if (!el || !el.getAttribute) continue;

    const finfo = getFocusableInfoSafe(el);
    if (!isTabbable(el, finfo)) continue;

    // Skip elements not rendered (display:none, visibility:hidden, [hidden], etc.)
    if (!isRendered(el)) continue;

    const hints = getVisibilityHints(el);
    if (!hints.length) continue; // <-- applicability gate

    const tagName = (() => {
      try {
        return lower(el.tagName || '');
      } catch {
        return '';
      }
    })();

    const hintOrder = ['opacityZero', 'offscreen', 'clipped', 'zeroSizeOverflowHidden'];
    const hintsArr = [];
    for (const k of hintOrder) if (hints.includes(k)) hintsArr.push(k);

    const runtimeProbe =
      runtimeProbeCount < maxRuntimeProbeCount ? probeImmediateFocusRedirect(el) : null;
    if (runtimeProbeCount < maxRuntimeProbeCount) runtimeProbeCount += 1;
    const downgradedToRedirectReview = !!(runtimeProbe && runtimeProbe.redirected);

    const baseOccurrence = {
      summary: downgradedToRedirectReview
        ? `Focusable ${tagName} appears visually hidden but focus moved immediately to another element. Verify sentinel/focus-trap behavior.`
        : `Focusable ${tagName} appears visually hidden (${hintsArr.join(',')}). Verify it becomes visible on keyboard focus.`,
      hint: downgradedToRedirectReview
        ? 'Verify this is an intentional focus sentinel/focus-trap handoff and that keyboard users never remain on visually hidden focus targets.'
        : 'Manually tab to the element and confirm a visible focus indicator and that the element is visible when focused. If it remains hidden while focused, fix CSS/JS so it becomes visible or is removed from the tab order until visible.',
      i18n: downgradedToRedirectReview
        ? null
        : {
            summaryKey: 'cssHidden_focus_summary_cantTell',
            hintKey: 'cssHidden_focus_hint_cantTell',
            params: { element: tagName, visibilityHints: hintsArr.join(',') }
          },
      data: {
        details: {
          reasonCode: downgradedToRedirectReview
            ? 'cssHiddenTabbable_runtimeRedirect_needsReview'
            : 'cssHiddenTabbable_needsFocusStateVerification',
          metrics: { visibilityHints: hintsArr.slice(0) },
          runtimeProbe: runtimeProbe || null
        }
      }
    };

    occurrences.push(
      reportOccurrence
        ? reportOccurrence(el, baseOccurrence)
        : { __node: el, selector: '', html: '', ...baseOccurrence }
    );
  }

  // Manual rule + validator invariant: cantTell must have >= 1 occurrence
  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "css-orientation-lock": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const CSS_MEDIA_RULE = 4;
  const CSS_STYLE_RULE = 1;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  // Converts a rotate()/rotateZ() angle argument (with unit) to degrees.
  // Only deg/grad/rad/turn are recognized; a unitless or unrecognized value
  // contributes 0 (ignored, not treated as a lock -- an unparseable angle
  // isn't a confirmed exploit).
  function angleToDegrees(raw) {
    const m = /(-?[\d.]+)\s*(deg|grad|rad|turn)/i.exec(raw);
    if (!m) return 0;
    const value = parseFloat(m[1]);
    if (!Number.isFinite(value)) return 0;
    const unit = m[2].toLowerCase();
    if (unit === 'rad') return value * (180 / Math.PI);
    if (unit === 'grad') return value * (360 / 400);
    if (unit === 'turn') return value * 360;
    return value; // deg
  }

  // Sums the degrees of every rotate()/rotateZ() function found in a
  // transform value (a real transform can legitimately compose more than
  // one, e.g. "translate(-50%) rotate(90deg)").
  function rotateDegreesFromTransform(t) {
    if (!t) return 0;
    let total = 0;
    const re = /rotate(?:Z)?\s*\(([^)]*)\)/gi;
    let m;
    while ((m = re.exec(t)) !== null) {
      total += angleToDegrees(m[1]);
    }
    return total;
  }

  // Whole-page-orientation-lock detection (see @implementation-notes): a
  // rotation near 0 or 180 degrees (mod 180) is a no-op or a flip, neither
  // of which changes portrait<->landscape, so it's NOT a lock; only a
  // rotation near 90 or 270 degrees (mod 90, once the 0/180 case is
  // excluded) is.
  function isLockingRotation(styleDecl) {
    if (!styleDecl) return false;
    const transformVal =
      trim(styleDecl.transform) ||
      trim(styleDecl.getPropertyValue ? styleDecl.getPropertyValue('-webkit-transform') : '');
    // The standalone CSS `rotate` property (distinct from `transform: rotate()`).
    const rotatePropVal = trim(
      styleDecl.getPropertyValue ? styleDecl.getPropertyValue('rotate') : ''
    );

    let degrees = rotateDegreesFromTransform(transformVal);
    if (!degrees && rotatePropVal) degrees = angleToDegrees(rotatePropVal);

    if (!degrees) return false;

    const abs = Math.abs(degrees);
    if (Math.abs(abs - 180) % 180 <= 0) return false; // near 0/180: not a lock
    return Math.abs(abs - 90) % 90 <= 0; // near 90/270: a lock
  }

  function isOrientationMedia(mediaText) {
    const m = trim(mediaText).toLowerCase();
    return m.includes('orientation') && (m.includes('portrait') || m.includes('landscape'));
  }

  function scanRuleList(rules, mediaText, findings) {
    if (!rules) return;
    for (const r of rules) {
      if (!r) continue;
      if (r.type === CSS_STYLE_RULE && isLockingRotation(r.style)) {
        findings.push({ mediaText, selectorText: trim(r.selectorText) || '(unknown selector)' });
      }
    }
  }

  const findings = [];
  let sheetCount = 0;

  try {
    const sheets = document.styleSheets || [];
    for (const sheet of sheets) {
      let rules = null;
      try {
        rules = sheet && sheet.cssRules ? sheet.cssRules : null;
      } catch {
        continue; // cross-origin stylesheet, not inspectable
      }
      if (!rules) continue;
      sheetCount += 1;

      for (const rule2 of rules) {
        if (!rule2 || rule2.type !== CSS_MEDIA_RULE) continue;
        const mediaText = rule2.media ? rule2.media.mediaText : '';
        if (!isOrientationMedia(mediaText)) continue;
        scanRuleList(rule2.cssRules, mediaText, findings);
      }
    }
  } catch {
    // no-throw: treat as no accessible stylesheets
  }

  if (sheetCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!findings.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  const target = document.documentElement || document.body || null;
  const stableSelector = helpers.buildSelector && target ? helpers.buildSelector(target) : 'html';
  const html =
    helpers.getOuterHtmlSnippet && target ? helpers.getOuterHtmlSnippet(target) : '<html>';

  const occurrences = findings.map((f) => ({
    selector: stableSelector,
    html,
    summary: `A "${f.mediaText}" media query rotates "${f.selectorText}", locking the page to one orientation.`,
    hint: 'Remove the rotate() transform from the orientation media query; let the page respond naturally to device orientation instead of forcing a visual rotation.',
    i18n: {
      summaryKey: 'cssOrientationLock_summary_fail',
      hintKey: 'cssOrientationLock_hint_fail',
      params: { mediaText: f.mediaText, selectorText: f.selectorText }
    },
    data: {
      details: {
        reasonCode: 'ORIENTATION_MEDIA_ROTATE_TRANSFORM',
        mediaText: f.mediaText,
        selectorText: f.selectorText
      }
    }
  }));

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'serious',
    occurrences
  };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "definition-list-children-valid": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  // Declared inside runInPage — see scripts/build-core.js header
  // ("runInPage MUST be self-contained").
  const PASSTHROUGH_TAGS = new Set(['dt', 'dd', 'script', 'template', 'style']);

  const nodes = helpers.queryAllSmart ? helpers.queryAllSmart('dl') : helpers.queryAll('dl');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.children) continue;
    if (!el.children.length) continue;

    applicableCount += 1;

    // Flatten one level of wrapping <div> (common dt/dd grouping pattern).
    const flattened = [];
    for (const child of el.children) {
      if (!child || !child.tagName) continue;
      if (child.tagName.toLowerCase() === 'div') {
        for (const grandchild of child.children || []) {
          if (grandchild && grandchild.tagName) flattened.push(grandchild);
        }
      } else {
        flattened.push(child);
      }
    }

    let hasDt = false;
    let hasDd = false;
    const invalidTags = [];
    for (const node of flattened) {
      const tag = node.tagName.toLowerCase();
      if (tag === 'dt') {
        hasDt = true;
        continue;
      }
      if (tag === 'dd') {
        hasDd = true;
        continue;
      }
      if (!PASSTHROUGH_TAGS.has(tag)) invalidTags.push(tag);
    }
    const dedupedInvalidTags = [...new Set(invalidTags)];

    // The dt/dd pairing is only required "when not empty" — a <dl> with
    // NEITHER dt nor dd (whether genuinely childless after flattening, only
    // passthrough script/template/style content, or an empty wrapping div)
    // is vacuously fine, not a violation. Only an UNBALANCED pairing (dt
    // present without any dd, or vice versa) is a real structural problem.
    const reasonCode = invalidTags.length
      ? 'DL_INVALID_CHILD'
      : (hasDt || hasDd) && !(hasDt && hasDd)
        ? 'DL_NO_DT_DD'
        : null;
    if (!reasonCode) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    const summary = invalidTags.length
      ? 'This description list contains a direct or wrapped child that is not part of a dt/dd group.'
      : 'This description list has no <dt>/<dd> term-definition group.';
    const hint = invalidTags.length
      ? 'Only use <dt>/<dd> (optionally wrapped in one <div>), <script>, <template>, or <style> inside <dl>.'
      : 'Add at least one <dt>/<dd> pair inside this <dl>.';

    occurrences.push({
      selector: stableSelector,
      html,
      summary,
      hint,
      i18n: {
        summaryKey: invalidTags.length
          ? 'definitionListChildrenValid_summary_fail_invalidChild'
          : 'definitionListChildrenValid_summary_fail_noDtDd',
        hintKey: invalidTags.length
          ? 'definitionListChildrenValid_hint_fail_invalidChild'
          : 'definitionListChildrenValid_hint_fail_noDtDd',
        params: { invalidChildren: dedupedInvalidTags.join(', ') }
      },
      data: {
        details: { reasonCode, invalidChildren: invalidTags }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "deprecated-elements-not-used": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('blink, marquee')
    : helpers.queryAll('blink, marquee');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.tagName) continue;

    applicableCount += 1;

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This element’s content cannot be paused, stopped, or hidden by the user.',
      hint: 'Remove this element; use static content, or an animation with a user-facing pause/stop control, instead.',
      i18n: {
        summaryKey: 'deprecatedElements_summary_fail',
        hintKey: 'deprecatedElements_hint_fail',
        params: { element: tag }
      },
      data: {
        details: { reasonCode: 'DEPRECATED_NON_STOPPABLE_ELEMENT', element: tag }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "dialog-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Resolve via the shared getTextFromIdRefs helper — computes each
    // referenced element's own ACCESSIBLE NAME (aria-label, then
    // aria-labelledby, then a value-like name, then content, then title),
    // not just its content text. This matters when aria-labelledby points
    // at an <iframe title="..."> (e.g. BBC News' cookie-consent dialog,
    // pointing at an `<iframe title="SP Consent Message">`), whose only
    // name source is its title attribute — an iframe's content is
    // opaque/cross-origin per HTML-AAM, so "name from content" is always
    // empty. Computing only name-from-content of the referenced node would
    // silently miss the title fallback and report no accessible name at
    // all, the same pattern every other *-name-present rule guards against.
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="dialog"],[role="alertdialog"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function evaluate(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // role="dialog"/"alertdialog" is name-from-author-only per WAI-ARIA: it
    // must NOT fall back to subtree content. A dialog's heading or body
    // text is not reliably exposed to assistive technologies as the
    // dialog's accessible name unless explicitly wired via aria-labelledby.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    const role = getAttr(el, 'role').toLowerCase();
    if (role !== 'dialog' && role !== 'alertdialog') continue;

    applicableCount += 1;

    const res = evaluate(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This dialog has no accessible name.',
      hint: 'Provide aria-labelledby (preferred) or aria-label so assistive technologies can announce the dialog.',
      i18n: {
        summaryKey: 'dialogNamePresent_summary_fail',
        hintKey: 'dialogNamePresent_hint_fail',
        params: { role }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: role, methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "dlitem-parent-valid": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('dt, dd')
    : helpers.queryAll('dt, dd');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el) continue;
    const parent = el.parentElement;
    if (!parent) continue;

    applicableCount += 1;

    const parentTag = parent.tagName ? parent.tagName.toLowerCase() : '';
    let valid = parentTag === 'dl';

    if (!valid && parentTag === 'div') {
      const grandparent = parent.parentElement;
      const grandparentTag =
        grandparent && grandparent.tagName ? grandparent.tagName.toLowerCase() : '';
      valid = grandparentTag === 'dl';
    }

    if (valid) continue;

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This description-list item is not contained by a <dl>.',
      hint: 'Place this <dt>/<dd> inside a <dl>, directly or wrapped in a single <div>.',
      i18n: {
        summaryKey: 'dlitemParentValid_summary_fail',
        hintKey: 'dlitemParentValid_hint_fail',
        params: { element: tag, parentElement: parentTag }
      },
      data: {
        details: { reasonCode: 'DLITEM_INVALID_PARENT', element: tag, parentElement: parentTag }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "duplicate-id-aria": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  // Declared inside runInPage — see scripts/build-core.js header
  // ("runInPage MUST be self-contained").
  const IDREF_ATTRS = [
    'aria-labelledby',
    'aria-describedby',
    'aria-owns',
    'aria-controls',
    'aria-activedescendant',
    'aria-flowto',
    'aria-errormessage',
    'aria-details'
  ];

  const idMap = new Map(); // id -> element[]
  const idNodes = document.querySelectorAll ? document.querySelectorAll('[id]') : [];
  for (const el of idNodes) {
    if (!el || !el.getAttribute) continue;
    const value = String(el.getAttribute('id') || '').trim();
    if (!value) continue;
    if (!idMap.has(value)) idMap.set(value, []);
    idMap.get(value).push(el);
  }

  const referencedIds = new Set();
  for (const attr of IDREF_ATTRS) {
    const selector = `[${attr}]`;
    const nodes = document.querySelectorAll ? document.querySelectorAll(selector) : [];
    for (const el of nodes) {
      if (!el || !el.getAttribute) continue;
      const raw = String(el.getAttribute(attr) || '').trim();
      if (!raw) continue;
      for (const token of raw.split(/\s+/)) {
        if (token) referencedIds.add(token);
      }
    }
  }

  const occurrences = [];

  for (const refId of referencedIds) {
    const els = idMap.get(refId) || [];
    if (els.length <= 1) continue;

    for (const el of els) {
      const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';

      occurrences.push({
        selector: stableSelector,
        html,
        summary: 'This id is referenced by an ARIA attribute but is used by more than one element.',
        hint: 'Make ids referenced by ARIA attributes unique within the document.',
        i18n: {
          summaryKey: 'duplicateIdAria_summary_fail',
          hintKey: 'duplicateIdAria_hint_fail',
          params: { id: refId, duplicateCount: String(els.length) }
        },
        data: {
          details: {
            reasonCode: 'DUPLICATE_ID_ARIA_REFERENCED',
            id: refId,
            duplicateCount: els.length
          }
        }
      });
    }
  }

  if (referencedIds.size === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "embed-text-alternative-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;

  const embeds = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('embed') : queryAll('embed')) || []);
    } catch {
      return queryAll('embed');
    }
  })();

  if (!embeds.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  const trim = (v) => (v == null ? '' : String(v)).trim();

  function computeNameInfo(el) {
    // For <embed>, treat ARIA name as primary. Do not accept HTML <label> associations.
    const flags = [];
    let aria = null;

    if (getAriaNameInfo) {
      aria = (() => {
        try {
          return getAriaNameInfo(el, ctx);
        } catch {
          return null;
        }
      })();
    }

    if (aria && aria.present && trim(aria.value)) {
      return {
        present: true,
        value: trim(aria.value),
        mechanism: aria.mechanism || 'aria',
        flags: (aria.flags || []).slice(0)
      };
    }

    const title = trim(el.getAttribute && el.getAttribute('title'));
    if (title) {
      flags.push('title-used');
      return { present: true, value: title, mechanism: 'title', flags };
    }

    if (aria && aria.flags && aria.flags.length) {
      for (const f of aria.flags) flags.push(f);
    }

    return { present: false, value: '', mechanism: 'none', flags };
  }

  // `title` is a weaker text-alternative mechanism than aria-label/
  // aria-labelledby — it is not reliably exposed to assistive technology
  // in every context (e.g. touch/mobile) — so a pass achieved only via
  // `title` is reported at reduced confidence rather than the rule's
  // default `high`.
  let anyPassedViaWeakMechanism = false;

  for (const el of embeds) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    // role presentation/none exclusion only when not focusable
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role === 'presentation' || role === 'none') {
      let focusable;
      if (getFocusableInfo) {
        const fi = (() => {
          try {
            return getFocusableInfo(el, ctx);
          } catch {
            return null;
          }
        })();
        focusable = !!(fi && fi.focusable);
      } else {
        const tabindex = el.getAttribute('tabindex');
        focusable =
          tabindex != null &&
          String(tabindex).trim() !== '' &&
          !Number.isNaN(Number(String(tabindex).trim()));
      }
      if (!focusable) continue;
    }

    applicableCount += 1;

    const name = computeNameInfo(el);
    if (name.present) {
      if (name.mechanism === 'title') anyPassedViaWeakMechanism = true;
      continue;
    }

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Missing text alternative for <embed>.',
      hint: 'Add an accessible name to <embed> (aria-label/aria-labelledby preferred, or a title attribute as a best-effort fallback).',
      i18n: {
        summaryKey: 'embed_textAltPresent_summary_fail',
        hintKey: 'embed_textAltPresent_hint_fail',
        params: { element: 'embed' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        name
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      // Never compute selector/snippet in the rule.
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'pass',
      severity: 'minor',
      confidence: anyPassedViaWeakMechanism ? 'medium' : 'high',
      occurrences: []
    };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "embed-text-alternative-quality": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const trim = (v) => (v == null ? '' : String(v)).trim();
  const getTextFromIdRefs =
    helpers && typeof helpers.getTextFromIdRefs === 'function' ? helpers.getTextFromIdRefs : null;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  const els = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('embed') : queryAll('embed')) || []);
    } catch {
      return queryAll('embed');
    }
  })();

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    let ariaLabel;
    let ariaLabelledBy;
    let title;
    try {
      ariaLabel = trim(el.getAttribute('aria-label'));
      ariaLabelledBy = trim(el.getAttribute('aria-labelledby'));
      title = trim(el.getAttribute('title'));
    } catch {
      ariaLabel = '';
      ariaLabelledBy = '';
      title = '';
    }

    // Only resolve IDREF text if aria-labelledby is present and aria-label is not already sufficient
    let labelledByText = '';

    if (!ariaLabel && ariaLabelledBy && getTextFromIdRefs) {
      try {
        const t = getTextFromIdRefs(ariaLabelledBy, ctx);
        labelledByText = trim(t && t.text);
      } catch {
        labelledByText = '';
      }
    }

    // A broken/empty-resolving aria-labelledby (e.g. pointing at a
    // nonexistent id) is not a "detected" text alternative to review the
    // QUALITY of -- there's no text here at all, and this element's
    // sibling automatic rule (embed-text-alternative-present) already
    // reports it as a fail (no accessible name). Like object-/svg-/
    // canvas-text-alternative-quality-manual, require aria-labelledby to
    // actually resolve to non-empty text rather than merely being present
    // as an attribute; a present-but-broken aria-labelledby has no text
    // alternative to review.
    const hasNameMechanism = !!(ariaLabel || title || labelledByText);
    if (!hasNameMechanism) continue;

    const details = {
      ariaLabel: ariaLabel || null,
      ariaLabelledBy: ariaLabelledBy || null,
      ariaLabelledByText: labelledByText || null,
      title: title || null
    };

    applicableCount += 1;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Review text alternative for <embed> for accuracy and appropriateness.',
      hint: 'Confirm the ARIA name or title accurately identifies the embedded content in context.',
      i18n: {
        summaryKey: 'embed_textAltQuality_summary_cantTell',
        hintKey: 'embed_textAltQuality_hint_cantTell',
        params: { element: (el.tagName || '').toLowerCase() }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "empty-heading": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  function isHeading(el) {
    const explicit = getExplicitRoleToken(el);
    if (explicit) return explicit === 'heading';
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    return /^h[1-6]$/.test(tag);
  }

  function getAccessibleNameText(el) {
    const al = normalizeWs(el.getAttribute && el.getAttribute('aria-label'));
    if (al) return al;
    const alb = normalizeWs(el.getAttribute && el.getAttribute('aria-labelledby'));
    if (alb) {
      const parts = [];
      for (const refId of alb.split(/\s+/).filter(Boolean)) {
        try {
          const ref = document.getElementById(refId);
          if (ref) {
            const t = normalizeWs(ref.textContent);
            if (t) parts.push(t);
          }
        } catch {}
      }
      const joined = normalizeWs(parts.join(' '));
      if (joined) return joined;
    }
    // Shared, accname-aligned "name from content" implementation (see
    // dom-helpers.js's getContentNameInfo header comment) — resolves an
    // <img> descendant's own alt text, an aria-label/aria-labelledby'd
    // descendant's own name, etc., and already gates every descendant on
    // full accessibility-tree eligibility (aria-hidden, display:none,
    // visibility:hidden, [hidden], inert, ...) the same way this rule
    // needs. Used instead of a fourth hand-rolled subtree walker here.
    if (helpers && typeof helpers.getContentNameInfo === 'function') {
      try {
        const info = helpers.getContentNameInfo(el, ctx);
        if (info && info.present && info.value) return info.value;
      } catch {}
    }
    return normalizeWs(el.getAttribute && el.getAttribute('title'));
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('h1, h2, h3, h4, h5, h6, [role]')
    : helpers.queryAll('h1, h2, h3, h4, h5, h6, [role]');

  const occurrences = [];
  let applicableCount = 0;
  const seen = new Set();

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isEligible(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (!isHeading(el)) continue;
    if (!isEligible(el)) continue;

    applicableCount += 1;

    const name = getAccessibleNameText(el);
    if (name) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const eligInfo = helpers.getEligibilityInfo
      ? (() => {
          try {
            return helpers.getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This heading has no accessible name.',
      hint: 'Add text content (or aria-label/aria-labelledby) to this heading, or remove it if it is not needed.',
      i18n: {
        summaryKey: 'emptyHeading_summary_cantTell',
        hintKey: 'emptyHeading_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'HEADING_EMPTY' },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "empty-table-header": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isEligible(node) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(node, ctx);
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // Plain el.textContent includes text from aria-hidden descendants, which
  // a real screen reader never announces. A <th> whose only text comes from
  // an aria-hidden descendant (e.g. <th><span aria-hidden="true">Name</span
  // ></th>) would otherwise be treated as having visible text, even though
  // AT announces nothing for it.
  function getVisibleText(el) {
    function walk(node) {
      if (node.nodeType === 3) return node.nodeValue || '';
      if (node.nodeType !== 1) return '';
      if (!isEligible(node)) return '';
      let text = '';
      for (const child of node.childNodes || []) text += walk(child);
      return text;
    }
    let text = '';
    for (const child of el.childNodes || []) text += walk(child);
    return normalizeWs(text);
  }

  function getAriaOnlyName(el) {
    const al = normalizeWs(el.getAttribute && el.getAttribute('aria-label'));
    if (al) return al;
    const alb = normalizeWs(el.getAttribute && el.getAttribute('aria-labelledby'));
    if (alb) {
      const parts = [];
      for (const refId of alb.split(/\s+/).filter(Boolean)) {
        try {
          const ref = document.getElementById(refId);
          if (ref) {
            const t = normalizeWs(ref.textContent);
            if (t) parts.push(t);
          }
        } catch {}
      }
      const joined = normalizeWs(parts.join(' '));
      if (joined) return joined;
    }
    return '';
  }

  // A <th> with no conflicting explicit role, plus any element carrying an
  // explicit columnheader/rowheader role (native or not).
  const selector = 'th:not([role]), [role="columnheader"], [role="rowheader"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el) continue;

    // A fully aria-hidden header cell isn't part of the AT-perceived
    // table structure at all -- queryAllSmart's default hidden-content
    // policy only excludes "hard" CSS-based hiding, not aria-hidden.
    if (!isEligible(el)) continue;

    applicableCount += 1;

    if (getVisibleText(el)) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    const ariaName = getAriaOnlyName(el);
    if (ariaName) {
      occurrences.push({
        selector: stableSelector,
        html,
        summary:
          'This table header cell has no visible text — its only accessible name comes from aria-label/aria-labelledby, which real screen-reader/browser combinations (e.g. NVDA+Firefox, iOS VoiceOver+Safari) are known to ignore on <th> elements.',
        hint: 'Add visible text content to this header cell (in addition to, or instead of, aria-label/aria-labelledby) — visible text is the only naming mechanism confirmed to work across tested screen readers.',
        i18n: {
          summaryKey: 'emptyTableHeader_summary_cantTell_ariaOnly',
          hintKey: 'emptyTableHeader_hint_cantTell_ariaOnly',
          params: {}
        },
        data: {
          details: { reasonCode: 'TABLE_HEADER_NAME_NOT_VISIBLE_TEXT', ariaName }
        }
      });
      continue;
    }

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This table header cell has no accessible name.',
      hint: 'Add text content (or aria-label/aria-labelledby) to this header cell, or remove it if it is not needed.',
      i18n: {
        summaryKey: 'emptyTableHeader_summary_cantTell',
        hintKey: 'emptyTableHeader_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'TABLE_HEADER_EMPTY' }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "focus-order-semantics": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const NON_INTERACTIVE_ROLES = new Set([
    'generic',
    'group',
    'text',
    'presentation',
    'none',
    'img',
    'heading',
    'article',
    'banner',
    'contentinfo',
    'main',
    'navigation',
    'complementary',
    'document',
    'note',
    'list',
    'listitem',
    'table',
    'row',
    'cell',
    'columnheader',
    'rowheader',
    'figure',
    'term',
    'definition',
    'paragraph',
    'caption',
    'status',
    'alert',
    'log',
    'tooltip'
  ]);

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[tabindex][role]')
    : helpers.queryAll('[tabindex][role]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const tabindexAttr = el.getAttribute('tabindex');
    const tabindex = Number.parseInt(tabindexAttr, 10);
    if (!Number.isFinite(tabindex) || tabindex < 0) continue;

    const role = (el.getAttribute('role') || '').trim().toLowerCase();
    if (!role) continue;

    applicableCount += 1;

    if (!NON_INTERACTIVE_ROLES.has(role)) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    const baseOccurrence = {
      selector: stableSelector,
      html,
      summary: `This element is in the tab order (tabindex="${tabindex}") but has a non-interactive role ("${role}").`,
      hint: 'Remove tabindex if this element is not meant to be interactive, or use an interactive role that matches its actual behavior.',
      i18n: {
        summaryKey: 'focusOrderSemantics_summary_cantTell',
        hintKey: 'focusOrderSemantics_hint_cantTell',
        params: { tabindex: String(tabindex), role }
      },
      data: {
        details: { reasonCode: 'TABBABLE_WITH_NON_INTERACTIVE_ROLE', tabindex, role }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push(baseOccurrence);
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }

  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "form-control-programmatic-label-present": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  // ACT e086e5 applies only to controls included in the accessibility tree, so
  // a focusable control inside aria-hidden is out of scope; aria-hidden-focus
  // reports that markup instead.
  const isEligibleHelper =
    helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
      ? helpers.isIncludedInAccessibilityTree
      : helpers && typeof helpers.isAccTreeEligible === 'function'
        ? helpers.isAccTreeEligible
        : null;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;
  const getLabelMethod =
    helpers && typeof helpers.getLabelMethod === 'function' ? helpers.getLabelMethod : null;

  const trim = (v) => (v == null ? '' : String(v)).trim();

  // Roles with their own *-name-present rule; a control carrying one
  // explicitly is skipped here so it is reported once.
  const ROLE_OWNED_ELSEWHERE = [
    'alertdialog',
    'button',
    'checkbox',
    'combobox',
    'dialog',
    'grid',
    'link',
    'listbox',
    'menu',
    'menubar',
    'menuitem',
    'menuitemcheckbox',
    'menuitemradio',
    'meter',
    'option',
    'progressbar',
    'radio',
    'radiogroup',
    'scrollbar',
    'searchbox',
    'slider',
    'spinbutton',
    'switch',
    'tab',
    'tablist',
    'textbox',
    'toolbar',
    'tooltip',
    'tree',
    'treeitem'
  ];

  const metrics = {
    applicableCount: 0,
    passCount: 0,
    failCount: 0,
    byMethod: {
      label: 0,
      'aria-labelledby': 0,
      'aria-label': 0,
      title: 0,
      placeholder: 0,
      none: 0
    },
    weakPassCount: 0
  };

  function getLabelStrength(method) {
    if (method === 'label' || method === 'aria-labelledby') return 'strong';
    if (method === 'aria-label') return 'medium';
    if (method === 'title' || method === 'placeholder') return 'weak';
    return 'none';
  }

  function isEligibleAcc(el) {
    if (!isEligibleHelper) return true;
    try {
      const r = isEligibleHelper(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // getLabelMethod is provided by the shared dom-helpers bundle that
  // dom-runner.js always constructs for every rule execution (built-in or
  // custom) — see createDomHelpers's own getLabelMethod, which implements
  // this exact <label>/aria-labelledby/aria-label/title/placeholder
  // priority order. No local reimplementation is needed as a fallback.
  function getLabelMethodSafe(el) {
    if (!getLabelMethod) return { method: 'none', value: '' };
    try {
      const r = getLabelMethod(el, ctx);
      const m = r && typeof r.method === 'string' ? r.method : 'none';
      const v = r && r.value != null ? trim(r.value) : '';
      if (!Object.prototype.hasOwnProperty.call(metrics.byMethod, m))
        return { method: 'none', value: '' };
      return { method: m, value: v };
    } catch {
      return { method: 'none', value: '' };
    }
  }

  // Collect candidate nodes via the shared queryAllSmart/queryAll helpers
  // (multi-root/shadow-DOM-aware, cached) rather than raw safeRoot DOM
  // access -- safeRoot is ctx.root, which is an array with multi-region
  // contextSelector support and never had a .querySelectorAll/
  // .getElementsByTagName of its own to call directly.
  const nodes = [];
  try {
    const sel = 'input,select,textarea';
    const candidates =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart(sel)
        : helpers && typeof helpers.queryAll === 'function'
          ? helpers.queryAll(sel)
          : [];

    for (const el of candidates || []) {
      if (!el || !el.getAttribute) continue;
      const tag = (el.tagName || '').toLowerCase();
      if (tag === 'input') {
        const t = trim(el.getAttribute('type')).toLowerCase();
        // exclude hidden|submit|reset|button|image
        if (t === 'hidden' || t === 'submit' || t === 'reset' || t === 'button' || t === 'image')
          continue;
      }
      nodes.push(el);
    }
  } catch {
    // no-throw
  }

  if (!nodes.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: 'minor',
      occurrences: [],
      data: { details: { metrics } }
    };
  }

  const occurrences = [];

  for (let i = 0; i < nodes.length; i++) {
    const el = nodes[i];
    if (!el || !el.getAttribute) continue;

    if (!isEligibleAcc(el)) continue;

    // role="presentation"/"none" exclusion only when NOT focusable
    let role;
    try {
      role = trim(el.getAttribute('role')).toLowerCase();
    } catch {
      role = '';
    }

    if (role && ROLE_OWNED_ELSEWHERE.indexOf(role) !== -1) continue;

    if (role === 'presentation' || role === 'none') {
      const fi = getFocusableInfo
        ? (() => {
            try {
              return getFocusableInfo(el, ctx);
            } catch {
              return null;
            }
          })()
        : null;
      const tabbable = !!(fi && fi.tabbable);
      if (!tabbable) continue;
    }

    metrics.applicableCount += 1;

    const label = getLabelMethodSafe(el);
    if (Object.prototype.hasOwnProperty.call(metrics.byMethod, label.method)) {
      metrics.byMethod[label.method] += 1;
    } else {
      metrics.byMethod.none += 1;
    }

    const strength = getLabelStrength(label.method);
    const ok = label.method !== 'none';

    if (ok) {
      metrics.passCount += 1;
      if (strength === 'weak') metrics.weakPassCount += 1;
      continue;
    }

    metrics.failCount += 1;

    const vf = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;

    const baseOccurrence = {
      summary: 'Form control is missing a programmatic label.',
      hint: 'Provide a <label> association, aria-label, aria-labelledby, title, or placeholder.',
      i18n: {
        summaryKey: 'formControl_programmaticLabelPresent_summary_fail',
        hintKey: 'formControl_programmaticLabelPresent_hint_fail',
        params: { element: (el.tagName || '').toLowerCase() }
      },
      data: {
        visibilityFilter: vf
          ? { targetSet: vf.targetSet, accEligible: vf.accEligible, reasons: vf.reasons }
          : { targetSet: 'acc', accEligible: null, reasons: [] },
        details: {
          reasonCode: 'missing_programmatic_label',
          labelMethod: 'none',
          labelStrength: 'none'
        }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (metrics.applicableCount === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: 'minor',
      occurrences: [],
      data: { details: { metrics } }
    };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences,
      data: { details: { metrics } }
    };
  }
  return {
    ruleId: rule.ruleId,
    outcome: 'pass',
    severity: 'minor',
    occurrences: [],
    data: { details: { metrics } }
  };
}), applicability: null },
    "form-control-programmatic-label-quality": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  const getLabelMethod =
    helpers && typeof helpers.getLabelMethod === 'function' ? helpers.getLabelMethod : null;

  const trim = (v) => (v == null ? '' : String(v)).trim();

  const metrics = {
    applicableCount: 0,
    flaggedCount: 0,
    byMethod: { label: 0, 'aria-labelledby': 0, 'aria-label': 0, title: 0, placeholder: 0, none: 0 }
  };

  function safeQueryAll(sel) {
    try {
      if (queryAllSmart) return Array.from(queryAllSmart(sel) || []);
      return safeRoot && safeRoot.querySelectorAll
        ? Array.from(safeRoot.querySelectorAll(sel))
        : [];
    } catch {
      return [];
    }
  }

  function isEligibleAcc(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // getLabelMethod is provided by the shared dom-helpers bundle that
  // dom-runner.js always constructs for every rule execution (built-in or
  // custom) — see createDomHelpers's own getLabelMethod, which implements
  // this exact <label>/aria-labelledby/aria-label/title/placeholder
  // priority order. No local reimplementation is needed as a fallback.
  function getLabelMethodSafe(el) {
    if (!getLabelMethod) return { method: 'none', value: '' };
    try {
      const r = getLabelMethod(el, ctx);
      const m = r && typeof r.method === 'string' ? r.method : 'none';
      const v = r && r.value != null ? trim(r.value) : '';
      if (!Object.prototype.hasOwnProperty.call(metrics.byMethod, m))
        return { method: 'none', value: '' };
      return { method: m, value: v };
    } catch {
      return { method: 'none', value: '' };
    }
  }

  // Native controls only (same as your current rule)
  const selector =
    'input:not([type="hidden"]):not([type="submit"]):not([type="reset"]):not([type="button"]):not([type="image"]),select,textarea';

  const nodes = safeQueryAll(selector);

  if (!nodes.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: 'minor',
      occurrences: [],
      data: { details: { metrics } }
    };
  }

  const occurrences = [];

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    if (!isEligibleAcc(el)) continue;

    const role = (() => {
      try {
        return trim(el.getAttribute('role')).toLowerCase();
      } catch {
        return '';
      }
    })();

    let fi = null;
    if (getFocusableInfo) {
      try {
        fi = getFocusableInfo(el, ctx);
      } catch {
        fi = null;
      }
    }
    const tabbable = !!(fi && fi.tabbable);

    if ((role === 'presentation' || role === 'none') && !tabbable) continue;

    metrics.applicableCount += 1;

    const label = getLabelMethodSafe(el);
    const method = label && typeof label.method === 'string' ? label.method : 'none';
    if (Object.prototype.hasOwnProperty.call(metrics.byMethod, method))
      metrics.byMethod[method] += 1;
    else metrics.byMethod.none += 1;

    // Flag only when the *primary* (best) method is title/placeholder
    const isWeakPrimary = method === 'title' || method === 'placeholder';
    if (!isWeakPrimary) continue;

    metrics.flaggedCount += 1;

    const vf = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const reasonCode =
      method === 'title' ? 'label_from_title_primary' : 'label_from_placeholder_primary';
    const methodLabel =
      method === 'title'
        ? 'title'
        : method === 'placeholder'
          ? 'placeholder'
          : 'title or placeholder';

    const baseOccurrence = {
      summary: 'Form control’s primary label is derived from title or placeholder.',
      hint: 'Prefer a persistent <label> or aria-labelledby. Avoid relying on placeholder/title as the primary label.',
      i18n: {
        summaryKey: 'formControl_programmaticLabelQuality_summary_cantTell',
        hintKey: 'formControl_programmaticLabelQuality_hint_cantTell',
        params: { element: (el.tagName || '').toLowerCase(), method, methodLabel }
      },
      data: {
        visibilityFilter: vf || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: {
          reasonCode,
          labelMethod: method,
          labelStrength: 'weak',
          recommendedMethods: ['label', 'aria-labelledby'],
          sourceText: label && label.value ? String(label.value).slice(0, 120) : ''
        }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (metrics.applicableCount === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: 'minor',
      occurrences: [],
      data: { details: { metrics } }
    };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences,
      data: { details: { metrics } }
    };
  }

  // Manual rules may only emit cantTell/notApplicable (never pass/fail) —
  // no applicable control relied on a weak (title/placeholder) primary
  // label, so there is nothing to flag for review.
  return {
    ruleId: rule.ruleId,
    outcome: 'notApplicable',
    severity: 'minor',
    occurrences: [],
    data: { details: { metrics } }
  };
}), applicability: null },
    "form-control-single-label": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;
  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;
  const labelContributesName =
    helpers && typeof helpers.labelContributesAccessibleName === 'function'
      ? helpers.labelContributesAccessibleName
      : null;

  const selector =
    'input:not([type="hidden"]):not([type="submit"]):not([type="reset"]):not([type="button"]):not([type="image"]),select,textarea';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  // Build a for-value -> label[] map once (avoids per-control dynamic
  // attribute-selector construction / CSS.escape, which is not guaranteed
  // to exist as a global in every runtime this engine executes in).
  const labelsByFor = new Map();
  const allLabels = document.getElementsByTagName ? document.getElementsByTagName('label') : [];
  for (const lab of allLabels) {
    if (!lab || !lab.getAttribute) continue;
    const forValue = String(lab.getAttribute('for') || '').trim();
    if (!forValue) continue;
    if (!labelsByFor.has(forValue)) labelsByFor.set(forValue, []);
    labelsByFor.get(forValue).push(lab);
  }

  const failOccurrences = [];
  const cantTellOccurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    applicableCount += 1;

    const labels = new Set();

    const wrappingLabel = el.closest ? el.closest('label') : null;
    if (wrappingLabel) labels.add(wrappingLabel);

    const controlId = String(el.getAttribute('id') || '').trim();
    if (controlId && labelsByFor.has(controlId)) {
      for (const lab of labelsByFor.get(controlId)) labels.add(lab);
    }

    const eligibleLabels = isAccTreeEligible
      ? new Set(
          [...labels].filter((lab) => {
            const elig = isAccTreeEligible(lab);
            return !elig || elig.eligible !== false;
          })
        )
      : labels;

    if (eligibleLabels.size <= 1) continue;

    // An override (aria-labelledby / aria-label) supersedes every native
    // <label>, so the labels contribute nothing to the accessible name and
    // cannot compete.
    const override = getAriaNameInfo ? getAriaNameInfo(el, ctx) : null;
    if (override && override.present && override.value) continue;

    // Without an override the labels feed the name; only labels with their
    // own text compete for it.
    const contributing = labelContributesName
      ? [...eligibleLabels].filter((lab) => labelContributesName(lab))
      : [...eligibleLabels];

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    if (contributing.length >= 2) {
      failOccurrences.push({
        selector: stableSelector,
        html,
        summary: 'This form control is associated with more than one <label>.',
        hint: 'Keep only one <label> per form control (either wrapping it or referencing it via for/id).',
        occurrenceOutcome: 'fail',
        i18n: {
          summaryKey: 'formControlSingleLabel_summary_fail',
          hintKey: 'formControlSingleLabel_hint_fail',
          params: { element: tag, labelCount: String(contributing.length) }
        },
        data: {
          details: {
            reasonCode: 'FORM_FIELD_MULTIPLE_LABELS',
            element: tag,
            labelCount: contributing.length
          }
        }
      });
    } else if (contributing.length === 1) {
      cantTellOccurrences.push({
        selector: stableSelector,
        html,
        summary:
          'This form control has one labelling <label> plus an extra empty <label> association.',
        hint: 'Remove the redundant empty <label> so exactly one <label> is associated with the control.',
        occurrenceOutcome: 'cantTell',
        i18n: {
          summaryKey: 'formControlSingleLabel_summary_cantTell',
          hintKey: 'formControlSingleLabel_hint_cantTell',
          params: { element: tag, labelCount: String(eligibleLabels.size) }
        },
        data: {
          details: {
            reasonCode: 'FORM_FIELD_EXTRA_EMPTY_LABEL',
            element: tag,
            labelCount: eligibleLabels.size,
            contributingLabelCount: contributing.length
          }
        }
      });
    }
    // contributing.length === 0: no label carries text, so nothing competes
    // for the name. A control left unnamed is form-control-programmatic-
    // label-present's concern.
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const resolved = helpers.resolveTieredOutcome
    ? helpers.resolveTieredOutcome(
        failOccurrences,
        cantTellOccurrences,
        rule.defaultSeverity || 'moderate'
      )
    : failOccurrences.length
      ? {
          outcome: 'fail',
          severity: rule.defaultSeverity || 'moderate',
          occurrences: failOccurrences
        }
      : cantTellOccurrences.length
        ? {
            outcome: 'cantTell',
            severity: rule.defaultSeverity || 'moderate',
            occurrences: cantTellOccurrences
          }
        : { outcome: 'pass', severity: 'minor', occurrences: [] };
  return { ruleId: rule.ruleId, ...resolved };
}), applicability: null },
    "heading-order": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  function getHeadingLevel(el) {
    const explicit = getExplicitRoleToken(el);
    if (explicit) {
      if (explicit !== 'heading') return 0;
      const raw = normalizeWs(el.getAttribute && el.getAttribute('aria-level'));
      const n = parseInt(raw, 10);
      return Number.isFinite(n) && n >= 1 ? n : 2;
    }
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    const m = /^h([1-6])$/.exec(tag);
    return m ? parseInt(m[1], 10) : 0;
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('h1, h2, h3, h4, h5, h6, [role]')
    : helpers.queryAll('h1, h2, h3, h4, h5, h6, [role]');

  const headings = [];
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);

    // queryAllSmart's default hidden-content policy only excludes "hard"
    // CSS-based hiding (display:none, visibility:hidden, etc.) -- it does
    // NOT exclude aria-hidden, a softer/semantic removal from the
    // accessibility tree that still leaves an element visually rendered.
    // A heading order rule whose whole premise is "the document outline
    // assistive technology users rely on" (see this rule's own header
    // comment) must not let an aria-hidden heading participate in that
    // outline at all: it's invisible to exactly the users this rule
    // exists to protect. Without this check an aria-hidden heading is both
    // flagged itself (though it isn't part of the AT-perceived outline)
    // and can mask a real skip immediately after it, by advancing the
    // "highest level reached so far" tracker on a level no AT user
    // actually encountered.
    if (helpers.isAccTreeEligible) {
      const elig = (() => {
        try {
          return helpers.isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    const level = getHeadingLevel(el);
    if (level > 0) headings.push({ el, level });
  }

  if (headings.length < 2) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let highestSoFar = headings[0].level;

  for (let i = 1; i < headings.length; i += 1) {
    const { el, level } = headings[i];

    if (level > highestSoFar + 1) {
      const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';

      occurrences.push({
        selector: stableSelector,
        html,
        summary: `This heading jumps from level ${highestSoFar} to level ${level}, skipping a level.`,
        hint: 'Use consecutive heading levels (do not skip a level when going deeper) so the document outline stays predictable.',
        i18n: {
          summaryKey: 'headingOrder_summary_cantTell',
          hintKey: 'headingOrder_hint_cantTell',
          params: { fromLevel: String(highestSoFar), toLevel: String(level) }
        },
        data: {
          details: {
            reasonCode: 'HEADING_ORDER_SKIPPED_LEVEL',
            fromLevel: highestSoFar,
            toLevel: level
          }
        }
      });
    }

    if (level > highestSoFar) highestSoFar = level;
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "html-lang-attr-present": { run: (function runInPage(ctx) {
  const { document, rule, helpers } = ctx;
  const html = document && document.documentElement;

  const tag = html && html.tagName ? String(html.tagName).toLowerCase() : '';
  if (!html || tag !== 'html') {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: 'minor',
      occurrences: []
    };
  }

  const visibilityFilter = { targetSet: 'acc', accEligible: true, reasons: [] };

  function pushFail(summaryKey, hintKey, params, details) {
    const baseOccurrence = {
      summary: '',
      hint: '',
      i18n: {
        summaryKey,
        hintKey,
        params: params && typeof params === 'object' ? params : {}
      },
      data: {
        visibilityFilter,
        details: details && typeof details === 'object' ? details : {}
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      return [helpers.reportOccurrence(html, baseOccurrence)];
    }

    // Never compute selector/snippet in the rule.
    return [{ selector: '', html: '', ...baseOccurrence }];
  }

  const rawLang = html.getAttribute('lang'); // null if missing
  const lang = (rawLang || '').trim();

  if (rawLang === null) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity,
      occurrences: pushFail(
        'html_lang_attr_missing_absent',
        'html_lang_attr_hint_missing_absent',
        {},
        { reasonCode: 'lang-missing', location: 'html' }
      )
    };
  }

  if (lang.length === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity,
      occurrences: pushFail(
        'html_lang_attr_missing_empty',
        'html_lang_attr_hint_missing_empty',
        {},
        { reasonCode: 'lang-empty', location: 'html' }
      )
    };
  }

  // Minimal BCP47 primary subtag check
  const isValidTag =
    helpers && typeof helpers.isValidLanguageTag === 'function'
      ? helpers.isValidLanguageTag
      : (v) => /^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,8})*$/.test(String(v || ''));
  if (!isValidTag(lang)) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity,
      occurrences: pushFail(
        'html_lang_attr_invalid',
        'html_lang_attr_hint_invalid',
        { lang },
        { reasonCode: 'lang-invalid-bcp47', lang }
      )
    };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'pass',
    severity: 'minor',
    occurrences: []
  };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "html-xml-lang-mismatch": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const html = document && document.documentElement;
  const tag = html && html.tagName ? String(html.tagName).toLowerCase() : '';
  if (!html || tag !== 'html') {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const lang = String(html.getAttribute('lang') || '').trim();
  const xmlLang = String(html.getAttribute('xml:lang') || '').trim();

  if (!lang || !xmlLang) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const primary = (s) => s.split('-')[0].toLowerCase();

  if (primary(lang) === primary(xmlLang)) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  const stableSelector = helpers.buildSelector ? helpers.buildSelector(html) : 'html';
  const htmlSnippet = helpers.getOuterHtmlSnippet
    ? helpers.getOuterHtmlSnippet(html)
    : (html.outerHTML || '').slice(0, 200);

  const occurrences = [
    {
      selector: stableSelector,
      html: htmlSnippet,
      summary: `The lang ("${lang}") and xml:lang ("${xmlLang}") attributes declare different languages.`,
      hint: 'Make lang and xml:lang declare the same primary language, or remove the deprecated xml:lang attribute.',
      i18n: {
        summaryKey: 'htmlXmlLangMismatch_summary_fail',
        hintKey: 'htmlXmlLangMismatch_hint_fail',
        params: { lang, xmlLang }
      },
      data: {
        details: { reasonCode: 'HTML_XML_LANG_MISMATCH', lang, xmlLang }
      }
    }
  ];

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'serious',
    occurrences
  };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "identical-links-same-purpose": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function normName(s) {
    return (s == null ? '' : String(s)).replace(/\s+/g, ' ').trim().toLowerCase();
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('a[href]')
    : helpers.queryAll('a[href]');

  const groups = new Map(); // normName -> [{ el, href }]
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const eligResult = helpers.isAccTreeEligible ? helpers.isAccTreeEligible(el, ctx) : true;
    const eligible =
      typeof eligResult === 'boolean' ? eligResult : !!(eligResult && eligResult.eligible);
    if (!eligible) continue;

    const nameInfo = helpers.getAccessibleNameInfo ? helpers.getAccessibleNameInfo(el, ctx) : null;
    const rawName =
      nameInfo && typeof nameInfo.value === 'string' && nameInfo.value.trim()
        ? nameInfo.value
        : el.textContent || '';
    const name = normName(rawName);
    if (!name) continue;

    let href;
    try {
      href = String(el.href || '');
    } catch {
      href = '';
    }
    if (!href) continue;

    applicableCount += 1;

    if (!groups.has(name)) groups.set(name, []);
    groups.get(name).push({ el, href });
  }

  const occurrences = [];

  for (const [name, entries] of groups) {
    const distinctHrefs = new Set(entries.map((e) => e.href));
    if (distinctHrefs.size <= 1) continue;

    for (const { el, href } of entries) {
      const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';

      const baseOccurrence = {
        selector: stableSelector,
        html,
        summary:
          'This link shares an accessible name with other links on the page that lead to a different destination.',
        hint: 'Ensure links with the same text serve the same purpose, or make the link text distinct enough to describe each destination.',
        i18n: {
          summaryKey: 'identicalLinksSamePurpose_summary_cantTell',
          hintKey: 'identicalLinksSamePurpose_hint_cantTell',
          params: { name, destinationCount: String(distinctHrefs.size) }
        },
        data: {
          details: {
            reasonCode: 'SAME_NAME_DIFFERENT_DESTINATION',
            name,
            href,
            distinctDestinationCount: distinctHrefs.size
          }
        }
      };

      if (helpers && typeof helpers.reportOccurrence === 'function') {
        occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
      } else {
        occurrences.push(baseOccurrence);
      }
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }

  // Manual rules may only emit cantTell/notApplicable (never pass/fail):
  // every name group already resolves to a single shared destination.
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "iframe-focusable-content": { run: (function runInPage(ctx) {
  const { helpers, rule, document } = ctx;

  // Self-contained rendering check for the embedded document (a distinct
  // realm — see this rule's own header comment on why the outer
  // document's shared eligibility helpers can't be reused here).
  // Deliberately checks only genuine non-rendering (display:none,
  // visibility:hidden, the hidden attribute) via the ancestor chain, NOT
  // aria-hidden: aria-hidden alone does not remove an element from a real
  // browser's native tab order (the same anti-pattern this engine's own
  // aria-hidden-focus rule exists to catch), so an aria-hidden-but-
  // visually-rendered focusable element inside the frame is still
  // genuinely reachable by keyboard and must stay flagged.
  function isRenderedInDoc(doc, el) {
    try {
      const view = doc.defaultView;
      if (!view || typeof view.getComputedStyle !== 'function') return true;
      let node = el;
      while (node && node.nodeType === 1) {
        if (node.hasAttribute && node.hasAttribute('hidden')) return false;
        const cs = view.getComputedStyle(node);
        if (cs) {
          if (cs.display === 'none') return false;
          if (cs.visibility === 'hidden' || cs.visibility === 'collapse') return false;
        }
        node = node.parentElement;
      }
      return true;
    } catch {
      return true;
    }
  }

  function getDeepActiveElement(docRef) {
    let cur = docRef && docRef.activeElement ? docRef.activeElement : null;
    let guard = 0;
    while (cur && cur.shadowRoot && cur.shadowRoot.activeElement && guard++ < 20) {
      cur = cur.shadowRoot.activeElement;
    }
    return cur;
  }

  function focusElementSafe(el) {
    if (!el || typeof el.focus !== 'function') return false;
    try {
      el.focus({ preventScroll: true });
      return true;
    } catch {
      try {
        el.focus();
        return true;
      } catch {
        return false;
      }
    }
  }

  function runFocusObservationWindow(win, fn) {
    if (!win || typeof fn !== 'function') return;
    const originalSetTimeout =
      typeof win.setTimeout === 'function' ? win.setTimeout.bind(win) : null;
    const originalRequestAnimationFrame =
      typeof win.requestAnimationFrame === 'function' ? win.requestAnimationFrame.bind(win) : null;
    const originalQueueMicrotask =
      typeof win.queueMicrotask === 'function' ? win.queueMicrotask.bind(win) : null;

    const queuedMicrotasks = [];
    const queuedRaf = [];
    const queuedTimers = [];
    let fakeTimerId = 1;

    const patchedSetTimeout = function (cb, delay) {
      const d = Number.isFinite(Number(delay)) ? Number(delay) : 0;
      if (typeof cb === 'function' && d <= 200) {
        const args = [];
        for (let i = 2; i < arguments.length; i++) args.push(arguments[i]);
        queuedTimers.push({ delay: d, cb: () => cb.apply(win, args) });
        return fakeTimerId++;
      }
      if (originalSetTimeout) return originalSetTimeout.apply(win, arguments);
      return fakeTimerId++;
    };

    const patchedRaf = function (cb) {
      if (typeof cb === 'function') {
        queuedRaf.push(cb);
        return fakeTimerId++;
      }
      if (originalRequestAnimationFrame) return originalRequestAnimationFrame.apply(win, arguments);
      return fakeTimerId++;
    };

    const patchedQueueMicrotask = function (cb) {
      if (typeof cb === 'function') queuedMicrotasks.push(cb);
    };

    try {
      if (originalSetTimeout) win.setTimeout = patchedSetTimeout;
      if (originalRequestAnimationFrame) win.requestAnimationFrame = patchedRaf;
      if (originalQueueMicrotask) win.queueMicrotask = patchedQueueMicrotask;
      fn();

      let guard = 0;
      while (
        (queuedMicrotasks.length || queuedRaf.length || queuedTimers.length) &&
        guard++ < 100
      ) {
        while (queuedMicrotasks.length) {
          const mt = queuedMicrotasks.shift();
          try {
            mt();
          } catch {}
        }
        while (queuedRaf.length) {
          const rf = queuedRaf.shift();
          try {
            rf(16);
          } catch {}
        }
        if (queuedTimers.length) {
          queuedTimers.sort((a, b) => a.delay - b.delay);
          const tt = queuedTimers.shift();
          try {
            tt.cb();
          } catch {}
        }
      }
    } finally {
      if (originalSetTimeout) win.setTimeout = originalSetTimeout;
      if (originalRequestAnimationFrame) win.requestAnimationFrame = originalRequestAnimationFrame;
      if (originalQueueMicrotask) win.queueMicrotask = originalQueueMicrotask;
    }
  }

  function getFocusableCandidates(doc) {
    if (!doc || !doc.querySelectorAll) return [];
    let els;
    try {
      els = doc.querySelectorAll(
        'a[href], area[href], button:not([disabled]), input:not([disabled]):not([type="hidden"]), ' +
          'select:not([disabled]), textarea:not([disabled]), iframe, [contenteditable="true"], [tabindex]'
      );
    } catch {
      return [];
    }
    const candidates = [];
    for (const el of els) {
      if (!el || !el.getAttribute) continue;
      const raw = el.getAttribute('tabindex');
      if (raw != null) {
        const n = Number(String(raw).trim());
        if (!Number.isNaN(n) && n < 0) continue; // explicitly removed from tab order
      }
      if (!isRenderedInDoc(doc, el)) continue; // display:none/visibility:hidden/[hidden]: never reachable at all
      candidates.push(el);
    }
    return candidates;
  }

  function probeImmediateFocusRedirect(frameEl, embeddedDoc, candidate) {
    if (!frameEl || !embeddedDoc || !candidate) return null;
    const embeddedWindow = embeddedDoc.defaultView;
    if (!embeddedWindow) return null;

    let focusedByEvent = false;
    const onFocusCapture = () => {
      focusedByEvent = true;
    };
    try {
      candidate.addEventListener('focus', onFocusCapture, true);
    } catch {}

    const innerFocusTrace = [];
    const outerFocusTrace = [];
    const onInnerFocusIn = (ev) => {
      if (ev && ev.target) innerFocusTrace.push(ev.target);
    };
    const onOuterFocusIn = (ev) => {
      if (ev && ev.target) outerFocusTrace.push(ev.target);
    };
    try {
      embeddedDoc.addEventListener('focusin', onInnerFocusIn, true);
      document.addEventListener('focusin', onOuterFocusIn, true);
    } catch {}

    const beforeInner = getDeepActiveElement(embeddedDoc);
    const beforeOuter = getDeepActiveElement(document);
    let focused = false;
    runFocusObservationWindow(embeddedWindow, () => {
      focused = focusElementSafe(candidate);
    });
    try {
      embeddedDoc.removeEventListener('focusin', onInnerFocusIn, true);
      document.removeEventListener('focusin', onOuterFocusIn, true);
    } catch {}
    try {
      candidate.removeEventListener('focus', onFocusCapture, true);
    } catch {}

    if (!focused) return null;

    const afterInner = getDeepActiveElement(embeddedDoc);
    const afterOuter = getDeepActiveElement(document);
    const sawRedirectedInnerTrace = innerFocusTrace.some((n) => n && n !== candidate);
    const sawRedirectedOuterTrace = outerFocusTrace.some(
      (n) => n && n !== frameEl && n !== candidate
    );
    const redirectedWithinFrame =
      !!(afterInner && afterInner !== candidate) || sawRedirectedInnerTrace;
    const redirectedOutOfFrame =
      !!(afterOuter && afterOuter !== frameEl) || sawRedirectedOuterTrace;
    const sawCandidateFocus = focusedByEvent || innerFocusTrace.some((n) => n === candidate);

    if (beforeInner && beforeInner !== afterInner) focusElementSafe(beforeInner);
    if (beforeOuter && beforeOuter !== afterOuter) focusElementSafe(beforeOuter);

    if (!sawCandidateFocus) return null;
    if (!redirectedWithinFrame && !redirectedOutOfFrame) return null;

    return {
      redirected: true,
      redirectedWithinFrame,
      redirectedOutOfFrame
    };
  }

  function getNegativeTabIndex(el) {
    const raw = el.getAttribute('tabindex');
    if (raw == null) return false;
    const n = Number(String(raw).trim());
    return !Number.isNaN(n) && n < 0;
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('iframe, frame')
    : helpers.queryAll('iframe, frame');

  const failOccurrences = [];
  const cantTellOccurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    if (!getNegativeTabIndex(el)) continue;

    let contentDoc;
    try {
      contentDoc = el.contentDocument || null;
    } catch {
      contentDoc = null;
    }
    if (!contentDoc || !contentDoc.querySelectorAll) continue; // cross-origin/unreachable: no constraint asserted

    applicableCount += 1;

    const candidates = getFocusableCandidates(contentDoc);
    if (!candidates.length) continue;

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const shouldProbe = candidates.length === 1;
    const runtimeProbe = shouldProbe
      ? probeImmediateFocusRedirect(el, contentDoc, candidates[0])
      : null;

    if (runtimeProbe && runtimeProbe.redirected) {
      cantTellOccurrences.push({
        selector: stableSelector,
        html,
        summary:
          'This frame has tabindex="-1" and a focusable candidate, but focus moves immediately to another target. Verify keyboard reachability in a real browser.',
        hint: 'If this is an intentional focus handoff, ensure keyboard users cannot remain on hidden/intermediate frame content.',
        i18n: null,
        data: {
          details: {
            reasonCode: 'IFRAME_TABINDEX_NEGATIVE_CONTENT_RUNTIME_REDIRECT',
            element: tag,
            runtimeProbe
          }
        }
      });
      continue;
    }

    failOccurrences.push({
      selector: stableSelector,
      html,
      summary:
        'This frame has tabindex="-1" but its content contains focusable elements, which remain reachable by keyboard.',
      hint: 'Remove focusable content from the frame, or remove tabindex="-1" if the frame is meant to be reachable.',
      i18n: {
        summaryKey: 'iframeFocusableContent_summary_fail',
        hintKey: 'iframeFocusableContent_hint_fail',
        params: { element: tag }
      },
      data: {
        details: { reasonCode: 'IFRAME_TABINDEX_NEGATIVE_CONTENT_FOCUSABLE', element: tag }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  // helpers.resolveTieredOutcome is unconditionally provided by dom-helpers.js
  // (see its own header comment) -- no fallback needed, matching the same
  // cleanup already applied to aria-hidden-focus.js/aria-prohibited-attr.js/
  // target-size-minimum.js.
  const resolved = helpers.resolveTieredOutcome(
    failOccurrences,
    cantTellOccurrences,
    rule.defaultSeverity || 'moderate'
  );
  return { ruleId: rule.ruleId, ...resolved };
}), applicability: null },
    "iframe-name-present": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('iframe, frame')
    : helpers.queryAll('iframe, frame');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.tagName) continue;

    if (helpers.isAccTreeEligible) {
      const elig = helpers.isAccTreeEligible(el);
      const isEligible = typeof elig === 'boolean' ? elig : !!(elig && elig.eligible);
      if (!isEligible) continue;
    }

    applicableCount += 1;

    const nameInfo = helpers.getAccessibleNameInfo
      ? helpers.getAccessibleNameInfo(el, ctx, { maxRefs: 8 })
      : null;
    if (nameInfo && nameInfo.present && nameInfo.value) continue;

    const eligInfo = helpers.getEligibilityInfo
      ? (() => {
          try {
            return helpers.getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This frame has no accessible name.',
      hint: 'Add a title attribute (or aria-label/aria-labelledby) describing the frame’s content or purpose.',
      i18n: {
        summaryKey: 'iframeNamePresent_summary_fail',
        hintKey: 'iframeNamePresent_hint_fail',
        params: { element: tag }
      },
      data: {
        details: { reasonCode: 'IFRAME_NAME_MISSING', element: tag },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "iframe-title-unique": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('iframe, frame')
    : helpers.queryAll('iframe, frame');

  const groups = new Map(); // trimmed title -> elements[]
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const title = String(el.getAttribute('title') || '').trim();
    if (!title) continue;

    applicableCount += 1;

    const list = groups.get(title);
    if (list) list.push(el);
    else groups.set(title, [el]);
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];

  for (const [title, els] of groups) {
    if (els.length < 2) continue;

    for (const el of els) {
      const tag = el.tagName.toLowerCase();
      const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';

      occurrences.push({
        selector: stableSelector,
        html,
        summary: 'This frame’s title is not unique among the frames on this page.',
        hint: 'Give each frame a distinct title describing its specific content or purpose.',
        i18n: {
          summaryKey: 'iframeTitleUnique_summary_fail',
          hintKey: 'iframeTitleUnique_hint_fail',
          params: { element: tag, title }
        },
        data: {
          details: {
            reasonCode: 'IFRAME_TITLE_DUPLICATE',
            element: tag,
            title,
            duplicateCount: els.length
          }
        }
      });
    }
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "image-redundant-alt": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getOwnTextExcludingImg(parent, imgEl) {
    const isAccTreeEligible =
      helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

    let text = '';
    for (const child of parent.childNodes || []) {
      if (child === imgEl) continue;
      if (child.nodeType === 3) {
        text += ' ' + (child.nodeValue || '');
      } else if (child.nodeType === 1 && child !== imgEl) {
        // An aria-hidden sibling is never actually announced to assistive
        // technology, so its text can't cause the "same words twice"
        // double-announcement this rule exists to catch -- counting it
        // anyway flags a redundancy that doesn't exist in what AT users
        // actually hear.
        if (isAccTreeEligible) {
          const elig = (() => {
            try {
              return isAccTreeEligible(child, ctx);
            } catch {
              return { eligible: true, reasons: [] };
            }
          })();
          if (elig && elig.eligible === false) continue;
        }
        text += ' ' + (child.textContent || '');
      }
    }
    return normalizeWs(text);
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('img[alt]')
    : helpers.queryAll('img[alt]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const alt = normalizeWs(el.getAttribute('alt'));
    if (!alt) continue;

    const parent = el.parentElement;
    if (!parent) continue;

    const otherText = getOwnTextExcludingImg(parent, el);
    if (!otherText) continue;

    applicableCount += 1;

    if (otherText.toLowerCase() !== alt.toLowerCase()) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: "This image's alt text duplicates other visible text right next to it.",
      hint: 'Make the alt text empty (alt="") if the image is purely decorative alongside the text, or remove the redundant duplication.',
      i18n: {
        summaryKey: 'imageRedundantAlt_summary_cantTell',
        hintKey: 'imageRedundantAlt_hint_cantTell',
        params: { alt }
      },
      data: {
        details: { reasonCode: 'IMAGE_ALT_REDUNDANT', alt }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "img-alt-decorative": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  const els = (() => {
    // Only likely candidates:
    // - alt="" (exact)
    // - alt that starts/ends with space (to catch whitespace-only like "   ")
    const sel = 'img[alt=""], img[alt^=" "], img[alt$=" "]';
    try {
      return Array.from((queryAllSmart ? queryAllSmart(sel) : queryAll(sel)) || []);
    } catch {
      return queryAll(sel);
    }
  })();
  const uniqueEls = [];
  const seen = new Set();
  for (const el of els) {
    if (!seen.has(el)) {
      seen.add(el);
      uniqueEls.push(el);
    }
  }

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of uniqueEls) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    // Rule-specific applicability (only elements that already have a text alternative mechanism)
    const rawAlt = el.getAttribute('alt');
    if (rawAlt == null) continue;
    if (String(rawAlt).trim() !== '') continue;

    applicableCount += 1;

    const baseOccurrence = {
      summary: 'Review whether <img> is decorative (alt="").',
      hint: 'Confirm the image is purely decorative. If it conveys information or function, provide meaningful alt text.',
      i18n: {
        summaryKey: 'img_altDecorative_summary_cantTell',
        hintKey: 'img_altDecorative_hint_cantTell',
        params: { element: 'img' }
      },
      data: {
        visibilityFilter: { targetSet: 'acc', accEligible: null, reasons: [] },
        details: null
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "img-alt-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  // ACT 23a2a8 exempts programmatically hidden images, and that glossary term
  // has no focusability carve-out, so an aria-hidden image stays out of scope
  // even when tabbable; aria-hidden-focus reports that markup instead.
  const isEligibleHelper =
    helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
      ? helpers.isIncludedInAccessibilityTree
      : helpers && typeof helpers.isAccTreeEligible === 'function'
        ? helpers.isAccTreeEligible
        : null;
  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;
  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;

  // Prefer tagName collection when available (cheap), otherwise fall back.
  function getImgsCollection() {
    try {
      if (queryAllSmart) {
        const r = queryAllSmart('img');
        return Array.isArray(r) ? r : Array.from(r || []);
      }
    } catch {
      // fall through
    }

    try {
      if (safeRoot && typeof safeRoot.getElementsByTagName === 'function') {
        return safeRoot.getElementsByTagName('img'); // HTMLCollection (live)
      }
    } catch {
      // fall through
    }

    try {
      if (safeRoot && typeof safeRoot.querySelectorAll === 'function')
        return safeRoot.querySelectorAll('img');
    } catch {
      // fall through
    }

    return [];
  }

  const imgs = getImgsCollection();
  const imgLen = imgs && typeof imgs.length === 'number' ? imgs.length : 0;

  if (!imgLen) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  // Helper-safe trim
  const trim = (v) => (v == null ? '' : String(v)).trim();

  for (let i = 0; i < imgLen; i++) {
    const el = imgs[i];
    if (!el || !el.getAttribute) continue;

    // Eligibility: only imgs exposed to assistive tech.
    if (isEligibleHelper) {
      let elig;
      try {
        elig = isEligibleHelper(el, ctx);
      } catch {
        elig = null;
      }
      if (elig && elig.eligible === false) continue;
      // If helper returns boolean, treat false as ineligible.
      if (typeof elig === 'boolean' && elig === false) continue;
    }

    // Role (presentation/none) exclusion only when NOT focusable.
    let role;
    try {
      role = trim(el.getAttribute('role')).toLowerCase();
    } catch {
      role = '';
    }

    if (role === 'presentation' || role === 'none') {
      let focusable;
      try {
        const fi = getFocusableInfo(el, ctx);
        // Preserve existing behavior: prefer fi.focusable; fall back to fi.tabbable if present.
        focusable = !!(fi && (fi.focusable || fi.tabbable));
      } catch {
        focusable = false;
      }

      if (!focusable) continue;
    }

    // From here: applicable
    applicableCount += 1;

    // alt attribute presence check (empty allowed)
    let hasAlt;
    try {
      hasAlt = el.getAttribute('alt') !== null;
    } catch {
      hasAlt = false;
    }
    if (hasAlt) continue;

    // aria-label / aria-labelledby is also a valid, standards-recognized
    // text-alternative mechanism for <img> (HTML-AAM accessible name
    // computation includes ARIA naming before falling back to alt); an
    // image with a non-empty ARIA name is not missing a text alternative
    // just because it lacks an alt attribute.
    if (getAriaNameInfo) {
      let ariaName;
      try {
        ariaName = getAriaNameInfo(el, ctx);
      } catch {
        ariaName = null;
      }
      if (ariaName && ariaName.present) continue;
    }

    // A non-empty title attribute is HTML-AAM's next fallback naming source
    // for <img> once alt is entirely absent (not merely alt="", which
    // explicitly marks decorative and stays excluded from this branch since
    // hasAlt already short-circuited above). An `<img src="..." title="...">`
    // with no alt attribute at all is not missing a text alternative.
    const title = trim(el.getAttribute('title'));
    if (title) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;

    const baseOccurrence = {
      summary: 'Missing alt attribute on <img>.',
      hint: 'Add an alt attribute (use alt="" only for decorative images).',
      i18n: {
        summaryKey: 'img_altPresent_summary_fail',
        hintKey: 'img_altPresent_hint_fail',
        params: {}
      },
      data: { visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] } }
    };

    occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "img-alt-quality": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  // Cap occurrences to keep manual “quality” checks fast on large pages.
  // Deterministic: we keep DOM order, just stop collecting after N.
  const MAX_OCCURRENCES = 50;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  const selector = 'img[alt]:not([alt=""])';
  const els = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart(selector) : queryAll(selector)) || []);
    } catch {
      return queryAll(selector);
    }
  })();

  if (!els.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: 'minor',
      occurrences: [],
      data: {
        details: {
          applicableCount: 0,
          reportedCount: 0,
          maxOccurrences: MAX_OCCURRENCES,
          truncated: false
        }
      }
    };
  }

  const occurrences = [];
  let applicableCount = 0; // total applicable elements
  let collectedCount = 0; // how many occurrences we actually reported

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    // Rule-specific applicability: non-empty alt
    const alt = (() => {
      try {
        return String(el.getAttribute('alt') || '').trim();
      } catch {
        return '';
      }
    })();
    if (!alt) continue;

    applicableCount += 1;

    // IMPORTANT: stop doing expensive occurrence building after we hit the cap
    if (collectedCount >= MAX_OCCURRENCES) continue;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;
    const baseOccurrence = {
      summary: 'Review alt text on <img> for accuracy and appropriateness.',
      hint: 'Ensure the alt text conveys the image’s purpose/information in context (not redundant, not filename-like).',
      i18n: {
        summaryKey: 'img_altQuality_summary_cantTell',
        hintKey: 'img_altQuality_hint_cantTell',
        params: { element: 'img' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: null
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }

    collectedCount += 1;
  }

  if (applicableCount === 0) {
    return {
      ruleId: rule.ruleId,
      outcome: 'notApplicable',
      severity: 'minor',
      occurrences: [],
      data: {
        details: {
          applicableCount: 0,
          reportedCount: 0,
          maxOccurrences: MAX_OCCURRENCES,
          truncated: false
        }
      }
    };
  }

  const truncated = applicableCount > collectedCount;

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: 'minor',
    occurrences,
    data: {
      details: {
        applicableCount,
        reportedCount: collectedCount,
        maxOccurrences: MAX_OCCURRENCES,
        truncated
      }
    }
  };
}), applicability: null },
    "input-image-alt-decorative": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  // alt="" plus a name from aria-label/aria-labelledby/title is the judgement
  // call this rule reviews. alt="" with no other source leaves the control
  // unnamed, which input-image-alt-present fails outright.
  function hasNameFromOtherSource(el) {
    if (getAriaNameInfo) {
      try {
        const aria = getAriaNameInfo(el, ctx);
        if (aria && aria.present && String(aria.value || '').trim()) return true;
      } catch {
        // fall through to title
      }
    }
    try {
      const title = el.getAttribute('title');
      return title != null && String(title).trim() !== '';
    } catch {
      return false;
    }
  }

  const els = (() => {
    try {
      return Array.from(
        (queryAllSmart ? queryAllSmart('input[type="image"]') : queryAll('input[type="image"]')) ||
          []
      );
    } catch {
      return queryAll('input[type="image"]');
    }
  })();

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    // Rule-specific applicability (only elements that already have a text alternative mechanism)
    if (!(el.getAttribute('alt') != null && String(el.getAttribute('alt')).trim() === '')) continue;
    if (!hasNameFromOtherSource(el)) continue;

    applicableCount += 1;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Review <input type="image"> with alt="".',
      hint: 'This control is typically functional. Confirm it has an equivalent accessible name elsewhere, or provide meaningful alt text.',
      i18n: {
        summaryKey: 'inputImage_altDecorative_summary_cantTell',
        hintKey: 'inputImage_altDecorative_hint_cantTell',
        params: { element: 'input[type=image]' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: null
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "input-image-alt-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  // ACT 59796f applies to image buttons included in the accessibility tree,
  // which excludes focusable content inside aria-hidden.
  const isEligibleHelper =
    helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
      ? helpers.isIncludedInAccessibilityTree
      : helpers && typeof helpers.isAccTreeEligible === 'function'
        ? helpers.isAccTreeEligible
        : null;

  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;

  const inputs = (() => {
    try {
      return Array.from(
        (queryAllSmart ? queryAllSmart('input[type="image"]') : queryAll('input[type="image"]')) ||
          []
      );
    } catch {
      return queryAll('input[type="image"]');
    }
  })();

  if (!inputs.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of inputs) {
    if (!el || !el.getAttribute) continue;

    if (isEligibleHelper) {
      const elig = (() => {
        try {
          return isEligibleHelper(el, ctx);
        } catch {
          return true;
        }
      })();
      const eligible = typeof elig === 'boolean' ? elig : !(elig && elig.eligible === false);
      if (!eligible) continue;
    }

    applicableCount += 1;

    // The browser's own fallback name for an image button carries no
    // information, so an author-supplied name equal to it is treated as no
    // name at all (ACT 59796f). Only the English defaults are recognised:
    // "Submit Query" from HTML-AAM, "Submit" from Chrome.
    const effectiveName = (() => {
      let v = '';
      if (getAriaNameInfo) {
        try {
          const aria = getAriaNameInfo(el, ctx);
          if (aria && aria.present && aria.value) v = String(aria.value);
        } catch {
          v = '';
        }
      }
      if (!v) {
        const alt = el.getAttribute('alt');
        if (alt != null && String(alt).trim()) v = String(alt);
      }
      if (!v) {
        const t = el.getAttribute('title');
        if (t != null && String(t).trim()) v = String(t);
      }
      return v.trim().toLowerCase();
    })();

    if (effectiveName === 'submit query' || effectiveName === 'submit') {
      const eligInfoDefault = getEligibilityInfo
        ? getEligibilityInfo(el, ctx, { targetSet: 'acc' })
        : null;
      const defaultNameOccurrence = {
        summary:
          'Accessible name is the browser default for an image button, which conveys nothing.',
        hint: 'Replace it with text describing what the button does, for example "Search".',
        i18n: {
          summaryKey: 'inputImage_altPresent_summary_defaultName',
          hintKey: 'inputImage_altPresent_hint_defaultName',
          params: { element: 'input[type=image]' }
        },
        data: {
          visibilityFilter: eligInfoDefault || { targetSet: 'acc', accEligible: null, reasons: [] },
          details: { reasonCode: 'default_name' }
        }
      };
      occurrences.push(
        helpers && typeof helpers.reportOccurrence === 'function'
          ? helpers.reportOccurrence(el, defaultNameOccurrence)
          : { selector: '', html: '', ...defaultNameOccurrence }
      );
      continue;
    }

    // effectiveName already covers the naming sources HTML-AAM allows here,
    // in order: aria-label/aria-labelledby, then alt, then title.
    if (effectiveName) continue;

    // An image button is a control, so an empty name fails whether alt is
    // absent or present-but-empty. alt="" marks a decorative image, and an
    // image button is never decorative.
    const emptyAlt = el.getAttribute('alt') !== null;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = emptyAlt
      ? {
          summary: 'Empty alt="" on <input type="image"> leaves the control unnamed.',
          hint: 'Describe the action in alt, or name the control with aria-label or aria-labelledby.',
          i18n: {
            summaryKey: 'inputImage_altPresent_summary_emptyAlt',
            hintKey: 'inputImage_altPresent_hint_emptyAlt',
            params: { element: 'input[type=image]' }
          },
          data: {
            visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
            details: { reasonCode: 'empty_alt' }
          }
        }
      : {
          summary: 'Missing alt attribute on <input type="image">.',
          hint: 'Add an alt attribute (use alt="" only when a separate accessible name is provided).',
          i18n: {
            summaryKey: 'inputImage_altPresent_summary_fail',
            hintKey: 'inputImage_altPresent_hint_fail',
            params: { element: 'input[type=image]' }
          },
          data: {
            visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
            details: { reasonCode: 'missing_alt' }
          }
        };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      // Never compute selector/snippet in the rule.
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "input-image-alt-quality": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  const els = (() => {
    try {
      return Array.from(
        (queryAllSmart ? queryAllSmart('input[type="image"]') : queryAll('input[type="image"]')) ||
          []
      );
    } catch {
      return queryAll('input[type="image"]');
    }
  })();

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    // Rule-specific applicability (only elements that already have a text alternative mechanism)
    if (!(el.getAttribute('alt') != null && String(el.getAttribute('alt')).trim() !== '')) continue;

    applicableCount += 1;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Review alt text on <input type="image"> for accuracy and appropriateness.',
      hint: 'Ensure the alt text describes the control’s action (e.g., “Search”, “Submit order”) in context.',
      i18n: {
        summaryKey: 'inputImage_altQuality_summary_cantTell',
        hintKey: 'inputImage_altQuality_hint_cantTell',
        params: { element: 'input[type=image]' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: { alt: String(el.getAttribute('alt') || '') } // optional but useful
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "label-in-name": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const occurrences = [];
  let applicableCount = 0;

  // Applicability: focus/activation controls with explicit ARIA naming.
  // NOTE: aria-hidden is intentionally NOT excluded here; it does not affect visual rendering.
  const selector =
    ':is(button, a[href], summary, input:not([type="hidden"]), textarea, select, [role="button"], [role="link"], [role="checkbox"], [role="radio"], [role="switch"], [role="searchbox"], [role="tab"], [role="menuitem"], [role="menuitemcheckbox"], [role="menuitemradio"], [role="option"], [role="treeitem"], [role="gridcell"]):not([hidden]):not([disabled]):not([aria-disabled="true"]):is([aria-label], [aria-labelledby])';

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function norm(s) {
    const v = s == null ? '' : String(s);
    // deterministic normalization: trim + collapse whitespace + case-fold
    return v.replace(/\s+/g, ' ').trim().toLowerCase();
  }

  // WCAG 2.5.3's label-in-name comparison is over words, not characters: drop
  // parenthesised text, case-fold and NFKD-normalise, then reduce every
  // non-letter/digit to a space. `hyphensJoin` deletes hyphens instead of
  // splitting on them, which distinguishes a real mismatch from one that is
  // only a hyphenation difference.
  function tokenize(s, hyphensJoin) {
    let v = (s == null ? '' : String(s)).replace(/\([^)]*\)/g, ' ').toLowerCase();
    try {
      v = v.normalize('NFKD');
    } catch {
      // Realm without String#normalize: the word comparison below still holds.
    }
    if (hyphensJoin) v = v.replace(/[-‐-―−]/g, '');
    return v
      .replace(/[^\p{L}\p{N}]+/gu, ' ')
      .split(' ')
      .filter(Boolean);
  }

  // The label's words must appear adjacent and in order inside the name, so a
  // scattered subsequence does not count. Words in `prefixable` may match a
  // longer name word they start.
  function containsWordRun(needle, hay, prefixable) {
    if (!needle.length) return true;
    for (let i = 0; i + needle.length <= hay.length; i++) {
      let ok = true;
      for (let j = 0; j < needle.length; j++) {
        const want = needle[j];
        const got = hay[i + j];
        if (got === want) continue;
        if (prefixable && prefixable.has(want) && got.indexOf(want) === 0) continue;
        ok = false;
        break;
      }
      if (ok) return true;
    }
    return false;
  }

  // A trailing period marks a word the author may have shortened ("Ave." for
  // "Avenue"). Tokenizing removes the period, so collect these beforehand.
  function abbreviatedWords(s) {
    const out = new Set();
    for (const w of (s == null ? '' : String(s)).split(/\s+/)) {
      const m = /^([\p{L}\p{N}]+)\.$/u.exec(w);
      if (m) out.add(m[1].toLowerCase());
    }
    return out;
  }

  function getElementDescriptor(el) {
    const tag = el && el.tagName ? String(el.tagName).toLowerCase() : 'element';
    let role;
    try {
      role = el && el.getAttribute ? el.getAttribute('role') || '' : '';
    } catch {
      role = '';
    }
    const r = String(role || '').trim();
    return r ? `${tag}[role="${r}"]` : tag;
  }

  function clipForSummary(s) {
    const v = (s == null ? '' : String(s)).replace(/\s+/g, ' ').trim();
    if (v.length <= 80) return v;
    return v.slice(0, 77) + '...';
  }

  function isNonRenderedTag(el) {
    const tn = el && el.tagName ? String(el.tagName).toLowerCase() : '';
    return (
      tn === 'script' ||
      tn === 'style' ||
      tn === 'template' ||
      tn === 'noscript' ||
      tn === 'meta' ||
      tn === 'link'
    );
  }

  function isDomVisible(el) {
    if (!el) return false;
    if (helpers.isDomVisibleEligible)
      return !!helpers.isDomVisibleEligible(el, ctx, { targetSet: 'dom' }).eligible;
    if (helpers.getEligibilityInfo)
      return !!helpers.getEligibilityInfo(el, ctx, { targetSet: 'dom' }).eligible;
    return true;
  }

  // Unlike isDomVisible above, this also excludes aria-hidden subtrees.
  // Needed for collectVisibleTextUnder's per-text-node check below: an
  // aria-hidden icon-font glyph (e.g. an <i aria-hidden="true"> ligature
  // rendering as "format_color_fill") is DOM-visible pixels but is never
  // perceived as literal readable words the way real visible text is.
  // Excluding aria-hidden content is a cheap static-markup signal that gets
  // the common case (decorative icon fonts) right — an icon-only button
  // named via aria-label shouldn't have its glyph name counted as text.
  function isAccEligible(el) {
    if (!el) return false;
    const fn =
      helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;
    if (!fn) return isDomVisible(el);
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return isDomVisible(el);
    }
  }

  // DOM's NodeFilter.SHOW_TEXT constant, inlined as a numeric literal rather
  // than referencing the global NodeFilter object directly: runInPage must
  // have zero free vars (see docs/RULE_AUTHORING.md's free-var footgun) and
  // NodeFilter is not itself present in the execution realm this function
  // actually runs in, unlike window/document. Referencing the global
  // directly would silently make createTreeWalker throw on every call,
  // falling back to raw container.textContent (which respects none of
  // isNonRenderedTag/isDomVisible/isAccEligible below, since that whole
  // per-node loop is skipped in the fallback path). Same pattern already
  // used correctly in
  // region-manual.js's own createTreeWalker call.
  const SHOW_TEXT = 4;

  function collectVisibleTextUnder(container) {
    if (!container) return '';
    if (!isDomVisible(container)) return '';

    // TreeWalker is deterministic in document order.
    let walker;
    try {
      walker = document.createTreeWalker(container, SHOW_TEXT, null);
    } catch {
      walker = null;
    }
    if (!walker) {
      try {
        const t = container.textContent;
        return t == null ? '' : String(t);
      } catch {
        return '';
      }
    }

    const parts = [];
    let n;
    // eslint-disable-next-line no-cond-assign
    while ((n = walker.nextNode())) {
      try {
        const raw = n && n.nodeValue != null ? String(n.nodeValue) : '';
        const t = raw.replace(/\s+/g, ' ').trim();
        if (!t) continue;

        const p = n.parentElement || null;
        if (!p || !p.tagName) continue;
        if (isNonRenderedTag(p)) continue;

        // Require the parent element to be visually eligible AND not inside
        // an aria-hidden subtree (see isAccEligible's docblock above).
        if (!isAccEligible(p)) continue;

        parts.push(t);
      } catch {
        // no-throws
      }
    }
    return parts.join(' ').replace(/\s+/g, ' ').trim();
  }

  function getAssociatedLabelElements(control) {
    const labels = [];
    try {
      if (control && control.labels && typeof control.labels.length === 'number') {
        for (const l of control.labels) labels.push(l);
      }
    } catch {
      // ignore
    }

    // Fallback: wrapped label
    try {
      const w = control && control.closest ? control.closest('label') : null;
      if (w) labels.push(w);
    } catch {
      // ignore
    }

    // Fallback: label[for=id]. Uses `document` directly rather than
    // ctx.root -- label[for] association is a document-wide relationship
    // (IDs are document-unique), not bounded by whatever contextSelector
    // region happens to be scanned, and ctx.root is an array (multi-region
    // contextSelector support), not a single element with its own
    // .querySelector to call directly.
    try {
      const idAttribute = control && control.getAttribute ? control.getAttribute('id') || '' : '';
      const key = String(idAttribute || '').trim();
      if (key && document && document.querySelector) {
        const l = document.querySelector('label[for="' + CSS.escape(key) + '"]');
        if (l) labels.push(l);
      }
    } catch {
      // ignore
    }

    // De-dupe in document order
    const seen = new Set();
    const out = [];
    for (const l of labels) {
      try {
        if (!l || !l.tagName) continue;
        if (seen.has(l)) continue;
        seen.add(l);
        out.push(l);
      } catch {
        // ignore
      }
    }
    return out;
  }

  function getVisibleTextLabelInfo(el) {
    // Returns { text, source } where source helps reporting.
    // Source policy (deterministic):
    // 1) <label> association for form controls
    // 2) visible text inside the element
    // 3) visible text from aria-labelledby referenced elements
    let text;
    let source = 'none';

    const tn = el && el.tagName ? String(el.tagName).toLowerCase() : '';

    // 1) Label association (native form controls)
    const isFormControl = tn === 'input' || tn === 'select' || tn === 'textarea';
    if (isFormControl) {
      const labels = getAssociatedLabelElements(el);
      const labelParts = [];
      for (const l of labels) {
        if (!l || !isDomVisible(l)) continue;
        const t = collectVisibleTextUnder(l);
        if (t) labelParts.push(t);
      }
      const joined = labelParts.join(' ').replace(/\s+/g, ' ').trim();
      if (joined) return { text: joined, source: 'label' };
    }

    // 2) Text inside the control itself
    text = collectVisibleTextUnder(el);
    if (text) return { text, source: 'self' };

    // 3) aria-labelledby referenced visible text (only if refs exist and are visible)
    try {
      const idrefs = el && el.getAttribute ? el.getAttribute('aria-labelledby') : null;
      if (idrefs && helpers.resolveIdRefs) {
        const r = helpers.resolveIdRefs(idrefs, ctx, { maxRefs: 8 });
        const parts = [];
        for (const ref of r && Array.isArray(r.refs) ? r.refs : []) {
          if (!ref || !ref.tagName) continue;
          if (!isDomVisible(ref)) continue;
          const t = collectVisibleTextUnder(ref);
          if (t) parts.push(t);
        }
        const joined = parts.join(' ').replace(/\s+/g, ' ').trim();
        if (joined) return { text: joined, source: 'aria-labelledby' };
      }
    } catch {
      // ignore
    }

    return { text: '', source };
  }

  for (const el of nodes) {
    // Gate: control must be visually rendered (SC is about visual labels).
    if (!isDomVisible(el)) continue;

    const labelInfo = getVisibleTextLabelInfo(el);
    const visibleLabel = labelInfo && labelInfo.text ? String(labelInfo.text) : '';
    const visibleNorm = norm(visibleLabel);

    // Applicability: only when we can deterministically extract visible label text.
    if (!visibleNorm) continue;

    applicableCount += 1;

    let acc = { present: false, value: '', mechanism: 'none', flags: [] };
    try {
      acc = helpers.getAccessibleNameInfo
        ? helpers.getAccessibleNameInfo(el, ctx, { maxRefs: 8 })
        : acc;
    } catch {
      acc = { present: false, value: '', mechanism: 'none', flags: ['exception'] };
    }
    const accName = acc && acc.value != null ? String(acc.value) : '';
    const accNorm = norm(accName);

    const labelTokens = tokenize(visibleLabel, false);
    const nameTokens = tokenize(accName, false);
    const contains = containsWordRun(labelTokens, nameTokens, null);

    // An abbreviation, or a word hyphenated differently in the two places, is
    // not something markup settles: the author may have meant either. Report
    // without asserting a defect instead of failing or staying silent.
    let uncertainty = '';
    if (!contains) {
      if (containsWordRun(tokenize(visibleLabel, true), tokenize(accName, true), null)) {
        uncertainty = 'HYPHENATION_DIFFERS';
      } else {
        const abbreviated = abbreviatedWords(visibleLabel);
        if (abbreviated.size && containsWordRun(labelTokens, nameTokens, abbreviated))
          uncertainty = 'POSSIBLE_ABBREVIATION';
      }
    }

    if (!contains) {
      const selectorOut = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';

      occurrences.push({
        selector: selectorOut,
        html,
        ...(uncertainty ? { outcome: 'cantTell' } : null),
        summary: uncertainty
          ? 'Accessible name may not contain the visible label text.'
          : 'Accessible name does not contain the visible label text.',
        hint: uncertainty
          ? 'Check by hand: the two differ only by an abbreviation or by hyphenation, which markup cannot settle.'
          : 'Ensure the accessible name includes the visible text label (e.g., update aria-label/aria-labelledby to include the visible wording).',
        i18n: {
          summaryKey: uncertainty ? 'labelInName_summary_cantTell' : 'labelInName_summary_fail',
          hintKey: uncertainty ? 'labelInName_hint_cantTell' : 'labelInName_hint_fail',
          params: {
            element: getElementDescriptor(el),
            visibleLabel: clipForSummary(visibleLabel),
            labelSource: labelInfo && labelInfo.source ? labelInfo.source : 'none',
            nameMechanism: acc && acc.mechanism ? acc.mechanism : 'none'
          }
        },
        data: {
          details: {
            reasonCode: uncertainty || 'VISIBLE_LABEL_NOT_IN_ACCESSIBLE_NAME',
            visibleLabel,
            accessibleName: accName,
            normalized: { visibleLabel: visibleNorm, accessibleName: accNorm },
            tokenized: { visibleLabel: labelTokens, accessibleName: nameTokens },
            labelSource: labelInfo && labelInfo.source ? labelInfo.source : 'none',
            nameMechanism: acc && acc.mechanism ? acc.mechanism : 'none'
          }
        }
      });
    }
  }

  if (applicableCount === 0)
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  if (occurrences.length) {
    const anyFail = occurrences.some((o) => o.outcome !== 'cantTell');
    return {
      ruleId: rule.ruleId,
      outcome: anyFail ? 'fail' : 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "label-title-only": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const selector =
    'input:not([type="hidden"]):not([type="submit"]):not([type="reset"]):not([type="button"]):not([type="image"]),select,textarea';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    if (helpers.isAccTreeEligible) {
      const elig = (() => {
        try {
          return helpers.isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    const title = String(el.getAttribute('title') || '').trim();
    if (!title) continue;

    applicableCount += 1;

    // Delegates to the shared helpers.getAccessibleNameInfo (aria ->
    // native <label> -> title, the same precedence every other
    // name-dependent rule in this engine uses) rather than a local,
    // hand-rolled "does a <label for>/wrapping <label> exist" check. A
    // structural-association-only check (for="..."/wrapping) never verifies
    // the label actually contributes a name -- an empty <label for="x">
    // </label> or empty wrapping <label> would exempt the control even
    // though title is functionally its only real label (see
    // dom-helpers.js's hasLabelAssociation/labelContributesAccessibleName).
    // If the resolved mechanism isn't 'title', some higher-priority
    // mechanism (aria-label/aria-labelledby/a real contributing label)
    // already won and this control isn't title-only.
    const nameInfo = helpers.getAccessibleNameInfo ? helpers.getAccessibleNameInfo(el, ctx) : null;
    if (!nameInfo || nameInfo.mechanism !== 'title') continue;

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This form control relies on the title attribute as its only label.',
      hint: 'Add a visible <label> (or aria-label/aria-labelledby) in addition to, or instead of, the title attribute.',
      i18n: {
        summaryKey: 'labelTitleOnly_summary_cantTell',
        hintKey: 'labelTitleOnly_hint_cantTell',
        params: { element: tag }
      },
      data: {
        details: { reasonCode: 'LABEL_TITLE_ONLY', element: tag }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "landmark-banner-is-top-level": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;

  // Declared inside runInPage — see scripts/build-core.js header
  // ("runInPage MUST be self-contained").
  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  // Delegates to the shared helpers.getLandmarkNameInfo (aria-label -> aria-labelledby, via the
  // target's own accessible name, not raw textContent -> title attribute fallback) rather than a
  // local copy -- see that function's header comment in src/core/dom-helpers.js. Sharing it keeps
  // the title-attribute fallback consistent across the landmark rules.
  function getAccessibleLandmarkName(el) {
    try {
      if (helpers && typeof helpers.getLandmarkNameInfo === 'function') {
        const info = helpers.getLandmarkNameInfo(el, ctx);
        if (info && info.present && info.value) return normalizeWs(info.value);
      }
    } catch {}
    return '';
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  // Delegates to the shared helpers.hasLandmarkScopingAncestor for the
  // question "does this element sit inside a sectioning-content/<main>
  // ancestor that suppresses its conditional implicit role" — role-aware
  // (an ancestor's bare TAG only counts when it carries no role attribute
  // at all; an explicit role="dialog"-style override no longer suppresses)
  // rather than a local tag-only copy. See that function's header comment
  // in src/core/aria-helpers.js for the full algorithm. Example: an
  // <aside role="dialog"> containing its own <header>.
  function hasSectioningAncestor(el, includeMain) {
    return helpers && typeof helpers.hasLandmarkScopingAncestor === 'function'
      ? helpers.hasLandmarkScopingAncestor(el, { includeMain })
      : false;
  }

  function getImplicitLandmarkRole(el) {
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    if (tag === 'header') return hasSectioningAncestor(el, true) ? '' : 'banner';
    if (tag === 'footer') return hasSectioningAncestor(el, true) ? '' : 'contentinfo';
    if (tag === 'main') return 'main';
    if (tag === 'nav') return 'navigation';
    if (tag === 'aside') {
      // A named <aside> is never suppressed, even when nested — it keeps
      // "complementary" when it has an accessible name, even inside
      // sectioning content. Matches landmark-unique's precedent.
      if (!hasSectioningAncestor(el, false)) return 'complementary';
      return getAccessibleLandmarkName(el) ? 'complementary' : '';
    }
    if (tag === 'section') return getAccessibleLandmarkName(el) ? 'region' : '';
    if (tag === 'form') return getAccessibleLandmarkName(el) ? 'form' : '';
    return '';
  }

  const LANDMARK_ROLES = new Set([
    'banner',
    'contentinfo',
    'main',
    'navigation',
    'complementary',
    'region',
    'form',
    'search'
  ]);

  function getLandmarkRole(el) {
    if (!el || !el.getAttribute) return '';
    const explicit = getExplicitRoleToken(el);
    if (explicit) return LANDMARK_ROLES.has(explicit) ? explicit : '';
    return getImplicitLandmarkRole(el);
  }

  // A candidate must actually have the banner role. Per HTML-AAM a <header>
  // descended from article/aside/main/nav/section is not a banner at all, so
  // flagging it as a nested banner reports a landmark that does not exist.
  function isBannerCandidate(el) {
    return getLandmarkRole(el) === 'banner';
  }

  function hasLandmarkAncestor(el) {
    const scopeRoots = Array.isArray(root) ? root : root ? [root] : [];
    let p = el.parentElement;
    while (p) {
      if (getLandmarkRole(p)) return true;
      // Don't climb past the scanned scope -- see aria-helpers.js's
      // hasLandmarkScopingAncestor for the same fix and rationale.
      if (scopeRoots.includes(p)) break;
      p = p.parentElement;
    }
    return false;
  }

  // queryAllSmart (shadow-DOM-aware) instead of plain document.querySelectorAll -- see
  // landmark-unique-manual.js's header comment. A third-party shadow-DOM-hosted
  // widget's own landmark is invisible to a light-DOM-only query.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('header, footer, main, nav, aside, section, form, [role]')
        : document.querySelectorAll('header, footer, main, nav, aside, section, form, [role]');
  } catch {
    nodes = [];
  }

  const banners = [];
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (!isBannerCandidate(el)) continue;

    // An aria-hidden banner candidate is removed from the accessibility
    // tree entirely -- it isn't part of the landmark structure assistive
    // technology users navigate at all, so it shouldn't be flagged as
    // "nested inside another landmark" (there's no real landmark there to
    // begin with, from AT's perspective). queryAllSmart's default hidden-
    // content policy only excludes "hard" CSS-based hiding (display:none,
    // etc.), not the softer aria-hidden exclusion, so this needs its own
    // check.
    if (helpers && typeof helpers.isAccTreeEligible === 'function') {
      const elig = (() => {
        try {
          return helpers.isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    banners.push(el);
  }

  if (banners.length === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  for (const el of banners) {
    if (!hasLandmarkAncestor(el)) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This banner landmark is nested inside another landmark region.',
      hint: 'Move the banner landmark (header/role="banner") so it is not contained by another landmark; a banner should be a top-level region of the page.',
      i18n: {
        summaryKey: 'landmarkBannerIsTopLevel_summary_cantTell',
        hintKey: 'landmarkBannerIsTopLevel_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'LANDMARK_BANNER_NOT_TOP_LEVEL' }
      }
    });
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "landmark-contentinfo-is-top-level": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  // Delegates to the shared helpers.getLandmarkNameInfo (aria-label -> aria-labelledby, via the
  // target's own accessible name, not raw textContent -> title attribute fallback) rather than a
  // local copy -- see that function's header comment in src/core/dom-helpers.js. Sharing it keeps
  // the title-attribute fallback consistent across the landmark rules.
  function getAccessibleLandmarkName(el) {
    try {
      if (helpers && typeof helpers.getLandmarkNameInfo === 'function') {
        const info = helpers.getLandmarkNameInfo(el, ctx);
        if (info && info.present && info.value) return normalizeWs(info.value);
      }
    } catch {}
    return '';
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  // Delegates to the shared helpers.hasLandmarkScopingAncestor for the
  // question "does this element sit inside a sectioning-content/<main>
  // ancestor that suppresses its conditional implicit role" — role-aware
  // (an ancestor's bare TAG only counts when it carries no role attribute
  // at all; an explicit role="dialog"-style override no longer suppresses)
  // rather than a local tag-only copy. See that function's header comment
  // in src/core/aria-helpers.js for the full algorithm. Example: an
  // <aside role="dialog"> containing its own <header>.
  function hasSectioningAncestor(el, includeMain) {
    return helpers && typeof helpers.hasLandmarkScopingAncestor === 'function'
      ? helpers.hasLandmarkScopingAncestor(el, { includeMain })
      : false;
  }

  function getImplicitLandmarkRole(el) {
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    if (tag === 'header') return hasSectioningAncestor(el, true) ? '' : 'banner';
    if (tag === 'footer') return hasSectioningAncestor(el, true) ? '' : 'contentinfo';
    if (tag === 'main') return 'main';
    if (tag === 'nav') return 'navigation';
    if (tag === 'aside') {
      // A named <aside> is never suppressed, even when nested — it keeps
      // "complementary" when it has an accessible name, even inside
      // sectioning content. Matches landmark-unique's precedent.
      if (!hasSectioningAncestor(el, false)) return 'complementary';
      return getAccessibleLandmarkName(el) ? 'complementary' : '';
    }
    if (tag === 'section') return getAccessibleLandmarkName(el) ? 'region' : '';
    if (tag === 'form') return getAccessibleLandmarkName(el) ? 'form' : '';
    return '';
  }

  const LANDMARK_ROLES = new Set([
    'banner',
    'contentinfo',
    'main',
    'navigation',
    'complementary',
    'region',
    'form',
    'search'
  ]);

  function getLandmarkRole(el) {
    if (!el || !el.getAttribute) return '';
    const explicit = getExplicitRoleToken(el);
    if (explicit) return LANDMARK_ROLES.has(explicit) ? explicit : '';
    return getImplicitLandmarkRole(el);
  }

  // Candidate selection is deliberately NOT the same as getLandmarkRole()
  // === 'contentinfo' — see the header comment above. A <footer> is
  // a candidate purely by tag + absence of any role attribute, independent
  // of whether sectioning-ancestor nesting would currently suppress its
  // implicit role; an explicit role="contentinfo" is always a candidate too.
  // A candidate must actually have the contentinfo role — a <footer> inside
  // article/aside/main/nav/section is not one, so flagging it as nested
  // would report a landmark that does not exist.
  function isContentinfoCandidate(el) {
    return getLandmarkRole(el) === 'contentinfo';
  }

  function hasLandmarkAncestor(el) {
    const scopeRoots = Array.isArray(root) ? root : root ? [root] : [];
    let p = el.parentElement;
    while (p) {
      if (getLandmarkRole(p)) return true;
      // Don't climb past the scanned scope -- see aria-helpers.js's
      // hasLandmarkScopingAncestor for the same fix and rationale.
      if (scopeRoots.includes(p)) break;
      p = p.parentElement;
    }
    return false;
  }

  // queryAllSmart (shadow-DOM-aware) instead of plain document.querySelectorAll -- see
  // landmark-unique-manual.js's header comment. A third-party shadow-DOM-hosted
  // widget's own landmark is invisible to a light-DOM-only query.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('header, footer, main, nav, aside, section, form, [role]')
        : document.querySelectorAll('header, footer, main, nav, aside, section, form, [role]');
  } catch {
    nodes = [];
  }

  const contentinfos = [];
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (!isContentinfoCandidate(el)) continue;

    // An aria-hidden contentinfo candidate is removed from the
    // accessibility tree entirely -- it isn't part of the landmark
    // structure assistive technology users navigate at all, so it
    // shouldn't be flagged as "nested inside another landmark" (there's
    // no real landmark there to begin with, from AT's perspective).
    // queryAllSmart's default hidden-content policy only excludes "hard"
    // CSS-based hiding (display:none, etc.), not the softer aria-hidden
    // exclusion, so this needs its own check.
    if (helpers && typeof helpers.isAccTreeEligible === 'function') {
      const elig = (() => {
        try {
          return helpers.isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    contentinfos.push(el);
  }

  if (contentinfos.length === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  for (const el of contentinfos) {
    if (!hasLandmarkAncestor(el)) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This contentinfo landmark is nested inside another landmark region.',
      hint: 'Move the contentinfo landmark (footer/role="contentinfo") so it is not contained by another landmark; contentinfo should be a top-level region of the page.',
      i18n: {
        summaryKey: 'landmarkContentinfoIsTopLevel_summary_cantTell',
        hintKey: 'landmarkContentinfoIsTopLevel_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'LANDMARK_CONTENTINFO_NOT_TOP_LEVEL' }
      }
    });
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "landmark-main-is-top-level": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  // Delegates to the shared helpers.getLandmarkNameInfo (aria-label -> aria-labelledby, via the
  // target's own accessible name, not raw textContent -> title attribute fallback) rather than a
  // local copy -- see that function's header comment in src/core/dom-helpers.js. Sharing it keeps
  // the title-attribute fallback consistent across the landmark rules.
  function getAccessibleLandmarkName(el) {
    try {
      if (helpers && typeof helpers.getLandmarkNameInfo === 'function') {
        const info = helpers.getLandmarkNameInfo(el, ctx);
        if (info && info.present && info.value) return normalizeWs(info.value);
      }
    } catch {}
    return '';
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  // Delegates to the shared helpers.hasLandmarkScopingAncestor for the
  // question "does this element sit inside a sectioning-content/<main>
  // ancestor that suppresses its conditional implicit role" — role-aware
  // (an ancestor's bare TAG only counts when it carries no role attribute
  // at all; an explicit role="dialog"-style override no longer suppresses)
  // rather than a local tag-only copy. See that function's header comment
  // in src/core/aria-helpers.js for the full algorithm. Example: an
  // <aside role="dialog"> containing its own <header>.
  function hasSectioningAncestor(el, includeMain) {
    return helpers && typeof helpers.hasLandmarkScopingAncestor === 'function'
      ? helpers.hasLandmarkScopingAncestor(el, { includeMain })
      : false;
  }

  function getImplicitLandmarkRole(el) {
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    if (tag === 'header') return hasSectioningAncestor(el, true) ? '' : 'banner';
    if (tag === 'footer') return hasSectioningAncestor(el, true) ? '' : 'contentinfo';
    if (tag === 'main') return 'main';
    if (tag === 'nav') return 'navigation';
    if (tag === 'aside') {
      // A named <aside> is never suppressed, even when nested — it keeps
      // "complementary" when it has an accessible name, even inside
      // sectioning content. Matches landmark-unique's precedent.
      if (!hasSectioningAncestor(el, false)) return 'complementary';
      return getAccessibleLandmarkName(el) ? 'complementary' : '';
    }
    if (tag === 'section') return getAccessibleLandmarkName(el) ? 'region' : '';
    if (tag === 'form') return getAccessibleLandmarkName(el) ? 'form' : '';
    return '';
  }

  const LANDMARK_ROLES = new Set([
    'banner',
    'contentinfo',
    'main',
    'navigation',
    'complementary',
    'region',
    'form',
    'search'
  ]);

  function getLandmarkRole(el) {
    if (!el || !el.getAttribute) return '';
    const explicit = getExplicitRoleToken(el);
    if (explicit) return LANDMARK_ROLES.has(explicit) ? explicit : '';
    return getImplicitLandmarkRole(el);
  }

  function hasLandmarkAncestor(el) {
    const scopeRoots = Array.isArray(root) ? root : root ? [root] : [];
    let p = el.parentElement;
    while (p) {
      if (getLandmarkRole(p)) return true;
      // Don't climb past the scanned scope -- see aria-helpers.js's
      // hasLandmarkScopingAncestor for the same fix and rationale.
      if (scopeRoots.includes(p)) break;
      p = p.parentElement;
    }
    return false;
  }

  // queryAllSmart (shadow-DOM-aware) instead of plain document.querySelectorAll -- see
  // landmark-unique-manual.js's header comment. A third-party shadow-DOM-hosted
  // widget's own landmark is invisible to a light-DOM-only query.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('header, footer, main, nav, aside, section, form, [role]')
        : document.querySelectorAll('header, footer, main, nav, aside, section, form, [role]');
  } catch {
    nodes = [];
  }

  const mains = [];
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (getLandmarkRole(el) !== 'main') continue;

    // An aria-hidden main candidate is removed from the accessibility
    // tree entirely -- it isn't part of the landmark structure assistive
    // technology users navigate at all, so it shouldn't be flagged as
    // "nested inside another landmark" (there's no real landmark there to
    // begin with, from AT's perspective). queryAllSmart's default hidden-
    // content policy only excludes "hard" CSS-based hiding (display:none,
    // etc.), not the softer aria-hidden exclusion, so this needs its own
    // check.
    if (helpers && typeof helpers.isAccTreeEligible === 'function') {
      const elig = (() => {
        try {
          return helpers.isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    mains.push(el);
  }

  if (mains.length === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  for (const el of mains) {
    if (!hasLandmarkAncestor(el)) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This main landmark is nested inside another landmark region.',
      hint: 'Move the main landmark (<main>/role="main") so it is not contained by another landmark; main should be a top-level region of the page.',
      i18n: {
        summaryKey: 'landmarkMainIsTopLevel_summary_cantTell',
        hintKey: 'landmarkMainIsTopLevel_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'LANDMARK_MAIN_NOT_TOP_LEVEL' }
      }
    });
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "landmark-no-duplicate-banner": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  // Delegates to the shared helpers.getLandmarkNameInfo (aria-label -> aria-labelledby, via the
  // target's own accessible name, not raw textContent -> title attribute fallback) rather than a
  // local copy -- see that function's header comment in src/core/dom-helpers.js.
  function getAccessibleLandmarkName(el) {
    try {
      if (helpers && typeof helpers.getLandmarkNameInfo === 'function') {
        const info = helpers.getLandmarkNameInfo(el, ctx);
        if (info && info.present && info.value) return normalizeWs(info.value);
      }
    } catch {}
    return '';
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  // Delegates to the shared helpers.hasLandmarkScopingAncestor for the
  // question "does this element sit inside a sectioning-content/<main>
  // ancestor that suppresses its conditional implicit role" — role-aware
  // (an ancestor's bare TAG only counts when it carries no role attribute
  // at all; an explicit role="dialog"-style override no longer suppresses)
  // rather than a local tag-only copy. See that function's header comment
  // in src/core/aria-helpers.js for the full algorithm — e.g. an
  // <aside role="dialog"> containing its own <header>, where the <header>
  // keeps its banner role.
  function hasSectioningAncestor(el, includeMain) {
    return helpers && typeof helpers.hasLandmarkScopingAncestor === 'function'
      ? helpers.hasLandmarkScopingAncestor(el, { includeMain })
      : false;
  }

  function getImplicitLandmarkRole(el) {
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    if (tag === 'header') return hasSectioningAncestor(el, true) ? '' : 'banner';
    if (tag === 'footer') return hasSectioningAncestor(el, true) ? '' : 'contentinfo';
    if (tag === 'main') return 'main';
    if (tag === 'nav') return 'navigation';
    if (tag === 'aside') {
      // A named <aside> is never suppressed, even when nested: keeps
      // "complementary" when the element has an accessible name, even
      // inside sectioning content. See landmark-unique-manual.js.
      if (!hasSectioningAncestor(el, false)) return 'complementary';
      return getAccessibleLandmarkName(el) ? 'complementary' : '';
    }
    if (tag === 'section') return getAccessibleLandmarkName(el) ? 'region' : '';
    if (tag === 'form') return getAccessibleLandmarkName(el) ? 'form' : '';
    return '';
  }

  const LANDMARK_ROLES = new Set([
    'banner',
    'contentinfo',
    'main',
    'navigation',
    'complementary',
    'region',
    'form',
    'search'
  ]);

  function getLandmarkRole(el) {
    if (!el || !el.getAttribute) return '';
    const explicit = getExplicitRoleToken(el);
    if (explicit) return LANDMARK_ROLES.has(explicit) ? explicit : '';
    return getImplicitLandmarkRole(el);
  }

  // queryAllSmart (shadow-DOM-aware) instead of plain document.querySelectorAll -- see
  // landmark-unique-manual.js's header comment. A third-party shadow-DOM-hosted
  // widget's own landmark is invisible to a light-DOM-only query.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('header, footer, main, nav, aside, section, form, [role]')
        : document.querySelectorAll('header, footer, main, nav, aside, section, form, [role]');
  } catch {
    nodes = [];
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isExposedToAt(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const banners = [];
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (!isExposedToAt(el)) continue;
    if (getLandmarkRole(el) === 'banner') banners.push(el);
  }

  if (banners.length <= 1) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = banners.map((el) => {
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    return {
      selector: stableSelector,
      html,
      summary: 'This page has more than one banner landmark.',
      hint: 'Keep only one banner landmark (header/role="banner") per page.',
      i18n: {
        summaryKey: 'landmarkNoDuplicateBanner_summary_cantTell',
        hintKey: 'landmarkNoDuplicateBanner_hint_cantTell',
        params: { count: String(banners.length) }
      },
      data: {
        details: { reasonCode: 'LANDMARK_DUPLICATE_BANNER', count: banners.length }
      }
    };
  });

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "landmark-no-duplicate-contentinfo": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  // Delegates to the shared helpers.getLandmarkNameInfo (aria-label -> aria-labelledby, via the
  // target's own accessible name, not raw textContent -> title attribute fallback) rather than a
  // local copy -- see that function's header comment in src/core/dom-helpers.js. Sharing it keeps
  // the title-attribute fallback consistent across the landmark rules.
  function getAccessibleLandmarkName(el) {
    try {
      if (helpers && typeof helpers.getLandmarkNameInfo === 'function') {
        const info = helpers.getLandmarkNameInfo(el, ctx);
        if (info && info.present && info.value) return normalizeWs(info.value);
      }
    } catch {}
    return '';
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  // Delegates to the shared helpers.hasLandmarkScopingAncestor for the
  // question "does this element sit inside a sectioning-content/<main>
  // ancestor that suppresses its conditional implicit role" — role-aware
  // (an ancestor's bare TAG only counts when it carries no role attribute
  // at all; an explicit role="dialog"-style override no longer suppresses)
  // rather than a local tag-only copy. See that function's header comment
  // in src/core/aria-helpers.js for the full algorithm. Example: an
  // <aside role="dialog"> containing its own <header>.
  function hasSectioningAncestor(el, includeMain) {
    return helpers && typeof helpers.hasLandmarkScopingAncestor === 'function'
      ? helpers.hasLandmarkScopingAncestor(el, { includeMain })
      : false;
  }

  function getImplicitLandmarkRole(el) {
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    if (tag === 'header') return hasSectioningAncestor(el, true) ? '' : 'banner';
    if (tag === 'footer') return hasSectioningAncestor(el, true) ? '' : 'contentinfo';
    if (tag === 'main') return 'main';
    if (tag === 'nav') return 'navigation';
    if (tag === 'aside') {
      // A named <aside> is never suppressed, even when nested — it keeps
      // "complementary" when it has an accessible name, even inside
      // sectioning content. Matches landmark-unique's precedent.
      if (!hasSectioningAncestor(el, false)) return 'complementary';
      return getAccessibleLandmarkName(el) ? 'complementary' : '';
    }
    if (tag === 'section') return getAccessibleLandmarkName(el) ? 'region' : '';
    if (tag === 'form') return getAccessibleLandmarkName(el) ? 'form' : '';
    return '';
  }

  const LANDMARK_ROLES = new Set([
    'banner',
    'contentinfo',
    'main',
    'navigation',
    'complementary',
    'region',
    'form',
    'search'
  ]);

  function getLandmarkRole(el) {
    if (!el || !el.getAttribute) return '';
    const explicit = getExplicitRoleToken(el);
    if (explicit) return LANDMARK_ROLES.has(explicit) ? explicit : '';
    return getImplicitLandmarkRole(el);
  }

  // queryAllSmart (shadow-DOM-aware) instead of plain document.querySelectorAll -- see
  // landmark-unique-manual.js's header comment. A third-party shadow-DOM-hosted
  // widget's own landmark is invisible to a light-DOM-only query.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('header, footer, main, nav, aside, section, form, [role]')
        : document.querySelectorAll('header, footer, main, nav, aside, section, form, [role]');
  } catch {
    nodes = [];
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isExposedToAt(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const contentinfos = [];
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (!isExposedToAt(el)) continue;
    if (getLandmarkRole(el) === 'contentinfo') contentinfos.push(el);
  }

  if (contentinfos.length <= 1) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = contentinfos.map((el) => {
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    return {
      selector: stableSelector,
      html,
      summary: 'This page has more than one contentinfo landmark.',
      hint: 'Keep only one contentinfo landmark (footer/role="contentinfo") per page.',
      i18n: {
        summaryKey: 'landmarkNoDuplicateContentinfo_summary_cantTell',
        hintKey: 'landmarkNoDuplicateContentinfo_hint_cantTell',
        params: { count: String(contentinfos.length) }
      },
      data: {
        details: { reasonCode: 'LANDMARK_DUPLICATE_CONTENTINFO', count: contentinfos.length }
      }
    };
  });

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "landmark-no-duplicate-main": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  function getLandmarkRole(el) {
    if (!el || !el.getAttribute) return '';
    const explicit = getExplicitRoleToken(el);
    if (explicit) return explicit === 'main' ? 'main' : '';
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    return tag === 'main' ? 'main' : '';
  }

  // queryAllSmart (shadow-DOM-aware) instead of plain document.querySelectorAll -- see
  // landmark-unique-manual.js's header comment. A third-party shadow-DOM-hosted
  // widget's own landmark is invisible to a light-DOM-only query.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('main, [role]')
        : document.querySelectorAll('main, [role]');
  } catch {
    nodes = [];
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isExposedToAt(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const mains = [];
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (!isExposedToAt(el)) continue;
    if (getLandmarkRole(el) === 'main') mains.push(el);
  }

  if (mains.length <= 1) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = mains.map((el) => {
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    return {
      selector: stableSelector,
      html,
      summary: 'This page has more than one main landmark.',
      hint: 'Keep only one main landmark (<main>/role="main") per page.',
      i18n: {
        summaryKey: 'landmarkNoDuplicateMain_summary_cantTell',
        hintKey: 'landmarkNoDuplicateMain_hint_cantTell',
        params: { count: String(mains.length) }
      },
      data: {
        details: { reasonCode: 'LANDMARK_DUPLICATE_MAIN', count: mains.length }
      }
    };
  });

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "landmark-one-main": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const body = document && document.body ? document.body : null;
  if (!body) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  function getExplicitRoleToken(el) {
    const raw = ((el.getAttribute && el.getAttribute('role')) || '').trim();
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  function isMainLandmark(el) {
    if (!el || !el.getAttribute) return false;
    const explicit = getExplicitRoleToken(el);
    if (explicit) return explicit === 'main';
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    return tag === 'main';
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isExposedToAt(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // queryAllSmart (shadow-DOM-aware) instead of plain document.querySelectorAll -- see
  // landmark-unique-manual.js's header comment. A third-party shadow-DOM-hosted
  // widget's own landmark is invisible to a light-DOM-only query.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('main, [role]')
        : document.querySelectorAll('main, [role]');
  } catch {
    nodes = [];
  }

  let hasMain = false;
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (!isMainLandmark(el)) continue;
    if (!isExposedToAt(el)) continue;
    hasMain = true;
    break;
  }

  if (hasMain) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const stableSelector = helpers.buildSelector ? helpers.buildSelector(body) : 'body';
  const html = helpers.getOuterHtmlSnippet
    ? helpers.getOuterHtmlSnippet(body)
    : (body.outerHTML || '').slice(0, 200);

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences: [
      {
        selector: stableSelector,
        html,
        summary: 'This page has no main landmark.',
        hint: 'Add a main landmark (<main> or role="main") around the page\'s primary content.',
        i18n: {
          summaryKey: 'landmarkOneMain_summary_cantTell_missing',
          hintKey: 'landmarkOneMain_hint_cantTell_missing',
          params: {}
        },
        data: {
          details: { reasonCode: 'LANDMARK_MAIN_MISSING' }
        }
      }
    ]
  };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "landmark-unique": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  // Delegates to the shared helpers.getLandmarkNameInfo (aria-label -> aria-labelledby, via the
  // target's own accessible name, not raw textContent -> title attribute fallback) rather than a
  // local copy -- see that function's header comment in src/core/dom-helpers.js.
  function getAccessibleLandmarkName(el) {
    try {
      if (helpers && typeof helpers.getLandmarkNameInfo === 'function') {
        const info = helpers.getLandmarkNameInfo(el, ctx);
        if (info && info.present && info.value) return normalizeWs(info.value);
      }
    } catch {}
    return '';
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  // Delegates to the shared helpers.hasLandmarkScopingAncestor (role-aware:
  // an ancestor's bare TAG only counts when it carries no role attribute at
  // all; an explicit role="dialog"-style override no longer suppresses —
  // see that function's header comment in src/core/aria-helpers.js), using
  // two distinct ancestor scopes rather than one shared list: <header>/
  // <footer> use "sectioning content PLUS <main>" (includeMain: true) to
  // decide banner/contentinfo suppression, but <aside> uses PLAIN
  // sectioning content only — NOT main (includeMain: false) — to decide
  // complementary suppression. A single shared sectioning-ancestors set
  // that includes 'main' is correct for header/footer but wrong for aside:
  // e.g. two unnamed <aside> elements that are direct children of <main>
  // would have their implicit "complementary" role incorrectly suppressed,
  // hiding a real duplicate-landmark violation. The role-aware half matters
  // too: e.g. an <aside role="dialog"> containing its own <header> —
  // role="dialog" isn't one of the four scoping roles, so the nested
  // <header> keeps "banner" per spec, but a tag-only (non-role-aware)
  // check would unconditionally suppress it just because the ancestor TAG
  // was <aside>.
  function hasSectioningAncestor(el, includeMain) {
    return helpers && typeof helpers.hasLandmarkScopingAncestor === 'function'
      ? helpers.hasLandmarkScopingAncestor(el, { includeMain })
      : false;
  }

  function getImplicitLandmarkRole(el) {
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    if (tag === 'header') return hasSectioningAncestor(el, true) ? '' : 'banner';
    if (tag === 'footer') return hasSectioningAncestor(el, true) ? '' : 'contentinfo';
    if (tag === 'main') return 'main';
    if (tag === 'nav') return 'navigation';
    if (tag === 'aside') {
      // An <aside> is suppressed by a sectioning-content ancestor ONLY
      // when it also has no accessible name — a named <aside> is never
      // suppressed, even when nested.
      if (!hasSectioningAncestor(el, false)) return 'complementary';
      return getAccessibleLandmarkName(el) ? 'complementary' : '';
    }
    if (tag === 'section') return getAccessibleLandmarkName(el) ? 'region' : '';
    if (tag === 'form') return getAccessibleLandmarkName(el) ? 'form' : '';
    return '';
  }

  const LANDMARK_ROLES = new Set([
    'banner',
    'contentinfo',
    'main',
    'navigation',
    'complementary',
    'region',
    'form',
    'search'
  ]);

  function getLandmarkRole(el) {
    if (!el || !el.getAttribute) return '';
    const explicit = getExplicitRoleToken(el);
    if (explicit) {
      if (!LANDMARK_ROLES.has(explicit)) return '';
      // <form>/<section> only count as landmarks when they have an
      // accessible name — a property of the ELEMENT, not of how the role
      // got there. This applies whether the role is implicit (already
      // handled in getImplicitLandmarkRole below) or explicit. Per the W3C
      // ARIA-in-HTML spec ("a form is not exposed as a landmark region
      // unless it has been provided an accessible name"). Otherwise an
      // unnamed <form role="search"> nested inside an unnamed
      // <div role="search"> gets wrongly counted as a second distinct
      // "search" landmark.
      const tag = el.tagName ? el.tagName.toLowerCase() : '';
      if (tag === 'form' || tag === 'section') {
        return getAccessibleLandmarkName(el) ? explicit : '';
      }
      return explicit;
    }
    return getImplicitLandmarkRole(el);
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isExposedToAt(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // queryAllSmart (shadow-DOM-aware, includeShadowDom defaults true) instead of a plain
  // document.querySelectorAll -- a third-party widget rendering its own
  // unnamed <nav>/<footer> inside a shadow root collides with the page's
  // own unnamed header <nav>/page <footer>, but is invisible to plain
  // querySelectorAll's light-DOM-only reach.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('header, footer, main, nav, aside, section, form, [role]')
        : document.querySelectorAll('header, footer, main, nav, aside, section, form, [role]');
  } catch {
    nodes = [];
  }

  // Only landmarks actually exposed to assistive technology can collide.
  // Without this, responsive layouts that render both a desktop and a
  // mobile copy of the same named nav (one hidden via CSS at any given
  // viewport) are wrongly flagged as duplicate landmarks, since the hidden
  // copy is never reachable by AT and can't really collide with the
  // visible one.
  const byRole = new Map(); // role -> [{el, name}]
  const seen = new Set();
  for (const el of nodes) {
    if (!el || seen.has(el)) continue;
    seen.add(el);
    if (!isExposedToAt(el)) continue;
    const role = getLandmarkRole(el);
    if (!role) continue;
    const list = byRole.get(role) || [];
    list.push({ el, name: getAccessibleLandmarkName(el) });
    byRole.set(role, list);
  }

  const occurrences = [];

  for (const [role, entries] of byRole) {
    if (entries.length <= 1) continue;

    const byName = new Map(); // normalized name -> entries[]
    for (const entry of entries) {
      const key = entry.name.toLowerCase();
      const list = byName.get(key) || [];
      list.push(entry);
      byName.set(key, list);
    }

    for (const [normalizedName, group] of byName) {
      if (group.length <= 1) continue;

      for (const { el } of group) {
        const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
        const html = helpers.getOuterHtmlSnippet
          ? helpers.getOuterHtmlSnippet(el)
          : el.outerHTML || '';

        occurrences.push({
          selector: stableSelector,
          html,
          summary: normalizedName
            ? `This ${role} landmark shares its accessible name with another ${role} landmark.`
            : `This ${role} landmark has no accessible name, and more than one unnamed ${role} landmark exists on this page.`,
          hint: `Give each ${role} landmark a distinct name via aria-label or aria-labelledby.`,
          i18n: {
            summaryKey: normalizedName
              ? 'landmarkUnique_summary_cantTell_duplicateName'
              : 'landmarkUnique_summary_cantTell_bothUnnamed',
            hintKey: 'landmarkUnique_hint_cantTell',
            params: { role }
          },
          data: {
            details: {
              reasonCode: 'LANDMARK_NOT_UNIQUE',
              role,
              name: normalizedName,
              groupSize: group.length
            }
          }
        });
      }
    }
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "link-in-text-block": { run: (function runInPage(ctx) {
  const { helpers, rule, engineOptions } = ctx;

  function safeComputedStyle(el) {
    try {
      if (!el || el.nodeType !== 1) return null;
      if (helpers && typeof helpers.computedStyle === 'function') {
        const cs = helpers.computedStyle(el);
        if (cs) return cs;
      }
      const view =
        el.ownerDocument && el.ownerDocument.defaultView ? el.ownerDocument.defaultView : null;
      if (view && typeof view.getComputedStyle === 'function') return view.getComputedStyle(el);
    } catch {}
    return null;
  }

  function decorationTokens(cs) {
    const raw =
      `${(cs && cs.textDecorationLine) || ''} ${(cs && cs.textDecoration) || ''}`.toLowerCase();
    return raw.split(/\s+/).filter(Boolean);
  }

  function hasUnderline(cs) {
    return decorationTokens(cs).includes('underline');
  }

  function hasSurroundingText(el, parent) {
    if (!parent || !parent.childNodes) return false;
    for (let i = 0; i < parent.childNodes.length; i++) {
      const n = parent.childNodes[i];
      if (n === el) continue;
      if (n.nodeType === 3 && n.nodeValue && n.nodeValue.trim().length > 0) return true;
    }
    return false;
  }

  const contrastOpts =
    engineOptions && typeof engineOptions.contrast === 'object' && engineOptions.contrast
      ? engineOptions.contrast
      : {};
  const mode = contrastOpts.mode === 'auditorAssist' ? 'auditorAssist' : 'strictConformance';
  const rootCanvasFallback =
    typeof contrastOpts.rootCanvasFallback === 'string' && contrastOpts.rootCanvasFallback.trim()
      ? contrastOpts.rootCanvasFallback.trim()
      : '#ffffff';

  const c = helpers && helpers.contrast ? helpers.contrast : null;

  const selector = 'a[href]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const eligResult = helpers.isAccTreeEligible ? helpers.isAccTreeEligible(el, ctx) : true;
    const eligible =
      typeof eligResult === 'boolean' ? eligResult : !!(eligResult && eligResult.eligible);
    if (!eligible) continue;

    const parent = el.parentElement;
    if (!hasSurroundingText(el, parent)) continue;

    applicableCount += 1;

    const linkCs = safeComputedStyle(el);
    const parentCs = safeComputedStyle(parent);

    if (hasUnderline(linkCs)) continue;

    const linkWeight = c && linkCs ? c.normalizeFontWeight(linkCs.fontWeight) : 400;
    const parentWeight = c && parentCs ? c.normalizeFontWeight(parentCs.fontWeight) : 400;
    if (linkWeight !== parentWeight) continue;

    const linkStyle = (linkCs && linkCs.fontStyle) || 'normal';
    const parentStyle = (parentCs && parentCs.fontStyle) || 'normal';
    if (linkStyle !== parentStyle) continue;

    if (!c) continue;

    let flagged = false;
    let ratio = null;
    let fgLinkHex = '';
    let fgParentHex = '';

    try {
      const blocker = c.getComputabilityBlocker(el);
      if (blocker && blocker.ok === false) {
        // Not confidently computable — skip (benefit of the doubt).
      } else {
        const bg = c.computeEffectiveBackground(el, {
          contrast: { mode, rootCanvasFallback },
          collectStack: false
        });
        const fgLink = c.computeEffectiveForeground(el);
        const fgParent = c.computeEffectiveForeground(parent);

        if (bg && bg.ok && bg.rgba && fgLink && fgLink.rgba && fgParent && fgParent.rgba) {
          const fgLinkOpaque =
            fgLink.rgba.a < 1
              ? c.compositeRgba(fgLink.rgba, bg.rgba)
              : { r: fgLink.rgba.r, g: fgLink.rgba.g, b: fgLink.rgba.b, a: 1 };
          const fgParentOpaque =
            fgParent.rgba.a < 1
              ? c.compositeRgba(fgParent.rgba, bg.rgba)
              : { r: fgParent.rgba.r, g: fgParent.rgba.g, b: fgParent.rgba.b, a: 1 };

          ratio = c.contrastRatio(fgLinkOpaque, fgParentOpaque);
          fgLinkHex = c.rgbToHex ? c.rgbToHex(fgLinkOpaque) : '';
          fgParentHex = c.rgbToHex ? c.rgbToHex(fgParentOpaque) : '';

          if (!(ratio >= 3)) flagged = true;
        }
        // else: not confidently computable — skip.
      }
    } catch {
      // no-throw: treat as not computable, skip.
    }

    if (!flagged) continue;

    const eligInfo = helpers.getEligibilityInfo
      ? helpers.getEligibilityInfo(el, ctx, { targetSet: 'acc' })
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const tag = (el.tagName || '').toLowerCase();
    const ratioStr = c.round2 ? c.round2(ratio) : String(ratio);

    occurrences.push({
      selector: stableSelector,
      html,
      summary:
        'This link in a block of text relies on color alone to be distinguished from the surrounding text.',
      hint: 'Add an underline, a font-weight/style difference, or increase the color contrast between the link and surrounding text to at least 3:1.',
      i18n: {
        summaryKey: 'linkInTextBlock_summary_fail',
        hintKey: 'linkInTextBlock_hint_fail',
        params: { element: tag, ratio: String(ratioStr), threshold: '3' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: {
          reasonCode: 'COLOR_ONLY_DIFFERENTIATION',
          metrics: { ratio, threshold: 3 },
          colors: { linkForegroundHex: fgLinkHex, surroundingTextForegroundHex: fgParentHex }
        }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "link-name-present": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const occurrences = [];
  let applicableCount = 0;

  function getConservativeSubtreeText(container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  const selector = 'a[href], area[href], [role="link"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  for (const el of nodes) {
    // isAccTreeEligible returns { eligible, reasons }, not a boolean.
    // Naming rules apply only to elements included in the accessibility tree
    // (ACT c487ae), which excludes focusable aria-hidden content;
    // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
    const eligResult = helpers.isIncludedInAccessibilityTree
      ? helpers.isIncludedInAccessibilityTree(el, ctx)
      : helpers.isAccTreeEligible
        ? helpers.isAccTreeEligible(el, ctx)
        : true;
    const eligible =
      typeof eligResult === 'boolean' ? eligResult : !!(eligResult && eligResult.eligible);
    if (!eligible) continue;

    applicableCount += 1;

    const nameInfo = helpers.getAccessibleNameInfo ? helpers.getAccessibleNameInfo(el, ctx) : null;
    const programmaticName = nameInfo && typeof nameInfo.value === 'string' ? nameInfo.value : '';

    const role = el.getAttribute ? el.getAttribute('role') : null;
    const roleNorm = String(role || '')
      .replace(/\s+/g, ' ')
      .trim()
      .toLowerCase();
    // ARIA 1.2 "Name From: author, contents". Every other known role is
    // name-from-author-only. An unknown role falls back to the implicit role.
    // <generated:aria-name-from-content>
    const NAME_FROM_CONTENT_ROLES = [
      'button',
      'cell',
      'checkbox',
      'columnheader',
      'doc-backlink',
      'doc-biblioref',
      'doc-glossref',
      'doc-noteref',
      'graphics-object',
      'gridcell',
      'heading',
      'link',
      'menuitem',
      'menuitemcheckbox',
      'menuitemradio',
      'option',
      'radio',
      'row',
      'rowgroup',
      'rowheader',
      'switch',
      'tab',
      'tooltip',
      'treeitem'
    ];
    // </generated:aria-name-from-content>
    const isKnownRoleToken =
      helpers && helpers.aria && typeof helpers.aria.isKnownRole === 'function'
        ? (() => {
            try {
              return !!helpers.aria.isKnownRole(roleNorm);
            } catch {
              return false;
            }
          })()
        : false;
    const isContentNameCandidate =
      !roleNorm || !isKnownRoleToken || NAME_FROM_CONTENT_ROLES.includes(roleNorm);

    const contentName =
      programmaticName.trim().length === 0 && isContentNameCandidate
        ? getConservativeSubtreeText(el)
        : '';

    const finalName = (programmaticName.trim().length ? programmaticName : contentName).trim();

    if (finalName.length === 0) {
      // Only compute the richer eligibility-info payload (used solely for
      // the occurrence's visibilityFilter) once we know an occurrence is
      // actually being built, rather than for every applicable element.
      const eligInfo = helpers.getEligibilityInfo
        ? helpers.getEligibilityInfo(el, ctx, { targetSet: 'acc' })
        : null;

      const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';
      const tag = (el.tagName || '').toLowerCase();

      occurrences.push({
        selector: stableSelector,
        html,

        // Human fallbacks (allowed)
        summary: 'This link has no accessible name.',
        hint: 'Provide link text or an accessible-name mechanism (for example aria-label) so assistive technologies can identify the link.',

        // Validator requires these keys to exist in the English dictionary
        i18n: {
          summaryKey: 'linkNamePresent_summary_fail',
          hintKey: 'linkNamePresent_hint_fail',
          params: { element: tag }
        },

        data: {
          visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
          details: {
            reasonCode: 'name_missing',
            metrics: {
              programmaticNameLength: programmaticName.trim().length,
              contentNameLength: contentName.trim().length
            },
            refs: { accessibleName: nameInfo || null }
          }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "link-name-quality": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const GENERIC_LINK_TEXT = new Set([
    'click here',
    'here',
    'click',
    'more',
    'more info',
    'more information',
    'read more',
    'learn more',
    'continue reading',
    'continue',
    'details',
    'more details',
    'link',
    'this link',
    'go',
    'download',
    'view more',
    'see more',
    'info'
  ]);

  function normalize(s) {
    return (s == null ? '' : String(s))
      .replace(/\s+/g, ' ')
      .trim()
      .toLowerCase()
      .replace(/[.,;:!?]+$/g, '')
      .trim();
  }

  const selector = 'a[href]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const eligResult = helpers.isAccTreeEligible ? helpers.isAccTreeEligible(el, ctx) : true;
    const eligible =
      typeof eligResult === 'boolean' ? eligResult : !!(eligResult && eligResult.eligible);
    if (!eligible) continue;

    const nameInfo = helpers.getAccessibleNameInfo ? helpers.getAccessibleNameInfo(el, ctx) : null;
    const programmaticName = nameInfo && typeof nameInfo.value === 'string' ? nameInfo.value : '';

    let rawName = programmaticName;
    if (!rawName.trim() && helpers.getContentNameInfo) {
      const contentInfo = helpers.getContentNameInfo(el, ctx);
      rawName = contentInfo && contentInfo.present ? contentInfo.value : '';
    }

    const normalized = normalize(rawName);
    if (!normalized) continue; // no name at all: link-name-present's concern, not this rule's.

    applicableCount += 1;

    if (!GENERIC_LINK_TEXT.has(normalized)) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const eligInfo = helpers.getEligibilityInfo
      ? helpers.getEligibilityInfo(el, ctx, { targetSet: 'acc' })
      : null;

    occurrences.push({
      selector: stableSelector,
      html,
      summary: `This link's accessible name ("${rawName.trim()}") is a generic, non-descriptive phrase.`,
      hint: 'Make the link text itself describe its destination/purpose (e.g. "Download the 2026 pricing guide" instead of "Download"), or confirm the surrounding context already makes the purpose clear.',
      i18n: {
        summaryKey: 'linkNameQuality_summary_cantTell',
        hintKey: 'linkNameQuality_hint_cantTell',
        params: { name: rawName.trim() }
      },
      data: {
        details: { reasonCode: 'GENERIC_LINK_TEXT', normalizedName: normalized },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }

  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "list-children-valid": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  // Declared inside runInPage — see scripts/build-core.js header
  // ("runInPage MUST be self-contained").
  const ALLOWED_CHILD_TAGS = new Set(['li', 'script', 'template']);

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('ul, ol')
    : helpers.queryAll('ul, ol');

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isExposedToAt(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.children) continue;
    if (!el.children.length) continue;

    applicableCount += 1;

    const invalidTags = [];
    for (const child of el.children) {
      if (!child || !child.tagName) continue;
      if (!isExposedToAt(child)) continue;
      const tag = child.tagName.toLowerCase();

      const roleAttr = child.getAttribute ? String(child.getAttribute('role') || '').trim() : '';
      const explicitRole = roleAttr ? (roleAttr.split(/\s+/)[0] || '').toLowerCase() : '';

      // An explicit role always wins over the tag — see header comment.
      const valid = explicitRole ? explicitRole === 'listitem' : ALLOWED_CHILD_TAGS.has(tag);

      if (!valid) invalidTags.push(tag);
    }

    if (!invalidTags.length) continue;

    const dedupedInvalidTags = [...new Set(invalidTags)];

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This list contains a direct child that is not a list item.',
      hint: 'Only use <li> (or <script>/<template>) as direct children of <ul>/<ol>; move other markup inside an <li>.',
      i18n: {
        summaryKey: 'listChildrenValid_summary_fail',
        hintKey: 'listChildrenValid_hint_fail',
        params: { element: tag, invalidChildren: dedupedInvalidTags.join(', ') }
      },
      data: {
        details: { reasonCode: 'LIST_INVALID_CHILD', element: tag, invalidChildren: invalidTags }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "listbox-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  // A <label> contributes a name via its own aria-label/aria-labelledby
  // (checked first, same ARIA-over-content precedence any element's
  // accessible name gives — e.g. <label aria-label="Search"><svg
  // aria-hidden="true">...</svg></label> names its control "Search" even
  // though the label's only child content is aria-hidden) or, failing
  // that, its rendered content (getConservativeSubtreeText).
  function getLabelText(lab) {
    if (helpers.getAriaNameInfo) {
      try {
        const aria = helpers.getAriaNameInfo(lab, ctx);
        if (aria && aria.present && aria.value) return normalizeWs(aria.value);
      } catch {}
    }
    const content = getConservativeSubtreeText(document, lab);
    if (content) return content;
    // Final fallback per the general accname text-alternative algorithm,
    // which applies to any element being asked for its name regardless of
    // why (own aria-label, an aria-labelledby reference, or — here — native
    // <label for> association): title, when nothing else yields a name.
    // Purely additive (only fills in a name where there was none before),
    // so it carries no false-positive risk — see dialog-name-present.js's
    // identical <iframe>-title-fallback fix for the concrete real-world
    // trigger this same accname step covers elsewhere.
    return getAttr(lab, 'title');
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  function buildLabelForMap(doc) {
    const map = new Map(); // id -> label element (first)
    try {
      const labels = doc && doc.getElementsByTagName ? doc.getElementsByTagName('label') : [];
      for (let i = 0; i < labels.length; i += 1) {
        const lab = labels[i];
        if (!lab || !lab.getAttribute) continue;
        const f = normalizeWs(lab.getAttribute('for'));
        if (!f) continue;
        if (!map.has(f)) map.set(f, lab);
      }
    } catch {}
    return map;
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="listbox"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  // Precompute label[for] map for listbox elements that are labelable
  // native form controls (e.g. <select multiple role="listbox">).
  const labelForMap = buildLabelForMap(document);

  function getNativeLabelText(el) {
    try {
      if ('labels' in el && el.labels && el.labels.length) {
        const parts = [];
        const max = Math.min(4, el.labels.length);
        for (let i = 0; i < max; i += 1) {
          const lab = el.labels[i];
          const t = lab ? getLabelText(lab) : '';
          if (t) parts.push(t);
        }
        const joined = normalizeWs(parts.join(' '));
        if (joined) return joined;
      }
    } catch {}
    try {
      if (el.closest) {
        const wrap = el.closest('label');
        if (wrap) {
          const t = getLabelText(wrap);
          if (t) return t;
        }
      }
    } catch {}
    try {
      const idAttr = getAttr(el, 'id');
      if (idAttr && labelForMap.has(idAttr)) {
        const lab = labelForMap.get(idAttr);
        const t = lab ? getLabelText(lab) : '';
        if (t) return t;
      }
    } catch {}
    return '';
  }

  function hasName(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const labelled = resolveAriaLabelledbyText(document, el, 8);
    if (labelled) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // Native <label> association (e.g. <select multiple role="listbox">).
    const lab = getNativeLabelText(el);
    if (lab) return { ok: true, method: 'label' };

    // role="listbox" is name-from-author-only per WAI-ARIA: it must NOT
    // fall back to subtree content.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    applicableCount += 1;

    const res = hasName(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This element has no accessible name.',
      hint: "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this listbox's accessible name.",
      i18n: {
        summaryKey: 'listboxNamePresent_summary_fail',
        hintKey: 'listboxNamePresent_hint_fail',
        params: { controlType: 'listbox' }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'listbox', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "listitem-parent-valid": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart ? helpers.queryAllSmart('li') : helpers.queryAll('li');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el) continue;
    const parent = el.parentElement;
    if (!parent) continue;

    // An explicit role on the <li> ITSELF overrides its native "listitem"
    // role entirely, the same "any explicit role wins over the tag's
    // native role" principle this check already applies to the PARENT
    // (see the header comment). An <li role="tab">/
    // role="menuitem">/role="presentation"> etc. is exposed to AT with
    // THAT role, never "listitem" -- so the "list item needs a valid list
    // container parent" concern this rule exists for doesn't apply to it
    // at all; there's no listitem semantics being claimed to validate.
    // role="listitem" itself is a no-op restatement, not an override, so
    // it still falls through to the normal parent check below.
    const ownRoleAttr = el.getAttribute ? String(el.getAttribute('role') || '').trim() : '';
    const ownExplicitRole = ownRoleAttr ? (ownRoleAttr.split(/\s+/)[0] || '').toLowerCase() : '';
    if (ownExplicitRole && ownExplicitRole !== 'listitem') continue;

    applicableCount += 1;

    const parentTag = parent.tagName ? parent.tagName.toLowerCase() : '';

    const roleAttr = parent.getAttribute ? String(parent.getAttribute('role') || '').trim() : '';
    const explicitRole = roleAttr ? (roleAttr.split(/\s+/)[0] || '').toLowerCase() : '';

    let valid;
    if (explicitRole) {
      // An explicit role always wins over the tag's native role, in either
      // direction — see the header comment.
      valid = explicitRole === 'list' || explicitRole === 'presentation' || explicitRole === 'none';
    } else {
      valid = parentTag === 'ul' || parentTag === 'ol';
    }

    if (valid) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This list item is not contained by a list container.',
      hint: 'Place this <li> inside a <ul>/<ol>, or give its parent role="list".',
      i18n: {
        summaryKey: 'listitemParentValid_summary_fail',
        hintKey: 'listitemParentValid_hint_fail',
        params: { parentElement: parentTag }
      },
      data: {
        details: { reasonCode: 'LISTITEM_INVALID_PARENT', parentElement: parentTag }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "manual-review": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;

  const getOuterHtmlSnippet =
    helpers && helpers.getOuterHtmlSnippet
      ? helpers.getOuterHtmlSnippet
      : (el) => (el && el.outerHTML) || '';

  const contextSelector = ctx.contextSelector || null;

  let rootEl = root || null;

  if (!rootEl) {
    if (contextSelector) {
      try {
        rootEl = document.querySelector(contextSelector);
      } catch {
        // invalid selector, fallback to full document
        rootEl = null;
      }
    }
    if (!rootEl) {
      rootEl = document.documentElement || document.body || document.querySelector('html');
    }
  }

  const fallbackRoot =
    rootEl || document.documentElement || document.body || document.querySelector('html');

  const html = getOuterHtmlSnippet(fallbackRoot);

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'moderate',
    occurrences: [
      {
        selector: contextSelector || 'html',
        html,
        summary: 'Manual review required for keyboard navigation and focus order.',
        i18n: {
          summaryKey: 'manualReview_summary_cantTell',
          hintKey: 'manualReview_hint_cantTell',
          params: {}
        }
      }
    ]
  };
}), applicability: null },
    "media-alternative-transcript-evidence": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  // Conservative keyword set (deterministic). Includes common EN/FR terms.
  // Keep this list strict to avoid false positives.
  const TRANSCRIPT_TOKENS = [
    'transcript',
    'transcription',
    'texte intégral',
    'compte rendu',
    'verbatim'
  ];

  // Minimum transcript body length to be considered "substantial" when used as evidence.
  // (Avoids treating short summaries as transcripts.)
  const MIN_TRANSCRIPT_CHARS = 200;

  // If aria-describedby contains no transcript token, require a larger body to consider it evidence.
  const MIN_DESCRIBEDBY_CHARS_WITHOUT_TOKEN = 400;

  function normText(s) {
    if (!s) return '';
    return String(s).replace(/\s+/g, ' ').trim().toLowerCase();
  }

  function containsTranscriptToken(s) {
    const t = normText(s);
    if (!t) return false;
    for (const tok of TRANSCRIPT_TOKENS) {
      if (t.includes(tok)) return true;
    }
    return false;
  }

  function textLen(s) {
    const t = normText(s);
    return t ? t.length : 0;
  }

  function getNodeText(el) {
    try {
      if (!el) return '';
      return el.textContent || '';
    } catch (e) {
      return '';
    }
  }

  function isElement(el) {
    return !!(el && el.nodeType === 1);
  }

  const __eligCache = new WeakMap();

  function getEligibility(node) {
    if (!node || node.nodeType !== 1)
      return { eligible: true, reasons: [], targetSet: 'acc', accEligible: null };
    const cached = __eligCache.get(node);
    if (cached) return cached;

    let info;
    try {
      info =
        helpers && typeof helpers.getEligibilityInfo === 'function'
          ? helpers.getEligibilityInfo(node, ctx, { targetSet: 'acc' })
          : null;
    } catch {
      info = null;
    }

    const norm =
      info && typeof info === 'object'
        ? info
        : { eligible: true, reasons: [], targetSet: 'acc', accEligible: null };

    __eligCache.set(node, norm);
    return norm;
  }

  function isEligible(node) {
    const info = getEligibility(node);
    return !!(info && info.eligible);
  }

  // Evidence object includes strength, so the rule can treat "unverified external link" as insufficient proof.
  function evidenceNone() {
    return {
      strength: 'none', // none | weak | strong
      method: 'none',
      transcriptNodeSelector: null,
      transcriptLinkHref: null,
      notes: []
    };
  }

  function nodeRef(el) {
    try {
      if (!el || el.nodeType !== 1) return null;
      const elementId = el.getAttribute && el.getAttribute('id');
      if (elementId) return { type: 'id', value: String(elementId) };
      const tag = (el.tagName || '').toLowerCase();
      return { type: 'tag', value: tag };
    } catch {
      return null;
    }
  }

  const __evidenceCache = new WeakMap();

  // Walk a small neighborhood around the media element to find transcript cues.
  // Bounded for determinism and performance.
  function findTranscriptEvidence(mediaEl) {
    const evidence = evidenceNone();

    // 1) aria-describedby strong binding
    const descInfo = helpers.getAccessibleDescriptionInfo
      ? helpers.getAccessibleDescriptionInfo(mediaEl, ctx)
      : null;

    if (descInfo && descInfo.mechanism === 'aria-describedby') {
      const descText = descInfo.value || '';
      const hasToken = containsTranscriptToken(descText);
      const len = textLen(descText);

      // Strong signal if the described text clearly indicates transcript, or is very substantial.
      if (hasToken || len >= MIN_DESCRIBEDBY_CHARS_WITHOUT_TOKEN) {
        evidence.strength = 'strong';
        evidence.method = 'aria-describedby';
        evidence.notes.push(
          'media has aria-describedby with explicit or substantial transcript text'
        );
        return evidence;
      }
    }

    // 2) In-container transcript heading + substantial visible text
    const parent = mediaEl.parentElement;
    if (isElement(parent) && isEligible(parent)) {
      const headings = parent.querySelectorAll('h1,h2,h3,h4,h5,h6');
      for (const h of headings) {
        if (!isEligible(h)) continue;
        const hText = getNodeText(h);
        if (!containsTranscriptToken(hText)) continue;

        // Look at a small set of following siblings for substantial text (and ensure visibility).
        let sib = h.nextElementSibling;
        let steps = 0;
        while (isElement(sib) && steps < 4) {
          if (isEligible(sib)) {
            const sibText = getNodeText(sib);
            if (textLen(sibText) >= MIN_TRANSCRIPT_CHARS) {
              evidence.strength = 'strong';
              evidence.method = 'adjacent-heading';
              evidence.transcriptNodeSelector = nodeRef(h);
              evidence.notes.push(
                'found transcript heading with substantial visible adjacent text'
              );
              return evidence;
            }
          }
          sib = sib.nextElementSibling;
          steps += 1;
        }
      }
    }

    // 3) Nearby explicit transcript link
    // - Same-document anchors can be verified (strong).
    // - Cross-document links are unverified (weak).
    if (isElement(parent) && isEligible(parent)) {
      const links = parent.querySelectorAll('a[href]');
      for (const a of links) {
        if (!isEligible(a)) continue;

        const nameInfo = helpers.getAccessibleNameInfo
          ? helpers.getAccessibleNameInfo(a, ctx)
          : null;
        const linkName = nameInfo && nameInfo.value ? nameInfo.value : getNodeText(a);
        if (!containsTranscriptToken(linkName)) continue;

        const href = a.getAttribute('href') || '';
        evidence.transcriptLinkHref = href;
        evidence.transcriptNodeSelector = nodeRef(a);

        // 3a) Same-document anchor: resolve and verify target content has transcript heading + substance.
        if (href.startsWith('#')) {
          const targetId = href.slice(1);
          const target = targetId
            ? safeRoot.getElementById
              ? safeRoot.getElementById(targetId)
              : document.getElementById(targetId)
            : null;

          if (isElement(target) && isEligible(target)) {
            // Find a transcript heading in the target, and ensure there is substantial text in the target subtree.
            const targetHeadings = target.querySelectorAll('h1,h2,h3,h4,h5,h6');
            let hasTranscriptHeading = false;
            for (const th of targetHeadings) {
              if (!isEligible(th)) continue;
              if (containsTranscriptToken(getNodeText(th))) {
                hasTranscriptHeading = true;
                break;
              }
            }
            const targetText = getNodeText(target);

            if (hasTranscriptHeading && textLen(targetText) >= MIN_TRANSCRIPT_CHARS) {
              evidence.strength = 'strong';
              evidence.method = 'anchor-target';
              evidence.notes.push(
                'resolved transcript link to an on-page section with transcript heading and substantial text'
              );
              return evidence;
            }
          }

          // If anchor cannot be verified, treat as weak (still better than nothing, but not proof).
          evidence.strength = 'weak';
          evidence.method = 'anchor-unverified';
          evidence.notes.push(
            'transcript link found but anchor target could not be verified as a transcript section'
          );
          return evidence;
        }

        // 3b) External or cross-document link: do not treat as proof without crawling.
        evidence.strength = 'weak';
        evidence.method = 'external-link';
        evidence.notes.push('transcript link found but cannot verify content without crawling');
        return evidence;
      }
    }

    return evidence;
  }

  const occurrences = [];
  let applicableCount = 0;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('audio,video')
    : helpers.queryAll('audio,video');

  for (const el of nodes) {
    const eligInfo = getEligibility(el);
    if (!eligInfo || !eligInfo.eligible) continue;

    applicableCount += 1;

    // Evidence must be computed and cached per media element: two sibling
    // <audio>/<video> elements under the same container do not necessarily
    // share the same transcript evidence (e.g. one has a strong
    // aria-describedby binding, the other has none). Keying this cache by
    // the shared container instead of the element itself previously caused
    // an undocumented sibling to silently inherit another element's
    // evidence classification.
    let evidence = __evidenceCache.get(el);
    if (!evidence) {
      evidence = findTranscriptEvidence(el);
      __evidenceCache.set(el, evidence);
    }

    const mediaTag = (el.tagName || '').toLowerCase();

    if (evidence.strength === 'none') {
      const baseOccurrence = {
        summary:
          'A transcript or other text alternative for this time-based media is not strongly evidenced on the page.',
        hint: 'Provide a clearly identified transcript or other text alternative for audio-only/video-only prerecorded media (for example, a “Transcript” section or link).',
        i18n: {
          summaryKey: 'mediaTranscriptPresent_summary_cantTell_missing',
          hintKey: 'mediaTranscriptPresent_hint_cantTell_missing',
          params: { element: mediaTag }
        },
        data: {
          details: {
            reasonCode: 'transcriptNotDetected',
            mediaTag,
            evidence
          },
          visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
        }
      };

      if (helpers && typeof helpers.reportOccurrence === 'function') {
        occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
      } else {
        occurrences.push({ selector: '', html: '', ...baseOccurrence });
      }
      continue;
    }

    if (evidence.strength === 'weak') {
      const baseOccurrence = {
        summary:
          'A transcript or other text alternative may be available for this time-based media, but it could not be verified from the page content.',
        hint: 'Ensure a clearly identified transcript or other text alternative is available and programmatically or visibly associated with the media on the page.',
        i18n: {
          summaryKey: 'mediaTranscriptPresent_summary_cantTell_unverified',
          hintKey: 'mediaTranscriptPresent_hint_cantTell_unverified',
          params: { element: mediaTag }
        },
        data: {
          details: {
            reasonCode: 'transcriptEvidenceUnverified',
            mediaTag,
            evidence
          },
          visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
        }
      };

      if (helpers && typeof helpers.reportOccurrence === 'function') {
        occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
      } else {
        occurrences.push({ selector: '', html: '', ...baseOccurrence });
      }
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }

  // Manual rules may only emit cantTell/notApplicable (never pass/fail).
  // Strong evidence was found for all applicable media elements, so there
  // is nothing to flag for review.
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "menuitem-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="menuitem"],[role="menuitemcheckbox"],[role="menuitemradio"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function evaluate(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // Menuitem name is commonly from content.
    const t = getConservativeSubtreeText(document, el);
    if (t) return { ok: true, method: 'content' };

    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    const role = getAttr(el, 'role').toLowerCase();
    if (role !== 'menuitem' && role !== 'menuitemcheckbox' && role !== 'menuitemradio') continue;

    applicableCount += 1;

    const res = evaluate(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This menu item has no accessible name.',
      hint: 'Provide visible text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.',
      i18n: {
        summaryKey: 'menuitemNamePresent_summary_fail',
        hintKey: 'menuitemNamePresent_hint_fail',
        params: { role }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: role, methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "meta-refresh-no-exceptions": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const nodes = document.querySelectorAll
    ? document.querySelectorAll('meta[http-equiv="refresh" i]')
    : [];

  const occurrences = [];
  // HTML's shared declarative refresh steps. Returns the delay in seconds, or
  // null when the value is not a valid refresh directive, in which case the
  // browser never refreshes and there is nothing to report. Rejects a leading
  // sign ("+72001"), a non-numeric time ("foo"), and a separator other than
  // "," or ";" ("0:1").
  function parseRefreshDelay(input) {
    const s = String(input == null ? '' : input);
    const isSpace = (c) => c === ' ' || c === '\t' || c === '\n' || c === '\f' || c === '\r';
    let i = 0;
    while (i < s.length && isSpace(s[i])) i++;
    let digits = '';
    while (i < s.length && s[i] >= '0' && s[i] <= '9') digits += s[i++];
    if (!digits) return null;
    if (s[i] === '.') {
      i++;
      while (i < s.length && s[i] >= '0' && s[i] <= '9') i++;
    }
    if (i >= s.length) return parseInt(digits, 10);
    let sawSpace = false;
    while (i < s.length && isSpace(s[i])) {
      sawSpace = true;
      i++;
    }
    if (i >= s.length) return parseInt(digits, 10);
    if (s[i] === ';' || s[i] === ',' || sawSpace) return parseInt(digits, 10);
    return null;
  }

  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    if (el.closest && el.closest('noscript')) continue; // never applies with scripting enabled
    const raw = String(el.getAttribute('content') || '').trim();
    if (!raw) continue;
    if (parseRefreshDelay(raw) === null) continue;

    applicableCount += 1;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary:
        'This page uses a meta refresh, which is an automatic context change not initiated by the user.',
      hint: 'Remove the meta refresh; trigger the redirect/refresh only in response to a user action instead.',
      i18n: {
        summaryKey: 'metaRefreshNoExceptions_summary_fail',
        hintKey: 'metaRefreshNoExceptions_hint_fail',
        params: {}
      },
      data: {
        details: { reasonCode: 'META_REFRESH_PRESENT' }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "meta-refresh-timing-absent": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  // WCAG 2.2.1 Exception 3: time limits longer than 20 hours are exempt.
  const EXEMPT_DELAY_SECONDS = 20 * 60 * 60;

  const nodes = document.querySelectorAll
    ? document.querySelectorAll('meta[http-equiv="refresh" i]')
    : [];

  const occurrences = [];
  let applicableCount = 0;

  // HTML's shared declarative refresh steps. Returns the delay in seconds, or
  // null when the value is not a valid refresh directive, in which case the
  // browser never refreshes and there is nothing to report. Rejects a leading
  // sign ("+72001"), a non-numeric time ("foo"), and a separator other than
  // "," or ";" ("0:1").
  function parseRefreshDelay(input) {
    const s = String(input == null ? '' : input);
    const isSpace = (c) => c === ' ' || c === '\t' || c === '\n' || c === '\f' || c === '\r';
    let i = 0;
    while (i < s.length && isSpace(s[i])) i++;
    let digits = '';
    while (i < s.length && s[i] >= '0' && s[i] <= '9') digits += s[i++];
    if (!digits) return null;
    if (s[i] === '.') {
      i++;
      while (i < s.length && s[i] >= '0' && s[i] <= '9') i++;
    }
    if (i >= s.length) return parseInt(digits, 10);
    let sawSpace = false;
    while (i < s.length && isSpace(s[i])) {
      sawSpace = true;
      i++;
    }
    if (i >= s.length) return parseInt(digits, 10);
    if (s[i] === ';' || s[i] === ',' || sawSpace) return parseInt(digits, 10);
    return null;
  }

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    if (el.closest && el.closest('noscript')) continue; // never applies with scripting enabled — see meta-refresh-no-exceptions.js's header comment
    const raw = String(el.getAttribute('content') || '').trim();
    if (!raw) continue;

    const delay = parseRefreshDelay(raw);
    if (delay === null) continue;

    applicableCount += 1;

    if (!(delay > 0)) continue;
    if (delay > EXEMPT_DELAY_SECONDS) continue; // WCAG 2.2.1 Exception 3 (>20 hours)

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This page refreshes itself automatically after a delay.',
      hint: 'Remove the timed meta refresh, or provide a way for users to turn it off, extend it, or pause it before it triggers.',
      i18n: {
        summaryKey: 'metaRefreshTimingAbsent_summary_fail',
        hintKey: 'metaRefreshTimingAbsent_hint_fail',
        params: { delay: String(delay) }
      },
      data: {
        details: { reasonCode: 'META_REFRESH_DELAYED', delay }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "meta-viewport-large": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function parseContent(raw) {
    const out = {};
    for (const pair of String(raw || '').split(/[,;]/)) {
      const eq = pair.indexOf('=');
      if (eq === -1) continue;
      const key = pair.slice(0, eq).trim().toLowerCase();
      const value = pair
        .slice(eq + 1)
        .trim()
        .toLowerCase();
      if (key) out[key] = value;
    }
    return out;
  }

  const nodes = document.querySelectorAll
    ? document.querySelectorAll('meta[name="viewport" i]')
    : [];

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const raw = String(el.getAttribute('content') || '').trim();
    if (!raw) continue;

    applicableCount += 1;

    const parsed = parseContent(raw);
    const reasons = [];

    const userScalable = parsed['user-scalable'];
    if (userScalable === 'no' || userScalable === '0') {
      reasons.push('user-scalable=' + userScalable);
    }

    const maxScaleRaw = parsed['maximum-scale'];
    if (maxScaleRaw !== undefined) {
      const maxScale = parseFloat(maxScaleRaw);
      if (!Number.isNaN(maxScale) && maxScale < 5) {
        reasons.push('maximum-scale=' + maxScaleRaw);
      }
    }

    if (!reasons.length) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This viewport meta tag restricts zoom below the 500% best-practice target.',
      hint: 'Remove user-scalable=no and raise maximum-scale to at least 5 (500%) if possible.',
      i18n: {
        summaryKey: 'metaViewportLarge_summary_cantTell',
        hintKey: 'metaViewportLarge_hint_cantTell',
        params: { reasons: reasons.join(', ') }
      },
      data: {
        details: { reasonCode: 'VIEWPORT_ZOOM_BELOW_500', reasons }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "meta-viewport-zoom-enabled": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function parseContent(raw) {
    const out = {};
    for (const pair of String(raw || '').split(/[,;]/)) {
      const eq = pair.indexOf('=');
      if (eq === -1) continue;
      const key = pair.slice(0, eq).trim().toLowerCase();
      const value = pair
        .slice(eq + 1)
        .trim()
        .toLowerCase();
      if (key) out[key] = value;
    }
    return out;
  }

  const nodes = document.querySelectorAll
    ? document.querySelectorAll('meta[name="viewport" i]')
    : [];

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const raw = String(el.getAttribute('content') || '').trim();
    if (!raw) continue;

    const parsed = parseContent(raw);

    // ACT b4f0c3 applies only when content carries maximum-scale or
    // user-scalable; content that sets neither cannot restrict zoom.
    if (parsed['user-scalable'] === undefined && parsed['maximum-scale'] === undefined) continue;

    applicableCount += 1;
    const reasons = [];

    // CSS Device Adaptation translates an unparseable value to 0, so
    // user-scalable=invalid and maximum-scale=yes disable zoom just as
    // user-scalable=no does. A negative maximum-scale is out of range and
    // dropped instead, which is why it does not restrict anything.
    const userScalable = parsed['user-scalable'];
    if (
      userScalable !== undefined &&
      userScalable !== 'yes' &&
      userScalable !== 'device-width' &&
      userScalable !== 'device-height'
    ) {
      const scale = parseFloat(userScalable);
      if (Number.isNaN(scale) || (scale > -1 && scale < 1)) {
        reasons.push('user-scalable=' + userScalable);
      }
    }

    const maxScaleRaw = parsed['maximum-scale'];
    if (
      maxScaleRaw !== undefined &&
      maxScaleRaw !== 'device-width' &&
      maxScaleRaw !== 'device-height'
    ) {
      const maxScale = parseFloat(maxScaleRaw);
      if (Number.isNaN(maxScale) || (maxScale >= 0 && maxScale < 2)) {
        reasons.push('maximum-scale=' + maxScaleRaw);
      }
    }

    if (!reasons.length) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This viewport meta tag restricts the user’s ability to zoom.',
      hint: 'Remove user-scalable=no and any maximum-scale below 2 from the viewport meta content.',
      i18n: {
        summaryKey: 'metaViewportZoomEnabled_summary_fail',
        hintKey: 'metaViewportZoomEnabled_hint_fail',
        params: { reasons: reasons.join(', ') }
      },
      data: {
        details: { reasonCode: 'VIEWPORT_ZOOM_RESTRICTED', reasons }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "meter-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="meter"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function evaluate(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // role="meter" is name-from-author-only per WAI-ARIA: aria-label,
    // aria-labelledby, or title — no content-based naming method at all.
    // It must NOT fall back to subtree content — visible text near/inside a
    // custom meter widget is not reliably exposed as its accessible name.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    const role = getAttr(el, 'role').toLowerCase();
    if (role !== 'meter') continue;

    applicableCount += 1;

    const res = evaluate(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This meter has no accessible name.',
      hint: "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this meter's accessible name.",
      i18n: {
        summaryKey: 'meterNamePresent_summary_fail',
        hintKey: 'meterNamePresent_hint_fail',
        params: {}
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'meter', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "mouse-only-event-handlers": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const MOUSE_ONLY_ATTRS = [
    'onmouseover',
    'onmouseout',
    'onmousedown',
    'onmouseup',
    'ondblclick',
    'onmousemove',
    'onmouseenter',
    'onmouseleave'
  ];
  const KEYBOARD_EQUIV_ATTRS = ['onkeydown', 'onkeyup', 'onkeypress', 'onfocus', 'onblur'];

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  const selector = MOUSE_ONLY_ATTRS.map((a) => `[${a}]`).join(', ');
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const presentMouseAttrs = MOUSE_ONLY_ATTRS.filter((a) => trim(el.getAttribute(a)));
    if (!presentMouseAttrs.length) continue;

    const eligResult = helpers.isAccTreeEligible ? helpers.isAccTreeEligible(el, ctx) : true;
    const eligible =
      typeof eligResult === 'boolean' ? eligResult : !!(eligResult && eligResult.eligible);
    if (!eligible) continue;

    applicableCount += 1;

    const hasKeyboardEquiv = KEYBOARD_EQUIV_ATTRS.some((a) => trim(el.getAttribute(a)));
    if (hasKeyboardEquiv) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';
    const eligInfo = helpers.getEligibilityInfo
      ? helpers.getEligibilityInfo(el, ctx, { targetSet: 'acc' })
      : null;

    occurrences.push({
      selector: stableSelector,
      html,
      summary: `This element has ${presentMouseAttrs.join(', ')} but no keyboard-reachable equivalent handler.`,
      hint: 'Add onkeydown/onkeyup/onkeypress (or onfocus/onblur for hover-triggered behavior) so this functionality is also reachable by keyboard.',
      i18n: {
        summaryKey: 'mouseOnlyEventHandlers_summary_cantTell',
        hintKey: 'mouseOnlyEventHandlers_hint_cantTell',
        params: { attrs: presentMouseAttrs.join(', ') }
      },
      data: {
        details: {
          reasonCode: 'MOUSE_ONLY_HANDLER_NO_KEYBOARD_EQUIVALENT',
          mouseAttrs: presentMouseAttrs
        },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }

  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "nested-interactive-controls-absent": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  // Declared inside runInPage — see scripts/build-core.js header
  // ("runInPage MUST be self-contained").
  const INTERACTIVE_SELECTOR = [
    'a[href]',
    'button',
    'input:not([type="hidden"])',
    'select',
    'textarea',
    '[role="button"]',
    '[role="link"]',
    '[role="checkbox"]',
    '[role="radio"]',
    '[role="switch"]',
    '[role="tab"]',
    '[role="textbox"]',
    '[role="combobox"]',
    '[role="listbox"]',
    '[role="menuitem"]',
    '[role="menuitemcheckbox"]',
    '[role="menuitemradio"]',
    '[role="option"]',
    '[role="slider"]',
    '[role="spinbutton"]',
    '[role="searchbox"]',
    '[role="treeitem"]'
  ].join(', ');

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;
  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isEligible(node) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(node, ctx);
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  function matchesInteractive(node) {
    return !!(
      node &&
      node.nodeType === 1 &&
      typeof node.matches === 'function' &&
      node.matches(INTERACTIVE_SELECTOR)
    );
  }

  // Operable = a native-interactive / ARIA-widget element that is exposed to
  // the accessibility tree and platform-focusable. A widget-role element with
  // no independent focus (e.g. a role="option" with no tabindex, driven by
  // its container via roving tabindex or aria-activedescendant) is not
  // operable and does not nest an interactive control. When no focus model is
  // available, role membership alone qualifies.
  function isOperableInteractive(node) {
    if (!matchesInteractive(node)) return false;
    if (!isEligible(node)) return false;
    if (!getFocusableInfo) return true;
    try {
      const info = getFocusableInfo(node, ctx);
      return !!(info && info.focusable);
    } catch {
      return true;
    }
  }

  // Shallowest operable interactive descendants of `root`. Traversal stops at
  // each counted node instead of descending into it, so a control nested
  // inside another operable control is attributed to its nearest operable
  // ancestor rather than to every enclosing container. Eligibility is applied
  // per node during the walk, so hidden or aria-hidden subtrees drop out.
  function collectNestedOperable(root) {
    const out = [];
    const top = root && root.children;
    if (!top || !top.length) return out;
    const stack = [];
    for (let i = top.length - 1; i >= 0; i--) stack.push(top[i]);
    while (stack.length) {
      const node = stack.pop();
      if (node && node.nodeType === 1 && isOperableInteractive(node)) {
        out.push(node);
        continue; // do not descend into a counted control
      }
      const kids = node && node.children;
      if (kids && kids.length) {
        for (let i = kids.length - 1; i >= 0; i--) stack.push(kids[i]);
      }
    }
    return out;
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(INTERACTIVE_SELECTOR)
    : helpers.queryAll(INTERACTIVE_SELECTOR);

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || el.nodeType !== 1) continue;
    if (!isEligible(el)) continue;

    applicableCount += 1;

    const nested = collectNestedOperable(el);
    if (!nested.length) continue;

    const nestedTags = nested.map((n) => (n && n.tagName ? n.tagName.toLowerCase() : 'unknown'));
    const dedupedNestedTags = [...new Set(nestedTags)];

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This interactive control contains one or more other interactive controls.',
      hint: 'Move the nested interactive control(s) outside this element; nested interactive controls are not reliably operable via assistive technology.',
      i18n: {
        summaryKey: 'nestedInteractiveControlsAbsent_summary_fail',
        hintKey: 'nestedInteractiveControlsAbsent_hint_fail',
        params: { element: tag, nestedElements: dedupedNestedTags.join(', ') }
      },
      data: {
        details: {
          reasonCode: 'NESTED_INTERACTIVE_CONTROL',
          element: tag,
          nestedElements: nestedTags
        }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "no-autoplay-audio": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('audio[autoplay], video[autoplay]')
    : helpers.queryAll('audio[autoplay], video[autoplay]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.hasAttribute) continue;
    if (el.hasAttribute('muted')) continue;

    applicableCount += 1;

    if (el.hasAttribute('controls')) continue;

    const mediaTag = (el.tagName || '').toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    const baseOccurrence = {
      selector: stableSelector,
      html,
      summary:
        'This element autoplays audio without a native pause/stop or volume-control mechanism.',
      hint: 'If this clip plays for more than 3 seconds, add a `controls` attribute (or an equivalent custom mechanism) so users can pause/stop it or control its volume independently of the system volume.',
      i18n: {
        summaryKey: 'noAutoplayAudio_summary_cantTell',
        hintKey: 'noAutoplayAudio_hint_cantTell',
        params: { element: mediaTag }
      },
      data: {
        details: { reasonCode: 'AUTOPLAY_NO_CONTROLS_MECHANISM', mediaTag }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push(baseOccurrence);
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }

  // Manual rules may only emit cantTell/notApplicable (never pass/fail):
  // every applicable autoplaying element already has a controls mechanism.
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "object-text-alternative-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  // ACT 8fc3b6 applies only to objects included in the accessibility tree, so a
  // focusable object inside aria-hidden is out of scope; aria-hidden-focus
  // reports that markup instead.
  const isEligibleHelper =
    helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
      ? helpers.isIncludedInAccessibilityTree
      : helpers && typeof helpers.isAccTreeEligible === 'function'
        ? helpers.isAccTreeEligible
        : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;

  const objects = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('object') : queryAll('object')) || []);
    } catch {
      return queryAll('object');
    }
  })();

  if (!objects.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  const trim = (v) => (v == null ? '' : String(v)).trim();

  function computeNameInfo(el) {
    // For <object>, treat ARIA name as primary. Do not accept HTML <label> associations.
    const flags = [];
    let aria = null;

    if (getAriaNameInfo) {
      aria = (() => {
        try {
          return getAriaNameInfo(el, ctx);
        } catch {
          return null;
        }
      })();
    }

    if (aria && aria.present && trim(aria.value)) {
      return {
        present: true,
        value: trim(aria.value),
        mechanism: aria.mechanism || 'aria',
        flags: (aria.flags || []).slice(0)
      };
    }

    const title = trim(el.getAttribute && el.getAttribute('title'));
    if (title) {
      flags.push('title-used');
      return { present: true, value: title, mechanism: 'title', flags };
    }

    if (aria && aria.flags && aria.flags.length) {
      for (const f of aria.flags) flags.push(f);
    }

    return { present: false, value: '', mechanism: 'none', flags };
  }

  function computeFallbackText(el) {
    try {
      // Deterministic + bounded: textContent can be large
      const raw = trim(el.textContent || '');
      const t = raw.length > 1000 ? raw.slice(0, 1000) : raw;
      return { present: !!t, value: t, mechanism: 'fallback', flags: t ? [] : ['empty'] };
    } catch {
      return { present: false, value: '', mechanism: 'fallback', flags: ['error'] };
    }
  }

  for (const el of objects) {
    if (!el || !el.getAttribute) continue;

    if (isEligibleHelper) {
      const elig = (() => {
        try {
          return isEligibleHelper(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig === false) continue;
      if (elig && elig.eligible === false) continue;
    }

    // role presentation/none exclusion only when not focusable
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role === 'presentation' || role === 'none') {
      let focusable;
      if (getFocusableInfo) {
        const fi = (() => {
          try {
            return getFocusableInfo(el, ctx);
          } catch {
            return null;
          }
        })();
        focusable = !!(fi && fi.focusable);
      } else {
        const tabindex = el.getAttribute('tabindex');
        focusable =
          tabindex != null &&
          String(tabindex).trim() !== '' &&
          !Number.isNaN(Number(String(tabindex).trim()));
      }
      if (!focusable) continue;
    }

    applicableCount += 1;

    const name = computeNameInfo(el);

    // Only compute fallback text if there is no name.
    // (textContent can be expensive; avoid when not needed)
    const fb = name.present
      ? { present: false, value: '', mechanism: 'fallback', flags: ['skipped-name-present'] }
      : computeFallbackText(el);

    const hasTextAlt = !!(name.present || fb.present);
    if (hasTextAlt) continue;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Missing text alternative for <object>.',
      hint: 'Provide meaningful fallback content inside <object>, add an accessible name (aria-label/aria-labelledby), or use a title attribute as a best-effort fallback.',
      i18n: {
        summaryKey: 'object_textAltPresent_summary_fail',
        hintKey: 'object_textAltPresent_hint_fail',
        params: { element: 'object' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        fallback: fb,
        name
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      // Never compute selector/snippet in the rule.
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "object-text-alternative-quality": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  const els = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('object') : queryAll('object')) || []);
    } catch {
      return queryAll('object');
    }
  })();

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    const trim = (v) => (v == null ? '' : String(v)).trim();

    let fallbackText = '';
    let ariaLabel = '';
    let ariaLabelledBy = '';
    let title = '';
    let labelledByText = '';

    try {
      fallbackText = trim(el.textContent || '');
      ariaLabel = trim(el.getAttribute('aria-label'));
      ariaLabelledBy = trim(el.getAttribute('aria-labelledby'));
      title = trim(el.getAttribute('title'));
    } catch {}

    // Only resolve idrefs if needed/present
    if (
      !ariaLabel &&
      ariaLabelledBy &&
      helpers &&
      typeof helpers.getTextFromIdRefs === 'function'
    ) {
      try {
        const t = helpers.getTextFromIdRefs(ariaLabelledBy, ctx);
        labelledByText = trim(t && t.text);
      } catch {}
    }

    const details = {
      fallbackText: fallbackText || null,
      ariaLabel: ariaLabel || null,
      ariaLabelledBy: ariaLabelledBy || null,
      ariaLabelledByText: labelledByText || null,
      title: title || null
    };

    const hasMechanism = !!(
      details.fallbackText ||
      details.ariaLabel ||
      details.ariaLabelledByText ||
      details.title
    );
    if (!hasMechanism) continue;

    applicableCount += 1;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Review text alternative for <object> for equivalence and appropriateness.',
      hint: 'Confirm the fallback content or ARIA name provides an equivalent alternative for the embedded content.',
      i18n: {
        summaryKey: 'object_textAltQuality_summary_cantTell',
        hintKey: 'object_textAltQuality_hint_cantTell',
        params: { element: 'object' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "option-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="option"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function hasName(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const labelled = resolveAriaLabelledbyText(document, el, 8);
    if (labelled) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    const t = getConservativeSubtreeText(document, el);
    if (t) return { ok: true, method: 'content' };

    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    applicableCount += 1;

    const res = hasName(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This element has no accessible name.',
      hint: 'Provide aria-label or aria-labelledby (preferred), or provide visible text that is not hidden from assistive technologies.',
      i18n: {
        summaryKey: 'optionNamePresent_summary_fail',
        hintKey: 'optionNamePresent_hint_fail',
        params: { controlType: 'option' }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'option', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "p-as-heading": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const MAX_HEADING_LIKE_CHARS = 120;
  const MIN_FONT_SIZE_PX = 18;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  function safeComputedStyle(el) {
    try {
      if (!el || el.nodeType !== 1) return null;
      if (helpers && typeof helpers.computedStyle === 'function') {
        const cs = helpers.computedStyle(el);
        if (cs) return cs;
      }
      const view =
        el.ownerDocument && el.ownerDocument.defaultView ? el.ownerDocument.defaultView : null;
      if (view && typeof view.getComputedStyle === 'function') return view.getComputedStyle(el);
    } catch {}
    return null;
  }

  function isBoldWeight(cs) {
    if (!cs) return false;
    const w = trim(cs.fontWeight).toLowerCase();
    if (w === 'bold' || w === 'bolder') return true;
    const n = Number.parseInt(w, 10);
    return Number.isFinite(n) && n >= 700;
  }

  function isEntirelyBold(p, text) {
    const cs = safeComputedStyle(p);
    if (isBoldWeight(cs)) return true;

    // A single <strong>/<b> child that wraps the whole text also counts.
    const children = Array.from(p.children || []);
    const boldWrap = children.find((c) => {
      const tag = (c.tagName || '').toLowerCase();
      return tag === 'strong' || tag === 'b';
    });
    if (boldWrap && children.length === 1) {
      const wrapText = trim(boldWrap.textContent || '');
      if (wrapText && wrapText === text) return true;
    }
    return false;
  }

  function getFontSizePx(p) {
    const cs = safeComputedStyle(p);
    if (!cs) return 0;
    const px = Number.parseFloat(cs.fontSize);
    return Number.isFinite(px) ? px : 0;
  }

  const nodes = helpers.queryAllSmart ? helpers.queryAllSmart('p') : helpers.queryAll('p');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const text = trim(el.textContent || '');
    if (!text || text.length > MAX_HEADING_LIKE_CHARS) continue;

    applicableCount += 1;

    if (!isEntirelyBold(el, text)) continue;

    const fontSizePx = getFontSizePx(el);
    if (fontSizePx < MIN_FONT_SIZE_PX) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    const baseOccurrence = {
      selector: stableSelector,
      html,
      summary: 'This paragraph is entirely bold and rendered at a heading-like size.',
      hint: 'If this text introduces a new section, use a real heading element (<h1>-<h6> or role="heading") instead of styling a paragraph to look like one.',
      i18n: {
        summaryKey: 'pAsHeading_summary_cantTell',
        hintKey: 'pAsHeading_hint_cantTell',
        params: { fontSizePx: String(fontSizePx) }
      },
      data: {
        details: { reasonCode: 'BOLD_LARGE_PARAGRAPH', fontSizePx }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push(baseOccurrence);
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }

  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "page-has-heading-one": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const body = document && document.body ? document.body : null;
  if (!body) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return raw.split(/\s+/)[0].toLowerCase();
  }

  function isLevelOneHeading(el) {
    const explicit = getExplicitRoleToken(el);
    if (explicit) {
      if (explicit !== 'heading') return false;
      const raw = normalizeWs(el.getAttribute && el.getAttribute('aria-level'));
      return raw === '1';
    }
    const tag = el.tagName ? el.tagName.toLowerCase() : '';
    return tag === 'h1';
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isExposedToAt(el) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // queryAllSmart (shadow-DOM-aware) instead of plain document.querySelectorAll -- see
  // landmark-one-main-manual.js's identical precedent.
  let nodes;
  try {
    nodes =
      helpers && typeof helpers.queryAllSmart === 'function'
        ? helpers.queryAllSmart('h1, [role]')
        : document.querySelectorAll('h1, [role]');
  } catch {
    nodes = [];
  }

  const hasH1 = Array.from(nodes).some((el) => el && isLevelOneHeading(el) && isExposedToAt(el));

  if (hasH1) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const stableSelector = helpers.buildSelector ? helpers.buildSelector(body) : 'body';
  const html = helpers.getOuterHtmlSnippet
    ? helpers.getOuterHtmlSnippet(body)
    : (body.outerHTML || '').slice(0, 200);

  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences: [
      {
        selector: stableSelector,
        html,
        summary: 'This page has no level-one heading.',
        hint: 'Add a level-one heading (<h1> or role="heading" aria-level="1") that identifies the page\'s main content.',
        i18n: {
          summaryKey: 'pageHasHeadingOne_summary_cantTell',
          hintKey: 'pageHasHeadingOne_hint_cantTell',
          params: {}
        },
        data: {
          details: { reasonCode: 'HEADING_ONE_MISSING' }
        }
      }
    ]
  };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "page-title-patterns": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const probes =
    ctx && ctx.inputs && ctx.inputs.probes && typeof ctx.inputs.probes === 'object'
      ? ctx.inputs.probes
      : null;

  const pageTitlesProbe =
    probes && probes['crawl.pageTitles'] && typeof probes['crawl.pageTitles'] === 'object'
      ? probes['crawl.pageTitles']
      : null;

  const occurrences = [];
  let applicableCount = 1;

  const titleEl = document.querySelector('head > title');
  const rawTitle = document.title || '';
  const titleText = rawTitle.replace(/\s+/g, ' ').trim();
  const titleLc = titleText.toLowerCase();
  // =========================
  // Cross-page pattern analysis (preferred) if crawl.pageTitles probe is provided
  // =========================
  if (pageTitlesProbe && Array.isArray(pageTitlesProbe.pages)) {
    const pages = pageTitlesProbe.pages.filter((p) => p && typeof p === 'object');

    // Require enough data to avoid noisy conclusions
    const MIN_PAGES = 10;
    const analyzable = pages
      .map((p) => ({
        url: p.url ? String(p.url) : null,
        title: typeof p.title === 'string' ? p.title.replace(/\s+/g, ' ').trim() : ''
      }))
      .filter((p) => p.url && p.title);

    if (analyzable.length >= MIN_PAGES) {
      // Build normalized title groups (case-insensitive)
      const groups = new Map(); // normTitle -> { title, urls: [] }
      for (const p of analyzable) {
        const norm = p.title.toLowerCase();
        if (!groups.has(norm)) groups.set(norm, { title: p.title, urls: [] });
        groups.get(norm).urls.push(p.url);
      }

      // Duplicate titles across distinct URLs is a strong "review" signal (not a guaranteed failure)
      const dupGroups = Array.from(groups.values()).filter((g) => g.urls.length >= 2);

      // Boilerplate-ish: detect a long common suffix/prefix across most titles.
      // Keep conservative: only flag if the common part is long and shared by many.
      const titles = analyzable.map((p) => p.title);
      function commonPrefix(a, b) {
        const n = Math.min(a.length, b.length);
        let i = 0;
        for (; i < n; i++) if (a[i] !== b[i]) break;
        return a.slice(0, i);
      }
      function commonSuffix(a, b) {
        const ra = a.split('').reverse().join('');
        const rb = b.split('').reverse().join('');
        return commonPrefix(ra, rb).split('').reverse().join('');
      }

      let sharedPrefix = titles[0] || '';
      let sharedSuffix = titles[0] || '';
      for (let i = 1; i < titles.length; i++) {
        sharedPrefix = commonPrefix(sharedPrefix, titles[i]);
        sharedSuffix = commonSuffix(sharedSuffix, titles[i]);
        if (sharedPrefix.length === 0 && sharedSuffix.length === 0) break;
      }

      const prefixLen = sharedPrefix.trim().length;
      const suffixLen = sharedSuffix.trim().length;

      const hasStrongTemplateSignal =
        (prefixLen >= 12 || suffixLen >= 12) &&
        (prefixLen >= 12 ? sharedPrefix.trim().length : 0) +
          (suffixLen >= 12 ? sharedSuffix.trim().length : 0) >=
          12;

      // If any cross-page signal exists, emit cantTell occurrence(s)
      if (dupGroups.length || hasStrongTemplateSignal) {
        const reasonCode = dupGroups.length
          ? 'duplicateTitlesAcrossPages'
          : 'templatedTitlesAcrossPages';

        // Deterministic example title for i18n params (lexicographic, case-insensitive)
        const exampleTitle = dupGroups.length
          ? dupGroups
              .map((g) =>
                String(g.title || '')
                  .replace(/\s+/g, ' ')
                  .trim()
              )
              .filter(Boolean)
              .sort((a, b) => a.localeCompare(b, undefined, { sensitivity: 'base' }))[0] || ''
          : '';

        const summaryKey = dupGroups.length
          ? 'pageTitlePatterns_summary_cantTell_duplicateAcrossPages'
          : 'pageTitlePatterns_summary_cantTell_templatedAcrossPages';

        const i18nParams = dupGroups.length
          ? {
              reasonCode,
              pagesAnalyzed: analyzable.length,
              duplicateGroups: dupGroups.length,
              exampleTitle
            }
          : {
              reasonCode,
              pagesAnalyzed: analyzable.length
            };

        const occBase = {
          selector: 'head > title',
          html: '',
          summary:
            'The set of page titles may not be descriptive enough to distinguish pages by topic or purpose.',
          hint: 'Ensure each page title is sufficiently descriptive and helps users distinguish pages (avoid identical or overly templated titles across many pages).',
          i18n: {
            summaryKey,
            hintKey: 'pageTitlePatterns_hint_cantTell',
            params: i18nParams
          },
          data: {
            details: {
              reasonCode,
              metrics: {
                pagesAnalyzed: analyzable.length,
                duplicateGroups: dupGroups.length,
                largestDuplicateGroupSize: dupGroups.length
                  ? Math.max(...dupGroups.map((g) => g.urls.length))
                  : 0,
                sharedPrefix: prefixLen >= 12 ? sharedPrefix.trim() : '',
                sharedSuffix: suffixLen >= 12 ? sharedSuffix.trim() : ''
              },
              refs: {
                exampleDuplicateTitles: dupGroups.slice(0, 3).map((g) => ({
                  title: g.title,
                  urls: g.urls.slice(0, 5)
                }))
              }
            },
            visibilityFilter: { targetSet: 'acc', accEligible: null, reasons: [] }
          }
        };

        if (titleEl && helpers && typeof helpers.reportOccurrence === 'function') {
          occurrences.push(helpers.reportOccurrence(titleEl, occBase));
        } else {
          // No node available: keep deterministic fallback snippet (no helper calls).
          occurrences.push({
            ...occBase,
            html:
              titleEl && titleEl.outerHTML
                ? String(titleEl.outerHTML).slice(0, 2000)
                : '<title>(unknown)</title>'
          });
        }

        // Cross-page analysis is authoritative when present; do not also run single-page heuristics.
        return {
          ruleId: rule.ruleId,
          outcome: 'cantTell',
          severity: rule.defaultSeverity || 'minor',
          occurrences
        };
      }

      // If we had enough pages and found no review signal, there is nothing
      // to flag for manual review.
      return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
    }

    // Not enough analyzable pages -> notApplicable for cross-page patterns, fall back to single-page logic.
  }

  // If there's no title (or empty), defer to the hard-fail rule.
  if (!titleEl || titleText.length === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const GENERIC_TITLES = new Set(['home', 'homepage', 'welcome', 'untitled', 'page', 'document']);

  // Conservative signals:
  // - very short title (likely non-descriptive)
  // - title is one of a small set of generic titles
  const isVeryShort = titleText.length > 0 && titleText.length < 8;
  const isGeneric = GENERIC_TITLES.has(titleLc);

  // Template-like: "Brand | Home" or "Home - Brand" where the page-specific part is a generic token.
  const templateLike =
    /\b(home|homepage|welcome)\b\s*(\||-|—|:)\s*.+/i.test(titleText) ||
    /.+\s*(\||-|—|:)\s*\b(home|homepage|welcome)\b/i.test(titleText);

  if (isGeneric || isVeryShort || templateLike) {
    const reasonCode = isGeneric
      ? 'genericTitle'
      : isVeryShort
        ? 'veryShortTitle'
        : 'templateLikeTitle';

    const summaryKey =
      reasonCode === 'genericTitle'
        ? 'pageTitlePatterns_summary_cantTell_generic'
        : reasonCode === 'veryShortTitle'
          ? 'pageTitlePatterns_summary_cantTell_veryShort'
          : 'pageTitlePatterns_summary_cantTell_templateLike';
    const occBase = {
      selector: 'head > title',
      html: '',
      summary:
        'The page title may not be descriptive enough to identify the page topic or purpose.',
      hint: 'Use a more specific title that identifies the page topic or purpose (for example, include the section name or task).',
      i18n: {
        summaryKey,
        hintKey: 'pageTitlePatterns_hint_cantTell',
        params: {}
      },
      data: {
        details: {
          reasonCode,
          titleText
        },
        visibilityFilter: { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    };

    if (titleEl && helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(titleEl, occBase));
    } else {
      occurrences.push({
        ...occBase,
        html:
          titleEl && titleEl.outerHTML
            ? String(titleEl.outerHTML).slice(0, 2000)
            : '<title>(unknown)</title>'
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    // Patterns are review signals: cantTell rather than fail.
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }

  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "page-title-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const occurrences = [];
  let applicableCount = 1;

  // Not restricted to `head > title`: per HTML parsing, a <title> encountered
  // after <head> has closed is not re-parented into <head>, yet the browser
  // (and document.title, used below) still recognizes it as the document's
  // title — matching that here avoids a false "missing title" report for a
  // <title> that legitimately ended up outside <head>.
  const titleEl = document.querySelector('title');
  const titleText = (document.title || '').replace(/\s+/g, ' ').trim();

  const missingTitleEl = !titleEl;
  const emptyTitle = !missingTitleEl && titleText.length === 0;

  if (missingTitleEl || emptyTitle) {
    const reasonCode = missingTitleEl ? 'missingTitleElement' : 'emptyTitleText';

    const occBase = {
      selector: 'head > title',
      html: '',
      summary: missingTitleEl
        ? 'The page is missing a <title> element.'
        : 'The page has an empty <title>.',
      hint: 'Provide a descriptive, non-empty <title> that identifies the page topic or purpose.',
      i18n: {
        summaryKey: missingTitleEl
          ? 'pageTitlePresent_summary_fail_missing'
          : 'pageTitlePresent_summary_fail_empty',
        hintKey: 'pageTitlePresent_hint_fail',
        params: {}
      },
      data: {
        details: {
          reasonCode,
          titleText
        },
        visibilityFilter: { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    };

    if (!missingTitleEl && titleEl && helpers && typeof helpers.reportOccurrence === 'function') {
      // empty title: we have a node, so engine can attach html
      occurrences.push(helpers.reportOccurrence(titleEl, occBase));
    } else {
      // missing title element: no node exists, keep deterministic html string
      occurrences.push({ ...occBase, html: '<title>(missing)</title>' });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "presentation-role-conflict": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  // The full set of ARIA attributes marked `global: true` per the WAI-ARIA
  // spec — any of these present on a presentational element restores its
  // implicit role, not just the naming ones.
  const CONFLICTING_ATTRS = [
    'aria-atomic',
    'aria-braillelabel',
    'aria-brailleroledescription',
    'aria-busy',
    'aria-controls',
    'aria-current',
    'aria-describedby',
    'aria-description',
    'aria-details',
    'aria-disabled',
    'aria-dropeffect',
    'aria-errormessage',
    'aria-flowto',
    'aria-grabbed',
    'aria-haspopup',
    'aria-hidden',
    'aria-invalid',
    'aria-keyshortcuts',
    'aria-label',
    'aria-labelledby',
    'aria-live',
    'aria-owns',
    'aria-relevant',
    'aria-roledescription'
  ];

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[role="presentation"], [role="none"], img[alt=""]')
    : helpers.queryAll('[role="presentation"], [role="none"], img[alt=""]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    applicableCount += 1;

    // Presence, not value truthiness: the WAI-ARIA role-conflict-resolution
    // rule triggers on a global ARIA attribute being SPECIFIED at all, even
    // with an empty value — e.g. <img alt="" aria-hidden="">, where
    // aria-hidden="" (empty string) is still a specified attribute. A
    // truthy-value check would miss this.
    let present = CONFLICTING_ATTRS.filter((attr) =>
      el.hasAttribute ? el.hasAttribute(attr) : el.getAttribute(attr) != null
    );

    // aria-hidden="true" (the exact, valid truthy value — not the
    // empty-string case above, which never actually hides anything) is a
    // special case: it removes the element and its subtree from the
    // accessibility tree unconditionally, independent of role. That makes
    // the "role restoration" this rule warns about ("...which restores its
    // implicit role and cancels the presentational intent") factually
    // inert — no AT will ever expose the restored role OR any of the other
    // conflicting attributes (aria-label, aria-describedby, ...) present
    // alongside it, since the whole element stays out of the tree
    // regardless. This pattern is extremely common (e.g. <svg
    // role="presentation" aria-hidden="true"> decorative icons — a
    // defensive belt-and-suspenders double-hide, not an authoring mistake).
    // Focusability is NOT covered by this exemption — a keyboard user can
    // still tab onto an aria-hidden="true" focusable element (the
    // aria-hidden-focus anti-pattern), a real, independent hazard
    // aria-hidden does nothing to prevent.
    if (el.getAttribute('aria-hidden') === 'true') {
      present = [];
    }

    let isFocusable = false;
    if (getFocusableInfo) {
      try {
        const fi = getFocusableInfo(el, ctx);
        isFocusable = !!(fi && fi.focusable);
      } catch {
        isFocusable = false;
      }
    }

    if (!present.length && !isFocusable) continue;

    const parts = present.slice();
    if (isFocusable) parts.push('focusable');

    // No explicit role attribute means this matched via the img[alt=""]
    // implicit-presentation case.
    const role =
      String(el.getAttribute('role') || '')
        .trim()
        .toLowerCase() || 'presentation';
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: `This role="${role}" element also has a conflicting condition (${parts.join(', ')}), which restores its implicit role and cancels the presentational intent.`,
      hint: 'Remove the conflicting naming attribute(s) and/or focusability (tabindex/native) if the element should stay presentational, or remove role="presentation"/"none" if it should be exposed to assistive technology.',
      i18n: {
        summaryKey: 'presentationRoleConflict_summary_cantTell',
        hintKey: 'presentationRoleConflict_hint_cantTell',
        params: { role, attrs: parts.join(', ') }
      },
      data: {
        details: {
          reasonCode: 'PRESENTATION_ROLE_CONFLICT',
          role,
          conflictingAttrs: present,
          focusable: isFocusable
        }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "progressbar-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="progressbar"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function evaluate(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // role="progressbar" is name-from-author-only per WAI-ARIA: aria-label,
    // aria-labelledby, or title — no content-based naming method at all. It
    // must NOT fall back to subtree content: e.g. a <ul role="progressbar">
    // loading skeleton with no name of its own, whose nested descendants
    // carry their own aria-label for a different purpose — falling back to
    // subtree content would wrongly treat that as the progressbar's name.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    const role = getAttr(el, 'role').toLowerCase();
    if (role !== 'progressbar') continue;

    applicableCount += 1;

    const res = evaluate(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This progress bar has no accessible name.',
      hint: "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this progress bar's accessible name.",
      i18n: {
        summaryKey: 'progressbarNamePresent_summary_fail',
        hintKey: 'progressbarNamePresent_hint_fail',
        params: {}
      },
      data: {
        details: {
          reasonCode: 'name_missing',
          controlType: 'progressbar',
          methodTried: res.method
        },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "region": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const body = document && document.body ? document.body : null;
  if (!body) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function lower(s) {
    return String(s || '').toLowerCase();
  }

  // Delegates to the shared helpers.getLandmarkNameInfo (aria-label -> aria-labelledby, via the
  // target's own accessible name, not raw textContent -> title attribute fallback) rather than a
  // local copy -- see that function's header comment in src/core/dom-helpers.js.
  function getAccessibleLandmarkName(el) {
    try {
      if (helpers && typeof helpers.getLandmarkNameInfo === 'function') {
        const info = helpers.getLandmarkNameInfo(el, ctx);
        if (info && info.present && info.value) return normalizeWs(info.value);
      }
    } catch {}
    return '';
  }

  function getExplicitRoleToken(el) {
    const raw = normalizeWs(el.getAttribute && el.getAttribute('role'));
    if (!raw) return '';
    return lower(raw.split(/\s+/)[0]);
  }

  // Delegates to the shared helpers.hasLandmarkScopingAncestor for the
  // question "does this element sit inside a sectioning-content/<main>
  // ancestor that suppresses its conditional implicit role" — role-aware
  // (an ancestor's bare TAG only counts when it carries no role attribute
  // at all; an explicit role="dialog"-style override no longer suppresses)
  // rather than a local tag-only copy. See that function's header comment
  // in src/core/aria-helpers.js for the full algorithm — e.g. an
  // <aside role="dialog"> containing its own <header>, where the <header>
  // keeps its banner role.
  function hasSectioningAncestor(el, includeMain) {
    return helpers && typeof helpers.hasLandmarkScopingAncestor === 'function'
      ? helpers.hasLandmarkScopingAncestor(el, { includeMain })
      : false;
  }

  function getImplicitLandmarkRole(el) {
    const tag = el.tagName ? lower(el.tagName) : '';
    if (tag === 'header') return hasSectioningAncestor(el, true) ? '' : 'banner';
    if (tag === 'footer') return hasSectioningAncestor(el, true) ? '' : 'contentinfo';
    if (tag === 'main') return 'main';
    if (tag === 'nav') return 'navigation';
    if (tag === 'aside') {
      // A named <aside> is never suppressed, even when nested: keeps
      // "complementary" when the element has an accessible name, even
      // inside sectioning content. See landmark-unique-manual.js.
      if (!hasSectioningAncestor(el, false)) return 'complementary';
      return getAccessibleLandmarkName(el) ? 'complementary' : '';
    }
    if (tag === 'section') return getAccessibleLandmarkName(el) ? 'region' : '';
    if (tag === 'form') return getAccessibleLandmarkName(el) ? 'form' : '';
    return '';
  }

  const LANDMARK_ROLES = new Set([
    'banner',
    'contentinfo',
    'main',
    'navigation',
    'complementary',
    'region',
    'form',
    'search'
  ]);

  function isLandmark(el) {
    if (!el || !el.getAttribute) return false;
    const explicit = getExplicitRoleToken(el);
    if (explicit) return LANDMARK_ROLES.has(explicit);
    return !!getImplicitLandmarkRole(el);
  }

  const SKIP_TAGS = new Set(['script', 'style', 'template', 'noscript', 'link', 'meta', 'title']);

  // Roles/attributes that make an element its own self-contained
  // announced area — not literally a WAI-ARIA landmark, but not "content
  // that needs a landmark" either.
  const LIVE_REGION_ROLES = new Set(['alert', 'status', 'log', 'marquee', 'timer']);

  function isAriaLive(el) {
    const v = lower(normalizeWs(el.getAttribute && el.getAttribute('aria-live')));
    return v === 'polite' || v === 'assertive';
  }

  function isDialogLike(el) {
    const tag = el.tagName ? lower(el.tagName) : '';
    if (tag === 'dialog') return true;
    const role = getExplicitRoleToken(el);
    return role === 'dialog' || role === 'alertdialog';
  }

  function isButtonLike(el) {
    const role = getExplicitRoleToken(el);
    if (role) return role === 'button';
    const tag = el.tagName ? lower(el.tagName) : '';
    if (tag === 'button' || tag === 'summary') return true;
    if (tag === 'input') {
      const type = lower(normalizeWs(el.getAttribute && el.getAttribute('type')));
      return type === 'button' || type === 'submit' || type === 'reset' || type === 'image';
    }
    return false;
  }

  // A "skip to content" link is deliberately placed outside the main
  // content flow at the very top of the page — exempting it (when its
  // fragment actually resolves to a real target, not a dead "#"
  // placeholder) avoids flagging a helpful, common accessibility pattern
  // as the very thing this rule is meant to catch.
  function isResolvableSkipLink(el) {
    const tag = el.tagName ? lower(el.tagName) : '';
    if (tag !== 'a') return false;
    const href = el.getAttribute && el.getAttribute('href');
    if (!href || href.charAt(0) !== '#' || href.length < 2) return false;
    try {
      return !!(document.getElementById && document.getElementById(href.slice(1)));
    } catch {
      return false;
    }
  }

  function isStopper(el) {
    if (isLandmark(el)) return true;
    if (isAriaLive(el)) return true;
    const role = getExplicitRoleToken(el);
    if (role && LIVE_REGION_ROLES.has(role)) return true;
    if (isDialogLike(el)) return true;
    if (isButtonLike(el)) return true;
    const tag = el.tagName ? lower(el.tagName) : '';
    if (tag === 'svg' || tag === 'iframe' || tag === 'frame') return true;
    if (isResolvableSkipLink(el)) return true;
    return false;
  }

  const VISUAL_CONTENT_TAGS = new Set(['img', 'video', 'audio', 'canvas', 'object', 'embed']);

  // Non-recursive "does THIS element, on its own, carry content" check —
  // deliberately mirrors only the direct-content half of getContentNameInfo,
  // not a full name-from-content recursion: the whole point is to keep
  // recursing through plain wrapper elements (a framework's root mount
  // <div> included) until reaching the actual content-bearing node, rather
  // than a coarse ancestor swallowing everything beneath it into one report.
  function hasOwnContent(el) {
    const kids = el.childNodes || [];
    for (let i = 0; i < kids.length; i++) {
      const k = kids[i];
      if (k.nodeType === 3 && normalizeWs(k.nodeValue)) return true;
    }
    const tag = el.tagName ? lower(el.tagName) : '';
    if (VISUAL_CONTENT_TAGS.has(tag)) return true;
    if (
      tag === 'input' &&
      lower(normalizeWs(el.getAttribute && el.getAttribute('type'))) !== 'hidden'
    )
      return true;
    if (normalizeWs(el.getAttribute && el.getAttribute('aria-label'))) return true;
    return false;
  }

  // Defensive guard against pathological pages (mirrors the maxContentNodes
  // pattern in src/core/dom-helpers.js's getContentNameInfo) -- a full-body
  // structural walk visits more nodes than a single element's name
  // computation, hence the larger budget.
  const MAX_VISITED_NODES = 20000;
  let visited = 0;
  let truncated = false;

  const leaves = [];
  const stopperFlagged = new WeakSet();

  function markFlaggedUpToBody(el) {
    let cur = el;
    while (cur) {
      if (stopperFlagged.has(cur)) break; // everything above is already marked
      stopperFlagged.add(cur);
      if (cur === body) break;
      cur = cur.parentElement;
    }
  }

  function walk(el) {
    if (truncated || !el || el.nodeType !== 1) return;
    visited += 1;
    if (visited > MAX_VISITED_NODES) {
      truncated = true;
      return;
    }

    const tag = el.tagName ? lower(el.tagName) : '';
    if (SKIP_TAGS.has(tag)) return;

    const eligRes = helpers.isAccTreeEligible ? helpers.isAccTreeEligible(el) : { eligible: true };
    if (!(eligRes && eligRes.eligible)) {
      markFlaggedUpToBody(el);
      return;
    }

    if (isStopper(el)) {
      markFlaggedUpToBody(el);
      if (tag === 'iframe' || tag === 'frame') leaves.push(el);
      return;
    }

    if (hasOwnContent(el)) {
      leaves.push(el);
      return;
    }

    const kids = el.children || [];
    for (let i = 0; i < kids.length; i++) {
      walk(kids[i]);
      if (truncated) return;
    }
  }

  for (const child of body.children || []) walk(child);

  // Collapse each candidate leaf upward through parents that have no OTHER
  // stopper anywhere in their subtree, so contiguous unplaced content
  // merges into ONE occurrence per real gap instead of one per text node --
  // this collapsing is what keeps ordinary pages (landmarked content mixed
  // with a little stray content) from producing noisy, one-per-leaf reports.
  const collapsed = [];
  const seen = new Set();
  for (const leaf of leaves) {
    let cur = leaf;
    while (
      cur.parentElement &&
      cur.parentElement !== body &&
      !stopperFlagged.has(cur.parentElement)
    ) {
      cur = cur.parentElement;
    }
    if (!seen.has(cur)) {
      seen.add(cur);
      collapsed.push(cur);
    }
  }

  const occurrences = collapsed.map((el) => {
    const tag = el.tagName ? lower(el.tagName) : '';
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    return {
      selector: stableSelector,
      html,
      summary: 'This content is not contained within a landmark region.',
      hint: 'Move this content inside a landmark region (main, nav, aside, a labeled section, etc.).',
      i18n: {
        summaryKey: 'region_summary_cantTell',
        hintKey: 'region_hint_cantTell',
        params: { element: tag }
      },
      data: {
        details: { reasonCode: 'CONTENT_OUTSIDE_LANDMARK', element: tag }
      }
    };
  });

  if (occurrences.length === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  return {
    ruleId: rule.ruleId,
    outcome: 'cantTell',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: (function applicability(ctx) {
  return ctx.helpers.isWholeDocumentScope ? ctx.helpers.isWholeDocumentScope() : true;
}) },
    "role-img-text-alternative-present": { run: (function runInPage(ctx) {
  const { root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const trim = (v) => {
    try {
      return String(v == null ? '' : v)
        .replace(/\s+/g, ' ')
        .trim();
    } catch {
      return '';
    }
  };

  const imgElements = (() => {
    // do not consider element "img" because it has its own rule
    const sel = '[role="img" i]:not(img)';
    try {
      return Array.from((queryAllSmart ? queryAllSmart(sel) : queryAll(sel)) || []);
    } catch {
      return queryAll(sel);
    }
  })();

  if (!imgElements.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getAccessibleNameInfo =
    helpers && typeof helpers.getAccessibleNameInfo === 'function'
      ? helpers.getAccessibleNameInfo
      : null;

  for (const el of imgElements) {
    if (!el || !el.getAttribute) continue;

    // Applicability: eligible in the acc tree (with helper exceptions).
    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    applicableCount += 1;

    // Expectation: aria-label OR aria-labelledby. We use helper name-info when available,
    // but we also validate the source to keep this rule scoped/deterministic.

    const ariaLabelRaw = (() => {
      try {
        return el.getAttribute('aria-label');
      } catch {
        return null;
      }
    })();
    const ariaLabel = trim(ariaLabelRaw);

    const ariaLabelledbyRaw = (() => {
      try {
        return el.getAttribute('aria-labelledby');
      } catch {
        return null;
      }
    })();
    const ariaLabelledby = trim(ariaLabelledbyRaw);

    const hasAriaLabelAttr = ariaLabelRaw !== null;
    const hasAriaLabelledbyAttr = ariaLabelledbyRaw !== null;

    const hasValidAriaLabel = hasAriaLabelAttr && ariaLabel.length > 0;
    const hasValidAriaLabelledbyAttr = hasAriaLabelledbyAttr && ariaLabelledby.length > 0;

    // Last-resort naming mechanism per HTML-AAM: a non-empty title attribute.
    const titleRaw = (() => {
      try {
        return el.getAttribute('title');
      } catch {
        return null;
      }
    })();
    const hasValidTitle = titleRaw !== null && trim(titleRaw).length > 0;

    let nameInfo = null;

    // Fast outcomes first (no helper needed)
    let reasonCode = '';
    let hasName = false;

    if (!hasAriaLabelAttr && !hasAriaLabelledbyAttr) {
      if (hasValidTitle) {
        hasName = true;
      } else {
        reasonCode = 'missingTextAlternative';
      }
    } else if (hasAriaLabelAttr && !hasValidAriaLabel) {
      if (hasValidTitle) {
        hasName = true;
      } else {
        reasonCode = 'emptyAriaLabel';
      }
    } else if (hasAriaLabelledbyAttr && !hasValidAriaLabelledbyAttr) {
      if (hasValidTitle) {
        hasName = true;
      } else {
        reasonCode = 'emptyAriaLabelledby';
      }
    } else {
      // Mechanism present + non-empty. Optionally validate resolution via helper.
      if (getAccessibleNameInfo) {
        nameInfo = (() => {
          try {
            return getAccessibleNameInfo(el, ctx);
          } catch {
            return null;
          }
        })();
        const helperSaysHasName = !!(nameInfo && nameInfo.present && trim(nameInfo.value));
        if (helperSaysHasName) {
          hasName = true;
        } else {
          reasonCode = 'nameNotResolved';
        }
      } else {
        // Without helper, accept non-empty aria-label/labelledby as sufficient.
        hasName = true;
      }
    }

    if (hasName) continue;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Missing text alternative on element with role="img".',
      hint: 'Provide aria-label or aria-labelledby (referencing non-empty text) to give this image a text alternative.',
      i18n: {
        summaryKey: 'roleImg_textAlternativePresent_summary_fail',
        hintKey: 'roleImg_textAlternativePresent_hint_fail',
        params: { role: 'img' }
      },
      data: {
        details: {
          reasonCode,
          ariaLabel: ariaLabelRaw === null ? null : ariaLabel,
          ariaLabelledby: ariaLabelledbyRaw === null ? null : ariaLabelledby,
          accessibleNameInfo: nameInfo || null
        },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "scope-attr-valid": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const VALID_SCOPES = new Set(['row', 'col', 'rowgroup', 'colgroup']);

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[scope]')
    : helpers.queryAll('[scope]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const raw = String(el.getAttribute('scope') || '').trim();
    if (!raw) continue;

    applicableCount += 1;

    if (VALID_SCOPES.has(raw.toLowerCase())) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This scope attribute value is not recognized.',
      hint: 'Use one of row, col, rowgroup, or colgroup for the scope attribute.',
      i18n: {
        summaryKey: 'scopeAttrValid_summary_cantTell',
        hintKey: 'scopeAttrValid_hint_cantTell',
        params: { value: raw }
      },
      data: {
        details: { reasonCode: 'SCOPE_ATTR_INVALID', value: raw }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "scrollable-region-focusable": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function safeComputedStyle(el) {
    try {
      if (!el || el.nodeType !== 1) return null;
      if (helpers && typeof helpers.computedStyle === 'function') {
        const cs = helpers.computedStyle(el);
        if (cs) return cs;
      }
      const view =
        el.ownerDocument && el.ownerDocument.defaultView ? el.ownerDocument.defaultView : null;
      if (view && typeof view.getComputedStyle === 'function') return view.getComputedStyle(el);
    } catch {}
    return null;
  }

  function isScrollableOverflow(cs) {
    if (!cs) return false;
    const x = String(cs.overflowX || '')
      .trim()
      .toLowerCase();
    const y = String(cs.overflowY || '')
      .trim()
      .toLowerCase();
    return x === 'auto' || x === 'scroll' || y === 'auto' || y === 'scroll';
  }

  function isSelfFocusable(el) {
    try {
      const tabindexAttr = el.getAttribute ? el.getAttribute('tabindex') : null;
      if (tabindexAttr != null) {
        const n = Number.parseInt(tabindexAttr, 10);
        if (Number.isFinite(n) && n >= 0) return true;
      }
    } catch {}
    return false;
  }

  const FOCUSABLE_DESCENDANT_SELECTOR =
    'a[href], button, input, select, textarea, [tabindex], iframe, [contenteditable]:not([contenteditable="false"])';

  function hasFocusableDescendant(el) {
    try {
      return !!(el.querySelector && el.querySelector(FOCUSABLE_DESCENDANT_SELECTOR));
    } catch {
      return false;
    }
  }

  const CANDIDATE_SELECTOR =
    'div, section, article, aside, main, nav, pre, table, blockquote, ul, ol, textarea';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(CANDIDATE_SELECTOR)
    : helpers.queryAll(CANDIDATE_SELECTOR);

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const cs = safeComputedStyle(el);
    if (!isScrollableOverflow(cs)) continue;

    applicableCount += 1;

    if (isSelfFocusable(el)) continue;
    if (hasFocusableDescendant(el)) continue;

    const tag = (el.tagName || '').toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    const baseOccurrence = {
      selector: stableSelector,
      html,
      summary:
        'This element declares overflow:auto/scroll, has no focusable descendant, and is not itself keyboard-focusable.',
      hint: 'If this region’s content actually overflows, add tabindex="0" (and a suitable label) so keyboard users can focus it and scroll with the arrow keys.',
      i18n: {
        summaryKey: 'scrollableRegionFocusable_summary_cantTell',
        hintKey: 'scrollableRegionFocusable_hint_cantTell',
        params: { element: tag }
      },
      data: {
        details: { reasonCode: 'SCROLLABLE_OVERFLOW_NOT_FOCUSABLE', element: tag }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push(baseOccurrence);
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }

  // Manual rules may only emit cantTell/notApplicable (never pass/fail):
  // every scrollable-overflow candidate is already reachable by keyboard.
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "searchbox-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  // A <label> contributes a name via its own aria-label/aria-labelledby
  // (checked first, same ARIA-over-content precedence any element's
  // accessible name gives — e.g. <label aria-label="Search"><svg
  // aria-hidden="true">...</svg></label> names its control "Search" even
  // though the label's only child content is aria-hidden) or, failing
  // that, its rendered content (getConservativeSubtreeText).
  function getLabelText(lab) {
    if (helpers.getAriaNameInfo) {
      try {
        const aria = helpers.getAriaNameInfo(lab, ctx);
        if (aria && aria.present && aria.value) return normalizeWs(aria.value);
      } catch {}
    }
    const content = getConservativeSubtreeText(document, lab);
    if (content) return content;
    // Final fallback per the general accname text-alternative algorithm,
    // which applies to any element being asked for its name regardless of
    // why (own aria-label, an aria-labelledby reference, or — here — native
    // <label for> association): title, when nothing else yields a name.
    // Purely additive (only fills in a name where there was none before),
    // so it carries no false-positive risk — see dialog-name-present.js's
    // identical <iframe>-title-fallback fix for the concrete real-world
    // trigger this same accname step covers elsewhere.
    return getAttr(lab, 'title');
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  function buildLabelForMap(doc) {
    const map = new Map(); // id -> label element (first)
    try {
      const labels = doc && doc.getElementsByTagName ? doc.getElementsByTagName('label') : [];
      for (let i = 0; i < labels.length; i += 1) {
        const lab = labels[i];
        if (!lab || !lab.getAttribute) continue;
        const f = normalizeWs(lab.getAttribute('for'));
        if (!f) continue;
        if (!map.has(f)) map.set(f, lab);
      }
    } catch {}
    return map;
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="searchbox"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  // Precompute label[for] map for searchbox elements that are labelable
  // native form controls (e.g. <input role="searchbox">).
  const labelForMap = buildLabelForMap(document);

  function getNativeLabelText(el) {
    try {
      if ('labels' in el && el.labels && el.labels.length) {
        const parts = [];
        const max = Math.min(4, el.labels.length);
        for (let i = 0; i < max; i += 1) {
          const lab = el.labels[i];
          const t = lab ? getLabelText(lab) : '';
          if (t) parts.push(t);
        }
        const joined = normalizeWs(parts.join(' '));
        if (joined) return joined;
      }
    } catch {}
    try {
      if (el.closest) {
        const wrap = el.closest('label');
        if (wrap) {
          const t = getLabelText(wrap);
          if (t) return t;
        }
      }
    } catch {}
    try {
      const idAttr = getAttr(el, 'id');
      if (idAttr && labelForMap.has(idAttr)) {
        const lab = labelForMap.get(idAttr);
        const t = lab ? getLabelText(lab) : '';
        if (t) return t;
      }
    } catch {}
    return '';
  }

  function hasName(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const labelled = resolveAriaLabelledbyText(document, el, 8);
    if (labelled) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // Native <label> association (e.g. <input role="searchbox">).
    const lab = getNativeLabelText(el);
    if (lab) return { ok: true, method: 'label' };

    // role="searchbox" is name-from-author-only per WAI-ARIA: it must NOT
    // fall back to subtree content.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    applicableCount += 1;

    const res = hasName(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This element has no accessible name.',
      hint: "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this searchbox's accessible name.",
      i18n: {
        summaryKey: 'searchboxNamePresent_summary_fail',
        hintKey: 'searchboxNamePresent_hint_fail',
        params: { controlType: 'searchbox' }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'searchbox', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "server-side-image-map-absent": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('img[ismap]')
    : helpers.queryAll('img[ismap]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el) continue;

    applicableCount += 1;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary:
        'This image uses a server-side image map, which has no keyboard-operable equivalent.',
      hint: 'Replace the server-side image map (ismap) with a client-side image map (<map>/<area>) or separate accessible links/buttons.',
      i18n: {
        summaryKey: 'serverSideImageMapAbsent_summary_fail',
        hintKey: 'serverSideImageMapAbsent_hint_fail',
        params: {}
      },
      data: {
        details: { reasonCode: 'SERVER_SIDE_IMAGE_MAP' }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "skip-link": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAccessibleNameText(el) {
    const al = normalizeWs(el.getAttribute && el.getAttribute('aria-label'));
    if (al) return al;
    const alb = normalizeWs(el.getAttribute && el.getAttribute('aria-labelledby'));
    if (alb) {
      const parts = [];
      for (const refId of alb.split(/\s+/).filter(Boolean)) {
        try {
          const ref = document.getElementById(refId);
          if (ref) {
            const t = normalizeWs(ref.textContent);
            if (t) parts.push(t);
          }
        } catch {}
      }
      const joined = normalizeWs(parts.join(' '));
      if (joined) return joined;
    }
    return normalizeWs(el.textContent);
  }

  function hasReliableGeometrySupport() {
    const probe = document.documentElement || document.body || null;
    if (!probe || !probe.getClientRects || !probe.getBoundingClientRect) return false;
    try {
      const rects = probe.getClientRects();
      const rectCount = rects ? rects.length : 0;
      const r = probe.getBoundingClientRect();
      const w = r && Number.isFinite(r.width) ? r.width : 0;
      const h = r && Number.isFinite(r.height) ? r.height : 0;
      return rectCount > 0 && (w > 0 || h > 0);
    } catch {
      return false;
    }
  }

  function toEligibility(info) {
    return {
      eligible: !!(info && info.eligible),
      reasons: info && Array.isArray(info.reasons) ? info.reasons.slice(0) : []
    };
  }

  const geometrySupported = hasReliableGeometrySupport();

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('a[href]')
    : helpers.queryAll('a[href]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const href = String(el.getAttribute('href') || '').trim();
    if (href.length < 2 || href.charAt(0) !== '#') continue;

    const name = getAccessibleNameText(el);
    if (!/skip/i.test(name) && !/jump\s*to/i.test(name)) continue;

    applicableCount += 1;

    let fragment = href.slice(1);
    try {
      fragment = decodeURIComponent(fragment);
    } catch {}
    fragment = fragment.trim();

    let target = null;
    if (fragment) {
      try {
        target = document.getElementById(fragment);
      } catch {
        target = null;
      }
      if (!target) {
        try {
          target = document.querySelector('a[name="' + fragment.replace(/"/g, '\\"') + '"]');
        } catch {
          target = null;
        }
      }
    }

    if (target) {
      const accEligibility = toEligibility(
        helpers.getEligibilityInfo
          ? helpers.getEligibilityInfo(target, ctx, { targetSet: 'acc' })
          : helpers.isAccTreeEligible
            ? helpers.isAccTreeEligible(target, ctx)
            : { eligible: true, reasons: [] }
      );

      let geometryEligibility = null;
      let geometryReasonCode = null;
      if (geometrySupported && helpers.isDomVisibleEligible) {
        geometryEligibility = toEligibility(
          helpers.isDomVisibleEligible(target, ctx, {
            visibilityMode: 'styleAndGeometry',
            ignoreOpacity: true
          })
        );
        if (
          !geometryEligibility.eligible &&
          geometryEligibility.reasons.includes('noClientRects')
        ) {
          geometryReasonCode = 'NO_CLIENT_RECTS';
        } else if (
          !geometryEligibility.eligible &&
          geometryEligibility.reasons.includes('zeroArea')
        ) {
          geometryReasonCode = 'ZERO_AREA_TARGET';
        }
      }

      const unusableByAcc = !accEligibility.eligible;
      const unusableByGeometry = !!geometryReasonCode;
      if (!unusableByAcc && !unusableByGeometry) continue;

      const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(el)
        : el.outerHTML || '';

      occurrences.push({
        selector: stableSelector,
        html,
        summary: 'This skip link points to a target that exists but is not currently usable.',
        hint: 'Point this skip link to a target that is exposed and usable as a navigation destination.',
        i18n: {
          summaryKey: 'skipLink_summary_unusableTarget_cantTell',
          hintKey: 'skipLink_hint_unusableTarget_cantTell',
          params: { href }
        },
        data: {
          details: {
            reasonCode: 'SKIP_LINK_TARGET_UNUSABLE',
            href,
            unusableReasonCode: unusableByAcc ? 'ACC_TREE_INELIGIBLE' : geometryReasonCode,
            targetSelector: helpers.buildSelector ? helpers.buildSelector(target) : null,
            geometryCheckEnabled: geometrySupported
          },
          visibilityFilter: {
            targetSet: 'acc',
            accEligible: accEligibility.eligible,
            reasons: accEligibility.reasons
          },
          targetGeometry: geometryEligibility
            ? { eligible: geometryEligibility.eligible, reasons: geometryEligibility.reasons }
            : { eligible: null, reasons: [] }
        }
      });
      continue;
    }

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: "This skip link's target does not exist.",
      hint: "Point the skip link's href at an id that exists in the document, or add the missing target element.",
      i18n: {
        summaryKey: 'skipLink_summary_cantTell',
        hintKey: 'skipLink_hint_cantTell',
        params: { href }
      },
      data: {
        details: { reasonCode: 'SKIP_LINK_TARGET_MISSING', href }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "slider-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function buildLabelForMap(doc) {
    const map = new Map(); // id -> label element (first)
    try {
      const labels = doc && doc.getElementsByTagName ? doc.getElementsByTagName('label') : [];
      for (let i = 0; i < labels.length; i += 1) {
        const lab = labels[i];
        if (!lab || !lab.getAttribute) continue;
        const f = normalizeWs(lab.getAttribute('for'));
        if (!f) continue;
        if (!map.has(f)) map.set(f, lab);
      }
    } catch {}
    return map;
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  // A <label> contributes a name via its own aria-label/aria-labelledby
  // (checked first, same ARIA-over-content precedence any element's
  // accessible name gives — e.g. <label aria-label="Search"><svg
  // aria-hidden="true">...</svg></label> names its control "Search" even
  // though the label's only child content is aria-hidden) or, failing
  // that, its rendered content (getConservativeSubtreeText).
  function getLabelText(lab) {
    if (helpers.getAriaNameInfo) {
      try {
        const aria = helpers.getAriaNameInfo(lab, ctx);
        if (aria && aria.present && aria.value) return normalizeWs(aria.value);
      } catch {}
    }
    const content = getConservativeSubtreeText(document, lab);
    if (content) return content;
    // Final fallback per the general accname text-alternative algorithm,
    // which applies to any element being asked for its name regardless of
    // why (own aria-label, an aria-labelledby reference, or — here — native
    // <label for> association): title, when nothing else yields a name.
    // Purely additive (only fills in a name where there was none before),
    // so it carries no false-positive risk — see dialog-name-present.js's
    // identical <iframe>-title-fallback fix for the concrete real-world
    // trigger this same accname step covers elsewhere.
    return getAttr(lab, 'title');
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  // Native input[type=range] belongs to
  // form-control-programmatic-label-present.
  const selector = '[role="slider"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  // Precompute label[for] map for native range inputs.
  const labelForMap = buildLabelForMap(document);

  function getNativeLabelText(el) {
    try {
      if ('labels' in el && el.labels && el.labels.length) {
        const parts = [];
        const max = Math.min(4, el.labels.length);
        for (let i = 0; i < max; i += 1) {
          const lab = el.labels[i];
          const t = lab ? getLabelText(lab) : '';
          if (t) parts.push(t);
        }
        const joined = normalizeWs(parts.join(' '));
        if (joined) return joined;
      }
    } catch {}
    try {
      if (el.closest) {
        const wrap = el.closest('label');
        if (wrap) {
          const t = getLabelText(wrap);
          if (t) return t;
        }
      }
    } catch {}
    try {
      const idAttr = getAttr(el, 'id');
      if (idAttr && labelForMap.has(idAttr)) {
        const lab = labelForMap.get(idAttr);
        const t = lab ? getLabelText(lab) : '';
        if (t) return t;
      }
    } catch {}
    return '';
  }

  function evaluate(el, kind) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    if (kind === 'native-slider') {
      const lab = getNativeLabelText(el);
      if (lab) return { ok: true, method: 'label' };
      return { ok: false, method: 'none' };
    }

    // role="slider" is name-from-author-only per WAI-ARIA (unlike e.g.
    // role="option"/"tab"), so it must NOT fall back to subtree content —
    // visible text near/inside a custom slider widget is not reliably
    // exposed as its accessible name by assistive technologies.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    const tag = (el.tagName || '').toLowerCase();
    const type = getAttr(el, 'type').toLowerCase();
    const role = getAttr(el, 'role').toLowerCase();

    let kind;
    if (tag === 'input' && type === 'range') kind = 'native-slider';
    else if (role === 'slider') kind = 'aria-slider';
    else continue;

    applicableCount += 1;

    const res = evaluate(el, kind);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This slider has no accessible name.',
      hint: 'Provide a label, aria-label, or aria-labelledby so assistive technologies can identify the slider.',
      i18n: {
        summaryKey: 'sliderNamePresent_summary_fail',
        hintKey: 'sliderNamePresent_hint_fail',
        params: { kind }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: kind, methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "spinbutton-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  // A <label> contributes a name via its own aria-label/aria-labelledby
  // (checked first, same ARIA-over-content precedence any element's
  // accessible name gives — e.g. <label aria-label="Search"><svg
  // aria-hidden="true">...</svg></label> names its control "Search" even
  // though the label's only child content is aria-hidden) or, failing
  // that, its rendered content (getConservativeSubtreeText).
  function getLabelText(lab) {
    if (helpers.getAriaNameInfo) {
      try {
        const aria = helpers.getAriaNameInfo(lab, ctx);
        if (aria && aria.present && aria.value) return normalizeWs(aria.value);
      } catch {}
    }
    const content = getConservativeSubtreeText(document, lab);
    if (content) return content;
    // Final fallback per the general accname text-alternative algorithm,
    // which applies to any element being asked for its name regardless of
    // why (own aria-label, an aria-labelledby reference, or — here — native
    // <label for> association): title, when nothing else yields a name.
    // Purely additive (only fills in a name where there was none before),
    // so it carries no false-positive risk — see dialog-name-present.js's
    // identical <iframe>-title-fallback fix for the concrete real-world
    // trigger this same accname step covers elsewhere.
    return getAttr(lab, 'title');
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  function buildLabelForMap(doc) {
    const map = new Map(); // id -> label element (first)
    try {
      const labels = doc && doc.getElementsByTagName ? doc.getElementsByTagName('label') : [];
      for (let i = 0; i < labels.length; i += 1) {
        const lab = labels[i];
        if (!lab || !lab.getAttribute) continue;
        const f = normalizeWs(lab.getAttribute('for'));
        if (!f) continue;
        if (!map.has(f)) map.set(f, lab);
      }
    } catch {}
    return map;
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="spinbutton"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  // Precompute label[for] map for spinbutton elements that are labelable
  // native form controls (e.g. <input role="spinbutton">).
  const labelForMap = buildLabelForMap(document);

  function getNativeLabelText(el) {
    try {
      if ('labels' in el && el.labels && el.labels.length) {
        const parts = [];
        const max = Math.min(4, el.labels.length);
        for (let i = 0; i < max; i += 1) {
          const lab = el.labels[i];
          const t = lab ? getLabelText(lab) : '';
          if (t) parts.push(t);
        }
        const joined = normalizeWs(parts.join(' '));
        if (joined) return joined;
      }
    } catch {}
    try {
      if (el.closest) {
        const wrap = el.closest('label');
        if (wrap) {
          const t = getLabelText(wrap);
          if (t) return t;
        }
      }
    } catch {}
    try {
      const idAttr = getAttr(el, 'id');
      if (idAttr && labelForMap.has(idAttr)) {
        const lab = labelForMap.get(idAttr);
        const t = lab ? getLabelText(lab) : '';
        if (t) return t;
      }
    } catch {}
    return '';
  }

  function hasName(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const labelled = resolveAriaLabelledbyText(document, el, 8);
    if (labelled) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // Native <label> association (e.g. <input role="spinbutton">).
    const lab = getNativeLabelText(el);
    if (lab) return { ok: true, method: 'label' };

    // role="spinbutton" is name-from-author-only per WAI-ARIA: it must NOT
    // fall back to subtree content.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    applicableCount += 1;

    const res = hasName(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This element has no accessible name.',
      hint: "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this spinbutton's accessible name.",
      i18n: {
        summaryKey: 'spinbuttonNamePresent_summary_fail',
        hintKey: 'spinbuttonNamePresent_hint_fail',
        params: { controlType: 'spinbutton' }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'spinbutton', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "summary-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('summary')
    : helpers.queryAll('summary');

  function evaluate(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    const t = getConservativeSubtreeText(document, el);
    if (t) return { ok: true, method: 'content' };

    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    applicableCount += 1;

    const res = evaluate(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This summary has no accessible name.',
      hint: 'Provide summary text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.',
      i18n: {
        summaryKey: 'summaryNamePresent_summary_fail',
        hintKey: 'summaryNamePresent_hint_fail',
        params: {}
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'summary', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "svg-image-text-alternative-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const isFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  const getAccessibleNameInfo =
    helpers && typeof helpers.getAccessibleNameInfo === 'function'
      ? helpers.getAccessibleNameInfo
      : null;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  // Per SVG accessible-name conventions, only a <title> that is literally
  // the first child element is used by assistive technologies as the
  // element's accessible name; a <title> appearing later among the
  // children is commonly ignored by AT even though it's still a valid DOM
  // child.
  function firstChildTitleText(el) {
    try {
      if (!el) return '';
      const first = el.firstElementChild;
      const tn = first ? (first.localName || first.tagName || '').toLowerCase() : '';
      if (tn === 'title') {
        const t = trim(first.textContent);
        if (t) return t;
      }
    } catch {}
    return '';
  }

  // <desc> counts when it is the first child, or the second child
  // immediately following a <title> — the standard <title>+<desc> pairing.
  // A <desc> appearing later than that is not reliably read by AT.
  function descText(el) {
    try {
      if (!el) return '';
      const first = el.firstElementChild;
      const firstTag = first ? (first.localName || first.tagName || '').toLowerCase() : '';
      if (firstTag === 'desc') {
        const t = trim(first.textContent);
        if (t) return t;
      } else if (firstTag === 'title' && first.nextElementSibling) {
        const second = first.nextElementSibling;
        const secondTag = (second.localName || second.tagName || '').toLowerCase();
        if (secondTag === 'desc') {
          const t = trim(second.textContent);
          if (t) return t;
        }
      }
    } catch {}
    return '';
  }

  const rawImages = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('image') : queryAll('image')) || []);
    } catch {
      return queryAll('svg image');
    }
  })();

  const images = rawImages.filter((el) => {
    try {
      return (
        el &&
        el.namespaceURI === 'http://www.w3.org/2000/svg' &&
        String(el.localName).toLowerCase() === 'image'
      );
    } catch {
      return false;
    }
  });

  if (!images.length)
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };

  const occurrences = [];
  let applicableCount = 0;

  for (const el of images) {
    if (!el || !el.getAttribute) continue;

    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    const role = trim(el.getAttribute('role')).toLowerCase();
    if (role === 'presentation' || role === 'none') {
      let focusable;
      if (isFocusableInfo) {
        const fi = (() => {
          try {
            return isFocusableInfo(el, ctx);
          } catch {
            return null;
          }
        })();
        focusable = !!(fi && fi.focusable);
      } else {
        const tabindex = el.getAttribute('tabindex');
        focusable =
          tabindex != null && trim(tabindex) !== '' && !Number.isNaN(Number(trim(tabindex)));
      }
      if (!focusable) continue;
    }

    applicableCount += 1;

    const titleText = firstChildTitleText(el);
    if (titleText) continue;

    const desc = descText(el);
    if (desc) continue;

    // Only now check “accessible name” (but scoped to allowed mechanisms)
    const ariaLabelRaw = (() => {
      try {
        return el.getAttribute('aria-label');
      } catch {
        return null;
      }
    })();
    const ariaLabelledbyRaw = (() => {
      try {
        return el.getAttribute('aria-labelledby');
      } catch {
        return null;
      }
    })();
    const titleAttrRaw = (() => {
      try {
        return el.getAttribute('title');
      } catch {
        return null;
      }
    })();

    const ariaLabel = trim(ariaLabelRaw);
    const ariaLabelledby = trim(ariaLabelledbyRaw);
    const titleAttr = trim(titleAttrRaw);

    const hasMechanism =
      (ariaLabelRaw !== null && ariaLabel.length > 0) ||
      (ariaLabelledbyRaw !== null && ariaLabelledby.length > 0) ||
      (titleAttrRaw !== null && titleAttr.length > 0);

    let nameInfo;
    let hasName = false;

    if (hasMechanism) {
      if (getAccessibleNameInfo) {
        nameInfo = (() => {
          try {
            return getAccessibleNameInfo(el, ctx);
          } catch {
            return null;
          }
        })();
        hasName = !!(nameInfo && nameInfo.present && trim(nameInfo.value));
      } else {
        // Without helper, accept presence of non-empty allowed attributes
        hasName = true;
      }
    }

    if (hasName) continue;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Missing text alternative on SVG <image>.',
      hint: 'Add a <title> (and optionally <desc>) inside <image>, or provide aria-label/aria-labelledby.',
      i18n: {
        summaryKey: 'svgImage_textAltPresent_summary_fail',
        hintKey: 'svgImage_textAltPresent_hint_fail',
        params: { element: 'image' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0)
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  if (!occurrences.length)
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "svg-text-alternative-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;

  function trim(v) {
    try {
      return (v == null ? '' : String(v)).trim();
    } catch {
      return '';
    }
  }

  // Per SVG accessible-name conventions, only a <title> that is literally
  // the first child element is used by assistive technologies as the
  // SVG's accessible name; a <title> appearing later among the children
  // is commonly ignored by AT even though it's still a valid DOM child.
  function nonEmptyFirstChildTitleText(svg) {
    try {
      const first = svg.firstElementChild;
      const tn = first ? (first.localName || first.tagName || '').toLowerCase() : '';
      if (tn === 'title') {
        const txt = trim(first.textContent);
        if (txt) return txt;
      }
    } catch {}
    return '';
  }

  // <desc> counts when it is the first child, or the second child
  // immediately following a <title> — the standard <title>+<desc> pairing
  // (e.g. <svg><title>...</title><desc>...</desc>...</svg>). A <desc>
  // appearing later than that is not reliably read by AT.
  function nonEmptyDescText(svg) {
    try {
      const first = svg.firstElementChild;
      const firstTag = first ? (first.localName || first.tagName || '').toLowerCase() : '';
      if (firstTag === 'desc') {
        const txt = trim(first.textContent);
        if (txt) return txt;
      } else if (firstTag === 'title' && first.nextElementSibling) {
        const second = first.nextElementSibling;
        const secondTag = (second.localName || second.tagName || '').toLowerCase();
        if (secondTag === 'desc') {
          const txt = trim(second.textContent);
          if (txt) return txt;
        }
      }
    } catch {}
    return '';
  }

  function isFocusable(svg) {
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(svg, ctx);
        } catch {
          return null;
        }
      })();
      return !!(fi && fi.focusable);
    }
    // deterministic fallback: tabindex presence/valid number
    try {
      const tabindex = svg && svg.getAttribute ? svg.getAttribute('tabindex') : null;
      return (
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()))
      );
    } catch {
      return false;
    }
  }

  const svgs = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('svg') : queryAll('svg')) || []);
    } catch {
      return queryAll('svg');
    }
  })();

  if (!svgs.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  for (const el of svgs) {
    if (!el || !el.getAttribute) continue;

    // Applicability step 1: only acc-tree eligible nodes (with helper exceptions)
    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    // Applicability step 2: role (presentation/none) exclusion only when not focusable
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();

    const focusable = isFocusable(el);

    if (role === 'presentation' || role === 'none') {
      if (!focusable) continue;
    }

    // Applicability step 3: intent signal gating (computed once per element)
    let hasAriaNamingAttr = false;
    try {
      hasAriaNamingAttr =
        el.getAttribute('aria-label') != null || el.getAttribute('aria-labelledby') != null;
    } catch {}

    const titleText = nonEmptyFirstChildTitleText(el);
    const descText = titleText ? '' : nonEmptyDescText(el); // avoid second scan if title already passes
    const hasTitleOrDesc = !!(titleText || descText);

    const hasIntent =
      role === 'img' ||
      role === 'graphics-symbol' ||
      role === 'graphics-document' ||
      hasAriaNamingAttr ||
      hasTitleOrDesc ||
      focusable;

    if (!hasIntent) continue;

    applicableCount += 1;

    // Expectation: non-empty title/desc OR ARIA name (but only resolve name if attrs exist)
    let hasAriaName = false;
    if (hasAriaNamingAttr) {
      if (getAriaNameInfo) {
        const info = (() => {
          try {
            return getAriaNameInfo(el, ctx);
          } catch {
            return null;
          }
        })();
        hasAriaName = !!(info && info.present && trim(info.value));
      } else {
        // minimal deterministic fallback
        const ariaLabel = trim(
          (() => {
            try {
              return el.getAttribute('aria-label');
            } catch {
              return '';
            }
          })()
        );
        const ariaLabelledby = trim(
          (() => {
            try {
              return el.getAttribute('aria-labelledby');
            } catch {
              return '';
            }
          })()
        );
        hasAriaName = !!(ariaLabel || ariaLabelledby);
      }
    }

    // Per SVG-AAM §7.1: <desc> contributes only to the accessible
    // DESCRIPTION, never the accessible NAME — so descText does not
    // count here even though it does count toward applicability above.
    const ok = !!titleText || hasAriaName;
    if (ok) continue;

    let eligInfo = null;
    if (getEligibilityInfo) {
      try {
        eligInfo = getEligibilityInfo(el, ctx, { targetSet: 'acc' });
      } catch {
        eligInfo = null;
      }
    }

    const baseOccurrence = {
      summary: 'Missing text alternative for <svg>.',
      hint: 'Provide a <title> element with text, or an ARIA name (aria-label/aria-labelledby) — a <desc> element alone does not provide an accessible name.',
      i18n: {
        summaryKey: 'svg_textAltPresent_summary_fail',
        hintKey: 'svg_textAltPresent_hint_fail',
        params: { element: 'svg' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (!occurrences.length) {
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null },
    "svg-text-alternative-quality": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll =
    helpers && typeof helpers.queryAll === 'function'
      ? helpers.queryAll
      : (sel) => {
          try {
            return safeRoot && safeRoot.querySelectorAll
              ? Array.from(safeRoot.querySelectorAll(sel))
              : [];
          } catch {
            return [];
          }
        };

  const buildSelector =
    helpers && typeof helpers.buildSelector === 'function'
      ? helpers.buildSelector
      : (el) => {
          try {
            if (!el || !el.tagName) return 'html';
            const tag = (el.tagName || 'html').toLowerCase();
            return el.id ? `${tag}#${el.id}` : tag;
          } catch {
            return 'html';
          }
        };

  const getOuterHtmlSnippet =
    helpers && typeof helpers.getOuterHtmlSnippet === 'function'
      ? helpers.getOuterHtmlSnippet
      : (el) => {
          try {
            return el && el.outerHTML ? String(el.outerHTML).slice(0, 2000) : '';
          } catch {
            return '';
          }
        };

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const getFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  function isRolePresentationExcluded(el) {
    const role = (() => {
      try {
        return String(el.getAttribute('role') || '')
          .trim()
          .toLowerCase();
      } catch {
        return '';
      }
    })();
    if (role !== 'presentation' && role !== 'none') return false;

    // Exclude only when NOT focusable (mirrors img-alt-present policy)
    let focusable;
    if (getFocusableInfo) {
      const fi = (() => {
        try {
          return getFocusableInfo(el, ctx);
        } catch {
          return null;
        }
      })();
      focusable = !!(fi && fi.focusable);
    } else {
      const tabindex = el.getAttribute('tabindex');
      focusable =
        tabindex != null &&
        String(tabindex).trim() !== '' &&
        !Number.isNaN(Number(String(tabindex).trim()));
    }
    return !focusable;
  }

  const els = (() => {
    try {
      return Array.from((queryAllSmart ? queryAllSmart('svg') : queryAll('svg')) || []);
    } catch {
      return queryAll('svg');
    }
  })();

  if (!els.length) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const occurrences = [];
  let applicableCount = 0;

  const trim = (v) => (v == null ? '' : String(v)).trim();

  for (const el of els) {
    if (!el || !el.getAttribute) continue;

    // acc eligibility
    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    if (isRolePresentationExcluded(el)) continue;

    // Detect mechanisms once
    let titleText = '';
    let descText = '';
    let ariaLabel = '';
    let ariaLabelledBy = '';
    let labelledByText = '';

    try {
      const titleEl = el.querySelector ? el.querySelector('title') : null;
      const descEl = el.querySelector ? el.querySelector('desc') : null;
      titleText = trim(titleEl && titleEl.textContent);
      descText = trim(descEl && descEl.textContent);

      ariaLabel = trim(el.getAttribute('aria-label'));
      ariaLabelledBy = trim(el.getAttribute('aria-labelledby'));
    } catch {}

    if (
      !ariaLabel &&
      ariaLabelledBy &&
      helpers &&
      typeof helpers.getTextFromIdRefs === 'function'
    ) {
      try {
        const t = helpers.getTextFromIdRefs(ariaLabelledBy, ctx);
        labelledByText = trim(t && t.text);
      } catch {}
    }

    const hasNonEmptyTitle = !!titleText;
    const hasNonEmptyDesc = !!descText;
    const hasAriaLabel = !!ariaLabel;
    const hasResolvedLabelledBy = !!labelledByText;

    const hasMechanism =
      hasNonEmptyTitle || hasNonEmptyDesc || hasAriaLabel || hasResolvedLabelledBy;
    if (!hasMechanism) continue;

    applicableCount += 1;

    const eligInfo = getEligibilityInfo ? getEligibilityInfo(el, ctx, { targetSet: 'acc' }) : null;

    const baseOccurrence = {
      summary: 'Review text alternative for <svg> for accuracy and appropriateness.',
      hint: 'Confirm the <title>/<desc> or ARIA name conveys the meaning/purpose of the graphic in context.',
      i18n: {
        summaryKey: 'svg_textAltQuality_summary_cantTell',
        hintKey: 'svg_textAltQuality_hint_cantTell',
        params: { element: 'svg' }
      },
      data: {
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        details: {
          hasNonEmptyTitle,
          hasNonEmptyDesc,
          ariaLabel: ariaLabel || null,
          ariaLabelledBy: ariaLabelledBy || null,
          ariaLabelledByText: labelledByText ? labelledByText.slice(0, 120) : null // deterministic truncation
        }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      const selectorStr = (() => {
        try {
          return buildSelector(el);
        } catch {
          return 'html';
        }
      })();
      const html = getOuterHtmlSnippet(el);
      occurrences.push({ selector: selectorStr, html, ...baseOccurrence });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  return { ruleId: rule.ruleId, outcome: 'cantTell', severity: 'minor', occurrences };
}), applicability: null },
    "tab-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="tab"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function evaluate(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    const t = getConservativeSubtreeText(document, el);
    if (t) return { ok: true, method: 'content' };

    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    const role = getAttr(el, 'role').toLowerCase();
    if (role !== 'tab') continue;

    applicableCount += 1;

    const res = evaluate(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This tab has no accessible name.',
      hint: 'Provide tab text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.',
      i18n: {
        summaryKey: 'tabNamePresent_summary_fail',
        hintKey: 'tabNamePresent_hint_fail',
        params: {}
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'tab', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "tabindex": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[tabindex]')
    : helpers.queryAll('[tabindex]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const raw = String(el.getAttribute('tabindex') || '').trim();
    if (!raw) continue;
    const n = Number(raw);
    if (!Number.isInteger(n)) continue;

    applicableCount += 1;

    if (n <= 0) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This element has a positive tabindex, overriding the natural tab order.',
      hint: 'Use tabindex="0" (or a negative value to remove from tab order) instead of a positive number; fix the DOM order if a different tab order is needed.',
      i18n: {
        summaryKey: 'tabindex_summary_cantTell',
        hintKey: 'tabindex_hint_cantTell',
        params: { value: String(n) }
      },
      data: {
        details: { reasonCode: 'TABINDEX_POSITIVE', value: n }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "table-duplicate-name": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('table[summary]')
    : helpers.queryAll('table[summary]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    const summary = normalizeWs(el.getAttribute('summary'));
    if (!summary) continue;

    const captionEl = el.querySelector ? el.querySelector('caption') : null;
    const captionText = captionEl ? normalizeWs(captionEl.textContent) : '';
    if (!captionText) continue;

    applicableCount += 1;

    if (captionText.toLowerCase() !== summary.toLowerCase()) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: "This table's caption duplicates its summary attribute.",
      hint: 'Remove the redundant summary attribute, or make it provide different information than the caption.',
      i18n: {
        summaryKey: 'tableDuplicateName_summary_cantTell',
        hintKey: 'tableDuplicateName_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'TABLE_CAPTION_SUMMARY_DUPLICATE' }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "table-fake-caption": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isEligible(node) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(node, ctx);
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const nodes = helpers.queryAllSmart ? helpers.queryAllSmart('table') : helpers.queryAll('table');

  const occurrences = [];
  let applicableCount = 0;

  for (const table of nodes) {
    if (!table || !table.rows) continue;

    const hasCaption = !!(table.querySelector && table.querySelector('caption'));
    if (hasCaption) continue;

    // An aria-hidden row (or cell) isn't part of the AT-perceived table
    // structure at all -- it must not be treated as the table's "first
    // row" (or counted toward a row's cell count) for this positional
    // heuristic.
    const rows = Array.from(table.rows).filter(isEligible);
    if (rows.length < 2) continue;

    applicableCount += 1;

    const firstRow = rows[0];
    const firstRowCells = Array.from(firstRow.cells || []).filter(isEligible);
    if (firstRowCells.length !== 1) continue;

    const candidateCell = firstRowCells[0];
    const candidateText = trim(candidateCell.textContent || '');
    if (!candidateText) continue;

    const hasMultiCellRow = rows
      .slice(1)
      .some((r) => Array.from(r.cells || []).filter(isEligible).length > 1);
    if (!hasMultiCellRow) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(table) : 'html';
    const html = helpers.getOuterHtmlSnippet
      ? helpers.getOuterHtmlSnippet(table)
      : table.outerHTML || '';

    const baseOccurrence = {
      selector: stableSelector,
      html,
      summary:
        'This table has no <caption>, but its first row is a single cell sitting above multi-cell rows — it may be acting as a fake caption.',
      hint: 'If this cell is meant to describe the table, use a real <caption> element instead of a lone first-row cell.',
      i18n: {
        summaryKey: 'tableFakeCaption_summary_cantTell',
        hintKey: 'tableFakeCaption_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'SINGLE_CELL_FIRST_ROW_NO_CAPTION' }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(table, baseOccurrence));
    } else {
      occurrences.push(baseOccurrence);
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }

  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "table-headers-attr-valid": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('td[headers], th[headers]')
    : helpers.queryAll('td[headers], th[headers]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;

    const raw = String(el.getAttribute('headers') || '').trim();
    if (!raw) continue;
    const ids = raw.split(/\s+/).filter(Boolean);
    if (!ids.length) continue;

    applicableCount += 1;

    const table = el.closest ? el.closest('table') : null;
    const invalid = [];

    for (const headerId of ids) {
      let ref;
      try {
        ref = document.getElementById(headerId);
      } catch {
        ref = null;
      }

      if (!ref) {
        invalid.push({ id: headerId, reason: 'missing' });
        continue;
      }
      if (ref === el) {
        invalid.push({ id: headerId, reason: 'self-reference' });
        continue;
      }
      if (!ref.tagName || ref.tagName.toLowerCase() !== 'th') {
        invalid.push({ id: headerId, reason: 'not-a-th' });
        continue;
      }
      if (table && (!ref.closest || ref.closest('table') !== table)) {
        invalid.push({ id: headerId, reason: 'different-table' });
      }
    }

    if (!invalid.length) continue;

    const dedupedInvalidIds = [...new Set(invalid.map((i) => i.id))];

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This cell’s headers attribute references one or more invalid header cells.',
      hint: 'Update the headers attribute so every id refers to a <th> element within the same table.',
      i18n: {
        summaryKey: 'tableHeadersAttrValid_summary_fail',
        hintKey: 'tableHeadersAttrValid_hint_fail',
        params: { element: tag, invalidIds: dedupedInvalidIds.join(', ') }
      },
      data: {
        details: { reasonCode: 'TABLE_HEADERS_ATTR_INVALID', element: tag, invalid }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "table-th-has-data-cells": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const tables = helpers.queryAllSmart ? helpers.queryAllSmart('table') : helpers.queryAll('table');

  const occurrences = [];
  let applicableCount = 0;

  function explicitRole(el) {
    try {
      return String((el && el.getAttribute && el.getAttribute('role')) || '')
        .trim()
        .toLowerCase()
        .split(/\s+/)[0];
    } catch {
      return '';
    }
  }

  function isIncludedInTree(el) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      return typeof r === 'boolean' ? r : !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  // role="presentation"/"none" removes the table role, and with it every
  // header cell the role would have implied. A table is not focusable, so
  // presentational role conflict resolution does not restore it.
  function hasTableSemantics(table) {
    const role = explicitRole(table);
    if (role === 'presentation' || role === 'none') return false;
    if (role && role !== 'table' && role !== 'grid' && role !== 'treegrid') return false;
    return isIncludedInTree(table);
  }

  // A <th> only carries a rowheader/columnheader role while nothing overrides
  // it, and a hidden header describes nothing to a screen reader either way.
  function isHeaderCellInScope(th) {
    if (!th) return false;
    const role = explicitRole(th);
    if (role && role !== 'rowheader' && role !== 'columnheader') return false;
    if (helpers.isDomVisibleEligible) {
      try {
        if (!helpers.isDomVisibleEligible(th, ctx, { targetSet: 'dom' }).eligible) return false;
      } catch {
        // fall through to the tree check
      }
    }
    return isIncludedInTree(th);
  }

  for (const table of tables) {
    if (!table || !table.querySelectorAll) continue;

    // A table stripped of its semantics has no header cells to describe
    // anything, so nothing in it is in scope.
    if (!hasTableSemantics(table)) continue;

    let ths;
    try {
      ths = table.querySelectorAll('th');
    } catch {
      ths = [];
    }

    const headers = Array.from(ths).filter(isHeaderCellInScope);
    if (!headers.length) continue;

    applicableCount += 1;

    let hasDataCell;
    try {
      hasDataCell = table.querySelectorAll('td').length > 0;
    } catch {
      hasDataCell = false;
    }
    if (hasDataCell) continue;

    for (const th of headers) {
      if (!th) continue;
      const stableSelector = helpers.buildSelector ? helpers.buildSelector(th) : 'html';
      const html = helpers.getOuterHtmlSnippet
        ? helpers.getOuterHtmlSnippet(th)
        : th.outerHTML || '';

      occurrences.push({
        selector: stableSelector,
        html,
        summary: 'This table has header cells but no data cells for them to describe.',
        hint: 'Add data cells (<td>) to the table, or remove the header cells if the table has no data.',
        i18n: {
          summaryKey: 'tableThHasDataCells_summary_fail',
          hintKey: 'tableThHasDataCells_hint_fail',
          params: {}
        },
        data: {
          details: { reasonCode: 'TABLE_TH_NO_DATA_CELLS' }
        }
      });
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "target-size-minimum": { run: (function runInPage(ctx) {
  'use strict';

  const { document, helpers, rule } = ctx;

  const RULE_ID = (rule && rule.ruleId) || 'target-size-minimum';
  const MIN = 24;
  const RADIUS = MIN / 2;

  // --- tiny safe helpers (never throw) ---
  // Goes through helpers.queryAllSmart/queryAll (multi-root and shadow-DOM
  // aware, cached) rather than a raw root/safeRoot DOM query -- ctx.root is
  // an array (multi-region contextSelector support), not a single element
  // with its own .querySelectorAll to call directly.
  function qsa(sel) {
    try {
      if (helpers && typeof helpers.queryAllSmart === 'function')
        return Array.from(helpers.queryAllSmart(sel) || []);
      if (helpers && typeof helpers.queryAll === 'function')
        return Array.from(helpers.queryAll(sel) || []);
      return [];
    } catch {
      return [];
    }
  }

  function buildSelector(el) {
    try {
      if (helpers && typeof helpers.buildSelector === 'function') return helpers.buildSelector(el);
    } catch {}
    try {
      if (el && el.id) return `#${el.id}`;
    } catch {}
    return 'html';
  }

  function htmlSnippet(el) {
    try {
      if (helpers && typeof helpers.getOuterHtmlSnippet === 'function')
        return helpers.getOuterHtmlSnippet(el);
    } catch {}
    try {
      return el && el.outerHTML ? String(el.outerHTML) : '';
    } catch {}
    return '';
  }

  function isInlineTextExceptionTarget(el) {
    // SC 2.5.8 exception: target is in a sentence/block of text (inline)
    // Deterministic heuristic:
    // - must be a link-like target
    // - must be rendered as inline/inline-*
    // - must be inside a typical text container
    try {
      if (!el || el.nodeType !== 1) return false;

      const tag = (el.tagName || '').toLowerCase();
      const role =
        (el.getAttribute &&
          String(el.getAttribute('role') || '')
            .trim()
            .toLowerCase()) ||
        '';
      const isLinkLike =
        (tag === 'a' && el.getAttribute && el.getAttribute('href')) || role === 'link';
      if (!isLinkLike) return false;

      const cs = getStyle(el);
      const display = cs && cs.display ? String(cs.display) : '';
      if (!display) return false;

      // Inline exception is for inline text runs; many design systems use inline-block for links,
      // so treat inline-block variants as eligible for this exception.
      const isInline =
        display === 'inline' ||
        display === 'inline-block' ||
        display === 'inline-flex' ||
        display === 'inline-grid' ||
        display === 'inline-table';

      if (!isInline) return false;

      const t = (el.textContent || '').trim();
      if (!t) return false;

      // Require that it sits in a "text block" context
      const textContainer = closest(el, 'p, li, dd, dt, blockquote, figcaption, caption, td, th');
      if (textContainer) return true;

      // Fallback: sometimes links are inside spans within a paragraph-like region
      const inlineContainer = closest(el, 'span, em, strong, small, label');
      if (inlineContainer) {
        const outerTextBlock = closest(
          inlineContainer,
          'p, li, dd, dt, blockquote, figcaption, caption, td, th'
        );
        if (outerTextBlock) return true;
      }

      return false;
    } catch {
      return false;
    }
  }

  function getRects(el) {
    try {
      if (!el || typeof el.getClientRects !== 'function') return [];
      const r = el.getClientRects();
      return r ? Array.from(r) : [];
    } catch {
      return [];
    }
  }

  function getBcr(el) {
    try {
      if (!el || typeof el.getBoundingClientRect !== 'function') return null;
      return el.getBoundingClientRect();
    } catch {
      return null;
    }
  }

  function hasHiddenAttr(el) {
    try {
      return !!(el && el.hasAttribute && el.hasAttribute('hidden'));
    } catch {
      return false;
    }
  }

  function closest(el, sel) {
    try {
      return el && typeof el.closest === 'function' ? el.closest(sel) : null;
    } catch {
      return null;
    }
  }

  function inClosedDetails(el) {
    const det = closest(el, 'details');
    if (!det) return false;

    try {
      if (det.hasAttribute('open')) return false;

      const tag = el && el.tagName ? String(el.tagName).toLowerCase() : '';
      // summary remains operable even when <details> is closed
      if (tag === 'summary') return false;

      // everything else inside closed details is suppressed
      return true;
    } catch {
      return false;
    }
  }

  function inInertSubtree(el) {
    // Any ancestor with [inert] suppresses, including self.
    try {
      return !!closest(el, '[inert]');
    } catch {
      return false;
    }
  }

  function getStyle(el) {
    try {
      return document && document.defaultView && document.defaultView.getComputedStyle
        ? document.defaultView.getComputedStyle(el)
        : null;
    } catch {
      return null;
    }
  }

  function isPointerReachable(el) {
    // Match test expectations:
    // - exclude display:none
    // - exclude [hidden]
    // - exclude content-visibility:hidden
    // - exclude visibility hidden/collapse
    // - exclude pointer-events:none
    // - exclude inert subtree
    // - exclude elements inside closed details (except summary)
    // - exclude elements with no client rects
    // - DO NOT exclude aria-hidden or opacity:0
    if (!el || el.nodeType !== 1) return false;

    if (hasHiddenAttr(el)) return false;
    if (inInertSubtree(el)) return false;
    if (inClosedDetails(el)) return false;

    // Not operable => exclude
    try {
      if (typeof el.matches === 'function' && el.matches(':disabled')) return false;
    } catch {}

    // aria-disabled elements are typically treated as not operable
    try {
      const ad =
        el.getAttribute &&
        String(el.getAttribute('aria-disabled') || '')
          .trim()
          .toLowerCase();
      if (ad === 'true') return false;
    } catch {}

    const rects = getRects(el);
    if (!rects || rects.length === 0) return false;

    const cs = getStyle(el);
    const display = cs && cs.display ? String(cs.display) : 'block';
    const visibility = cs && cs.visibility ? String(cs.visibility) : 'visible';
    const contentVisibility = cs && cs.contentVisibility ? String(cs.contentVisibility) : 'visible';
    const pointerEvents = cs && cs.pointerEvents ? String(cs.pointerEvents) : 'auto';

    if (display === 'none') return false;
    if (visibility === 'hidden' || visibility === 'collapse') return false;
    if (contentVisibility === 'hidden') return false;
    if (pointerEvents === 'none') return false;

    return true;
  }

  function centerOfRect(r) {
    return { cx: r.left + r.width / 2, cy: r.top + r.height / 2 };
  }

  function dist(a, b) {
    const dx = a.cx - b.cx;
    const dy = a.cy - b.cy;
    return Math.sqrt(dx * dx + dy * dy);
  }

  function elementFromPoint(x, y) {
    try {
      if (document && typeof document.elementFromPoint === 'function')
        return document.elementFromPoint(x, y);
    } catch {}
    return null;
  }

  // Bidirectional containment check: true when `a` and `b` are the same
  // element, or either one is an ancestor of the other. A nested-interactive
  // pattern (e.g. a small <button> inside a wrapping <a href>, or vice
  // versa) is a single visual/interactive region, not two independently
  // placed targets — the spacing exception's "does the circle intersect
  // ANOTHER target" language is about separate targets, not an element and
  // its own container. (Nested interactive controls are their own,
  // separately-flagged anti-pattern — nested-interactive-controls-
  // absent — not a target-size spacing concern.)
  function isRelated(a, b) {
    try {
      if (!a || !b) return false;
      if (a === b) return true;
      if (typeof a.contains === 'function' && a.contains(b)) return true;
      if (typeof b.contains === 'function' && b.contains(a)) return true;
      return false;
    } catch {
      return false;
    }
  }

  const CANDIDATE_SELECTOR =
    'button, summary, a[href], area[href], input, select, textarea, [role="button"], [role="link"]';

  // --- candidate collection ---
  const candidates = qsa(CANDIDATE_SELECTOR);

  const applicable = [];
  for (const el of candidates) {
    if (isPointerReachable(el)) applicable.push(el);
  }

  if (applicable.length === 0) {
    return { ruleId: RULE_ID, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  // Precompute geometry for applicable elements.
  const items = [];
  for (const el of applicable) {
    const r = getBcr(el);
    if (!r) continue;
    // Guard against nonsense
    const w = Number(r.width);
    const h = Number(r.height);
    if (!Number.isFinite(w) || !Number.isFinite(h)) continue;
    if (w <= 0 || h <= 0) continue;

    items.push({
      el,
      rect: { left: r.left, top: r.top, width: w, height: h, right: r.left + w, bottom: r.top + h },
      center: centerOfRect({ left: r.left, top: r.top, width: w, height: h })
    });
  }

  if (items.length === 0) {
    // had “applicable” but no measurable geometry
    return { ruleId: RULE_ID, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  const undersized = items.filter((it) => it.rect.width < MIN || it.rect.height < MIN);

  // --- spacing/occlusion evaluation ---
  function hasSpacingConflict(target) {
    // 0) Pure geometry: deterministic center-distance check against ANY
    // nearby target, not just other undersized ones. Per WCAG 2.5.8, the
    // spacing exception depends on proximity to any adjacent target — an
    // undersized target sitting flush against an adequately-sized one still
    // fails the exception, which an undersized-only comparison would miss.
    for (const other of items) {
      if (!other || !other.el || isRelated(target.el, other.el)) continue;

      // Ignore inline-text exception targets when evaluating spacing conflicts.
      // (Inline links in text are exempt and should not invalidate spacing.)
      if (isInlineTextExceptionTarget(other.el)) continue;

      if (dist(target.center, other.center) < MIN) {
        return { conflict: true, confident: true, hitCount: 0, conflictEl: other.el };
      }
    }

    // 1) Perimeter sampling: reduce false positives from incidental overlaps/stacking.
    // Sample every point (no early exit) so the confidence banding below can
    // be based on the full hit count rather than stopping the instant a
    // threshold is crossed.
    const HIT_THRESHOLD = 3;
    // Comfortably above HIT_THRESHOLD: only a hit count this high is treated
    // as a confident conflict. Perimeter sampling is an approximation
    // (rounded corners, border-radius, and sub-pixel geometry can shift a
    // sample point in or out of a neighboring element), so a result that
    // merely reaches HIT_THRESHOLD is not asserted as a deterministic
    // fail — see the ambiguous band below.
    const CONFIDENT_THRESHOLD = 5;
    let hitCount = 0;
    let firstConflictEl = null;

    const STEPS = 16;
    for (let i = 0; i < STEPS; i++) {
      const ang = (Math.PI * 2 * i) / STEPS;
      const x = target.center.cx + RADIUS * Math.cos(ang);
      const y = target.center.cy + RADIUS * Math.sin(ang);

      const hit = elementFromPoint(x, y);
      if (!hit) continue;

      let hitCandidate = null;
      try {
        hitCandidate = hit.closest ? hit.closest(CANDIDATE_SELECTOR) : null;
      } catch {}

      if (!hitCandidate) continue;
      if (isRelated(hitCandidate, target.el)) continue;
      if (!isPointerReachable(hitCandidate)) continue;

      // If the thing we hit is an inline-text exception target, don't treat it as a spacing conflict.
      if (isInlineTextExceptionTarget(hitCandidate)) continue;

      hitCount++;
      if (!firstConflictEl) firstConflictEl = hitCandidate;
    }

    if (hitCount >= CONFIDENT_THRESHOLD) {
      return { conflict: true, confident: true, hitCount, conflictEl: firstConflictEl };
    }

    // Ambiguous band: close enough to HIT_THRESHOLD that sampling noise
    // could have tipped the result either way. Defer to manual review
    // instead of committing to pass or fail.
    if (hitCount >= HIT_THRESHOLD - 1) {
      return { conflict: false, confident: false, hitCount, conflictEl: firstConflictEl };
    }

    return { conflict: false, confident: true, hitCount, conflictEl: null };
  }

  // WCAG 2.5.8 "User Agent Control" exception: the target's size requirement
  // does not apply at all when its size is determined by the user agent and
  // not modified by the author — the canonical example being an unstyled
  // native checkbox/radio (browsers render these well under 24px by
  // default, and that's not the author's choice). Scoped narrowly to
  // input[type=checkbox]/[type=radio] specifically (the only form-control
  // types with a universally-recognized, unambiguous native rendering) —
  // deliberately not extended to select/range/color/file, whose "default"
  // sizing varies enough across browsers/OSes that a wrong exemption there
  // risks masking a real author-introduced undersized target.
  //
  // Detection signal: `appearance` (or the legacy `-webkit-appearance`)
  // computed as `none` is the near-universal first step of custom
  // checkbox/radio styling across every CSS framework/design system —
  // if the author hasn't reset it, the browser is still rendering its own
  // default control chrome, so the size is genuinely UA-determined.
  function isUserAgentSizedControl(el) {
    try {
      if (!el || el.nodeType !== 1) return false;
      const tag = (el.tagName || '').toLowerCase();
      if (tag !== 'input') return false;

      const type =
        (el.getAttribute &&
          String(el.getAttribute('type') || '')
            .trim()
            .toLowerCase()) ||
        '';
      if (type !== 'checkbox' && type !== 'radio') return false;

      const cs = getStyle(el);
      let appearance = '';
      try {
        if (cs && typeof cs.getPropertyValue === 'function') {
          appearance =
            cs.getPropertyValue('appearance') || cs.getPropertyValue('-webkit-appearance') || '';
        } else if (cs) {
          appearance = cs.appearance || cs.webkitAppearance || '';
        }
      } catch {}
      appearance = String(appearance).trim().toLowerCase();

      // Author has reset the native chrome => size is now author-controlled;
      // the exception no longer applies, evaluate normally.
      if (appearance === 'none') return false;

      return true;
    } catch {
      return false;
    }
  }

  function isPlausiblyEssentialOrEquivalent(el) {
    try {
      if (!el || el.nodeType !== 1) return false;

      const tag = (el.tagName || '').toLowerCase();

      // Image map targets are often constrained by the underlying image.
      // Currently unreachable in practice: <area> never becomes a
      // measurable candidate at all (see the file header's "Known,
      // deliberately unimplemented gap" note) — kept for forward
      // compatibility if that gap is closed later.
      if (tag === 'area') return true;

      // Graphics / spatial interaction regions are commonly essential by design.
      if (closest(el, 'svg, canvas, map')) return true;

      // Otherwise: do NOT guess "essential/equivalent" from layout containers.
      return false;
    } catch {
      return false; // conservative: don't mask failures as cantTell
    }
  }

  const failOccurrences = [];
  const cantTellOccurrences = [];

  for (const it of undersized) {
    // Inline-text exception: do not fail purely on size/spacing for inline links in text.
    if (isInlineTextExceptionTarget(it.el)) {
      continue; // pass by exception (no occurrence)
    }

    // User Agent Control exception: size isn't the author's choice, so the
    // size requirement (and therefore any spacing conflict stemming from
    // it) doesn't apply at all — skip straight to pass, no need to even
    // evaluate spacing.
    if (isUserAgentSizedControl(it.el)) {
      continue;
    }

    const info = hasSpacingConflict(it);

    if (!info.conflict && info.confident === false) {
      // Ambiguous perimeter-sampling result near the decision threshold —
      // previously recorded only as a page-level boolean with no per-target
      // occurrence at all, so this specific target was unrecoverable from
      // the result once any other target on the page had a confident
      // fail (see helpers.resolveTieredOutcome's header comment). Now
      // reported as its own cantTell-tier occurrence instead.
      cantTellOccurrences.push({
        selector: buildSelector(it.el),
        html: htmlSnippet(it.el),
        occurrenceOutcome: 'cantTell',
        summary:
          'Target may be too small and too close to another target, but the overlap is near the detection threshold and could not be confidently measured.',
        hint: 'Manually verify the effective spacing between this target and its neighbor; increase target size or spacing if the overlap is real.',
        i18n: {
          summaryKey: 'targetSizeMinimum_summary_cantTell_ambiguousSpacing',
          hintKey: 'targetSizeMinimum_hint_cantTell_ambiguousSpacing',
          params: {}
        },
        data: {
          details: {
            measured: { width: it.rect.width, height: it.rect.height },
            reasonCode: 'undersized-ambiguous-spacing',
            conflictHitCount: info.hitCount,
            conflictWith: info.conflictEl ? buildSelector(info.conflictEl) : null
          }
        }
      });
      continue;
    }

    if (info.conflict) {
      if (isPlausiblyEssentialOrEquivalent(it.el)) {
        // Confident spacing conflict, but the target may be exempt as part
        // of an essential graphic/image-map region — same "previously
        // unrecoverable" gap as above, now reported instead of dropped.
        cantTellOccurrences.push({
          selector: buildSelector(it.el),
          html: htmlSnippet(it.el),
          occurrenceOutcome: 'cantTell',
          summary:
            'Target is too small and too close to another target, but may be exempt as part of an essential graphic or image-map region.',
          hint: 'Verify whether this target’s size is genuinely essential to its function (e.g. part of an SVG/canvas/image map); if not, increase target size or spacing.',
          i18n: {
            summaryKey: 'targetSizeMinimum_summary_cantTell_plausiblyEssential',
            hintKey: 'targetSizeMinimum_hint_cantTell_plausiblyEssential',
            params: {}
          },
          data: {
            details: {
              measured: { width: it.rect.width, height: it.rect.height },
              reasonCode: 'undersized-plausibly-essential',
              conflictHitCount: info.hitCount,
              conflictWith: info.conflictEl ? buildSelector(info.conflictEl) : null
            }
          }
        });
        continue;
      }

      failOccurrences.push({
        selector: buildSelector(it.el),
        html: htmlSnippet(it.el),
        occurrenceOutcome: 'fail',
        summary: 'Target is too small and too close to another target.',
        hint: 'Increase target size to at least 24 by 24 CSS pixels, or add sufficient spacing.',
        i18n: {
          summaryKey: 'targetSizeMinimum_summary_fail',
          hintKey: 'targetSizeMinimum_hint_fail',
          params: {}
        },
        data: {
          details: {
            measured: { width: it.rect.width, height: it.rect.height },
            reasonCode: 'undersized-and-too-close',
            conflictHitCount: info.hitCount,
            conflictWith: info.conflictEl ? buildSelector(info.conflictEl) : null
          }
        }
      });
    }
  }

  // See helpers.resolveTieredOutcome's own header comment (src/core/dom-helpers.js):
  // a fail-tier finding never silently discards cantTell-tier findings from
  // the same run — both are returned together when the outcome is 'fail'.
  const resolved = helpers.resolveTieredOutcome(
    failOccurrences,
    cantTellOccurrences,
    (rule && rule.defaultSeverity) || 'minor'
  );
  return { ruleId: RULE_ID, ...resolved };
}), applicability: null },
    "td-has-header": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const MIN_SIZE = 4;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  function isEligible(node) {
    if (!isAccTreeEligible) return true;
    try {
      const r = isAccTreeEligible(node, ctx);
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const tables = helpers.queryAllSmart ? helpers.queryAllSmart('table') : helpers.queryAll('table');

  const occurrences = [];
  let applicableCount = 0;

  for (const table of tables) {
    if (!table || !table.rows) continue;

    const rows = Array.from(table.rows);
    if (rows.length < MIN_SIZE) continue;

    const rowCells = rows.map((r) => Array.from(r.cells || []));
    const maxCols = rowCells.reduce((m, cells) => Math.max(m, cells.length), 0);
    if (maxCols < MIN_SIZE) continue;

    const hasSpan = rowCells.some((cells) =>
      cells.some((c) => {
        const cs = Number.parseInt(c.getAttribute('colspan') || '1', 10);
        const rs = Number.parseInt(c.getAttribute('rowspan') || '1', 10);
        return (Number.isFinite(cs) && cs > 1) || (Number.isFinite(rs) && rs > 1);
      })
    );
    if (hasSpan) continue;

    applicableCount += 1;

    // An aria-hidden <th> is removed from the accessibility tree entirely
    // -- a screen reader never announces it, so it can't actually serve as
    // another cell's row/column header, even though it's still structurally
    // a <th>.
    function isHeaderCell(cell) {
      return !!(cell && cell.tagName && cell.tagName.toLowerCase() === 'th' && isEligible(cell));
    }

    function hasColumnHeaderAbove(r, c) {
      for (let ri = 0; ri < r; ri++) {
        const cell = rowCells[ri] && rowCells[ri][c];
        if (isHeaderCell(cell)) return true;
      }
      return false;
    }

    function hasRowHeaderBefore(r, c) {
      const cells = rowCells[r] || [];
      for (let ci = 0; ci < c; ci++) {
        if (isHeaderCell(cells[ci])) return true;
      }
      return false;
    }

    for (let r = 0; r < rowCells.length; r++) {
      const cells = rowCells[r];
      for (let c = 0; c < cells.length; c++) {
        const cell = cells[c];
        if (!cell || isHeaderCell(cell)) continue;

        // An aria-hidden data cell isn't exposed to AT either, so it has
        // no need for an accessible header association.
        if (!isEligible(cell)) continue;

        const headersAttr = trim(cell.getAttribute('headers'));
        if (headersAttr) continue;

        if (hasColumnHeaderAbove(r, c)) continue;
        if (hasRowHeaderBefore(r, c)) continue;

        const stableSelector = helpers.buildSelector ? helpers.buildSelector(cell) : 'html';
        const html = helpers.getOuterHtmlSnippet
          ? helpers.getOuterHtmlSnippet(cell)
          : cell.outerHTML || '';

        occurrences.push({
          selector: stableSelector,
          html,
          summary:
            'This data cell has no associated header (no headers attribute, no column <th> above it, no row <th> to its left).',
          hint: 'Add a headers attribute referencing the relevant <th> id(s), or restructure the table so this cell has an implicit row/column header.',
          i18n: {
            summaryKey: 'tdHasHeader_summary_fail',
            hintKey: 'tdHasHeader_hint_fail',
            params: { row: String(r), column: String(c) }
          },
          data: {
            details: { reasonCode: 'TD_NO_ASSOCIATED_HEADER', row: r, column: c }
          }
        });
      }
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'serious',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "textbox-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  // A <label> contributes a name via its own aria-label/aria-labelledby
  // (checked first, same ARIA-over-content precedence any element's
  // accessible name gives — e.g. <label aria-label="Search"><svg
  // aria-hidden="true">...</svg></label> names its control "Search" even
  // though the label's only child content is aria-hidden) or, failing
  // that, its rendered content (getConservativeSubtreeText).
  function getLabelText(lab) {
    if (helpers.getAriaNameInfo) {
      try {
        const aria = helpers.getAriaNameInfo(lab, ctx);
        if (aria && aria.present && aria.value) return normalizeWs(aria.value);
      } catch {}
    }
    const content = getConservativeSubtreeText(document, lab);
    if (content) return content;
    // Final fallback per the general accname text-alternative algorithm,
    // which applies to any element being asked for its name regardless of
    // why (own aria-label, an aria-labelledby reference, or — here — native
    // <label for> association): title, when nothing else yields a name.
    // Purely additive (only fills in a name where there was none before),
    // so it carries no false-positive risk — see dialog-name-present.js's
    // identical <iframe>-title-fallback fix for the concrete real-world
    // trigger this same accname step covers elsewhere.
    return getAttr(lab, 'title');
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  function buildLabelForMap(doc) {
    const map = new Map(); // id -> label element (first)
    try {
      const labels = doc && doc.getElementsByTagName ? doc.getElementsByTagName('label') : [];
      for (let i = 0; i < labels.length; i += 1) {
        const lab = labels[i];
        if (!lab || !lab.getAttribute) continue;
        const f = normalizeWs(lab.getAttribute('for'));
        if (!f) continue;
        if (!map.has(f)) map.set(f, lab);
      }
    } catch {}
    return map;
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="textbox"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  // Precompute label[for] map for textbox elements that are labelable
  // native form controls (e.g. <input role="textbox">).
  const labelForMap = buildLabelForMap(document);

  function getNativeLabelText(el) {
    try {
      if ('labels' in el && el.labels && el.labels.length) {
        const parts = [];
        const max = Math.min(4, el.labels.length);
        for (let i = 0; i < max; i += 1) {
          const lab = el.labels[i];
          const t = lab ? getLabelText(lab) : '';
          if (t) parts.push(t);
        }
        const joined = normalizeWs(parts.join(' '));
        if (joined) return joined;
      }
    } catch {}
    try {
      if (el.closest) {
        const wrap = el.closest('label');
        if (wrap) {
          const t = getLabelText(wrap);
          if (t) return t;
        }
      }
    } catch {}
    try {
      const idAttr = getAttr(el, 'id');
      if (idAttr && labelForMap.has(idAttr)) {
        const lab = labelForMap.get(idAttr);
        const t = lab ? getLabelText(lab) : '';
        if (t) return t;
      }
    } catch {}
    return '';
  }

  function hasName(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const labelled = resolveAriaLabelledbyText(document, el, 8);
    if (labelled) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    // Native <label> association (e.g. <input role="textbox">).
    const lab = getNativeLabelText(el);
    if (lab) return { ok: true, method: 'label' };

    // role="textbox" is name-from-author-only per WAI-ARIA: it must NOT
    // fall back to subtree content.
    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    applicableCount += 1;

    const res = hasName(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This element has no accessible name.',
      hint: "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this textbox's accessible name.",
      i18n: {
        summaryKey: 'textboxNamePresent_summary_fail',
        hintKey: 'textboxNamePresent_hint_fail',
        params: { controlType: 'textbox' }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'textbox', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "tooltip-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="tooltip"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function evaluate(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const ariaLabelledby = resolveAriaLabelledbyText(document, el, 8);
    if (ariaLabelledby) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    const t = getConservativeSubtreeText(document, el);
    if (t) return { ok: true, method: 'content' };

    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    const role = getAttr(el, 'role').toLowerCase();
    if (role !== 'tooltip') continue;

    applicableCount += 1;

    const res = evaluate(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This tooltip has no accessible name.',
      hint: 'Provide tooltip text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.',
      i18n: {
        summaryKey: 'tooltipNamePresent_summary_fail',
        hintKey: 'tooltipNamePresent_hint_fail',
        params: {}
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'tooltip', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "treeitem-name-present": { run: (function runInPage(ctx) {
  const { document, helpers, rule } = ctx;
  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  function normalizeWs(s) {
    return String(s || '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  function getAttr(el, name) {
    try {
      if (!el || !el.getAttribute) return '';
      return normalizeWs(el.getAttribute(name));
    } catch {
      return '';
    }
  }

  function getConservativeSubtreeText(document, container) {
    // "Name from content" — recurses into descendants and uses each one's
    // own accessible name (img alt, aria-label/aria-labelledby, title) when
    // it has one, not just literal text nodes. See getContentNameInfo's
    // header comment in src/core/dom-helpers.js for the full rationale
    // (this replaced a text-node-only TreeWalker that missed the common
    // "<a><img alt='...'></a>" logo-link / "<button><img alt='...'></button>"
    // icon-button pattern).
    if (helpers.getContentNameInfo) {
      const info = helpers.getContentNameInfo(container, ctx);
      return info && info.present ? info.value : '';
    }
    const t = container && container.textContent ? String(container.textContent) : '';
    return t.replace(/\s+/g, ' ').trim();
  }

  function resolveAriaLabelledbyText(document, el, maxRefs) {
    const raw = getAttr(el, 'aria-labelledby');
    if (!raw) return '';
    // Delegates to the shared getTextFromIdRefs helper instead of computing
    // name-from-content of the referenced element — see dialog-name-
    // present.js's identical fix for the full rationale (an <iframe>
    // aria-labelledby target's only name source is its title attribute,
    // which name-from-content alone can never see).
    if (helpers.getTextFromIdRefs) {
      try {
        const r = helpers.getTextFromIdRefs(raw, ctx, { maxRefs: maxRefs || 8 });
        return normalizeWs(r && r.text);
      } catch {}
    }
    return '';
  }

  // Naming rules apply only to elements included in the accessibility tree
  // (ACT c487ae and siblings), which excludes focusable aria-hidden content.
  // aria-hidden-focus (ACT 6cfa84) covers that markup instead.
  function isEligibleAcc(helpers, el, ctx) {
    const fn =
      helpers && typeof helpers.isIncludedInAccessibilityTree === 'function'
        ? helpers.isIncludedInAccessibilityTree
        : helpers && typeof helpers.isAccTreeEligible === 'function'
          ? helpers.isAccTreeEligible
          : null;
    if (!fn) return true;
    try {
      const r = fn(el, ctx);
      if (typeof r === 'boolean') return r;
      return !!(r && r.eligible);
    } catch {
      return true;
    }
  }

  const occurrences = [];
  let applicableCount = 0;

  const selector = '[role="treeitem"]';
  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart(selector)
    : helpers.queryAll(selector);

  function hasName(el) {
    const ariaLabel = getAttr(el, 'aria-label');
    if (ariaLabel) return { ok: true, method: 'aria-label' };

    const labelled = resolveAriaLabelledbyText(document, el, 8);
    if (labelled) return { ok: true, method: 'aria-labelledby' };

    const title = getAttr(el, 'title');
    if (title) return { ok: true, method: 'title' };

    const t = getConservativeSubtreeText(document, el);
    if (t) return { ok: true, method: 'content' };

    return { ok: false, method: 'none' };
  }

  for (const el of nodes) {
    if (!el) continue;
    if (!isEligibleAcc(helpers, el, ctx)) continue;

    applicableCount += 1;

    const res = hasName(el);
    if (res.ok) continue;

    const eligInfo = getEligibilityInfo
      ? (() => {
          try {
            return getEligibilityInfo(el, ctx, { targetSet: 'acc' });
          } catch {
            return null;
          }
        })()
      : null;
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: 'This element has no accessible name.',
      hint: 'Provide aria-label or aria-labelledby (preferred), or provide visible text that is not hidden from assistive technologies.',
      i18n: {
        summaryKey: 'treeitemNamePresent_summary_fail',
        hintKey: 'treeitemNamePresent_hint_fail',
        params: { controlType: 'treeitem' }
      },
      data: {
        details: { reasonCode: 'name_missing', controlType: 'treeitem', methodTried: res.method },
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'minor',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "valid-lang": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  // Shape alone accepts unregistered tags such as "eng" and "em-US", so the
  // primary subtag is checked against the IANA registry via the shared helper.
  const BCP47_RE = /^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,8})*$/;
  const isValidTag =
    typeof helpers.isValidLanguageTag === 'function'
      ? helpers.isValidLanguageTag
      : (v) => BCP47_RE.test(String(v || ''));

  const nodes = helpers.queryAllSmart
    ? helpers.queryAllSmart('[lang]')
    : helpers.queryAll('[lang]');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.getAttribute) continue;
    if (el.tagName && el.tagName.toLowerCase() === 'html') continue;

    const rawAttr = el.getAttribute('lang');
    if (rawAttr === null || rawAttr === '') continue; // ACT de46e4: empty is out of scope

    // The rule applies only where text actually inherits the language, so an
    // element with no text, or whose text reaches no one, has nothing to
    // declare a language for.
    if (!String(el.textContent || '').trim()) continue;

    applicableCount += 1;

    // A whitespace-only value is in scope and has no primary language tag.
    const raw = String(rawAttr).trim();
    if (isValidTag(raw)) continue;

    const tag = el.tagName.toLowerCase();
    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    occurrences.push({
      selector: stableSelector,
      html,
      summary: `This lang attribute value ("${raw}") is not a syntactically valid language tag.`,
      hint: 'Use a valid BCP47 language tag (e.g. "fr", "es-MX").',
      i18n: {
        summaryKey: 'validLang_summary_fail',
        hintKey: 'validLang_hint_fail',
        params: { element: tag, value: raw }
      },
      data: {
        details: { reasonCode: 'ELEMENT_LANG_INVALID', element: tag, value: raw }
      }
    });
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }
  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'fail',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }
  return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
}), applicability: null },
    "video-caption": { run: (function runInPage(ctx) {
  const { helpers, rule } = ctx;

  const nodes = helpers.queryAllSmart ? helpers.queryAllSmart('video') : helpers.queryAll('video');

  const occurrences = [];
  let applicableCount = 0;

  for (const el of nodes) {
    if (!el || !el.querySelectorAll) continue;

    applicableCount += 1;

    let hasCaptionsTrack = false;
    const tracks = el.querySelectorAll('track');
    for (const t of tracks) {
      const kind = (t.getAttribute('kind') || '').trim().toLowerCase();
      const src = (t.getAttribute('src') || '').trim();
      if ((kind === 'captions' || kind === 'subtitles') && src) {
        hasCaptionsTrack = true;
        break;
      }
    }

    if (hasCaptionsTrack) continue;

    const stableSelector = helpers.buildSelector ? helpers.buildSelector(el) : 'html';
    const html = helpers.getOuterHtmlSnippet ? helpers.getOuterHtmlSnippet(el) : el.outerHTML || '';

    const baseOccurrence = {
      selector: stableSelector,
      html,
      summary: 'This video has no captions (or subtitles) track.',
      hint: 'If this video has an audio track that conveys information, add a <track kind="captions" src="..."> with the captioned content.',
      i18n: {
        summaryKey: 'videoCaption_summary_cantTell',
        hintKey: 'videoCaption_hint_cantTell',
        params: {}
      },
      data: {
        details: { reasonCode: 'CAPTIONS_TRACK_NOT_DETECTED' }
      }
    };

    if (helpers && typeof helpers.reportOccurrence === 'function') {
      occurrences.push(helpers.reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push(baseOccurrence);
    }
  }

  if (applicableCount === 0) {
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  }

  if (occurrences.length) {
    return {
      ruleId: rule.ruleId,
      outcome: 'cantTell',
      severity: rule.defaultSeverity || 'moderate',
      occurrences
    };
  }

  // Manual rules may only emit cantTell/notApplicable (never pass/fail):
  // every <video> already has a captions/subtitles track.
  return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
}), applicability: null },
    "video-poster-text-alternative-present": { run: (function runInPage(ctx) {
  const { document, root, helpers, rule } = ctx;
  const safeRoot = root || document;

  const reportOccurrence =
    helpers && typeof helpers.reportOccurrence === 'function' ? helpers.reportOccurrence : null;

  const queryAllSmart =
    helpers && typeof helpers.queryAllSmart === 'function' ? helpers.queryAllSmart : null;
  const queryAll = helpers && typeof helpers.queryAll === 'function' ? helpers.queryAll : null;

  const getEligibilityInfo =
    helpers && typeof helpers.getEligibilityInfo === 'function' ? helpers.getEligibilityInfo : null;

  const isAccTreeEligible =
    helpers && typeof helpers.isAccTreeEligible === 'function' ? helpers.isAccTreeEligible : null;

  const isFocusableInfo =
    helpers && typeof helpers.getFocusableInfo === 'function' ? helpers.getFocusableInfo : null;

  const getAriaNameInfo =
    helpers && typeof helpers.getAriaNameInfo === 'function' ? helpers.getAriaNameInfo : null;

  function trim(v) {
    return (v == null ? '' : String(v)).trim();
  }

  function computeNameInfo(el) {
    // <video> is not a labelable element (no browser computes an accessible
    // name from <label for="...">), so only ARIA naming and title count —
    // do not accept native <label> associations.
    const flags = [];
    let aria = null;

    if (getAriaNameInfo) {
      aria = (() => {
        try {
          return getAriaNameInfo(el, ctx);
        } catch {
          return null;
        }
      })();
    }

    if (aria && aria.present && trim(aria.value)) {
      return {
        present: true,
        value: trim(aria.value),
        mechanism: aria.mechanism || 'aria',
        flags: (aria.flags || []).slice(0)
      };
    }

    const title = trim(el.getAttribute && el.getAttribute('title'));
    if (title) {
      flags.push('title-used');
      return { present: true, value: title, mechanism: 'title', flags };
    }

    if (aria && aria.flags && aria.flags.length) {
      for (const f of aria.flags) flags.push(f);
    }

    return { present: false, value: '', mechanism: 'none', flags };
  }

  const videos = (() => {
    try {
      if (queryAllSmart) return Array.from(queryAllSmart('video') || []);
      if (queryAll) return Array.from(queryAll('video') || []);
      return safeRoot && safeRoot.querySelectorAll
        ? Array.from(safeRoot.querySelectorAll('video'))
        : [];
    } catch {
      try {
        return safeRoot && safeRoot.querySelectorAll
          ? Array.from(safeRoot.querySelectorAll('video'))
          : [];
      } catch {
        return [];
      }
    }
  })();

  if (!videos.length)
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };

  const occurrences = [];
  let applicableCount = 0;

  for (const el of videos) {
    if (!el || !el.getAttribute) continue;

    const poster = trim(el.getAttribute('poster'));
    if (!poster) continue; // not applicable: no poster image

    // Eligibility: only elements exposed to AT (with helper exceptions)
    if (isAccTreeEligible) {
      const elig = (() => {
        try {
          return isAccTreeEligible(el, ctx);
        } catch {
          return { eligible: true, reasons: [] };
        }
      })();
      if (elig && elig.eligible === false) continue;
    }

    // Role presentation/none excluded ONLY if not focusable (mirrors img behavior)
    const role = trim(el.getAttribute('role')).toLowerCase();
    if (role === 'presentation' || role === 'none') {
      let focusable;
      if (isFocusableInfo) {
        const fi = (() => {
          try {
            return isFocusableInfo(el, ctx);
          } catch {
            return null;
          }
        })();
        focusable = !!(fi && fi.focusable);
      } else {
        const tabindex = el.getAttribute('tabindex');
        focusable =
          tabindex != null && trim(tabindex) !== '' && !Number.isNaN(Number(trim(tabindex)));
      }
      if (!focusable) continue;
    }

    applicableCount += 1;

    const nameInfo = computeNameInfo(el);
    if (nameInfo.present) continue;

    let eligInfo = null;
    if (getEligibilityInfo) {
      try {
        eligInfo = getEligibilityInfo(el, ctx, { targetSet: 'acc' });
      } catch {
        eligInfo = null;
      }
    }

    const baseOccurrence = {
      summary: 'Missing text alternative for <video> poster.',
      hint: 'Provide an accessible name for the poster image (aria-label/aria-labelledby preferred, or a title attribute as a fallback).',
      i18n: {
        summaryKey: 'videoPoster_textAltPresent_summary_fail',
        hintKey: 'videoPoster_textAltPresent_hint_fail',
        params: { element: 'video' }
      },
      data: {
        poster,
        visibilityFilter: eligInfo || { targetSet: 'acc', accEligible: null, reasons: [] },
        nameInfo: nameInfo || null
      }
    };

    if (reportOccurrence) {
      occurrences.push(reportOccurrence(el, baseOccurrence));
    } else {
      occurrences.push({ selector: '', html: '', ...baseOccurrence });
    }
  }

  if (applicableCount === 0)
    return { ruleId: rule.ruleId, outcome: 'notApplicable', severity: 'minor', occurrences: [] };
  if (!occurrences.length)
    return { ruleId: rule.ruleId, outcome: 'pass', severity: 'minor', occurrences: [] };
  return {
    ruleId: rule.ruleId,
    outcome: 'fail',
    severity: rule.defaultSeverity || 'minor',
    occurrences
  };
}), applicability: null }
  };

  const DEFAULT_POLICY = {
  allowedOutcomes: ['fail', 'pass', 'cantTell', 'notApplicable'],
  allowedConfidence: ['high', 'medium', 'low'],
  coerceManualFailToCantTell: true
};

// Built-in message catalogs (inlined at build time)
const I18N = {
  "de": {
    "img_altPresent_title": "<img> muss ein alt-Attribut haben",
    "img_altPresent_description": "Prüft, ob <img>-Elemente ein alt-Attribut bereitstellen, um einen Mechanismus für eine Textalternative zu unterstützen.",
    "img_altPresent_summary_fail": "Fehlendes alt-Attribut auf <img>.",
    "img_altPresent_hint_fail": "Fügen Sie ein alt-Attribut hinzu (verwenden Sie alt=\"\" nur für dekorative Bilder).",
    "area_altPresent_title": "<area> muss ein alt-Attribut haben",
    "area_altPresent_description": "Prüft, ob <area>-Elemente ein alt-Attribut bereitstellen, um einen Mechanismus für eine Textalternative zu unterstützen.",
    "area_altPresent_summary_fail": "Fehlendes alt-Attribut auf <area>.",
    "area_altPresent_hint_fail": "Fügen Sie ein alt-Attribut hinzu (verwenden Sie alt=\"\" nur für dekorative Bereiche).",
    "inputImage_altPresent_title": "<input type=\"image\"> muss ein alt-Attribut haben",
    "inputImage_altPresent_description": "Prüft, ob <input type=\"image\">-Elemente ein alt-Attribut bereitstellen, um einen Mechanismus für eine Textalternative zu unterstützen.",
    "inputImage_altPresent_summary_fail": "Fehlendes alt-Attribut auf <input type=\"image\">.",
    "inputImage_altPresent_hint_fail": "Fügen Sie ein alt-Attribut hinzu (verwenden Sie alt=\"\" nur, wenn ein separater zugänglicher Name bereitgestellt wird).",
    "inputImage_altPresent_summary_defaultName": "Der zugaengliche Name ist der Browser-Standard fuer eine Bildschaltflaeche und sagt nichts aus.",
    "inputImage_altPresent_hint_defaultName": "Ersetzen Sie ihn durch Text, der die Aktion beschreibt, zum Beispiel \"Suchen\".",
    "inputImage_altPresent_summary_emptyAlt": "Ein leeres alt=\"\" auf <input type=\"image\"> laesst das Steuerelement ohne Namen.",
    "inputImage_altPresent_hint_emptyAlt": "Beschreiben Sie die Aktion in alt, oder benennen Sie das Steuerelement mit aria-label oder aria-labelledby.",
    "ariaHidden_programmaticFocus_review_title": "Programmatischen Fokus bei aria-hidden überprüfen",
    "ariaHidden_programmaticFocus_review_description": "Markiert Elemente, die aria-hidden sind, aber aufgrund von programmatischem Fokus (z. B. tabindex < 0) als geeignet gelten. Überprüfen Sie die beabsichtigte Fokusverwaltung und die Sichtbarkeit für assistive Technologien.",
    "ariaHidden_programmaticFocus_review_summary": "Überprüfung: Ein aria-hidden-Element ist programmatisch fokussierbar.",
    "ariaHidden_programmaticFocus_review_hint": "Prüfen Sie, ob die Fokusverwaltung beabsichtigt ist und ob das Element vor assistiven Technologien verborgen bleiben soll.",
    "canvas_textAltPresent_title": "<canvas> muss eine Textalternative bereitstellen",
    "canvas_textAltPresent_description": "Prüft, ob <canvas>-Elemente eine Textalternative über Ersatzinhalt oder einen zugänglichen Namen bereitstellen.",
    "canvas_textAltPresent_summary_fail": "Fehlende Textalternative für <canvas>.",
    "canvas_textAltPresent_hint_fail": "Stellen Sie einen Ersatztext innerhalb von <canvas> oder einen zugänglichen Namen bereit (z. B. aria-label/aria-labelledby).",
    "svg_textAltPresent_title": "<svg> muss eine Textalternative bereitstellen",
    "svg_textAltPresent_description": "Prüft, ob inline eingebundene <svg>-Elemente eine Textalternative über ein <title>-Element oder einen ARIA-Namen bereitstellen (ein <desc>-Element allein zählt nicht).",
    "svg_textAltPresent_summary_fail": "Fehlende Textalternative für <svg>.",
    "svg_textAltPresent_hint_fail": "Fügen Sie ein <title>-Element mit Text oder einen ARIA-Namen hinzu (aria-label/aria-labelledby) — ein <desc>-Element allein liefert keinen zugänglichen Namen.",
    "object_textAltPresent_title": "<object> muss eine Textalternative bereitstellen",
    "object_textAltPresent_description": "Prüft, ob <object>-Elemente eine Textalternative über Ersatzinhalt oder einen zugänglichen Namen bereitstellen.",
    "object_textAltPresent_summary_fail": "Fehlende Textalternative für <object>.",
    "object_textAltPresent_hint_fail": "Stellen Sie sinnvollen Ersatzinhalt innerhalb von <object> bereit, fügen Sie einen zugänglichen Namen hinzu (aria-label/aria-labelledby), oder verwenden Sie ein title-Attribut als Notlösung.",
    "embed_textAltPresent_title": "<embed> muss eine Textalternative bereitstellen",
    "embed_textAltPresent_description": "Prüft, ob <embed>-Elemente eine Textalternative über einen zugänglichen Namen bereitstellen.",
    "embed_textAltPresent_summary_fail": "Fehlende Textalternative für <embed>.",
    "embed_textAltPresent_hint_fail": "Fügen Sie <embed> einen zugänglichen Namen hinzu (vorzugsweise aria-label/aria-labelledby, oder ein title-Attribut als Notlösung).",
    "img_altQuality_title": "<img>-Alternativtext muss angemessen sein (manuelle Überprüfung)",
    "img_altQuality_description": "Markiert <img>-Elemente mit nicht leerem Alternativtext zur manuellen Überprüfung der Angemessenheit.",
    "img_altQuality_summary_cantTell": "Überprüfen Sie den Alternativtext von <img> auf Genauigkeit und Angemessenheit.",
    "img_altQuality_hint_cantTell": "Stellen Sie sicher, dass der Alternativtext den Zweck/die Information des Bildes im Kontext vermittelt (nicht redundant, kein Dateiname).",
    "img_altDecorative_title": "<img> mit alt=\"\" muss dekorativ sein (manuelle Überprüfung)",
    "img_altDecorative_description": "Markiert <img>-Elemente mit leerem alt zur manuellen Überprüfung, ob sie rein dekorativ sind.",
    "img_altDecorative_summary_cantTell": "Überprüfen Sie, ob <img> dekorativ ist (alt=\"\").",
    "img_altDecorative_hint_cantTell": "Bestätigen Sie, dass das Bild rein dekorativ ist. Falls es Information oder Funktion vermittelt, stellen Sie einen aussagekräftigen Alternativtext bereit.",
    "area_altQuality_title": "<area>-Alternativtext muss angemessen sein (manuelle Überprüfung)",
    "area_altQuality_description": "Markiert <area>-Elemente mit nicht leerem Alternativtext zur manuellen Überprüfung der Angemessenheit.",
    "area_altQuality_summary_cantTell": "Überprüfen Sie den Alternativtext von <area> auf Genauigkeit und Angemessenheit.",
    "area_altQuality_hint_cantTell": "Stellen Sie sicher, dass der Alternativtext das Ziel/die Aktion des Bereichs der Image-Map im Kontext identifiziert.",
    "area_altDecorative_title": "<area> mit alt=\"\" muss dekorativ sein (manuelle Überprüfung)",
    "area_altDecorative_description": "Markiert <area>-Elemente mit leerem alt zur manuellen Überprüfung, ob sie dekorativ/nicht informativ sind.",
    "area_altDecorative_summary_cantTell": "Überprüfen Sie, ob <area> dekorativ ist (alt=\"\").",
    "area_altDecorative_hint_cantTell": "Bestätigen Sie, dass der Bereich keine Information oder Funktion vermittelt. Falls er interaktiv oder bedeutsam ist, stellen Sie einen aussagekräftigen Alternativtext bereit.",
    "inputImage_altQuality_title": "<input type=\"image\">-Alternativtext muss angemessen sein (manuelle Überprüfung)",
    "inputImage_altQuality_description": "Markiert <input type=\"image\">-Elemente mit nicht leerem Alternativtext zur manuellen Überprüfung der Angemessenheit.",
    "inputImage_altQuality_summary_cantTell": "Überprüfen Sie den Alternativtext von <input type=\"image\"> auf Genauigkeit und Angemessenheit.",
    "inputImage_altQuality_hint_cantTell": "Stellen Sie sicher, dass der Alternativtext die Aktion des Elements beschreibt (z. B. „Suchen“, „Bestellung abschicken“) im jeweiligen Kontext.",
    "inputImage_altDecorative_title": "<input type=\"image\"> mit alt=\"\" muss angemessen sein (manuelle Überprüfung)",
    "inputImage_altDecorative_description": "Markiert <input type=\"image\">-Elemente mit leerem alt zur manuellen Überprüfung (in der Regel bei funktionalen Formularelementen nicht angemessen).",
    "inputImage_altDecorative_summary_cantTell": "Überprüfen Sie <input type=\"image\"> mit alt=\"\".",
    "inputImage_altDecorative_hint_cantTell": "Dieses Formularelement ist typischerweise funktional. Bestätigen Sie, dass an anderer Stelle ein gleichwertiger zugänglicher Name vorhanden ist, oder stellen Sie einen aussagekräftigen Alternativtext bereit.",
    "canvas_textAltQuality_title": "<canvas>-Textalternative muss angemessen sein (manuelle Überprüfung)",
    "canvas_textAltQuality_description": "Markiert <canvas>-Elemente mit erkannter Textalternative zur manuellen Überprüfung von Gleichwertigkeit und Angemessenheit.",
    "canvas_textAltQuality_summary_cantTell": "Überprüfen Sie die Textalternative von <canvas> auf Gleichwertigkeit und Angemessenheit.",
    "canvas_textAltQuality_hint_cantTell": "Bestätigen Sie, dass der Ersatztext oder der zugängliche Name dieselbe Information/Funktion wie der Canvas-Inhalt vermittelt.",
    "svg_textAltQuality_title": "<svg>-Textalternative muss angemessen sein (manuelle Überprüfung)",
    "svg_textAltQuality_description": "Markiert zutreffende <svg>-Grafiken mit erkannter Textalternative zur manuellen Überprüfung der Angemessenheit.",
    "svg_textAltQuality_summary_cantTell": "Überprüfen Sie die Textalternative von <svg> auf Genauigkeit und Angemessenheit.",
    "svg_textAltQuality_hint_cantTell": "Bestätigen Sie, dass <title>/<desc> oder der ARIA-Name die Bedeutung/den Zweck der Grafik im Kontext vermittelt.",
    "object_textAltQuality_title": "<object>-Textalternative muss angemessen sein (manuelle Überprüfung)",
    "object_textAltQuality_description": "Markiert <object>-Elemente mit erkanntem Ersatzinhalt oder Namen zur manuellen Überprüfung von Gleichwertigkeit und Angemessenheit.",
    "object_textAltQuality_summary_cantTell": "Überprüfen Sie die Textalternative von <object> auf Gleichwertigkeit und Angemessenheit.",
    "object_textAltQuality_hint_cantTell": "Bestätigen Sie, dass der Ersatzinhalt oder der ARIA-Name eine gleichwertige Alternative für den eingebetteten Inhalt bietet.",
    "embed_textAltQuality_title": "<embed>-Textalternative muss angemessen sein (manuelle Überprüfung)",
    "embed_textAltQuality_description": "Markiert <embed>-Elemente mit erkanntem Namen zur manuellen Überprüfung der Angemessenheit.",
    "embed_textAltQuality_summary_cantTell": "Überprüfen Sie die Textalternative von <embed> auf Genauigkeit und Angemessenheit.",
    "embed_textAltQuality_hint_cantTell": "Bestätigen Sie, dass der ARIA-Name oder der title-Wert den eingebetteten Inhalt im Kontext korrekt identifiziert.",
    "videoPoster_textAltPresent_title": "Das Vorschaubild (poster) von <video> muss eine Textalternative haben",
    "videoPoster_textAltPresent_description": "Prüft, ob <video>-Elemente mit einem Vorschaubild (poster) eine Textalternative (zugänglicher Name) bereitstellen.",
    "videoPoster_textAltPresent_summary_fail": "Fehlende Textalternative für das Vorschaubild (poster) von <video>.",
    "videoPoster_textAltPresent_hint_fail": "Stellen Sie für das Vorschaubild einen zugänglichen Namen bereit (vorzugsweise aria-label/aria-labelledby, oder ein title-Attribut als Notlösung).",
    "svgImage_textAltPresent_title": "SVG <image> muss eine Textalternative haben",
    "svgImage_textAltPresent_description": "Prüft, ob SVG-<image>-Elemente eine Textalternative über <title>/<desc> oder einen zugänglichen ARIA-Namen bereitstellen.",
    "svgImage_textAltPresent_summary_fail": "Fehlende Textalternative auf SVG <image>.",
    "svgImage_textAltPresent_hint_fail": "Fügen Sie innerhalb von <image> ein <title> (und optional <desc>) hinzu, oder stellen Sie aria-label/aria-labelledby bereit.",
    "formControl_programmaticLabelPresent_title": "Formularelemente müssen eine programmatische Beschriftung haben",
    "formControl_programmaticLabelPresent_description": "Prüft, ob Formularelemente eine programmatische Beschriftung über <label>, aria-label oder aria-labelledby haben.",
    "formControl_programmaticLabelPresent_summary_fail": "Dem Formularelement fehlt eine programmatische Beschriftung.",
    "formControl_programmaticLabelPresent_hint_fail": "Stellen Sie eine <label>-Zuordnung, aria-label oder aria-labelledby bereit (placeholder/title zählen nicht als Beschriftung).",
    "formControlAccessibleName_description": "Schlägt fehl, wenn ein zutreffendes Formularelement keinen zugänglichen Namen hat (z. B. label, aria-label, aria-labelledby).",
    "formControlAccessibleName_hint_fail": "Stellen Sie einen zugänglichen Namen über ein <label>, aria-label oder aria-labelledby bereit.",
    "formControlAccessibleName_summary_fail": "Das Formularelement hat keinen zugänglichen Namen.",
    "formControlAccessibleName_title": "Formularelemente müssen einen zugänglichen Namen haben",
    "linksTargetBlankNoopener_description": "Stellt sicher, dass Links mit target=\"_blank\" die Risiken von Reverse-Tabnabbing verringern.",
    "linksTargetBlankNoopener_hint_cantTell": "Beachten Sie die Hinweise zu dieser Regel.",
    "linksTargetBlankNoopener_summary_cantTell": "Links, die in einem neuen Tab geöffnet werden, sollten rel=\"noopener\" verwenden",
    "linksTargetBlankNoopener_title": "Links, die in einem neuen Tab geöffnet werden, sollten rel=\"noopener\" verwenden",
    "manualReview_description": "Markiert, dass eine manuelle Überprüfung der Tastaturnavigation und der Fokusreihenfolge erforderlich ist.",
    "manualReview_hint_cantTell": "Beachten Sie die Hinweise zu dieser Regel.",
    "manualReview_summary_cantTell": "Manuelle Überprüfung: Tastaturnavigation und Fokusreihenfolge",
    "manualReview_title": "Manuelle Überprüfung: Tastaturnavigation und Fokusreihenfolge",
    "rules.img-alt-suspicious.meta.title": "Verdächtiger Alternativtext erfordert eine Überprüfung",
    "rules.img-alt-suspicious.meta.description": "Identifiziert Bilder, deren Alternativtext gängigen verdächtigen Mustern entspricht (z. B. Dateinamen, URLs, Platzhaltern oder generischen Begriffen), und erfordert eine manuelle Überprüfung.",
    "rules.img-alt-suspicious.occurrence.cantTell.summary": "Der Alternativtext des Bildes wirkt verdächtig („{{alt}}“ ähnelt {{pattern}}) und erfordert eine Überprüfung.",
    "rules.img-alt-suspicious.occurrence.cantTell.hint": "Überprüfen Sie den Alternativtext. Vermeiden Sie Dateinamen, URLs, Platzhalter oder generische Begriffe, und stellen Sie sicher, dass die Textalternative den Zweck oder die Funktion des Bildes im Kontext beschreibt.",
    "formControl_programmaticLabelQuality_title": "Formularelemente sollten sich nicht auf placeholder oder title als primäre Beschriftung verlassen",
    "formControl_programmaticLabelQuality_description": "Markiert Formularelemente, deren berechneter zugänglicher Name sich auf placeholder oder title als primäre Beschriftungsmethode stützt. Bevorzugen Sie <label> oder aria-labelledby.",
    "formControl_programmaticLabelQuality_summary_cantTell": "Die primäre Beschriftung des Formularelements stammt von {{methodLabel}}.",
    "formControl_programmaticLabelQuality_hint_cantTell": "Bevorzugen Sie ein dauerhaftes <label> oder aria-labelledby. Verlassen Sie sich nicht auf placeholder/title als primäre Beschriftung.",
    "html_lang_attr_title": "Die Sprache der Seite ist deklariert",
    "html_lang_attr_description": "Prüft, ob die Standardsprache der Seite programmatisch deklariert ist.",
    "html_lang_attr_missing_absent": "Die Standardsprache der Seite ist nicht deklariert.",
    "html_lang_attr_hint_missing_absent": "Fügen Sie dem <html>-Element ein lang-Attribut hinzu (zum Beispiel: <html lang=\"de\">).",
    "html_lang_attr_missing_empty": "Die Standardsprache der Seite ist deklariert, aber leer.",
    "html_lang_attr_hint_missing_empty": "Legen Sie im lang-Attribut des <html>-Elements einen gültigen Sprachwert fest (zum Beispiel: <html lang=\"de\">).",
    "html_lang_attr_invalid": "Die Standardsprache der Seite ist deklariert, aber der Wert „{{lang}}“ ist kein gültiges Sprach-Tag.",
    "html_lang_attr_hint_invalid": "Verwenden Sie in <html lang=\"…\"> ein gültiges BCP-47-Sprach-Tag (zum Beispiel: „de“, „fr“, „en-US“).",
    "mediaTranscriptPresent_title": "Zeitbasierte Medien: Nachweis einer Transkription oder Textalternative",
    "mediaTranscriptPresent_description": "Findet Audio- und Video-Elemente, bei denen eine Transkription oder eine andere Textalternative im Seiteninhalt nicht eindeutig nachgewiesen ist. Diese Regel ist konservativ ausgelegt und meldet manuelle Überprüfung (cantTell), wenn Nachweise fehlen oder nicht verifiziert werden können.",
    "mediaTranscriptPresent_summary_cantTell_missing": "Das Vorhandensein einer Transkription oder einer anderen Textalternative für dieses <{{element}}> ist auf der Seite nicht eindeutig nachgewiesen.",
    "mediaTranscriptPresent_hint_cantTell_missing": "Stellen Sie für vorab aufgezeichnete Nur-Audio- oder Nur-Video-Medien eine klar erkennbare Transkription oder eine andere Textalternative bereit, zum Beispiel einen sichtbaren Abschnitt oder Link „Transkription“.",
    "mediaTranscriptPresent_summary_cantTell_unverified": "Für dieses zeitbasierte Medium ist möglicherweise eine Transkription oder eine andere Textalternative verfügbar, dies konnte jedoch anhand des Seiteninhalts nicht verifiziert werden.",
    "mediaTranscriptPresent_hint_cantTell_unverified": "Stellen Sie sicher, dass eine klar erkennbare Transkription oder eine andere Textalternative verfügbar und sichtbar oder programmatisch mit dem Medium auf der Seite verknüpft ist.",
    "pageTitlePresent_title": "Die Seite hat einen nicht leeren Titel",
    "pageTitlePresent_description": "Prüft, ob die Seite ein nicht leeres <title>-Element enthält, das die Seite identifiziert.",
    "pageTitlePresent_summary_fail": "Die Seite hat keinen nicht leeren Titel.",
    "pageTitlePresent_hint_fail": "Fügen Sie ein <title>-Element mit Text hinzu, der das Thema oder den Zweck der Seite beschreibt.",
    "pageTitlePatterns_title": "Seitentitel-Muster, die möglicherweise unzureichend aussagekräftig sind",
    "pageTitlePatterns_description": "Identifiziert Muster in Seitentiteln, die auf eine geringe Aussagekraft hindeuten können, etwa generische, duplizierte oder übermäßig schablonenhafte Titel. Diese Regel liefert Hinweise zur Überprüfung und schlägt nicht automatisch fehl.",
    "pageTitlePatterns_summary_cantTell": "Der Seitentitel ist möglicherweise nicht aussagekräftig genug, um das Thema oder den Zweck der Seite zu identifizieren.",
    "pageTitlePresent_summary_fail_missing": "Der Seite fehlt ein <title>-Element.",
    "pageTitlePresent_summary_fail_empty": "Die Seite hat ein leeres <title>.",
    "pageTitlePatterns_summary_cantTell_duplicateAcrossPages": "Mehrere Seiten teilen sich denselben Titel, was die Unterscheidung der Seiten erschweren kann ({{duplicateGroups}} Duplikatgruppen über {{pagesAnalyzed}} Seiten). Beispiel: „{{exampleTitle}}“.",
    "pageTitlePatterns_summary_cantTell_templatedAcrossPages": "Viele Seitentitel wirken stark schablonenhaft, was beeinträchtigen kann, wie gut Titel Seiten voneinander unterscheiden ({{pagesAnalyzed}} Seiten).",
    "pageTitlePatterns_summary_cantTell_generic": "Der Seitentitel ist generisch und identifiziert das Thema oder den Zweck der Seite möglicherweise nicht.",
    "pageTitlePatterns_summary_cantTell_veryShort": "Der Seitentitel ist sehr kurz und identifiziert das Thema oder den Zweck der Seite möglicherweise nicht.",
    "pageTitlePatterns_summary_cantTell_templateLike": "Der Seitentitel wirkt schablonenhaft und identifiziert das Thema oder den Zweck der Seite möglicherweise nicht.",
    "pageTitlePatterns_hint_cantTell": "Überprüfen Sie den Seitentitel und stellen Sie sicher, dass er das Thema oder den Zweck der Seite klar identifiziert und hilft, die Seite von anderen zu unterscheiden.",
    "contrastComputable_title": "Der Farbkontrast ist für gerenderten Text berechenbar",
    "contrastComputable_description": "Bestimmt, ob ausreichend Informationen verfügbar sind, um den WCAG-Farbkontrast für sichtbaren Text zu berechnen (z. B. keine Verläufe/Bilder/Blend-Modi, die den Hintergrund unbestimmbar machen).",
    "contrastComputable_pass_allComputable": "Der Kontrast ist für allen zutreffenden Text berechenbar ({{eligibleTextCount}} Textknoten).",
    "contrastComputable_cantTell_generic": "Der Kontrast ist möglicherweise nicht berechenbar ({{reasonCode}}).",
    "contrastComputable_cantTell_bgImageOrGradient": "Der Kontrast ist nicht berechenbar, weil der Hintergrund ein Bild oder einen Verlauf verwendet ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgImage": "Der Kontrast ist nicht berechenbar, weil der Hintergrund ein Bild verwendet ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgGradient": "Der Kontrast ist nicht berechenbar, weil der Hintergrund einen Verlauf verwendet ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgImageAndGradient": "Der Kontrast ist nicht berechenbar, weil der Hintergrund ein Bild und einen Verlauf verwendet ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_mixBlendMode": "Der Kontrast ist nicht berechenbar, weil mix-blend-mode verwendet wird ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_filter": "Der Kontrast ist nicht berechenbar, weil filter/backdrop-filter verwendet wird ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_rootNotOpaque": "Der Kontrast ist nicht berechenbar, weil der effektive Hintergrund an der Wurzel nicht vollständig opak ist (alpha={{backgroundAlpha}}).",
    "contrastComputable_cantTell_foregroundUnparsable": "Der Kontrast ist nicht berechenbar, weil die berechnete Vordergrundfarbe nicht ausgewertet werden konnte.",
    "contrastComputable_cantTell_engineFailure": "Die Berechenbarkeit des Kontrasts konnte aufgrund eines internen Engine-Fehlers nicht bestimmt werden ({{reasonCode}}).",
    "contrastComputable_cantTell_backdropFilter": "Der Kontrast ist nicht berechenbar, weil backdrop-filter verwendet wird ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_filterOrBackdropFilter": "Der Kontrast ist nicht berechenbar, weil filter verwendet wird ({{blockerProperty}}={{blockerValue}}).",
    "contrastMinimum_title": "Text erfüllt den Mindestfarbkontrast (AA)",
    "contrastMinimum_description": "Prüft, ob sichtbarer Text ein Kontrastverhältnis von mindestens 4,5:1 (normal) oder 3,0:1 (groß) aufweist, sofern der Kontrast aus CSS berechenbar ist.",
    "contrastMinimum_fail_belowThreshold": "Das Element weist einen unzureichenden Farbkontrast von {{ratio}}:1 auf (Vordergrund: {{foregroundHex}}, Hintergrund: {{backgroundHex}}, Schriftgröße: {{fontSizePx}}px, Schriftgewicht: {{fontWeightLabel}}). Erwartetes Kontrastverhältnis: {{threshold}}:1 ({{#isLargeText}}großer Text{{/isLargeText}}{{^isLargeText}}normaler Text{{/isLargeText}}).",
    "contrastMinimum_pass_allAboveThreshold": "Der gesamte berechenbare Text erfüllt den Mindestkontrast (AA). Zutreffende Textknoten: {{eligibleTextCount}}. Berechenbar: {{computableTextCount}}.",
    "contrastMinimum_notApplicable_noComputableText": "Bei keinem zutreffenden Text war der Kontrast berechenbar (zutreffende Textknoten: {{eligibleTextCount}}). Details siehe Regel zur Kontrastberechenbarkeit.",
    "contrastMinimum_cantTell_engineFailure": "Der Mindestkontrast (AA) konnte aufgrund eines internen Engine-Fehlers nicht bestimmt werden ({{reasonCode}}).",
    "contrastEnhanced_title": "Text erfüllt den erweiterten Farbkontrast (AAA)",
    "contrastEnhanced_description": "Prüft, ob sichtbarer Text ein Kontrastverhältnis von mindestens 7,0:1 (normal) oder 4,5:1 (groß) aufweist, sofern der Kontrast aus CSS berechenbar ist.",
    "contrastEnhanced_fail_belowThreshold": "Das Element weist einen unzureichenden Farbkontrast (AAA) von {{ratio}}:1 auf (Vordergrund: {{foregroundHex}}, Hintergrund: {{backgroundHex}}, Schriftgröße: {{fontSizePx}}px, Schriftgewicht: {{fontWeightLabel}}). Erwartetes Kontrastverhältnis: {{threshold}}:1 ({{#isLargeText}}großer Text{{/isLargeText}}{{^isLargeText}}normaler Text{{/isLargeText}}).",
    "contrastEnhanced_pass_allAboveThreshold": "Der gesamte berechenbare Text erfüllt den erweiterten Kontrast (AAA). Zutreffende Textknoten: {{eligibleTextCount}}. Berechenbar: {{computableTextCount}}.",
    "contrastEnhanced_notApplicable_noComputableText": "Bei keinem zutreffenden Text war der Kontrast berechenbar (zutreffende Textknoten: {{eligibleTextCount}}). Details siehe Regel zur Kontrastberechenbarkeit.",
    "contrastEnhanced_cantTell_engineFailure": "Der erweiterte Kontrast (AAA) konnte aufgrund eines internen Engine-Fehlers nicht bestimmt werden ({{reasonCode}}).",
    "dom_textContrastMinimum_title": "Text muss ausreichenden Kontrast aufweisen (Minimum)",
    "dom_textContrastMinimum_description": "Prüft den Kontrast von sichtbarem Text gegenüber seinem berechneten Hintergrund gemäß WCAG 2.2 Erfolgskriterium 1.4.3 (AA), unter Verwendung der gerenderten Stile (Schriftgröße/-gewicht) zur Bestimmung des erforderlichen Verhältnisses.",
    "dom_textContrastMinimum_summary_fail": "Unzureichender Textkontrast: {{contrastRatio}}:1 (erforderlich {{requiredRatio}}:1). Vordergrund {{fgColor}} auf Hintergrund {{bgColor}}. Schrift {{fontSizePx}}px, Gewicht {{fontWeight}}{{#isBold}}, fett{{/isBold}}{{#isLargeText}} (großer Text){{/isLargeText}}.",
    "dom_textContrastMinimum_hint_fail": "Passen Sie die Vordergrund- oder Hintergrundfarbe so an, dass das Kontrastverhältnis für diese Textgröße/dieses Textgewicht mindestens {{requiredRatio}}:1 beträgt.",
    "dom_textContrastMinimum_summary_pass": "Textkontrast in Ordnung: {{contrastRatio}}:1 (erforderlich {{requiredRatio}}:1). Vordergrund {{fgColor}} auf Hintergrund {{bgColor}}. Schrift {{fontSizePx}}px, Gewicht {{fontWeight}}{{#isBold}}, fett{{/isBold}}{{#isLargeText}} (großer Text){{/isLargeText}}.",
    "dom_textContrastMinimum_summary_cantTell": "Der Textkontrast konnte nicht zuverlässig berechnet werden, da der effektive Hintergrund nicht deterministisch auflösbar ist (z. B. Bild, Verlauf, Video, Canvas, komplexe Transparenz oder Blending).",
    "dom_textContrastMinimum_hint_cantTell": "Überprüfen Sie den Kontrast manuell, wenn Text über Bildern/Verläufen/Transparenz liegt; stellen Sie sicher, dass er {{requiredRatio}}:1 für die berechnete Textgröße/das Textgewicht erreicht.",
    "dom_textContrastEnhanced_title": "Text muss ausreichenden Kontrast aufweisen (erweitert)",
    "dom_textContrastEnhanced_description": "Prüft den Kontrast von sichtbarem Text gegenüber seinem berechneten Hintergrund gemäß WCAG 2.2 Erfolgskriterium 1.4.6 (AAA), unter Verwendung der gerenderten Stile (Schriftgröße/-gewicht) zur Bestimmung des erforderlichen Verhältnisses.",
    "dom_textContrastEnhanced_summary_fail": "Unzureichender erweiterter Textkontrast: {{contrastRatio}}:1 (erforderlich {{requiredRatio}}:1). Vordergrund {{fgColor}} auf Hintergrund {{bgColor}}. Schrift {{fontSizePx}}px, Gewicht {{fontWeight}}{{#isBold}}, fett{{/isBold}}{{#isLargeText}} (großer Text){{/isLargeText}}.",
    "dom_textContrastEnhanced_hint_fail": "Passen Sie die Vordergrund- oder Hintergrundfarbe so an, dass das Kontrastverhältnis für den erweiterten (AAA-)Kontrast mindestens {{requiredRatio}}:1 beträgt.",
    "dom_textContrastEnhanced_summary_pass": "Erweiterter Textkontrast in Ordnung: {{contrastRatio}}:1 (erforderlich {{requiredRatio}}:1). Vordergrund {{fgColor}} auf Hintergrund {{bgColor}}. Schrift {{fontSizePx}}px, Gewicht {{fontWeight}}{{#isBold}}, fett{{/isBold}}{{#isLargeText}} (großer Text){{/isLargeText}}.",
    "dom_textContrastEnhanced_summary_cantTell": "Der erweiterte Textkontrast konnte nicht zuverlässig berechnet werden, da der effektive Hintergrund nicht deterministisch auflösbar ist (z. B. Bild, Verlauf, Video, Canvas, komplexe Transparenz oder Blending).",
    "dom_textContrastEnhanced_hint_cantTell": "Überprüfen Sie den erweiterten (AAA-)Kontrast manuell, wenn Text über Bildern/Verläufen/Transparenz liegt; stellen Sie sicher, dass er {{requiredRatio}}:1 für die berechnete Textgröße/das Textgewicht erreicht.",
    "dom_nonTextContrast_title": "UI-Komponenten und Grafiken müssen ausreichenden Kontrast aufweisen",
    "dom_nonTextContrast_description": "Prüft den Kontrast nicht-textueller visueller Informationen (UI-Komponentengrenzen, Zustände und bedeutungstragende grafische Objekte) gemäß WCAG 2.2 Erfolgskriterium 1.4.11 (AA).",
    "dom_nonTextContrast_summary_fail": "Unzureichender Nicht-Text-Kontrast: {{contrastRatio}}:1 (erforderlich {{requiredRatio}}:1). Vordergrund {{fgColor}} gegenüber Hintergrund {{bgColor}}. Komponente: {{componentKind}}{{#componentState}} (Zustand: {{componentState}}){{/componentState}}.",
    "dom_nonTextContrast_hint_fail": "Passen Sie die Farben der Komponente/Grafik so an, dass das Kontrastverhältnis für die wahrnehmbare Abgrenzung oder die wesentliche visuelle Information mindestens {{requiredRatio}}:1 beträgt.",
    "dom_nonTextContrast_summary_pass": "Nicht-Text-Kontrast in Ordnung: {{contrastRatio}}:1 (erforderlich {{requiredRatio}}:1). Vordergrund {{fgColor}} gegenüber Hintergrund {{bgColor}}. Komponente: {{componentKind}}{{#componentState}} (Zustand: {{componentState}}){{/componentState}}.",
    "dom_nonTextContrast_summary_cantTell": "Der Nicht-Text-Kontrast konnte nicht zuverlässig berechnet werden, da der effektive Hintergrund oder die gezeichneten Pixel nicht deterministisch auflösbar sind (z. B. Bild/Verlauf/Video/Canvas, komplexe Transparenz oder Blending).",
    "dom_nonTextContrast_hint_cantTell": "Überprüfen Sie den Kontrast der Komponente/Grafik manuell gegenüber den angrenzenden Farben; stellen Sie sicher, dass er {{requiredRatio}}:1 für die wesentliche nicht-textuelle visuelle Information erreicht.",
    "contrastEnhanced_pass_allTextMeetsThreshold": "Der gesamte berechenbare Text erfüllt den erweiterten Kontrast (AAA).",
    "contrastMinimum_pass_allTextMeetsThreshold": "Der gesamte berechenbare Text erfüllt den Mindestkontrast (AA).",
    "contrastComputable_cantTell_notComputable": "Der Kontrast konnte für diesen Text nicht berechnet werden ({{reasonCode}}).",
    "roleImg_textAlternativePresent_title": "[role=\"img\"] muss eine zugängliche Textalternative haben",
    "roleImg_textAlternativePresent_description": "Prüft, ob Elemente mit role=\"img\" eine zugängliche Textalternative über aria-label, aria-labelledby oder ein title-Attribut bereitstellen.",
    "roleImg_textAlternativePresent_summary_fail": "Das Element mit role=\"img\" hat keine zugängliche Textalternative.",
    "roleImg_textAlternativePresent_hint_fail": "Stellen Sie eine Textalternative über aria-label oder aria-labelledby bereit, das auf nicht leeren Text verweist.",
    "targetSizeMinimum_title": "Zeigerziele müssen mindestens 24x24px groß sein oder ausreichend Abstand zu anderen Zielen einhalten",
    "targetSizeMinimum_description": "Prüft, ob per Zeiger bedienbare Ziele eine effektive Zielgröße von mindestens 24 mal 24 CSS-Pixel haben oder eine zulässige Ausnahme erfüllen (z. B. ausreichender Abstand).",
    "targetSizeMinimum_summary_fail": "Ein oder mehrere Zeigerziele sind kleiner als 24×24 CSS-Pixel und liegen zu nah an einem anderen Ziel.",
    "targetSizeMinimum_hint_fail": "Vergrößern Sie das Ziel auf mindestens 24×24 CSS-Pixel, oder fügen Sie ausreichend Abstand zu benachbarten Zielen hinzu.",
    "targetSizeMinimum_summary_cantTell_ambiguousSpacing": "Das Ziel ist möglicherweise zu klein und zu nah an einem anderen Ziel, aber die Überlappung liegt nahe am Erkennungsschwellenwert und konnte nicht zuverlässig gemessen werden.",
    "targetSizeMinimum_hint_cantTell_ambiguousSpacing": "Überprüfen Sie manuell den effektiven Abstand zwischen diesem Ziel und seinem Nachbarn; vergrößern Sie Zielgröße oder Abstand, falls die Überlappung real ist.",
    "targetSizeMinimum_summary_cantTell_plausiblyEssential": "Das Ziel ist zu klein und zu nah an einem anderen Ziel, könnte aber als Teil einer essenziellen Grafik oder eines Image-Map-Bereichs ausgenommen sein.",
    "targetSizeMinimum_hint_cantTell_plausiblyEssential": "Prüfen Sie, ob die Größe dieses Ziels wirklich für seine Funktion essenziell ist (z. B. Teil eines SVG/Canvas/einer Image-Map); falls nicht, vergrößern Sie Zielgröße oder Abstand.",
    "targetSizeMinimum_notApplicable_noTargets": "Es gab keine per Zeiger bedienbaren Ziele, die für die Bewertung infrage kamen.",
    "targetSizeMinimum_pass_allOk": "Alle zutreffenden Zeigerziele erfüllen die Mindestgröße oder eine zulässige Ausnahme.",
    "ariaHidden_focus_title": "ARIA-hidden-Elemente dürfen nicht fokussierbar sein",
    "ariaHidden_focus_description": "Prüft, ob Elemente mit aria-hidden=\"true\" nicht fokussierbar sind und keine fokussierbaren Nachfahren enthalten.",
    "ariaHidden_focus_summary_fail_desc": "aria-hidden {{element}} enthält {{focusableCount}} fokussierbare(s) Element(e).",
    "ariaHidden_focus_summary_fail_self": "aria-hidden {{element}} ist fokussierbar ({{focusableCount}} fokussierbare(s) Element(e)).",
    "ariaHidden_focus_summary_fail_self_and_desc": "aria-hidden {{element}} ist fokussierbar und enthält {{descendantFocusableCount}} fokussierbare(n) Nachfahren ({{focusableCount}} fokussierbare Elemente insgesamt).",
    "ariaHidden_focus_hint_fail": "Entfernen Sie die Fokussierbarkeit der Nachfahren oder entfernen Sie aria-hidden; stellen Sie sicher, dass Fokus- und Accessibility-Baum übereinstimmen.",
    "ariaHidden_focus_summary_cantTell_redirect": "aria-hidden {{element}} hat den Fokus erhalten, der Fokus wurde jedoch sofort zu einem anderen Element verschoben. Überprüfen Sie das Sentinel-/Fokus-Trap-Verhalten.",
    "ariaHidden_focus_hint_cantTell_redirect": "Überprüfen Sie, ob es sich um eine beabsichtigte Fokus-Sentinel-/Fokus-Trap-Übergabe handelt und dass Tastaturnutzer niemals auf verborgenen Fokuszielen verbleiben.",
    "cssHidden_focus_title": "Fokussierbare Elemente dürfen nicht visuell verborgen sein",
    "cssHidden_focus_description": "Prüft, ob per Tastatur fokussierbare Elemente nicht durch CSS-Techniken visuell verborgen werden, die sie dennoch in der Tab-Reihenfolge belassen können.",
    "cssHidden_focus_summary_cantTell": "Das fokussierbare {{element}} ist visuell verborgen ({{visibilityHints}}).",
    "cssHidden_focus_hint_cantTell": "Machen Sie das Element sichtbar, wenn es den Tastaturfokus erhalten kann, oder entfernen Sie es aus der Tab-Reihenfolge, solange es nicht sichtbar ist.",
    "linkNamePresent_title": "Links haben einen zugänglichen Namen",
    "linkNamePresent_description": "Prüft, ob Links einen nicht leeren zugänglichen Namen aufweisen.",
    "linkNamePresent_summary_fail": "Dieser Link hat keinen zugänglichen Namen.",
    "linkNamePresent_hint_fail": "Stellen Sie einen Linktext oder einen Mechanismus für einen zugänglichen Namen bereit (zum Beispiel aria-label), damit assistive Technologien den Link identifizieren können.",
    "buttonNamePresent_title": "Schaltflächen haben einen zugänglichen Namen",
    "buttonNamePresent_description": "Prüft, ob Schaltflächen einen nicht leeren zugänglichen Namen aufweisen.",
    "buttonNamePresent_summary_fail": "Diese Schaltfläche hat keinen zugänglichen Namen.",
    "buttonNamePresent_hint_fail": "Stellen Sie einen sichtbaren Schaltflächentext oder einen programmatischen Mechanismus für einen zugänglichen Namen bereit (zum Beispiel aria-label), damit assistive Technologien die Schaltfläche identifizieren können.",
    "binaryControlNamePresent_title": "Binäre Formularelemente haben einen zugänglichen Namen",
    "binaryControlNamePresent_description": "Prüft, ob Kontrollkästchen, Optionsfelder und Schalter (switch) einen nicht leeren zugänglichen Namen aufweisen.",
    "binaryControlNamePresent_summary_fail": "Dieses Formularelement hat keinen zugänglichen Namen.",
    "binaryControlNamePresent_hint_fail": "Stellen Sie eine Beschriftung, aria-label, aria-labelledby oder einen anderen Mechanismus für einen zugänglichen Namen bereit, damit assistive Technologien das Formularelement identifizieren können.",
    "comboboxNamePresent_title": "Comboboxen haben einen zugänglichen Namen",
    "comboboxNamePresent_description": "Prüft, ob Elemente mit role=\"combobox\" einen nicht leeren zugänglichen Namen aufweisen.",
    "comboboxNamePresent_summary_fail": "Diese Combobox hat keinen zugänglichen Namen.",
    "comboboxNamePresent_hint_fail": "Stellen Sie aria-label, aria-labelledby oder ein title-Attribut bereit — sichtbarer Textinhalt wird bei dieser Combobox nicht als zugänglicher Name ausgegeben.",
    "dialogNamePresent_title": "Dialoge haben einen zugänglichen Namen",
    "dialogNamePresent_description": "Prüft, ob Elemente mit role=\"dialog\" oder role=\"alertdialog\" einen nicht leeren zugänglichen Namen aufweisen.",
    "dialogNamePresent_summary_fail": "Dieser Dialog hat keinen zugänglichen Namen.",
    "dialogNamePresent_hint_fail": "Stellen Sie aria-labelledby (bevorzugt) oder aria-label bereit, damit assistive Technologien den Dialog ansagen können.",
    "menuitemNamePresent_title": "Menüeinträge haben einen zugänglichen Namen",
    "menuitemNamePresent_description": "Prüft, ob Menüeinträge (role=\"menuitem*\", einschließlich Checkbox-/Radio-Varianten) einen nicht leeren zugänglichen Namen aufweisen.",
    "menuitemNamePresent_summary_fail": "Dieser Menüeintrag hat keinen zugänglichen Namen.",
    "menuitemNamePresent_hint_fail": "Stellen Sie sichtbaren Text bereit, der nicht vor assistiven Technologien verborgen ist, oder stellen Sie aria-label oder aria-labelledby bereit.",
    "tabNamePresent_title": "Tabs haben einen zugänglichen Namen",
    "tabNamePresent_description": "Prüft, ob Elemente mit role=\"tab\" einen nicht leeren zugänglichen Namen aufweisen.",
    "tabNamePresent_summary_fail": "Dieser Tab hat keinen zugänglichen Namen.",
    "tabNamePresent_hint_fail": "Stellen Sie einen Tab-Text bereit, der nicht vor assistiven Technologien verborgen ist, oder stellen Sie aria-label oder aria-labelledby bereit.",
    "sliderNamePresent_title": "Schieberegler haben einen zugänglichen Namen",
    "sliderNamePresent_description": "Prüft, ob Schieberegler (input[type=\"range\"] und role=\"slider\") einen nicht leeren zugänglichen Namen aufweisen.",
    "sliderNamePresent_summary_fail": "Dieser Schieberegler hat keinen zugänglichen Namen.",
    "sliderNamePresent_hint_fail": "Stellen Sie eine Beschriftung, aria-label oder aria-labelledby bereit, damit assistive Technologien den Schieberegler identifizieren können.",
    "textboxNamePresent_title": "Textfelder haben einen zugänglichen Namen",
    "textboxNamePresent_description": "Prüft, ob Elemente mit role=\"textbox\" einen nicht leeren zugänglichen Namen aufweisen.",
    "textboxNamePresent_summary_fail": "Dieses Textfeld hat keinen zugänglichen Namen.",
    "textboxNamePresent_hint_fail": "Stellen Sie aria-label, aria-labelledby oder ein title-Attribut bereit — sichtbarer Textinhalt wird bei diesem Textfeld nicht als zugänglicher Name ausgegeben.",
    "searchboxNamePresent_title": "Suchfelder haben einen zugänglichen Namen",
    "searchboxNamePresent_description": "Prüft, ob Elemente mit role=\"searchbox\" einen nicht leeren zugänglichen Namen aufweisen.",
    "searchboxNamePresent_summary_fail": "Dieses Suchfeld hat keinen zugänglichen Namen.",
    "searchboxNamePresent_hint_fail": "Stellen Sie aria-label, aria-labelledby oder ein title-Attribut bereit — sichtbarer Textinhalt wird bei diesem Suchfeld nicht als zugänglicher Name ausgegeben.",
    "spinbuttonNamePresent_title": "Spinbuttons haben einen zugänglichen Namen",
    "spinbuttonNamePresent_description": "Prüft, ob Elemente mit role=\"spinbutton\" einen nicht leeren zugänglichen Namen aufweisen.",
    "spinbuttonNamePresent_summary_fail": "Dieser Spinbutton hat keinen zugänglichen Namen.",
    "spinbuttonNamePresent_hint_fail": "Stellen Sie aria-label, aria-labelledby oder ein title-Attribut bereit — sichtbarer Textinhalt wird bei diesem Spinbutton nicht als zugänglicher Name ausgegeben.",
    "listboxNamePresent_title": "Listboxen haben einen zugänglichen Namen",
    "listboxNamePresent_description": "Prüft, ob Elemente mit role=\"listbox\" einen nicht leeren zugänglichen Namen aufweisen.",
    "listboxNamePresent_summary_fail": "Diese Listbox hat keinen zugänglichen Namen.",
    "listboxNamePresent_hint_fail": "Stellen Sie aria-label, aria-labelledby oder ein title-Attribut bereit — sichtbarer Textinhalt wird bei dieser Listbox nicht als zugänglicher Name ausgegeben.",
    "optionNamePresent_title": "Optionen haben einen zugänglichen Namen",
    "optionNamePresent_description": "Prüft, ob Elemente mit role=\"option\" einen nicht leeren zugänglichen Namen aufweisen.",
    "optionNamePresent_summary_fail": "Diese Option hat keinen zugänglichen Namen.",
    "optionNamePresent_hint_fail": "Stellen Sie einen Optionstext bereit, der nicht vor assistiven Technologien verborgen ist, oder stellen Sie aria-label oder aria-labelledby bereit.",
    "treeitemNamePresent_title": "Baumeinträge haben einen zugänglichen Namen",
    "treeitemNamePresent_description": "Prüft, ob Elemente mit role=\"treeitem\" einen nicht leeren zugänglichen Namen aufweisen.",
    "treeitemNamePresent_summary_fail": "Dieser Baumeintrag hat keinen zugänglichen Namen.",
    "treeitemNamePresent_hint_fail": "Stellen Sie einen Baumeintrag-Text bereit, der nicht vor assistiven Technologien verborgen ist, oder stellen Sie aria-label oder aria-labelledby bereit.",
    "ariaRoleNamePresent_title": "ARIA-Widget-/Container-Rollen haben einen zugänglichen Namen",
    "ariaRoleNamePresent_description": "Prüft, ob bestimmte ARIA-Widget-/Container-Rollen einen nicht leeren zugänglichen Namen aufweisen.",
    "ariaRoleNamePresent_summary_fail": "Dieses Element hat keinen zugänglichen Namen.",
    "ariaRoleNamePresent_hint_fail": "Stellen Sie aria-label oder aria-labelledby bereit (bevorzugt), oder ein nicht leeres title-Attribut.",
    "composite_rollup_summary": "Ergebnis der zusammengesetzten Regel: {{reasonCode}} ({{testCount}} Prüfungen)",
    "catalog.rules.wcag_111_non_text_content.title": "Nicht-Text-Inhalt: Textalternativen",
    "catalog.rules.wcag_111_non_text_content.description": "Zusammenfassung von Prüfungen, die sicherstellen, dass Nicht-Text-Inhalte eine passende Textalternative haben.",
    "catalog.rules.wcag_121_prerecorded_transcript.title": "Nur Audio und nur Video (voraufgezeichnet): Transkription",
    "catalog.rules.wcag_121_prerecorded_transcript.description": "Zusammenfassung von Prüfungen zur Verfügbarkeit einer Transkription für voraufgezeichnete Nur-Audio- oder Nur-Video-Medien.",
    "catalog.rules.wcag_143_contrast_minimum.title": "Kontrast: Minimum",
    "catalog.rules.wcag_143_contrast_minimum.description": "Zusammenfassung von Prüfungen zum Mindesttextkontrast.",
    "catalog.rules.wcag_146_contrast_enhanced.title": "Kontrast: erweitert",
    "catalog.rules.wcag_146_contrast_enhanced.description": "Zusammenfassung von Prüfungen zum erweiterten Textkontrast.",
    "catalog.rules.wcag_242_page_titled.title": "Seite mit Titel",
    "catalog.rules.wcag_242_page_titled.description": "Zusammenfassung von Prüfungen, die sicherstellen, dass Dokumente einen aussagekräftigen Seitentitel haben.",
    "catalog.rules.wcag_247_focus_visible.title": "Fokus sichtbar",
    "catalog.rules.wcag_247_focus_visible.description": "Zusammenfassung von Prüfungen, die sicherstellen, dass der Tastaturfokus nicht verborgen ist und wahrnehmbar bleibt.",
    "catalog.rules.wcag_258_target_size_minimum.title": "Zielgröße: Minimum",
    "catalog.rules.wcag_258_target_size_minimum.description": "Zusammenfassung von Prüfungen, die sicherstellen, dass Zeigerziele die Mindestgrößenanforderungen erfüllen.",
    "catalog.rules.wcag_311_language_of_page.title": "Sprache der Seite",
    "catalog.rules.wcag_311_language_of_page.description": "Zusammenfassung von Prüfungen, die sicherstellen, dass die Sprache der Seite angegeben ist.",
    "catalog.rules.wcag_412_name.title": "Name, Rolle, Wert: zugänglicher Name",
    "catalog.rules.wcag_412_name.description": "Zusammenfassung von Prüfungen, die sicherstellen, dass gängige interaktive Elemente einen nicht leeren zugänglichen Namen aufweisen.",
    "labelInName_title": "Beschriftung im Namen: Der zugängliche Name enthält den sichtbaren Text",
    "labelInName_description": "Prüft, ob bei einem Formularelement mit sichtbarer Textbeschriftung der zugängliche Name diesen sichtbaren Beschriftungstext enthält (WCAG 2.5.3).",
    "labelInName_summary_fail": "{{element}}: Die sichtbare Beschriftung „{{visibleLabel}}“ (aus {{labelSource}}) ist nicht im zugänglichen Namen enthalten (aus {{nameMechanism}}).",
    "labelInName_hint_fail": "Aktualisieren Sie aria-label/aria-labelledby (oder den sichtbaren Beschriftungstext), damit der zugängliche Name den Wortlaut der sichtbaren Beschriftung enthält.",
    "labelInName_summary_cantTell": "{{element}}: Die sichtbare Beschriftung „{{visibleLabel}}“ (aus {{labelSource}}) unterscheidet sich vom zugänglichen Namen (aus {{nameMechanism}}) nur durch eine Abkürzung oder eine Bindestrichschreibung.",
    "labelInName_hint_cantTell": "Prüfen Sie manuell, ob beide Formulierungen übereinstimmen: Aus dem Markup lässt sich eine beabsichtigte Abkürzung nicht von einer Abweichung unterscheiden.",
    "ariaRolesValid_title": "Das role-Attribut muss eine gültige, nicht abstrakte ARIA-Rolle sein",
    "ariaRolesValid_description": "Prüft, ob ein explizites role=\"\"-Attribut zu einer echten, nicht abstrakten WAI-ARIA-Rolle aufgelöst wird.",
    "ariaRolesValid_summary_invalid": "role=\"{{role}}\" ist keine erkannte ARIA-Rolle.",
    "ariaRolesValid_hint_invalid": "Verwenden Sie einen gültigen ARIA-Rollenwert, oder entfernen Sie das role-Attribut, wenn keine Rolle zutrifft.",
    "ariaRolesValid_summary_abstract": "role=\"{{role}}\" ist eine abstrakte ARIA-Rolle, die nicht direkt verwendet werden darf.",
    "ariaRolesValid_hint_abstract": "Ersetzen Sie diese abstrakte Rolle durch eine konkrete Rolle, die zu dem Widget/der Struktur passt.",
    "ariaDeprecatedRole_title": "Das role-Attribut sollte keine veraltete oder für Autoren nicht empfohlene ARIA-Rolle verwenden",
    "ariaDeprecatedRole_description": "Prüft, ob ein explizites role=\"\"-Attribut keine durch die WAI-ARIA-Spezifikation als veraltet markierte Rolle verwendet, und keine Rolle, die für die interne Verwendung durch den User-Agent reserviert ist (z. B. role=\"generic\").",
    "ariaDeprecatedRole_summary_fail": "Dieses Element verwendet role=\"{{role}}\", was Autoren nicht explizit deklarieren dürfen.",
    "ariaDeprecatedRole_hint_fail": "{{guidance}}",
    "ariaDeprecatedRole_summary_cantTell": "Dieses Element verwendet role=\"{{role}}\", die in WAI-ARIA veraltet ist (weiterhin gültig, aber nicht empfohlen).",
    "ariaDeprecatedRole_summary_cantTell_discouraged": "Dieses Element verwendet role=\"{{role}}\", die User-Agents vorbehalten ist (weiterhin gültig, aber nicht empfohlen).",
    "ariaDeprecatedRole_hint_cantTell": "{{guidance}}",
    "ariaValidAttr_title": "aria-*-Attribute müssen echte, definierte ARIA-Attribute sein",
    "ariaValidAttr_description": "Prüft, ob jeder im DOM vorhandene aria-*-Attributname ein echtes, durch die WAI-ARIA-Spezifikation definiertes Attribut ist.",
    "ariaValidAttr_summary_fail": "{{attr}} ist kein erkanntes ARIA-Attribut.",
    "ariaValidAttr_hint_fail": "Korrigieren Sie den Attributnamen (prüfen Sie auf Tippfehler), oder entfernen Sie ihn, falls er nicht benötigt wird.",
    "ariaValidAttrValue_title": "Die Werte von aria-*-Attributen müssen ihrem deklarierten Typ entsprechen",
    "ariaValidAttrValue_description": "Prüft, ob jedes erkannte aria-*-Attribut einen Wert hat, der seinem WAI-ARIA-deklarierten Werttyp entspricht (boolesch, tristate, Token, Ganzzahl, Zahl oder ID-Referenz).",
    "ariaValidAttrValue_summary_fail": "{{attr}}=\"{{value}}\" ist kein gültiger Wert für dieses Attribut.",
    "ariaValidAttrValue_hint_fail": "Verwenden Sie einen Wert, der dem erwarteten Typ des Attributs entspricht (siehe WAI-ARIA-Spezifikation für dieses Attribut).",
    "ariaAllowedAttr_title": "aria-*-Attribute müssen für die Rolle des Elements zulässig sein",
    "ariaAllowedAttr_description": "Prüft, ob jedes erkannte aria-*-Attribut auf einem Element mit expliziter Rolle entweder global unterstützt wird oder von dieser Rolle unterstützt wird.",
    "ariaAllowedAttr_summary_fail": "{{attr}} ist bei role=\"{{role}}\" nicht zulässig.",
    "ariaAllowedAttr_hint_fail": "Entfernen Sie dieses Attribut, oder verwenden Sie eine Rolle, die es unterstützt.",
    "ariaAllowedAttr_summary_cantTell": "{{attr}} ist bei role=\"{{role}}\" veraltet (weiterhin zulässig, aber nicht empfohlen).",
    "ariaAllowedAttr_hint_cantTell": "Dieses Attribut wurde in ARIA 1.2 aus dem globalen Satz entfernt; entfernen Sie es oder verwenden Sie eine Rolle, die es unterstützt, da eine künftige ARIA-Version es möglicherweise nicht mehr zulässt.",
    "ariaProhibitedAttr_title": "ARIA-Benennungsattribute dürfen nicht bei Rollen verwendet werden, die sie untersagen",
    "ariaProhibitedAttr_description": "Prüft, ob aria-label/aria-labelledby nicht bei WAI-ARIA-Rollen vorhanden sind, deren Spezifikation die ARIA-Benennung ausdrücklich untersagt (z. B. generic, emphasis, strong, paragraph).",
    "ariaProhibitedAttr_summary_fail": "{{attr}} ist bei role=\"{{role}}\" untersagt.",
    "ariaProhibitedAttr_hint_fail": "Entfernen Sie dieses Attribut; diese Rolle darf keinen zugänglichen Namen tragen.",
    "ariaProhibitedAttr_summary_fail_roleless": "Dieses {{element}} hat keine Rolle und keine andere Quelle für einen zugänglichen Namen, sodass {{attr}} assistiven Technologien nicht zuverlässig zur Verfügung gestellt wird.",
    "ariaProhibitedAttr_hint_fail_roleless": "Geben Sie diesem Element eine Rolle, die einen zugänglichen Namen unterstützt (z. B. role=\"img\"/\"button\"), oder entfernen Sie dieses Attribut, wenn es ohne Rolle keinen Zweck erfüllt.",
    "ariaProhibitedAttr_summary_cantTell_roleless": "Dieses {{element}} hat keine Rolle, sodass {{attr}} von assistiven Technologien möglicherweise nicht als zugänglicher Name ausgegeben wird — der eigene Inhalt des Elements liefert jedoch bereits einen.",
    "ariaProhibitedAttr_hint_cantTell_roleless": "Prüfen Sie, ob der vorhandene Textinhalt bereits als Beschriftung dieses Elements dient; falls ja, ist das Benennungsattribut redundant, andernfalls geben Sie dem Element eine Rolle, die die Benennung unterstützt (z. B. role=\"img\").",
    "ariaRequiredAttr_title": "Rollen mit einem erforderlichen ARIA-Status/-Eigenschaft müssen diesen tragen",
    "ariaRequiredAttr_description": "Prüft, ob Elemente mit expliziter Rolle jeden eindeutigen, kontextunabhängigen erforderlichen aria-*-Status/-Eigenschaft für diese Rolle tragen (z. B. muss role=\"checkbox\" aria-checked haben).",
    "ariaRequiredAttr_summary_fail": "{{attr}} ist für role=\"{{role}}\" erforderlich, fehlt jedoch.",
    "ariaRequiredAttr_hint_fail": "Fügen Sie dieses Attribut mit einem für diese Rolle gültigen Wert hinzu.",
    "ariaAllowedRole_title": "Die explizite Rolle muss für ihr Host-Element zulässig sein",
    "ariaAllowedRole_description": "Prüft, ob ein explizites role=\"\"-Attribut eine der Rollen ist, die die ARIA-in-HTML-Spezifikation für das Host-Element zulässt (z. B. ist role=\"tab\" bei <nav> nicht zulässig).",
    "ariaAllowedRole_summary_fail": "role=\"{{role}}\" ist bei <{{element}}> nicht zulässig.",
    "ariaAllowedRole_hint_fail": "Verwenden Sie eine für dieses Element zulässige Rolle, oder ändern Sie das Host-Element.",
    "ariaRequiredChildren_title": "Container-Rollen müssen mindestens eine erforderliche Kind-Rolle besitzen",
    "ariaRequiredChildren_description": "Prüft, ob Container-Rollen mit einem dokumentierten Eintrag „erforderliche eigene Elemente“ (list, listbox, menu, radiogroup, table, grid, tablist, tree, row, …) mindestens einen Nachfahren oder ein über aria-owns referenziertes Element mit einer zulässigen eigenen Rolle enthalten.",
    "ariaRequiredChildren_summary_fail": "role=\"{{role}}\" besitzt kein eigenes Kind mit einer der erforderlichen Rollen: {{requiredRoles}}.",
    "ariaRequiredChildren_hint_fail": "Fügen Sie einen Nachfahren (oder ein über aria-owns referenziertes Element) mit einer der erforderlichen eigenen Rollen hinzu.",
    "ariaProhibitedChildren_title": "Container-Rollen dürfen kein Kind mit einer unzulässigen Rolle besitzen",
    "ariaProhibitedChildren_description": "Prüft, ob jedes im Accessibility-Baum zugehörige Kind einer Container-Rolle (list, listbox, menu, menubar, radiogroup, rowgroup, table, grid, treegrid, tablist, tree, row) eine der von dieser Rolle zulässigen eigenen Rollen hat.",
    "ariaProhibitedChildren_summary_fail": "Dieses Element hat role=\"{{childRole}}\", was kein zulässiges eigenes Kind des umschließenden Containers role=\"{{containerRole}}\" ist.",
    "ariaProhibitedChildren_hint_fail": "Entfernen oder ändern Sie diese Rolle, sodass sie einer der zulässigen eigenen Rollen des Containers entspricht ({{allowedRoles}}), oder verschieben Sie dieses Element außerhalb des {{containerRole}}-Containers.",
    "ariaProhibitedChildren_summary_fail_roleless": "Dieses Element hat keine explizite Rolle, trägt jedoch {{attr}}, wodurch es zu einem echten Knoten im Accessibility-Baum wird, der kein zulässiges eigenes Kind des umschließenden Containers role=\"{{containerRole}}\" ist.",
    "ariaProhibitedChildren_hint_fail_roleless": "Entfernen Sie {{attr}} (oder die Container-Zugehörigkeit zu role=\"{{containerRole}}\"), oder geben Sie diesem Element role=\"presentation\"/\"none\", falls es kein eigener Knoten im Accessibility-Baum sein soll.",
    "ariaProhibitedChildren_summary_fail_native_focusable": "Dieses Element hat keine explizite Rolle, ist jedoch nativ fokussierbar, wodurch es zu einem echten Knoten im Accessibility-Baum wird, der kein zulässiges eigenes Kind des umschließenden Containers role=\"{{containerRole}}\" ist.",
    "ariaProhibitedChildren_hint_fail_native_focusable": "Geben Sie diesem Element role=\"presentation\"/\"none\", entfernen Sie seine native Fokussierbarkeit (z. B. das href/tabindex-verleihende Attribut), oder verschieben Sie es außerhalb des {{containerRole}}-Containers.",
    "ariaRequiredParent_title": "Rollen, die einen bestimmten Kontext-Rolle erfordern, müssen sich in diesem Kontext befinden",
    "ariaRequiredParent_description": "Prüft, ob Rollen mit einem dokumentierten Eintrag „erforderliche Kontext-Rolle“ (listitem, option, tab, treeitem, row, cell, …) einen Vorfahren oder aria-owns-Eigentümer mit einer zulässigen Kontext-Rolle haben.",
    "ariaRequiredParent_summary_fail": "role=\"{{role}}\" erfordert eine Kontext-Rolle aus: {{requiredRoles}}, die nicht gefunden wurde.",
    "ariaRequiredParent_hint_fail": "Platzieren Sie dieses Element innerhalb eines Elements mit einer zulässigen Kontext-Rolle (oder referenzieren Sie es von dort über aria-owns).",
    "deprecatedElements_title": "Veraltete, nicht anhaltbare Elemente (<blink>, <marquee>) dürfen nicht verwendet werden",
    "deprecatedElements_description": "Prüft, ob veraltete, nicht standardisierte HTML-Elemente, deren blinkender/scrollender Inhalt vom Nutzer nicht angehalten, gestoppt oder ausgeblendet werden kann (<blink>, <marquee>), nicht vorhanden sind.",
    "deprecatedElements_summary_fail": "Der Inhalt von <{{element}}> kann vom Nutzer nicht angehalten, gestoppt oder ausgeblendet werden.",
    "deprecatedElements_hint_fail": "Entfernen Sie dieses Element; verwenden Sie stattdessen statischen Inhalt oder eine Animation mit einer für den Nutzer zugänglichen Pause-/Stopp-Steuerung.",
    "iframeNamePresent_title": "Frames haben einen zugänglichen Namen",
    "iframeNamePresent_description": "Prüft, ob <iframe>/<frame>-Elemente über aria-label, aria-labelledby oder das title-Attribut einen nicht leeren zugänglichen Namen aufweisen.",
    "iframeNamePresent_summary_fail": "Dieses <{{element}}> hat keinen zugänglichen Namen.",
    "iframeNamePresent_hint_fail": "Fügen Sie ein title-Attribut (oder aria-label/aria-labelledby) hinzu, das Inhalt oder Zweck des Frames beschreibt.",
    "iframeTitleUnique_title": "Frame-Titel müssen eindeutig sein",
    "iframeTitleUnique_description": "Prüft, ob nicht zwei <iframe>/<frame>-Elemente im betrachteten Bereich denselben title-Attributwert teilen.",
    "iframeTitleUnique_summary_fail": "Der Titel „{{title}}“ dieses <{{element}}> ist unter den Frames dieser Seite nicht eindeutig.",
    "iframeTitleUnique_hint_fail": "Geben Sie jedem Frame einen eigenen Titel, der seinen jeweiligen Inhalt oder Zweck beschreibt.",
    "iframeFocusableContent_title": "Frames mit tabindex=\"-1\" dürfen keinen fokussierbaren Inhalt enthalten",
    "iframeFocusableContent_description": "Prüft, ob <iframe>/<frame>-Elemente derselben Herkunft mit tabindex=\"-1\" keinen fokussierbaren Inhalt enthalten, da Browser diese Einschränkung nicht in das eingebettete Dokument des Frames übertragen.",
    "iframeFocusableContent_summary_fail": "Dieses <{{element}}> hat tabindex=\"-1\", aber sein Inhalt enthält fokussierbare Elemente, die weiterhin per Tastatur erreichbar sind.",
    "iframeFocusableContent_hint_fail": "Entfernen Sie den fokussierbaren Inhalt aus dem Frame, oder entfernen Sie tabindex=\"-1\", falls der Frame erreichbar sein soll.",
    "tableHeadersAttrValid_title": "Das headers-Attribut einer Tabellenzelle muss auf gültige Kopfzellen verweisen",
    "tableHeadersAttrValid_description": "Prüft, ob jede id im headers-Attribut eines <td>/<th> auf ein <th>-Element innerhalb derselben Tabelle aufgelöst wird (nicht fehlend, kein Nicht-th-Element, nicht sich selbst).",
    "tableHeadersAttrValid_summary_fail": "Das headers-Attribut dieses <{{element}}> verweist auf ungültige Kopfzelle(n): {{invalidIds}}.",
    "tableHeadersAttrValid_hint_fail": "Aktualisieren Sie das headers-Attribut so, dass jede id auf ein <th>-Element innerhalb derselben Tabelle verweist.",
    "tableThHasDataCells_title": "<th>-Elemente müssen mindestens eine Datenzelle beschreiben",
    "tableThHasDataCells_description": "Prüft, ob eine Tabelle, die <th>-Elemente enthält, auch mindestens eine <td>-Datenzelle enthält, die von diesen Kopfzeilen beschrieben wird.",
    "tableThHasDataCells_summary_fail": "Diese Tabelle hat Kopfzellen, aber keine Datenzellen, die sie beschreiben.",
    "tableThHasDataCells_hint_fail": "Fügen Sie der Tabelle Datenzellen (<td>) hinzu, oder entfernen Sie die Kopfzellen, wenn die Tabelle keine Daten enthält.",
    "ariaHiddenBody_title": "Das <body>-Element des Dokuments darf nicht aria-hidden sein",
    "ariaHiddenBody_description": "Prüft, ob <body> nicht aria-hidden=\"true\" hat, was die gesamte Seite aus dem Accessibility-Baum entfernen würde.",
    "ariaHiddenBody_summary_fail": "Der Dokumentkörper hat aria-hidden=\"true\", wodurch die gesamte Seite vor assistiven Technologien verborgen wird.",
    "ariaHiddenBody_hint_fail": "Entfernen Sie aria-hidden von <body>. Verbergen Sie stattdessen einzelne Elemente, falls das die Absicht war.",
    "listChildrenValid_title": "Listen dürfen direkt nur Listenelemente enthalten",
    "listChildrenValid_description": "Prüft, ob <ul>/<ol>-Elemente als direkte Kinder nur <li>, <script> oder <template> haben.",
    "listChildrenValid_summary_fail": "Dieses <{{element}}> enthält ein direktes Kind, das kein Listenelement ist: {{invalidChildren}}.",
    "listChildrenValid_hint_fail": "Verwenden Sie als direkte Kinder von <ul>/<ol> nur <li> (oder <script>/<template>); verschieben Sie anderes Markup in ein <li>.",
    "listitemParentValid_title": "Listenelemente müssen sich innerhalb eines Listen-Containers befinden",
    "listitemParentValid_description": "Prüft, ob <li>-Elemente von <ul>, <ol> oder einem Element mit role=\"list\" enthalten sind.",
    "listitemParentValid_summary_fail": "Das übergeordnete Element (<{{parentElement}}>) dieses Listenelements ist kein Listen-Container.",
    "listitemParentValid_hint_fail": "Platzieren Sie dieses <li> innerhalb eines <ul>/<ol>, oder geben Sie seinem übergeordneten Element role=\"list\".",
    "definitionListChildrenValid_title": "Beschreibungslisten müssen korrekt strukturiert sein",
    "definitionListChildrenValid_description": "Prüft, ob <dl>-Elemente direkt nur <dt>/<dd>-Gruppen (optional in einem einzigen <div> verpackt), <script>, <template> oder <style> enthalten.",
    "definitionListChildrenValid_summary_fail_invalidChild": "Diese Beschreibungsliste enthält ein direktes oder verpacktes Kind, das nicht Teil einer dt/dd-Gruppe ist: {{invalidChildren}}.",
    "definitionListChildrenValid_hint_fail_invalidChild": "Verwenden Sie innerhalb von <dl> nur <dt>/<dd> (optional in einem einzigen <div> verpackt), <script>, <template> oder <style>.",
    "definitionListChildrenValid_summary_fail_noDtDd": "Diese Beschreibungsliste hat keine <dt>/<dd>-Begriff-Definitions-Gruppe.",
    "definitionListChildrenValid_hint_fail_noDtDd": "Fügen Sie mindestens ein <dt>/<dd>-Paar innerhalb dieses <dl> hinzu.",
    "dlitemParentValid_title": "Beschreibungslisten-Elemente müssen sich innerhalb einer Beschreibungsliste befinden",
    "dlitemParentValid_description": "Prüft, ob <dt>/<dd>-Elemente von einem <dl> enthalten sind, direkt oder über ein umschließendes <div>.",
    "dlitemParentValid_summary_fail": "Das übergeordnete Element (<{{parentElement}}>) dieses <{{element}}> ist keine Beschreibungsliste.",
    "dlitemParentValid_hint_fail": "Platzieren Sie dieses <dt>/<dd> innerhalb eines <dl>, direkt oder in einem einzigen <div> verpackt.",
    "duplicateIdAria_title": "Von ARIA referenzierte IDs müssen eindeutig sein",
    "duplicateIdAria_description": "Prüft, ob jeder id-Wert, der durch ein ARIA-ID-Referenzattribut referenziert wird (aria-labelledby, aria-describedby, aria-owns, aria-controls, aria-activedescendant, aria-flowto, aria-errormessage, aria-details), im Dokument eindeutig ist.",
    "duplicateIdAria_summary_fail": "Die id „{{id}}“ wird von einem ARIA-Attribut referenziert, wird aber von {{duplicateCount}} Elementen verwendet.",
    "duplicateIdAria_hint_fail": "Machen Sie von ARIA-Attributen referenzierte ids innerhalb des Dokuments eindeutig.",
    "summaryNamePresent_title": "Summary-Elemente haben einen zugänglichen Namen",
    "summaryNamePresent_description": "Prüft, ob <summary>-Elemente einen nicht leeren zugänglichen Namen aufweisen.",
    "summaryNamePresent_summary_fail": "Dieses Summary-Element hat keinen zugänglichen Namen.",
    "summaryNamePresent_hint_fail": "Stellen Sie einen Summary-Text bereit, der nicht vor assistiven Technologien verborgen ist, oder stellen Sie aria-label oder aria-labelledby bereit.",
    "metaViewportZoomEnabled_title": "Der Viewport-Meta-Tag darf das Zoomen nicht deaktivieren",
    "metaViewportZoomEnabled_description": "Prüft, ob <meta name=\"viewport\"> nicht user-scalable=no oder maximum-scale unter 2 (200 %) setzt.",
    "metaViewportZoomEnabled_summary_fail": "Dieser Viewport-Meta-Tag schränkt die Zoom-Fähigkeit des Nutzers ein ({{reasons}}).",
    "metaViewportZoomEnabled_hint_fail": "Entfernen Sie user-scalable=no und jedes maximum-scale unter 2 aus dem Inhalt des Viewport-Meta-Tags.",
    "metaRefreshTimingAbsent_title": "Die Seite darf keinen zeitgesteuerten Meta-Refresh verwenden",
    "metaRefreshTimingAbsent_description": "Prüft, ob <meta http-equiv=\"refresh\"> keine positive Verzögerung von 20 Stunden oder weniger erzwingt.",
    "metaRefreshTimingAbsent_summary_fail": "Diese Seite lädt sich nach {{delay}} Sekunden automatisch neu.",
    "metaRefreshTimingAbsent_hint_fail": "Entfernen Sie den zeitgesteuerten Meta-Refresh, oder bieten Sie den Nutzern eine Möglichkeit, ihn zu deaktivieren, zu verlängern oder vor dem Auslösen zu pausieren.",
    "meterNamePresent_title": "Meter-Elemente haben einen zugänglichen Namen",
    "meterNamePresent_description": "Prüft, ob Elemente mit role=\"meter\" einen nicht leeren zugänglichen Namen aufweisen.",
    "meterNamePresent_summary_fail": "Dieses Meter-Element hat keinen zugänglichen Namen.",
    "meterNamePresent_hint_fail": "Stellen Sie aria-label, aria-labelledby oder ein title-Attribut bereit — sichtbarer Textinhalt wird bei diesem Meter-Element nicht als zugänglicher Name ausgegeben.",
    "progressbarNamePresent_title": "Fortschrittsanzeigen haben einen zugänglichen Namen",
    "progressbarNamePresent_description": "Prüft, ob Elemente mit role=\"progressbar\" einen nicht leeren zugänglichen Namen aufweisen.",
    "progressbarNamePresent_summary_fail": "Diese Fortschrittsanzeige hat keinen zugänglichen Namen.",
    "progressbarNamePresent_hint_fail": "Stellen Sie aria-label, aria-labelledby oder ein title-Attribut bereit — sichtbarer Textinhalt wird bei dieser Fortschrittsanzeige nicht als zugänglicher Name ausgegeben.",
    "tooltipNamePresent_title": "Tooltips haben einen zugänglichen Namen",
    "tooltipNamePresent_description": "Prüft, ob Elemente mit role=\"tooltip\" einen nicht leeren zugänglichen Namen aufweisen.",
    "tooltipNamePresent_summary_fail": "Dieser Tooltip hat keinen zugänglichen Namen.",
    "tooltipNamePresent_hint_fail": "Stellen Sie einen Tooltip-Text bereit, der nicht vor assistiven Technologien verborgen ist, oder stellen Sie aria-label oder aria-labelledby bereit.",
    "serverSideImageMapAbsent_title": "Bilder dürfen keine serverseitige Image-Map verwenden",
    "serverSideImageMapAbsent_description": "Prüft, ob <img>-Elemente nicht das ismap-Attribut tragen (serverseitige Image-Maps haben kein per Tastatur bedienbares Äquivalent).",
    "serverSideImageMapAbsent_summary_fail": "Dieses Bild verwendet eine serverseitige Image-Map, die kein per Tastatur bedienbares Äquivalent hat.",
    "serverSideImageMapAbsent_hint_fail": "Ersetzen Sie die serverseitige Image-Map (ismap) durch eine clientseitige Image-Map (<map>/<area>) oder separate zugängliche Links/Schaltflächen.",
    "formControlSingleLabel_title": "Formularelemente dürfen nicht mehrere Beschriftungen haben",
    "formControlSingleLabel_description": "Prüft, ob ein Formularelement mit höchstens einem <label> verknüpft ist (durch Umschließen oder durch label[for]).",
    "formControlSingleLabel_summary_fail": "Dieses <{{element}}> ist mit {{labelCount}} Beschriftungen verknüpft.",
    "formControlSingleLabel_hint_fail": "Behalten Sie nur ein <label> pro Formularelement (entweder umschließend oder über for/id referenziert).",
    "formControlSingleLabel_summary_cantTell": "Dieses <{{element}}> hat ein beschriftendes <label> und zusätzlich eine leere <label>-Verknüpfung; prüfen Sie, wie es angesagt wird.",
    "formControlSingleLabel_hint_cantTell": "Entfernen Sie das überflüssige leere <label>, sodass genau ein <label> mit dem Element verknüpft ist.",
    "nestedInteractiveControlsAbsent_title": "Interaktive Formularelemente dürfen nicht verschachtelt sein",
    "nestedInteractiveControlsAbsent_description": "Prüft, ob ein interaktives Element (Link, Schaltfläche, Formularelement oder ARIA-Widget-Rolle) kein weiteres interaktives Element enthält.",
    "nestedInteractiveControlsAbsent_summary_fail": "Dieses <{{element}}> enthält ein oder mehrere verschachtelte interaktive Elemente: {{nestedElements}}.",
    "nestedInteractiveControlsAbsent_hint_fail": "Verschieben Sie die verschachtelten interaktiven Elemente außerhalb dieses Elements; verschachtelte interaktive Elemente sind über assistive Technologien nicht zuverlässig bedienbar.",
    "bypassBlocksPresent_title": "Die Seite muss eine Möglichkeit bieten, wiederkehrende Blöcke zu überspringen",
    "bypassBlocksPresent_description": "Prüft, ob die Seite mindestens einen anerkannten Mechanismus nach WCAG 2.4.1 zum Überspringen wiederkehrender Blöcke hat: eine main-Landmarke, einen funktionierenden Anker-Link auf derselben Seite oder eine Überschrift.",
    "bypassBlocksPresent_summary_cantTell": "Auf dieser Seite wurde keine anerkannte Möglichkeit erkannt, wiederkehrende Inhaltsblöcke zu überspringen – prüfen Sie, ob ein Überspring-Mechanismus vorhanden ist.",
    "bypassBlocksPresent_hint_cantTell": "Bestätigen Sie, dass die Seite einen Überspring-Mechanismus bietet: eine main-Landmarke (<main> oder role=\"main\"), einen funktionierenden „Zum Inhalt springen“-Link oder Überschriften-Elemente, die assistive Technologien nutzen können, um wiederkehrende Inhalte zu überspringen. (Ein Mechanismus kann vorübergehend verborgen sein – z. B. während ein modaler Dialog die Seite inert macht – oder seitenweit bereitgestellt werden; dies erfordert eine menschliche Bestätigung.)",
    "landmarkBannerIsTopLevel_title": "Die banner-Landmarke muss auf oberster Ebene liegen",
    "landmarkBannerIsTopLevel_description": "Prüft, ob die banner-Landmarke (role=\"banner\" oder ein nicht verschachtelter <header>) nicht innerhalb einer anderen Landmarke verschachtelt ist.",
    "landmarkBannerIsTopLevel_summary_cantTell": "Diese banner-Landmarke ist innerhalb einer anderen Landmarke verschachtelt.",
    "landmarkBannerIsTopLevel_hint_cantTell": "Verschieben Sie die banner-Landmarke (header/role=\"banner\") so, dass sie nicht von einer anderen Landmarke umschlossen wird; eine banner-Landmarke sollte eine Region auf oberster Ebene der Seite sein.",
    "landmarkContentinfoIsTopLevel_title": "Die contentinfo-Landmarke muss auf oberster Ebene liegen",
    "landmarkContentinfoIsTopLevel_description": "Prüft, ob die contentinfo-Landmarke (role=\"contentinfo\" oder ein nicht verschachtelter <footer>) nicht innerhalb einer anderen Landmarke verschachtelt ist.",
    "landmarkContentinfoIsTopLevel_summary_cantTell": "Diese contentinfo-Landmarke ist innerhalb einer anderen Landmarke verschachtelt.",
    "landmarkContentinfoIsTopLevel_hint_cantTell": "Verschieben Sie die contentinfo-Landmarke (footer/role=\"contentinfo\") so, dass sie nicht von einer anderen Landmarke umschlossen wird; contentinfo sollte eine Region auf oberster Ebene der Seite sein.",
    "landmarkMainIsTopLevel_title": "Die main-Landmarke muss auf oberster Ebene liegen",
    "landmarkMainIsTopLevel_description": "Prüft, ob die main-Landmarke (role=\"main\" oder <main>) nicht innerhalb einer anderen Landmarke verschachtelt ist.",
    "landmarkMainIsTopLevel_summary_cantTell": "Diese main-Landmarke ist innerhalb einer anderen Landmarke verschachtelt.",
    "landmarkMainIsTopLevel_hint_cantTell": "Verschieben Sie die main-Landmarke (<main>/role=\"main\") so, dass sie nicht von einer anderen Landmarke umschlossen wird; main sollte eine Region auf oberster Ebene der Seite sein.",
    "landmarkNoDuplicateBanner_title": "Die Seite darf nicht mehr als eine banner-Landmarke haben",
    "landmarkNoDuplicateBanner_description": "Prüft, ob höchstens eine banner-Landmarke (role=\"banner\" oder ein nicht verschachtelter <header>) auf der Seite vorhanden ist.",
    "landmarkNoDuplicateBanner_summary_cantTell": "Diese Seite hat mehr als eine banner-Landmarke.",
    "landmarkNoDuplicateBanner_hint_cantTell": "Behalten Sie nur eine banner-Landmarke (header/role=\"banner\") pro Seite.",
    "landmarkNoDuplicateContentinfo_title": "Die Seite darf nicht mehr als eine contentinfo-Landmarke haben",
    "landmarkNoDuplicateContentinfo_description": "Prüft, ob höchstens eine contentinfo-Landmarke (role=\"contentinfo\" oder ein nicht verschachtelter <footer>) auf der Seite vorhanden ist.",
    "landmarkNoDuplicateContentinfo_summary_cantTell": "Diese Seite hat mehr als eine contentinfo-Landmarke.",
    "landmarkNoDuplicateContentinfo_hint_cantTell": "Behalten Sie nur eine contentinfo-Landmarke (footer/role=\"contentinfo\") pro Seite.",
    "landmarkNoDuplicateMain_title": "Die Seite darf nicht mehr als eine main-Landmarke haben",
    "landmarkNoDuplicateMain_description": "Prüft, ob höchstens eine main-Landmarke (role=\"main\" oder <main>) auf der Seite vorhanden ist.",
    "landmarkNoDuplicateMain_summary_cantTell": "Diese Seite hat mehr als eine main-Landmarke.",
    "landmarkNoDuplicateMain_hint_cantTell": "Behalten Sie nur eine main-Landmarke (<main>/role=\"main\") pro Seite.",
    "landmarkOneMain_title": "Die Seite sollte eine main-Landmarke haben",
    "landmarkOneMain_description": "Prüft, ob die Seite mindestens eine main-Landmarke (role=\"main\" oder <main>) hat.",
    "landmarkOneMain_summary_cantTell_missing": "Diese Seite hat keine main-Landmarke.",
    "landmarkOneMain_hint_cantTell_missing": "Fügen Sie eine main-Landmarke (<main> oder role=\"main\") um den primären Inhalt der Seite hinzu.",
    "landmarkUnique_title": "Landmarken mit derselben Rolle müssen eindeutige Namen haben",
    "landmarkUnique_description": "Prüft, ob bei zwei oder mehr Landmarken mit derselben Rolle jede einen eigenen zugänglichen Namen hat.",
    "landmarkUnique_summary_cantTell_duplicateName": "Diese {{role}}-Landmarke teilt ihren zugänglichen Namen mit einer anderen {{role}}-Landmarke.",
    "landmarkUnique_summary_cantTell_bothUnnamed": "Diese {{role}}-Landmarke hat keinen zugänglichen Namen, und es gibt mehr als eine unbenannte {{role}}-Landmarke auf dieser Seite.",
    "landmarkUnique_hint_cantTell": "Geben Sie jeder {{role}}-Landmarke über aria-label oder aria-labelledby einen eigenen Namen.",
    "emptyHeading_title": "Überschriften dürfen nicht leer sein",
    "emptyHeading_description": "Prüft, ob Überschriften-Elemente (<h1>-<h6> oder role=\"heading\") einen nicht leeren zugänglichen Namen haben.",
    "emptyHeading_summary_cantTell": "Diese Überschrift hat keinen zugänglichen Namen.",
    "emptyHeading_hint_cantTell": "Fügen Sie dieser Überschrift Textinhalt (oder aria-label/aria-labelledby) hinzu, oder entfernen Sie sie, wenn sie nicht benötigt wird.",
    "headingOrder_title": "Überschriftenebenen dürfen keine Ebene überspringen",
    "headingOrder_description": "Prüft, ob Überschriftenebenen in Dokumentreihenfolge jeweils höchstens um eine Ebene steigen.",
    "headingOrder_summary_cantTell": "Diese Überschrift springt von Ebene {{fromLevel}} auf Ebene {{toLevel}} und überspringt dabei eine Ebene.",
    "headingOrder_hint_cantTell": "Verwenden Sie aufeinanderfolgende Überschriftenebenen (überspringen Sie beim Tieferwerden keine Ebene), damit die Dokumentgliederung vorhersehbar bleibt.",
    "pageHasHeadingOne_title": "Die Seite sollte eine Überschrift der Ebene eins haben",
    "pageHasHeadingOne_description": "Prüft, ob die Seite mindestens eine Überschrift der Ebene eins hat (<h1> oder role=\"heading\" mit aria-level=\"1\").",
    "pageHasHeadingOne_summary_cantTell": "Diese Seite hat keine Überschrift der Ebene eins.",
    "pageHasHeadingOne_hint_cantTell": "Fügen Sie eine Überschrift der Ebene eins hinzu (<h1> oder role=\"heading\" aria-level=\"1\"), die den Hauptinhalt der Seite identifiziert.",
    "accesskeys_title": "accesskey-Werte müssen eindeutig sein",
    "accesskeys_description": "Prüft, ob nicht zwei Elemente auf der Seite denselben accesskey-Attributwert teilen.",
    "accesskeys_summary_cantTell": "Der accesskey dieses Elements wird mit einem anderen Element auf der Seite geteilt.",
    "accesskeys_hint_cantTell": "Machen Sie jeden accesskey-Wert auf der gesamten Seite eindeutig.",
    "scopeAttrValid_title": "Das scope-Attribut muss einen gültigen Wert haben",
    "scopeAttrValid_description": "Prüft, ob scope=\"…\" row, col, rowgroup oder colgroup ist.",
    "scopeAttrValid_summary_cantTell": "Dieser scope-Attributwert wird nicht erkannt.",
    "scopeAttrValid_hint_cantTell": "Verwenden Sie für das scope-Attribut row, col, rowgroup oder colgroup.",
    "tabindex_title": "tabindex sollte nicht größer als 0 sein",
    "tabindex_description": "Prüft, ob tabindex-Werte 0 oder negativ sind, nicht eine positive Zahl.",
    "tabindex_summary_cantTell": "Dieses Element hat einen positiven tabindex, der die natürliche Tab-Reihenfolge überschreibt.",
    "tabindex_hint_cantTell": "Verwenden Sie tabindex=\"0\" (oder einen negativen Wert, um es aus der Tab-Reihenfolge zu entfernen) anstelle einer positiven Zahl; korrigieren Sie stattdessen die DOM-Reihenfolge, wenn eine andere Tab-Reihenfolge benötigt wird.",
    "emptyTableHeader_title": "Tabellen-Kopfzellen dürfen nicht leer sein",
    "emptyTableHeader_description": "Prüft, ob <th>-Elemente sichtbaren Textinhalt haben — eine Kopfzelle, die nur über aria-label/aria-labelledby benannt ist, wird ebenfalls markiert, da die tatsächliche Unterstützung durch Screenreader/Browser dafür uneinheitlich ist.",
    "emptyTableHeader_summary_cantTell": "Diese Tabellen-Kopfzelle hat keinen zugänglichen Namen.",
    "emptyTableHeader_hint_cantTell": "Fügen Sie dieser Kopfzelle Textinhalt (oder aria-label/aria-labelledby) hinzu, oder entfernen Sie sie, wenn sie nicht benötigt wird.",
    "emptyTableHeader_summary_cantTell_ariaOnly": "Diese Tabellen-Kopfzelle hat keinen sichtbaren Text — ihr einziger zugänglicher Name stammt von aria-label/aria-labelledby, was bekanntermaßen von realen Screenreader-/Browser-Kombinationen (z. B. NVDA+Firefox, iOS VoiceOver+Safari) bei <th>-Elementen ignoriert wird.",
    "emptyTableHeader_hint_cantTell_ariaOnly": "Fügen Sie dieser Kopfzelle sichtbaren Textinhalt hinzu (zusätzlich zu, oder anstelle von, aria-label/aria-labelledby) — sichtbarer Text ist der einzige Benennungsmechanismus, der bei getesteten Screenreadern nachweislich funktioniert.",
    "labelTitleOnly_title": "Formularelemente sollten title nicht als einzige Beschriftung verwenden",
    "labelTitleOnly_description": "Prüft, ob ein Formularelement mit einem title-Attribut auch eine echte Beschriftung hat (label-Element, aria-label oder aria-labelledby).",
    "labelTitleOnly_summary_cantTell": "Dieses Formularelement stützt sich auf das title-Attribut als einzige Beschriftung.",
    "labelTitleOnly_hint_cantTell": "Fügen Sie zusätzlich zu, oder anstelle von, dem title-Attribut ein sichtbares <label> (oder aria-label/aria-labelledby) hinzu.",
    "imageRedundantAlt_title": "Der Alternativtext eines Bildes darf angrenzenden sichtbaren Text nicht duplizieren",
    "imageRedundantAlt_description": "Prüft, ob der Alternativtext eines <img> nicht mit anderem, bereits in seinem unmittelbaren übergeordneten Element vorhandenem sichtbaren Text identisch ist.",
    "imageRedundantAlt_summary_cantTell": "Der Alternativtext dieses Bildes dupliziert anderen sichtbaren Text direkt daneben.",
    "imageRedundantAlt_hint_cantTell": "Setzen Sie den Alternativtext auf leer (alt=\"\"), wenn das Bild neben dem Text rein dekorativ ist, oder entfernen Sie die redundante Duplizierung.",
    "tableDuplicateName_title": "Die Tabellenbeschriftung darf ihr summary-Attribut nicht duplizieren",
    "tableDuplicateName_description": "Prüft, ob der <caption>-Text einer <table> nicht identisch mit ihrem (veralteten) summary-Attribut ist.",
    "tableDuplicateName_summary_cantTell": "Die Beschriftung dieser Tabelle dupliziert ihr summary-Attribut.",
    "tableDuplicateName_hint_cantTell": "Entfernen Sie das redundante summary-Attribut, oder lassen Sie es andere Informationen als die Beschriftung liefern.",
    "metaViewportLarge_title": "Der Viewport-Meta-Tag sollte ein Zoomen bis zu 500 % erlauben",
    "metaViewportLarge_description": "Prüft, ob <meta name=\"viewport\"> nicht user-scalable=no oder maximum-scale unter 5 (500 %) setzt.",
    "metaViewportLarge_summary_cantTell": "Dieser Viewport-Meta-Tag schränkt den Zoom unter das empfohlene Ziel von 500 % ein.",
    "metaViewportLarge_hint_cantTell": "Entfernen Sie user-scalable=no und erhöhen Sie maximum-scale nach Möglichkeit auf mindestens 5 (500 %).",
    "presentationRoleConflict_title": "Eine präsentationale Rolle darf nicht mit einem globalen ARIA-Attribut oder Fokussierbarkeit im Konflikt stehen",
    "presentationRoleConflict_description": "Prüft, ob role=\"presentation\"/\"none\" (einschließlich der impliziten presentation-Rolle eines <img alt=\"\">) nicht mit einem globalen ARIA-Attribut (aria-label, aria-hidden, aria-describedby, …) oder Fokussierbarkeit (tabindex/nativ) kombiniert wird.",
    "presentationRoleConflict_summary_cantTell": "Dieses Element mit role=\"{{role}}\" hat außerdem eine widersprüchliche Bedingung ({{attrs}}), die seine implizite Rolle wiederherstellt und die präsentationale Absicht aufhebt.",
    "presentationRoleConflict_hint_cantTell": "Entfernen Sie das/die widersprüchliche(n) Benennungsattribut(e) und/oder die Fokussierbarkeit (tabindex/nativ), wenn das Element präsentational bleiben soll, oder entfernen Sie role=\"presentation\"/\"none\", wenn es assistiven Technologien zur Verfügung gestellt werden soll.",
    "region_title": "Der Seiteninhalt sollte sich innerhalb einer Landmarke befinden",
    "region_description": "Prüft, ob Inhalt unter <body> innerhalb einer Landmarke enthalten ist.",
    "region_summary_cantTell": "Dieser Inhalt befindet sich innerhalb keiner Landmarke.",
    "region_hint_cantTell": "Verschieben Sie diesen Inhalt in eine Landmarke (main, nav, aside, ein beschrifteter Abschnitt usw.).",
    "skipLink_title": "Ein Sprunglink muss ein auflösbares, nutzbares Ziel haben",
    "skipLink_description": "Prüft, ob das href-Fragment eines „Zu … springen“-Links auf ein reales, derzeit nutzbares Element im Dokument aufgelöst wird.",
    "skipLink_summary_cantTell": "Das Ziel dieses Sprunglinks existiert nicht.",
    "skipLink_hint_cantTell": "Verweisen Sie mit dem href des Sprunglinks auf eine im Dokument existierende id, oder fügen Sie das fehlende Zielelement hinzu.",
    "skipLink_summary_unusableTarget_cantTell": "Dieser Sprunglink zeigt auf ein Ziel, das existiert, aber derzeit nicht nutzbar ist.",
    "skipLink_hint_unusableTarget_cantTell": "Verweisen Sie mit diesem Sprunglink auf ein Ziel, das als Navigationsziel verfügbar und nutzbar ist.",
    "autocompleteValid_title": "Das autocomplete-Attribut muss ein gültiger Autofill-Wert sein",
    "autocompleteValid_description": "Prüft, ob ein nicht leeres autocomplete-Attribut „on“/„off“ oder eine wohlgeformte Liste von Autofill-Detail-Tokens ist.",
    "autocompleteValid_summary_fail": "Dieser autocomplete-Attributwert ist kein gültiger Autofill-Wert.",
    "autocompleteValid_hint_fail": "Verwenden Sie „on“/„off“ oder eine gültige Liste von Autofill-Tokens (z. B. „shipping street-address“, „cc-number“).",
    "htmlXmlLangMismatch_title": "lang und xml:lang dürfen sich nicht widersprechen",
    "htmlXmlLangMismatch_description": "Prüft, ob die Attribute lang und xml:lang des <html>-Elements dieselbe Hauptsprache deklarieren, wenn beide vorhanden sind.",
    "htmlXmlLangMismatch_summary_fail": "Die Attribute lang („{{lang}}“) und xml:lang („{{xmlLang}}“) deklarieren unterschiedliche Sprachen.",
    "htmlXmlLangMismatch_hint_fail": "Lassen Sie lang und xml:lang dieselbe Hauptsprache deklarieren, oder entfernen Sie das veraltete Attribut xml:lang.",
    "avoidInlineSpacing_title": "Inline-Stile dürfen den Textabstand nicht unter den WCAG-Wert erzwingen",
    "avoidInlineSpacing_description": "Prüft, dass ein per !important erzwungener Wert für line-height, letter-spacing oder word-spacing WCAG 1.4.12 bereits erfüllt, sodass dem Nutzer nichts zu überschreiben bleibt.",
    "avoidInlineSpacing_summary_fail": "Der Inline-Stil dieses Elements erzwingt {{properties}} mit !important und blockiert damit Überschreibungen des Textabstands durch den Nutzer.",
    "avoidInlineSpacing_hint_fail": "Entfernen Sie !important von line-height/letter-spacing/word-spacing in Inline-Stilen, damit Nutzer den Textabstand überschreiben können.",
    "metaRefreshNoExceptions_title": "Die Seite darf überhaupt keinen Meta-Refresh verwenden (AAA)",
    "metaRefreshNoExceptions_description": "Prüft, ob <meta http-equiv=\"refresh\"> unabhängig von der Verzögerung überhaupt nicht vorhanden ist — das strengere AAA-Gegenstück zur A-Prüfung, die nur positive Verzögerungen betrachtet.",
    "metaRefreshNoExceptions_summary_fail": "Diese Seite verwendet einen Meta-Refresh, eine automatische Kontextänderung, die nicht vom Nutzer ausgelöst wird.",
    "metaRefreshNoExceptions_hint_fail": "Entfernen Sie den Meta-Refresh; lösen Sie die Weiterleitung/Aktualisierung stattdessen nur als Reaktion auf eine Nutzeraktion aus.",
    "validLang_title": "Das lang-Attribut eines Elements muss syntaktisch gültig sein",
    "validLang_description": "Prüft, ob jedes Element (außer dem Wurzelelement <html>) mit einem nicht leeren lang-Attribut ein syntaktisch gültiges Sprach-Tag verwendet.",
    "validLang_summary_fail": "Dieser lang-Attributwert („{{value}}“) ist kein syntaktisch gültiges Sprach-Tag.",
    "validLang_hint_fail": "Verwenden Sie ein gültiges BCP-47-Sprach-Tag (z. B. „fr“, „es-MX“).",
    "linkInTextBlock_title": "Links in Textblöcken müssen sich vom umgebenden Text unterscheiden lassen, ohne sich allein auf Farbe zu verlassen",
    "linkInTextBlock_description": "Prüft, ob ein Link innerhalb eines Textabschnitts durch Unterstreichung, einen Unterschied in Schriftgewicht/-stil oder einen ausreichenden (>= 3:1) Farbkontrastunterschied visuell vom umgebenden Text unterscheidbar ist — nicht allein durch Farbe.",
    "linkInTextBlock_summary_fail": "Dieser Link in einem Textblock unterscheidet sich vom umgebenden Text ausschließlich durch Farbe.",
    "linkInTextBlock_hint_fail": "Fügen Sie eine Unterstreichung oder einen Unterschied in Schriftgewicht/-stil hinzu, oder erhöhen Sie den Farbkontrast zwischen Link und umgebendem Text auf mindestens 3:1.",
    "noAutoplayAudio_title": "Automatisch abgespieltes Audio sollte einen Mechanismus zum Pausieren/Stoppen oder zur Lautstärkeregelung bieten",
    "noAutoplayAudio_description": "Markiert <audio>/<video>-Elemente, die unstummgeschaltet automatisch abspielen und kein natives controls-Attribut haben, zur manuellen Überprüfung im Hinblick auf die 3-Sekunden-Ausnahme in WCAG 1.4.2.",
    "noAutoplayAudio_summary_cantTell": "Dieses Element spielt Audio automatisch ab, ohne einen nativen Mechanismus zum Pausieren/Stoppen oder zur Lautstärkeregelung.",
    "noAutoplayAudio_hint_cantTell": "Falls dieser Clip länger als 3 Sekunden abläuft, fügen Sie ein controls-Attribut (oder einen gleichwertigen benutzerdefinierten Mechanismus) hinzu, damit Nutzer ihn pausieren/stoppen oder seine Lautstärke unabhängig von der Systemlautstärke regeln können.",
    "videoCaption_title": "Voraufgezeichnetes Video sollte eine Untertitelspur bereitstellen",
    "videoCaption_description": "Markiert <video>-Elemente ohne <track kind=\"captions\"|\"subtitles\">-Kind zur manuellen Überprüfung, ob das Video eine Audiospur hat, die Untertitel benötigt.",
    "videoCaption_summary_cantTell": "Dieses Video hat keine Untertitelspur (captions oder subtitles).",
    "videoCaption_hint_cantTell": "Falls dieses Video eine informationstragende Audiospur hat, fügen Sie ein <track kind=\"captions\" src=\"…\"> mit dem untertitelten Inhalt hinzu.",
    "scrollableRegionFocusable_title": "Scrollbare Bereiche ohne fokussierbaren Inhalt sollten per Tastatur fokussierbar sein",
    "scrollableRegionFocusable_description": "Markiert Elemente, deren CSS overflow:auto/scroll deklariert, die keinen fokussierbaren Nachfahren enthalten und selbst nicht per Tastatur fokussierbar sind, zur manuellen Überprüfung, ob ihr Inhalt tatsächlich überläuft und Tastaturzugriff zum Scrollen benötigt.",
    "scrollableRegionFocusable_summary_cantTell": "Dieses Element deklariert overflow:auto/scroll, hat keinen fokussierbaren Nachfahren und ist selbst nicht per Tastatur fokussierbar.",
    "scrollableRegionFocusable_hint_cantTell": "Falls der Inhalt dieses Bereichs tatsächlich überläuft, fügen Sie tabindex=\"0\" (und eine geeignete Beschriftung) hinzu, damit Tastaturnutzer ihn fokussieren und mit den Pfeiltasten scrollen können.",
    "identicalLinksSamePurpose_title": "Links mit demselben zugänglichen Namen sollten zum selben Ziel führen",
    "identicalLinksSamePurpose_description": "Markiert Gruppen von Links, die denselben zugänglichen Namen teilen, aber zu mehr als einem unterschiedlichen Ziel führen, zur manuellen Überprüfung, ob sie demselben Zweck dienen.",
    "identicalLinksSamePurpose_summary_cantTell": "Dieser Link teilt einen zugänglichen Namen mit anderen Links auf der Seite, die zu einem anderen Ziel führen.",
    "identicalLinksSamePurpose_hint_cantTell": "Stellen Sie sicher, dass Links mit demselben Text demselben Zweck dienen, oder machen Sie den Linktext eindeutig genug, um jedes Ziel zu beschreiben.",
    "ariaBrailleEquivalent_title": "aria-braillelabel/aria-brailleroledescription müssen ein Nicht-Braille-Äquivalent haben",
    "ariaBrailleEquivalent_description": "Prüft, ob Elemente, die aria-braillelabel verwenden, auch einen regulären zugänglichen Namen haben, und Elemente, die aria-brailleroledescription verwenden, auch aria-roledescription haben.",
    "ariaBrailleEquivalent_summary_fail": "Dieses Element hat {{attr}}, aber nicht {{requires}}, sein Nicht-Braille-Äquivalent.",
    "ariaBrailleEquivalent_hint_fail": "{{attr}} ist eine Braille-spezifische Ergänzung, kein Ersatz — stellen Sie zusätzlich {{requires}} bereit.",
    "ariaConditionalAttr_title": "aria-errormessage erfordert, dass aria-invalid auf einen Wert ungleich false gesetzt ist",
    "ariaConditionalAttr_description": "Prüft, ob Elemente mit aria-errormessage auch aria-invalid auf „true“, „grammar“ oder „spelling“ gesetzt haben — andernfalls wird die Fehlermeldung aus dem Accessibility-Baum entfernt.",
    "ariaConditionalAttr_summary_fail": "Dieses Element hat aria-errormessage, aber aria-invalid fehlt oder ist „false“, sodass die Fehlermeldung nicht zur Verfügung gestellt wird.",
    "ariaConditionalAttr_hint_fail": "Setzen Sie aria-invalid auf „true“ (oder „grammar“/„spelling“), wann immer aria-errormessage assistiven Technologien zur Verfügung gestellt werden soll.",
    "ariaCheckedStateMismatch_title": "Das aria-checked eines nativen Kontrollkästchens/Optionsfelds sollte seinem tatsächlichen Zustand entsprechen",
    "ariaCheckedStateMismatch_description": "Markiert ein natives <input type=\"checkbox\">/<input type=\"radio\">, dessen expliziter aria-checked-Wert nicht mit seinem tatsächlichen aktivierten/unbestimmten Zustand übereinstimmt, zur manuellen Überprüfung.",
    "ariaCheckedStateMismatch_summary_cantTell": "Der aria-checked-Wert dieses Elements stimmt nicht mit seinem tatsächlichen aktivierten/unbestimmten Zustand überein.",
    "ariaCheckedStateMismatch_hint_cantTell": "Setzen Sie aria-checked passend zum tatsächlichen Zustand des Elements, oder entfernen Sie es — ein natives Kontrollkästchen/Optionsfeld stellt diesen Zustand bereits ohne dieses Attribut zur Verfügung.",
    "cssOrientationLock_title": "CSS darf die Seite nicht auf eine einzige Ausrichtung festlegen",
    "cssOrientationLock_description": "Prüft, ob keine @media (orientation: portrait|landscape)-Regel ein transform: rotate(...) auf der Seite setzt, eine bekannte Technik, um die Geräteausrichtung zu umgehen.",
    "cssOrientationLock_summary_fail": "Eine Media Query „{{mediaText}}“ dreht „{{selectorText}}“ und legt die Seite damit auf eine Ausrichtung fest.",
    "cssOrientationLock_hint_fail": "Entfernen Sie die rotate()-Transformation aus der Ausrichtungs-Media-Query; lassen Sie die Seite stattdessen natürlich auf die Geräteausrichtung reagieren, anstatt eine visuelle Drehung zu erzwingen.",
    "ariaText_title": "Elemente mit role=\"text\" sollten keine fokussierbaren Nachfahren haben",
    "ariaText_description": "Prüft, ob Elemente mit role=\"text\" keinen fokussierbaren Nachfahren enthalten (Link, Schaltfläche, Formularelement, tabindex, iframe oder contenteditable).",
    "ariaText_summary_cantTell": "Dieses Element mit role=\"text\" enthält einen fokussierbaren Nachfahren.",
    "ariaText_hint_cantTell": "Entfernen Sie role=\"text\" (oder entfernen Sie den fokussierbaren Nachfahren) — ein Bereich mit „reinem Text“ sollte keinen fokussierbaren Inhalt enthalten.",
    "focusOrderSemantics_title": "Zur Tab-Reihenfolge hinzugefügte Elemente sollten interaktive Semantik haben",
    "focusOrderSemantics_description": "Markiert Elemente mit tabindex >= 0, deren explizite Rolle eine nicht interaktive strukturelle/dokumentarische Rolle ist (z. B. heading, list, region, presentation), zur manuellen Überprüfung.",
    "focusOrderSemantics_summary_cantTell": "Dieses Element befindet sich in der Tab-Reihenfolge (tabindex=\"{{tabindex}}\"), hat jedoch eine nicht interaktive Rolle („{{role}}“).",
    "focusOrderSemantics_hint_cantTell": "Entfernen Sie tabindex, wenn dieses Element nicht interaktiv sein soll, oder verwenden Sie eine interaktive Rolle, die seinem tatsächlichen Verhalten entspricht.",
    "pAsHeading_title": "Ein <p>, das wie eine Überschrift aussieht, sollte wahrscheinlich eine echte Überschrift sein",
    "pAsHeading_description": "Markiert kurze <p>-Elemente, deren gesamter Text fett ist und in >= 18px dargestellt wird, zur manuellen Überprüfung, ob stattdessen ein echtes Überschriften-Element verwendet werden sollte.",
    "pAsHeading_summary_cantTell": "Dieser Absatz ist vollständig fett und wird in einer überschriftenähnlichen Größe dargestellt.",
    "pAsHeading_hint_cantTell": "Wenn dieser Text einen neuen Abschnitt einleitet, verwenden Sie ein echtes Überschriften-Element (<h1>-<h6> oder role=\"heading\"), anstatt einen Absatz so zu gestalten, dass er wie eine Überschrift aussieht.",
    "tableFakeCaption_title": "Die erste Zeile einer Tabelle sollte nicht eine echte <caption> ersetzen",
    "tableFakeCaption_description": "Markiert Tabellen ohne <caption>, deren erste Zeile eine einzelne nicht leere Zelle hat, während andere Zeilen mehrere Zellen haben, zur manuellen Überprüfung, ob diese Zelle als unechte Beschriftung fungiert.",
    "tableFakeCaption_summary_cantTell": "Diese Tabelle hat keine <caption>, aber ihre erste Zeile besteht aus einer einzelnen Zelle oberhalb von Zeilen mit mehreren Zellen — sie könnte als unechte Beschriftung fungieren.",
    "tableFakeCaption_hint_cantTell": "Wenn diese Zelle die Tabelle beschreiben soll, verwenden Sie ein echtes <caption>-Element anstelle einer einzelnen Zelle in der ersten Zeile.",
    "tdHasHeader_title": "Datenzellen in großen Tabellen müssen einen zugeordneten Header haben",
    "tdHasHeader_description": "Prüft, ob jedes <td> in einer großen, einfachen Tabelle (ohne colspan/rowspan) einen zugeordneten Header hat — über ein headers-Attribut, ein implizites Spalten-<th> darüber oder ein implizites Zeilen-<th> links davon.",
    "tdHasHeader_summary_fail": "Diese Datenzelle hat keinen zugeordneten Header (kein headers-Attribut, kein Spalten-<th> darüber, kein Zeilen-<th> links davon).",
    "tdHasHeader_hint_fail": "Fügen Sie ein headers-Attribut hinzu, das auf die relevante(n) <th>-id(s) verweist, oder strukturieren Sie die Tabelle so um, dass diese Zelle einen impliziten Zeilen-/Spalten-Header hat.",
    "mouseOnlyEventHandlers_title": "Nur für den Zeiger bestimmte Inline-Event-Handler sollten ein per Tastatur erreichbares Äquivalent haben",
    "mouseOnlyEventHandlers_description": "Markiert Elemente mit einem nur für den Zeiger bestimmten Inline-Event-Handler (onmouseover, onmouseout, onmousedown, onmouseup, ondblclick, onmousemove, onmouseenter, onmouseleave) ohne per Tastatur erreichbares Äquivalent (onkeydown/onkeyup/onkeypress/onfocus/onblur), zur manuellen Überprüfung.",
    "mouseOnlyEventHandlers_summary_cantTell": "Dieses Element hat {{attrs}}, aber keinen entsprechenden, per Tastatur erreichbaren Handler.",
    "mouseOnlyEventHandlers_hint_cantTell": "Fügen Sie onkeydown/onkeyup/onkeypress (oder onfocus/onblur für durch Hover ausgelöstes Verhalten) hinzu, damit diese Funktionalität auch per Tastatur erreichbar ist.",
    "linkNameQuality_title": "Der Linktext sollte aussagekräftig sein, nicht generisch",
    "linkNameQuality_description": "Markiert Links, deren vollständiger zugänglicher Name eine bekannte, wenig aussagekräftige Formulierung ist (z. B. „hier klicken“, „mehr erfahren“, „mehr“), zur manuellen Überprüfung, ob der Zweck ohne zusätzlichen Kontext klar ist.",
    "linkNameQuality_summary_cantTell": "Der zugängliche Name dieses Links („{{name}}“) ist eine generische, wenig aussagekräftige Formulierung.",
    "linkNameQuality_hint_cantTell": "Lassen Sie den Linktext selbst sein Ziel/seinen Zweck beschreiben (z. B. „Preisliste 2026 herunterladen“ statt „Herunterladen“), oder bestätigen Sie, dass der umgebende Kontext den Zweck bereits klar macht."
  },
  "en": {
    "img_altPresent_title": "<img> must have an alt attribute",
    "img_altPresent_description": "Checks that <img> elements provide an alt attribute to support a text alternative mechanism.",
    "img_altPresent_summary_fail": "Missing alt attribute on <img>.",
    "img_altPresent_hint_fail": "Add an alt attribute (use alt=\"\" only for decorative images).",
    "area_altPresent_title": "<area> must have an alt attribute",
    "area_altPresent_description": "Checks that <area> elements provide an alt attribute to support a text alternative mechanism.",
    "area_altPresent_summary_fail": "Missing alt attribute on <area>.",
    "area_altPresent_hint_fail": "Add an alt attribute (use alt=\"\" only for decorative areas).",
    "inputImage_altPresent_title": "<input type=\"image\"> must have an alt attribute",
    "inputImage_altPresent_description": "Checks that <input type=\"image\"> elements provide an alt attribute to support a text alternative mechanism.",
    "inputImage_altPresent_summary_fail": "Missing alt attribute on <input type=\"image\">.",
    "inputImage_altPresent_hint_fail": "Add an alt attribute (use alt=\"\" only when a separate accessible name is provided).",
    "inputImage_altPresent_summary_defaultName": "Accessible name is the browser default for an image button, which conveys nothing.",
    "inputImage_altPresent_hint_defaultName": "Replace it with text describing what the button does, for example \"Search\".",
    "inputImage_altPresent_summary_emptyAlt": "Empty alt=\"\" on <input type=\"image\"> leaves the control unnamed.",
    "inputImage_altPresent_hint_emptyAlt": "Describe the action in alt, or name the control with aria-label or aria-labelledby.",
    "ariaHidden_programmaticFocus_review_title": "Review aria-hidden programmatic focus",
    "ariaHidden_programmaticFocus_review_description": "Flags elements that are aria-hidden but considered eligible due to programmatic focus (e.g., tabindex < 0). Verify intended focus management and assistive technology exposure.",
    "ariaHidden_programmaticFocus_review_summary": "Review: aria-hidden element is programmatically focusable.",
    "ariaHidden_programmaticFocus_review_hint": "Check that focus management is intentional and that the element should remain hidden from assistive technologies.",
    "canvas_textAltPresent_title": "<canvas> must provide a text alternative",
    "canvas_textAltPresent_description": "Checks that <canvas> elements provide a text alternative via fallback content or an accessible name.",
    "canvas_textAltPresent_summary_fail": "Missing text alternative for <canvas>.",
    "canvas_textAltPresent_hint_fail": "Provide fallback text inside <canvas> or an accessible name (e.g., aria-label/aria-labelledby).",
    "svg_textAltPresent_title": "<svg> must provide a text alternative",
    "svg_textAltPresent_description": "Checks that inline <svg> elements provide a text alternative via a <title> element or an ARIA name (a <desc> element alone does not count).",
    "svg_textAltPresent_summary_fail": "Missing text alternative for <svg>.",
    "svg_textAltPresent_hint_fail": "Provide a <title> element with text, or an ARIA name (aria-label/aria-labelledby) — a <desc> element alone does not provide an accessible name.",
    "object_textAltPresent_title": "<object> must provide a text alternative",
    "object_textAltPresent_description": "Checks that <object> elements provide a text alternative via fallback content or an accessible name.",
    "object_textAltPresent_summary_fail": "Missing text alternative for <object>.",
    "object_textAltPresent_hint_fail": "Provide meaningful fallback content inside <object>, add an accessible name (aria-label/aria-labelledby), or use a title attribute as a best-effort fallback.",
    "embed_textAltPresent_title": "<embed> must provide a text alternative",
    "embed_textAltPresent_description": "Checks that <embed> elements provide a text alternative via an accessible name.",
    "embed_textAltPresent_summary_fail": "Missing text alternative for <embed>.",
    "embed_textAltPresent_hint_fail": "Add an accessible name to <embed> (aria-label/aria-labelledby preferred, or a title attribute as a best-effort fallback).",
    "img_altQuality_title": "<img> alt text must be appropriate (manual review)",
    "img_altQuality_description": "Flags <img> elements with non-empty alt text for human review of appropriateness.",
    "img_altQuality_summary_cantTell": "Review alt text on <img> for accuracy and appropriateness.",
    "img_altQuality_hint_cantTell": "Ensure the alt text conveys the image’s purpose/information in context (not redundant, not filename-like).",
    "img_altDecorative_title": "<img> with alt=\"\" must be decorative (manual review)",
    "img_altDecorative_description": "Flags <img> elements with empty alt for human review that they are purely decorative.",
    "img_altDecorative_summary_cantTell": "Review whether <img> is decorative (alt=\"\").",
    "img_altDecorative_hint_cantTell": "Confirm the image is purely decorative. If it conveys information or function, provide meaningful alt text.",
    "area_altQuality_title": "<area> alt text must be appropriate (manual review)",
    "area_altQuality_description": "Flags <area> elements with non-empty alt text for human review of appropriateness.",
    "area_altQuality_summary_cantTell": "Review alt text on <area> for accuracy and appropriateness.",
    "area_altQuality_hint_cantTell": "Ensure the alt text identifies the destination/action of the image map area in context.",
    "area_altDecorative_title": "<area> with alt=\"\" must be decorative (manual review)",
    "area_altDecorative_description": "Flags <area> elements with empty alt for human review that they are decorative/non-informative.",
    "area_altDecorative_summary_cantTell": "Review whether <area> is decorative (alt=\"\").",
    "area_altDecorative_hint_cantTell": "Confirm the area does not convey information or function. If it is interactive or meaningful, provide meaningful alt text.",
    "inputImage_altQuality_title": "<input type=\"image\"> alt text must be appropriate (manual review)",
    "inputImage_altQuality_description": "Flags <input type=\"image\"> elements with non-empty alt text for human review of appropriateness.",
    "inputImage_altQuality_summary_cantTell": "Review alt text on <input type=\"image\"> for accuracy and appropriateness.",
    "inputImage_altQuality_hint_cantTell": "Ensure the alt text describes the control’s action (e.g., “Search”, “Submit order”) in context.",
    "inputImage_altDecorative_title": "<input type=\"image\"> with alt=\"\" must be appropriate (manual review)",
    "inputImage_altDecorative_description": "Flags <input type=\"image\"> elements with empty alt for human review (usually not appropriate for functional controls).",
    "inputImage_altDecorative_summary_cantTell": "Review <input type=\"image\"> with alt=\"\".",
    "inputImage_altDecorative_hint_cantTell": "This control is typically functional. Confirm it has an equivalent accessible name elsewhere, or provide meaningful alt text.",
    "canvas_textAltQuality_title": "<canvas> text alternative must be appropriate (manual review)",
    "canvas_textAltQuality_description": "Flags <canvas> elements with a detected text alternative for human review of equivalence and appropriateness.",
    "canvas_textAltQuality_summary_cantTell": "Review text alternative for <canvas> for equivalence and appropriateness.",
    "canvas_textAltQuality_hint_cantTell": "Confirm the fallback text or accessible name conveys the same information/function as the canvas content.",
    "svg_textAltQuality_title": "<svg> text alternative must be appropriate (manual review)",
    "svg_textAltQuality_description": "Flags applicable <svg> graphics with a detected text alternative for human review of appropriateness.",
    "svg_textAltQuality_summary_cantTell": "Review text alternative for <svg> for accuracy and appropriateness.",
    "svg_textAltQuality_hint_cantTell": "Confirm the <title>/<desc> or ARIA name conveys the meaning/purpose of the graphic in context.",
    "object_textAltQuality_title": "<object> text alternative must be appropriate (manual review)",
    "object_textAltQuality_description": "Flags <object> elements with detected fallback or name for human review of equivalence and appropriateness.",
    "object_textAltQuality_summary_cantTell": "Review text alternative for <object> for equivalence and appropriateness.",
    "object_textAltQuality_hint_cantTell": "Confirm the fallback content or ARIA name provides an equivalent alternative for the embedded content.",
    "embed_textAltQuality_title": "<embed> text alternative must be appropriate (manual review)",
    "embed_textAltQuality_description": "Flags <embed> elements with a detected name for human review of appropriateness.",
    "embed_textAltQuality_summary_cantTell": "Review text alternative for <embed> for accuracy and appropriateness.",
    "embed_textAltQuality_hint_cantTell": "Confirm the ARIA name or title accurately identifies the embedded content in context.",
    "videoPoster_textAltPresent_title": "<video> poster must have a text alternative",
    "videoPoster_textAltPresent_description": "Checks that <video> elements with a poster image provide a text alternative (accessible name).",
    "videoPoster_textAltPresent_summary_fail": "Missing text alternative for <video> poster.",
    "videoPoster_textAltPresent_hint_fail": "Provide an accessible name for the poster image (aria-label/aria-labelledby preferred, or a title attribute as a fallback).",
    "svgImage_textAltPresent_title": "SVG <image> must have a text alternative",
    "svgImage_textAltPresent_description": "Checks that SVG <image> elements provide a text alternative via <title>/<desc> or an ARIA accessible name.",
    "svgImage_textAltPresent_summary_fail": "Missing text alternative on SVG <image>.",
    "svgImage_textAltPresent_hint_fail": "Add a <title> (and optionally <desc>) inside <image>, or provide aria-label/aria-labelledby.",
    "formControl_programmaticLabelPresent_title": "Form controls must have a programmatic label",
    "formControl_programmaticLabelPresent_description": "Checks that form controls have a programmatic label via <label>, aria-label, or aria-labelledby.",
    "formControl_programmaticLabelPresent_summary_fail": "Form control is missing a programmatic label.",
    "formControl_programmaticLabelPresent_hint_fail": "Provide a <label> association, aria-label, or aria-labelledby (placeholder/title do not count as labels).",
    "formControlAccessibleName_description": "Fails when an applicable form control has no accessible name (e.g., label, aria-label, aria-labelledby).",
    "formControlAccessibleName_hint_fail": "Provide an accessible name via a <label>, aria-label, or aria-labelledby.",
    "formControlAccessibleName_summary_fail": "Form control has no accessible name.",
    "formControlAccessibleName_title": "Form controls must have an accessible name",
    "linksTargetBlankNoopener_description": "Ensures links with target=\"_blank\" mitigate reverse tabnabbing risks.",
    "linksTargetBlankNoopener_hint_cantTell": "See guidance for this rule.",
    "linksTargetBlankNoopener_summary_cantTell": "Links that open in a new tab should use rel=\"noopener\"",
    "linksTargetBlankNoopener_title": "Links that open in a new tab should use rel=\"noopener\"",
    "manualReview_description": "Flags that a manual review of keyboard navigation and focus order is required.",
    "manualReview_hint_cantTell": "See guidance for this rule.",
    "manualReview_summary_cantTell": "Manual review: keyboard navigation and focus order",
    "manualReview_title": "Manual review: keyboard navigation and focus order",
    "rules.img-alt-suspicious.meta.title": "Suspicious alt text requires verification",
    "rules.img-alt-suspicious.meta.description": "Identifies images whose alt text matches common suspicious patterns (such as filenames, URLs, placeholders, or generic terms) and requires manual verification.",
    "rules.img-alt-suspicious.occurrence.cantTell.summary": "Image alt text appears suspicious (\"{{alt}}\" looks like {{pattern}}) and requires verification.",
    "rules.img-alt-suspicious.occurrence.cantTell.hint": "Review the alt text. Avoid filenames, URLs, placeholders, or generic terms, and ensure the text alternative describes the image’s purpose or function in context.",
    "formControl_programmaticLabelQuality_title": "Form controls should not rely on placeholder or title as the primary label",
    "formControl_programmaticLabelQuality_description": "Flags form controls whose computed accessible name relies on placeholder or title as the primary labeling method. Prefer <label> or aria-labelledby.",
    "formControl_programmaticLabelQuality_summary_cantTell": "Form control’s primary label is derived from {{methodLabel}}.",
    "formControl_programmaticLabelQuality_hint_cantTell": "Prefer a persistent <label> or aria-labelledby. Avoid relying on placeholder/title as the primary label.",
    "html_lang_attr_title": "Page language is declared",
    "html_lang_attr_description": "Checks that the default language of the page is programmatically declared.",
    "html_lang_attr_missing_absent": "The default language of the page is not declared.",
    "html_lang_attr_hint_missing_absent": "Add a lang attribute to the <html> element (for example: <html lang=\"en\">).",
    "html_lang_attr_missing_empty": "The default language of the page is declared but empty.",
    "html_lang_attr_hint_missing_empty": "Set a valid language value in the lang attribute of the <html> element (for example: <html lang=\"en\">).",
    "html_lang_attr_invalid": "The default language of the page is declared, but the value \"{{lang}}\" is not a valid language tag.",
    "html_lang_attr_hint_invalid": "Use a valid BCP 47 language tag in <html lang=\"…\"> (for example: \"en\", \"fr\", \"en-US\").",
    "mediaTranscriptPresent_title": "Time-based media: transcript or text alternative evidence",
    "mediaTranscriptPresent_description": "Finds audio and video elements where a transcript or other text alternative is not strongly evidenced in the page content. This rule is conservative and reports cantTell when evidence is missing or cannot be verified.",
    "mediaTranscriptPresent_summary_cantTell_missing": "A transcript or other text alternative for this <{{element}}> is not strongly evidenced on the page.",
    "mediaTranscriptPresent_hint_cantTell_missing": "Provide a clearly identified transcript or other text alternative for prerecorded audio-only or video-only media, for example a visible “Transcript” section or link.",
    "mediaTranscriptPresent_summary_cantTell_unverified": "A transcript or other text alternative may be available for this time-based media, but it could not be verified from the page content.",
    "mediaTranscriptPresent_hint_cantTell_unverified": "Ensure a clearly identified transcript or other text alternative is available and visibly or programmatically associated with the media on the page.",
    "pageTitlePresent_title": "Page has a non-empty title",
    "pageTitlePresent_description": "Checks that the page includes a non-empty <title> element that identifies the page.",
    "pageTitlePresent_summary_fail": "The page does not have a non-empty title.",
    "pageTitlePresent_hint_fail": "Add a <title> element with text that describes the page topic or purpose.",
    "pageTitlePatterns_title": "Page title patterns that may be insufficiently descriptive",
    "pageTitlePatterns_description": "Identifies page title patterns that may indicate low descriptiveness, such as generic, duplicated, or overly templated titles. This rule provides review signals and does not fail automatically.",
    "pageTitlePatterns_summary_cantTell": "The page title may not be descriptive enough to identify the page topic or purpose.",
    "pageTitlePresent_summary_fail_missing": "The page is missing a <title> element.",
    "pageTitlePresent_summary_fail_empty": "The page has an empty <title>.",
    "pageTitlePatterns_summary_cantTell_duplicateAcrossPages": "Several pages share the same title, which may make it harder to distinguish pages ({{duplicateGroups}} duplicate groups across {{pagesAnalyzed}} pages). Example: “{{exampleTitle}}”.",
    "pageTitlePatterns_summary_cantTell_templatedAcrossPages": "Many page titles appear highly templated, which may reduce how well titles distinguish pages ({{pagesAnalyzed}} pages).",
    "pageTitlePatterns_summary_cantTell_generic": "The page title is generic and may not identify the page topic or purpose.",
    "pageTitlePatterns_summary_cantTell_veryShort": "The page title is very short and may not identify the page topic or purpose.",
    "pageTitlePatterns_summary_cantTell_templateLike": "The page title appears templated and may not identify the page topic or purpose.",
    "pageTitlePatterns_hint_cantTell": "Review the page title and ensure it clearly identifies the page topic or purpose and helps distinguish the page from others.",
    "contrastComputable_title": "Color contrast is computable for rendered text",
    "contrastComputable_description": "Determines whether sufficient information is available to compute WCAG color contrast for visible text (e.g., no gradients/images/blend modes that make background indeterminate).",
    "contrastComputable_pass_allComputable": "Contrast is computable for all eligible text ({{eligibleTextCount}} text node(s)).",
    "contrastComputable_cantTell_generic": "Contrast may not be computable ({{reasonCode}}).",
    "contrastComputable_cantTell_bgImageOrGradient": "Contrast is not computable because the background uses an image or gradient ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgImage": "Contrast is not computable because the background uses an image ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgGradient": "Contrast is not computable because the background uses a gradient ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgImageAndGradient": "Contrast is not computable because the background uses an image and a gradient ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_mixBlendMode": "Contrast is not computable because mix-blend-mode is used ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_filter": "Contrast is not computable because filter/backdrop-filter is used ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_rootNotOpaque": "Contrast is not computable because the effective background is not fully opaque at the root (alpha={{backgroundAlpha}}).",
    "contrastComputable_cantTell_foregroundUnparsable": "Contrast is not computable because the computed foreground color could not be parsed.",
    "contrastComputable_cantTell_engineFailure": "Contrast computability could not be determined due to an internal engine error ({{reasonCode}}).",
    "contrastComputable_cantTell_backdropFilter": "Contrast is not computable because backdrop-filter is used ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_filterOrBackdropFilter": "Contrast is not computable because filter is used ({{blockerProperty}}={{blockerValue}}).",
    "contrastMinimum_title": "Text meets minimum color contrast (AA)",
    "contrastMinimum_description": "Checks that visible text has a contrast ratio of at least 4.5:1 (normal) or 3.0:1 (large), when contrast is computable from CSS.",
    "contrastMinimum_fail_belowThreshold": "Element has insufficient color contrast of {{ratio}}:1 (foreground: {{foregroundHex}}, background: {{backgroundHex}}, font size: {{fontSizePx}}px, font weight: {{fontWeightLabel}}). Expected contrast ratio of {{threshold}}:1 ({{#isLargeText}}large text{{/isLargeText}}{{^isLargeText}}normal text{{/isLargeText}}).",
    "contrastMinimum_pass_allAboveThreshold": "All computable text meets minimum contrast (AA). Eligible text nodes: {{eligibleTextCount}}. Computable: {{computableTextCount}}.",
    "contrastMinimum_notApplicable_noComputableText": "No eligible text had computable contrast (eligible text nodes: {{eligibleTextCount}}). See the contrast computability rule for details.",
    "contrastMinimum_cantTell_engineFailure": "Minimum contrast (AA) could not be determined due to an internal engine error ({{reasonCode}}).",
    "contrastEnhanced_title": "Text meets enhanced color contrast (AAA)",
    "contrastEnhanced_description": "Checks that visible text has a contrast ratio of at least 7.0:1 (normal) or 4.5:1 (large), when contrast is computable from CSS.",
    "contrastEnhanced_fail_belowThreshold": "Element has insufficient color contrast (AAA) of {{ratio}}:1 (foreground: {{foregroundHex}}, background: {{backgroundHex}}, font size: {{fontSizePx}}px, font weight: {{fontWeightLabel}}). Expected contrast ratio of {{threshold}}:1 ({{#isLargeText}}large text{{/isLargeText}}{{^isLargeText}}normal text{{/isLargeText}}).",
    "contrastEnhanced_pass_allAboveThreshold": "All computable text meets enhanced contrast (AAA). Eligible text nodes: {{eligibleTextCount}}. Computable: {{computableTextCount}}.",
    "contrastEnhanced_notApplicable_noComputableText": "No eligible text had computable contrast (eligible text nodes: {{eligibleTextCount}}). See the contrast computability rule for details.",
    "contrastEnhanced_cantTell_engineFailure": "Enhanced contrast (AAA) could not be determined due to an internal engine error ({{reasonCode}}).",
    "dom_textContrastMinimum_title": "Text must have sufficient contrast (minimum)",
    "dom_textContrastMinimum_description": "Checks visible text contrast against its computed background per WCAG 2.2 SC 1.4.3 (AA), using rendered styles (font size/weight) to determine the required ratio.",
    "dom_textContrastMinimum_summary_fail": "Insufficient text contrast: {{contrastRatio}}:1 (required {{requiredRatio}}:1). Foreground {{fgColor}} on background {{bgColor}}. Font {{fontSizePx}}px, weight {{fontWeight}}{{#isBold}}, bold{{/isBold}}{{#isLargeText}} (large text){{/isLargeText}}.",
    "dom_textContrastMinimum_hint_fail": "Adjust the foreground or background color so the contrast ratio is at least {{requiredRatio}}:1 for this text size/weight.",
    "dom_textContrastMinimum_summary_pass": "Text contrast OK: {{contrastRatio}}:1 (required {{requiredRatio}}:1). Foreground {{fgColor}} on background {{bgColor}}. Font {{fontSizePx}}px, weight {{fontWeight}}{{#isBold}}, bold{{/isBold}}{{#isLargeText}} (large text){{/isLargeText}}.",
    "dom_textContrastMinimum_summary_cantTell": "Could not reliably compute text contrast because the effective background is not deterministically resolvable (e.g. image, gradient, video, canvas, complex transparency, or blending).",
    "dom_textContrastMinimum_hint_cantTell": "Manually verify contrast where text overlays imagery/gradients/transparency; ensure it meets {{requiredRatio}}:1 for the computed text size/weight.",
    "dom_textContrastEnhanced_title": "Text must have sufficient contrast (enhanced)",
    "dom_textContrastEnhanced_description": "Checks visible text contrast against its computed background per WCAG 2.2 SC 1.4.6 (AAA), using rendered styles (font size/weight) to determine the required ratio.",
    "dom_textContrastEnhanced_summary_fail": "Insufficient enhanced text contrast: {{contrastRatio}}:1 (required {{requiredRatio}}:1). Foreground {{fgColor}} on background {{bgColor}}. Font {{fontSizePx}}px, weight {{fontWeight}}{{#isBold}}, bold{{/isBold}}{{#isLargeText}} (large text){{/isLargeText}}.",
    "dom_textContrastEnhanced_hint_fail": "Adjust the foreground or background color so the contrast ratio is at least {{requiredRatio}}:1 for enhanced (AAA) contrast.",
    "dom_textContrastEnhanced_summary_pass": "Enhanced text contrast OK: {{contrastRatio}}:1 (required {{requiredRatio}}:1). Foreground {{fgColor}} on background {{bgColor}}. Font {{fontSizePx}}px, weight {{fontWeight}}{{#isBold}}, bold{{/isBold}}{{#isLargeText}} (large text){{/isLargeText}}.",
    "dom_textContrastEnhanced_summary_cantTell": "Could not reliably compute enhanced text contrast because the effective background is not deterministically resolvable (e.g. image, gradient, video, canvas, complex transparency, or blending).",
    "dom_textContrastEnhanced_hint_cantTell": "Manually verify enhanced (AAA) contrast where text overlays imagery/gradients/transparency; ensure it meets {{requiredRatio}}:1 for the computed text size/weight.",
    "dom_nonTextContrast_title": "UI components and graphics must have sufficient contrast",
    "dom_nonTextContrast_description": "Checks contrast for non-text visual information (UI component boundaries, states, and meaningful graphical objects) per WCAG 2.2 SC 1.4.11 (AA).",
    "dom_nonTextContrast_summary_fail": "Insufficient non-text contrast: {{contrastRatio}}:1 (required {{requiredRatio}}:1). Foreground {{fgColor}} against background {{bgColor}}. Component: {{componentKind}}{{#componentState}} (state: {{componentState}}){{/componentState}}.",
    "dom_nonTextContrast_hint_fail": "Adjust the component/graphic colors so the contrast ratio is at least {{requiredRatio}}:1 for the perceivable boundary or essential visual information.",
    "dom_nonTextContrast_summary_pass": "Non-text contrast OK: {{contrastRatio}}:1 (required {{requiredRatio}}:1). Foreground {{fgColor}} against background {{bgColor}}. Component: {{componentKind}}{{#componentState}} (state: {{componentState}}){{/componentState}}.",
    "dom_nonTextContrast_summary_cantTell": "Could not reliably compute non-text contrast because the effective background or painted pixels are not deterministically resolvable (e.g. image/gradient/video/canvas, complex transparency, or blending).",
    "dom_nonTextContrast_hint_cantTell": "Manually verify the component/graphic contrast against adjacent colors; ensure it meets {{requiredRatio}}:1 for essential non-text visual information.",
    "contrastEnhanced_pass_allTextMeetsThreshold": "All computable text meets enhanced contrast (AAA).",
    "contrastMinimum_pass_allTextMeetsThreshold": "All computable text meets minimum contrast (AA).",
    "contrastComputable_cantTell_notComputable": "Contrast could not be computed for this text ({{reasonCode}}).",
    "roleImg_textAlternativePresent_title": "[role=\"img\"] must have an accessible text alternative",
    "roleImg_textAlternativePresent_description": "Checks that elements with role=\"img\" provide an accessible text alternative using aria-label, aria-labelledby, or a title attribute.",
    "roleImg_textAlternativePresent_summary_fail": "The element with role=\"img\" does not have an accessible text alternative.",
    "roleImg_textAlternativePresent_hint_fail": "Provide a text alternative using aria-label, or aria-labelledby that references non-empty text.",
    "targetSizeMinimum_title": "Pointer targets must be at least 24x24px large, or leave sufficient distance to other targets",
    "targetSizeMinimum_description": "Checks that pointer-operable targets have an effective hit region of at least 24 by 24 CSS pixels, or meet an allowed exception (e.g. sufficient spacing).",
    "targetSizeMinimum_summary_fail": "One or more pointer targets are smaller than 24×24 CSS px and are too close to another target.",
    "targetSizeMinimum_hint_fail": "Increase the target size to at least 24×24 CSS px or add sufficient spacing from neighboring targets.",
    "targetSizeMinimum_summary_cantTell_ambiguousSpacing": "Target may be too small and too close to another target, but the overlap is near the detection threshold and could not be confidently measured.",
    "targetSizeMinimum_hint_cantTell_ambiguousSpacing": "Manually verify the effective spacing between this target and its neighbor; increase target size or spacing if the overlap is real.",
    "targetSizeMinimum_summary_cantTell_plausiblyEssential": "Target is too small and too close to another target, but may be exempt as part of an essential graphic or image-map region.",
    "targetSizeMinimum_hint_cantTell_plausiblyEssential": "Verify whether this target’s size is genuinely essential to its function (e.g. part of an SVG/canvas/image map); if not, increase target size or spacing.",
    "targetSizeMinimum_notApplicable_noTargets": "No pointer-operable targets were eligible for evaluation.",
    "targetSizeMinimum_pass_allOk": "All eligible pointer targets meet the minimum size or a permitted exception.",
    "ariaHidden_focus_title": "ARIA hidden elements must not be focusable",
    "ariaHidden_focus_description": "Checks that aria-hidden=\"true\" elements are not focusable and do not contain focusable descendants.",
    "ariaHidden_focus_summary_fail_desc": "aria-hidden {{element}} contains {{focusableCount}} focusable element(s).",
    "ariaHidden_focus_summary_fail_self": "aria-hidden {{element}} is focusable ({{focusableCount}} focusable element(s)).",
    "ariaHidden_focus_summary_fail_self_and_desc": "aria-hidden {{element}} is focusable and contains {{descendantFocusableCount}} focusable descendant(s) ({{focusableCount}} focusable element(s) total).",
    "ariaHidden_focus_hint_fail": "Remove focusability from descendants or remove aria-hidden; ensure focus and accessibility trees stay aligned.",
    "ariaHidden_focus_summary_cantTell_redirect": "aria-hidden {{element}} received focus but focus moved immediately to another element. Verify sentinel/focus-trap behavior.",
    "ariaHidden_focus_hint_cantTell_redirect": "Verify this is an intentional focus sentinel/focus-trap handoff and that keyboard users never remain on hidden focus targets.",
    "cssHidden_focus_title": "Focusable elements must not be visually hidden",
    "cssHidden_focus_description": "Checks that keyboard-focusable elements are not visually hidden by CSS techniques that can leave them in the tab order.",
    "cssHidden_focus_summary_cantTell": "Focusable {{element}} is visually hidden ({{visibilityHints}}).",
    "cssHidden_focus_hint_cantTell": "Make the element visible when it can receive keyboard focus, or remove it from the tab order until it is visible.",
    "linkNamePresent_title": "Links have an accessible name",
    "linkNamePresent_description": "Checks that links expose a non-empty accessible name.",
    "linkNamePresent_summary_fail": "This link has no accessible name.",
    "linkNamePresent_hint_fail": "Provide link text or an accessible-name mechanism (for example aria-label) so assistive technologies can identify the link.",
    "buttonNamePresent_title": "Buttons have an accessible name",
    "buttonNamePresent_description": "Checks that buttons expose a non-empty accessible name.",
    "buttonNamePresent_summary_fail": "This button has no accessible name.",
    "buttonNamePresent_hint_fail": "Provide visible button text or a programmatic accessible-name mechanism (for example aria-label) so assistive technologies can identify the button.",
    "binaryControlNamePresent_title": "Binary controls have an accessible name",
    "binaryControlNamePresent_description": "Checks that checkbox, radio, and switch controls expose a non-empty accessible name.",
    "binaryControlNamePresent_summary_fail": "This control has no accessible name.",
    "binaryControlNamePresent_hint_fail": "Provide a label, aria-label, aria-labelledby, or other accessible-name mechanism so assistive technologies can identify the control.",
    "comboboxNamePresent_title": "Comboboxes have an accessible name",
    "comboboxNamePresent_description": "Checks that elements with role=\"combobox\" expose a non-empty accessible name.",
    "comboboxNamePresent_summary_fail": "This combobox has no accessible name.",
    "comboboxNamePresent_hint_fail": "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this combobox's accessible name.",
    "dialogNamePresent_title": "Dialogs have an accessible name",
    "dialogNamePresent_description": "Checks that elements with role=\"dialog\" or role=\"alertdialog\" expose a non-empty accessible name.",
    "dialogNamePresent_summary_fail": "This dialog has no accessible name.",
    "dialogNamePresent_hint_fail": "Provide aria-labelledby (preferred) or aria-label so assistive technologies can announce the dialog.",
    "menuitemNamePresent_title": "Menu items have an accessible name",
    "menuitemNamePresent_description": "Checks that menu items (role=\"menuitem*\", including checkbox/radio variants) expose a non-empty accessible name.",
    "menuitemNamePresent_summary_fail": "This menu item has no accessible name.",
    "menuitemNamePresent_hint_fail": "Provide visible text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.",
    "tabNamePresent_title": "Tabs have an accessible name",
    "tabNamePresent_description": "Checks that elements with role=\"tab\" expose a non-empty accessible name.",
    "tabNamePresent_summary_fail": "This tab has no accessible name.",
    "tabNamePresent_hint_fail": "Provide tab text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.",
    "sliderNamePresent_title": "Sliders have an accessible name",
    "sliderNamePresent_description": "Checks that sliders (input[type=\"range\"] and role=\"slider\") expose a non-empty accessible name.",
    "sliderNamePresent_summary_fail": "This slider has no accessible name.",
    "sliderNamePresent_hint_fail": "Provide a label, aria-label, or aria-labelledby so assistive technologies can identify the slider.",
    "textboxNamePresent_title": "Textboxes have an accessible name",
    "textboxNamePresent_description": "Checks that elements with role=\"textbox\" expose a non-empty accessible name.",
    "textboxNamePresent_summary_fail": "This textbox has no accessible name.",
    "textboxNamePresent_hint_fail": "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this textbox's accessible name.",
    "searchboxNamePresent_title": "Searchboxes have an accessible name",
    "searchboxNamePresent_description": "Checks that elements with role=\"searchbox\" expose a non-empty accessible name.",
    "searchboxNamePresent_summary_fail": "This searchbox has no accessible name.",
    "searchboxNamePresent_hint_fail": "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this searchbox's accessible name.",
    "spinbuttonNamePresent_title": "Spinbuttons have an accessible name",
    "spinbuttonNamePresent_description": "Checks that elements with role=\"spinbutton\" expose a non-empty accessible name.",
    "spinbuttonNamePresent_summary_fail": "This spinbutton has no accessible name.",
    "spinbuttonNamePresent_hint_fail": "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this spinbutton's accessible name.",
    "listboxNamePresent_title": "Listboxes have an accessible name",
    "listboxNamePresent_description": "Checks that elements with role=\"listbox\" expose a non-empty accessible name.",
    "listboxNamePresent_summary_fail": "This listbox has no accessible name.",
    "listboxNamePresent_hint_fail": "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this listbox's accessible name.",
    "optionNamePresent_title": "Options have an accessible name",
    "optionNamePresent_description": "Checks that elements with role=\"option\" expose a non-empty accessible name.",
    "optionNamePresent_summary_fail": "This option has no accessible name.",
    "optionNamePresent_hint_fail": "Provide option text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.",
    "treeitemNamePresent_title": "Tree items have an accessible name",
    "treeitemNamePresent_description": "Checks that elements with role=\"treeitem\" expose a non-empty accessible name.",
    "treeitemNamePresent_summary_fail": "This tree item has no accessible name.",
    "treeitemNamePresent_hint_fail": "Provide tree item text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.",
    "ariaRoleNamePresent_title": "ARIA widget/container roles have an accessible name",
    "ariaRoleNamePresent_description": "Checks that selected ARIA widget/container roles expose a non-empty accessible name.",
    "ariaRoleNamePresent_summary_fail": "This element has no accessible name.",
    "ariaRoleNamePresent_hint_fail": "Provide aria-label or aria-labelledby (preferred), or a non-empty title attribute.",
    "composite_rollup_summary": "Composite rule result: {{reasonCode}} ({{testCount}} checks)",
    "catalog.rules.wcag_111_non_text_content.title": "Non-text content: text alternatives",
    "catalog.rules.wcag_111_non_text_content.description": "Rollup of checks ensuring non-text content has an appropriate text alternative.",
    "catalog.rules.wcag_121_prerecorded_transcript.title": "Audio-only and video-only (prerecorded): transcript",
    "catalog.rules.wcag_121_prerecorded_transcript.description": "Rollup of checks for transcript availability for prerecorded audio-only or video-only media.",
    "catalog.rules.wcag_143_contrast_minimum.title": "Contrast: minimum",
    "catalog.rules.wcag_143_contrast_minimum.description": "Rollup of checks for minimum text contrast.",
    "catalog.rules.wcag_146_contrast_enhanced.title": "Contrast: enhanced",
    "catalog.rules.wcag_146_contrast_enhanced.description": "Rollup of checks for enhanced text contrast.",
    "catalog.rules.wcag_242_page_titled.title": "Page titled",
    "catalog.rules.wcag_242_page_titled.description": "Rollup of checks ensuring documents have a meaningful page title.",
    "catalog.rules.wcag_247_focus_visible.title": "Focus visible",
    "catalog.rules.wcag_247_focus_visible.description": "Rollup of checks ensuring keyboard focus is not hidden and remains perceivable.",
    "catalog.rules.wcag_258_target_size_minimum.title": "Target size: minimum",
    "catalog.rules.wcag_258_target_size_minimum.description": "Rollup of checks ensuring pointer targets meet minimum size requirements.",
    "catalog.rules.wcag_311_language_of_page.title": "Language of page",
    "catalog.rules.wcag_311_language_of_page.description": "Rollup of checks ensuring the page language is specified.",
    "catalog.rules.wcag_412_name.title": "Name, role, value: accessible name",
    "catalog.rules.wcag_412_name.description": "Rollup of checks that common interactive elements expose a non-empty accessible name.",
    "labelInName_title": "Label in Name: accessible name contains visible text",
    "labelInName_description": "Checks that when a control has a visible text label, the accessible name contains that visible label text (WCAG 2.5.3).",
    "labelInName_summary_fail": "{{element}}: visible label \"{{visibleLabel}}\" (from {{labelSource}}) is not included in the accessible name (from {{nameMechanism}}).",
    "labelInName_hint_fail": "Update aria-label/aria-labelledby (or the visible label text) so the accessible name includes the visible label wording.",
    "labelInName_summary_cantTell": "{{element}}: visible label \"{{visibleLabel}}\" (from {{labelSource}}) differs from the accessible name (from {{nameMechanism}}) only by an abbreviation or by hyphenation.",
    "labelInName_hint_cantTell": "Check by hand whether the two wordings match: markup cannot tell an intended abbreviation from a mismatch.",
    "ariaRolesValid_title": "role attribute must be a valid, non-abstract ARIA role",
    "ariaRolesValid_description": "Checks that an explicit role=\"\" attribute resolves to a real, non-abstract WAI-ARIA role.",
    "ariaRolesValid_summary_invalid": "role=\"{{role}}\" is not a recognized ARIA role.",
    "ariaRolesValid_hint_invalid": "Use a valid ARIA role token, or remove the role attribute if none applies.",
    "ariaRolesValid_summary_abstract": "role=\"{{role}}\" is an abstract ARIA role, which must not be used directly.",
    "ariaRolesValid_hint_abstract": "Replace this abstract role with a concrete role appropriate for the widget/structure.",
    "ariaDeprecatedRole_title": "role attribute should not use a deprecated or author-discouraged ARIA role",
    "ariaDeprecatedRole_description": "Checks that an explicit role=\"\" attribute does not use a role deprecated by the WAI-ARIA specification, or one reserved for user-agent-internal use (e.g. role=\"generic\").",
    "ariaDeprecatedRole_summary_fail": "This element uses role=\"{{role}}\", which authors must not explicitly declare.",
    "ariaDeprecatedRole_hint_fail": "{{guidance}}",
    "ariaDeprecatedRole_summary_cantTell": "This element uses role=\"{{role}}\", which is deprecated in WAI-ARIA (still valid, but discouraged).",
    "ariaDeprecatedRole_summary_cantTell_discouraged": "This element uses role=\"{{role}}\", which is reserved for user agents (still valid, but discouraged).",
    "ariaDeprecatedRole_hint_cantTell": "{{guidance}}",
    "ariaValidAttr_title": "aria-* attributes must be real, defined ARIA attributes",
    "ariaValidAttr_description": "Checks that every aria-* attribute name present in the DOM is a real attribute defined by the WAI-ARIA specification.",
    "ariaValidAttr_summary_fail": "{{attr}} is not a recognized ARIA attribute.",
    "ariaValidAttr_hint_fail": "Correct the attribute name (check for typos), or remove it if not needed.",
    "ariaValidAttrValue_title": "aria-* attribute values must match their declared type",
    "ariaValidAttrValue_description": "Checks that every recognized aria-* attribute has a value conforming to its WAI-ARIA-declared value type (boolean, tristate, token, integer, number, or ID reference).",
    "ariaValidAttrValue_summary_fail": "{{attr}}=\"{{value}}\" is not a valid value for this attribute.",
    "ariaValidAttrValue_hint_fail": "Use a value that matches the attribute’s expected type (see the WAI-ARIA specification for this attribute).",
    "ariaAllowedAttr_title": "aria-* attributes must be permitted for the element’s role",
    "ariaAllowedAttr_description": "Checks that every recognized aria-* attribute present on an element with an explicit role is either globally supported or supported by that role.",
    "ariaAllowedAttr_summary_fail": "{{attr}} is not permitted on role=\"{{role}}\".",
    "ariaAllowedAttr_hint_fail": "Remove this attribute, or use a role that supports it.",
    "ariaAllowedAttr_summary_cantTell": "{{attr}} is deprecated on role=\"{{role}}\" (still allowed, but discouraged).",
    "ariaAllowedAttr_hint_cantTell": "This attribute was removed from the ARIA global set in 1.2; remove it or use a role that supports it, as a future ARIA version may disallow it.",
    "ariaProhibitedAttr_title": "ARIA naming attributes must not be used on roles that prohibit them",
    "ariaProhibitedAttr_description": "Checks that aria-label/aria-labelledby are not present on WAI-ARIA roles whose specification explicitly prohibits ARIA naming (e.g. generic, emphasis, strong, paragraph).",
    "ariaProhibitedAttr_summary_fail": "{{attr}} is prohibited on role=\"{{role}}\".",
    "ariaProhibitedAttr_hint_fail": "Remove this attribute; this role must not carry an accessible name.",
    "ariaProhibitedAttr_summary_fail_roleless": "This {{element}} has no role and no other accessible-name source, so {{attr}} is not reliably exposed to assistive technology.",
    "ariaProhibitedAttr_hint_fail_roleless": "Give this element a role that supports an accessible name (e.g. role=\"img\"/\"button\"), or remove this attribute if it serves no purpose without one.",
    "ariaProhibitedAttr_summary_cantTell_roleless": "This {{element}} has no role, so {{attr}} may not be exposed as its accessible name by assistive technology — but the element's own content already provides one.",
    "ariaProhibitedAttr_hint_cantTell_roleless": "Verify whether the existing text content already serves as this element's label; if so the naming attribute is redundant, otherwise give the element a role that supports naming (e.g. role=\"img\").",
    "ariaRequiredAttr_title": "Roles with a required ARIA state/property must carry it",
    "ariaRequiredAttr_description": "Checks that elements with an explicit role carry every unambiguous, context-independent required aria-* state/property for that role (e.g. role=\"checkbox\" must have aria-checked).",
    "ariaRequiredAttr_summary_fail": "{{attr}} is required for role=\"{{role}}\", but is missing.",
    "ariaRequiredAttr_hint_fail": "Add this attribute with a valid value for this role.",
    "ariaAllowedRole_title": "Explicit role must be permitted for its host element",
    "ariaAllowedRole_description": "Checks that an explicit role=\"\" attribute is one of the roles the ARIA-in-HTML specification permits for the host element (e.g. role=\"tab\" is not permitted on <nav>).",
    "ariaAllowedRole_summary_fail": "role=\"{{role}}\" is not permitted on <{{element}}>.",
    "ariaAllowedRole_hint_fail": "Use a role permitted for this element, or change the host element.",
    "ariaRequiredChildren_title": "Container roles must own at least one required child role",
    "ariaRequiredChildren_description": "Checks that container roles with a documented \"required owned elements\" entry (list, listbox, menu, radiogroup, table, grid, tablist, tree, row, ...) contain at least one descendant or aria-owns-referenced element with an acceptable owned role.",
    "ariaRequiredChildren_summary_fail": "role=\"{{role}}\" has no owned child with one of the required roles: {{requiredRoles}}.",
    "ariaRequiredChildren_hint_fail": "Add a descendant (or aria-owns-referenced element) with one of the required owned roles.",
    "ariaProhibitedChildren_title": "Container roles must not own a child with a disallowed role",
    "ariaProhibitedChildren_description": "Checks that every accessible-tree-owned child of a container role (list, listbox, menu, menubar, radiogroup, rowgroup, table, grid, treegrid, tablist, tree, row) has one of that role's allowed owned roles.",
    "ariaProhibitedChildren_summary_fail": "This element has role=\"{{childRole}}\", which is not an allowed owned child of the enclosing role=\"{{containerRole}}\" container.",
    "ariaProhibitedChildren_hint_fail": "Remove or change this role so it matches one of the container's allowed owned roles ({{allowedRoles}}), or move this element outside the {{containerRole}} container.",
    "ariaProhibitedChildren_summary_fail_roleless": "This element has no explicit role but carries {{attr}}, making it a real accessible-tree node that is not an allowed owned child of the enclosing role=\"{{containerRole}}\" container.",
    "ariaProhibitedChildren_hint_fail_roleless": "Remove {{attr}} (or the role=\"{{containerRole}}\" container ownership), or give this element role=\"presentation\"/\"none\" if it isn't meant to be its own accessible-tree node.",
    "ariaProhibitedChildren_summary_fail_native_focusable": "This element has no explicit role but is natively focusable, making it a real accessible-tree node that is not an allowed owned child of the enclosing role=\"{{containerRole}}\" container.",
    "ariaProhibitedChildren_hint_fail_native_focusable": "Give this element role=\"presentation\"/\"none\", remove its native focusability (e.g. drop the href/tabindex-granting attribute), or move it outside the {{containerRole}} container.",
    "ariaRequiredParent_title": "Roles requiring a specific context role must be in that context",
    "ariaRequiredParent_description": "Checks that roles with a documented \"required context role\" entry (listitem, option, tab, treeitem, row, cell, ...) have an ancestor or aria-owns owner with an acceptable context role.",
    "ariaRequiredParent_summary_fail": "role=\"{{role}}\" requires a context role of one of: {{requiredRoles}}, which was not found.",
    "ariaRequiredParent_hint_fail": "Place this element inside (or aria-owns-reference it from) an element with an acceptable context role.",
    "deprecatedElements_title": "Obsolete non-stoppable elements (<blink>, <marquee>) must not be used",
    "deprecatedElements_description": "Checks that deprecated, non-standard HTML elements whose blinking/scrolling content cannot be paused, stopped, or hidden by the user (<blink>, <marquee>) are not present.",
    "deprecatedElements_summary_fail": "<{{element}}> content cannot be paused, stopped, or hidden by the user.",
    "deprecatedElements_hint_fail": "Remove this element; use static content, or an animation with a user-facing pause/stop control, instead.",
    "iframeNamePresent_title": "Frames have an accessible name",
    "iframeNamePresent_description": "Checks that <iframe>/<frame> elements expose a non-empty accessible name via aria-label, aria-labelledby, or the title attribute.",
    "iframeNamePresent_summary_fail": "This <{{element}}> has no accessible name.",
    "iframeNamePresent_hint_fail": "Add a title attribute (or aria-label/aria-labelledby) describing the frame’s content or purpose.",
    "iframeTitleUnique_title": "Frame titles must be unique",
    "iframeTitleUnique_description": "Checks that no two <iframe>/<frame> elements in scope share the same title attribute value.",
    "iframeTitleUnique_summary_fail": "This <{{element}}>'s title \"{{title}}\" is not unique among the frames on this page.",
    "iframeTitleUnique_hint_fail": "Give each frame a distinct title describing its specific content or purpose.",
    "iframeFocusableContent_title": "Frames with tabindex=\"-1\" must not contain focusable content",
    "iframeFocusableContent_description": "Checks that same-origin <iframe>/<frame> elements with tabindex=\"-1\" do not contain focusable content, since browsers do not propagate that restriction into the frame’s embedded document.",
    "iframeFocusableContent_summary_fail": "This <{{element}}> has tabindex=\"-1\" but its content contains focusable elements, which remain reachable by keyboard.",
    "iframeFocusableContent_hint_fail": "Remove focusable content from the frame, or remove tabindex=\"-1\" if the frame is meant to be reachable.",
    "tableHeadersAttrValid_title": "Table cell \"headers\" attribute must reference valid header cells",
    "tableHeadersAttrValid_description": "Checks that each id in a <td>/<th> headers attribute resolves to a <th> element within the same table (not missing, not a non-th element, not itself).",
    "tableHeadersAttrValid_summary_fail": "This <{{element}}>'s headers attribute references invalid header cell(s): {{invalidIds}}.",
    "tableHeadersAttrValid_hint_fail": "Update the headers attribute so every id refers to a <th> element within the same table.",
    "tableThHasDataCells_title": "<th> elements must describe at least one data cell",
    "tableThHasDataCells_description": "Checks that a table containing <th> elements also contains at least one <td> data cell for those headers to describe.",
    "tableThHasDataCells_summary_fail": "This table has header cells but no data cells for them to describe.",
    "tableThHasDataCells_hint_fail": "Add data cells (<td>) to the table, or remove the header cells if the table has no data.",
    "ariaHiddenBody_title": "The document <body> must not be aria-hidden",
    "ariaHiddenBody_description": "Checks that <body> does not have aria-hidden=\"true\", which would remove the entire page from the accessibility tree.",
    "ariaHiddenBody_summary_fail": "The document body has aria-hidden=\"true\", which hides the entire page from assistive technologies.",
    "ariaHiddenBody_hint_fail": "Remove aria-hidden from <body>. Hide specific elements instead, if that was the intent.",
    "listChildrenValid_title": "Lists must only directly contain list items",
    "listChildrenValid_description": "Checks that <ul>/<ol> elements only have <li>, <script>, or <template> as direct children.",
    "listChildrenValid_summary_fail": "This <{{element}}> contains a direct child that is not a list item: {{invalidChildren}}.",
    "listChildrenValid_hint_fail": "Only use <li> (or <script>/<template>) as direct children of <ul>/<ol>; move other markup inside an <li>.",
    "listitemParentValid_title": "List items must be inside a list container",
    "listitemParentValid_description": "Checks that <li> elements are contained by <ul>, <ol>, or an element with role=\"list\".",
    "listitemParentValid_summary_fail": "This list item's parent (<{{parentElement}}>) is not a list container.",
    "listitemParentValid_hint_fail": "Place this <li> inside a <ul>/<ol>, or give its parent role=\"list\".",
    "definitionListChildrenValid_title": "Description lists must be structured correctly",
    "definitionListChildrenValid_description": "Checks that <dl> elements only directly contain <dt>/<dd> groups (optionally wrapped in one <div>), <script>, <template>, or <style>.",
    "definitionListChildrenValid_summary_fail_invalidChild": "This description list contains a direct or wrapped child that is not part of a dt/dd group: {{invalidChildren}}.",
    "definitionListChildrenValid_hint_fail_invalidChild": "Only use <dt>/<dd> (optionally wrapped in one <div>), <script>, <template>, or <style> inside <dl>.",
    "definitionListChildrenValid_summary_fail_noDtDd": "This description list has no <dt>/<dd> term-definition group.",
    "definitionListChildrenValid_hint_fail_noDtDd": "Add at least one <dt>/<dd> pair inside this <dl>.",
    "dlitemParentValid_title": "Description-list items must be inside a description list",
    "dlitemParentValid_description": "Checks that <dt>/<dd> elements are contained by a <dl>, directly or via one wrapping <div>.",
    "dlitemParentValid_summary_fail": "This <{{element}}>'s parent (<{{parentElement}}>) is not a description list.",
    "dlitemParentValid_hint_fail": "Place this <dt>/<dd> inside a <dl>, directly or wrapped in a single <div>.",
    "duplicateIdAria_title": "IDs referenced by ARIA must be unique",
    "duplicateIdAria_description": "Checks that any id value referenced by an ARIA ID-reference attribute (aria-labelledby, aria-describedby, aria-owns, aria-controls, aria-activedescendant, aria-flowto, aria-errormessage, aria-details) is unique in the document.",
    "duplicateIdAria_summary_fail": "The id \"{{id}}\" is referenced by an ARIA attribute but is used by {{duplicateCount}} elements.",
    "duplicateIdAria_hint_fail": "Make ids referenced by ARIA attributes unique within the document.",
    "summaryNamePresent_title": "Summary elements have an accessible name",
    "summaryNamePresent_description": "Checks that <summary> elements expose a non-empty accessible name.",
    "summaryNamePresent_summary_fail": "This summary has no accessible name.",
    "summaryNamePresent_hint_fail": "Provide summary text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.",
    "metaViewportZoomEnabled_title": "Viewport meta tag must not disable zoom",
    "metaViewportZoomEnabled_description": "Checks that <meta name=\"viewport\"> does not set user-scalable=no or maximum-scale below 2 (200%).",
    "metaViewportZoomEnabled_summary_fail": "This viewport meta tag restricts the user's ability to zoom ({{reasons}}).",
    "metaViewportZoomEnabled_hint_fail": "Remove user-scalable=no and any maximum-scale below 2 from the viewport meta content.",
    "metaRefreshTimingAbsent_title": "Page must not use a timed meta refresh",
    "metaRefreshTimingAbsent_description": "Checks that <meta http-equiv=\"refresh\"> does not impose a positive delay of 20 hours or less.",
    "metaRefreshTimingAbsent_summary_fail": "This page refreshes itself automatically after {{delay}} seconds.",
    "metaRefreshTimingAbsent_hint_fail": "Remove the timed meta refresh, or provide a way for users to turn it off, extend it, or pause it before it triggers.",
    "meterNamePresent_title": "Meters have an accessible name",
    "meterNamePresent_description": "Checks that elements with role=\"meter\" expose a non-empty accessible name.",
    "meterNamePresent_summary_fail": "This meter has no accessible name.",
    "meterNamePresent_hint_fail": "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this meter's accessible name.",
    "progressbarNamePresent_title": "Progress bars have an accessible name",
    "progressbarNamePresent_description": "Checks that elements with role=\"progressbar\" expose a non-empty accessible name.",
    "progressbarNamePresent_summary_fail": "This progress bar has no accessible name.",
    "progressbarNamePresent_hint_fail": "Provide aria-label, aria-labelledby, or a title attribute — visible text content is not exposed as this progress bar's accessible name.",
    "tooltipNamePresent_title": "Tooltips have an accessible name",
    "tooltipNamePresent_description": "Checks that elements with role=\"tooltip\" expose a non-empty accessible name.",
    "tooltipNamePresent_summary_fail": "This tooltip has no accessible name.",
    "tooltipNamePresent_hint_fail": "Provide tooltip text that is not hidden from assistive technologies, or provide aria-label or aria-labelledby.",
    "serverSideImageMapAbsent_title": "Images must not use a server-side image map",
    "serverSideImageMapAbsent_description": "Checks that <img> elements do not carry the ismap attribute (server-side image maps have no keyboard-operable equivalent).",
    "serverSideImageMapAbsent_summary_fail": "This image uses a server-side image map, which has no keyboard-operable equivalent.",
    "serverSideImageMapAbsent_hint_fail": "Replace the server-side image map (ismap) with a client-side image map (<map>/<area>) or separate accessible links/buttons.",
    "formControlSingleLabel_title": "Form controls must not have multiple labels",
    "formControlSingleLabel_description": "Checks that a form control is associated with at most one <label> (by wrapping or by label[for]).",
    "formControlSingleLabel_summary_fail": "This <{{element}}> is associated with {{labelCount}} labels.",
    "formControlSingleLabel_hint_fail": "Keep only one <label> per form control (either wrapping it or referencing it via for/id).",
    "formControlSingleLabel_summary_cantTell": "This <{{element}}> has one labelling <label> plus an extra empty <label> association; verify how it is announced.",
    "formControlSingleLabel_hint_cantTell": "Remove the redundant empty <label> so exactly one <label> is associated with the control.",
    "nestedInteractiveControlsAbsent_title": "Interactive controls must not be nested",
    "nestedInteractiveControlsAbsent_description": "Checks that an interactive control (link, button, form control, or ARIA widget role) does not contain another interactive control.",
    "nestedInteractiveControlsAbsent_summary_fail": "This <{{element}}> contains one or more nested interactive controls: {{nestedElements}}.",
    "nestedInteractiveControlsAbsent_hint_fail": "Move the nested interactive control(s) outside this element; nested interactive controls are not reliably operable via assistive technology.",
    "bypassBlocksPresent_title": "Page must provide a way to bypass repeated blocks",
    "bypassBlocksPresent_description": "Checks that the page has at least one recognized WCAG 2.4.1 bypass-blocks mechanism: a main landmark, a working same-page anchor link, or a heading.",
    "bypassBlocksPresent_summary_cantTell": "No recognized way to bypass repeated blocks of content was detected on this page — verify a bypass mechanism exists.",
    "bypassBlocksPresent_hint_cantTell": "Confirm the page offers a bypass mechanism: a main landmark (<main> or role=\"main\"), a working \"skip to content\" link, or heading elements that assistive technology can use to jump past repeated content. (A mechanism may be temporarily hidden — e.g. while a modal dialog makes the page inert — or provided on a per-site basis; this needs human confirmation.)",
    "landmarkBannerIsTopLevel_title": "Banner landmark must be top-level",
    "landmarkBannerIsTopLevel_description": "Checks that the banner landmark (role=\"banner\" or a non-nested <header>) is not nested inside another landmark region.",
    "landmarkBannerIsTopLevel_summary_cantTell": "This banner landmark is nested inside another landmark region.",
    "landmarkBannerIsTopLevel_hint_cantTell": "Move the banner landmark (header/role=\"banner\") so it is not contained by another landmark; a banner should be a top-level region of the page.",
    "landmarkContentinfoIsTopLevel_title": "Contentinfo landmark must be top-level",
    "landmarkContentinfoIsTopLevel_description": "Checks that the contentinfo landmark (role=\"contentinfo\" or a non-nested <footer>) is not nested inside another landmark region.",
    "landmarkContentinfoIsTopLevel_summary_cantTell": "This contentinfo landmark is nested inside another landmark region.",
    "landmarkContentinfoIsTopLevel_hint_cantTell": "Move the contentinfo landmark (footer/role=\"contentinfo\") so it is not contained by another landmark; contentinfo should be a top-level region of the page.",
    "landmarkMainIsTopLevel_title": "Main landmark must be top-level",
    "landmarkMainIsTopLevel_description": "Checks that the main landmark (role=\"main\" or <main>) is not nested inside another landmark region.",
    "landmarkMainIsTopLevel_summary_cantTell": "This main landmark is nested inside another landmark region.",
    "landmarkMainIsTopLevel_hint_cantTell": "Move the main landmark (<main>/role=\"main\") so it is not contained by another landmark; main should be a top-level region of the page.",
    "landmarkNoDuplicateBanner_title": "Page must not have more than one banner landmark",
    "landmarkNoDuplicateBanner_description": "Checks that at most one banner landmark (role=\"banner\" or a non-nested <header>) exists on the page.",
    "landmarkNoDuplicateBanner_summary_cantTell": "This page has more than one banner landmark.",
    "landmarkNoDuplicateBanner_hint_cantTell": "Keep only one banner landmark (header/role=\"banner\") per page.",
    "landmarkNoDuplicateContentinfo_title": "Page must not have more than one contentinfo landmark",
    "landmarkNoDuplicateContentinfo_description": "Checks that at most one contentinfo landmark (role=\"contentinfo\" or a non-nested <footer>) exists on the page.",
    "landmarkNoDuplicateContentinfo_summary_cantTell": "This page has more than one contentinfo landmark.",
    "landmarkNoDuplicateContentinfo_hint_cantTell": "Keep only one contentinfo landmark (footer/role=\"contentinfo\") per page.",
    "landmarkNoDuplicateMain_title": "Page must not have more than one main landmark",
    "landmarkNoDuplicateMain_description": "Checks that at most one main landmark (role=\"main\" or <main>) exists on the page.",
    "landmarkNoDuplicateMain_summary_cantTell": "This page has more than one main landmark.",
    "landmarkNoDuplicateMain_hint_cantTell": "Keep only one main landmark (<main>/role=\"main\") per page.",
    "landmarkOneMain_title": "Page should have a main landmark",
    "landmarkOneMain_description": "Checks that the page has at least one main landmark (role=\"main\" or <main>).",
    "landmarkOneMain_summary_cantTell_missing": "This page has no main landmark.",
    "landmarkOneMain_hint_cantTell_missing": "Add a main landmark (<main> or role=\"main\") around the page's primary content.",
    "landmarkUnique_title": "Landmarks with the same role must have unique names",
    "landmarkUnique_description": "Checks that when two or more landmarks share the same role, each has a distinct accessible name.",
    "landmarkUnique_summary_cantTell_duplicateName": "This {{role}} landmark shares its accessible name with another {{role}} landmark.",
    "landmarkUnique_summary_cantTell_bothUnnamed": "This {{role}} landmark has no accessible name, and more than one unnamed {{role}} landmark exists on this page.",
    "landmarkUnique_hint_cantTell": "Give each {{role}} landmark a distinct name via aria-label or aria-labelledby.",
    "emptyHeading_title": "Headings must not be empty",
    "emptyHeading_description": "Checks that heading elements (<h1>-<h6> or role=\"heading\") have a non-empty accessible name.",
    "emptyHeading_summary_cantTell": "This heading has no accessible name.",
    "emptyHeading_hint_cantTell": "Add text content (or aria-label/aria-labelledby) to this heading, or remove it if it is not needed.",
    "headingOrder_title": "Heading levels must not skip a level",
    "headingOrder_description": "Checks that heading levels increase by at most one at a time in document order.",
    "headingOrder_summary_cantTell": "This heading jumps from level {{fromLevel}} to level {{toLevel}}, skipping a level.",
    "headingOrder_hint_cantTell": "Use consecutive heading levels (do not skip a level when going deeper) so the document outline stays predictable.",
    "pageHasHeadingOne_title": "Page should have a level-one heading",
    "pageHasHeadingOne_description": "Checks that the page has at least one level-one heading (<h1> or role=\"heading\" with aria-level=\"1\").",
    "pageHasHeadingOne_summary_cantTell": "This page has no level-one heading.",
    "pageHasHeadingOne_hint_cantTell": "Add a level-one heading (<h1> or role=\"heading\" aria-level=\"1\") that identifies the page's main content.",
    "accesskeys_title": "accesskey values must be unique",
    "accesskeys_description": "Checks that no two elements on the page share the same accesskey attribute value.",
    "accesskeys_summary_cantTell": "This element's accesskey is shared with another element on the page.",
    "accesskeys_hint_cantTell": "Make each accesskey value unique across the page.",
    "scopeAttrValid_title": "scope attribute must have a valid value",
    "scopeAttrValid_description": "Checks that scope=\"...\" is one of row, col, rowgroup, or colgroup.",
    "scopeAttrValid_summary_cantTell": "This scope attribute value is not recognized.",
    "scopeAttrValid_hint_cantTell": "Use one of row, col, rowgroup, or colgroup for the scope attribute.",
    "tabindex_title": "tabindex should not be greater than 0",
    "tabindex_description": "Checks that tabindex values are 0 or negative, not a positive number.",
    "tabindex_summary_cantTell": "This element has a positive tabindex, overriding the natural tab order.",
    "tabindex_hint_cantTell": "Use tabindex=\"0\" (or a negative value to remove from tab order) instead of a positive number; fix the DOM order if a different tab order is needed.",
    "emptyTableHeader_title": "Table header cells must not be empty",
    "emptyTableHeader_description": "Checks that <th> elements have visible text content — a header named only via aria-label/aria-labelledby is also flagged, since real screen-reader/browser support for that is inconsistent.",
    "emptyTableHeader_summary_cantTell": "This table header cell has no accessible name.",
    "emptyTableHeader_hint_cantTell": "Add text content (or aria-label/aria-labelledby) to this header cell, or remove it if it is not needed.",
    "emptyTableHeader_summary_cantTell_ariaOnly": "This table header cell has no visible text — its only accessible name comes from aria-label/aria-labelledby, which real screen-reader/browser combinations (e.g. NVDA+Firefox, iOS VoiceOver+Safari) are known to ignore on <th> elements.",
    "emptyTableHeader_hint_cantTell_ariaOnly": "Add visible text content to this header cell (in addition to, or instead of, aria-label/aria-labelledby) — visible text is the only naming mechanism confirmed to work across tested screen readers.",
    "labelTitleOnly_title": "Form controls should not use title as their only label",
    "labelTitleOnly_description": "Checks that a form control with a title attribute also has a real label (label element, aria-label, or aria-labelledby).",
    "labelTitleOnly_summary_cantTell": "This form control relies on the title attribute as its only label.",
    "labelTitleOnly_hint_cantTell": "Add a visible <label> (or aria-label/aria-labelledby) in addition to, or instead of, the title attribute.",
    "imageRedundantAlt_title": "Image alt text must not duplicate adjacent visible text",
    "imageRedundantAlt_description": "Checks that an <img> alt text is not identical to other visible text already present in its immediate parent element.",
    "imageRedundantAlt_summary_cantTell": "This image's alt text duplicates other visible text right next to it.",
    "imageRedundantAlt_hint_cantTell": "Make the alt text empty (alt=\"\") if the image is purely decorative alongside the text, or remove the redundant duplication.",
    "tableDuplicateName_title": "Table caption must not duplicate its summary attribute",
    "tableDuplicateName_description": "Checks that a <table>'s <caption> text is not identical to its (deprecated) summary attribute.",
    "tableDuplicateName_summary_cantTell": "This table's caption duplicates its summary attribute.",
    "tableDuplicateName_hint_cantTell": "Remove the redundant summary attribute, or make it provide different information than the caption.",
    "metaViewportLarge_title": "Viewport meta tag should allow zooming up to 500%",
    "metaViewportLarge_description": "Checks that <meta name=\"viewport\"> does not set user-scalable=no or maximum-scale below 5 (500%).",
    "metaViewportLarge_summary_cantTell": "This viewport meta tag restricts zoom below the 500% best-practice target.",
    "metaViewportLarge_hint_cantTell": "Remove user-scalable=no and raise maximum-scale to at least 5 (500%) if possible.",
    "presentationRoleConflict_title": "Presentational role must not conflict with a global ARIA attribute or focusability",
    "presentationRoleConflict_description": "Checks that role=\"presentation\"/\"none\" (including an <img alt=\"\"> implicit presentation role) is not combined with a global ARIA attribute (aria-label, aria-hidden, aria-describedby, ...) or focusability (tabindex/native).",
    "presentationRoleConflict_summary_cantTell": "This role=\"{{role}}\" element also has a conflicting condition ({{attrs}}), which restores its implicit role and cancels the presentational intent.",
    "presentationRoleConflict_hint_cantTell": "Remove the conflicting naming attribute(s) and/or focusability (tabindex/native) if the element should stay presentational, or remove role=\"presentation\"/\"none\" if it should be exposed to assistive technology.",
    "region_title": "Page content should be inside a landmark region",
    "region_description": "Checks that content under <body> is contained within a landmark region.",
    "region_summary_cantTell": "This content is not contained within a landmark region.",
    "region_hint_cantTell": "Move this content inside a landmark region (main, nav, aside, a labeled section, etc.).",
    "skipLink_title": "Skip link must have a resolvable, usable target",
    "skipLink_description": "Checks that a \"skip to ...\" link's href fragment resolves to a real, currently usable element in the document.",
    "skipLink_summary_cantTell": "This skip link's target does not exist.",
    "skipLink_hint_cantTell": "Point the skip link's href at an id that exists in the document, or add the missing target element.",
    "skipLink_summary_unusableTarget_cantTell": "This skip link points to a target that exists but is not currently usable.",
    "skipLink_hint_unusableTarget_cantTell": "Point this skip link to a target that is exposed and usable as a navigation destination.",
    "autocompleteValid_title": "autocomplete attribute must be a valid autofill value",
    "autocompleteValid_description": "Checks that a non-empty autocomplete attribute is \"on\"/\"off\" or a well-formed autofill detail token list.",
    "autocompleteValid_summary_fail": "This autocomplete attribute value is not a valid autofill value.",
    "autocompleteValid_hint_fail": "Use \"on\"/\"off\", or a valid autofill token list (e.g. \"shipping street-address\", \"cc-number\").",
    "htmlXmlLangMismatch_title": "lang and xml:lang must not disagree",
    "htmlXmlLangMismatch_description": "Checks that the <html> element's lang and xml:lang attributes declare the same primary language, when both are present.",
    "htmlXmlLangMismatch_summary_fail": "The lang (\"{{lang}}\") and xml:lang (\"{{xmlLang}}\") attributes declare different languages.",
    "htmlXmlLangMismatch_hint_fail": "Make lang and xml:lang declare the same primary language, or remove the deprecated xml:lang attribute.",
    "avoidInlineSpacing_title": "Inline style must not force text spacing below the WCAG metric",
    "avoidInlineSpacing_description": "Checks that where inline style forces line-height, letter-spacing or word-spacing with !important, the value already meets WCAG 1.4.12, so the user has nothing left to override.",
    "avoidInlineSpacing_summary_fail": "This element's inline style forces {{properties}} with !important, blocking user text-spacing overrides.",
    "avoidInlineSpacing_hint_fail": "Remove !important from line-height/letter-spacing/word-spacing in inline styles so users can override text spacing.",
    "metaRefreshNoExceptions_title": "Page must not use a meta refresh at all (AAA)",
    "metaRefreshNoExceptions_description": "Checks that <meta http-equiv=\"refresh\"> is not present at all, regardless of delay — the stricter AAA-level counterpart of the A-level positive-delay-only check.",
    "metaRefreshNoExceptions_summary_fail": "This page uses a meta refresh, which is an automatic context change not initiated by the user.",
    "metaRefreshNoExceptions_hint_fail": "Remove the meta refresh; trigger the redirect/refresh only in response to a user action instead.",
    "validLang_title": "Element lang attribute must be syntactically valid",
    "validLang_description": "Checks that any element (other than the root <html>) with a non-empty lang attribute uses a syntactically valid language tag.",
    "validLang_summary_fail": "This lang attribute value (\"{{value}}\") is not a syntactically valid language tag.",
    "validLang_hint_fail": "Use a valid BCP47 language tag (e.g. \"fr\", \"es-MX\").",
    "linkInTextBlock_title": "Links in text blocks must be distinguishable from surrounding text without relying on color alone",
    "linkInTextBlock_description": "Checks that a link inside a run of text is visually distinguishable from the surrounding text by underline, a font-weight/style difference, or a sufficient (>=3:1) color-contrast difference — not by color alone.",
    "linkInTextBlock_summary_fail": "This link in a block of text relies on color alone to be distinguished from the surrounding text.",
    "linkInTextBlock_hint_fail": "Add an underline, a font-weight/style difference, or increase the color contrast between the link and surrounding text to at least 3:1.",
    "noAutoplayAudio_title": "Autoplaying audio should provide a pause/stop or volume-control mechanism",
    "noAutoplayAudio_description": "Flags <audio>/<video> elements that autoplay unmuted with no native controls attribute, for manual review against the 3-second exemption in WCAG 1.4.2.",
    "noAutoplayAudio_summary_cantTell": "This element autoplays audio without a native pause/stop or volume-control mechanism.",
    "noAutoplayAudio_hint_cantTell": "If this clip plays for more than 3 seconds, add a controls attribute (or an equivalent custom mechanism) so users can pause/stop it or control its volume independently of the system volume.",
    "videoCaption_title": "Prerecorded video should provide a captions track",
    "videoCaption_description": "Flags <video> elements with no <track kind=\"captions\"|\"subtitles\"> child, for manual review of whether the video has an audio track that needs captions.",
    "videoCaption_summary_cantTell": "This video has no captions (or subtitles) track.",
    "videoCaption_hint_cantTell": "If this video has an audio track that conveys information, add a <track kind=\"captions\" src=\"...\"> with the captioned content.",
    "scrollableRegionFocusable_title": "Scrollable regions with no focusable content should be keyboard-focusable",
    "scrollableRegionFocusable_description": "Flags elements whose CSS declares overflow:auto/scroll, contain no focusable descendant, and are not themselves keyboard-focusable, for manual review of whether their content actually overflows and needs keyboard scroll access.",
    "scrollableRegionFocusable_summary_cantTell": "This element declares overflow:auto/scroll, has no focusable descendant, and is not itself keyboard-focusable.",
    "scrollableRegionFocusable_hint_cantTell": "If this region's content actually overflows, add tabindex=\"0\" (and a suitable label) so keyboard users can focus it and scroll with the arrow keys.",
    "identicalLinksSamePurpose_title": "Links with the same accessible name should lead to the same destination",
    "identicalLinksSamePurpose_description": "Flags groups of links that share the same accessible name but resolve to more than one distinct destination, for manual review of whether they serve the same purpose.",
    "identicalLinksSamePurpose_summary_cantTell": "This link shares an accessible name with other links on the page that lead to a different destination.",
    "identicalLinksSamePurpose_hint_cantTell": "Ensure links with the same text serve the same purpose, or make the link text distinct enough to describe each destination.",
    "ariaBrailleEquivalent_title": "aria-braillelabel/aria-brailleroledescription must have a non-braille equivalent",
    "ariaBrailleEquivalent_description": "Checks that elements using aria-braillelabel also have a regular accessible name, and elements using aria-brailleroledescription also have aria-roledescription.",
    "ariaBrailleEquivalent_summary_fail": "This element has {{attr}} but no {{requires}}, its non-braille equivalent.",
    "ariaBrailleEquivalent_hint_fail": "{{attr}} is a Braille-specific supplement, not a replacement — also provide {{requires}}.",
    "ariaConditionalAttr_title": "aria-errormessage requires aria-invalid to be set to a non-false value",
    "ariaConditionalAttr_description": "Checks that elements with aria-errormessage also have aria-invalid set to \"true\", \"grammar\", or \"spelling\" — otherwise the error message is dropped from the accessibility tree.",
    "ariaConditionalAttr_summary_fail": "This element has aria-errormessage but aria-invalid is missing or \"false\", so the error message is not exposed.",
    "ariaConditionalAttr_hint_fail": "Set aria-invalid to \"true\" (or \"grammar\"/\"spelling\") whenever aria-errormessage should be exposed to assistive technology.",
    "ariaCheckedStateMismatch_title": "Native checkbox/radio aria-checked should match its actual state",
    "ariaCheckedStateMismatch_description": "Flags a native <input type=\"checkbox\">/<input type=\"radio\"> whose explicit aria-checked value disagrees with its actual checked/indeterminate state, for manual review.",
    "ariaCheckedStateMismatch_summary_cantTell": "This element’s aria-checked value does not match its actual checked/indeterminate state.",
    "ariaCheckedStateMismatch_hint_cantTell": "Set aria-checked to match the element’s real state, or remove it — a native checkbox/radio already exposes this state without it.",
    "cssOrientationLock_title": "CSS must not lock the page to a single orientation",
    "cssOrientationLock_description": "Checks that no @media (orientation: portrait|landscape) rule sets a transform: rotate(...) on the page, a known technique for defeating device orientation.",
    "cssOrientationLock_summary_fail": "A \"{{mediaText}}\" media query rotates \"{{selectorText}}\", locking the page to one orientation.",
    "cssOrientationLock_hint_fail": "Remove the rotate() transform from the orientation media query; let the page respond naturally to device orientation instead of forcing a visual rotation.",
    "ariaText_title": "role=\"text\" elements should have no focusable descendants",
    "ariaText_description": "Checks that elements with role=\"text\" contain no focusable descendant (link, button, form control, tabindex, iframe, or contenteditable).",
    "ariaText_summary_cantTell": "This role=\"text\" element contains a focusable descendant.",
    "ariaText_hint_cantTell": "Remove role=\"text\" (or remove the focusable descendant) — a \"plain text\" region should not contain focusable content.",
    "focusOrderSemantics_title": "Elements added to the tab order should have interactive semantics",
    "focusOrderSemantics_description": "Flags elements with tabindex >= 0 whose explicit role is a non-interactive structural/document role (e.g. heading, list, region, presentation), for manual review.",
    "focusOrderSemantics_summary_cantTell": "This element is in the tab order (tabindex=\"{{tabindex}}\") but has a non-interactive role (\"{{role}}\").",
    "focusOrderSemantics_hint_cantTell": "Remove tabindex if this element is not meant to be interactive, or use an interactive role that matches its actual behavior.",
    "pAsHeading_title": "A <p> styled to look like a heading should probably be a real heading",
    "pAsHeading_description": "Flags short <p> elements whose entire text is bold and rendered at >=18px, for manual review of whether a real heading element should be used instead.",
    "pAsHeading_summary_cantTell": "This paragraph is entirely bold and rendered at a heading-like size.",
    "pAsHeading_hint_cantTell": "If this text introduces a new section, use a real heading element (<h1>-<h6> or role=\"heading\") instead of styling a paragraph to look like one.",
    "tableFakeCaption_title": "A table's first row should not stand in for a real <caption>",
    "tableFakeCaption_description": "Flags tables with no <caption> whose first row has a single non-empty cell while other rows have multiple cells, for manual review of whether that cell is acting as a fake caption.",
    "tableFakeCaption_summary_cantTell": "This table has no <caption>, but its first row is a single cell sitting above multi-cell rows — it may be acting as a fake caption.",
    "tableFakeCaption_hint_cantTell": "If this cell is meant to describe the table, use a real <caption> element instead of a lone first-row cell.",
    "tdHasHeader_title": "Data cells in large tables must have an associated header",
    "tdHasHeader_description": "Checks that every <td> in a large, simple (no colspan/rowspan) table has an associated header — via a headers attribute, an implicit column <th> above it, or an implicit row <th> to its left.",
    "tdHasHeader_summary_fail": "This data cell has no associated header (no headers attribute, no column <th> above it, no row <th> to its left).",
    "tdHasHeader_hint_fail": "Add a headers attribute referencing the relevant <th> id(s), or restructure the table so this cell has an implicit row/column header.",
    "mouseOnlyEventHandlers_title": "Pointer-only inline event handlers should have a keyboard-reachable equivalent",
    "mouseOnlyEventHandlers_description": "Flags elements with an inline pointer-only event handler (onmouseover, onmouseout, onmousedown, onmouseup, ondblclick, onmousemove, onmouseenter, onmouseleave) and no keyboard-reachable equivalent (onkeydown/onkeyup/onkeypress/onfocus/onblur), for manual review.",
    "mouseOnlyEventHandlers_summary_cantTell": "This element has {{attrs}} but no keyboard-reachable equivalent handler.",
    "mouseOnlyEventHandlers_hint_cantTell": "Add onkeydown/onkeyup/onkeypress (or onfocus/onblur for hover-triggered behavior) so this functionality is also reachable by keyboard.",
    "linkNameQuality_title": "Link text should be descriptive, not generic",
    "linkNameQuality_description": "Flags links whose full accessible name is a known non-descriptive phrase (e.g. \"click here\", \"read more\", \"more\"), for manual review of whether the purpose is clear without additional context.",
    "linkNameQuality_summary_cantTell": "This link's accessible name (\"{{name}}\") is a generic, non-descriptive phrase.",
    "linkNameQuality_hint_cantTell": "Make the link text itself describe its destination/purpose (e.g. \"Download the 2026 pricing guide\" instead of \"Download\"), or confirm the surrounding context already makes the purpose clear."
  },
  "es": {
    "img_altPresent_title": "<img> debe tener un atributo alt",
    "img_altPresent_description": "Comprueba que los elementos <img> incluyan un atributo alt para ofrecer un mecanismo de alternativa textual.",
    "img_altPresent_summary_fail": "Falta el atributo alt en <img>.",
    "img_altPresent_hint_fail": "Agregar un atributo alt (usar alt=\"\" solo para imágenes decorativas).",
    "area_altPresent_title": "<area> debe tener un atributo alt",
    "area_altPresent_description": "Comprueba que los elementos <area> incluyan un atributo alt para ofrecer un mecanismo de alternativa textual.",
    "area_altPresent_summary_fail": "Falta el atributo alt en <area>.",
    "area_altPresent_hint_fail": "Agregar un atributo alt (usar alt=\"\" solo para áreas decorativas).",
    "inputImage_altPresent_title": "<input type=\"image\"> debe tener un atributo alt",
    "inputImage_altPresent_description": "Comprueba que los elementos <input type=\"image\"> incluyan un atributo alt para ofrecer un mecanismo de alternativa textual.",
    "inputImage_altPresent_summary_fail": "Falta el atributo alt en <input type=\"image\">.",
    "inputImage_altPresent_hint_fail": "Agregar un atributo alt (usar alt=\"\" solo cuando se proporcione un nombre accesible por separado).",
    "inputImage_altPresent_summary_defaultName": "El nombre accesible es el predeterminado del navegador para un boton de imagen y no aporta informacion.",
    "inputImage_altPresent_hint_defaultName": "Sustituyelo por un texto que describa la accion del boton, por ejemplo \"Buscar\".",
    "inputImage_altPresent_summary_emptyAlt": "Un alt=\"\" vacio en <input type=\"image\"> deja el control sin nombre.",
    "inputImage_altPresent_hint_emptyAlt": "Describe la accion en alt, o nombra el control con aria-label o aria-labelledby.",
    "ariaHidden_programmaticFocus_review_title": "Revisar el foco programático en aria-hidden",
    "ariaHidden_programmaticFocus_review_description": "Señala elementos aria-hidden considerados elegibles debido a un foco programático (por ejemplo, tabindex < 0). Verificar que la gestión del foco es intencionada y la exposición a las tecnologías de asistencia.",
    "ariaHidden_programmaticFocus_review_summary": "Revisión: un elemento aria-hidden es enfocable de forma programática.",
    "ariaHidden_programmaticFocus_review_hint": "Comprobar que la gestión del foco es intencionada y que el elemento debe permanecer oculto para las tecnologías de asistencia.",
    "canvas_textAltPresent_title": "<canvas> debe proporcionar una alternativa textual",
    "canvas_textAltPresent_description": "Comprueba que los elementos <canvas> proporcionen una alternativa textual mediante contenido de reserva o un nombre accesible.",
    "canvas_textAltPresent_summary_fail": "Falta la alternativa textual para <canvas>.",
    "canvas_textAltPresent_hint_fail": "Proporcionar texto de reserva dentro de <canvas> o un nombre accesible (por ejemplo, aria-label/aria-labelledby).",
    "svg_textAltPresent_title": "<svg> debe proporcionar una alternativa textual",
    "svg_textAltPresent_description": "Comprueba que los elementos <svg> en línea proporcionen una alternativa textual mediante un elemento <title> o un nombre ARIA (un elemento <desc> por sí solo no cuenta).",
    "svg_textAltPresent_summary_fail": "Falta la alternativa textual para <svg>.",
    "svg_textAltPresent_hint_fail": "Proporcionar un elemento <title> con texto, o un nombre ARIA (aria-label/aria-labelledby); un elemento <desc> por sí solo no proporciona un nombre accesible.",
    "object_textAltPresent_title": "<object> debe proporcionar una alternativa textual",
    "object_textAltPresent_description": "Comprueba que los elementos <object> proporcionen una alternativa textual mediante contenido de reserva o un nombre accesible.",
    "object_textAltPresent_summary_fail": "Falta la alternativa textual para <object>.",
    "object_textAltPresent_hint_fail": "Proporcionar contenido de reserva significativo dentro de <object>, agregar un nombre accesible (aria-label/aria-labelledby), o usar un atributo title como alternativa de último recurso.",
    "embed_textAltPresent_title": "<embed> debe proporcionar una alternativa textual",
    "embed_textAltPresent_description": "Comprueba que los elementos <embed> proporcionen una alternativa textual mediante un nombre accesible.",
    "embed_textAltPresent_summary_fail": "Falta la alternativa textual para <embed>.",
    "embed_textAltPresent_hint_fail": "Agregar un nombre accesible a <embed> (se prefiere aria-label/aria-labelledby, o un atributo title como alternativa de último recurso).",
    "img_altQuality_title": "El texto alt de <img> debe ser apropiado (revisión manual)",
    "img_altQuality_description": "Señala elementos <img> con texto alt no vacío para su revisión manual en cuanto a idoneidad.",
    "img_altQuality_summary_cantTell": "Revisar el texto alt de <img> en cuanto a exactitud e idoneidad.",
    "img_altQuality_hint_cantTell": "Asegurarse de que el texto alt transmita el propósito o la información de la imagen en su contexto (que no sea redundante ni parezca un nombre de archivo).",
    "img_altDecorative_title": "<img> con alt=\"\" debe ser decorativa (revisión manual)",
    "img_altDecorative_description": "Señala elementos <img> con alt vacío para su revisión manual de que son puramente decorativos.",
    "img_altDecorative_summary_cantTell": "Revisar si <img> es decorativa (alt=\"\").",
    "img_altDecorative_hint_cantTell": "Confirmar que la imagen es puramente decorativa. Si transmite información o función, proporcionar un texto alt significativo.",
    "area_altQuality_title": "El texto alt de <area> debe ser apropiado (revisión manual)",
    "area_altQuality_description": "Señala elementos <area> con texto alt no vacío para su revisión manual en cuanto a idoneidad.",
    "area_altQuality_summary_cantTell": "Revisar el texto alt de <area> en cuanto a exactitud e idoneidad.",
    "area_altQuality_hint_cantTell": "Asegurarse de que el texto alt identifique el destino o la acción del área del mapa de imagen en su contexto.",
    "area_altDecorative_title": "<area> con alt=\"\" debe ser decorativa (revisión manual)",
    "area_altDecorative_description": "Señala elementos <area> con alt vacío para su revisión manual de que son decorativos o no informativos.",
    "area_altDecorative_summary_cantTell": "Revisar si <area> es decorativa (alt=\"\").",
    "area_altDecorative_hint_cantTell": "Confirmar que el área no transmite información ni función. Si es interactiva o significativa, proporcionar un texto alt significativo.",
    "inputImage_altQuality_title": "El texto alt de <input type=\"image\"> debe ser apropiado (revisión manual)",
    "inputImage_altQuality_description": "Señala elementos <input type=\"image\"> con texto alt no vacío para su revisión manual en cuanto a idoneidad.",
    "inputImage_altQuality_summary_cantTell": "Revisar el texto alt de <input type=\"image\"> en cuanto a exactitud e idoneidad.",
    "inputImage_altQuality_hint_cantTell": "Asegurarse de que el texto alt describa la acción del control (por ejemplo, \"Buscar\", \"Enviar pedido\") en su contexto.",
    "inputImage_altDecorative_title": "<input type=\"image\"> con alt=\"\" debe ser apropiado (revisión manual)",
    "inputImage_altDecorative_description": "Señala elementos <input type=\"image\"> con alt vacío para su revisión manual (normalmente no es apropiado para controles funcionales).",
    "inputImage_altDecorative_summary_cantTell": "Revisar <input type=\"image\"> con alt=\"\".",
    "inputImage_altDecorative_hint_cantTell": "Este control suele ser funcional. Confirmar que tiene un nombre accesible equivalente en otro lugar, o proporcionar un texto alt significativo.",
    "canvas_textAltQuality_title": "La alternativa textual de <canvas> debe ser apropiada (revisión manual)",
    "canvas_textAltQuality_description": "Señala elementos <canvas> con una alternativa textual detectada para su revisión manual en cuanto a equivalencia e idoneidad.",
    "canvas_textAltQuality_summary_cantTell": "Revisar la alternativa textual de <canvas> en cuanto a equivalencia e idoneidad.",
    "canvas_textAltQuality_hint_cantTell": "Confirmar que el texto de reserva o el nombre accesible transmite la misma información o función que el contenido del canvas.",
    "svg_textAltQuality_title": "La alternativa textual de <svg> debe ser apropiada (revisión manual)",
    "svg_textAltQuality_description": "Señala gráficos <svg> aplicables con una alternativa textual detectada para su revisión manual en cuanto a idoneidad.",
    "svg_textAltQuality_summary_cantTell": "Revisar la alternativa textual de <svg> en cuanto a exactitud e idoneidad.",
    "svg_textAltQuality_hint_cantTell": "Confirmar que el <title>/<desc> o el nombre ARIA transmite el significado o propósito del gráfico en su contexto.",
    "object_textAltQuality_title": "La alternativa textual de <object> debe ser apropiada (revisión manual)",
    "object_textAltQuality_description": "Señala elementos <object> con contenido de reserva o nombre detectados para su revisión manual en cuanto a equivalencia e idoneidad.",
    "object_textAltQuality_summary_cantTell": "Revisar la alternativa textual de <object> en cuanto a equivalencia e idoneidad.",
    "object_textAltQuality_hint_cantTell": "Confirmar que el contenido de reserva o el nombre ARIA proporciona una alternativa equivalente para el contenido incrustado.",
    "embed_textAltQuality_title": "La alternativa textual de <embed> debe ser apropiada (revisión manual)",
    "embed_textAltQuality_description": "Señala elementos <embed> con un nombre detectado para su revisión manual en cuanto a idoneidad.",
    "embed_textAltQuality_summary_cantTell": "Revisar la alternativa textual de <embed> en cuanto a exactitud e idoneidad.",
    "embed_textAltQuality_hint_cantTell": "Confirmar que el nombre ARIA o el title identifica con precisión el contenido incrustado en su contexto.",
    "videoPoster_textAltPresent_title": "El póster de <video> debe tener una alternativa textual",
    "videoPoster_textAltPresent_description": "Comprueba que los elementos <video> con una imagen de póster proporcionen una alternativa textual (nombre accesible).",
    "videoPoster_textAltPresent_summary_fail": "Falta la alternativa textual para el póster de <video>.",
    "videoPoster_textAltPresent_hint_fail": "Proporcionar un nombre accesible para la imagen del póster (se prefiere aria-label/aria-labelledby, o un atributo title como alternativa de reserva).",
    "svgImage_textAltPresent_title": "El elemento <image> de SVG debe tener una alternativa textual",
    "svgImage_textAltPresent_description": "Comprueba que los elementos <image> de SVG proporcionen una alternativa textual mediante <title>/<desc> o un nombre accesible ARIA.",
    "svgImage_textAltPresent_summary_fail": "Falta la alternativa textual en el <image> de SVG.",
    "svgImage_textAltPresent_hint_fail": "Agregar un <title> (y opcionalmente un <desc>) dentro de <image>, o proporcionar aria-label/aria-labelledby.",
    "formControl_programmaticLabelPresent_title": "Los controles de formulario deben tener una etiqueta programática",
    "formControl_programmaticLabelPresent_description": "Comprueba que los controles de formulario tengan una etiqueta programática mediante <label>, aria-label o aria-labelledby.",
    "formControl_programmaticLabelPresent_summary_fail": "Al control de formulario le falta una etiqueta programática.",
    "formControl_programmaticLabelPresent_hint_fail": "Proporcionar una asociación <label>, aria-label o aria-labelledby (placeholder/title no cuentan como etiquetas).",
    "formControlAccessibleName_description": "Falla cuando un control de formulario aplicable no tiene nombre accesible (por ejemplo, label, aria-label, aria-labelledby).",
    "formControlAccessibleName_hint_fail": "Proporcionar un nombre accesible mediante un <label>, aria-label o aria-labelledby.",
    "formControlAccessibleName_summary_fail": "El control de formulario no tiene nombre accesible.",
    "formControlAccessibleName_title": "Los controles de formulario deben tener un nombre accesible",
    "linksTargetBlankNoopener_description": "Garantiza que los enlaces con target=\"_blank\" mitiguen los riesgos de reverse tabnabbing.",
    "linksTargetBlankNoopener_hint_cantTell": "Consultar las indicaciones para esta regla.",
    "linksTargetBlankNoopener_summary_cantTell": "Los enlaces que se abren en una nueva pestaña deben usar rel=\"noopener\"",
    "linksTargetBlankNoopener_title": "Los enlaces que se abren en una nueva pestaña deben usar rel=\"noopener\"",
    "manualReview_description": "Señala que se requiere una revisión manual de la navegación por teclado y el orden del foco.",
    "manualReview_hint_cantTell": "Consultar las indicaciones para esta regla.",
    "manualReview_summary_cantTell": "Revisión manual: navegación por teclado y orden del foco",
    "manualReview_title": "Revisión manual: navegación por teclado y orden del foco",
    "rules.img-alt-suspicious.meta.title": "El texto alt sospechoso requiere verificación",
    "rules.img-alt-suspicious.meta.description": "Identifica imágenes cuyo texto alt coincide con patrones sospechosos comunes (como nombres de archivo, URL, marcadores de posición o términos genéricos) y requiere verificación manual.",
    "rules.img-alt-suspicious.occurrence.cantTell.summary": "El texto alt de la imagen parece sospechoso (\"{{alt}}\" parece {{pattern}}) y requiere verificación.",
    "rules.img-alt-suspicious.occurrence.cantTell.hint": "Revisar el texto alt. Evitar nombres de archivo, URL, marcadores de posición o términos genéricos, y asegurarse de que la alternativa textual describa el propósito o la función de la imagen en su contexto.",
    "formControl_programmaticLabelQuality_title": "Los controles de formulario no deben depender de placeholder o title como etiqueta principal",
    "formControl_programmaticLabelQuality_description": "Señala controles de formulario cuyo nombre accesible calculado depende de placeholder o title como método de etiquetado principal. Se prefiere <label> o aria-labelledby.",
    "formControl_programmaticLabelQuality_summary_cantTell": "La etiqueta principal del control de formulario proviene de {{methodLabel}}.",
    "formControl_programmaticLabelQuality_hint_cantTell": "Preferir un <label> persistente o aria-labelledby. Evitar depender de placeholder/title como etiqueta principal.",
    "html_lang_attr_title": "El idioma de la página está declarado",
    "html_lang_attr_description": "Comprueba que el idioma predeterminado de la página esté declarado de forma programática.",
    "html_lang_attr_missing_absent": "El idioma predeterminado de la página no está declarado.",
    "html_lang_attr_hint_missing_absent": "Agregar un atributo lang al elemento <html> (por ejemplo: <html lang=\"en\">).",
    "html_lang_attr_missing_empty": "El idioma predeterminado de la página está declarado pero vacío.",
    "html_lang_attr_hint_missing_empty": "Establecer un valor de idioma válido en el atributo lang del elemento <html> (por ejemplo: <html lang=\"en\">).",
    "html_lang_attr_invalid": "El idioma predeterminado de la página está declarado, pero el valor \"{{lang}}\" no es una etiqueta de idioma válida.",
    "html_lang_attr_hint_invalid": "Usar una etiqueta de idioma BCP 47 válida en <html lang=\"…\"> (por ejemplo: \"en\", \"fr\", \"en-US\").",
    "mediaTranscriptPresent_title": "Medios basados en tiempo: evidencia de transcripción o alternativa textual",
    "mediaTranscriptPresent_description": "Encuentra elementos de audio y video en los que no hay evidencia sólida de una transcripción u otra alternativa textual en el contenido de la página. Esta regla es conservadora e informa revisión manual cuando falta evidencia o no se puede verificar.",
    "mediaTranscriptPresent_summary_cantTell_missing": "No se demuestra claramente en el contenido de la página la presencia de una transcripción u otra alternativa textual para este <{{element}}>.",
    "mediaTranscriptPresent_hint_cantTell_missing": "Proporcionar una transcripción claramente identificada u otra alternativa textual para medios de solo audio o solo video pregrabados, por ejemplo una sección o enlace visible de \"Transcripción\".",
    "mediaTranscriptPresent_summary_cantTell_unverified": "Puede haber disponible una transcripción u otra alternativa textual para este medio basado en tiempo, pero no se pudo verificar a partir del contenido de la página.",
    "mediaTranscriptPresent_hint_cantTell_unverified": "Asegurarse de que una transcripción claramente identificada u otra alternativa textual esté disponible y asociada visible o programáticamente con el medio en la página.",
    "pageTitlePresent_title": "La página tiene un título no vacío",
    "pageTitlePresent_description": "Comprueba que la página incluya un elemento <title> no vacío que la identifique.",
    "pageTitlePresent_summary_fail": "La página no tiene un título no vacío.",
    "pageTitlePresent_hint_fail": "Agregar un elemento <title> con texto que describa el tema o propósito de la página.",
    "pageTitlePatterns_title": "Patrones de título de página que pueden ser poco descriptivos",
    "pageTitlePatterns_description": "Identifica patrones de título de página que pueden indicar baja descriptividad, como títulos genéricos, duplicados o excesivamente basados en plantillas. Esta regla ofrece señales de revisión y no falla automáticamente.",
    "pageTitlePatterns_summary_cantTell": "Es posible que el título de la página no sea lo bastante descriptivo para identificar su tema o propósito.",
    "pageTitlePresent_summary_fail_missing": "A la página le falta un elemento <title>.",
    "pageTitlePresent_summary_fail_empty": "La página tiene un <title> vacío.",
    "pageTitlePatterns_summary_cantTell_duplicateAcrossPages": "Varias páginas comparten el mismo título, lo que puede dificultar distinguirlas ({{duplicateGroups}} grupos duplicados en {{pagesAnalyzed}} páginas). Ejemplo: \"{{exampleTitle}}\".",
    "pageTitlePatterns_summary_cantTell_templatedAcrossPages": "Muchos títulos de página parecen muy basados en plantillas, lo que puede reducir su capacidad de distinguir páginas ({{pagesAnalyzed}} páginas).",
    "pageTitlePatterns_summary_cantTell_generic": "El título de la página es genérico y puede no identificar su tema o propósito.",
    "pageTitlePatterns_summary_cantTell_veryShort": "El título de la página es muy corto y puede no identificar su tema o propósito.",
    "pageTitlePatterns_summary_cantTell_templateLike": "El título de la página parece basado en una plantilla y puede no identificar su tema o propósito.",
    "pageTitlePatterns_hint_cantTell": "Revisar el título de la página y asegurarse de que identifique claramente su tema o propósito y ayude a distinguirla de otras.",
    "contrastComputable_title": "El contraste de color es computable para el texto renderizado",
    "contrastComputable_description": "Determina si hay suficiente información disponible para calcular el contraste de color WCAG del texto visible (por ejemplo, sin degradados/imágenes/modos de fusión que hagan indeterminado el fondo).",
    "contrastComputable_pass_allComputable": "El contraste es computable para todo el texto elegible ({{eligibleTextCount}} nodo(s) de texto).",
    "contrastComputable_cantTell_generic": "Es posible que el contraste no sea computable ({{reasonCode}}).",
    "contrastComputable_cantTell_bgImageOrGradient": "El contraste no es computable porque el fondo usa una imagen o un degradado ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgImage": "El contraste no es computable porque el fondo usa una imagen ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgGradient": "El contraste no es computable porque el fondo usa un degradado ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgImageAndGradient": "El contraste no es computable porque el fondo usa una imagen y un degradado ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_mixBlendMode": "El contraste no es computable porque se usa mix-blend-mode ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_filter": "El contraste no es computable porque se usa filter/backdrop-filter ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_rootNotOpaque": "El contraste no es computable porque el fondo efectivo no es completamente opaco en la raíz (alpha={{backgroundAlpha}}).",
    "contrastComputable_cantTell_foregroundUnparsable": "El contraste no es computable porque no se pudo analizar el color de primer plano calculado.",
    "contrastComputable_cantTell_engineFailure": "No se pudo determinar la computabilidad del contraste debido a un error interno del motor ({{reasonCode}}).",
    "contrastComputable_cantTell_backdropFilter": "El contraste no es computable porque se usa backdrop-filter ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_filterOrBackdropFilter": "El contraste no es computable porque se usa filter ({{blockerProperty}}={{blockerValue}}).",
    "contrastMinimum_title": "El texto cumple el contraste de color mínimo (AA)",
    "contrastMinimum_description": "Comprueba que el texto visible tenga una relación de contraste de al menos 4.5:1 (normal) o 3.0:1 (grande), cuando el contraste es computable a partir de CSS.",
    "contrastMinimum_fail_belowThreshold": "El elemento tiene un contraste de color insuficiente de {{ratio}}:1 (primer plano: {{foregroundHex}}, fondo: {{backgroundHex}}, tamaño de fuente: {{fontSizePx}}px, grosor de fuente: {{fontWeightLabel}}). Se esperaba una relación de contraste de {{threshold}}:1 ({{#isLargeText}}texto grande{{/isLargeText}}{{^isLargeText}}texto normal{{/isLargeText}}).",
    "contrastMinimum_pass_allAboveThreshold": "Todo el texto computable cumple el contraste mínimo (AA). Nodos de texto elegibles: {{eligibleTextCount}}. Computables: {{computableTextCount}}.",
    "contrastMinimum_notApplicable_noComputableText": "Ningún texto elegible tuvo contraste computable (nodos de texto elegibles: {{eligibleTextCount}}). Consultar la regla de computabilidad de contraste para más información.",
    "contrastMinimum_cantTell_engineFailure": "No se pudo determinar el contraste mínimo (AA) debido a un error interno del motor ({{reasonCode}}).",
    "contrastEnhanced_title": "El texto cumple el contraste de color mejorado (AAA)",
    "contrastEnhanced_description": "Comprueba que el texto visible tenga una relación de contraste de al menos 7.0:1 (normal) o 4.5:1 (grande), cuando el contraste es computable a partir de CSS.",
    "contrastEnhanced_fail_belowThreshold": "El elemento tiene un contraste de color insuficiente (AAA) de {{ratio}}:1 (primer plano: {{foregroundHex}}, fondo: {{backgroundHex}}, tamaño de fuente: {{fontSizePx}}px, grosor de fuente: {{fontWeightLabel}}). Se esperaba una relación de contraste de {{threshold}}:1 ({{#isLargeText}}texto grande{{/isLargeText}}{{^isLargeText}}texto normal{{/isLargeText}}).",
    "contrastEnhanced_pass_allAboveThreshold": "Todo el texto computable cumple el contraste mejorado (AAA). Nodos de texto elegibles: {{eligibleTextCount}}. Computables: {{computableTextCount}}.",
    "contrastEnhanced_notApplicable_noComputableText": "Ningún texto elegible tuvo contraste computable (nodos de texto elegibles: {{eligibleTextCount}}). Consultar la regla de computabilidad de contraste para más información.",
    "contrastEnhanced_cantTell_engineFailure": "No se pudo determinar el contraste mejorado (AAA) debido a un error interno del motor ({{reasonCode}}).",
    "dom_textContrastMinimum_title": "El texto debe tener contraste suficiente (mínimo)",
    "dom_textContrastMinimum_description": "Comprueba el contraste del texto visible contra su fondo calculado según el criterio de éxito 1.4.3 (AA) de WCAG 2.2, usando los estilos renderizados (tamaño/grosor de fuente) para determinar la relación requerida.",
    "dom_textContrastMinimum_summary_fail": "Contraste de texto insuficiente: {{contrastRatio}}:1 (requerido {{requiredRatio}}:1). Primer plano {{fgColor}} sobre fondo {{bgColor}}. Fuente {{fontSizePx}}px, grosor {{fontWeight}}{{#isBold}}, negrita{{/isBold}}{{#isLargeText}} (texto grande){{/isLargeText}}.",
    "dom_textContrastMinimum_hint_fail": "Ajustar el color de primer plano o de fondo para que la relación de contraste sea de al menos {{requiredRatio}}:1 para este tamaño/grosor de texto.",
    "dom_textContrastMinimum_summary_pass": "Contraste de texto correcto: {{contrastRatio}}:1 (requerido {{requiredRatio}}:1). Primer plano {{fgColor}} sobre fondo {{bgColor}}. Fuente {{fontSizePx}}px, grosor {{fontWeight}}{{#isBold}}, negrita{{/isBold}}{{#isLargeText}} (texto grande){{/isLargeText}}.",
    "dom_textContrastMinimum_summary_cantTell": "No se pudo calcular de forma fiable el contraste del texto porque el fondo efectivo no se puede resolver de forma determinista (por ejemplo, imagen, degradado, video, canvas, transparencia compleja o fusión de capas).",
    "dom_textContrastMinimum_hint_cantTell": "Verificar manualmente el contraste donde el texto se superpone a imágenes/degradados/transparencias; asegurarse de que cumpla {{requiredRatio}}:1 para el tamaño/grosor de texto calculado.",
    "dom_textContrastEnhanced_title": "El texto debe tener contraste suficiente (mejorado)",
    "dom_textContrastEnhanced_description": "Comprueba el contraste del texto visible contra su fondo calculado según el criterio de éxito 1.4.6 (AAA) de WCAG 2.2, usando los estilos renderizados (tamaño/grosor de fuente) para determinar la relación requerida.",
    "dom_textContrastEnhanced_summary_fail": "Contraste de texto mejorado insuficiente: {{contrastRatio}}:1 (requerido {{requiredRatio}}:1). Primer plano {{fgColor}} sobre fondo {{bgColor}}. Fuente {{fontSizePx}}px, grosor {{fontWeight}}{{#isBold}}, negrita{{/isBold}}{{#isLargeText}} (texto grande){{/isLargeText}}.",
    "dom_textContrastEnhanced_hint_fail": "Ajustar el color de primer plano o de fondo para que la relación de contraste sea de al menos {{requiredRatio}}:1 para el contraste mejorado (AAA).",
    "dom_textContrastEnhanced_summary_pass": "Contraste de texto mejorado correcto: {{contrastRatio}}:1 (requerido {{requiredRatio}}:1). Primer plano {{fgColor}} sobre fondo {{bgColor}}. Fuente {{fontSizePx}}px, grosor {{fontWeight}}{{#isBold}}, negrita{{/isBold}}{{#isLargeText}} (texto grande){{/isLargeText}}.",
    "dom_textContrastEnhanced_summary_cantTell": "No se pudo calcular de forma fiable el contraste de texto mejorado porque el fondo efectivo no se puede resolver de forma determinista (por ejemplo, imagen, degradado, video, canvas, transparencia compleja o fusión de capas).",
    "dom_textContrastEnhanced_hint_cantTell": "Verificar manualmente el contraste mejorado (AAA) donde el texto se superpone a imágenes/degradados/transparencias; asegurarse de que cumpla {{requiredRatio}}:1 para el tamaño/grosor de texto calculado.",
    "dom_nonTextContrast_title": "Los componentes de interfaz y los gráficos deben tener contraste suficiente",
    "dom_nonTextContrast_description": "Comprueba el contraste de información visual no textual (límites y estados de componentes de interfaz, y objetos gráficos significativos) según el criterio de éxito 1.4.11 (AA) de WCAG 2.2.",
    "dom_nonTextContrast_summary_fail": "Contraste no textual insuficiente: {{contrastRatio}}:1 (requerido {{requiredRatio}}:1). Primer plano {{fgColor}} contra fondo {{bgColor}}. Componente: {{componentKind}}{{#componentState}} (estado: {{componentState}}){{/componentState}}.",
    "dom_nonTextContrast_hint_fail": "Ajustar los colores del componente o gráfico para que la relación de contraste sea de al menos {{requiredRatio}}:1 para el límite perceptible o la información visual esencial.",
    "dom_nonTextContrast_summary_pass": "Contraste no textual correcto: {{contrastRatio}}:1 (requerido {{requiredRatio}}:1). Primer plano {{fgColor}} contra fondo {{bgColor}}. Componente: {{componentKind}}{{#componentState}} (estado: {{componentState}}){{/componentState}}.",
    "dom_nonTextContrast_summary_cantTell": "No se pudo calcular de forma fiable el contraste no textual porque el fondo efectivo o los píxeles pintados no se pueden resolver de forma determinista (por ejemplo, imagen/degradado/video/canvas, transparencia compleja o fusión de capas).",
    "dom_nonTextContrast_hint_cantTell": "Verificar manualmente el contraste del componente o gráfico contra los colores adyacentes; asegurarse de que cumpla {{requiredRatio}}:1 para la información visual no textual esencial.",
    "contrastEnhanced_pass_allTextMeetsThreshold": "Todo el texto computable cumple el contraste mejorado (AAA).",
    "contrastMinimum_pass_allTextMeetsThreshold": "Todo el texto computable cumple el contraste mínimo (AA).",
    "contrastComputable_cantTell_notComputable": "No se pudo calcular el contraste para este texto ({{reasonCode}}).",
    "roleImg_textAlternativePresent_title": "Los elementos con role=\"img\" deben tener una alternativa textual accesible",
    "roleImg_textAlternativePresent_description": "Comprueba que los elementos con role=\"img\" proporcionen una alternativa textual accesible mediante aria-label, aria-labelledby o un atributo title.",
    "roleImg_textAlternativePresent_summary_fail": "El elemento con role=\"img\" no tiene una alternativa textual accesible.",
    "roleImg_textAlternativePresent_hint_fail": "Proporcionar una alternativa textual mediante aria-label, o aria-labelledby que haga referencia a texto no vacío.",
    "targetSizeMinimum_title": "Los objetivos de puntero deben tener al menos 24x24px de tamaño, o dejar suficiente distancia respecto a otros objetivos",
    "targetSizeMinimum_description": "Comprueba que los objetivos operables por puntero tengan una zona de contacto efectiva de al menos 24 por 24 píxeles CSS, o cumplan una excepción permitida (por ejemplo, espaciado suficiente).",
    "targetSizeMinimum_summary_fail": "Uno o más objetivos de puntero son más pequeños que 24×24 px CSS y están demasiado cerca de otro objetivo.",
    "targetSizeMinimum_hint_fail": "Aumentar el tamaño del objetivo a al menos 24×24 px CSS o agregar suficiente espaciado respecto a los objetivos vecinos.",
    "targetSizeMinimum_summary_cantTell_ambiguousSpacing": "El objetivo puede ser demasiado pequeño y estar demasiado cerca de otro objetivo, pero la superposición está cerca del umbral de detección y no se pudo medir con confianza.",
    "targetSizeMinimum_hint_cantTell_ambiguousSpacing": "Verificar manualmente el espaciado efectivo entre este objetivo y su vecino; aumentar el tamaño del objetivo o el espaciado si la superposición es real.",
    "targetSizeMinimum_summary_cantTell_plausiblyEssential": "El objetivo es demasiado pequeño y está demasiado cerca de otro objetivo, pero puede estar exento por formar parte de un gráfico esencial o una región de mapa de imagen.",
    "targetSizeMinimum_hint_cantTell_plausiblyEssential": "Verificar si el tamaño de este objetivo es realmente esencial para su función (por ejemplo, parte de un SVG/canvas/mapa de imagen); si no lo es, aumentar el tamaño del objetivo o el espaciado.",
    "targetSizeMinimum_notApplicable_noTargets": "Ningún objetivo operable por puntero era elegible para la evaluación.",
    "targetSizeMinimum_pass_allOk": "Todos los objetivos de puntero elegibles cumplen el tamaño mínimo o una excepción permitida.",
    "ariaHidden_focus_title": "Los elementos ocultos con ARIA no deben ser enfocables",
    "ariaHidden_focus_description": "Comprueba que los elementos con aria-hidden=\"true\" no sean enfocables ni contengan descendientes enfocables.",
    "ariaHidden_focus_summary_fail_desc": "El elemento aria-hidden {{element}} contiene {{focusableCount}} elemento(s) enfocable(s).",
    "ariaHidden_focus_summary_fail_self": "El elemento aria-hidden {{element}} es enfocable ({{focusableCount}} elemento(s) enfocable(s)).",
    "ariaHidden_focus_summary_fail_self_and_desc": "El elemento aria-hidden {{element}} es enfocable y contiene {{descendantFocusableCount}} descendiente(s) enfocable(s) ({{focusableCount}} elemento(s) enfocable(s) en total).",
    "ariaHidden_focus_hint_fail": "Eliminar la capacidad de enfoque de los descendientes o eliminar aria-hidden; asegurarse de que el orden de foco y el árbol de accesibilidad permanezcan alineados.",
    "ariaHidden_focus_summary_cantTell_redirect": "El elemento aria-hidden {{element}} recibió el foco, pero el foco se movió de inmediato a otro elemento. Verificar el comportamiento de centinela/trampa de foco.",
    "ariaHidden_focus_hint_cantTell_redirect": "Verificar que se trate de un traspaso intencionado de centinela/trampa de foco y que los usuarios de teclado nunca queden en objetivos de foco ocultos.",
    "cssHidden_focus_title": "Los elementos enfocables no deben estar ocultos visualmente",
    "cssHidden_focus_description": "Comprueba que los elementos enfocables por teclado no estén ocultos visualmente mediante técnicas CSS que puedan dejarlos en el orden de tabulación.",
    "cssHidden_focus_summary_cantTell": "El elemento enfocable {{element}} está oculto visualmente ({{visibilityHints}}).",
    "cssHidden_focus_hint_cantTell": "Hacer visible el elemento cuando pueda recibir foco de teclado, o eliminarlo del orden de tabulación hasta que sea visible.",
    "linkNamePresent_title": "Los enlaces tienen un nombre accesible",
    "linkNamePresent_description": "Comprueba que los enlaces expongan un nombre accesible no vacío.",
    "linkNamePresent_summary_fail": "Este enlace no tiene nombre accesible.",
    "linkNamePresent_hint_fail": "Proporcionar texto de enlace o un mecanismo de nombre accesible (por ejemplo, aria-label) para que las tecnologías de asistencia puedan identificar el enlace.",
    "buttonNamePresent_title": "Los botones tienen un nombre accesible",
    "buttonNamePresent_description": "Comprueba que los botones expongan un nombre accesible no vacío.",
    "buttonNamePresent_summary_fail": "Este botón no tiene nombre accesible.",
    "buttonNamePresent_hint_fail": "Proporcionar texto de botón visible o un mecanismo de nombre accesible programático (por ejemplo, aria-label) para que las tecnologías de asistencia puedan identificar el botón.",
    "binaryControlNamePresent_title": "Los controles binarios tienen un nombre accesible",
    "binaryControlNamePresent_description": "Comprueba que los controles de tipo casilla de verificación, botón de opción e interruptor expongan un nombre accesible no vacío.",
    "binaryControlNamePresent_summary_fail": "Este control no tiene nombre accesible.",
    "binaryControlNamePresent_hint_fail": "Proporcionar una etiqueta, aria-label, aria-labelledby, u otro mecanismo de nombre accesible para que las tecnologías de asistencia puedan identificar el control.",
    "comboboxNamePresent_title": "Los cuadros combinados tienen un nombre accesible",
    "comboboxNamePresent_description": "Comprueba que los elementos con role=\"combobox\" expongan un nombre accesible no vacío.",
    "comboboxNamePresent_summary_fail": "Este cuadro combinado no tiene nombre accesible.",
    "comboboxNamePresent_hint_fail": "Proporcionar aria-label, aria-labelledby o un atributo title; el contenido de texto visible no se expone como el nombre accesible de este cuadro combinado.",
    "dialogNamePresent_title": "Los diálogos tienen un nombre accesible",
    "dialogNamePresent_description": "Comprueba que los elementos con role=\"dialog\" o role=\"alertdialog\" expongan un nombre accesible no vacío.",
    "dialogNamePresent_summary_fail": "Este diálogo no tiene nombre accesible.",
    "dialogNamePresent_hint_fail": "Proporcionar aria-labelledby (preferido) o aria-label para que las tecnologías de asistencia puedan anunciar el diálogo.",
    "menuitemNamePresent_title": "Los elementos de menú tienen un nombre accesible",
    "menuitemNamePresent_description": "Comprueba que los elementos de menú (role=\"menuitem*\", incluidas las variantes casilla/opción) expongan un nombre accesible no vacío.",
    "menuitemNamePresent_summary_fail": "Este elemento de menú no tiene nombre accesible.",
    "menuitemNamePresent_hint_fail": "Proporcionar texto visible que no esté oculto a las tecnologías de asistencia, o proporcionar aria-label o aria-labelledby.",
    "tabNamePresent_title": "Las pestañas tienen un nombre accesible",
    "tabNamePresent_description": "Comprueba que los elementos con role=\"tab\" expongan un nombre accesible no vacío.",
    "tabNamePresent_summary_fail": "Esta pestaña no tiene nombre accesible.",
    "tabNamePresent_hint_fail": "Proporcionar texto de pestaña que no esté oculto a las tecnologías de asistencia, o proporcionar aria-label o aria-labelledby.",
    "sliderNamePresent_title": "Los controles deslizantes tienen un nombre accesible",
    "sliderNamePresent_description": "Comprueba que los controles deslizantes (input[type=\"range\"] y role=\"slider\") expongan un nombre accesible no vacío.",
    "sliderNamePresent_summary_fail": "Este control deslizante no tiene nombre accesible.",
    "sliderNamePresent_hint_fail": "Proporcionar una etiqueta, aria-label o aria-labelledby para que las tecnologías de asistencia puedan identificar el control deslizante.",
    "textboxNamePresent_title": "Los cuadros de texto tienen un nombre accesible",
    "textboxNamePresent_description": "Comprueba que los elementos con role=\"textbox\" expongan un nombre accesible no vacío.",
    "textboxNamePresent_summary_fail": "Este cuadro de texto no tiene nombre accesible.",
    "textboxNamePresent_hint_fail": "Proporcionar aria-label, aria-labelledby o un atributo title; el contenido de texto visible no se expone como el nombre accesible de este cuadro de texto.",
    "searchboxNamePresent_title": "Los cuadros de búsqueda tienen un nombre accesible",
    "searchboxNamePresent_description": "Comprueba que los elementos con role=\"searchbox\" expongan un nombre accesible no vacío.",
    "searchboxNamePresent_summary_fail": "Este cuadro de búsqueda no tiene nombre accesible.",
    "searchboxNamePresent_hint_fail": "Proporcionar aria-label, aria-labelledby o un atributo title; el contenido de texto visible no se expone como el nombre accesible de este cuadro de búsqueda.",
    "spinbuttonNamePresent_title": "Los controles numéricos tienen un nombre accesible",
    "spinbuttonNamePresent_description": "Comprueba que los elementos con role=\"spinbutton\" expongan un nombre accesible no vacío.",
    "spinbuttonNamePresent_summary_fail": "Este control numérico no tiene nombre accesible.",
    "spinbuttonNamePresent_hint_fail": "Proporcionar aria-label, aria-labelledby o un atributo title; el contenido de texto visible no se expone como el nombre accesible de este control numérico.",
    "listboxNamePresent_title": "Los cuadros de lista tienen un nombre accesible",
    "listboxNamePresent_description": "Comprueba que los elementos con role=\"listbox\" expongan un nombre accesible no vacío.",
    "listboxNamePresent_summary_fail": "Este cuadro de lista no tiene nombre accesible.",
    "listboxNamePresent_hint_fail": "Proporcionar aria-label, aria-labelledby o un atributo title; el contenido de texto visible no se expone como el nombre accesible de este cuadro de lista.",
    "optionNamePresent_title": "Las opciones tienen un nombre accesible",
    "optionNamePresent_description": "Comprueba que los elementos con role=\"option\" expongan un nombre accesible no vacío.",
    "optionNamePresent_summary_fail": "Esta opción no tiene nombre accesible.",
    "optionNamePresent_hint_fail": "Proporcionar texto de opción que no esté oculto a las tecnologías de asistencia, o proporcionar aria-label o aria-labelledby.",
    "treeitemNamePresent_title": "Los elementos de árbol tienen un nombre accesible",
    "treeitemNamePresent_description": "Comprueba que los elementos con role=\"treeitem\" expongan un nombre accesible no vacío.",
    "treeitemNamePresent_summary_fail": "Este elemento de árbol no tiene nombre accesible.",
    "treeitemNamePresent_hint_fail": "Proporcionar texto de elemento de árbol que no esté oculto a las tecnologías de asistencia, o proporcionar aria-label o aria-labelledby.",
    "ariaRoleNamePresent_title": "Los roles ARIA de widget/contenedor tienen un nombre accesible",
    "ariaRoleNamePresent_description": "Comprueba que determinados roles ARIA de widget/contenedor expongan un nombre accesible no vacío.",
    "ariaRoleNamePresent_summary_fail": "Este elemento no tiene nombre accesible.",
    "ariaRoleNamePresent_hint_fail": "Proporcionar aria-label o aria-labelledby (preferido), o un atributo title no vacío.",
    "composite_rollup_summary": "Resultado de regla compuesta: {{reasonCode}} ({{testCount}} comprobaciones)",
    "catalog.rules.wcag_111_non_text_content.title": "Contenido no textual: alternativas textuales",
    "catalog.rules.wcag_111_non_text_content.description": "Agrupación de comprobaciones que garantizan que el contenido no textual tenga una alternativa textual apropiada.",
    "catalog.rules.wcag_121_prerecorded_transcript.title": "Solo audio y solo video (pregrabado): transcripción",
    "catalog.rules.wcag_121_prerecorded_transcript.description": "Agrupación de comprobaciones sobre la disponibilidad de transcripción para medios pregrabados de solo audio o solo video.",
    "catalog.rules.wcag_143_contrast_minimum.title": "Contraste: mínimo",
    "catalog.rules.wcag_143_contrast_minimum.description": "Agrupación de comprobaciones del contraste mínimo del texto.",
    "catalog.rules.wcag_146_contrast_enhanced.title": "Contraste: mejorado",
    "catalog.rules.wcag_146_contrast_enhanced.description": "Agrupación de comprobaciones del contraste de texto mejorado.",
    "catalog.rules.wcag_242_page_titled.title": "Página titulada",
    "catalog.rules.wcag_242_page_titled.description": "Agrupación de comprobaciones que garantizan que los documentos tengan un título de página significativo.",
    "catalog.rules.wcag_247_focus_visible.title": "Foco visible",
    "catalog.rules.wcag_247_focus_visible.description": "Agrupación de comprobaciones que garantizan que el foco de teclado no esté oculto y siga siendo perceptible.",
    "catalog.rules.wcag_258_target_size_minimum.title": "Tamaño del objetivo: mínimo",
    "catalog.rules.wcag_258_target_size_minimum.description": "Agrupación de comprobaciones que garantizan que los objetivos de puntero cumplan los requisitos de tamaño mínimo.",
    "catalog.rules.wcag_311_language_of_page.title": "Idioma de la página",
    "catalog.rules.wcag_311_language_of_page.description": "Agrupación de comprobaciones que garantizan que el idioma de la página esté especificado.",
    "catalog.rules.wcag_412_name.title": "Nombre, rol, valor: nombre accesible",
    "catalog.rules.wcag_412_name.description": "Agrupación de comprobaciones que garantizan que los elementos interactivos comunes expongan un nombre accesible no vacío.",
    "labelInName_title": "Etiqueta en el nombre: el nombre accesible contiene el texto visible",
    "labelInName_description": "Comprueba que, cuando un control tiene una etiqueta de texto visible, el nombre accesible contenga ese texto de etiqueta visible (WCAG 2.5.3).",
    "labelInName_summary_fail": "{{element}}: la etiqueta visible \"{{visibleLabel}}\" (de {{labelSource}}) no está incluida en el nombre accesible (de {{nameMechanism}}).",
    "labelInName_hint_fail": "Actualizar aria-label/aria-labelledby (o el texto de la etiqueta visible) para que el nombre accesible incluya el texto de la etiqueta visible.",
    "labelInName_summary_cantTell": "{{element}}: la etiqueta visible \"{{visibleLabel}}\" (de {{labelSource}}) se diferencia del nombre accesible (de {{nameMechanism}}) solo por una abreviatura o por la separación con guion.",
    "labelInName_hint_cantTell": "Comprobar manualmente si ambas redacciones coinciden: el marcado no permite distinguir una abreviatura intencionada de una discrepancia.",
    "ariaRolesValid_title": "El atributo role debe ser un rol ARIA válido y no abstracto",
    "ariaRolesValid_description": "Comprueba que un atributo role=\"\" explícito se resuelva en un rol WAI-ARIA real y no abstracto.",
    "ariaRolesValid_summary_invalid": "role=\"{{role}}\" no es un rol ARIA reconocido.",
    "ariaRolesValid_hint_invalid": "Usar un token de rol ARIA válido, o eliminar el atributo role si no aplica ninguno.",
    "ariaRolesValid_summary_abstract": "role=\"{{role}}\" es un rol ARIA abstracto, que no debe usarse directamente.",
    "ariaRolesValid_hint_abstract": "Reemplazar este rol abstracto por un rol concreto adecuado para el widget o la estructura.",
    "ariaDeprecatedRole_title": "El atributo role no debería usar un rol ARIA obsoleto o desaconsejado para autores",
    "ariaDeprecatedRole_description": "Comprueba que un atributo role=\"\" explícito no use un rol obsoleto según la especificación WAI-ARIA, ni uno reservado para uso interno del agente de usuario (por ejemplo, role=\"generic\").",
    "ariaDeprecatedRole_summary_fail": "Este elemento usa role=\"{{role}}\", que los autores no deben declarar explícitamente.",
    "ariaDeprecatedRole_hint_fail": "{{guidance}}",
    "ariaDeprecatedRole_summary_cantTell": "Este elemento usa role=\"{{role}}\", que está obsoleto en WAI-ARIA (todavía válido, pero desaconsejado).",
    "ariaDeprecatedRole_summary_cantTell_discouraged": "Este elemento usa role=\"{{role}}\", que está reservado para los agentes de usuario (todavía válido, pero desaconsejado).",
    "ariaDeprecatedRole_hint_cantTell": "{{guidance}}",
    "ariaValidAttr_title": "Los atributos aria-* deben ser atributos ARIA reales y definidos",
    "ariaValidAttr_description": "Comprueba que cada nombre de atributo aria-* presente en el DOM sea un atributo real definido por la especificación WAI-ARIA.",
    "ariaValidAttr_summary_fail": "{{attr}} no es un atributo ARIA reconocido.",
    "ariaValidAttr_hint_fail": "Corregir el nombre del atributo (comprobar si hay errores tipográficos), o eliminarlo si no es necesario.",
    "ariaValidAttrValue_title": "Los valores de los atributos aria-* deben coincidir con su tipo declarado",
    "ariaValidAttrValue_description": "Comprueba que cada atributo aria-* reconocido tenga un valor conforme a su tipo declarado por WAI-ARIA (booleano, tri-estado, token, entero, número o referencia de ID).",
    "ariaValidAttrValue_summary_fail": "{{attr}}=\"{{value}}\" no es un valor válido para este atributo.",
    "ariaValidAttrValue_hint_fail": "Usar un valor que coincida con el tipo esperado del atributo (consultar la especificación WAI-ARIA para este atributo).",
    "ariaAllowedAttr_title": "Los atributos aria-* deben estar permitidos para el rol del elemento",
    "ariaAllowedAttr_description": "Comprueba que cada atributo aria-* reconocido presente en un elemento con un rol explícito esté admitido globalmente o admitido por ese rol.",
    "ariaAllowedAttr_summary_fail": "{{attr}} no está permitido en role=\"{{role}}\".",
    "ariaAllowedAttr_hint_fail": "Eliminar este atributo, o usar un rol que lo admita.",
    "ariaAllowedAttr_summary_cantTell": "{{attr}} está obsoleto en role=\"{{role}}\" (todavía permitido, pero desaconsejado).",
    "ariaAllowedAttr_hint_cantTell": "Este atributo se eliminó del conjunto global de ARIA en 1.2; elimínelo o use un rol que lo admita, ya que una versión futura de ARIA podría no permitirlo.",
    "ariaProhibitedAttr_title": "Los atributos de nombrado ARIA no deben usarse en roles que los prohíban",
    "ariaProhibitedAttr_description": "Comprueba que aria-label/aria-labelledby no estén presentes en roles WAI-ARIA cuya especificación prohíbe explícitamente el nombrado ARIA (por ejemplo, generic, emphasis, strong, paragraph).",
    "ariaProhibitedAttr_summary_fail": "{{attr}} está prohibido en role=\"{{role}}\".",
    "ariaProhibitedAttr_hint_fail": "Eliminar este atributo; este rol no debe tener un nombre accesible.",
    "ariaProhibitedAttr_summary_fail_roleless": "Este {{element}} no tiene rol ni ninguna otra fuente de nombre accesible, por lo que {{attr}} no se expone de forma fiable a las tecnologías de asistencia.",
    "ariaProhibitedAttr_hint_fail_roleless": "Asignar a este elemento un rol que admita un nombre accesible (por ejemplo, role=\"img\"/\"button\"), o eliminar este atributo si no cumple ningún propósito sin uno.",
    "ariaProhibitedAttr_summary_cantTell_roleless": "Este {{element}} no tiene rol, por lo que es posible que {{attr}} no se exponga como su nombre accesible a las tecnologías de asistencia; pero el propio contenido del elemento ya proporciona uno.",
    "ariaProhibitedAttr_hint_cantTell_roleless": "Verificar si el contenido de texto existente ya funciona como etiqueta de este elemento; si es así, el atributo de nombrado es redundante, de lo contrario asignar al elemento un rol que admita nombrado (por ejemplo, role=\"img\").",
    "ariaRequiredAttr_title": "Los roles con un estado/propiedad ARIA obligatorio deben incluirlo",
    "ariaRequiredAttr_description": "Comprueba que los elementos con un rol explícito lleven todo estado/propiedad aria-* obligatorio, inequívoco e independiente del contexto para ese rol (por ejemplo, role=\"checkbox\" debe tener aria-checked).",
    "ariaRequiredAttr_summary_fail": "{{attr}} es obligatorio para role=\"{{role}}\", pero falta.",
    "ariaRequiredAttr_hint_fail": "Agregar este atributo con un valor válido para este rol.",
    "ariaAllowedRole_title": "El rol explícito debe estar permitido para su elemento anfitrión",
    "ariaAllowedRole_description": "Comprueba que un atributo role=\"\" explícito sea uno de los roles que la especificación ARIA-in-HTML permite para el elemento anfitrión (por ejemplo, role=\"tab\" no está permitido en <nav>).",
    "ariaAllowedRole_summary_fail": "role=\"{{role}}\" no está permitido en <{{element}}>.",
    "ariaAllowedRole_hint_fail": "Usar un rol permitido para este elemento, o cambiar el elemento anfitrión.",
    "ariaRequiredChildren_title": "Los roles contenedores deben poseer al menos un rol hijo obligatorio",
    "ariaRequiredChildren_description": "Comprueba que los roles contenedores con una entrada documentada de \"elementos propios obligatorios\" (list, listbox, menu, radiogroup, table, grid, tablist, tree, row, ...) contengan al menos un descendiente, o un elemento referenciado por aria-owns, con un rol propio aceptable.",
    "ariaRequiredChildren_summary_fail": "role=\"{{role}}\" no tiene ningún hijo propio con alguno de los roles obligatorios: {{requiredRoles}}.",
    "ariaRequiredChildren_hint_fail": "Agregar un descendiente (o un elemento referenciado por aria-owns) con alguno de los roles propios obligatorios.",
    "ariaProhibitedChildren_title": "Los roles contenedores no deben poseer un hijo con un rol no permitido",
    "ariaProhibitedChildren_description": "Comprueba que todo hijo perteneciente al árbol de accesibilidad de un rol contenedor (list, listbox, menu, menubar, radiogroup, rowgroup, table, grid, treegrid, tablist, tree, row) tenga uno de los roles propios permitidos por ese rol.",
    "ariaProhibitedChildren_summary_fail": "Este elemento tiene role=\"{{childRole}}\", que no es un rol hijo propio permitido dentro del contenedor role=\"{{containerRole}}\" que lo envuelve.",
    "ariaProhibitedChildren_hint_fail": "Eliminar o cambiar este rol para que coincida con uno de los roles propios permitidos del contenedor ({{allowedRoles}}), o mover este elemento fuera del contenedor {{containerRole}}.",
    "ariaProhibitedChildren_summary_fail_roleless": "Este elemento no tiene un rol explícito pero lleva {{attr}}, lo que lo convierte en un nodo real del árbol de accesibilidad que no es un hijo propio permitido dentro del contenedor role=\"{{containerRole}}\" que lo envuelve.",
    "ariaProhibitedChildren_hint_fail_roleless": "Eliminar {{attr}} (o la pertenencia al contenedor role=\"{{containerRole}}\"), o asignar a este elemento role=\"presentation\"/\"none\" si no está pensado para ser su propio nodo del árbol de accesibilidad.",
    "ariaProhibitedChildren_summary_fail_native_focusable": "Este elemento no tiene un rol explícito, pero es enfocable de forma nativa, lo que lo convierte en un nodo real del árbol de accesibilidad que no es un hijo propio permitido dentro del contenedor role=\"{{containerRole}}\" que lo envuelve.",
    "ariaProhibitedChildren_hint_fail_native_focusable": "Asignar a este elemento role=\"presentation\"/\"none\", eliminar su capacidad de enfoque nativa (por ejemplo, quitar el atributo href/tabindex que la otorga), o moverlo fuera del contenedor {{containerRole}}.",
    "ariaRequiredParent_title": "Los roles que requieren un rol de contexto específico deben estar en ese contexto",
    "ariaRequiredParent_description": "Comprueba que los roles con una entrada documentada de \"rol de contexto obligatorio\" (listitem, option, tab, treeitem, row, cell, ...) tengan un ancestro, o un propietario aria-owns, con un rol de contexto aceptable.",
    "ariaRequiredParent_summary_fail": "role=\"{{role}}\" requiere un rol de contexto entre: {{requiredRoles}}, que no se encontró.",
    "ariaRequiredParent_hint_fail": "Colocar este elemento dentro de (o referenciarlo mediante aria-owns desde) un elemento con un rol de contexto aceptable.",
    "deprecatedElements_title": "No deben usarse elementos obsoletos no detenibles (<blink>, <marquee>)",
    "deprecatedElements_description": "Comprueba que no estén presentes elementos HTML obsoletos y no estándar cuyo contenido parpadeante o en desplazamiento no pueda ser pausado, detenido u ocultado por el usuario (<blink>, <marquee>).",
    "deprecatedElements_summary_fail": "El contenido de <{{element}}> no puede ser pausado, detenido ni ocultado por el usuario.",
    "deprecatedElements_hint_fail": "Eliminar este elemento; usar contenido estático, o una animación con un control de pausa/detención visible para el usuario, en su lugar.",
    "iframeNamePresent_title": "Los marcos tienen un nombre accesible",
    "iframeNamePresent_description": "Comprueba que los elementos <iframe>/<frame> expongan un nombre accesible no vacío mediante aria-label, aria-labelledby o el atributo title.",
    "iframeNamePresent_summary_fail": "Este <{{element}}> no tiene nombre accesible.",
    "iframeNamePresent_hint_fail": "Agregar un atributo title (o aria-label/aria-labelledby) que describa el contenido o el propósito del marco.",
    "iframeTitleUnique_title": "Los títulos de los marcos deben ser únicos",
    "iframeTitleUnique_description": "Comprueba que dos elementos <iframe>/<frame> no compartan, dentro del alcance, el mismo valor de atributo title.",
    "iframeTitleUnique_summary_fail": "El título \"{{title}}\" de este <{{element}}> no es único entre los marcos de esta página.",
    "iframeTitleUnique_hint_fail": "Asignar a cada marco un título distinto que describa su contenido o propósito específico.",
    "iframeFocusableContent_title": "Los marcos con tabindex=\"-1\" no deben contener contenido enfocable",
    "iframeFocusableContent_description": "Comprueba que los elementos <iframe>/<frame> del mismo origen con tabindex=\"-1\" no contengan contenido enfocable, ya que los navegadores no propagan esa restricción al documento incrustado del marco.",
    "iframeFocusableContent_summary_fail": "Este <{{element}}> tiene tabindex=\"-1\" pero su contenido incluye elementos enfocables, que siguen siendo alcanzables por teclado.",
    "iframeFocusableContent_hint_fail": "Eliminar el contenido enfocable del marco, o eliminar tabindex=\"-1\" si el marco está pensado para ser alcanzable.",
    "tableHeadersAttrValid_title": "El atributo \"headers\" de la celda de tabla debe referenciar celdas de encabezado válidas",
    "tableHeadersAttrValid_description": "Comprueba que cada id en un atributo headers de <td>/<th> se resuelva en un elemento <th> dentro de la misma tabla (que no falte, que no sea un elemento distinto de th, y que no sea él mismo).",
    "tableHeadersAttrValid_summary_fail": "El atributo headers de este <{{element}}> referencia celda(s) de encabezado no válida(s): {{invalidIds}}.",
    "tableHeadersAttrValid_hint_fail": "Actualizar el atributo headers para que cada id haga referencia a un elemento <th> dentro de la misma tabla.",
    "tableThHasDataCells_title": "Los elementos <th> deben describir al menos una celda de datos",
    "tableThHasDataCells_description": "Comprueba que una tabla que contiene elementos <th> también contenga al menos una celda de datos <td> para que esos encabezados describan.",
    "tableThHasDataCells_summary_fail": "Esta tabla tiene celdas de encabezado pero ninguna celda de datos que describir.",
    "tableThHasDataCells_hint_fail": "Agregar celdas de datos (<td>) a la tabla, o eliminar las celdas de encabezado si la tabla no tiene datos.",
    "ariaHiddenBody_title": "El <body> del documento no debe ser aria-hidden",
    "ariaHiddenBody_description": "Comprueba que <body> no tenga aria-hidden=\"true\", lo que eliminaría toda la página del árbol de accesibilidad.",
    "ariaHiddenBody_summary_fail": "El body del documento tiene aria-hidden=\"true\", lo que oculta toda la página a las tecnologías de asistencia.",
    "ariaHiddenBody_hint_fail": "Eliminar aria-hidden de <body>. Ocultar elementos específicos en su lugar, si esa era la intención.",
    "listChildrenValid_title": "Las listas solo deben contener directamente elementos de lista",
    "listChildrenValid_description": "Comprueba que los elementos <ul>/<ol> solo tengan como hijos directos <li>, <script> o <template>.",
    "listChildrenValid_summary_fail": "Este <{{element}}> contiene un hijo directo que no es un elemento de lista: {{invalidChildren}}.",
    "listChildrenValid_hint_fail": "Usar únicamente <li> (o <script>/<template>) como hijos directos de <ul>/<ol>; mover el resto del marcado dentro de un <li>.",
    "listitemParentValid_title": "Los elementos de lista deben estar dentro de un contenedor de lista",
    "listitemParentValid_description": "Comprueba que los elementos <li> estén contenidos por <ul>, <ol>, o un elemento con role=\"list\".",
    "listitemParentValid_summary_fail": "El padre de este elemento de lista (<{{parentElement}}>) no es un contenedor de lista.",
    "listitemParentValid_hint_fail": "Colocar este <li> dentro de un <ul>/<ol>, o asignar a su padre role=\"list\".",
    "definitionListChildrenValid_title": "Las listas de descripción deben estar estructuradas correctamente",
    "definitionListChildrenValid_description": "Comprueba que los elementos <dl> solo contengan directamente grupos <dt>/<dd> (opcionalmente envueltos en un <div>), <script>, <template> o <style>.",
    "definitionListChildrenValid_summary_fail_invalidChild": "Esta lista de descripción contiene un hijo directo o envuelto que no forma parte de un grupo dt/dd: {{invalidChildren}}.",
    "definitionListChildrenValid_hint_fail_invalidChild": "Usar únicamente <dt>/<dd> (opcionalmente envueltos en un <div>), <script>, <template> o <style> dentro de <dl>.",
    "definitionListChildrenValid_summary_fail_noDtDd": "Esta lista de descripción no tiene ningún grupo término-definición <dt>/<dd>.",
    "definitionListChildrenValid_hint_fail_noDtDd": "Agregar al menos un par <dt>/<dd> dentro de este <dl>.",
    "dlitemParentValid_title": "Los elementos de lista de descripción deben estar dentro de una lista de descripción",
    "dlitemParentValid_description": "Comprueba que los elementos <dt>/<dd> estén contenidos por un <dl>, directamente o mediante un único <div> envolvente.",
    "dlitemParentValid_summary_fail": "El padre (<{{parentElement}}>) de este <{{element}}> no es una lista de descripción.",
    "dlitemParentValid_hint_fail": "Colocar este <dt>/<dd> dentro de un <dl>, directamente o envuelto en un único <div>.",
    "duplicateIdAria_title": "Los IDs referenciados por ARIA deben ser únicos",
    "duplicateIdAria_description": "Comprueba que cualquier valor de id referenciado por un atributo de referencia de ID de ARIA (aria-labelledby, aria-describedby, aria-owns, aria-controls, aria-activedescendant, aria-flowto, aria-errormessage, aria-details) sea único en el documento.",
    "duplicateIdAria_summary_fail": "El id \"{{id}}\" está referenciado por un atributo ARIA, pero lo usan {{duplicateCount}} elementos.",
    "duplicateIdAria_hint_fail": "Hacer únicos dentro del documento los ids referenciados por atributos ARIA.",
    "summaryNamePresent_title": "Los elementos <summary> tienen un nombre accesible",
    "summaryNamePresent_description": "Comprueba que los elementos <summary> expongan un nombre accesible no vacío.",
    "summaryNamePresent_summary_fail": "Este resumen no tiene nombre accesible.",
    "summaryNamePresent_hint_fail": "Proporcionar texto de resumen que no esté oculto a las tecnologías de asistencia, o proporcionar aria-label o aria-labelledby.",
    "metaViewportZoomEnabled_title": "La etiqueta meta viewport no debe deshabilitar el zoom",
    "metaViewportZoomEnabled_description": "Comprueba que <meta name=\"viewport\"> no establezca user-scalable=no ni maximum-scale por debajo de 2 (200%).",
    "metaViewportZoomEnabled_summary_fail": "Esta etiqueta meta viewport restringe la capacidad del usuario para hacer zoom ({{reasons}}).",
    "metaViewportZoomEnabled_hint_fail": "Eliminar user-scalable=no y cualquier maximum-scale por debajo de 2 del contenido de la etiqueta meta viewport.",
    "metaRefreshTimingAbsent_title": "La página no debe usar un meta refresh con temporización",
    "metaRefreshTimingAbsent_description": "Comprueba que <meta http-equiv=\"refresh\"> no imponga un retraso positivo de 20 horas o menos.",
    "metaRefreshTimingAbsent_summary_fail": "Esta página se actualiza automáticamente después de {{delay}} segundos.",
    "metaRefreshTimingAbsent_hint_fail": "Eliminar el meta refresh con temporización, o proporcionar una forma de que los usuarios lo desactiven, lo extiendan o lo pausen antes de que se active.",
    "meterNamePresent_title": "Los medidores tienen un nombre accesible",
    "meterNamePresent_description": "Comprueba que los elementos con role=\"meter\" expongan un nombre accesible no vacío.",
    "meterNamePresent_summary_fail": "Este medidor no tiene nombre accesible.",
    "meterNamePresent_hint_fail": "Proporcionar aria-label, aria-labelledby o un atributo title; el contenido de texto visible no se expone como el nombre accesible de este medidor.",
    "progressbarNamePresent_title": "Las barras de progreso tienen un nombre accesible",
    "progressbarNamePresent_description": "Comprueba que los elementos con role=\"progressbar\" expongan un nombre accesible no vacío.",
    "progressbarNamePresent_summary_fail": "Esta barra de progreso no tiene nombre accesible.",
    "progressbarNamePresent_hint_fail": "Proporcionar aria-label, aria-labelledby o un atributo title; el contenido de texto visible no se expone como el nombre accesible de esta barra de progreso.",
    "tooltipNamePresent_title": "La información sobre herramientas tiene un nombre accesible",
    "tooltipNamePresent_description": "Comprueba que los elementos con role=\"tooltip\" expongan un nombre accesible no vacío.",
    "tooltipNamePresent_summary_fail": "Esta información sobre herramientas no tiene nombre accesible.",
    "tooltipNamePresent_hint_fail": "Proporcionar texto de información sobre herramientas que no esté oculto a las tecnologías de asistencia, o proporcionar aria-label o aria-labelledby.",
    "serverSideImageMapAbsent_title": "Las imágenes no deben usar un mapa de imagen del lado del servidor",
    "serverSideImageMapAbsent_description": "Comprueba que los elementos <img> no lleven el atributo ismap (los mapas de imagen del lado del servidor no tienen equivalente operable por teclado).",
    "serverSideImageMapAbsent_summary_fail": "Esta imagen usa un mapa de imagen del lado del servidor, que no tiene equivalente operable por teclado.",
    "serverSideImageMapAbsent_hint_fail": "Reemplazar el mapa de imagen del lado del servidor (ismap) por un mapa de imagen del lado del cliente (<map>/<area>) o por enlaces/botones accesibles independientes.",
    "formControlSingleLabel_title": "Los controles de formulario no deben tener varias etiquetas",
    "formControlSingleLabel_description": "Comprueba que un control de formulario esté asociado a como máximo una <label> (por envoltura o mediante label[for]).",
    "formControlSingleLabel_summary_fail": "Este <{{element}}> está asociado a {{labelCount}} etiquetas.",
    "formControlSingleLabel_hint_fail": "Mantener solo una <label> por control de formulario (ya sea envolviéndolo o referenciándolo mediante for/id).",
    "formControlSingleLabel_summary_cantTell": "Este <{{element}}> tiene una <label> que lo etiqueta más una asociación de <label> vacía adicional; verifique cómo se anuncia.",
    "formControlSingleLabel_hint_cantTell": "Elimine la <label> vacía redundante para que solo una <label> quede asociada al control.",
    "nestedInteractiveControlsAbsent_title": "Los controles interactivos no deben estar anidados",
    "nestedInteractiveControlsAbsent_description": "Comprueba que un control interactivo (enlace, botón, control de formulario o rol de widget ARIA) no contenga otro control interactivo.",
    "nestedInteractiveControlsAbsent_summary_fail": "Este <{{element}}> contiene uno o más controles interactivos anidados: {{nestedElements}}.",
    "nestedInteractiveControlsAbsent_hint_fail": "Mover el/los control(es) interactivo(s) anidado(s) fuera de este elemento; los controles interactivos anidados no son operables de forma fiable mediante tecnología de asistencia.",
    "bypassBlocksPresent_title": "La página debe proporcionar una forma de omitir bloques repetidos",
    "bypassBlocksPresent_description": "Comprueba que la página tenga al menos un mecanismo reconocido de omisión de bloques del criterio de éxito 2.4.1 de WCAG: un landmark main, un enlace de anclaje funcional dentro de la misma página, o un encabezado.",
    "bypassBlocksPresent_summary_cantTell": "No se detectó ninguna forma reconocida de omitir bloques de contenido repetidos en esta página; verifique que exista un mecanismo de omisión.",
    "bypassBlocksPresent_hint_cantTell": "Confirme que la página ofrece un mecanismo de omisión: un landmark main (<main> o role=\"main\"), un enlace funcional de \"saltar al contenido\", o elementos de encabezado que las tecnologías de asistencia puedan usar para saltar el contenido repetido. (Un mecanismo puede estar oculto temporalmente —por ejemplo, mientras un diálogo modal deja la página inerte— o proporcionarse a nivel de sitio; esto requiere confirmación humana.)",
    "landmarkBannerIsTopLevel_title": "El landmark banner debe ser de nivel superior",
    "landmarkBannerIsTopLevel_description": "Comprueba que el landmark banner (role=\"banner\" o un <header> no anidado) no esté anidado dentro de otra región landmark.",
    "landmarkBannerIsTopLevel_summary_cantTell": "Este landmark banner está anidado dentro de otra región landmark.",
    "landmarkBannerIsTopLevel_hint_cantTell": "Mover el landmark banner (header/role=\"banner\") para que no esté contenido por otro landmark; un banner debe ser una región de nivel superior de la página.",
    "landmarkContentinfoIsTopLevel_title": "El landmark contentinfo debe ser de nivel superior",
    "landmarkContentinfoIsTopLevel_description": "Comprueba que el landmark contentinfo (role=\"contentinfo\" o un <footer> no anidado) no esté anidado dentro de otra región landmark.",
    "landmarkContentinfoIsTopLevel_summary_cantTell": "Este landmark contentinfo está anidado dentro de otra región landmark.",
    "landmarkContentinfoIsTopLevel_hint_cantTell": "Mover el landmark contentinfo (footer/role=\"contentinfo\") para que no esté contenido por otro landmark; contentinfo debe ser una región de nivel superior de la página.",
    "landmarkMainIsTopLevel_title": "El landmark main debe ser de nivel superior",
    "landmarkMainIsTopLevel_description": "Comprueba que el landmark main (role=\"main\" o <main>) no esté anidado dentro de otra región landmark.",
    "landmarkMainIsTopLevel_summary_cantTell": "Este landmark main está anidado dentro de otra región landmark.",
    "landmarkMainIsTopLevel_hint_cantTell": "Mover el landmark main (<main>/role=\"main\") para que no esté contenido por otro landmark; main debe ser una región de nivel superior de la página.",
    "landmarkNoDuplicateBanner_title": "La página no debe tener más de un landmark banner",
    "landmarkNoDuplicateBanner_description": "Comprueba que exista como máximo un landmark banner (role=\"banner\" o un <header> no anidado) en la página.",
    "landmarkNoDuplicateBanner_summary_cantTell": "Esta página tiene más de un landmark banner.",
    "landmarkNoDuplicateBanner_hint_cantTell": "Mantener solo un landmark banner (header/role=\"banner\") por página.",
    "landmarkNoDuplicateContentinfo_title": "La página no debe tener más de un landmark contentinfo",
    "landmarkNoDuplicateContentinfo_description": "Comprueba que exista como máximo un landmark contentinfo (role=\"contentinfo\" o un <footer> no anidado) en la página.",
    "landmarkNoDuplicateContentinfo_summary_cantTell": "Esta página tiene más de un landmark contentinfo.",
    "landmarkNoDuplicateContentinfo_hint_cantTell": "Mantener solo un landmark contentinfo (footer/role=\"contentinfo\") por página.",
    "landmarkNoDuplicateMain_title": "La página no debe tener más de un landmark main",
    "landmarkNoDuplicateMain_description": "Comprueba que exista como máximo un landmark main (role=\"main\" o <main>) en la página.",
    "landmarkNoDuplicateMain_summary_cantTell": "Esta página tiene más de un landmark main.",
    "landmarkNoDuplicateMain_hint_cantTell": "Mantener solo un landmark main (<main>/role=\"main\") por página.",
    "landmarkOneMain_title": "La página debería tener un landmark main",
    "landmarkOneMain_description": "Comprueba que la página tenga al menos un landmark main (role=\"main\" o <main>).",
    "landmarkOneMain_summary_cantTell_missing": "Esta página no tiene ningún landmark main.",
    "landmarkOneMain_hint_cantTell_missing": "Agregar un landmark main (<main> o role=\"main\") alrededor del contenido principal de la página.",
    "landmarkUnique_title": "Los landmarks con el mismo rol deben tener nombres únicos",
    "landmarkUnique_description": "Comprueba que, cuando dos o más landmarks comparten el mismo rol, cada uno tenga un nombre accesible distinto.",
    "landmarkUnique_summary_cantTell_duplicateName": "Este landmark {{role}} comparte su nombre accesible con otro landmark {{role}}.",
    "landmarkUnique_summary_cantTell_bothUnnamed": "Este landmark {{role}} no tiene nombre accesible, y existe más de un landmark {{role}} sin nombre en esta página.",
    "landmarkUnique_hint_cantTell": "Asignar a cada landmark {{role}} un nombre distinto mediante aria-label o aria-labelledby.",
    "emptyHeading_title": "Los encabezados no deben estar vacíos",
    "emptyHeading_description": "Comprueba que los elementos de encabezado (<h1>-<h6> o role=\"heading\") tengan un nombre accesible no vacío.",
    "emptyHeading_summary_cantTell": "Este encabezado no tiene nombre accesible.",
    "emptyHeading_hint_cantTell": "Agregar contenido de texto (o aria-label/aria-labelledby) a este encabezado, o eliminarlo si no es necesario.",
    "headingOrder_title": "Los niveles de encabezado no deben saltar un nivel",
    "headingOrder_description": "Comprueba que los niveles de encabezado aumenten como máximo en uno a la vez en el orden del documento.",
    "headingOrder_summary_cantTell": "Este encabezado salta del nivel {{fromLevel}} al nivel {{toLevel}}, saltándose un nivel.",
    "headingOrder_hint_cantTell": "Usar niveles de encabezado consecutivos (no saltar un nivel al profundizar) para que el esquema del documento se mantenga predecible.",
    "pageHasHeadingOne_title": "La página debería tener un encabezado de nivel uno",
    "pageHasHeadingOne_description": "Comprueba que la página tenga al menos un encabezado de nivel uno (<h1> o role=\"heading\" con aria-level=\"1\").",
    "pageHasHeadingOne_summary_cantTell": "Esta página no tiene ningún encabezado de nivel uno.",
    "pageHasHeadingOne_hint_cantTell": "Agregar un encabezado de nivel uno (<h1> o role=\"heading\" aria-level=\"1\") que identifique el contenido principal de la página.",
    "accesskeys_title": "Los valores de accesskey deben ser únicos",
    "accesskeys_description": "Comprueba que no haya dos elementos en la página que compartan el mismo valor de atributo accesskey.",
    "accesskeys_summary_cantTell": "El accesskey de este elemento se comparte con otro elemento de la página.",
    "accesskeys_hint_cantTell": "Hacer único cada valor de accesskey en toda la página.",
    "scopeAttrValid_title": "El atributo scope debe tener un valor válido",
    "scopeAttrValid_description": "Comprueba que scope=\"...\" sea uno de los valores row, col, rowgroup o colgroup.",
    "scopeAttrValid_summary_cantTell": "Este valor del atributo scope no se reconoce.",
    "scopeAttrValid_hint_cantTell": "Usar uno de los valores row, col, rowgroup o colgroup para el atributo scope.",
    "tabindex_title": "tabindex no debería ser mayor que 0",
    "tabindex_description": "Comprueba que los valores de tabindex sean 0 o negativos, no un número positivo.",
    "tabindex_summary_cantTell": "Este elemento tiene un tabindex positivo, lo que sobrescribe el orden de tabulación natural.",
    "tabindex_hint_cantTell": "Usar tabindex=\"0\" (o un valor negativo para eliminarlo del orden de tabulación) en lugar de un número positivo; corregir el orden del DOM si se necesita un orden de tabulación diferente.",
    "emptyTableHeader_title": "Las celdas de encabezado de tabla no deben estar vacías",
    "emptyTableHeader_description": "Comprueba que los elementos <th> tengan contenido de texto visible; un encabezado nombrado únicamente mediante aria-label/aria-labelledby también se señala, ya que el soporte real de lectores de pantalla/navegadores para eso es inconsistente.",
    "emptyTableHeader_summary_cantTell": "Esta celda de encabezado de tabla no tiene nombre accesible.",
    "emptyTableHeader_hint_cantTell": "Agregar contenido de texto (o aria-label/aria-labelledby) a esta celda de encabezado, o eliminarla si no es necesaria.",
    "emptyTableHeader_summary_cantTell_ariaOnly": "Esta celda de encabezado de tabla no tiene texto visible; su único nombre accesible proviene de aria-label/aria-labelledby, que se sabe que ciertas combinaciones reales de lector de pantalla/navegador (por ejemplo, NVDA+Firefox, iOS VoiceOver+Safari) ignoran en elementos <th>.",
    "emptyTableHeader_hint_cantTell_ariaOnly": "Agregar contenido de texto visible a esta celda de encabezado (además de, o en lugar de, aria-label/aria-labelledby); el texto visible es el único mecanismo de nombrado confirmado que funciona en los lectores de pantalla probados.",
    "labelTitleOnly_title": "Los controles de formulario no deben usar title como única etiqueta",
    "labelTitleOnly_description": "Comprueba que un control de formulario con un atributo title también tenga una etiqueta real (elemento label, aria-label o aria-labelledby).",
    "labelTitleOnly_summary_cantTell": "Este control de formulario depende del atributo title como única etiqueta.",
    "labelTitleOnly_hint_cantTell": "Agregar una <label> visible (o aria-label/aria-labelledby) además de, o en lugar de, el atributo title.",
    "imageRedundantAlt_title": "El texto alt de la imagen no debe duplicar el texto visible adyacente",
    "imageRedundantAlt_description": "Comprueba que el texto alt de un <img> no sea idéntico a otro texto visible ya presente en su elemento padre inmediato.",
    "imageRedundantAlt_summary_cantTell": "El texto alt de esta imagen duplica otro texto visible justo al lado.",
    "imageRedundantAlt_hint_cantTell": "Dejar el texto alt vacío (alt=\"\") si la imagen es puramente decorativa junto al texto, o eliminar la duplicación redundante.",
    "tableDuplicateName_title": "El caption de la tabla no debe duplicar su atributo summary",
    "tableDuplicateName_description": "Comprueba que el texto del <caption> de una <table> no sea idéntico a su atributo summary (obsoleto).",
    "tableDuplicateName_summary_cantTell": "El caption de esta tabla duplica su atributo summary.",
    "tableDuplicateName_hint_cantTell": "Eliminar el atributo summary redundante, o hacer que proporcione información diferente a la del caption.",
    "metaViewportLarge_title": "La etiqueta meta viewport debería permitir el zoom hasta el 500%",
    "metaViewportLarge_description": "Comprueba que <meta name=\"viewport\"> no establezca user-scalable=no ni maximum-scale por debajo de 5 (500%).",
    "metaViewportLarge_summary_cantTell": "Esta etiqueta meta viewport restringe el zoom por debajo del objetivo recomendado del 500%.",
    "metaViewportLarge_hint_cantTell": "Eliminar user-scalable=no y elevar maximum-scale a al menos 5 (500%) si es posible.",
    "presentationRoleConflict_title": "El rol presentacional no debe entrar en conflicto con un atributo ARIA global ni con la capacidad de enfoque",
    "presentationRoleConflict_description": "Comprueba que role=\"presentation\"/\"none\" (incluido el rol de presentación implícito de <img alt=\"\">) no se combine con un atributo ARIA global (aria-label, aria-hidden, aria-describedby, ...) ni con la capacidad de enfoque (tabindex/nativa).",
    "presentationRoleConflict_summary_cantTell": "Este elemento con role=\"{{role}}\" también tiene una condición conflictiva ({{attrs}}), lo que restaura su rol implícito y anula la intención presentacional.",
    "presentationRoleConflict_hint_cantTell": "Eliminar el/los atributo(s) de nombrado conflictivo(s) y/o la capacidad de enfoque (tabindex/nativa) si el elemento debe permanecer presentacional, o eliminar role=\"presentation\"/\"none\" si debe exponerse a las tecnologías de asistencia.",
    "region_title": "El contenido de la página debería estar dentro de una región landmark",
    "region_description": "Comprueba que el contenido bajo <body> esté contenido dentro de una región landmark.",
    "region_summary_cantTell": "Este contenido no está contenido dentro de ninguna región landmark.",
    "region_hint_cantTell": "Mover este contenido dentro de una región landmark (main, nav, aside, una section etiquetada, etc.).",
    "skipLink_title": "El enlace de salto debe tener un destino resoluble y utilizable",
    "skipLink_description": "Comprueba que el fragmento href de un enlace de \"saltar a...\" se resuelva en un elemento real y actualmente utilizable del documento.",
    "skipLink_summary_cantTell": "El destino de este enlace de salto no existe.",
    "skipLink_hint_cantTell": "Apuntar el href del enlace de salto a un id que exista en el documento, o agregar el elemento destino que falta.",
    "skipLink_summary_unusableTarget_cantTell": "Este enlace de salto apunta a un destino que existe pero que actualmente no es utilizable.",
    "skipLink_hint_unusableTarget_cantTell": "Apuntar este enlace de salto a un destino que esté expuesto y sea utilizable como destino de navegación.",
    "autocompleteValid_title": "El atributo autocomplete debe tener un valor de autocompletado válido",
    "autocompleteValid_description": "Comprueba que un atributo autocomplete no vacío sea \"on\"/\"off\" o una lista de tokens de detalle de autocompletado bien formada.",
    "autocompleteValid_summary_fail": "Este valor del atributo autocomplete no es un valor de autocompletado válido.",
    "autocompleteValid_hint_fail": "Usar \"on\"/\"off\", o una lista de tokens de autocompletado válida (por ejemplo, \"shipping street-address\", \"cc-number\").",
    "htmlXmlLangMismatch_title": "lang y xml:lang no deben estar en desacuerdo",
    "htmlXmlLangMismatch_description": "Comprueba que los atributos lang y xml:lang del elemento <html> declaren el mismo idioma principal, cuando ambos están presentes.",
    "htmlXmlLangMismatch_summary_fail": "Los atributos lang (\"{{lang}}\") y xml:lang (\"{{xmlLang}}\") declaran idiomas diferentes.",
    "htmlXmlLangMismatch_hint_fail": "Hacer que lang y xml:lang declaren el mismo idioma principal, o eliminar el atributo obsoleto xml:lang.",
    "avoidInlineSpacing_title": "El estilo en línea no debe forzar el espaciado de texto por debajo del umbral WCAG",
    "avoidInlineSpacing_description": "Comprueba que cuando el estilo en línea fuerza line-height, letter-spacing o word-spacing con !important, el valor ya cumple WCAG 1.4.12, por lo que al usuario no le queda nada que anular.",
    "avoidInlineSpacing_summary_fail": "El estilo en línea de este elemento fuerza {{properties}} con !important, bloqueando las anulaciones de espaciado de texto del usuario.",
    "avoidInlineSpacing_hint_fail": "Eliminar !important de line-height/letter-spacing/word-spacing en los estilos en línea para que los usuarios puedan anular el espaciado de texto.",
    "metaRefreshNoExceptions_title": "La página no debe usar un meta refresh en absoluto (AAA)",
    "metaRefreshNoExceptions_description": "Comprueba que <meta http-equiv=\"refresh\"> no esté presente en absoluto, independientemente del retraso; es la contraparte más estricta de nivel AAA de la comprobación de nivel A que solo se aplica a los retrasos positivos.",
    "metaRefreshNoExceptions_summary_fail": "Esta página usa un meta refresh, que es un cambio de contexto automático no iniciado por el usuario.",
    "metaRefreshNoExceptions_hint_fail": "Eliminar el meta refresh; activar la redirección/actualización solo en respuesta a una acción del usuario.",
    "validLang_title": "El atributo lang del elemento debe ser sintácticamente válido",
    "validLang_description": "Comprueba que cualquier elemento (distinto de la raíz <html>) con un atributo lang no vacío use una etiqueta de idioma sintácticamente válida.",
    "validLang_summary_fail": "Este valor del atributo lang (\"{{value}}\") no es una etiqueta de idioma sintácticamente válida.",
    "validLang_hint_fail": "Usar una etiqueta de idioma BCP47 válida (por ejemplo, \"fr\", \"es-MX\").",
    "linkInTextBlock_title": "Los enlaces dentro de bloques de texto deben distinguirse del texto circundante sin depender únicamente del color",
    "linkInTextBlock_description": "Comprueba que un enlace dentro de un bloque de texto se distinga visualmente del texto circundante mediante subrayado, una diferencia de grosor/estilo de fuente, o una diferencia de contraste de color suficiente (>=3:1); no solo por el color.",
    "linkInTextBlock_summary_fail": "Este enlace dentro de un bloque de texto depende únicamente del color para distinguirse del texto circundante.",
    "linkInTextBlock_hint_fail": "Agregar un subrayado, una diferencia de grosor/estilo de fuente, o aumentar el contraste de color entre el enlace y el texto circundante a al menos 3:1.",
    "noAutoplayAudio_title": "El audio en reproducción automática debería proporcionar un mecanismo de pausa/detención o control de volumen",
    "noAutoplayAudio_description": "Señala elementos <audio>/<video> que se reproducen automáticamente sin silenciar y sin un atributo de controles nativo, para su revisión manual frente a la excepción de 3 segundos del criterio de éxito 1.4.2 de WCAG.",
    "noAutoplayAudio_summary_cantTell": "Este elemento reproduce audio automáticamente sin un mecanismo nativo de pausa/detención o control de volumen.",
    "noAutoplayAudio_hint_cantTell": "Si este clip dura más de 3 segundos, agregar un atributo controls (o un mecanismo personalizado equivalente) para que los usuarios puedan pausarlo/detenerlo o controlar su volumen independientemente del volumen del sistema.",
    "videoCaption_title": "El video pregrabado debería proporcionar una pista de subtítulos",
    "videoCaption_description": "Señala elementos <video> sin un hijo <track kind=\"captions\"|\"subtitles\">, para su revisión manual sobre si el video tiene una pista de audio que necesita subtítulos.",
    "videoCaption_summary_cantTell": "Este video no tiene ninguna pista de subtítulos (captions o subtitles).",
    "videoCaption_hint_cantTell": "Si este video tiene una pista de audio que transmite información, agregar un <track kind=\"captions\" src=\"...\"> con el contenido subtitulado.",
    "scrollableRegionFocusable_title": "Las regiones desplazables sin contenido enfocable deberían ser enfocables por teclado",
    "scrollableRegionFocusable_description": "Señala elementos cuyo CSS declara overflow:auto/scroll, que no contienen ningún descendiente enfocable, y que no son enfocables por teclado por sí mismos, para su revisión manual sobre si su contenido realmente desborda y necesita acceso de desplazamiento por teclado.",
    "scrollableRegionFocusable_summary_cantTell": "Este elemento declara overflow:auto/scroll, no tiene ningún descendiente enfocable, y no es enfocable por teclado por sí mismo.",
    "scrollableRegionFocusable_hint_cantTell": "Si el contenido de esta región realmente desborda, agregar tabindex=\"0\" (y una etiqueta adecuada) para que los usuarios de teclado puedan enfocarla y desplazarse con las teclas de flecha.",
    "identicalLinksSamePurpose_title": "Los enlaces con el mismo nombre accesible deberían llevar al mismo destino",
    "identicalLinksSamePurpose_description": "Señala grupos de enlaces que comparten el mismo nombre accesible pero resuelven a más de un destino distinto, para su revisión manual sobre si cumplen el mismo propósito.",
    "identicalLinksSamePurpose_summary_cantTell": "Este enlace comparte un nombre accesible con otros enlaces de la página que llevan a un destino diferente.",
    "identicalLinksSamePurpose_hint_cantTell": "Asegurarse de que los enlaces con el mismo texto cumplan el mismo propósito, o hacer que el texto del enlace sea lo bastante distintivo para describir cada destino.",
    "ariaBrailleEquivalent_title": "aria-braillelabel/aria-brailleroledescription deben tener un equivalente no braille",
    "ariaBrailleEquivalent_description": "Comprueba que los elementos que usan aria-braillelabel también tengan un nombre accesible normal, y que los elementos que usan aria-brailleroledescription también tengan aria-roledescription.",
    "ariaBrailleEquivalent_summary_fail": "Este elemento tiene {{attr}} pero no {{requires}}, su equivalente no braille.",
    "ariaBrailleEquivalent_hint_fail": "{{attr}} es un complemento específico para braille, no un reemplazo; proporcionar también {{requires}}.",
    "ariaConditionalAttr_title": "aria-errormessage requiere que aria-invalid tenga un valor distinto de false",
    "ariaConditionalAttr_description": "Comprueba que los elementos con aria-errormessage también tengan aria-invalid establecido en \"true\", \"grammar\" o \"spelling\"; de lo contrario, el mensaje de error se elimina del árbol de accesibilidad.",
    "ariaConditionalAttr_summary_fail": "Este elemento tiene aria-errormessage pero aria-invalid falta o es \"false\", por lo que el mensaje de error no se expone.",
    "ariaConditionalAttr_hint_fail": "Establecer aria-invalid en \"true\" (o \"grammar\"/\"spelling\") siempre que aria-errormessage deba exponerse a las tecnologías de asistencia.",
    "ariaCheckedStateMismatch_title": "El aria-checked de una casilla/botón de opción nativo debe coincidir con su estado real",
    "ariaCheckedStateMismatch_description": "Señala un <input type=\"checkbox\">/<input type=\"radio\"> nativo cuyo valor explícito de aria-checked no coincide con su estado real marcado/indeterminado, para su revisión manual.",
    "ariaCheckedStateMismatch_summary_cantTell": "El valor de aria-checked de este elemento no coincide con su estado real marcado/indeterminado.",
    "ariaCheckedStateMismatch_hint_cantTell": "Establecer aria-checked para que coincida con el estado real del elemento, o eliminarlo; una casilla/botón de opción nativo ya expone este estado sin necesidad de él.",
    "cssOrientationLock_title": "El CSS no debe bloquear la página a una única orientación",
    "cssOrientationLock_description": "Comprueba que ninguna regla @media (orientation: portrait|landscape) establezca un transform: rotate(...) en la página, una técnica conocida para anular la orientación del dispositivo.",
    "cssOrientationLock_summary_fail": "Una media query \"{{mediaText}}\" rota \"{{selectorText}}\", bloqueando la página a una sola orientación.",
    "cssOrientationLock_hint_fail": "Eliminar la transformación rotate() de la media query de orientación; dejar que la página responda de forma natural a la orientación del dispositivo en lugar de forzar una rotación visual.",
    "ariaText_title": "Los elementos con role=\"text\" no deberían tener descendientes enfocables",
    "ariaText_description": "Comprueba que los elementos con role=\"text\" no contengan ningún descendiente enfocable (enlace, botón, control de formulario, tabindex, iframe o contenteditable).",
    "ariaText_summary_cantTell": "Este elemento con role=\"text\" contiene un descendiente enfocable.",
    "ariaText_hint_cantTell": "Eliminar role=\"text\" (o eliminar el descendiente enfocable); una región de \"texto sin formato\" no debería contener contenido enfocable.",
    "focusOrderSemantics_title": "Los elementos agregados al orden de tabulación deberían tener semántica interactiva",
    "focusOrderSemantics_description": "Señala elementos con tabindex >= 0 cuyo rol explícito es un rol estructural/documental no interactivo (por ejemplo, heading, list, region, presentation), para su revisión manual.",
    "focusOrderSemantics_summary_cantTell": "Este elemento está en el orden de tabulación (tabindex=\"{{tabindex}}\") pero tiene un rol no interactivo (\"{{role}}\").",
    "focusOrderSemantics_hint_cantTell": "Eliminar tabindex si este elemento no está pensado para ser interactivo, o usar un rol interactivo que coincida con su comportamiento real.",
    "pAsHeading_title": "Un <p> con estilo de encabezado probablemente debería ser un encabezado real",
    "pAsHeading_description": "Señala elementos <p> cortos cuyo texto completo está en negrita y se renderiza a >=18px, para su revisión manual sobre si debería usarse un elemento de encabezado real en su lugar.",
    "pAsHeading_summary_cantTell": "Este párrafo está completamente en negrita y se renderiza con un tamaño similar al de un encabezado.",
    "pAsHeading_hint_cantTell": "Si este texto introduce una nueva sección, usar un elemento de encabezado real (<h1>-<h6> o role=\"heading\") en lugar de dar a un párrafo el estilo de un encabezado.",
    "tableFakeCaption_title": "La primera fila de una tabla no debería sustituir a un <caption> real",
    "tableFakeCaption_description": "Señala tablas sin <caption> cuya primera fila tiene una única celda no vacía mientras que las demás filas tienen varias celdas, para su revisión manual sobre si esa celda actúa como un caption falso.",
    "tableFakeCaption_summary_cantTell": "Esta tabla no tiene <caption>, pero su primera fila es una única celda situada sobre filas con varias celdas; puede estar actuando como un caption falso.",
    "tableFakeCaption_hint_cantTell": "Si esta celda está pensada para describir la tabla, usar un elemento <caption> real en lugar de una única celda en la primera fila.",
    "tdHasHeader_title": "Las celdas de datos en tablas grandes deben tener un encabezado asociado",
    "tdHasHeader_description": "Comprueba que cada <td> en una tabla grande y simple (sin colspan/rowspan) tenga un encabezado asociado, ya sea mediante un atributo headers, un <th> de columna implícito por encima de ella, o un <th> de fila implícito a su izquierda.",
    "tdHasHeader_summary_fail": "Esta celda de datos no tiene ningún encabezado asociado (sin atributo headers, sin <th> de columna por encima, sin <th> de fila a la izquierda).",
    "tdHasHeader_hint_fail": "Agregar un atributo headers que referencie el/los id de <th> correspondiente(s), o reestructurar la tabla para que esta celda tenga un encabezado de fila/columna implícito.",
    "mouseOnlyEventHandlers_title": "Los controladores de eventos en línea exclusivos de puntero deberían tener un equivalente alcanzable por teclado",
    "mouseOnlyEventHandlers_description": "Señala elementos con un controlador de eventos en línea exclusivo de puntero (onmouseover, onmouseout, onmousedown, onmouseup, ondblclick, onmousemove, onmouseenter, onmouseleave) y sin un controlador equivalente alcanzable por teclado (onkeydown/onkeyup/onkeypress/onfocus/onblur), para su revisión manual.",
    "mouseOnlyEventHandlers_summary_cantTell": "Este elemento tiene {{attrs}} pero ningún controlador equivalente alcanzable por teclado.",
    "mouseOnlyEventHandlers_hint_cantTell": "Agregar onkeydown/onkeyup/onkeypress (o onfocus/onblur para comportamiento activado por hover) para que esta funcionalidad también sea alcanzable por teclado.",
    "linkNameQuality_title": "El texto del enlace debe ser descriptivo, no genérico",
    "linkNameQuality_description": "Señala enlaces cuyo nombre accesible completo es una frase no descriptiva conocida (por ejemplo, \"haz clic aquí\", \"leer más\", \"más\"), para su revisión manual sobre si el propósito queda claro sin contexto adicional.",
    "linkNameQuality_summary_cantTell": "El nombre accesible de este enlace (\"{{name}}\") es una frase genérica y no descriptiva.",
    "linkNameQuality_hint_cantTell": "Hacer que el propio texto del enlace describa su destino/propósito (por ejemplo, \"Descargar la guía de precios 2026\" en lugar de \"Descargar\"), o confirmar que el contexto circundante ya deja claro el propósito."
  },
  "fr": {
    "img_altPresent_title": "<img> doit avoir un attribut alt",
    "img_altPresent_description": "Vérifie que les éléments <img> fournissent un attribut alt afin de proposer un mécanisme d’alternative textuelle.",
    "img_altPresent_summary_fail": "Attribut alt manquant sur <img>.",
    "img_altPresent_hint_fail": "Ajoutez un attribut alt (utilisez alt=\"\" uniquement pour les images décoratives).",
    "area_altPresent_title": "<area> doit avoir un attribut alt",
    "area_altPresent_description": "Vérifie que les éléments <area> fournissent un attribut alt afin de proposer un mécanisme d’alternative textuelle.",
    "area_altPresent_summary_fail": "Attribut alt manquant sur <area>.",
    "area_altPresent_hint_fail": "Ajoutez un attribut alt (utilisez alt=\"\" uniquement pour les zones décoratives).",
    "inputImage_altPresent_title": "<input type=\"image\"> doit avoir un attribut alt",
    "inputImage_altPresent_description": "Vérifie que les éléments <input type=\"image\"> fournissent un attribut alt afin de proposer un mécanisme d’alternative textuelle.",
    "inputImage_altPresent_summary_fail": "Attribut alt manquant sur <input type=\"image\">.",
    "inputImage_altPresent_hint_fail": "Ajoutez un attribut alt (utilisez alt=\"\" uniquement lorsqu’un nom accessible séparé est fourni).",
    "inputImage_altPresent_summary_defaultName": "Le nom accessible est celui par defaut du navigateur pour un bouton image et n'apporte aucune information.",
    "inputImage_altPresent_hint_defaultName": "Remplacez-le par un texte decrivant l action du bouton, par exemple \"Rechercher\".",
    "inputImage_altPresent_summary_emptyAlt": "Un alt=\"\" vide sur <input type=\"image\"> laisse le controle sans nom.",
    "inputImage_altPresent_hint_emptyAlt": "Decrivez l action dans alt, ou nommez le controle avec aria-label ou aria-labelledby.",
    "ariaHidden_programmaticFocus_review_title": "Vérifier le focus programmatique avec aria-hidden",
    "ariaHidden_programmaticFocus_review_description": "Signale les éléments aria-hidden considérés comme éligibles uniquement via un focus programmatique (ex. tabindex < 0). Vérifiez l’intention de gestion du focus et l’exposition aux technologies d’assistance.",
    "ariaHidden_programmaticFocus_review_summary": "Vérification : un élément aria-hidden est focusable de façon programmatique.",
    "ariaHidden_programmaticFocus_review_hint": "Vérifiez que la gestion du focus est intentionnelle et que l’élément doit rester masqué aux technologies d’assistance.",
    "canvas_textAltPresent_title": "<canvas> doit fournir une alternative textuelle",
    "canvas_textAltPresent_description": "Vérifie que les éléments <canvas> fournissent une alternative textuelle via un contenu de repli ou un nom accessible.",
    "canvas_textAltPresent_summary_fail": "Alternative textuelle manquante pour <canvas>.",
    "canvas_textAltPresent_hint_fail": "Fournissez un texte de repli dans <canvas> ou un nom accessible (par ex. aria-label/aria-labelledby).",
    "svg_textAltPresent_title": "<svg> doit fournir une alternative textuelle",
    "svg_textAltPresent_description": "Vérifie que les éléments <svg> en ligne fournissent une alternative textuelle via un élément <title> ou un nom ARIA (un élément <desc> seul ne suffit pas).",
    "svg_textAltPresent_summary_fail": "Alternative textuelle manquante pour <svg>.",
    "svg_textAltPresent_hint_fail": "Fournissez un élément <title> avec du texte, ou un nom ARIA (aria-label/aria-labelledby) — un élément <desc> seul ne fournit pas de nom accessible.",
    "object_textAltPresent_title": "<object> doit fournir une alternative textuelle",
    "object_textAltPresent_description": "Vérifie que les éléments <object> fournissent une alternative textuelle via un contenu de repli ou un nom accessible.",
    "object_textAltPresent_summary_fail": "Alternative textuelle manquante pour <object>.",
    "object_textAltPresent_hint_fail": "Fournissez un contenu de repli pertinent dans <object>, ajoutez un nom accessible (aria-label/aria-labelledby), ou utilisez un attribut title comme solution de repli.",
    "embed_textAltPresent_title": "<embed> doit fournir une alternative textuelle",
    "embed_textAltPresent_description": "Vérifie que les éléments <embed> fournissent une alternative textuelle via un nom accessible.",
    "embed_textAltPresent_summary_fail": "Alternative textuelle manquante pour <embed>.",
    "embed_textAltPresent_hint_fail": "Ajoutez un nom accessible à <embed> (aria-label/aria-labelledby de préférence, ou un attribut title comme solution de repli).",
    "img_altQuality_title": "<img> : texte alt à vérifier (revue manuelle)",
    "img_altQuality_description": "Signale les éléments <img> dont l’attribut alt n’est pas vide afin de vérifier manuellement sa pertinence.",
    "img_altQuality_summary_cantTell": "Vérifiez le texte alt de <img> (exactitude et pertinence).",
    "img_altQuality_hint_cantTell": "Assurez-vous que le texte alt exprime le but/l’information de l’image dans son contexte (ni redondant, ni nom de fichier).",
    "img_altDecorative_title": "<img> avec alt=\"\" : décoratif à confirmer (revue manuelle)",
    "img_altDecorative_description": "Signale les éléments <img> dont l’attribut alt est vide afin de confirmer qu’ils sont purement décoratifs.",
    "img_altDecorative_summary_cantTell": "Vérifiez si <img> est décoratif (alt=\"\").",
    "img_altDecorative_hint_cantTell": "Confirmez que l’image est purement décorative. Sinon, fournissez un texte alt pertinent.",
    "area_altQuality_title": "<area> : texte alt à vérifier (revue manuelle)",
    "area_altQuality_description": "Signale les éléments <area> dont l’attribut alt n’est pas vide afin de vérifier manuellement sa pertinence.",
    "area_altQuality_summary_cantTell": "Vérifiez le texte alt de <area> (exactitude et pertinence).",
    "area_altQuality_hint_cantTell": "Assurez-vous que le texte alt identifie la destination/l’action de la zone dans son contexte.",
    "area_altDecorative_title": "<area> avec alt=\"\" : décoratif à confirmer (revue manuelle)",
    "area_altDecorative_description": "Signale les éléments <area> dont l’attribut alt est vide afin de confirmer qu’ils sont décoratifs ou non informatifs.",
    "area_altDecorative_summary_cantTell": "Vérifiez si <area> est décoratif (alt=\"\").",
    "area_altDecorative_hint_cantTell": "Confirmez que la zone n’a pas de fonction ni d’information. Sinon, fournissez un texte alt pertinent.",
    "inputImage_altQuality_title": "<input type=\"image\"> : texte alt à vérifier (revue manuelle)",
    "inputImage_altQuality_description": "Signale les éléments <input type=\"image\"> dont l’attribut alt n’est pas vide afin de vérifier manuellement sa pertinence.",
    "inputImage_altQuality_summary_cantTell": "Vérifiez le texte alt de <input type=\"image\"> (exactitude et pertinence).",
    "inputImage_altQuality_hint_cantTell": "Assurez-vous que le texte alt décrit l’action du contrôle (ex. « Rechercher », « Envoyer ») dans son contexte.",
    "inputImage_altDecorative_title": "<input type=\"image\"> avec alt=\"\" : à vérifier (revue manuelle)",
    "inputImage_altDecorative_description": "Signale les éléments <input type=\"image\"> dont l’attribut alt est vide afin de vérifier manuellement (souvent inadapté pour un contrôle fonctionnel).",
    "inputImage_altDecorative_summary_cantTell": "Vérifiez <input type=\"image\"> avec alt=\"\".",
    "inputImage_altDecorative_hint_cantTell": "Ce contrôle est généralement fonctionnel. Confirmez qu’un nom accessible équivalent existe, sinon fournissez un texte alt pertinent.",
    "canvas_textAltQuality_title": "<canvas> : alternative textuelle à vérifier (revue manuelle)",
    "canvas_textAltQuality_description": "Signale les éléments <canvas> pour lesquels une alternative textuelle a été détectée, afin de vérifier manuellement son équivalence.",
    "canvas_textAltQuality_summary_cantTell": "Vérifiez l’alternative textuelle de <canvas> (équivalence et pertinence).",
    "canvas_textAltQuality_hint_cantTell": "Confirmez que le texte de secours ou le nom accessible transmet la même information/fonction que le contenu du canvas.",
    "svg_textAltQuality_title": "<svg> : alternative textuelle à vérifier (revue manuelle)",
    "svg_textAltQuality_description": "Signale les graphiques <svg> pour lesquels une alternative textuelle a été détectée, afin de vérifier manuellement sa pertinence.",
    "svg_textAltQuality_summary_cantTell": "Vérifiez l’alternative textuelle de <svg> (exactitude et pertinence).",
    "svg_textAltQuality_hint_cantTell": "Confirmez que <title>/<desc> ou le nom ARIA transmet le sens/le but du graphique dans son contexte.",
    "object_textAltQuality_title": "<object> : alternative textuelle à vérifier (revue manuelle)",
    "object_textAltQuality_description": "Signale les éléments <object> avec contenu de secours ou nom détecté, afin de vérifier manuellement l’équivalence.",
    "object_textAltQuality_summary_cantTell": "Vérifiez l’alternative textuelle de <object> (équivalence et pertinence).",
    "object_textAltQuality_hint_cantTell": "Confirmez que le contenu de secours ou le nom ARIA fournit une alternative équivalente au contenu embarqué.",
    "embed_textAltQuality_title": "<embed> : alternative textuelle à vérifier (revue manuelle)",
    "embed_textAltQuality_description": "Signale les éléments <embed> avec nom détecté, afin de vérifier manuellement sa pertinence.",
    "embed_textAltQuality_summary_cantTell": "Vérifiez l’alternative textuelle de <embed> (exactitude et pertinence).",
    "embed_textAltQuality_hint_cantTell": "Confirmez que le nom ARIA ou l’attribut title identifie correctement le contenu embarqué dans son contexte.",
    "videoPoster_textAltPresent_title": "L’image poster de <video> doit avoir une alternative textuelle",
    "videoPoster_textAltPresent_description": "Vérifie que les éléments <video> avec une image poster fournissent une alternative textuelle (nom accessible).",
    "videoPoster_textAltPresent_summary_fail": "Alternative textuelle manquante pour l’image poster de <video>.",
    "videoPoster_textAltPresent_hint_fail": "Fournissez un nom accessible pour l’image poster (aria-label/aria-labelledby de préférence, ou un attribut title comme solution de repli).",
    "svgImage_textAltPresent_title": "<image> dans un SVG doit avoir une alternative textuelle",
    "svgImage_textAltPresent_description": "Vérifie que les éléments SVG <image> fournissent une alternative textuelle via <title>/<desc> ou un nom accessible ARIA.",
    "svgImage_textAltPresent_summary_fail": "Alternative textuelle manquante sur <image> (SVG).",
    "svgImage_textAltPresent_hint_fail": "Ajoutez un <title> (et éventuellement <desc>) dans <image>, ou fournissez aria-label/aria-labelledby.",
    "formControl_programmaticLabelPresent_title": "Les contrôles de formulaire doivent avoir un libellé programmatique",
    "formControl_programmaticLabelPresent_description": "Vérifie que les contrôles de formulaire ont un libellé programmatique via <label>, aria-label ou aria-labelledby.",
    "formControl_programmaticLabelPresent_summary_fail": "Le contrôle de formulaire n’a pas de libellé programmatique.",
    "formControl_programmaticLabelPresent_hint_fail": "Associez un <label>, ou utilisez aria-label / aria-labelledby (placeholder/title ne sont pas des libellés).",
    "formControlAccessibleName_description": "Échec lorsqu’un contrôle de formulaire applicable n’a pas de nom accessible (ex. label, aria-label, aria-labelledby).",
    "formControlAccessibleName_hint_fail": "Fournissez un nom accessible via un <label>, aria-label ou aria-labelledby.",
    "formControlAccessibleName_summary_fail": "Le contrôle de formulaire n’a pas de nom accessible.",
    "formControlAccessibleName_title": "Les champs de formulaire doivent avoir un nom accessible.",
    "linksTargetBlankNoopener_description": "Échec lorsqu’un lien avec target=\"_blank\" n’inclut pas rel=\"noopener\" (risque de sécurité via window.opener).",
    "linksTargetBlankNoopener_hint_cantTell": "Ajoutez rel=\"noopener\" (et éventuellement noreferrer) aux liens avec target=\"_blank\".",
    "linksTargetBlankNoopener_summary_cantTell": "Lien avec target=\"_blank\" sans rel=\"noopener\".",
    "linksTargetBlankNoopener_title": "Les liens qui s’ouvrent dans un nouvel onglet doivent utiliser rel=\"noopener\".",
    "manualReview_description": "Cette règle renvoie toujours cantTell et nécessite une vérification manuelle.",
    "manualReview_hint_cantTell": "Examinez la page et validez ce point manuellement selon le contexte.",
    "manualReview_summary_cantTell": "Vérification manuelle requise.",
    "manualReview_title": "Vérification manuelle requise.",
    "rules.img-alt-suspicious.meta.title": "Texte alternatif suspect nécessitant une vérification",
    "rules.img-alt-suspicious.meta.description": "Identifie les images dont le texte alternatif correspond à des motifs suspects courants (nom de fichier, URL, texte fictif ou terme générique) et nécessite une vérification manuelle.",
    "rules.img-alt-suspicious.occurrence.cantTell.summary": "Le texte alternatif de l’image semble suspect (« {{alt}} » ressemble à {{pattern}}) et nécessite une vérification.",
    "rules.img-alt-suspicious.occurrence.cantTell.hint": "Vérifiez le texte alternatif. Évitez les noms de fichiers, les URL, les textes fictifs ou les termes génériques, et assurez-vous que l’alternative textuelle décrit la fonction ou le contenu de l’image dans son contexte.",
    "formControl_programmaticLabelQuality_title": "Les champs de formulaire ne devraient pas dépendre du placeholder ou du title comme libellé principal",
    "formControl_programmaticLabelQuality_description": "Signale les champs de formulaire dont le nom accessible est principalement dérivé du placeholder ou de l’attribut title. Préférez un <label> ou aria-labelledby.",
    "formControl_programmaticLabelQuality_summary_cantTell": "Le libellé principal du champ provient de {{methodLabel}}.",
    "formControl_programmaticLabelQuality_hint_cantTell": "Préférez un <label> persistant ou aria-labelledby. Évitez d’utiliser placeholder/title comme libellé principal.",
    "html_lang_attr_title": "La langue de la page est déclarée",
    "html_lang_attr_description": "Vérifie que la langue par défaut de la page est déclarée de manière programmatique.",
    "html_lang_attr_missing_absent": "La langue par défaut de la page n’est pas déclarée.",
    "html_lang_attr_hint_missing_absent": "Ajoutez un attribut lang à l’élément <html> (par exemple : <html lang=\"fr\">).",
    "html_lang_attr_missing_empty": "La langue par défaut de la page est déclarée mais vide.",
    "html_lang_attr_hint_missing_empty": "Renseignez une valeur de langue valide dans l’attribut lang de l’élément <html> (par exemple : <html lang=\"fr\">).",
    "html_lang_attr_invalid": "La langue par défaut de la page est déclarée, mais la valeur « {{lang}} » n’est pas une balise de langue valide.",
    "html_lang_attr_hint_invalid": "Utilisez une balise de langue BCP 47 valide dans <html lang=\"…\"> (par exemple : « fr », « en », « fr-FR »).",
    "mediaTranscriptPresent_title": "Média temporel : preuve de transcription ou d’alternative textuelle",
    "mediaTranscriptPresent_description": "Détecte les éléments audio et vidéo pour lesquels la présence d’une transcription ou d’une autre alternative textuelle n’est pas clairement établie dans le contenu de la page. Cette règle est volontairement conservatrice et retourne cantTell lorsque la preuve est absente ou invérifiable.",
    "mediaTranscriptPresent_summary_cantTell_missing": "Aucune transcription ou autre alternative textuelle pour ce média temporel n’est clairement établie sur la page.",
    "mediaTranscriptPresent_hint_cantTell_missing": "Fournir une transcription ou une autre alternative textuelle clairement identifiée pour les médias préenregistrés audio seuls ou vidéo seuls, par exemple une section ou un lien « Transcription » visible.",
    "mediaTranscriptPresent_summary_cantTell_unverified": "Une transcription ou une autre alternative textuelle peut être disponible pour ce média temporel, mais elle n’a pas pu être vérifiée à partir du contenu de la page.",
    "mediaTranscriptPresent_hint_cantTell_unverified": "Aucune transcription ou autre alternative textuelle pour cet élément {element} n’est clairement établie sur la page.",
    "pageTitlePresent_title": "La page possède un titre non vide",
    "pageTitlePresent_description": "Vérifie que la page contient un élément <title> non vide permettant d’identifier la page.",
    "pageTitlePresent_summary_fail": "La page ne possède pas de titre non vide.",
    "pageTitlePresent_hint_fail": "Ajouter un élément <title> contenant un texte décrivant le sujet ou l’objectif de la page.",
    "pageTitlePatterns_title": "Motifs de titres de page pouvant indiquer un manque de descriptivité",
    "pageTitlePatterns_description": "Identifie des motifs de titres de page pouvant indiquer un manque de descriptivité, tels que des titres génériques, dupliqués ou excessivement modélisés. Cette règle fournit des signaux de revue et n’entraîne pas d’échec automatique.",
    "pageTitlePatterns_summary_cantTell": "Le titre de la page peut ne pas être suffisamment descriptif pour identifier le sujet ou l’objectif de la page.",
    "pageTitlePresent_summary_fail_missing": "La page ne contient pas d’élément <title>.",
    "pageTitlePresent_summary_fail_empty": "La page contient un élément <title> vide.",
    "pageTitlePatterns_summary_cantTell_duplicateAcrossPages": "Plusieurs pages partagent le même titre, ce qui peut rendre plus difficile la distinction entre les pages ({{duplicateGroups}} groupes dupliqués sur {{pagesAnalyzed}} pages). Exemple : « {{exampleTitle}} ».",
    "pageTitlePatterns_summary_cantTell_templatedAcrossPages": "De nombreux titres de page semblent fortement modélisés, ce qui peut réduire la capacité des titres à distinguer les pages ({{pagesAnalyzed}} pages).",
    "pageTitlePatterns_summary_cantTell_generic": "Le titre de la page est générique et peut ne pas identifier le sujet ou l’objectif de la page.",
    "pageTitlePatterns_summary_cantTell_veryShort": "Le titre de la page est très court et peut ne pas identifier le sujet ou l’objectif de la page.",
    "pageTitlePatterns_summary_cantTell_templateLike": "Le titre de la page semble modélisé et peut ne pas identifier le sujet ou l’objectif de la page.",
    "pageTitlePatterns_hint_cantTell": "Vérifier que le titre de la page identifie clairement le sujet ou l’objectif de la page et permet de la distinguer des autres pages.",
    "contrastComputable_title": "Le contraste des couleurs est calculable pour le texte rendu",
    "contrastComputable_description": "Détermine si suffisamment d’informations sont disponibles pour calculer le contraste WCAG du texte visible (ex. pas de dégradés/images/modes de fusion rendant l’arrière-plan indéterminé).",
    "contrastComputable_pass_allComputable": "Le contraste est calculable pour tout le texte éligible ({{eligibleTextCount}} nœud(s) de texte).",
    "contrastComputable_cantTell_generic": "Le contraste peut ne pas être calculable ({{reasonCode}}).",
    "contrastComputable_cantTell_bgImageOrGradient": "Le contraste n’est pas calculable car l’arrière-plan utilise une image ou un dégradé ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgImage": "Le contraste ne peut pas être calculé car l’arrière-plan utilise une image ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgGradient": "Le contraste ne peut pas être calculé car l’arrière-plan utilise un dégradé ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_bgImageAndGradient": "Le contraste ne peut pas être calculé car l’arrière-plan utilise une image et un dégradé ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_mixBlendMode": "Le contraste n’est pas calculable car mix-blend-mode est utilisé ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_filter": "Le contraste ne peut pas être calculé car la propriété filter est utilisée ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_rootNotOpaque": "Le contraste n’est pas calculable car l’arrière-plan effectif n’est pas totalement opaque à la racine (alpha={{backgroundAlpha}}).",
    "contrastComputable_cantTell_foregroundUnparsable": "Le contraste n’est pas calculable car la couleur de premier plan calculée n’a pas pu être analysée.",
    "contrastComputable_cantTell_engineFailure": "La calculabilité du contraste n’a pas pu être déterminée en raison d’une erreur interne du moteur ({{reasonCode}}).",
    "contrastComputable_cantTell_backdropFilter": "Le contraste ne peut pas être calculé car la propriété backdrop-filter est utilisée ({{blockerProperty}}={{blockerValue}}).",
    "contrastComputable_cantTell_filterOrBackdropFilter": "Le contraste ne peut pas être calculé car une propriété filter ou backdrop-filter est utilisée ({{blockerProperty}}={{blockerValue}}).",
    "contrastMinimum_title": "Le texte respecte le contraste minimum (AA)",
    "contrastMinimum_description": "Vérifie que le texte visible atteint un ratio de contraste d’au moins 4,5:1 (texte normal) ou 3,0:1 (grand texte), lorsque le contraste est calculable à partir du CSS.",
    "contrastMinimum_fail_belowThreshold": "L’élément présente un contraste de couleur insuffisant de {{ratio}}:1 (premier plan : {{foregroundHex}}, arrière-plan : {{backgroundHex}}, taille de police : {{fontSizePx}}px, graisse de police : {{fontWeightLabel}}). Le ratio de contraste attendu est de {{threshold}}:1 ({{#isLargeText}}texte de grande taille{{/isLargeText}}{{^isLargeText}}texte normal{{/isLargeText}}).",
    "contrastMinimum_pass_allAboveThreshold": "Tout le texte calculable respecte le contraste minimum (AA). Nœuds de texte éligibles : {{eligibleTextCount}}. Calculables : {{computableTextCount}}.",
    "contrastMinimum_notApplicable_noComputableText": "Aucun texte éligible n’avait un contraste calculable (nœuds de texte éligibles : {{eligibleTextCount}}). Voir la règle de calculabilité du contraste pour les détails.",
    "contrastMinimum_cantTell_engineFailure": "Le contraste minimum (AA) n’a pas pu être déterminé en raison d’une erreur interne du moteur ({{reasonCode}}).",
    "contrastEnhanced_title": "Le texte respecte le contraste renforcé (AAA)",
    "contrastEnhanced_description": "Vérifie que le texte visible atteint un ratio de contraste d’au moins 7,0:1 (texte normal) ou 4,5:1 (grand texte), lorsque le contraste est calculable à partir du CSS.",
    "dom_textContrastMinimum_title": "Le texte doit avoir un contraste suffisant (minimum)",
    "dom_textContrastMinimum_description": "Vérifie le contraste du texte visible par rapport à son arrière-plan calculé selon WCAG 2.2 SC 1.4.3 (AA), en utilisant les styles rendus (taille/épaisseur) pour déterminer le ratio requis.",
    "dom_textContrastMinimum_summary_fail": "Contraste de texte insuffisant : {{contrastRatio}}:1 (minimum requis {{requiredRatio}}:1). Premier plan {{fgColor}} sur arrière-plan {{bgColor}}. Police {{fontSizePx}}px, graisse {{fontWeight}}{{#isBold}}, en gras{{/isBold}}{{#isLargeText}} (grand texte){{/isLargeText}}.",
    "dom_textContrastMinimum_hint_fail": "Ajustez la couleur du texte ou l’arrière-plan afin d’atteindre au moins {{requiredRatio}}:1 pour cette taille/épaisseur de texte.",
    "dom_textContrastMinimum_summary_pass": "Contraste de texte conforme : {{contrastRatio}}:1 (minimum requis {{requiredRatio}}:1). Premier plan {{fgColor}} sur arrière-plan {{bgColor}}. Police {{fontSizePx}}px, graisse {{fontWeight}}{{#isBold}}, en gras{{/isBold}}{{#isLargeText}} (grand texte){{/isLargeText}}.",
    "dom_textContrastMinimum_summary_cantTell": "Impossible de calculer fiablement le contraste du texte car l’arrière-plan effectif n’est pas déterminable de manière fiable (ex. image, dégradé, vidéo, canvas, transparence ou fusion complexes).",
    "dom_textContrastMinimum_hint_cantTell": "Vérifiez manuellement le contraste lorsque le texte est superposé à des images/dégradés/transparences ; assurez-vous qu’il respecte {{requiredRatio}}:1 selon la taille/épaisseur calculée.",
    "contrastEnhanced_fail_belowThreshold": "L’élément présente un contraste de couleur insuffisant renforcé (AAA) de {{ratio}}:1 (premier plan : {{foregroundHex}}, arrière-plan : {{backgroundHex}}, taille de police : {{fontSizePx}}px, graisse de police : {{fontWeightLabel}}). Le ratio de contraste attendu est de {{threshold}}:1 ({{#isLargeText}}texte de grande taille{{/isLargeText}}{{^isLargeText}}texte normal{{/isLargeText}}).",
    "contrastEnhanced_pass_allAboveThreshold": "Tout le texte calculable respecte le contraste renforcé (AAA). Nœuds de texte éligibles : {{eligibleTextCount}}. Calculables : {{computableTextCount}}.",
    "contrastEnhanced_notApplicable_noComputableText": "Aucun texte éligible n’avait un contraste calculable (nœuds de texte éligibles : {{eligibleTextCount}}). Voir la règle de calculabilité du contraste pour les détails.",
    "contrastEnhanced_cantTell_engineFailure": "Le contraste renforcé (AAA) n’a pas pu être déterminé en raison d’une erreur interne du moteur ({{reasonCode}}).",
    "dom_textContrastEnhanced_title": "Le texte doit avoir un contraste suffisant (renforcé)",
    "dom_textContrastEnhanced_description": "Vérifie le contraste du texte visible par rapport à son arrière-plan calculé selon WCAG 2.2 SC 1.4.6 (AAA), en utilisant les styles rendus (taille/épaisseur) pour déterminer le ratio requis.",
    "dom_textContrastEnhanced_summary_fail": "Contraste de texte renforcé insuffisant : {{contrastRatio}}:1 (requis {{requiredRatio}}:1). Premier plan {{fgColor}} sur arrière-plan {{bgColor}}. Police {{fontSizePx}}px, graisse {{fontWeight}}{{#isBold}}, en gras{{/isBold}}{{#isLargeText}} (grand texte){{/isLargeText}}.",
    "dom_textContrastEnhanced_hint_fail": "Ajustez la couleur du texte ou l’arrière-plan afin d’atteindre au moins {{requiredRatio}}:1 pour le niveau renforcé (AAA).",
    "dom_textContrastEnhanced_summary_pass": "Contraste de texte renforcé conforme : {{contrastRatio}}:1 (requis {{requiredRatio}}:1). Premier plan {{fgColor}} sur arrière-plan {{bgColor}}. Police {{fontSizePx}}px, graisse {{fontWeight}}{{#isBold}}, en gras{{/isBold}}{{#isLargeText}} (grand texte){{/isLargeText}}.",
    "dom_textContrastEnhanced_summary_cantTell": "Impossible de calculer fiablement le contraste renforcé car l’arrière-plan effectif n’est pas déterminable de manière fiable (ex. image, dégradé, vidéo, canvas, transparence ou fusion complexes).",
    "dom_textContrastEnhanced_hint_cantTell": "Vérifiez manuellement le contraste renforcé (AAA) lorsque le texte est superposé à des images/dégradés/transparences ; assurez-vous qu’il respecte {{requiredRatio}}:1 selon la taille/épaisseur calculée.",
    "dom_nonTextContrast_title": "Les composants d’interface et les graphiques doivent avoir un contraste suffisant",
    "dom_nonTextContrast_description": "Vérifie le contraste des informations visuelles non textuelles (contours de composants, états, et objets graphiques porteurs d’information) selon WCAG 2.2 SC 1.4.11 (AA).",
    "dom_nonTextContrast_summary_fail": "Contraste non-textuel insuffisant : {{contrastRatio}}:1 (requis {{requiredRatio}}:1). Premier plan {{fgColor}} par rapport à {{bgColor}}. Composant : {{componentKind}}{{#componentState}} (état : {{componentState}}){{/componentState}}.",
    "dom_nonTextContrast_hint_fail": "Ajustez les couleurs du composant/graphique afin d’atteindre au moins {{requiredRatio}}:1 pour le contour perceptible ou l’information visuelle essentielle.",
    "dom_nonTextContrast_summary_pass": "Contraste non-textuel conforme : {{contrastRatio}}:1 (requis {{requiredRatio}}:1). Premier plan {{fgColor}} par rapport à {{bgColor}}. Composant : {{componentKind}}{{#componentState}} (état : {{componentState}}){{/componentState}}.",
    "dom_nonTextContrast_summary_cantTell": "Impossible de calculer fiablement le contraste non-textuel car l’arrière-plan effectif ou les pixels peints ne sont pas déterminables (ex. image/dégradé/vidéo/canvas, transparence ou fusion complexes).",
    "dom_nonTextContrast_hint_cantTell": "Vérifiez manuellement le contraste du composant/graphique par rapport aux couleurs adjacentes ; assurez-vous qu’il respecte {{requiredRatio}}:1 pour l’information visuelle non textuelle essentielle.",
    "contrastEnhanced_pass_allTextMeetsThreshold": "Tout le texte calculable respecte le contraste renforcé (AAA).",
    "contrastMinimum_pass_allTextMeetsThreshold": "Tout le texte calculable respecte le contraste minimum (AA).",
    "contrastComputable_cantTell_notComputable": "Le contraste ne peut pas être calculé pour ce texte ({{reasonCode}}).",
    "roleImg_textAlternativePresent_title": "Les éléments avec role=\"img\" doivent avoir une alternative textuelle accessible",
    "roleImg_textAlternativePresent_description": "Vérifie que les éléments ayant le rôle \"img\" fournissent une alternative textuelle accessible via aria-label ou aria-labelledby.",
    "roleImg_textAlternativePresent_summary_fail": "L’élément avec le rôle \"img\" ne possède pas d’alternative textuelle accessible.",
    "roleImg_textAlternativePresent_hint_fail": "Fournissez une alternative textuelle à l’aide de aria-label ou de aria-labelledby pointant vers un texte non vide.",
    "targetSizeMinimum_title": "Les cibles activables au pointeur respectent la taille minimale (AA)",
    "targetSizeMinimum_description": "Vérifie que les cibles activables au pointeur ont une zone cliquable effective d’au moins 24×24 pixels CSS, ou respectent une exception autorisée (par ex. un espacement suffisant).",
    "targetSizeMinimum_summary_fail": "La cible est plus petite que 24×24 px CSS et est trop proche d’une autre cible.",
    "targetSizeMinimum_hint_fail": "Augmentez la taille de la cible à au moins 24×24 px CSS, ou ajoutez un espacement suffisant par rapport aux cibles voisines.",
    "targetSizeMinimum_summary_cantTell_ambiguousSpacing": "La cible est peut-être trop petite et trop proche d’une autre cible, mais le chevauchement est proche du seuil de détection et n’a pas pu être mesuré avec certitude.",
    "targetSizeMinimum_hint_cantTell_ambiguousSpacing": "Vérifiez manuellement l’espacement effectif entre cette cible et sa voisine ; augmentez la taille de la cible ou l’espacement si le chevauchement est réel.",
    "targetSizeMinimum_summary_cantTell_plausiblyEssential": "La cible est trop petite et trop proche d’une autre cible, mais pourrait être exemptée en tant qu’élément essentiel d’une zone graphique ou d’une image cliquable.",
    "targetSizeMinimum_hint_cantTell_plausiblyEssential": "Vérifiez si la taille de cette cible est réellement essentielle à sa fonction (par ex. partie d’un SVG/canvas/plan d’image) ; sinon, augmentez la taille de la cible ou l’espacement.",
    "targetSizeMinimum_notApplicable_noTargets": "Aucune cible activable par pointeur n’était éligible à l’évaluation.",
    "targetSizeMinimum_pass_allOk": "Toutes les cibles activables par pointeur respectent la taille minimale ou une exception autorisée.",
    "ariaHidden_focus_title": "Les éléments aria-hidden ne doivent pas être focalisables",
    "ariaHidden_focus_description": "Vérifie que les éléments avec aria-hidden=\"true\" ne sont pas focalisables et ne contiennent pas d’éléments focalisables.",
    "ariaHidden_focus_summary_fail_desc": "L’élément aria-hidden {{element}} contient {{focusableCount}} élément(s) focalisable(s).",
    "ariaHidden_focus_summary_fail_self": "L’élément aria-hidden {{element}} est focalisable ({{focusableCount}} élément(s) focalisable(s)).",
    "ariaHidden_focus_summary_fail_self_and_desc": "L’élément aria-hidden {{element}} est focalisable et contient {{descendantFocusableCount}} descendant(s) focalisable(s) ({{focusableCount}} élément(s) focalisable(s) au total).",
    "ariaHidden_focus_hint_fail": "Supprimez la focalisation des descendants ou retirez aria-hidden ; assurez la cohérence entre l’ordre de focus et l’arbre d’accessibilité.",
    "ariaHidden_focus_summary_cantTell_redirect": "L’élément aria-hidden {{element}} reçoit le focus mais le focus est immédiatement déplacé vers un autre élément. Vérifiez le comportement de sentinelle/piège de focus.",
    "ariaHidden_focus_hint_cantTell_redirect": "Vérifiez qu’il s’agit d’un transfert intentionnel de sentinelle/piège de focus et que les utilisateurs clavier ne restent jamais sur une cible de focus masquée.",
    "cssHidden_focus_title": "Les éléments focalisables ne doivent pas être masqués visuellement",
    "cssHidden_focus_description": "Vérifie que les éléments focalisables au clavier ne sont pas masqués visuellement par des techniques CSS pouvant les laisser dans l’ordre de tabulation.",
    "cssHidden_focus_summary_cantTell": "L’élément focalisable {{element}} est masqué visuellement ({{visibilityHints}}).",
    "cssHidden_focus_hint_cantTell": "Rendez l’élément visible lorsqu’il peut recevoir le focus clavier, ou retirez-le de l’ordre de tabulation tant qu’il n’est pas visible.",
    "linkNamePresent_title": "Les liens ont un nom accessible",
    "linkNamePresent_description": "Vérifie que les liens exposent un nom accessible non vide.",
    "linkNamePresent_summary_fail": "Ce lien n’a pas de nom accessible.",
    "linkNamePresent_hint_fail": "Fournissez un texte de lien ou un mécanisme de nom accessible (par exemple aria-label) afin que les technologies d’assistance puissent identifier le lien.",
    "buttonNamePresent_title": "Les boutons ont un nom accessible",
    "buttonNamePresent_description": "Vérifie que les boutons exposent un nom accessible non vide.",
    "buttonNamePresent_summary_fail": "Ce bouton n’a pas de nom accessible.",
    "buttonNamePresent_hint_fail": "Fournissez un texte visible pour le bouton ou un mécanisme de nom accessible (par exemple aria-label) afin que les technologies d’assistance puissent identifier le bouton.",
    "binaryControlNamePresent_title": "Les contrôles binaires ont un nom accessible",
    "binaryControlNamePresent_description": "Vérifie que les cases à cocher, les boutons radio et les interrupteurs exposent un nom accessible non vide.",
    "binaryControlNamePresent_summary_fail": "Ce contrôle n’a pas de nom accessible.",
    "binaryControlNamePresent_hint_fail": "Fournissez un libellé, aria-label, aria-labelledby ou un autre mécanisme de nom accessible afin que les technologies d’assistance puissent identifier le contrôle.",
    "comboboxNamePresent_title": "Les listes déroulantes (combobox) ont un nom accessible",
    "comboboxNamePresent_description": "Vérifie que les éléments avec role=\"combobox\" exposent un nom accessible non vide.",
    "comboboxNamePresent_summary_fail": "Cette combobox n’a pas de nom accessible.",
    "comboboxNamePresent_hint_fail": "Fournissez aria-label, aria-labelledby ou un attribut title — le contenu textuel visible n’est pas exposé comme nom accessible de cet élément.",
    "dialogNamePresent_title": "Les dialogues ont un nom accessible",
    "dialogNamePresent_description": "Vérifie que les éléments avec role=\"dialog\" ou role=\"alertdialog\" exposent un nom accessible non vide.",
    "dialogNamePresent_summary_fail": "Ce dialogue n’a pas de nom accessible.",
    "dialogNamePresent_hint_fail": "Fournissez aria-labelledby (préféré) ou aria-label afin que les technologies d’assistance puissent annoncer le dialogue.",
    "menuitemNamePresent_title": "Les éléments de menu ont un nom accessible",
    "menuitemNamePresent_description": "Vérifie que les éléments de menu (role=\"menuitem*\", y compris les variantes case à cocher/radio) exposent un nom accessible non vide.",
    "menuitemNamePresent_summary_fail": "Cet élément de menu n’a pas de nom accessible.",
    "menuitemNamePresent_hint_fail": "Fournissez un texte visible qui n’est pas masqué aux technologies d’assistance, ou fournissez aria-label ou aria-labelledby.",
    "tabNamePresent_title": "Les onglets ont un nom accessible",
    "tabNamePresent_description": "Vérifie que les éléments avec role=\"tab\" exposent un nom accessible non vide.",
    "tabNamePresent_summary_fail": "Cet onglet n’a pas de nom accessible.",
    "tabNamePresent_hint_fail": "Fournissez un texte d’onglet qui n’est pas masqué aux technologies d’assistance, ou fournissez aria-label ou aria-labelledby.",
    "sliderNamePresent_title": "Les curseurs ont un nom accessible",
    "sliderNamePresent_description": "Vérifie que les curseurs (input[type=\"range\"] et role=\"slider\") exposent un nom accessible non vide.",
    "sliderNamePresent_summary_fail": "Ce curseur n’a pas de nom accessible.",
    "sliderNamePresent_hint_fail": "Fournissez un libellé, aria-label ou aria-labelledby afin que les technologies d’assistance puissent identifier le curseur.",
    "textboxNamePresent_title": "Les champs de texte ont un nom accessible",
    "textboxNamePresent_description": "Vérifie que les éléments avec role=\"textbox\" exposent un nom accessible non vide.",
    "textboxNamePresent_summary_fail": "Ce champ de texte n’a pas de nom accessible.",
    "textboxNamePresent_hint_fail": "Fournissez aria-label, aria-labelledby ou un attribut title — le contenu textuel visible n’est pas exposé comme nom accessible de ce champ de texte.",
    "searchboxNamePresent_title": "Les champs de recherche ont un nom accessible",
    "searchboxNamePresent_description": "Vérifie que les éléments avec role=\"searchbox\" exposent un nom accessible non vide.",
    "searchboxNamePresent_summary_fail": "Ce champ de recherche n’a pas de nom accessible.",
    "searchboxNamePresent_hint_fail": "Fournissez aria-label, aria-labelledby ou un attribut title — le contenu textuel visible n’est pas exposé comme nom accessible de ce champ de recherche.",
    "spinbuttonNamePresent_title": "Les sélecteurs numériques (spinbutton) ont un nom accessible",
    "spinbuttonNamePresent_description": "Vérifie que les éléments avec role=\"spinbutton\" exposent un nom accessible non vide.",
    "spinbuttonNamePresent_summary_fail": "Ce spinbutton n’a pas de nom accessible.",
    "spinbuttonNamePresent_hint_fail": "Fournissez aria-label, aria-labelledby ou un attribut title — le contenu textuel visible n’est pas exposé comme nom accessible de ce spinbutton.",
    "listboxNamePresent_title": "Les listes (listbox) ont un nom accessible",
    "listboxNamePresent_description": "Vérifie que les éléments avec role=\"listbox\" exposent un nom accessible non vide.",
    "listboxNamePresent_summary_fail": "Cette listbox n’a pas de nom accessible.",
    "listboxNamePresent_hint_fail": "Fournissez aria-label, aria-labelledby ou un attribut title — le contenu textuel visible n’est pas exposé comme nom accessible de cette listbox.",
    "optionNamePresent_title": "Les options ont un nom accessible",
    "optionNamePresent_description": "Vérifie que les éléments avec role=\"option\" exposent un nom accessible non vide.",
    "optionNamePresent_summary_fail": "Cette option n’a pas de nom accessible.",
    "optionNamePresent_hint_fail": "Fournissez un texte d’option qui n’est pas masqué aux technologies d’assistance, ou fournissez aria-label ou aria-labelledby.",
    "treeitemNamePresent_title": "Les éléments d’arborescence ont un nom accessible",
    "treeitemNamePresent_description": "Vérifie que les éléments avec role=\"treeitem\" exposent un nom accessible non vide.",
    "treeitemNamePresent_summary_fail": "Cet élément d’arborescence n’a pas de nom accessible.",
    "treeitemNamePresent_hint_fail": "Fournissez un texte d’élément d’arborescence qui n’est pas masqué aux technologies d’assistance, ou fournissez aria-label ou aria-labelledby.",
    "ariaRoleNamePresent_title": "Les rôles ARIA de type widget/conteneur ont un nom accessible",
    "ariaRoleNamePresent_description": "Vérifie que certains rôles ARIA de type widget/conteneur exposent un nom accessible non vide.",
    "ariaRoleNamePresent_summary_fail": "Cet élément n’a pas de nom accessible.",
    "ariaRoleNamePresent_hint_fail": "Fournissez aria-label ou aria-labelledby (préféré), ou un attribut title non vide.",
    "composite_rollup_summary": "Résultat de la règle composite : {{reasonCode}} ({{testCount}} contrôles)",
    "catalog.rules.wcag_111_non_text_content.title": "Contenu non textuel : alternatives textuelles",
    "catalog.rules.wcag_111_non_text_content.description": "Regroupe les contrôles garantissant que le contenu non textuel dispose d’une alternative textuelle appropriée.",
    "catalog.rules.wcag_121_prerecorded_transcript.title": "Audio seul et vidéo seule (préenregistrés) : transcription",
    "catalog.rules.wcag_121_prerecorded_transcript.description": "Regroupe les contrôles vérifiant la disponibilité d’une transcription pour les médias audio seuls ou vidéo seuls préenregistrés.",
    "catalog.rules.wcag_143_contrast_minimum.title": "Contraste : minimum",
    "catalog.rules.wcag_143_contrast_minimum.description": "Regroupe les contrôles relatifs au contraste minimal du texte.",
    "catalog.rules.wcag_146_contrast_enhanced.title": "Contraste : renforcé",
    "catalog.rules.wcag_146_contrast_enhanced.description": "Regroupe les contrôles relatifs au contraste renforcé du texte.",
    "catalog.rules.wcag_242_page_titled.title": "Page titrée",
    "catalog.rules.wcag_242_page_titled.description": "Regroupe les contrôles garantissant que les documents possèdent un titre de page pertinent.",
    "catalog.rules.wcag_247_focus_visible.title": "Focus visible",
    "catalog.rules.wcag_247_focus_visible.description": "Regroupe les contrôles garantissant que le focus clavier n’est pas masqué et reste perceptible.",
    "catalog.rules.wcag_258_target_size_minimum.title": "Taille de la cible : minimum",
    "catalog.rules.wcag_258_target_size_minimum.description": "Regroupe les contrôles garantissant que les cibles de pointage respectent les dimensions minimales requises.",
    "catalog.rules.wcag_311_language_of_page.title": "Langue de la page",
    "catalog.rules.wcag_311_language_of_page.description": "Regroupe les contrôles garantissant que la langue de la page est spécifiée.",
    "catalog.rules.wcag_412_name.title": "Nom, rôle, valeur : nom accessible",
    "catalog.rules.wcag_412_name.description": "Regroupe les contrôles garantissant que les éléments interactifs courants exposent un nom accessible non vide.",
    "labelInName_title": "Intitulé dans le nom : le nom accessible contient le texte visible",
    "labelInName_description": "Vérifie que lorsqu’un composant possède un libellé textuel visible, le nom accessible contient ce libellé visible (WCAG 2.5.3).",
    "labelInName_summary_fail": "{{element}} : le libellé visible « {{visibleLabel}} » (source : {{labelSource}}) n’est pas inclus dans le nom accessible (source : {{nameMechanism}}).",
    "labelInName_hint_fail": "Modifiez aria-label ou aria-labelledby (ou le texte du libellé visible) afin que le nom accessible inclue le libellé visible.",
    "labelInName_summary_cantTell": "{{element}} : le libellé visible « {{visibleLabel}} » (source : {{labelSource}}) ne diffère du nom accessible (source : {{nameMechanism}}) que par une abréviation ou une césure.",
    "labelInName_hint_cantTell": "Vérifiez manuellement que les deux formulations correspondent : le balisage ne permet pas de distinguer une abréviation volontaire d’une incohérence.",
    "ariaRolesValid_title": "L’attribut role doit être un rôle ARIA valide et non abstrait",
    "ariaRolesValid_description": "Vérifie qu’un attribut role=\"\" explicite correspond à un rôle WAI-ARIA réel et non abstrait.",
    "ariaRolesValid_summary_invalid": "role=\"{{role}}\" n’est pas un rôle ARIA reconnu.",
    "ariaRolesValid_hint_invalid": "Utilisez un rôle ARIA valide, ou retirez l’attribut role si aucun ne s’applique.",
    "ariaRolesValid_summary_abstract": "role=\"{{role}}\" est un rôle ARIA abstrait, qui ne doit pas être utilisé directement.",
    "ariaRolesValid_hint_abstract": "Remplacez ce rôle abstrait par un rôle concret adapté au composant/à la structure.",
    "ariaDeprecatedRole_title": "L’attribut role ne devrait pas utiliser un rôle ARIA obsolète ou déconseillé aux auteurs",
    "ariaDeprecatedRole_description": "Vérifie qu’un attribut role=\"\" explicite n’utilise pas un rôle rendu obsolète par la spécification WAI-ARIA, ni un rôle réservé à un usage interne à l’agent utilisateur (ex. role=\"generic\").",
    "ariaDeprecatedRole_summary_fail": "Cet élément utilise role=\"{{role}}\", que les auteurs ne doivent pas déclarer explicitement.",
    "ariaDeprecatedRole_hint_fail": "{{guidance}}",
    "ariaDeprecatedRole_summary_cantTell": "Cet élément utilise role=\"{{role}}\", qui est obsolète dans WAI-ARIA (toujours valide, mais déconseillé).",
    "ariaDeprecatedRole_summary_cantTell_discouraged": "Cet élément utilise role=\"{{role}}\", qui est réservé aux agents utilisateurs (toujours valide, mais déconseillé).",
    "ariaDeprecatedRole_hint_cantTell": "{{guidance}}",
    "ariaValidAttr_title": "Les attributs aria-* doivent être des attributs ARIA réels et définis",
    "ariaValidAttr_description": "Vérifie que chaque nom d’attribut aria-* présent dans le DOM est un attribut réel défini par la spécification WAI-ARIA.",
    "ariaValidAttr_summary_fail": "{{attr}} n’est pas un attribut ARIA reconnu.",
    "ariaValidAttr_hint_fail": "Corrigez le nom de l’attribut (vérifiez les fautes de frappe), ou retirez-le s’il n’est pas nécessaire.",
    "ariaValidAttrValue_title": "Les valeurs des attributs aria-* doivent correspondre à leur type déclaré",
    "ariaValidAttrValue_description": "Vérifie que chaque attribut aria-* reconnu a une valeur conforme à son type déclaré par WAI-ARIA (booléen, tri-état, jeton, entier, nombre ou référence d’ID).",
    "ariaValidAttrValue_summary_fail": "{{attr}}=\"{{value}}\" n’est pas une valeur valide pour cet attribut.",
    "ariaValidAttrValue_hint_fail": "Utilisez une valeur correspondant au type attendu de l’attribut (consultez la spécification WAI-ARIA pour cet attribut).",
    "ariaAllowedAttr_title": "Les attributs aria-* doivent être autorisés pour le rôle de l’élément",
    "ariaAllowedAttr_description": "Vérifie que chaque attribut aria-* reconnu présent sur un élément ayant un rôle explicite est soit globalement pris en charge, soit pris en charge par ce rôle.",
    "ariaAllowedAttr_summary_fail": "{{attr}} n’est pas autorisé sur role=\"{{role}}\".",
    "ariaAllowedAttr_hint_fail": "Retirez cet attribut, ou utilisez un rôle qui le prend en charge.",
    "ariaAllowedAttr_summary_cantTell": "{{attr}} est obsolète sur role=\"{{role}}\" (toujours autorisé, mais déconseillé).",
    "ariaAllowedAttr_hint_cantTell": "Cet attribut a été retiré de l’ensemble global d’ARIA en 1.2 ; retirez-le ou utilisez un rôle qui le prend en charge, car une future version d’ARIA pourrait l’interdire.",
    "ariaProhibitedAttr_title": "Les attributs de nommage ARIA ne doivent pas être utilisés sur des rôles qui les interdisent",
    "ariaProhibitedAttr_description": "Vérifie que aria-label/aria-labelledby ne sont pas présents sur des rôles WAI-ARIA dont la spécification interdit explicitement le nommage ARIA (ex. generic, emphasis, strong, paragraph).",
    "ariaProhibitedAttr_summary_fail": "{{attr}} est interdit sur role=\"{{role}}\".",
    "ariaProhibitedAttr_hint_fail": "Retirez cet attribut ; ce rôle ne doit pas porter de nom accessible.",
    "ariaProhibitedAttr_summary_fail_roleless": "Cet élément {{element}} n'a aucun rôle ni autre source de nom accessible, donc {{attr}} n'est pas exposé de façon fiable aux technologies d'assistance.",
    "ariaProhibitedAttr_hint_fail_roleless": "Donnez à cet élément un rôle prenant en charge un nom accessible (par ex. role=\"img\"/\"button\"), ou retirez cet attribut s'il ne sert à rien sans rôle.",
    "ariaProhibitedAttr_summary_cantTell_roleless": "Cet élément {{element}} n'a aucun rôle, donc {{attr}} pourrait ne pas être exposé comme nom accessible par les technologies d'assistance — mais le contenu de l'élément en fournit déjà un.",
    "ariaProhibitedAttr_hint_cantTell_roleless": "Vérifiez si le contenu textuel existant sert déjà de libellé à cet élément ; si oui, l'attribut de nommage est redondant, sinon donnez à l'élément un rôle prenant en charge le nommage (par ex. role=\"img\").",
    "ariaRequiredAttr_title": "Les rôles avec un état/une propriété ARIA requis doivent le porter",
    "ariaRequiredAttr_description": "Vérifie que les éléments ayant un rôle explicite portent chaque état/propriété aria-* requis, non ambigu et indépendant du contexte, pour ce rôle (ex. role=\"checkbox\" doit avoir aria-checked).",
    "ariaRequiredAttr_summary_fail": "{{attr}} est requis pour role=\"{{role}}\", mais est absent.",
    "ariaRequiredAttr_hint_fail": "Ajoutez cet attribut avec une valeur valide pour ce rôle.",
    "ariaAllowedRole_title": "Le rôle explicite doit être autorisé pour son élément hôte",
    "ariaAllowedRole_description": "Vérifie qu’un attribut role=\"\" explicite fait partie des rôles que la spécification ARIA-in-HTML autorise pour l’élément hôte (ex. role=\"tab\" n’est pas autorisé sur <nav>).",
    "ariaAllowedRole_summary_fail": "role=\"{{role}}\" n’est pas autorisé sur <{{element}}>.",
    "ariaAllowedRole_hint_fail": "Utilisez un rôle autorisé pour cet élément, ou changez l’élément hôte.",
    "ariaRequiredChildren_title": "Les rôles conteneurs doivent posséder au moins un rôle enfant requis",
    "ariaRequiredChildren_description": "Vérifie que les rôles conteneurs disposant d’une entrée documentée « éléments possédés requis » (list, listbox, menu, radiogroup, table, grid, tablist, tree, row, ...) contiennent au moins un descendant, ou un élément référencé par aria-owns, ayant un rôle possédé acceptable.",
    "ariaRequiredChildren_summary_fail": "role=\"{{role}}\" ne possède aucun enfant ayant l’un des rôles requis : {{requiredRoles}}.",
    "ariaRequiredChildren_hint_fail": "Ajoutez un descendant (ou un élément référencé par aria-owns) ayant l’un des rôles possédés requis.",
    "ariaProhibitedChildren_title": "Les rôles conteneurs ne doivent pas posséder d’enfant ayant un rôle interdit",
    "ariaProhibitedChildren_description": "Vérifie que chaque enfant possédé (dans l’arbre d’accessibilité) d’un rôle conteneur (list, listbox, menu, menubar, radiogroup, rowgroup, table, grid, treegrid, tablist, tree, row) a l’un des rôles possédés autorisés par ce rôle.",
    "ariaProhibitedChildren_summary_fail": "Cet élément a le rôle role=\"{{childRole}}\", qui n’est pas un rôle enfant possédé autorisé par le conteneur englobant role=\"{{containerRole}}\".",
    "ariaProhibitedChildren_hint_fail": "Retirez ou modifiez ce rôle afin qu’il corresponde à l’un des rôles possédés autorisés du conteneur ({{allowedRoles}}), ou déplacez cet élément hors du conteneur {{containerRole}}.",
    "ariaProhibitedChildren_summary_fail_roleless": "Cet élément n’a pas de rôle explicite mais porte {{attr}}, ce qui en fait un véritable nœud de l’arbre d’accessibilité qui n’est pas un enfant possédé autorisé du conteneur englobant role=\"{{containerRole}}\".",
    "ariaProhibitedChildren_hint_fail_roleless": "Retirez {{attr}} (ou la relation de possession avec le conteneur role=\"{{containerRole}}\"), ou attribuez à cet élément role=\"presentation\"/\"none\" s’il n’est pas censé constituer son propre nœud dans l’arbre d’accessibilité.",
    "ariaProhibitedChildren_summary_fail_native_focusable": "Cet élément n’a pas de rôle explicite mais est nativement focalisable, ce qui en fait un véritable nœud de l’arbre d’accessibilité qui n’est pas un enfant possédé autorisé du conteneur englobant role=\"{{containerRole}}\".",
    "ariaProhibitedChildren_hint_fail_native_focusable": "Attribuez à cet élément role=\"presentation\"/\"none\", retirez sa focalisabilité native (par ex. supprimez l’attribut href/tabindex à l’origine), ou déplacez-le hors du conteneur {{containerRole}}.",
    "ariaRequiredParent_title": "Les rôles exigeant un rôle de contexte spécifique doivent se trouver dans ce contexte",
    "ariaRequiredParent_description": "Vérifie que les rôles disposant d’une entrée documentée « rôle de contexte requis » (listitem, option, tab, treeitem, row, cell, ...) ont un ancêtre, ou un propriétaire aria-owns, ayant un rôle de contexte acceptable.",
    "ariaRequiredParent_summary_fail": "role=\"{{role}}\" exige un rôle de contexte parmi : {{requiredRoles}}, qui n’a pas été trouvé.",
    "ariaRequiredParent_hint_fail": "Placez cet élément à l’intérieur d’un élément ayant un rôle de contexte acceptable (ou référencez-le depuis celui-ci via aria-owns).",
    "deprecatedElements_title": "Les éléments obsolètes non interruptibles (<blink>, <marquee>) ne doivent pas être utilisés",
    "deprecatedElements_description": "Vérifie que les éléments HTML obsolètes et non standards dont le contenu clignotant/défilant ne peut pas être mis en pause, arrêté ou masqué par l’utilisateur (<blink>, <marquee>) ne sont pas présents.",
    "deprecatedElements_summary_fail": "Le contenu de <{{element}}> ne peut pas être mis en pause, arrêté ou masqué par l’utilisateur.",
    "deprecatedElements_hint_fail": "Retirez cet élément ; utilisez à la place un contenu statique, ou une animation dotée d’un contrôle de pause/arrêt accessible à l’utilisateur.",
    "iframeNamePresent_title": "Les cadres ont un nom accessible",
    "iframeNamePresent_description": "Vérifie que les éléments <iframe>/<frame> exposent un nom accessible non vide via aria-label, aria-labelledby, ou l’attribut title.",
    "iframeNamePresent_summary_fail": "Ce <{{element}}> n’a pas de nom accessible.",
    "iframeNamePresent_hint_fail": "Ajoutez un attribut title (ou aria-label/aria-labelledby) décrivant le contenu ou l’objet du cadre.",
    "iframeTitleUnique_title": "Les titres de cadres doivent être uniques",
    "iframeTitleUnique_description": "Vérifie qu’aucun <iframe>/<frame> dans le périmètre analysé ne partage la même valeur d’attribut title qu’un autre.",
    "iframeTitleUnique_summary_fail": "Le titre « {{title}} » de ce <{{element}}> n’est pas unique parmi les cadres de cette page.",
    "iframeTitleUnique_hint_fail": "Donnez à chaque cadre un titre distinct décrivant son contenu ou son objet spécifique.",
    "iframeFocusableContent_title": "Les cadres avec tabindex=\"-1\" ne doivent pas contenir de contenu focalisable",
    "iframeFocusableContent_description": "Vérifie que les éléments <iframe>/<frame> de même origine avec tabindex=\"-1\" ne contiennent pas de contenu focalisable, car les navigateurs ne propagent pas cette restriction au document intégré du cadre.",
    "iframeFocusableContent_summary_fail": "Ce <{{element}}> a tabindex=\"-1\" mais son contenu contient des éléments focalisables, qui restent accessibles au clavier.",
    "iframeFocusableContent_hint_fail": "Retirez le contenu focalisable du cadre, ou retirez tabindex=\"-1\" si le cadre est censé être accessible.",
    "tableHeadersAttrValid_title": "L’attribut headers d’une cellule de tableau doit référencer des cellules d’en-tête valides",
    "tableHeadersAttrValid_description": "Vérifie que chaque id de l’attribut headers d’un <td>/<th> correspond à un élément <th> du même tableau (ni absent, ni un élément autre que th, ni lui-même).",
    "tableHeadersAttrValid_summary_fail": "L’attribut headers de ce <{{element}}> référence des cellules d’en-tête invalides : {{invalidIds}}.",
    "tableHeadersAttrValid_hint_fail": "Mettez à jour l’attribut headers afin que chaque id désigne un élément <th> du même tableau.",
    "tableThHasDataCells_title": "Les éléments <th> doivent décrire au moins une cellule de données",
    "tableThHasDataCells_description": "Vérifie qu’un tableau contenant des éléments <th> contient aussi au moins une cellule de données <td> que ces en-têtes décrivent.",
    "tableThHasDataCells_summary_fail": "Ce tableau a des cellules d’en-tête mais aucune cellule de données à décrire.",
    "tableThHasDataCells_hint_fail": "Ajoutez des cellules de données (<td>) au tableau, ou retirez les cellules d’en-tête si le tableau n’a pas de données.",
    "ariaHiddenBody_title": "Le <body> du document ne doit pas être aria-hidden",
    "ariaHiddenBody_description": "Vérifie que <body> n’a pas aria-hidden=\"true\", ce qui retirerait toute la page de l’arbre d’accessibilité.",
    "ariaHiddenBody_summary_fail": "Le corps du document a aria-hidden=\"true\", ce qui masque toute la page aux technologies d’assistance.",
    "ariaHiddenBody_hint_fail": "Retirez aria-hidden de <body>. Masquez des éléments spécifiques à la place, si c’était l’intention.",
    "listChildrenValid_title": "Les listes ne doivent contenir directement que des éléments de liste",
    "listChildrenValid_description": "Vérifie que les éléments <ul>/<ol> n’ont comme enfants directs que <li>, <script> ou <template>.",
    "listChildrenValid_summary_fail": "Ce <{{element}}> contient un enfant direct qui n’est pas un élément de liste : {{invalidChildren}}.",
    "listChildrenValid_hint_fail": "N’utilisez que <li> (ou <script>/<template>) comme enfants directs de <ul>/<ol> ; déplacez tout autre balisage à l’intérieur d’un <li>.",
    "listitemParentValid_title": "Les éléments de liste doivent se trouver à l’intérieur d’un conteneur de liste",
    "listitemParentValid_description": "Vérifie que les éléments <li> sont contenus par <ul>, <ol>, ou un élément ayant role=\"list\".",
    "listitemParentValid_summary_fail": "Le parent de cet élément de liste (<{{parentElement}}>) n’est pas un conteneur de liste.",
    "listitemParentValid_hint_fail": "Placez ce <li> à l’intérieur d’un <ul>/<ol>, ou donnez à son parent role=\"list\".",
    "definitionListChildrenValid_title": "Les listes de définitions doivent être structurées correctement",
    "definitionListChildrenValid_description": "Vérifie que les éléments <dl> ne contiennent directement que des groupes <dt>/<dd> (éventuellement enveloppés dans un seul <div>), <script>, <template>, ou <style>.",
    "definitionListChildrenValid_summary_fail_invalidChild": "Cette liste de définitions contient un enfant direct ou enveloppé qui ne fait pas partie d’un groupe dt/dd : {{invalidChildren}}.",
    "definitionListChildrenValid_hint_fail_invalidChild": "N’utilisez que <dt>/<dd> (éventuellement enveloppés dans un seul <div>), <script>, <template>, ou <style> à l’intérieur de <dl>.",
    "definitionListChildrenValid_summary_fail_noDtDd": "Cette liste de définitions n’a aucun groupe terme/définition <dt>/<dd>.",
    "definitionListChildrenValid_hint_fail_noDtDd": "Ajoutez au moins une paire <dt>/<dd> à l’intérieur de ce <dl>.",
    "dlitemParentValid_title": "Les éléments d’une liste de définitions doivent se trouver à l’intérieur d’une liste de définitions",
    "dlitemParentValid_description": "Vérifie que les éléments <dt>/<dd> sont contenus par un <dl>, directement ou via un <div> englobant.",
    "dlitemParentValid_summary_fail": "Le parent (<{{parentElement}}>) de ce <{{element}}> n’est pas une liste de définitions.",
    "dlitemParentValid_hint_fail": "Placez ce <dt>/<dd> à l’intérieur d’un <dl>, directement ou enveloppé dans un seul <div>.",
    "duplicateIdAria_title": "Les ID référencés par ARIA doivent être uniques",
    "duplicateIdAria_description": "Vérifie que toute valeur id référencée par un attribut de référence d’ID ARIA (aria-labelledby, aria-describedby, aria-owns, aria-controls, aria-activedescendant, aria-flowto, aria-errormessage, aria-details) est unique dans le document.",
    "duplicateIdAria_summary_fail": "L’id « {{id}} » est référencé par un attribut ARIA mais est utilisé par {{duplicateCount}} éléments.",
    "duplicateIdAria_hint_fail": "Rendez uniques dans le document les id référencés par des attributs ARIA.",
    "summaryNamePresent_title": "Les éléments summary ont un nom accessible",
    "summaryNamePresent_description": "Vérifie que les éléments <summary> exposent un nom accessible non vide.",
    "summaryNamePresent_summary_fail": "Ce summary n’a pas de nom accessible.",
    "summaryNamePresent_hint_fail": "Fournissez un texte de summary qui ne soit pas masqué aux technologies d’assistance, ou fournissez aria-label ou aria-labelledby.",
    "metaViewportZoomEnabled_title": "La balise meta viewport ne doit pas désactiver le zoom",
    "metaViewportZoomEnabled_description": "Vérifie que <meta name=\"viewport\"> ne définit pas user-scalable=no ni maximum-scale en dessous de 2 (200 %).",
    "metaViewportZoomEnabled_summary_fail": "Cette balise meta viewport restreint la capacité de l’utilisateur à zoomer ({{reasons}}).",
    "metaViewportZoomEnabled_hint_fail": "Retirez user-scalable=no et tout maximum-scale inférieur à 2 du contenu de la balise meta viewport.",
    "metaRefreshTimingAbsent_title": "La page ne doit pas utiliser un rafraîchissement meta minuté",
    "metaRefreshTimingAbsent_description": "Vérifie que <meta http-equiv=\"refresh\"> n’impose pas un délai positif de 20 heures ou moins.",
    "metaRefreshTimingAbsent_summary_fail": "Cette page se rafraîchit automatiquement après {{delay}} secondes.",
    "metaRefreshTimingAbsent_hint_fail": "Retirez le rafraîchissement meta minuté, ou proposez un moyen pour les utilisateurs de le désactiver, de le prolonger ou de le mettre en pause avant qu’il ne se déclenche.",
    "meterNamePresent_title": "Les éléments meter ont un nom accessible",
    "meterNamePresent_description": "Vérifie que les éléments avec role=\"meter\" exposent un nom accessible non vide.",
    "meterNamePresent_summary_fail": "Ce meter n’a pas de nom accessible.",
    "meterNamePresent_hint_fail": "Fournissez aria-label, aria-labelledby ou un attribut title — le contenu textuel visible n’est pas exposé comme nom accessible de ce meter.",
    "progressbarNamePresent_title": "Les barres de progression ont un nom accessible",
    "progressbarNamePresent_description": "Vérifie que les éléments avec role=\"progressbar\" exposent un nom accessible non vide.",
    "progressbarNamePresent_summary_fail": "Cette barre de progression n’a pas de nom accessible.",
    "progressbarNamePresent_hint_fail": "Fournissez aria-label, aria-labelledby ou un attribut title — le contenu textuel visible n’est pas exposé comme nom accessible de cette barre de progression.",
    "tooltipNamePresent_title": "Les infobulles ont un nom accessible",
    "tooltipNamePresent_description": "Vérifie que les éléments avec role=\"tooltip\" exposent un nom accessible non vide.",
    "tooltipNamePresent_summary_fail": "Cette infobulle n’a pas de nom accessible.",
    "tooltipNamePresent_hint_fail": "Fournissez un texte pour l’infobulle qui ne soit pas masqué aux technologies d’assistance, ou fournissez aria-label ou aria-labelledby.",
    "serverSideImageMapAbsent_title": "Les images ne doivent pas utiliser une carte d’image côté serveur",
    "serverSideImageMapAbsent_description": "Vérifie que les éléments <img> ne portent pas l’attribut ismap (les cartes d’image côté serveur n’ont pas d’équivalent utilisable au clavier).",
    "serverSideImageMapAbsent_summary_fail": "Cette image utilise une carte d’image côté serveur, qui n’a pas d’équivalent utilisable au clavier.",
    "serverSideImageMapAbsent_hint_fail": "Remplacez la carte d’image côté serveur (ismap) par une carte d’image côté client (<map>/<area>) ou par des liens/boutons accessibles distincts.",
    "formControlSingleLabel_title": "Les contrôles de formulaire ne doivent pas avoir plusieurs étiquettes",
    "formControlSingleLabel_description": "Vérifie qu’un contrôle de formulaire est associé à au plus un <label> (par imbrication ou par label[for]).",
    "formControlSingleLabel_summary_fail": "Ce <{{element}}> est associé à {{labelCount}} étiquettes.",
    "formControlSingleLabel_hint_fail": "Conservez une seule <label> par contrôle de formulaire (soit en l’enveloppant, soit en la référençant via for/id).",
    "formControlSingleLabel_summary_cantTell": "Ce <{{element}}> a une <label> qui l’étiquette plus une association de <label> vide supplémentaire ; vérifiez comment il est annoncé.",
    "formControlSingleLabel_hint_cantTell": "Supprimez la <label> vide redondante afin qu’une seule <label> soit associée au contrôle.",
    "nestedInteractiveControlsAbsent_title": "Les contrôles interactifs ne doivent pas être imbriqués",
    "nestedInteractiveControlsAbsent_description": "Vérifie qu’un contrôle interactif (lien, bouton, contrôle de formulaire, ou rôle de widget ARIA) ne contient pas un autre contrôle interactif.",
    "nestedInteractiveControlsAbsent_summary_fail": "Ce <{{element}}> contient un ou plusieurs contrôles interactifs imbriqués : {{nestedElements}}.",
    "nestedInteractiveControlsAbsent_hint_fail": "Déplacez le(s) contrôle(s) interactif(s) imbriqué(s) hors de cet élément ; les contrôles interactifs imbriqués ne sont pas utilisables de façon fiable via les technologies d’assistance.",
    "bypassBlocksPresent_title": "La page doit proposer un moyen de contourner les blocs répétés",
    "bypassBlocksPresent_description": "Vérifie que la page dispose d’au moins un mécanisme reconnu de contournement des blocs répétés (WCAG 2.4.1) : un point de repère main, un lien d’ancrage fonctionnel vers la même page, ou un titre.",
    "bypassBlocksPresent_summary_cantTell": "Aucun moyen reconnu de contourner les blocs de contenu répétés n’a été détecté sur cette page — vérifiez qu’un mécanisme de contournement existe.",
    "bypassBlocksPresent_hint_cantTell": "Confirmez que la page propose un mécanisme de contournement : un point de repère main (<main> ou role=\"main\"), un lien « aller au contenu » fonctionnel, ou des éléments de titre que les technologies d’assistance peuvent utiliser pour passer le contenu répété. (Un mécanisme peut être temporairement masqué — par exemple pendant qu’une boîte de dialogue modale rend la page inerte — ou fourni à l’échelle du site ; cela nécessite une confirmation humaine.)",
    "landmarkBannerIsTopLevel_title": "Le point de repère banner doit être de premier niveau",
    "landmarkBannerIsTopLevel_description": "Vérifie que le point de repère banner (role=\"banner\" ou un <header> non imbriqué) n’est pas imbriqué dans un autre point de repère.",
    "landmarkBannerIsTopLevel_summary_cantTell": "Ce point de repère banner est imbriqué dans un autre point de repère.",
    "landmarkBannerIsTopLevel_hint_cantTell": "Déplacez le point de repère banner (header/role=\"banner\") afin qu’il ne soit contenu par aucun autre point de repère ; un banner doit être une région de premier niveau de la page.",
    "landmarkContentinfoIsTopLevel_title": "Le point de repère contentinfo doit être de premier niveau",
    "landmarkContentinfoIsTopLevel_description": "Vérifie que le point de repère contentinfo (role=\"contentinfo\" ou un <footer> non imbriqué) n’est pas imbriqué dans un autre point de repère.",
    "landmarkContentinfoIsTopLevel_summary_cantTell": "Ce point de repère contentinfo est imbriqué dans un autre point de repère.",
    "landmarkContentinfoIsTopLevel_hint_cantTell": "Déplacez le point de repère contentinfo (footer/role=\"contentinfo\") afin qu’il ne soit contenu par aucun autre point de repère ; contentinfo doit être une région de premier niveau de la page.",
    "landmarkMainIsTopLevel_title": "Le point de repère main doit être de premier niveau",
    "landmarkMainIsTopLevel_description": "Vérifie que le point de repère main (role=\"main\" ou <main>) n’est pas imbriqué dans un autre point de repère.",
    "landmarkMainIsTopLevel_summary_cantTell": "Ce point de repère main est imbriqué dans un autre point de repère.",
    "landmarkMainIsTopLevel_hint_cantTell": "Déplacez le point de repère main (<main>/role=\"main\") afin qu’il ne soit contenu par aucun autre point de repère ; main doit être une région de premier niveau de la page.",
    "landmarkNoDuplicateBanner_title": "La page ne doit pas avoir plus d’un point de repère banner",
    "landmarkNoDuplicateBanner_description": "Vérifie qu’au plus un point de repère banner (role=\"banner\" ou un <header> non imbriqué) existe sur la page.",
    "landmarkNoDuplicateBanner_summary_cantTell": "Cette page a plus d’un point de repère banner.",
    "landmarkNoDuplicateBanner_hint_cantTell": "Ne conservez qu’un seul point de repère banner (header/role=\"banner\") par page.",
    "landmarkNoDuplicateContentinfo_title": "La page ne doit pas avoir plus d’un point de repère contentinfo",
    "landmarkNoDuplicateContentinfo_description": "Vérifie qu’au plus un point de repère contentinfo (role=\"contentinfo\" ou un <footer> non imbriqué) existe sur la page.",
    "landmarkNoDuplicateContentinfo_summary_cantTell": "Cette page a plus d’un point de repère contentinfo.",
    "landmarkNoDuplicateContentinfo_hint_cantTell": "Ne conservez qu’un seul point de repère contentinfo (footer/role=\"contentinfo\") par page.",
    "landmarkNoDuplicateMain_title": "La page ne doit pas avoir plus d’un point de repère main",
    "landmarkNoDuplicateMain_description": "Vérifie qu’au plus un point de repère main (role=\"main\" ou <main>) existe sur la page.",
    "landmarkNoDuplicateMain_summary_cantTell": "Cette page a plus d’un point de repère main.",
    "landmarkNoDuplicateMain_hint_cantTell": "Ne conservez qu’un seul point de repère main (<main>/role=\"main\") par page.",
    "landmarkOneMain_title": "La page devrait avoir un point de repère main",
    "landmarkOneMain_description": "Vérifie que la page a au moins un point de repère main (role=\"main\" ou <main>).",
    "landmarkOneMain_summary_cantTell_missing": "Cette page n’a aucun point de repère main.",
    "landmarkOneMain_hint_cantTell_missing": "Ajoutez un point de repère main (<main> ou role=\"main\") autour du contenu principal de la page.",
    "landmarkUnique_title": "Les points de repère partageant le même rôle doivent avoir des noms uniques",
    "landmarkUnique_description": "Vérifie que lorsque deux points de repère ou plus partagent le même rôle, chacun a un nom accessible distinct.",
    "landmarkUnique_summary_cantTell_duplicateName": "Ce point de repère {{role}} partage son nom accessible avec un autre point de repère {{role}}.",
    "landmarkUnique_summary_cantTell_bothUnnamed": "Ce point de repère {{role}} n’a pas de nom accessible, et plus d’un point de repère {{role}} sans nom existe sur cette page.",
    "landmarkUnique_hint_cantTell": "Donnez à chaque point de repère {{role}} un nom distinct via aria-label ou aria-labelledby.",
    "emptyHeading_title": "Les titres ne doivent pas être vides",
    "emptyHeading_description": "Vérifie que les éléments de titre (<h1>-<h6> ou role=\"heading\") ont un nom accessible non vide.",
    "emptyHeading_summary_cantTell": "Ce titre n’a pas de nom accessible.",
    "emptyHeading_hint_cantTell": "Ajoutez du contenu textuel (ou aria-label/aria-labelledby) à ce titre, ou retirez-le s’il n’est pas nécessaire.",
    "headingOrder_title": "Les niveaux de titre ne doivent pas sauter un niveau",
    "headingOrder_description": "Vérifie que les niveaux de titre n’augmentent que d’un niveau à la fois, dans l’ordre du document.",
    "headingOrder_summary_cantTell": "Ce titre passe du niveau {{fromLevel}} au niveau {{toLevel}}, en sautant un niveau.",
    "headingOrder_hint_cantTell": "Utilisez des niveaux de titre consécutifs (ne sautez pas de niveau en descendant dans la hiérarchie) afin que le plan du document reste prévisible.",
    "pageHasHeadingOne_title": "La page devrait avoir un titre de niveau un",
    "pageHasHeadingOne_description": "Vérifie que la page a au moins un titre de niveau un (<h1> ou role=\"heading\" avec aria-level=\"1\").",
    "pageHasHeadingOne_summary_cantTell": "Cette page n’a aucun titre de niveau un.",
    "pageHasHeadingOne_hint_cantTell": "Ajoutez un titre de niveau un (<h1> ou role=\"heading\" aria-level=\"1\") identifiant le contenu principal de la page.",
    "accesskeys_title": "Les valeurs accesskey doivent être uniques",
    "accesskeys_description": "Vérifie qu’aucun élément de la page ne partage la même valeur d’attribut accesskey qu’un autre.",
    "accesskeys_summary_cantTell": "L’accesskey de cet élément est partagée avec un autre élément de la page.",
    "accesskeys_hint_cantTell": "Rendez chaque valeur accesskey unique sur l’ensemble de la page.",
    "scopeAttrValid_title": "L’attribut scope doit avoir une valeur valide",
    "scopeAttrValid_description": "Vérifie que scope=\"...\" vaut row, col, rowgroup, ou colgroup.",
    "scopeAttrValid_summary_cantTell": "Cette valeur d’attribut scope n’est pas reconnue.",
    "scopeAttrValid_hint_cantTell": "Utilisez row, col, rowgroup, ou colgroup pour l’attribut scope.",
    "tabindex_title": "tabindex ne devrait pas être supérieur à 0",
    "tabindex_description": "Vérifie que les valeurs de tabindex sont égales à 0 ou négatives, jamais un nombre positif.",
    "tabindex_summary_cantTell": "Cet élément a un tabindex positif, ce qui modifie l’ordre naturel de tabulation.",
    "tabindex_hint_cantTell": "Utilisez tabindex=\"0\" (ou une valeur négative pour le retirer de l’ordre de tabulation) plutôt qu’un nombre positif ; corrigez l’ordre dans le DOM si un ordre de tabulation différent est nécessaire.",
    "emptyTableHeader_title": "Les cellules d’en-tête de tableau ne doivent pas être vides",
    "emptyTableHeader_description": "Vérifie que les éléments <th> ont un contenu textuel visible — un en-tête nommé uniquement via aria-label/aria-labelledby est aussi signalé, car la prise en charge réelle par les lecteurs d’écran/navigateurs est incohérente.",
    "emptyTableHeader_summary_cantTell": "Cette cellule d’en-tête de tableau n’a pas de nom accessible.",
    "emptyTableHeader_hint_cantTell": "Ajoutez du contenu textuel (ou aria-label/aria-labelledby) à cette cellule d’en-tête, ou retirez-la si elle n’est pas nécessaire.",
    "emptyTableHeader_summary_cantTell_ariaOnly": "Cette cellule d’en-tête de tableau n’a pas de texte visible — son seul nom accessible provient de aria-label/aria-labelledby, que des combinaisons réelles de lecteur d’écran/navigateur (ex. NVDA+Firefox, iOS VoiceOver+Safari) sont connues pour ignorer sur les éléments <th>.",
    "emptyTableHeader_hint_cantTell_ariaOnly": "Ajoutez du contenu textuel visible à cette cellule d’en-tête (en plus de, ou à la place de, aria-label/aria-labelledby) — le texte visible est le seul mécanisme de nommage confirmé comme fonctionnel sur les lecteurs d’écran testés.",
    "labelTitleOnly_title": "Les contrôles de formulaire ne devraient pas utiliser title comme seule étiquette",
    "labelTitleOnly_description": "Vérifie qu’un contrôle de formulaire ayant un attribut title possède aussi une véritable étiquette (élément label, aria-label, ou aria-labelledby).",
    "labelTitleOnly_summary_cantTell": "Ce contrôle de formulaire repose sur l’attribut title comme seule étiquette.",
    "labelTitleOnly_hint_cantTell": "Ajoutez une <label> visible (ou aria-label/aria-labelledby) en plus de, ou à la place de, l’attribut title.",
    "imageRedundantAlt_title": "Le texte alt d’une image ne doit pas dupliquer le texte visible adjacent",
    "imageRedundantAlt_description": "Vérifie que le texte alt d’un <img> n’est pas identique à un autre texte visible déjà présent dans son élément parent immédiat.",
    "imageRedundantAlt_summary_cantTell": "Le texte alt de cette image duplique un autre texte visible juste à côté.",
    "imageRedundantAlt_hint_cantTell": "Videz le texte alt (alt=\"\") si l’image est purement décorative à côté du texte, ou supprimez la duplication redondante.",
    "tableDuplicateName_title": "La légende d’un tableau ne doit pas dupliquer son attribut summary",
    "tableDuplicateName_description": "Vérifie que le texte de la <caption> d’un <table> n’est pas identique à son attribut summary (obsolète).",
    "tableDuplicateName_summary_cantTell": "La légende de ce tableau duplique son attribut summary.",
    "tableDuplicateName_hint_cantTell": "Retirez l’attribut summary redondant, ou faites en sorte qu’il apporte une information différente de la légende.",
    "metaViewportLarge_title": "La balise meta viewport devrait permettre un zoom jusqu’à 500 %",
    "metaViewportLarge_description": "Vérifie que <meta name=\"viewport\"> ne définit pas user-scalable=no ni maximum-scale en dessous de 5 (500 %).",
    "metaViewportLarge_summary_cantTell": "Cette balise meta viewport restreint le zoom en dessous de l’objectif de bonne pratique de 500 %.",
    "metaViewportLarge_hint_cantTell": "Retirez user-scalable=no et augmentez maximum-scale à au moins 5 (500 %) si possible.",
    "presentationRoleConflict_title": "Un rôle de présentation ne doit pas entrer en conflit avec un attribut ARIA global ou la focalisabilité",
    "presentationRoleConflict_description": "Vérifie que role=\"presentation\"/\"none\" (y compris le rôle presentation implicite d’un <img alt=\"\">) n’est pas combiné avec un attribut ARIA global (aria-label, aria-hidden, aria-describedby, ...) ou avec la focalisabilité (tabindex/native).",
    "presentationRoleConflict_summary_cantTell": "Cet élément role=\"{{role}}\" a aussi une condition conflictuelle ({{attrs}}), qui restaure son rôle implicite et annule l’intention présentationnelle.",
    "presentationRoleConflict_hint_cantTell": "Retirez le(s) attribut(s) de nommage conflictuel(s) et/ou la focalisabilité (tabindex/native) si l’élément doit rester présentationnel, ou retirez role=\"presentation\"/\"none\" s’il doit être exposé aux technologies d’assistance.",
    "region_title": "Le contenu de la page devrait se trouver à l’intérieur d’un point de repère",
    "region_description": "Vérifie que le contenu sous <body> est contenu dans un point de repère.",
    "region_summary_cantTell": "Ce contenu n’est contenu dans aucun point de repère.",
    "region_hint_cantTell": "Déplacez ce contenu à l’intérieur d’un point de repère (main, nav, aside, une section nommée, etc.).",
    "skipLink_title": "Un lien d’évitement doit avoir une cible qui se résout et qui est utilisable",
    "skipLink_description": "Vérifie que le fragment href d’un lien « aller au... » se résout vers un élément réel et actuellement utilisable du document.",
    "skipLink_summary_cantTell": "La cible de ce lien d’évitement n’existe pas.",
    "skipLink_hint_cantTell": "Faites pointer le href du lien d’évitement vers un id qui existe dans le document, ou ajoutez l’élément cible manquant.",
    "skipLink_summary_unusableTarget_cantTell": "Ce lien d’évitement pointe vers une cible qui existe mais qui n’est pas actuellement utilisable.",
    "skipLink_hint_unusableTarget_cantTell": "Faites pointer ce lien d’évitement vers une cible exposée et utilisable comme destination de navigation.",
    "autocompleteValid_title": "L’attribut autocomplete doit être une valeur d’auto-remplissage valide",
    "autocompleteValid_description": "Vérifie qu’un attribut autocomplete non vide vaut « on »/« off » ou une liste de jetons d’auto-remplissage bien formée.",
    "autocompleteValid_summary_fail": "Cette valeur d’attribut autocomplete n’est pas une valeur d’auto-remplissage valide.",
    "autocompleteValid_hint_fail": "Utilisez « on »/« off », ou une liste de jetons d’auto-remplissage valide (ex. « shipping street-address », « cc-number »).",
    "htmlXmlLangMismatch_title": "lang et xml:lang ne doivent pas se contredire",
    "htmlXmlLangMismatch_description": "Vérifie que les attributs lang et xml:lang de l’élément <html> déclarent la même langue principale, lorsque les deux sont présents.",
    "htmlXmlLangMismatch_summary_fail": "Les attributs lang (« {{lang}} ») et xml:lang (« {{xmlLang}} ») déclarent des langues différentes.",
    "htmlXmlLangMismatch_hint_fail": "Faites en sorte que lang et xml:lang déclarent la même langue principale, ou retirez l’attribut obsolète xml:lang.",
    "avoidInlineSpacing_title": "Un style en ligne ne doit pas forcer l’espacement du texte en dessous du seuil WCAG",
    "avoidInlineSpacing_description": "Vérifie que lorsqu’un style en ligne force line-height, letter-spacing ou word-spacing avec !important, la valeur respecte déjà WCAG 1.4.12, ne laissant rien à surcharger à l’utilisateur.",
    "avoidInlineSpacing_summary_fail": "Le style en ligne de cet élément force {{properties}} avec !important, bloquant les surcharges d’espacement de texte par l’utilisateur.",
    "avoidInlineSpacing_hint_fail": "Retirez !important de line-height/letter-spacing/word-spacing dans les styles en ligne afin que les utilisateurs puissent surcharger l’espacement du texte.",
    "metaRefreshNoExceptions_title": "La page ne doit utiliser aucun rafraîchissement meta (AAA)",
    "metaRefreshNoExceptions_description": "Vérifie que <meta http-equiv=\"refresh\"> n’est présent en aucun cas, quel que soit le délai — la variante plus stricte, de niveau AAA, de la vérification de niveau A qui ne porte que sur les délais positifs.",
    "metaRefreshNoExceptions_summary_fail": "Cette page utilise un rafraîchissement meta, un changement de contexte automatique non initié par l’utilisateur.",
    "metaRefreshNoExceptions_hint_fail": "Retirez le rafraîchissement meta ; déclenchez la redirection/le rafraîchissement uniquement en réponse à une action de l’utilisateur.",
    "validLang_title": "L’attribut lang d’un élément doit être syntaxiquement valide",
    "validLang_description": "Vérifie que tout élément (autre que la racine <html>) ayant un attribut lang non vide utilise une étiquette de langue syntaxiquement valide.",
    "validLang_summary_fail": "Cette valeur d’attribut lang (« {{value}} ») n’est pas une étiquette de langue syntaxiquement valide.",
    "validLang_hint_fail": "Utilisez une étiquette de langue BCP47 valide (ex. « fr », « es-MX »).",
    "linkInTextBlock_title": "Les liens dans des blocs de texte doivent être distinguables du texte environnant sans se fier uniquement à la couleur",
    "linkInTextBlock_description": "Vérifie qu’un lien à l’intérieur d’un bloc de texte est visuellement distinguable du texte environnant par un soulignement, une différence de graisse/style de police, ou un contraste de couleur suffisant (>= 3:1) — pas seulement par la couleur.",
    "linkInTextBlock_summary_fail": "Ce lien dans un bloc de texte se distingue du texte environnant uniquement par la couleur.",
    "linkInTextBlock_hint_fail": "Ajoutez un soulignement, une différence de graisse/style de police, ou augmentez le contraste de couleur entre le lien et le texte environnant à au moins 3:1.",
    "noAutoplayAudio_title": "Un audio en lecture automatique devrait proposer un mécanisme de pause/arrêt ou de contrôle du volume",
    "noAutoplayAudio_description": "Signale les éléments <audio>/<video> qui se lancent automatiquement sans être coupés et sans attribut controls natif, pour une revue manuelle par rapport à l’exemption de 3 secondes de la WCAG 1.4.2.",
    "noAutoplayAudio_summary_cantTell": "Cet élément lit un audio automatiquement sans mécanisme natif de pause/arrêt ou de contrôle du volume.",
    "noAutoplayAudio_hint_cantTell": "Si ce clip dure plus de 3 secondes, ajoutez un attribut controls (ou un mécanisme personnalisé équivalent) afin que les utilisateurs puissent le mettre en pause/l’arrêter ou contrôler son volume indépendamment du volume du système.",
    "videoCaption_title": "Une vidéo préenregistrée devrait proposer une piste de sous-titres",
    "videoCaption_description": "Signale les éléments <video> sans enfant <track kind=\"captions\"|\"subtitles\">, pour une revue manuelle visant à déterminer si la vidéo a une piste audio nécessitant des sous-titres.",
    "videoCaption_summary_cantTell": "Cette vidéo n’a aucune piste de sous-titres (captions ou subtitles).",
    "videoCaption_hint_cantTell": "Si cette vidéo a une piste audio porteuse d’information, ajoutez un <track kind=\"captions\" src=\"...\"> avec le contenu sous-titré.",
    "scrollableRegionFocusable_title": "Les régions défilantes sans contenu focalisable devraient être focalisables au clavier",
    "scrollableRegionFocusable_description": "Signale les éléments dont le CSS déclare overflow:auto/scroll, qui ne contiennent aucun descendant focalisable, et qui ne sont pas eux-mêmes focalisables au clavier, pour une revue manuelle visant à déterminer si leur contenu déborde réellement et nécessite un accès au défilement au clavier.",
    "scrollableRegionFocusable_summary_cantTell": "Cet élément déclare overflow:auto/scroll, n’a aucun descendant focalisable, et n’est pas lui-même focalisable au clavier.",
    "scrollableRegionFocusable_hint_cantTell": "Si le contenu de cette région déborde réellement, ajoutez tabindex=\"0\" (et une étiquette adaptée) afin que les utilisateurs du clavier puissent la focaliser et faire défiler avec les flèches.",
    "identicalLinksSamePurpose_title": "Les liens partageant le même nom accessible devraient mener à la même destination",
    "identicalLinksSamePurpose_description": "Signale les groupes de liens qui partagent le même nom accessible mais mènent à plus d’une destination distincte, pour une revue manuelle visant à déterminer s’ils servent le même objectif.",
    "identicalLinksSamePurpose_summary_cantTell": "Ce lien partage un nom accessible avec d’autres liens de la page qui mènent à une destination différente.",
    "identicalLinksSamePurpose_hint_cantTell": "Assurez-vous que les liens ayant le même texte servent le même objectif, ou rendez le texte du lien suffisamment distinct pour décrire chaque destination.",
    "ariaBrailleEquivalent_title": "aria-braillelabel/aria-brailleroledescription doivent avoir un équivalent non braille",
    "ariaBrailleEquivalent_description": "Vérifie que les éléments utilisant aria-braillelabel ont aussi un nom accessible ordinaire, et que ceux utilisant aria-brailleroledescription ont aussi aria-roledescription.",
    "ariaBrailleEquivalent_summary_fail": "Cet élément a {{attr}} mais pas {{requires}}, son équivalent non braille.",
    "ariaBrailleEquivalent_hint_fail": "{{attr}} est un complément spécifique au braille, pas un remplacement — fournissez aussi {{requires}}.",
    "ariaConditionalAttr_title": "aria-errormessage exige que aria-invalid soit défini à une valeur autre que false",
    "ariaConditionalAttr_description": "Vérifie que les éléments ayant aria-errormessage ont aussi aria-invalid défini à « true », « grammar », ou « spelling » — sinon le message d’erreur est retiré de l’arbre d’accessibilité.",
    "ariaConditionalAttr_summary_fail": "Cet élément a aria-errormessage mais aria-invalid est absent ou « false », donc le message d’erreur n’est pas exposé.",
    "ariaConditionalAttr_hint_fail": "Définissez aria-invalid à « true » (ou « grammar »/« spelling ») chaque fois que aria-errormessage doit être exposé aux technologies d’assistance.",
    "ariaCheckedStateMismatch_title": "L’aria-checked d’une case à cocher/d’un bouton radio natif devrait correspondre à son état réel",
    "ariaCheckedStateMismatch_description": "Signale un <input type=\"checkbox\">/<input type=\"radio\"> natif dont la valeur explicite d’aria-checked ne correspond pas à son état réel (coché/indéterminé), pour une revue manuelle.",
    "ariaCheckedStateMismatch_summary_cantTell": "La valeur d’aria-checked de cet élément ne correspond pas à son état réel (coché/indéterminé).",
    "ariaCheckedStateMismatch_hint_cantTell": "Définissez aria-checked pour qu’il corresponde à l’état réel de l’élément, ou retirez-le — une case à cocher/un bouton radio natif expose déjà cet état sans lui.",
    "cssOrientationLock_title": "Le CSS ne doit pas verrouiller la page à une seule orientation",
    "cssOrientationLock_description": "Vérifie qu’aucune règle @media (orientation: portrait|landscape) ne définit un transform: rotate(...) sur la page, une technique connue pour contourner l’orientation de l’appareil.",
    "cssOrientationLock_summary_fail": "Une media query « {{mediaText}} » fait pivoter « {{selectorText}} », verrouillant la page à une seule orientation.",
    "cssOrientationLock_hint_fail": "Retirez la transformation rotate() de la media query d’orientation ; laissez la page répondre naturellement à l’orientation de l’appareil au lieu de forcer une rotation visuelle.",
    "ariaText_title": "Les éléments role=\"text\" ne devraient avoir aucun descendant focalisable",
    "ariaText_description": "Vérifie que les éléments ayant role=\"text\" ne contiennent aucun descendant focalisable (lien, bouton, contrôle de formulaire, tabindex, iframe, ou contenteditable).",
    "ariaText_summary_cantTell": "Cet élément role=\"text\" contient un descendant focalisable.",
    "ariaText_hint_cantTell": "Retirez role=\"text\" (ou retirez le descendant focalisable) — une région de « texte brut » ne devrait pas contenir de contenu focalisable.",
    "focusOrderSemantics_title": "Les éléments ajoutés à l’ordre de tabulation devraient avoir une sémantique interactive",
    "focusOrderSemantics_description": "Signale les éléments ayant tabindex >= 0 dont le rôle explicite est un rôle structurel/documentaire non interactif (ex. heading, list, region, presentation), pour une revue manuelle.",
    "focusOrderSemantics_summary_cantTell": "Cet élément est dans l’ordre de tabulation (tabindex=\"{{tabindex}}\") mais a un rôle non interactif (« {{role}} »).",
    "focusOrderSemantics_hint_cantTell": "Retirez tabindex si cet élément n’est pas censé être interactif, ou utilisez un rôle interactif correspondant à son comportement réel.",
    "pAsHeading_title": "Un <p> stylé pour ressembler à un titre devrait probablement être un véritable titre",
    "pAsHeading_description": "Signale les éléments <p> courts dont tout le texte est en gras et affiché à >= 18px, pour une revue manuelle visant à déterminer si un véritable élément de titre devrait être utilisé à la place.",
    "pAsHeading_summary_cantTell": "Ce paragraphe est entièrement en gras et affiché à une taille évoquant un titre.",
    "pAsHeading_hint_cantTell": "Si ce texte introduit une nouvelle section, utilisez un véritable élément de titre (<h1>-<h6> ou role=\"heading\") plutôt que de styler un paragraphe pour qu’il y ressemble.",
    "tableFakeCaption_title": "La première ligne d’un tableau ne devrait pas tenir lieu de véritable <caption>",
    "tableFakeCaption_description": "Signale les tableaux sans <caption> dont la première ligne a une seule cellule non vide alors que les autres lignes ont plusieurs cellules, pour une revue manuelle visant à déterminer si cette cellule fait office de légende factice.",
    "tableFakeCaption_summary_cantTell": "Ce tableau n’a pas de <caption>, mais sa première ligne est une cellule unique placée au-dessus de lignes à plusieurs cellules — elle fait peut-être office de légende factice.",
    "tableFakeCaption_hint_cantTell": "Si cette cellule est censée décrire le tableau, utilisez un véritable élément <caption> plutôt qu’une cellule isolée en première ligne.",
    "tdHasHeader_title": "Les cellules de données des grands tableaux doivent avoir un en-tête associé",
    "tdHasHeader_description": "Vérifie que chaque <td> d’un tableau grand et simple (sans colspan/rowspan) a un en-tête associé — via un attribut headers, un <th> de colonne implicite au-dessus, ou un <th> de ligne implicite à sa gauche.",
    "tdHasHeader_summary_fail": "Cette cellule de données n’a pas d’en-tête associé (pas d’attribut headers, pas de <th> de colonne au-dessus, pas de <th> de ligne à gauche).",
    "tdHasHeader_hint_fail": "Ajoutez un attribut headers référençant le(s) id du/des <th> pertinent(s), ou restructurez le tableau afin que cette cellule ait un en-tête de ligne/colonne implicite.",
    "mouseOnlyEventHandlers_title": "Les gestionnaires d’événements en ligne réservés au pointeur devraient avoir un équivalent accessible au clavier",
    "mouseOnlyEventHandlers_description": "Signale les éléments ayant un gestionnaire d’événement en ligne réservé au pointeur (onmouseover, onmouseout, onmousedown, onmouseup, ondblclick, onmousemove, onmouseenter, onmouseleave) sans équivalent accessible au clavier (onkeydown/onkeyup/onkeypress/onfocus/onblur), pour une revue manuelle.",
    "mouseOnlyEventHandlers_summary_cantTell": "Cet élément a {{attrs}} mais aucun gestionnaire équivalent accessible au clavier.",
    "mouseOnlyEventHandlers_hint_cantTell": "Ajoutez onkeydown/onkeyup/onkeypress (ou onfocus/onblur pour un comportement déclenché au survol) afin que cette fonctionnalité soit aussi accessible au clavier.",
    "linkNameQuality_title": "Le texte des liens devrait être descriptif, pas générique",
    "linkNameQuality_description": "Signale les liens dont le nom accessible complet est une formule connue comme non descriptive (ex. « cliquez ici », « en savoir plus », « plus »), pour une revue manuelle visant à déterminer si l’objet du lien est clair sans contexte supplémentaire.",
    "linkNameQuality_summary_cantTell": "Le nom accessible de ce lien (« {{name}} ») est une formule générique et non descriptive.",
    "linkNameQuality_hint_cantTell": "Faites en sorte que le texte du lien lui-même décrive sa destination/son objet (ex. « Télécharger le guide tarifaire 2026 » plutôt que « Télécharger »), ou confirmez que le contexte environnant rend déjà l’objet clair."
  }
};

function normalizeLocale(locale) {
  if (typeof locale !== 'string') return 'en';
  const s = locale.trim();
  return s ? s : 'en';
}

function getLocaleDict(engineOptions) {
  const loc = normalizeLocale(engineOptions && engineOptions.locale);
  return (I18N && I18N[loc]) ? I18N[loc] : (I18N && I18N.en ? I18N.en : {});
}

  function isTruthyMustache(val) {
    if (val === false || val === null || val === undefined) return false;
    if (typeof val === 'number') return val !== 0 && !Number.isNaN(val);
    if (typeof val === 'string') return val.length > 0;
    if (Array.isArray(val)) return val.length > 0;
    return true;
  }

  function renderMustacheLite(template, params) {
    const str = (typeof template === 'string') ? template : '';
    const ctx = (params && typeof params === 'object') ? params : null;
    if (!str || !ctx) return str;

    // Tokenize: {{...}}
    const tagRe = /\{\{\s*([#^/]?)([^}\s]+)\s*\}\}/g;

    // We render by building an AST-like stack of frames (small + deterministic).
    const root = { type: 'root', key: null, inverted: false, parts: [] };
    const stack = [root];

    let lastIndex = 0;
    let m;

    while ((m = tagRe.exec(str)) !== null) {
      const before = str.slice(lastIndex, m.index);
      if (before) stack[stack.length - 1].parts.push({ type: 'text', value: before });

      const sigil = m[1];           // '', '#', '^', '/'
      const rawKey = m[2] || '';
      const key = String(rawKey).trim();

      if (!key) {
        // Treat empty tags as literal text (no-throw).
        stack[stack.length - 1].parts.push({ type: 'text', value: m[0] });
        lastIndex = tagRe.lastIndex;
        continue;
      }

      if (sigil === '#') {
        const frame = { type: 'section', key, inverted: false, parts: [] };
        stack[stack.length - 1].parts.push(frame);
        stack.push(frame);
      } else if (sigil === '^') {
        const frame = { type: 'section', key, inverted: true, parts: [] };
        stack[stack.length - 1].parts.push(frame);
        stack.push(frame);
      } else if (sigil === '/') {
        // Close section if it matches; otherwise treat as literal.
        const top = stack[stack.length - 1];
        if (top && top.type === 'section' && top.key === key) {
          stack.pop();
        } else {
          stack[stack.length - 1].parts.push({ type: 'text', value: m[0] });
        }
      } else {
        // Variable
        stack[stack.length - 1].parts.push({ type: 'var', key });
      }

      lastIndex = tagRe.lastIndex;
    }

    // Tail text
    const tail = str.slice(lastIndex);
    if (tail) stack[stack.length - 1].parts.push({ type: 'text', value: tail });

    // If we have unclosed sections, we *don’t throw*; we just render them as literal
    // by flattening them with their original markers removed. (Deterministic.)
    function evalParts(parts) {
      let out = '';
      for (const p of parts) {
        if (!p || typeof p !== 'object') continue;
        if (p.type === 'text') out += p.value || '';
        else if (p.type === 'var') {
          const v = Object.prototype.hasOwnProperty.call(ctx, p.key) ? ctx[p.key] : '';
          out += (v === null || v === undefined) ? '' : String(v);
        } else if (p.type === 'section') {
          const v = Object.prototype.hasOwnProperty.call(ctx, p.key) ? ctx[p.key] : undefined;
          const ok = isTruthyMustache(v);
          const shouldRender = p.inverted ? !ok : ok;
          if (shouldRender) out += evalParts(p.parts || []);
        }
      }
      return out;
    }

    return evalParts(root.parts);
  }

  function applyI18nParams(str, params) {
    return renderMustacheLite(str, params);
  }


function t(key, fallback, params, engineOptions) {
  if (typeof key !== 'string' || !key.trim()) return typeof fallback === 'string' ? fallback : '';

  const dict = getLocaleDict(engineOptions);
  const v = dict ? dict[key] : null;

  // fallback to English if missing in requested locale
  const vEn = (I18N && I18N.en) ? I18N.en[key] : null;

  const base =
    (typeof v === 'string' && v) ? v :
    (typeof vEn === 'string' && vEn) ? vEn :
    (typeof fallback === 'string' ? fallback : '');

  return applyI18nParams(base, params);
}

function resolveRuleDefI18n(def, engineOptions) {
  if (!def || typeof def !== 'object') return def;
  const out = { ...def };
  if (out.i18n && typeof out.i18n === 'object') {
    out.title = t(out.i18n.titleKey, out.title, null, engineOptions);
    out.description = t(out.i18n.descriptionKey, out.description, null, engineOptions);
  }
  return out;
}

const POLICY_CONTRACTS = {
  "a11y": {
    "id": "a11y",
    "allowedOutcomes": [
      "fail",
      "pass",
      "cantTell",
      "notApplicable"
    ],
    "allowedConfidence": [
      "high",
      "medium",
      "low"
    ],
    "coerceManualFailToCantTell": true
  },
  "generic": {
    "id": "generic",
    "allowedOutcomes": [
      "fail",
      "pass",
      "cantTell",
      "notApplicable"
    ],
    "allowedConfidence": [
      "high",
      "medium",
      "low"
    ],
    "coerceManualFailToCantTell": false
  }
};

// This is the single source of truth, inlined from src/policy/resolvePolicy.js
const resolvePolicy = (function resolvePolicy(POLICY_CONTRACTS, engineOptions) {
  function normalizePolicyContract(POLICY_CONTRACTS, contract, fallbackId) {
    const fallback = POLICY_CONTRACTS[fallbackId] || POLICY_CONTRACTS.a11y;
    if (typeof contract === 'string') return POLICY_CONTRACTS[contract] || fallback;

    if (contract && typeof contract === 'object') {
      const allowedOutcomes = Array.isArray(contract.allowedOutcomes)
        ? contract.allowedOutcomes.slice()
        : fallback.allowedOutcomes.slice();

      const allowedConfidence = Array.isArray(contract.allowedConfidence)
        ? contract.allowedConfidence.slice()
        : fallback.allowedConfidence.slice();

      return {
        id:
          typeof contract.id === 'string' && contract.id.trim()
            ? contract.id.trim()
            : fallback.id || fallbackId || 'custom',
        allowedOutcomes,
        allowedConfidence,
        coerceManualFailToCantTell:
          typeof contract.coerceManualFailToCantTell === 'boolean'
            ? contract.coerceManualFailToCantTell
            : !!fallback.coerceManualFailToCantTell
      };
    }

    return fallback;
  }

  function normalizePolicyOverrides(policy) {
    const p = policy && typeof policy === 'object' ? policy : {};
    return {
      allowedOutcomes: Array.isArray(p.allowedOutcomes) ? p.allowedOutcomes.slice() : null,
      allowedConfidence: Array.isArray(p.allowedConfidence) ? p.allowedConfidence.slice() : null,
      coerceManualFailToCantTell:
        typeof p.coerceManualFailToCantTell === 'boolean' ? p.coerceManualFailToCantTell : null
    };
  }

  const opts = engineOptions && typeof engineOptions === 'object' ? engineOptions : {};
  const contract = normalizePolicyContract(POLICY_CONTRACTS, opts.policyContract, 'a11y');
  const ov = normalizePolicyOverrides(opts.policy);

  return {
    contractId: contract.id,
    allowedOutcomes: ov.allowedOutcomes || contract.allowedOutcomes.slice(),
    allowedConfidence: ov.allowedConfidence || contract.allowedConfidence.slice(),
    coerceManualFailToCantTell:
      ov.coerceManualFailToCantTell !== null
        ? ov.coerceManualFailToCantTell
        : !!contract.coerceManualFailToCantTell
  };
});

function parseCommaList(value, { lower = false } = {}) {
  if (value == null) return [];
  if (Array.isArray(value)) {
    const arr = value.map(String).map((s) => s.trim()).filter(Boolean);
    const norm = lower ? arr.map((s) => s.toLowerCase()) : arr.slice();
    // de-dupe while preserving first-seen order (deterministic)
    const seen = new Set();
    const out = [];
    for (const v of norm) {
      if (!seen.has(v)) { seen.add(v); out.push(v); }
    }
    return out;
  }
  if (typeof value !== 'string') return [];
  const raw = value.split(',').map((s) => String(s).trim()).filter(Boolean);
  const norm = lower ? raw.map((s) => s.toLowerCase()) : raw.slice();
  const seen = new Set();
  const out = [];
  for (const v of norm) {
    if (!seen.has(v)) { seen.add(v); out.push(v); }
  }
  return out;
}

function normalizeIncludeMode(mode) {
  const m = typeof mode === 'string' ? mode.trim().toLowerCase() : '';
  return m === 'or' ? 'or' : 'and';
}

function hasAnyRunOnlyKeys(runOnly) {
  if (!runOnly || typeof runOnly !== 'object') return false;

  // legacy reference-engine-like: { type:'tag', values:[...] }
  const hasLegacyTag =
    runOnly.type === 'tag' && Array.isArray(runOnly.values) && runOnly.values.length > 0;

  const hasAnyFilters =
    hasLegacyTag ||
    (Array.isArray(runOnly.tags) && runOnly.tags.length > 0) ||
    (Array.isArray(runOnly.excludeTags) && runOnly.excludeTags.length > 0) ||
    (Array.isArray(runOnly.includeRuleIds) && runOnly.includeRuleIds.length > 0) ||
    (Array.isArray(runOnly.excludeRuleIds) && runOnly.excludeRuleIds.length > 0) ||
    (Array.isArray(runOnly.includeTestIds) && runOnly.includeTestIds.length > 0) ||
    (Array.isArray(runOnly.excludeTestIds) && runOnly.excludeTestIds.length > 0);

  // IMPORTANT: includeMode by itself should NOT cause runOnly to take precedence.
  return hasAnyFilters;
}

/**
 * Normalize the selection object used at runtime.
 *
 * Supported inputs:
 * - legacy runOnly object (arrays)
 * - legacy reference-engine-like runOnly: { type:'tag', values:[...] }
 * - extended runOnly: { includeMode:'and'|'or', excludeTags:[...] }
 *
 * Output shape:
 * { includeMode, tags, excludeTags, includeRuleIds, excludeRuleIds, includeTestIds, excludeTestIds }
 */
function normalizeRunOnly(runOnly) {
  const out = {
    includeMode: 'and',
    tags: [],
    excludeTags: [],
    includeRuleIds: [],
    excludeRuleIds: [],
    includeTestIds: [],
    excludeTestIds: []
  };
  if (!runOnly || typeof runOnly !== 'object') return out;

  out.includeMode = normalizeIncludeMode(runOnly.includeMode);

  // legacy reference-engine-like: { type:'tag', values:[...] }
  if (runOnly.type === 'tag' && Array.isArray(runOnly.values)) {
    out.tags = parseCommaList(runOnly.values, { lower: true });
    return out;
  }

  out.tags = parseCommaList(runOnly.tags, { lower: true });
  out.excludeTags = parseCommaList(runOnly.excludeTags, { lower: true });

  out.includeRuleIds = parseCommaList(runOnly.includeRuleIds, { lower: false });
  out.excludeRuleIds = parseCommaList(runOnly.excludeRuleIds, { lower: false });
  
  out.includeTestIds = parseCommaList(runOnly.includeTestIds, { lower: false });
  out.excludeTestIds = parseCommaList(runOnly.excludeTestIds, { lower: false });

  return out;
}

/**
 * Resolve effective selection from engineOptions (preferred) or runOnly (legacy).
 *
 * Precedence:
 * - If runOnly is provided and non-empty => use it (legacy behavior, plus extended fields)
 * - Else => derive from engineOptions.rules/tags/includeMode (comma-separated strings)
 */
function resolveEffectiveRunOnly(engineOptions, runOnly) {
  if (hasAnyRunOnlyKeys(runOnly)) return normalizeRunOnly(runOnly);

  const eo = (engineOptions && typeof engineOptions === 'object') ? engineOptions : {};
  const mode = normalizeIncludeMode(eo.includeMode);

  const rules = (eo.rules && typeof eo.rules === 'object') ? eo.rules : null;
  const tags = (eo.tags && typeof eo.tags === 'object') ? eo.tags : null;
  const tests = (eo.tests && typeof eo.tests === 'object') ? eo.tests : null;

  const includeRuleIds = parseCommaList(rules && rules.include, { lower: false });
  const excludeRuleIds = parseCommaList(rules && rules.exclude, { lower: false });

  const includeTags = parseCommaList(tags && tags.include, { lower: true });
  const excludeTags = parseCommaList(tags && tags.exclude, { lower: true });

  const includeTestIds = parseCommaList(tests && tests.include, { lower: false });
  const excludeTestIds = parseCommaList(tests && tests.exclude, { lower: false });

  return {
    includeMode: mode,
    tags: includeTags,
    excludeTags,
    includeRuleIds,
    excludeRuleIds,
    includeTestIds,
    excludeTestIds
  };

}

function ruleIdMatches(candidate, ruleId, engineTag) {
  if (!candidate || !ruleId) return false;
  if (candidate === ruleId) return true;

  const prefix = (engineTag ? String(engineTag) : '') + '-';
  if (ruleId.startsWith(prefix) && candidate === ruleId.slice(prefix.length)) return true;
  if (candidate.startsWith(prefix) && candidate.slice(prefix.length) === ruleId) return true;

  return false;
}

function buildCompositeRuleIndex() {
  const idx = Object.create(null);
  if (!Array.isArray(COMPOSITE_RULES)) return idx;

  for (const entry of COMPOSITE_RULES) {
    if (!entry || typeof entry !== 'object') continue;
    const id = typeof entry.id === 'string' ? entry.id.trim() : String(entry.id || '').trim();
    if (!id) continue;

    const checksIds = Array.isArray(entry.checksIds)
      ? entry.checksIds.map(String).map((s) => s.trim()).filter(Boolean)
      : [];

    if (checksIds.length) idx[id] = checksIds;
  }

  return idx;
}

const COMPOSITE_RULE_INDEX = buildCompositeRuleIndex();

function expandCompositeRuleId(candidateId) {
  const id = typeof candidateId === 'string' ? candidateId.trim() : '';
  if (!id) return null;
  const checksIds = COMPOSITE_RULE_INDEX[id];
  return Array.isArray(checksIds) && checksIds.length ? checksIds : null;
}

function ruleMatchesRunOnly(def, runOnly, engineTag) {
  const norm = normalizeRunOnly(runOnly);
  const includeMode = normalizeIncludeMode(norm.includeMode);

  const defTags = Array.isArray(def.tags) ? def.tags.map((t) => String(t).toLowerCase()) : [];

  const hasRuleInclude = norm.includeRuleIds.length > 0;
  const hasTestInclude = norm.includeTestIds.length > 0;
  const hasTagInclude = norm.tags.length > 0;

  let idMatch = true;
  let tagMatch = true;

  if (hasRuleInclude) {
    idMatch = norm.includeRuleIds.some((ruleId) => {
      // 1) Direct match always wins (this allows selecting the composite itself)
      if (ruleIdMatches(ruleId, def.ruleId, engineTag || ENGINE_TAG)) return true;
      
      // 2) If candidate is a composite id, include atomic children as well
      const expanded = expandCompositeRuleId(ruleId);
      if (expanded) return expanded.includes(def.ruleId);
      
      return false;
    });
  }

  if (hasTestInclude) {
    const testMatch = norm.includeTestIds.some((id) => ruleIdMatches(id, def.ruleId, engineTag || ENGINE_TAG));
    idMatch = hasRuleInclude ? (idMatch && testMatch) : testMatch;
  }

  if (hasTagInclude) {
    tagMatch = defTags.some((t) => norm.tags.includes(t));
  }

  // Includes
  const hasAnyIdInclude = hasRuleInclude || hasTestInclude;

  if (hasAnyIdInclude || hasTagInclude) {
    if (includeMode === 'or' && hasAnyIdInclude && hasTagInclude) {
      if (!(idMatch || tagMatch)) return false;
    } else {
      // 'and' semantics (or only one include dimension present)
      if (hasAnyIdInclude && !idMatch) return false;
      if (hasTagInclude && !tagMatch) return false;
    }
  }

  // Excludes (always subtractive; apply after include)
  if (norm.excludeRuleIds.length) {
    const blocked = norm.excludeRuleIds.some((ruleId) => {
      // 1) Direct match excludes the composite itself (and any atomic with same id)
      if (ruleIdMatches(ruleId, def.ruleId, engineTag || ENGINE_TAG)) return true;
  
      // 2) If candidate is a composite id, exclude its atomic children too
      const expanded = expandCompositeRuleId(ruleId);
      if (expanded) return expanded.includes(def.ruleId);
  
      return false;
    });
    if (blocked) return false;
  }

  if (norm.excludeTestIds.length) {
    const blocked = norm.excludeTestIds.some((id) => ruleIdMatches(id, def.ruleId, engineTag || ENGINE_TAG));
    if (blocked) return false;
  }

  if (norm.excludeTags.length) {
    const blockedTag = defTags.some((t) => norm.excludeTags.includes(t));
    if (blockedTag) return false;
  }

  return true;
}

function normalizeRuleResult(def, raw, schemaVersion, policy, helpers) {
  if (!policy || typeof policy !== 'object') {
    throw new Error('normalizeRuleResult requires a resolved policy');
  }
  const pol = policy;
  const out = raw && typeof raw === 'object' ? { ...raw } : {};
  out.ruleId = def.ruleId;
  
  // NOTE: title and description are included here (already localized)
  // so consumers do not need to rejoin with the rule catalog.
  out.title = def.title;
  out.description = def.description;
  out.i18n = def.i18n || null;

  if (!pol.allowedOutcomes.includes(out.outcome)) out.outcome = 'cantTell';

  out.outcomeNormalized =
    out.outcome === 'notApplicable' ? 'inapplicable' : out.outcome;
    
    const output = (out.engineOptions && out.engineOptions.output && typeof out.engineOptions.output === 'object')
    ? out.engineOptions.output
    : null;

  const includeSelector = !(output && output.includeSelector === false);
  const includeHtml = !(output && output.includeHtml === false);

  const needsDetails = (out.outcome === 'fail' || out.outcome === 'cantTell');

  // Manual rules must never "fail" automatically
  if (pol.coerceManualFailToCantTell && (def.type === 'manual' || out.type === 'manual') && out.outcome === 'fail') {
    out.outcome = 'cantTell';
    out.outcomeNormalized = 'cantTell';
    out.error = (out.error ? String(out.error) + ' | ' : '') + 'Manual rules cannot return outcome=fail; coerced to cantTell.';
  }

  out.severity = out.severity || def.defaultSeverity;

  let conf = raw && raw.confidence;
  if (!pol.allowedConfidence.includes(conf)) conf = def.defaultConfidence;
  out.confidence = conf;

  out.type = def.type;

  // Standards-only metadata passthrough for traceability
  out.meta = {
    ruleId: def.ruleId,
    ruleInterfaceVersion: def.ruleInterfaceVersion,
    ruleVersion: def.ruleVersion,
    normative: def.normative,
    atomic: def.atomic,
    deprecated: !!def.deprecated,
    deprecation: def.deprecation || null,
    category: def.category || null,
    normativeMappings: Array.isArray(def.normativeMappings) ? def.normativeMappings.map((o) => ({ ...o })) : [],
    standard: def.standard || null,
    applicability: def.applicability || '',
    expectation: def.expectation || '',
    references: Array.isArray(def.references) ? def.references.slice() : [],
    requirements: def.requirements || null,
    mappings: def.mappings || null
  };

  out.schemaVersion = schemaVersion;

  const occ = Array.isArray(out.occurrences) ? out.occurrences : [];
  out.occurrences = occ.map((item) => {
    const o = item && typeof item === 'object' ? { ...item } : {};

    // Engine-side finalization (only if rule reported a node)
    const node = o.__node || null;
    if (node) delete o.__node;

    if (needsDetails && node && helpers && typeof helpers === 'object') {
      if (includeSelector && (!o.selector || typeof o.selector !== 'string')) {
        try {
          o.selector = (typeof helpers.buildSelector === 'function') ? String(helpers.buildSelector(node) || '') : '';
        } catch {
          o.selector = '';
        }
      }
      if (includeHtml && (!o.html || typeof o.html !== 'string')) {
        try {
          o.html = (typeof helpers.getOuterHtmlSnippet === 'function') ? String(helpers.getOuterHtmlSnippet(node) || '') : '';
        } catch {
          o.html = '';
        }
      }
    }

    // A more robust element-identity mechanism than the CSS selector string
    // alone (see dom-helpers.js's buildStructuralPath for the full
    // rationale) -- computed centrally here, for every occurrence, rather
    // than requiring each of the ~124 rules to compute it themselves.
    // Prefers the actual element reference (node, when the rule reported
    // one); falls back to re-resolving via the occurrence's own selector
    // otherwise, same as buildStructuralPath already does internally.
    if (needsDetails && helpers && typeof helpers.buildStructuralPath === 'function') {
      try {
        o.structuralPath = helpers.buildStructuralPath(node, o.selector);
      } catch {
        o.structuralPath = null;
      }
    } else {
      o.structuralPath = null;
    }

    // Enforce string types (deterministic / no-throw)
    if (typeof o.selector !== 'string') o.selector = '';
    if (typeof o.summary !== 'string') o.summary = '';
    if (typeof o.hint !== 'string') o.hint = '';
    if (typeof o.html !== 'string') o.html = '';

    // Existing i18n normalization/resolution (leave as-is, shown shortened here)
    if (o.i18n && typeof o.i18n === 'object' && !Array.isArray(o.i18n)) {
      const ii = { ...o.i18n };
      if (typeof ii.summaryKey !== 'string') ii.summaryKey = '';
      if (typeof ii.hintKey !== 'string') ii.hintKey = '';
      if (ii.params && typeof ii.params === 'object' && !Array.isArray(ii.params)) {
        ii.params = { ...ii.params };
      } else {
        ii.params = {};
      }
      o.i18n = ii;

      if (ii.summaryKey) o.summary = t(ii.summaryKey, o.summary, ii.params, out.engineOptions || null);
      if (ii.hintKey) o.hint = t(ii.hintKey, o.hint, ii.params, out.engineOptions || null);
    } else {
      o.i18n = null;
    }

    return o;
  });

  if (raw && raw.error) out.error = String(raw.error);

  return out;
}

function toCatalogEntry(r, engineOptions) {
  return {
    ruleId: r.ruleId,
    title: (r && r.i18n ? t(r.i18n.titleKey, r.title, null, engineOptions) : r.title),
    description: (r && r.i18n ? t(r.i18n.descriptionKey, r.description, null, engineOptions) : r.description),
    i18n: r.i18n || null,
    helpUrl: r.helpUrl,
    tags: Array.isArray(r.tags) ? r.tags.slice() : [],
    wcagSc: Array.isArray(r.wcagSc) ? r.wcagSc.slice() : [],
    normativeMappings: Array.isArray(r.normativeMappings) ? r.normativeMappings.map((o) => ({ ...o })) : [],
    defaultSeverity: r.defaultSeverity,
    defaultConfidence: r.defaultConfidence,
    type: r.type,
    coverage: r.coverage || null,

    data: (r.data === undefined ? null : r.data),

    ruleInterfaceVersion: r.ruleInterfaceVersion,
    ruleVersion: r.ruleVersion,
    normative: r.normative,
    atomic: r.atomic,
    deprecated: !!r.deprecated,
    deprecation: r.deprecation || null,
    category: r.category || null,
    standard: r.standard || null,
    applicability: r.applicability || '',
    expectation: r.expectation || '',
    references: Array.isArray(r.references) ? r.references.slice() : [],
    requirements: r.requirements || null,
    mappings: r.mappings || null
  };
}

// Inlined from src/core/contrast-helpers.js
const createContrastHelpers = (function createContrastHelpers(opts, shared) {
  const window = opts && opts.window ? opts.window : null;

  const trim = shared.trim;
  const computedStyle = shared.computedStyle;
  const composedParent = shared.composedParent;
  const buildSimpleSelector = shared.buildSimpleSelector;

  const clamp01 = (n) => {
    const x = Number(n);
    if (Number.isNaN(x)) return 0;
    if (x < 0) return 0;
    if (x > 1) return 1;
    return x;
  };

  const clamp255 = (n) => {
    const x = Number(n);
    if (Number.isNaN(x)) return 0;
    if (x < 0) return 0;
    if (x > 255) return 255;
    return x;
  };

  // -------- Shared per-run caches (shared.__contrastSharedCache lifetime is per engine run) --------

  function __getSharedWeakMapCache(propName) {
    try {
      const sc = shared && shared.__contrastSharedCache ? shared.__contrastSharedCache : null;
      if (!sc) return null;
      const existing = sc[propName];
      if (existing && typeof existing.get === 'function' && typeof existing.set === 'function')
        return existing;
      const wm = new WeakMap();
      sc[propName] = wm;
      return wm;
    } catch (_e) {
      return null;
    }
  }

  function __getSharedTextScanCache() {
    try {
      const sc = shared && shared.__contrastSharedCache ? shared.__contrastSharedCache : null;
      if (!sc) return null;

      if (
        sc.__textScanCache &&
        typeof sc.__textScanCache.get === 'function' &&
        typeof sc.__textScanCache.set === 'function' &&
        typeof sc.__textScanCache.has === 'function'
      ) {
        return sc.__textScanCache;
      }

      try {
        Object.defineProperty(sc, '__textScanCache', {
          value: new Map(),
          writable: false,
          enumerable: false,
          configurable: true
        });
      } catch {
        sc.__textScanCache = new Map();
      }
      return sc.__textScanCache;
    } catch {
      return null;
    }
  }

  function __getSharedColorParseCache() {
    try {
      const sc = shared && shared.__contrastSharedCache ? shared.__contrastSharedCache : null;
      if (!sc) return null;

      if (sc.__colorParseCache && typeof sc.__colorParseCache.get === 'function') {
        return sc.__colorParseCache;
      }
      const m = new Map();
      sc.__colorParseCache = m;
      return m;
    } catch (_e) {
      return null;
    }
  }

  // -------- Computed style memoization (per element, per run) --------

  const __localComputedStyleCache = new WeakMap();
  const __computedStyleCache =
    __getSharedWeakMapCache('__computedStyleCache') || __localComputedStyleCache;

  function __contrastComputedStyle(el) {
    try {
      if (!el || el.nodeType !== 1) return computedStyle(el);
      if (__computedStyleCache.has(el)) return __computedStyleCache.get(el);
      const cs = computedStyle(el);
      __computedStyleCache.set(el, cs);
      return cs;
    } catch {
      // Always no-throw: return empty object on any failure
      try {
        const cs = computedStyle(el);
        if (el && el.nodeType === 1) __computedStyleCache.set(el, cs);
        return cs;
      } catch {
        return {};
      }
    }
  }

  // Cache common booleans per element (per run)
  const __localHasBgImgCache = new WeakMap();
  const __localHasBlendModeCache = new WeakMap();
  const __localHasFilterCache = new WeakMap();
  const __hasBgImgCache = __getSharedWeakMapCache('__hasBgImgCache') || __localHasBgImgCache;
  const __hasBlendModeCache =
    __getSharedWeakMapCache('__hasBlendModeCache') || __localHasBlendModeCache;
  const __hasFilterCache = __getSharedWeakMapCache('__hasFilterCache') || __localHasFilterCache;

  // -------- Visibility mode resolution for getTextScan --------

  function __getVisibilityMode(engineOptions) {
    const m =
      engineOptions && typeof engineOptions.visibilityMode === 'string'
        ? engineOptions.visibilityMode
        : '';
    return m || 'styleOnly';
  }

  function __resolveVisibilityMode(ctx, engineOptions, d, w) {
    // If getTextScan was called with a direct string (unlikely, but safe)
    if (typeof engineOptions === 'string') return engineOptions;

    const candidates = [
      engineOptions,

      // common shapes
      ctx && ctx.engineOptions,
      ctx && ctx.options && ctx.options.engineOptions,
      ctx && ctx.options,
      ctx && ctx.opts && ctx.opts.engineOptions,
      ctx && ctx.opts,

      // policy layering shapes
      ctx && ctx.policyOverrides && ctx.policyOverrides.engineOptions,
      ctx && ctx.policyOverrides,
      ctx && ctx.policy && ctx.policy.engineOptions,
      ctx && ctx.policy,

      // sometimes hoisted
      ctx,

      // opts passed into createContrastHelpers
      opts && opts.engineOptions,
      opts && opts.options && opts.options.engineOptions,
      opts && opts.options,
      opts && opts.opts && opts.opts.engineOptions,
      opts && opts.opts,

      opts && opts.policyOverrides && opts.policyOverrides.engineOptions,
      opts && opts.policyOverrides,
      opts && opts.policy && opts.policy.engineOptions,
      opts && opts.policy,

      // globals sometimes used by runners
      w && w.__a11ycoreEngineOptions,
      d && d.__a11ycoreEngineOptions
    ];

    for (const c of candidates) {
      if (!c || typeof c !== 'object') continue;

      if (typeof c.visibilityMode === 'string') return c.visibilityMode;

      if (
        c.engineOptions &&
        typeof c.engineOptions === 'object' &&
        typeof c.engineOptions.visibilityMode === 'string'
      ) {
        return c.engineOptions.visibilityMode;
      }

      if (
        c.options &&
        typeof c.options === 'object' &&
        c.options.engineOptions &&
        typeof c.options.engineOptions === 'object' &&
        typeof c.options.engineOptions.visibilityMode === 'string'
      ) {
        return c.options.engineOptions.visibilityMode;
      }

      if (
        c.policy &&
        typeof c.policy === 'object' &&
        c.policy.engineOptions &&
        typeof c.policy.engineOptions === 'object' &&
        typeof c.policy.engineOptions.visibilityMode === 'string'
      ) {
        return c.policy.engineOptions.visibilityMode;
      }

      if (
        c.policyOverrides &&
        typeof c.policyOverrides === 'object' &&
        c.policyOverrides.engineOptions &&
        typeof c.policyOverrides.engineOptions === 'object' &&
        typeof c.policyOverrides.engineOptions.visibilityMode === 'string'
      ) {
        return c.policyOverrides.engineOptions.visibilityMode;
      }
    }

    return 'styleOnly';
  }

  function __asEligibilityBool(v) {
    if (typeof v === 'boolean') return v;
    if (v && typeof v === 'object' && typeof v.eligible === 'boolean') return v.eligible;
    return !!v;
  }

  // WCAG 1.4.3/1.4.6 "Incidental" exception: text that is part of an inactive
  // (disabled) user interface component has no contrast requirement. Walk the
  // ancestor chain (not just the text's immediate parent) so text nested inside
  // a disabled control, e.g. <button disabled><span>Label</span></button>, is
  // still recognized as inactive.
  function isInactiveUiComponent(el) {
    let node = el;
    let depth = 0;
    while (node && node.nodeType === 1 && depth++ < 100) {
      try {
        if (typeof node.matches === 'function' && node.matches(':disabled')) return true;
      } catch {}
      try {
        const ad = node.getAttribute
          ? String(node.getAttribute('aria-disabled') || '')
              .trim()
              .toLowerCase()
          : '';
        if (ad === 'true') return true;
      } catch {}
      node = node.parentElement;
    }
    return false;
  }

  function getTextScan(ctx, helpers, engineOptions) {
    try {
      const d = (ctx && ctx.document) || (opts && opts.document) || null;

      const w = (ctx && ctx.window) || (d && d.defaultView) || window || null;

      const rawMode = __resolveVisibilityMode(ctx, engineOptions, d, w);

      const visibilityMode =
        __getVisibilityMode({ visibilityMode: rawMode }) === 'styleAndGeometry'
          ? 'styleAndGeometry'
          : 'styleOnly';

      if (!d || typeof d.createTreeWalker !== 'function') {
        return { eligibleTextCount: 0, elements: [], visibilityMode };
      }

      const cache = __getSharedTextScanCache();
      const cacheKey = `visibilityMode=${visibilityMode}`;
      if (cache && cache.has(cacheKey)) return cache.get(cacheKey);

      // ctx.root is an array with multi-region contextSelector support
      // (dom-runner.js resolves it that way now) but back-compat with
      // any caller still passing a single element directly.
      const walkRootsRaw =
        ctx && ctx.root
          ? Array.isArray(ctx.root)
            ? ctx.root
            : [ctx.root]
          : [d.body || d.documentElement || d];
      const walkRoots = walkRootsRaw
        .map((wr) => (wr && wr.nodeType === 9 ? wr.body || wr.documentElement || wr : wr))
        .filter(Boolean);

      const SHOW_TEXT =
        w && w.NodeFilter && typeof w.NodeFilter.SHOW_TEXT === 'number'
          ? w.NodeFilter.SHOW_TEXT
          : 4;

      const isNonEmptyText = (t) => t != null && /\S/.test(String(t));

      const elToCount = new WeakMap();
      const elements = [];
      let eligibleTextCount = 0;

      const eligCache = new WeakMap();
      const inactiveCache = new WeakMap();
      const clipHiddenCache = new WeakMap();

      // clip:rect(0,0,0,0) / clip-path:inset(50%+) (the sr-only technique) has
      // no visually-presented color, so contrast rules exempt it even though
      // isDomVisibleEligible itself keeps it eligible for other callers
      // (aria-hidden-focus etc. need to find clipped-but-focusable elements).
      const isClipHidden = (el) => {
        if (!helpers || typeof helpers.getVisibilityHintsInfo !== 'function') return false;
        if (clipHiddenCache.has(el)) return clipHiddenCache.get(el);

        let hidden = false;
        try {
          let cur = el;
          let guard = 0;
          while (cur && cur.nodeType === 1 && guard++ < 100) {
            const info = helpers.getVisibilityHintsInfo(cur, ctx, {});
            if (info && Array.isArray(info.hints) && info.hints.indexOf('clipped') !== -1) {
              hidden = true;
              break;
            }
            cur = composedParent ? composedParent(cur) : cur.parentElement;
          }
        } catch {
          hidden = false;
        }

        clipHiddenCache.set(el, hidden);
        return hidden;
      };

      const isVisibleEligible = (el) => {
        if (!helpers || typeof helpers.isDomVisibleEligible !== 'function') return true;
        if (eligCache.has(el)) return eligCache.get(el);

        let ok;
        try {
          const r = helpers.isDomVisibleEligible(el, ctx, { visibilityMode });
          ok = __asEligibilityBool(r);
          if (ok && isClipHidden(el)) ok = false;
        } catch {
          ok = false;
        }

        eligCache.set(el, ok);
        return ok;
      };

      const isInactive = (el) => {
        if (inactiveCache.has(el)) return inactiveCache.get(el);
        let inactive;
        try {
          inactive = isInactiveUiComponent(el);
        } catch {
          inactive = false;
        }
        inactiveCache.set(el, inactive);
        return inactive;
      };

      let node = null;
      let guard = 0;
      // Guards against double-counting text nodes reachable from more
      // than one root when contextSelector regions overlap/nest (a
      // single TreeWalker never revisits a node, but running one
      // walker per root could otherwise walk the same subtree twice).
      const visitedTextNodes = new Set();

      for (const walkRoot of walkRoots) {
        if (guard >= 500000) break;
        let walker;
        try {
          walker = d.createTreeWalker(walkRoot, SHOW_TEXT, null);
        } catch {
          continue;
        }

        while ((node = walker.nextNode()) && guard++ < 500000) {
          if (visitedTextNodes.has(node)) continue;
          visitedTextNodes.add(node);

          const text = node && node.nodeValue;
          if (!isNonEmptyText(text)) continue;

          const el =
            node.parentElement ||
            (node.parentNode && node.parentNode.nodeType === 1 ? node.parentNode : null);

          if (!el) continue;
          // Respect subtree exclusions from engineOptions.excludeSelectors
          try {
            if (helpers && typeof helpers.isExcluded === 'function' && helpers.isExcluded(el))
              continue;
          } catch {}
          if (!isVisibleEligible(el)) continue;
          if (isInactive(el)) continue;

          eligibleTextCount++;

          const prev = elToCount.get(el);
          if (prev === undefined) {
            elToCount.set(el, 1);
            elements.push(el);
          } else {
            elToCount.set(el, prev + 1);
          }
        }
      }

      // <input type="submit"|"button"|"reset">'s visible label is
      // rendered from its `value` attribute, not a DOM text node, so it's
      // structurally invisible to the SHOW_TEXT walk above (void elements
      // can't have text-node children at all). Without this, these inputs
      // would be silently skipped by both contrast-minimum and
      // contrast-enhanced. Same eligibility gates as the text-node path
      // above, applied to the input element itself.
      const visitedValueInputs = new Set();
      for (const walkRoot of walkRoots) {
        let candidates;
        try {
          candidates = walkRoot.querySelectorAll(
            'input[type="submit" i], input[type="button" i], input[type="reset" i]'
          );
        } catch {
          continue;
        }
        for (const el of candidates) {
          if (visitedValueInputs.has(el)) continue;
          visitedValueInputs.add(el);

          const value = el.getAttribute ? el.getAttribute('value') : el.value;
          if (!isNonEmptyText(value)) continue;

          try {
            if (helpers && typeof helpers.isExcluded === 'function' && helpers.isExcluded(el))
              continue;
          } catch {}
          if (!isVisibleEligible(el)) continue;
          if (isInactive(el)) continue;

          eligibleTextCount++;

          const prev = elToCount.get(el);
          if (prev === undefined) {
            elToCount.set(el, 1);
            elements.push(el);
          } else {
            elToCount.set(el, prev + 1);
          }
        }
      }

      const out = Object.freeze({
        eligibleTextCount,
        visibilityMode,
        elements: Object.freeze(
          elements.map((el) => Object.freeze({ el, textCount: elToCount.get(el) || 0 }))
        )
      });

      if (cache) cache.set(cacheKey, out);
      return out;
    } catch {
      return { eligibleTextCount: 0, elements: [], visibilityMode: 'styleOnly' };
    }
  }

  // -------- Formatting helpers --------

  function toHex2(n) {
    const x = clamp255(n);
    const s = x.toString(16).toLowerCase();
    return s.length === 1 ? '0' + s : s;
  }

  function rgbToHex(rgb) {
    try {
      if (!rgb || typeof rgb !== 'object') return '';
      return '#' + toHex2(rgb.r) + toHex2(rgb.g) + toHex2(rgb.b);
    } catch {
      return '';
    }
  }

  // 96px/in, 72pt/in => 1px = 0.75pt
  function pxToPt(px) {
    const x = parseFloat(px);
    if (!Number.isFinite(x)) return '';
    return (x * 0.75).toFixed(1);
  }

  function fontWeightLabel(fontWeightNum) {
    const w = Number(fontWeightNum);
    if (Number.isFinite(w) && w >= 700) return 'bold';
    return 'normal';
  }

  function round2(n) {
    const x = Number(n);
    if (!Number.isFinite(x)) return '0.00';
    return (Math.round(x * 100) / 100).toFixed(2);
  }

  function rgbaToString(rgba) {
    if (!rgba || typeof rgba !== 'object') return '';
    const r = clamp255(rgba.r);
    const g = clamp255(rgba.g);
    const b = clamp255(rgba.b);
    const a = clamp01(rgba.a);
    return `rgba(${r}, ${g}, ${b}, ${a.toFixed(2)})`;
  }

  function parsePx(value) {
    if (value == null) return null;

    if (typeof value === 'number' && Number.isFinite(value)) return value;

    const s = String(value).trim().toLowerCase();
    if (!s) return null;

    const n = parseFloat(s);
    if (!Number.isFinite(n)) return null;

    if (s.endsWith('px')) return n;
    if (s.endsWith('pt')) return n * (96 / 72);

    if (s.endsWith('rem')) return n * 16;
    if (s.endsWith('em')) return n * 16;
    if (s.endsWith('%')) return (n / 100) * 16;

    return n;
  }

  function normalizeFontWeight(v) {
    const s = trim(v).toLowerCase();
    if (!s) return 400;
    if (s === 'normal') return 400;
    if (s === 'bold' || s === 'bolder') return 700;
    if (s === 'lighter') return 300;
    const n = Number.parseInt(s, 10);
    return Number.isFinite(n) ? n : 400;
  }

  function isLargeText(fontSizePx, fontWeightNum) {
    const size = parseFloat(fontSizePx);
    const w = Number(fontWeightNum);
    if (!Number.isFinite(size)) return false;
    if (size >= 24) return true;
    if (size >= 18.6667 && Number.isFinite(w) && w >= 700) return true;
    return false;
  }

  function requiredRatio(level, large) {
    const l = String(level || '').toUpperCase();
    if (l === 'AAA') return large ? 4.5 : 7.0;
    return large ? 3.0 : 4.5;
  }

  // -------- CSS color parsing + memoization --------

  const __localColorParseCache = new Map();
  const __colorParseCache = __getSharedColorParseCache() || __localColorParseCache;

  function __normalizeCssColorCacheKey(input) {
    const raw = input == null ? '' : String(input);
    let s = trim(raw).toLowerCase();
    if (!s) return '';
    s = s.replace(/\s+/g, ' ');
    s = s.replace(/\s*,\s*/g, ',');
    s = s.replace(/\(\s+/g, '(');
    s = s.replace(/\s+\)/g, ')');
    s = s.replace(/\s*\/\s*/g, '/');
    // Only strip whitespace BEFORE a '%' (e.g. "50 %" -> "50%"). Stripping
    // trailing whitespace too (the original /\s*%\s*/ pattern) ate the
    // space that separates adjacent percentage-suffixed channels in the
    // modern space-separated syntax (e.g. "rgb(100% 0% 0%)" collapsed to
    // "rgb(100%0%0%)", one fused token instead of three), silently
    // breaking that whole syntax variant.
    s = s.replace(/\s*%/g, '%');
    return s;
  }

  function __parseCssColorToRgbaUncached(input) {
    const s = trim(input).toLowerCase();
    if (!s) return null;
    if (s === 'transparent') return { r: 0, g: 0, b: 0, a: 0 };

    if (s[0] === '#') {
      const hex = s.slice(1);
      const isHex = /^[0-9a-f]+$/i.test(hex);
      if (!isHex) return null;

      const hexToInt = (h) => Number.parseInt(h, 16);

      try {
        if (hex.length === 3) {
          const r = hexToInt(hex[0] + hex[0]);
          const g = hexToInt(hex[1] + hex[1]);
          const b = hexToInt(hex[2] + hex[2]);
          return { r, g, b, a: 1 };
        }
        if (hex.length === 4) {
          const r = hexToInt(hex[0] + hex[0]);
          const g = hexToInt(hex[1] + hex[1]);
          const b = hexToInt(hex[2] + hex[2]);
          const a = hexToInt(hex[3] + hex[3]) / 255;
          return { r, g, b, a: clamp01(a) };
        }
        if (hex.length === 6) {
          const r = hexToInt(hex.slice(0, 2));
          const g = hexToInt(hex.slice(2, 4));
          const b = hexToInt(hex.slice(4, 6));
          return { r, g, b, a: 1 };
        }
        if (hex.length === 8) {
          const r = hexToInt(hex.slice(0, 2));
          const g = hexToInt(hex.slice(2, 4));
          const b = hexToInt(hex.slice(4, 6));
          const a = hexToInt(hex.slice(6, 8)) / 255;
          return { r, g, b, a: clamp01(a) };
        }
      } catch {}
      return null;
    }

    const m = s.match(/^rgba?\((.*)\)$/);

    // Modern space-separated: rgb(0 0 0 / 0.5)
    if (m && m[1] && m[1].indexOf(',') === -1) {
      const body = trim(m[1]);
      const parts2 = body.split('/').map((x) => trim(x));
      const rgbPart = parts2[0] || '';
      const aPart = parts2[1] || '';

      const rgbNums = rgbPart
        .split(/\s+/)
        .map((x) => trim(x))
        .filter(Boolean);
      if (rgbNums.length >= 3) {
        const parseChannel2 = (t) => {
          if (!t) return null;
          if (t.endsWith('%')) {
            const p = Number.parseFloat(t);
            if (!Number.isFinite(p)) return null;
            return clamp255(Math.round((p / 100) * 255));
          }
          const n = Number.parseFloat(t);
          if (!Number.isFinite(n)) return null;
          return clamp255(Math.round(n));
        };

        const r = parseChannel2(rgbNums[0]);
        const g = parseChannel2(rgbNums[1]);
        const b = parseChannel2(rgbNums[2]);
        if (r == null || g == null || b == null) return null;

        let a = 1;
        if (aPart) {
          if (aPart.endsWith('%')) {
            const p = Number.parseFloat(aPart);
            if (Number.isFinite(p)) a = clamp01(p / 100);
          } else {
            const n = Number.parseFloat(aPart);
            if (Number.isFinite(n)) a = clamp01(n);
          }
        }

        return { r, g, b, a };
      }
    }

    // Comma-separated: rgb(0,0,0) / rgba(0,0,0,0.5)
    if (m && m[1]) {
      const parts = m[1].split(',').map((x) => trim(x));
      if (parts.length < 3) return null;

      const parseChannel = (t) => {
        if (!t) return null;
        if (t.endsWith('%')) {
          const p = Number.parseFloat(t);
          if (!Number.isFinite(p)) return null;
          return clamp255(Math.round((p / 100) * 255));
        }
        const n = Number.parseFloat(t);
        if (!Number.isFinite(n)) return null;
        return clamp255(Math.round(n));
      };

      const r = parseChannel(parts[0]);
      const g = parseChannel(parts[1]);
      const b = parseChannel(parts[2]);
      if (r == null || g == null || b == null) return null;

      let a = 1;
      if (parts.length >= 4) {
        const t = parts[3];
        if (t && t.endsWith('%')) {
          const p = Number.parseFloat(t);
          if (Number.isFinite(p)) a = clamp01(p / 100);
        } else {
          const n = Number.parseFloat(t);
          if (Number.isFinite(n)) a = clamp01(n);
        }
      }
      return { r, g, b, a };
    }

    // Fallback: let the platform parse named/system colors.
    // Useful in jsdom/browsers where computed styles may return keywords like "black" or "CanvasText".
    try {
      const w = window || null;
      const d = w && w.document ? w.document : null;
      if (
        w &&
        d &&
        typeof d.createElement === 'function' &&
        typeof w.getComputedStyle === 'function'
      ) {
        const probe = d.createElement('span');
        // Avoid layout/paint side effects
        probe.style.position = 'absolute';
        probe.style.left = '-9999px';
        probe.style.top = '-9999px';
        probe.style.opacity = '0';
        probe.style.color = String(input);
        const parent = d.body || d.documentElement;
        if (parent && typeof parent.appendChild === 'function') parent.appendChild(probe);

        let computed = '';
        try {
          computed = (w.getComputedStyle(probe) && w.getComputedStyle(probe).color) || '';
        } catch (_e) {
          computed = '';
        }

        try {
          if (probe && probe.parentNode) probe.parentNode.removeChild(probe);
        } catch (_e) {}

        const normalized = __normalizeCssColorCacheKey(computed);
        if (normalized && normalized !== __normalizeCssColorCacheKey(input)) {
          // Reuse the parser on the computed rgb()/rgba() string.
          const parsed = __parseCssColorToRgbaUncached(normalized);
          if (parsed) return parsed;
        }
      }
    } catch (_e) {}

    return null;
  }

  function parseCssColorToRgba(input) {
    const key = __normalizeCssColorCacheKey(input);
    if (!key) return null;
    if (__colorParseCache.has(key)) return __colorParseCache.get(key);

    const out = __parseCssColorToRgbaUncached(key);
    __colorParseCache.set(key, out);
    return out;
  }

  // -------- Color math --------

  function compositeRgba(src, dst) {
    const s = src && typeof src === 'object' ? src : { r: 0, g: 0, b: 0, a: 0 };
    const d = dst && typeof dst === 'object' ? dst : { r: 0, g: 0, b: 0, a: 0 };

    const as = clamp01(s.a);
    const ad = clamp01(d.a);

    const outA = as + ad * (1 - as);
    if (outA <= 0) return { r: 0, g: 0, b: 0, a: 0 };

    const rs = clamp255(s.r);
    const gs = clamp255(s.g);
    const bs = clamp255(s.b);

    const rd = clamp255(d.r);
    const gd = clamp255(d.g);
    const bd = clamp255(d.b);

    const outR = (rs * as + rd * ad * (1 - as)) / outA;
    const outG = (gs * as + gd * ad * (1 - as)) / outA;
    const outB = (bs * as + bd * ad * (1 - as)) / outA;

    return {
      r: clamp255(Math.round(outR)),
      g: clamp255(Math.round(outG)),
      b: clamp255(Math.round(outB)),
      a: clamp01(outA)
    };
  }

  function srgbToLinear(c) {
    const cs = Number(c) / 255;
    if (cs <= 0.03928) return cs / 12.92;
    return Math.pow((cs + 0.055) / 1.055, 2.4);
  }

  function relativeLuminance(rgb) {
    const r = srgbToLinear(clamp255(rgb.r));
    const g = srgbToLinear(clamp255(rgb.g));
    const b = srgbToLinear(clamp255(rgb.b));
    return 0.2126 * r + 0.7152 * g + 0.0722 * b;
  }

  function contrastRatio(fgRgb, bgRgb) {
    const L1 = relativeLuminance(fgRgb);
    const L2 = relativeLuminance(bgRgb);
    const lighter = Math.max(L1, L2);
    const darker = Math.min(L1, L2);
    return (lighter + 0.05) / (darker + 0.05);
  }

  function truncateCssValue(v, maxLen) {
    const s = trim(v);
    const n = (Number(maxLen) | 0) > 10 ? Number(maxLen) | 0 : 80;
    if (s.length <= n) return s;
    return s.slice(0, n - 3) + '...';
  }

  function hasBackgroundImageOrGradient(style) {
    try {
      const v = style && style.backgroundImage;
      return !!(v && String(v).trim() && String(v).trim().toLowerCase() !== 'none');
    } catch {
      return false;
    }
  }

  function hasBlendMode(style) {
    try {
      const v = style && style.mixBlendMode;
      return !!(v && String(v).trim() && String(v).trim().toLowerCase() !== 'normal');
    } catch {
      return false;
    }
  }

  function hasFilter(style) {
    try {
      const f = style && style.filter;
      const bf = style && style.backdropFilter;
      const fOn = f && String(f).trim().toLowerCase() !== 'none';
      const bfOn = bf && String(bf).trim().toLowerCase() !== 'none';
      return !!(fOn || bfOn);
    } catch {
      return false;
    }
  }

  function __hasBackgroundImageOrGradientEl(el, cs) {
    try {
      if (!el || el.nodeType !== 1) return hasBackgroundImageOrGradient(cs);
      if (__hasBgImgCache.has(el)) return __hasBgImgCache.get(el);
      const v = hasBackgroundImageOrGradient(cs);
      __hasBgImgCache.set(el, v);
      return v;
    } catch {
      return false;
    }
  }

  function __hasBlendModeEl(el, cs) {
    try {
      if (!el || el.nodeType !== 1) return hasBlendMode(cs);
      if (__hasBlendModeCache.has(el)) return __hasBlendModeCache.get(el);
      const v = hasBlendMode(cs);
      __hasBlendModeCache.set(el, v);
      return v;
    } catch {
      return false;
    }
  }

  function __hasFilterEl(el, cs) {
    try {
      if (!el || el.nodeType !== 1) return hasFilter(cs);
      if (__hasFilterCache.has(el)) return __hasFilterCache.get(el);
      const v = hasFilter(cs);
      __hasFilterCache.set(el, v);
      return v;
    } catch {
      return false;
    }
  }

  // -------- Opacity product memoization --------

  const __localOpacityProductCache = new WeakMap();
  const __opacityProductCache =
    __getSharedWeakMapCache('__opacityProductCache') || __localOpacityProductCache;

  function computeOpacityProduct(el) {
    try {
      if (!el || (typeof el !== 'object' && typeof el !== 'function')) return 1;
      if (__opacityProductCache.has(el)) return __opacityProductCache.get(el);

      let prod = 1;
      let cur = el;
      let guard = 0;
      while (cur && guard++ < 200) {
        if (cur.nodeType !== 1) {
          cur = composedParent(cur);
          continue;
        }
        const cs = __contrastComputedStyle(cur);
        const o = clamp01(Number.parseFloat(cs && cs.opacity != null ? cs.opacity : '1'));
        prod *= o;
        cur = composedParent(cur);
        if (prod <= 0) break;
      }

      const out = clamp01(prod);
      __opacityProductCache.set(el, out);
      return out;
    } catch (_e) {
      return 1;
    }
  }

  // -------- Effective foreground/background memoization --------

  const __localEffectiveForegroundCache = new WeakMap();
  const __effectiveForegroundCache =
    __getSharedWeakMapCache('__effectiveForegroundCache') || __localEffectiveForegroundCache;

  function computeEffectiveForeground(el) {
    try {
      if (el && __effectiveForegroundCache.has(el)) return __effectiveForegroundCache.get(el);
    } catch (_e) {}

    const cs = __contrastComputedStyle(el);
    const c = parseCssColorToRgba(cs && cs.color);
    if (!c) {
      const out = { rgba: null, alpha: 0, opacityProduct: computeOpacityProduct(el) };
      try {
        if (el) __effectiveForegroundCache.set(el, out);
      } catch (_e) {}
      return out;
    }

    const op = computeOpacityProduct(el);
    const out = {
      rgba: { r: c.r, g: c.g, b: c.b, a: clamp01(c.a * op) },
      alpha: clamp01(c.a * op),
      opacityProduct: op
    };
    try {
      if (el) __effectiveForegroundCache.set(el, out);
    } catch (_e) {}
    return out;
  }

  const __localEffectiveBackgroundCache = new WeakMap();
  const __effectiveBackgroundCache =
    __getSharedWeakMapCache('__effectiveBackgroundCache') || __localEffectiveBackgroundCache;

  function __bgCacheKey(opts2) {
    const contrast =
      opts2 && opts2.contrast && typeof opts2.contrast === 'object' ? opts2.contrast : {};
    const mode = contrast.mode === 'auditorAssist' ? 'auditorAssist' : 'strictConformance';
    const rootCanvasFallback =
      typeof contrast.rootCanvasFallback === 'string' && contrast.rootCanvasFallback.trim()
        ? contrast.rootCanvasFallback.trim()
        : '#ffffff';

    // Cache key must include any input that can affect computed background.
    // Note: we only cache when collectStack is false.
    return `${mode}|${rootCanvasFallback}`;
  }

  function computeEffectiveBackground(el, opts2) {
    const __bgKey = __bgCacheKey(opts2);
    const __collectStack = !!(opts2 && opts2.collectStack);

    // Only cache when stack collection is off
    if (!__collectStack) {
      try {
        if (el && __effectiveBackgroundCache.has(el)) {
          const m = __effectiveBackgroundCache.get(el);
          if (m && typeof m.get === 'function' && m.has(__bgKey)) return m.get(__bgKey);
        }
      } catch (_e) {}
    }

    const contrast =
      opts2 && opts2.contrast && typeof opts2.contrast === 'object' ? opts2.contrast : {};
    const mode = contrast.mode === 'auditorAssist' ? 'auditorAssist' : 'strictConformance';
    const rootCanvasFallback =
      typeof contrast.rootCanvasFallback === 'string' && contrast.rootCanvasFallback.trim()
        ? contrast.rootCanvasFallback.trim()
        : '#ffffff';

    const collectStack = !!(opts2 && opts2.collectStack);
    const stack = collectStack ? [] : null;

    let acc = { r: 0, g: 0, b: 0, a: 0 };
    let cur = el;
    let guard = 0;

    while (cur && guard++ < 200) {
      if (cur.nodeType !== 1) {
        cur = composedParent(cur);
        continue;
      }

      const cs = __contrastComputedStyle(cur);
      const bg = parseCssColorToRgba(cs && cs.backgroundColor);
      const op = clamp01(Number.parseFloat(cs && cs.opacity != null ? cs.opacity : '1'));

      if (bg) {
        const layer = { r: bg.r, g: bg.g, b: bg.b, a: clamp01(bg.a) };
        if (collectStack) {
          stack.push({
            selector: __getSimpleSelectorCached(cur, (cur.tagName || '').toLowerCase() || 'html'),
            bg: { r: layer.r, g: layer.g, b: layer.b, a: layer.a },
            opacity: op
          });
        }
        // This ancestor's own background sits BEHIND everything
        // already accumulated from its descendants (acc is painted
        // over it here).
        acc = compositeRgba(acc, layer);
      }

      // CSS opacity on an ancestor scales the *entire rendered
      // subtree* (its own background plus everything already
      // accumulated from descendants) as one compositing group
      // against whatever is further out — not just that ancestor's
      // own background layer. Applying it here (after folding in
      // this ancestor's own bg) keeps that correct even when
      // accumulated alpha already reached 1 from an inner opaque
      // layer, which is why there is no longer an early
      // `acc.a >= 1` exit: a still-unvisited outer ancestor's
      // opacity can still reduce that alpha.
      if (op < 1) {
        acc = { r: acc.r, g: acc.g, b: acc.b, a: clamp01(acc.a * op) };
      }

      cur = composedParent(cur);
    }

    let out;
    const allowAssumptions = mode === 'auditorAssist';

    if (acc.a < 1) {
      if (allowAssumptions) {
        // If the root is not opaque, apply an explicit canvas fallback.
        const fb = parseCssColorToRgba(rootCanvasFallback) || { r: 255, g: 255, b: 255, a: 1 };
        const fbOpaque = { r: fb.r, g: fb.g, b: fb.b, a: 1 };

        acc = compositeRgba(fbOpaque, acc);

        out = {
          ok: true,
          rgba: { r: acc.r, g: acc.g, b: acc.b, a: 1 },
          alpha: 1,
          stack: stack || [],
          reasonCode: null,
          assumptionsApplied: ['ROOT_CANVAS_FALLBACK'],
          assumedRootCanvasColor: rootCanvasFallback
        };
      } else {
        out = {
          ok: false,
          rgba: acc,
          alpha: acc.a,
          stack: stack || [],
          reasonCode: 'BACKGROUND_NOT_OPAQUE_AT_ROOT'
        };
      }
    } else {
      out = { ok: true, rgba: acc, alpha: acc.a, stack: stack || [], reasonCode: null };
    }

    if (!__collectStack && el) {
      try {
        let m = __effectiveBackgroundCache.get(el);
        if (!m) {
          m = new Map();
          __effectiveBackgroundCache.set(el, m);
        }
        m.set(__bgKey, out);
      } catch (_e) {}
    }

    return out;
  }

  // -------- Selector memoization --------

  const __localSimpleSelectorCache = new WeakMap();
  const __simpleSelectorCache =
    __getSharedWeakMapCache('__simpleSelectorCache') || __localSimpleSelectorCache;

  function __getSimpleSelectorCached(el, fallbackTag) {
    try {
      if (!el || el.nodeType !== 1) return '';
      if (__simpleSelectorCache.has(el)) return __simpleSelectorCache.get(el) || '';
      const s = buildSimpleSelector(el, fallbackTag);
      __simpleSelectorCache.set(el, s || '');
      return s || '';
    } catch (_e) {
      return '';
    }
  }

  function classifyBackgroundImageValue(bgImageValue) {
    try {
      const v = bgImageValue == null ? '' : String(bgImageValue).trim();
      if (!v) return 'unknown';
      const s = v.toLowerCase();
      if (s === 'none') return 'unknown';

      // Multiple layers can be comma-separated; we keep it simple + deterministic:
      // if any layer has url()/image-set() => image
      // if any layer has *gradient( => gradient
      const hasGradient = /gradient\s*\(/i.test(s);
      const hasUrl = /\burl\s*\(/i.test(s);
      const hasImageSet = /\bimage-set\s*\(/i.test(s);

      const isImage = hasUrl || hasImageSet;
      const isGradient = hasGradient;

      if (isImage && isGradient) return 'imageAndGradient';
      if (isImage) return 'image';
      if (isGradient) return 'gradient';

      // Other background-image functions exist; treat as unknown rather than guessing.
      return 'unknown';
    } catch {
      return 'unknown';
    }
  }

  // -------- Computability blocker (memoized per element, per run) --------

  const __localComputabilityBlockerCache = new WeakMap();
  const __computabilityBlockerCache =
    __getSharedWeakMapCache('__computabilityBlockerCache') || __localComputabilityBlockerCache;

  function getComputabilityBlocker(el) {
    try {
      if (el && __computabilityBlockerCache.has(el)) return __computabilityBlockerCache.get(el);
    } catch (_e) {}

    let cur = el;
    let guard = 0;
    // Once a closer ancestor's own background-color is confirmed fully
    // opaque (and that ancestor is itself free of blend-mode/filter), it
    // visually PAINTS OVER anything farther out -- a background-image/
    // gradient beyond that point can no longer affect what's rendered
    // behind el's text, so continuing to flag it as a computability blocker
    // would be a false "not computable". This covers the common "solid
    // card/nav/modal over a page-level hero image" pattern.
    //
    // This does NOT extend to mix-blend-mode/filter or an ancestor's
    // `opacity` — those are compositing-GROUP operations applied to that
    // ancestor's entire rendered subtree (including any "opaque" layer
    // inside it) before blending against whatever is further out, so a
    // closer opaque paint layer does not shield against them the way it
    // shields against a plain background-image. Applying the same
    // short-circuit there would risk a confidently wrong pass —
    // deliberately NOT done, matching this engine's no-false-positives
    // bar.
    //
    // `backdrop-filter` IS extended, because it is the
    // opposite kind of operation: it samples/filters whatever is already
    // rendered BEHIND the element (earlier in paint order), not the
    // element's own subtree, so a closer-to-el fully-opaque
    // background-color paints OVER the filtered result at el's screen
    // position and hides it completely — the same physical occlusion
    // background-image gets, just sourced from "behind" instead of
    // "this element's own background image". Confirmed with a live
    // Chromium repro (not just spec-reading): a `backdrop-filter:
    // blur()` ancestor containing an inner fully-opaque
    // `background-color` div renders that div pixel-flat, with zero
    // blur bleed-through, while sibling content without that opaque
    // layer shows the blurred backdrop clearly.
    let paintOccluded = false;

    while (cur && guard++ < 200) {
      if (cur.nodeType !== 1) {
        cur = composedParent(cur);
        continue;
      }
      const cs = __contrastComputedStyle(cur);

      if (__hasBlendModeEl(cur, cs)) {
        const out = {
          ok: false,
          reasonCode: 'MIX_BLEND_MODE',
          blockerSelector: __getSimpleSelectorCached(
            cur,
            (cur.tagName || '').toLowerCase() || 'html'
          ),
          blockerProperty: 'mix-blend-mode',
          blockerValue: truncateCssValue(cs && cs.mixBlendMode, 80)
        };
        try {
          if (el) __computabilityBlockerCache.set(el, out);
        } catch (_e) {}
        return out;
      }

      if (__hasFilterEl(cur, cs)) {
        const isFilter = cs && cs.filter && String(cs.filter).trim().toLowerCase() !== 'none';
        // __hasFilterEl's OR means: if it's not plain `filter`, it must be
        // `backdrop-filter` that's set. Unlike `filter` (unconditional
        // blocker, see the paintOccluded comment above the loop),
        // backdrop-filter is skipped once a closer opaque layer already
        // occludes it -- it can't affect what's actually rendered at el's
        // position anymore, so continue the walk rather than reporting it.
        if (!(paintOccluded && !isFilter)) {
          const out = {
            ok: false,
            reasonCode: 'BACKGROUND_FILTER_OR_BACKDROP_FILTER',
            blockerSelector: __getSimpleSelectorCached(
              cur,
              (cur.tagName || '').toLowerCase() || 'html'
            ),
            blockerProperty: isFilter ? 'filter' : 'backdrop-filter',
            blockerValue: truncateCssValue((isFilter ? cs.filter : cs.backdropFilter) || '', 80)
          };
          try {
            if (el) __computabilityBlockerCache.set(el, out);
          } catch (_e) {}
          return out;
        }
      }

      if (!paintOccluded && __hasBackgroundImageOrGradientEl(cur, cs)) {
        const bgImg = (cs && cs.backgroundImage) || '';
        const out = {
          ok: false,
          reasonCode: 'BACKGROUND_IMAGE_OR_GRADIENT',
          blockerSelector: __getSimpleSelectorCached(
            cur,
            (cur.tagName || '').toLowerCase() || 'html'
          ),
          blockerProperty: 'background-image',
          blockerValue: truncateCssValue(bgImg, 80),
          backgroundFillType: classifyBackgroundImageValue(bgImg)
        };
        try {
          if (el) __computabilityBlockerCache.set(el, out);
        } catch (_e) {}
        return out;
      }

      // An ANCESTOR (not el itself) with fractional opacity is treated
      // as a computability blocker rather than being folded into a
      // confident ratio. Group opacity uniformly scales an ancestor's
      // *entire* rendered subtree (its own background AND everything
      // already accumulated from descendants, including el's text)
      // when compositing against what's behind it — computing that
      // precisely for the foreground would require re-deriving the
      // text's rendered color the same way the background is folded
      // (rather than compositing a separately opacity-scaled
      // foreground against the fully-folded background, which
      // double-counts the ancestor's opacity). Rather than risk a
      // confidently wrong pass/fail from that mismatch, defer to
      // manual review. (el's own opacity, if any, does not trigger
      // this: it is already handled correctly by the existing
      // per-element opacity product used for the foreground.)
      if (cur !== el) {
        const ancestorOpacity = clamp01(
          Number.parseFloat(cs && cs.opacity != null ? cs.opacity : '1')
        );
        if (ancestorOpacity < 1) {
          const out = {
            ok: false,
            reasonCode: 'ANCESTOR_OPACITY',
            blockerSelector: __getSimpleSelectorCached(
              cur,
              (cur.tagName || '').toLowerCase() || 'html'
            ),
            blockerProperty: 'opacity',
            blockerValue: truncateCssValue(String(cs && cs.opacity != null ? cs.opacity : '1'), 80)
          };
          try {
            if (el) __computabilityBlockerCache.set(el, out);
          } catch (_e) {}
          return out;
        }
      }

      // `cur` cleared every check above (no blend-mode/filter/
      // background-image/gradient of its own, and no reduced
      // opacity) -- if its own background-color also happens to be
      // fully opaque, it paints over everything farther out, so
      // suppress BACKGROUND_IMAGE_OR_GRADIENT for any ancestor beyond
      // this point (see the paintOccluded comment above the loop).
      if (!paintOccluded) {
        const ownBg = parseCssColorToRgba(cs && cs.backgroundColor);
        if (ownBg && clamp01(ownBg.a) >= 1) paintOccluded = true;
      }

      cur = composedParent(cur);
    }

    const out = {
      ok: true,
      reasonCode: null,
      blockerSelector: '',
      blockerProperty: '',
      blockerValue: ''
    };
    try {
      if (el) __computabilityBlockerCache.set(el, out);
    } catch (_e) {}
    return out;
  }

  return {
    clamp01,
    clamp255,
    round2,
    rgbaToString,
    parsePx,
    normalizeFontWeight,
    isLargeText,
    requiredRatio,
    parseCssColorToRgba,
    compositeRgba,
    relativeLuminance,
    contrastRatio,
    toHex2,
    rgbToHex,
    pxToPt,
    fontWeightLabel,
    hasBackgroundImageOrGradient,
    hasBlendMode,
    hasFilter,
    computeOpacityProduct,
    computeEffectiveForeground,
    computeEffectiveBackground,
    getComputabilityBlocker,
    getTextScan,
    isInactiveUiComponent
  };
});

// Inlined from src/core/aria-helpers.js
const createAriaHelpers = (function createAriaHelpers(opts, shared) {
  const trim = (shared && shared.trim) || ((v) => (v == null ? '' : String(v)).trim());
  const lower = (v) => trim(v).toLowerCase();
  const ariaDocument = opts && opts.document;
  // Same normalization as createDomHelpers's `roots` (src/core/dom-helpers.js)
  // -- opts.root accepts a single element or an array (multi-region
  // contextSelector support). Used to bound hasLandmarkScopingAncestor's
  // ancestor walk to the scanned scope; see that function below.
  const ariaRoots = (() => {
    const r = opts && opts.root;
    if (Array.isArray(r)) return r.filter((x) => x && typeof x === 'object');
    if (r && typeof r === 'object') return [r];
    return [];
  })();

  // Existence check for a single ID token — never throws, returns false
  // (not "unknown") when the document isn't available so callers degrade
  // to their pre-existing format-only behavior rather than guessing.
  function idExists(id) {
    if (!ariaDocument || typeof ariaDocument.getElementById !== 'function') return true;
    try {
      return !!ariaDocument.getElementById(id);
    } catch {
      return true;
    }
  }

  // Presence-only accessible-name check (aria-label / aria-labelledby /
  // title), for the few role-permission decisions conditioned on whether
  // an element currently has a name (e.g. <section>'s permitted-roles set
  // — see ALLOWED_ROLES_BY_ELEMENT below). Uses the same precedence as
  // dom-helpers.js's getLandmarkNameInfo (aria-label -> aria-labelledby ->
  // title). title counts, so a <section title="..."> resolves to the
  // 'section[named]' role key rather than plain 'section'.
  function hasAccessibleNameHint(el) {
    const al = trim(getAttr(el, 'aria-label'));
    if (al) return true;
    const alb = trim(getAttr(el, 'aria-labelledby'));
    if (alb && ariaDocument && typeof ariaDocument.getElementById === 'function') {
      for (const refId of alb.split(/\s+/).filter(Boolean)) {
        try {
          const ref = ariaDocument.getElementById(refId);
          if (ref && trim(ref.textContent)) return true;
        } catch {}
      }
    }
    const title = trim(getAttr(el, 'title'));
    if (title) return true;
    return false;
  }

  // Shared "does this element have a landmark-scoping ancestor" primitive
  // — <header>'s "banner", <footer>'s "contentinfo", and <aside>'s
  // "complementary" implicit roles are all conditioned on the same W3C
  // ARIA-in-HTML exclusion: suppressed when nested inside sectioning
  // content (article/aside/nav/section), and for header/footer only,
  // also suppressed when nested inside <main> (pass includeMain: true).
  // <aside> itself omits <main> from its own exclusion list — see the
  // `aside` case in getElementRoleKey below — so callers must pass the
  // right includeMain for the role they're computing.
  //
  // Role-aware, not tag-only: an ancestor's bare tag only counts when it
  // has no role attribute; once any role attribute is present, only that
  // attribute's first-token value decides membership. So an
  // <aside role="dialog"> does not scope a nested <header> (dialog isn't a
  // scoping role) even though a plain <aside> would.
  const LANDMARK_SCOPING_TAGS = new Set(['article', 'aside', 'nav', 'section']);
  const LANDMARK_SCOPING_ROLE_TOKENS = new Set([
    'article',
    'complementary',
    'navigation',
    'region'
  ]);

  function isLandmarkScopingAncestorElement(el, includeMain) {
    const tag = lower(el.tagName || '');
    const roleAttr = getAttr(el, 'role');
    if (roleAttr == null) {
      // No role attribute at all: falls back to the plain HTML tag.
      if (LANDMARK_SCOPING_TAGS.has(tag)) return true;
      return includeMain && tag === 'main';
    }
    // A role attribute is present (even empty/invalid) — the element's
    // bare TAG no longer counts; only an explicit, scoping-relevant
    // role value does.
    const token = trim(roleAttr).split(/\s+/)[0].toLowerCase();
    if (LANDMARK_SCOPING_ROLE_TOKENS.has(token)) return true;
    return includeMain && token === 'main';
  }

  function hasLandmarkScopingAncestor(el, opts) {
    if (!isElement(el)) return false;
    const includeMain = !!(opts && opts.includeMain);
    let cur = el.parentElement;
    let guard = 0;
    while (cur && guard++ < 200) {
      if (isLandmarkScopingAncestorElement(cur, includeMain)) return true;
      // Don't climb past the scanned scope -- a contextSelector-scoped
      // (or fragment) scan should never let ancestry OUTSIDE the
      // analyzed subtree affect a role computed WITHIN it.
      if (ariaRoots.includes(cur)) break;
      cur = cur.parentElement;
    }
    return false;
  }

  // -------------------------------------------------------------------
  // A) Abstract roles — MUST NOT be used directly in a role="" attribute.
  // -------------------------------------------------------------------
  // <generated:aria-abstract-roles>
  const ABSTRACT_ROLES = new Set([
    'command',
    'composite',
    'input',
    'landmark',
    'range',
    'roletype',
    'section',
    'sectionhead',
    'select',
    'structure',
    'widget',
    'window'
  ]);
  // </generated:aria-abstract-roles>

  // -------------------------------------------------------------------
  // B) Valid, concrete (non-abstract) roles authors should not explicitly
  //    declare — either because WAI-ARIA has deprecated them (a direct
  //    replacement exists) or because they are reserved for
  //    user-agent-internal use. Flagged by aria-deprecated-role, not
  //    aria-roles-valid (which only checks existence/abstractness) — see
  //    DEPRECATED_ROLE_GUIDANCE below for per-role, reason-accurate
  //    messaging.
  // -------------------------------------------------------------------
  // Deprecated but still VALID roles (SHOULD NOT, still conforming). Reported
  // as cantTell so the author decides whether it matters to them.
  const DEPRECATED_ROLES = new Set([
    'directory' // superseded by role="list"
  ]);

  // Valid roles reserved for user-agent-internal use, which ARIA states at
  // SHOULD NOT strength — conforming, so reported as cantTell.
  const AUTHOR_DISCOURAGED_ROLES = new Set([
    'generic' // "primarily for implementors of user agents"
  ]);

  // Roles carrying an author MUST NOT, reported as fail. Empty under ARIA 1.2
  // and 1.3, whose only author MUST NOT covers abstract roles — the concern of
  // aria-roles-valid.
  const AUTHOR_PROHIBITED_ROLES = new Set([]);

  // Deprecated but still ALLOWED states/properties (SHOULD NOT, still
  // conforming): the four ARIA 1.2 keeps in the global set as deprecated and
  // marks "deprecated on this role" wherever a role does not support them.
  // Reported as cantTell, not a not-allowed fail. Flat rather than per-role
  // because the deprecation is uniform and no role prohibits any of the four.
  const DEPRECATED_ATTRS = new Set([
    'aria-disabled',
    'aria-errormessage',
    'aria-haspopup',
    'aria-invalid'
  ]);

  const DEPRECATED_ROLE_GUIDANCE = {
    directory: 'Replace it with role="list" (its recommended replacement).',
    generic:
      'Remove it — this role is reserved for user-agent-internal use, not authors. Use role="presentation"/"none" to strip semantics, a semantic role like "group" to convey grouping, or simply a plain element (which already carries the implicit generic role) instead.'
  };

  function getDeprecatedRoleGuidance(role) {
    const key = lower(role);
    return Object.prototype.hasOwnProperty.call(DEPRECATED_ROLE_GUIDANCE, key)
      ? DEPRECATED_ROLE_GUIDANCE[key]
      : 'Replace the deprecated role with its recommended replacement.';
  }

  // -------------------------------------------------------------------
  // C) Complete set of concrete (non-abstract) WAI-ARIA 1.2 role tokens,
  //    plus the WAI-ARIA Graphics Module 1.0 roles (graphics-document/
  //    graphics-object/graphics-symbol) — a separate W3C Recommendation
  //    that extends core ARIA, with a companion Graphics Accessibility API
  //    Mappings REC defining AT support. Without these, aria-roles-valid
  //    would wrongly report an AT-recognized role as unrecognized.
  //    Digital Publishing WAI-ARIA (doc-abstract etc.) is a separate
  //    module, deliberately left out of scope for now.
  // -------------------------------------------------------------------
  // <generated:aria-concrete-roles>
  const CONCRETE_ROLES = new Set([
    'alert',
    'alertdialog',
    'application',
    'article',
    'banner',
    'blockquote',
    'button',
    'caption',
    'cell',
    'checkbox',
    'code',
    'columnheader',
    'combobox',
    'comment',
    'complementary',
    'contentinfo',
    'definition',
    'deletion',
    'dialog',
    'directory',
    'doc-abstract',
    'doc-acknowledgments',
    'doc-afterword',
    'doc-appendix',
    'doc-backlink',
    'doc-biblioentry',
    'doc-bibliography',
    'doc-biblioref',
    'doc-chapter',
    'doc-colophon',
    'doc-conclusion',
    'doc-cover',
    'doc-credit',
    'doc-credits',
    'doc-dedication',
    'doc-endnote',
    'doc-endnotes',
    'doc-epigraph',
    'doc-epilogue',
    'doc-errata',
    'doc-example',
    'doc-footnote',
    'doc-foreword',
    'doc-glossary',
    'doc-glossref',
    'doc-index',
    'doc-introduction',
    'doc-noteref',
    'doc-notice',
    'doc-pagebreak',
    'doc-pagefooter',
    'doc-pageheader',
    'doc-pagelist',
    'doc-part',
    'doc-preface',
    'doc-prologue',
    'doc-pullquote',
    'doc-qna',
    'doc-subtitle',
    'doc-tip',
    'doc-toc',
    'document',
    'emphasis',
    'feed',
    'figure',
    'form',
    'generic',
    'graphics-document',
    'graphics-object',
    'graphics-symbol',
    'grid',
    'gridcell',
    'group',
    'heading',
    'img',
    'insertion',
    'link',
    'list',
    'listbox',
    'listitem',
    'log',
    'main',
    'mark',
    'marquee',
    'math',
    'menu',
    'menubar',
    'menuitem',
    'menuitemcheckbox',
    'menuitemradio',
    'meter',
    'navigation',
    'none',
    'note',
    'option',
    'paragraph',
    'presentation',
    'progressbar',
    'radio',
    'radiogroup',
    'region',
    'row',
    'rowgroup',
    'rowheader',
    'scrollbar',
    'search',
    'searchbox',
    'separator',
    'slider',
    'spinbutton',
    'status',
    'strong',
    'subscript',
    'suggestion',
    'superscript',
    'switch',
    'tab',
    'table',
    'tablist',
    'tabpanel',
    'term',
    'text',
    'textbox',
    'time',
    'timer',
    'toolbar',
    'tooltip',
    'tree',
    'treegrid',
    'treeitem'
  ]);
  // </generated:aria-concrete-roles>

  // -------------------------------------------------------------------
  // D) ARIA attribute value types.
  //    'token'        — one value from a fixed enumerated set
  //    'token-list'   — space-separated values from a fixed enumerated set
  //    'boolean'      — "true" | "false"
  //    'tristate'     — "true" | "false" | "mixed"
  //    'boolean-undefined' — "true" | "false" | "undefined"
  //    'idref'        — a single ID token (existence not verified here)
  //    'idref-list'   — space-separated ID tokens
  //    'integer'      — a base-10 integer (may be negative where noted)
  //    'number'       — a real number
  //    'string'       — free-form text (only non-emptiness may be checked)
  // -------------------------------------------------------------------
  const ATTR_VALUE_TYPES = {
    'aria-activedescendant': 'idref',
    'aria-atomic': 'boolean',
    'aria-autocomplete': 'token',
    'aria-braillelabel': 'string',
    'aria-brailleroledescription': 'string',
    'aria-busy': 'boolean',
    'aria-checked': 'tristate',
    'aria-colcount': 'integer',
    'aria-colindex': 'integer',
    'aria-colindextext': 'string',
    'aria-colspan': 'integer',
    'aria-controls': 'idref-list',
    'aria-current': 'token', // also allows 'true'/'false', handled in token set
    'aria-describedby': 'idref-list',
    'aria-description': 'string',
    'aria-details': 'idref-list',
    'aria-disabled': 'boolean',
    'aria-dropeffect': 'token-list', // deprecated but still validated if present
    'aria-errormessage': 'idref',
    'aria-expanded': 'boolean-undefined',
    'aria-flowto': 'idref-list',
    'aria-grabbed': 'boolean-undefined', // deprecated but still validated if present
    'aria-haspopup': 'token', // also allows 'true'/'false'
    'aria-hidden': 'boolean-undefined',
    'aria-invalid': 'token', // also allows 'true'/'false'
    'aria-keyshortcuts': 'string',
    'aria-label': 'string',
    'aria-labelledby': 'idref-list',
    'aria-level': 'integer',
    'aria-live': 'token',
    'aria-modal': 'boolean',
    'aria-multiline': 'boolean',
    'aria-multiselectable': 'boolean',
    'aria-orientation': 'token',
    'aria-owns': 'idref-list',
    'aria-placeholder': 'string',
    'aria-posinset': 'integer',
    'aria-pressed': 'tristate',
    'aria-readonly': 'boolean',
    'aria-relevant': 'token-list',
    'aria-required': 'boolean',
    'aria-roledescription': 'string',
    'aria-rowcount': 'integer',
    'aria-rowindex': 'integer',
    'aria-rowindextext': 'string',
    'aria-rowspan': 'integer',
    'aria-selected': 'boolean-undefined',
    'aria-setsize': 'integer',
    'aria-sort': 'token',
    'aria-valuemax': 'number',
    'aria-valuemin': 'number',
    'aria-valuenow': 'number',
    'aria-valuetext': 'string'
  };

  // Enumerated token sets for 'token'/'token-list' attributes.
  const ATTR_TOKEN_SETS = {
    'aria-autocomplete': new Set(['inline', 'list', 'both', 'none']),
    'aria-current': new Set(['page', 'step', 'location', 'date', 'time', 'true', 'false']),
    'aria-dropeffect': new Set(['copy', 'execute', 'link', 'move', 'none', 'popup']),
    'aria-haspopup': new Set(['false', 'true', 'menu', 'listbox', 'tree', 'grid', 'dialog']),
    'aria-invalid': new Set(['grammar', 'false', 'spelling', 'true']),
    'aria-live': new Set(['off', 'polite', 'assertive']),
    'aria-orientation': new Set(['horizontal', 'vertical', 'undefined']),
    'aria-relevant': new Set(['additions', 'removals', 'text', 'all']),
    'aria-sort': new Set(['ascending', 'descending', 'none', 'other'])
  };

  // -------------------------------------------------------------------
  // E) Required states/properties per role (see file header — deliberately
  //    conservative; only unambiguous, context-independent cases).
  // -------------------------------------------------------------------
  const REQUIRED_PROPS_BY_ROLE = {
    checkbox: ['aria-checked'],
    combobox: ['aria-expanded'],
    heading: ['aria-level'],
    menuitemcheckbox: ['aria-checked'],
    menuitemradio: ['aria-checked'],
    // meter always represents a concrete measurement (no "indeterminate"
    // state, no focusable/non-focusable split), so aria-valuenow is
    // unconditionally required. progressbar and separator are excluded on
    // purpose: an indeterminate progressbar may omit aria-valuenow, and
    // separator only requires it when focusable. combobox's aria-controls
    // is likewise conditional (required only once the popup is displayed),
    // so it's left out too.
    meter: ['aria-valuenow'],
    radio: ['aria-checked'],
    scrollbar: ['aria-valuenow'],
    slider: ['aria-valuenow'],
    switch: ['aria-checked']
  };

  // -------------------------------------------------------------------
  // F) Required owned (child) roles for composite/container roles.
  //    Value is an array of alternative acceptable child roles (any one
  //    satisfies the requirement). aria-owns references also count as
  //    "owning" — checked by the rule, not this table.
  // -------------------------------------------------------------------
  const REQUIRED_OWNED_ROLES = {
    list: ['listitem'],
    listbox: ['option', 'group'],
    menu: ['menuitem', 'menuitemcheckbox', 'menuitemradio', 'group'],
    menubar: ['menuitem', 'menuitemcheckbox', 'menuitemradio', 'group'],
    radiogroup: ['radio'],
    rowgroup: ['row'],
    table: ['row', 'rowgroup'],
    grid: ['row', 'rowgroup'],
    treegrid: ['row', 'rowgroup'],
    tablist: ['tab'],
    tree: ['treeitem', 'group'],
    row: ['cell', 'gridcell', 'columnheader', 'rowheader']
  };

  // -------------------------------------------------------------------
  // G) Required context (parent) role for roles that must be owned by a
  //    specific ancestor role. Value is an array of acceptable ancestor
  //    roles (any one satisfies the requirement); ownership may be via
  //    DOM containment OR aria-owns (checked by the rule).
  // -------------------------------------------------------------------
  const REQUIRED_CONTEXT_ROLE = {
    listitem: ['list'],
    option: ['listbox', 'group'],
    menuitem: ['menu', 'menubar', 'group'],
    menuitemcheckbox: ['menu', 'menubar', 'group'],
    menuitemradio: ['menu', 'menubar', 'group'],
    tab: ['tablist'],
    tabpanel: [], // no single required container in ARIA 1.2; left unconstrained
    treeitem: ['tree', 'group'],
    row: ['rowgroup', 'grid', 'table', 'treegrid'],
    cell: ['row'],
    gridcell: ['row'],
    columnheader: ['row'],
    rowheader: ['row'],
    rowgroup: ['grid', 'table', 'treegrid']
  };

  // -------------------------------------------------------------------
  // H) ARIA-in-HTML permitted roles per element (deliberately scoped to
  //    the most common elements first — see file header). `null` values
  //    are used for elements that permit "any role" in typical states.
  //    Element keys may include a simple attribute condition using the
  //    form 'tag[attr]' or 'tag[attr=value]' for the small number of
  //    elements whose permitted roles depend on an attribute.
  // -------------------------------------------------------------------
  const ALLOWED_ROLES_BY_ELEMENT = {
    // A plain <a href> is constrained to these override roles — unlike a
    // hrefless <a>, which is unconstrained. Restating the native 'link'
    // role is always permitted via the native-role fallback below.
    'a[href]': [
      'button',
      'checkbox',
      'menuitem',
      'menuitemcheckbox',
      'menuitemradio',
      'option',
      'radio',
      'switch',
      'tab',
      'treeitem',
      'doc-backlink',
      'doc-biblioref',
      'doc-glossref',
      'doc-noteref'
    ],
    // Restating the native 'article' role remains permitted via the
    // native-role fallback below regardless of this list.
    article: ['feed', 'presentation', 'none', 'document', 'application', 'main', 'region'],
    // <area href> permits no override role at all — only its native 'link'
    // role, via the native-role fallback below. Empty array (not null)
    // encodes "constrained to nothing", same convention as
    // 'label[associated]' below.
    'area[href]': [],
    // <area> without href permits only these two roles. ('generic' is
    // technically allowed but SHOULD NOT be used, so it's left out.)
    area: ['button', 'link'],
    // No explicit role is permitted on <html>, and it has no native role
    // to restate, so an empty array is correct (not "unconstrained").
    html: [],
    // No override role is permitted on <picture>, and it has no implicit
    // ARIA role to restate — empty array, same convention as 'html' /
    // 'area[href]' above.
    picture: [],
    // Includes 'gridcell', 'separator', 'slider', 'treeitem': composite-grid
    // widgets commonly build interactive cells on <button> (e.g. a date
    // picker whose day cells are role="gridcell").
    button: [
      'checkbox',
      'combobox',
      'gridcell',
      'link',
      'menuitem',
      'menuitemcheckbox',
      'menuitemradio',
      'option',
      'radio',
      'separator',
      'slider',
      'switch',
      'tab',
      'treeitem'
    ],
    h1: ['tab', 'presentation', 'none'],
    h2: ['tab', 'presentation', 'none'],
    h3: ['tab', 'presentation', 'none'],
    h4: ['tab', 'presentation', 'none'],
    h5: ['tab', 'presentation', 'none'],
    h6: ['tab', 'presentation', 'none'],
    hr: ['none', 'presentation'],
    // 'complementary' is <aside>'s own native role — allowed via the
    // native-role fallback below even though it's not in this list
    // (spec: "also allowed, but NOT RECOMMENDED", same shape as <nav>).
    aside: ['feed', 'none', 'note', 'presentation', 'region', 'search'],
    form: ['form', 'search', 'none', 'presentation'],
    // The array is identical for both keys; what differs by nesting is only
    // the native-role match (see 'header[toplevel]' below and
    // getElementRoleKey's header branch). A top-level <header role="banner">
    // restates its own implicit "banner" role — a no-op, always permitted —
    // even though 'banner' isn't in this array (same shape as <section>'s
    // named/unnamed split).
    'header[toplevel]': ['group', 'none', 'presentation', 'doc-footnote'],
    header: ['group', 'none', 'presentation', 'doc-footnote'],
    // A <label> associated with a labelable control permits no explicit
    // role (see getElementRoleKey's label[associated] split above).
    'label[associated]': [],
    // Permitted roles depend on whether the img has a non-empty alt (see
    // getElementRoleKey's img[alt]/img split above).
    'img[alt]': [
      'button',
      'checkbox',
      'link',
      'math',
      'menuitem',
      'menuitemcheckbox',
      'menuitemradio',
      'meter',
      'option',
      'progressbar',
      'radio',
      'scrollbar',
      'separator',
      'slider',
      'switch',
      'tab',
      'treeitem'
    ],
    img: ['presentation', 'none'],
    li: [
      'menuitem',
      'menuitemcheckbox',
      'menuitemradio',
      'option',
      'radio',
      'separator',
      'tab',
      'treeitem',
      'listitem',
      'presentation',
      'none'
    ],
    nav: [
      'doc-index',
      'doc-pagelist',
      'doc-toc',
      'menu',
      'menubar',
      'none',
      'presentation',
      'tablist'
    ],
    // Only 'application' is permitted on <video>.
    video: ['application'],
    // Same as <video>, plus 'img'/'document' — an <object> can stand in
    // for an image or a full document.
    object: ['application', 'img', 'document'],
    // 'region' is only permitted when the section has an accessible name
    // (its conditional native role in that case — see getElementRoleKey's
    // section[named]/section split above); every other role here is
    // permitted regardless of naming.
    'section[named]': [
      'alert',
      'alertdialog',
      'application',
      'banner',
      'complementary',
      'contentinfo',
      'dialog',
      'document',
      'feed',
      'group',
      'log',
      'main',
      'marquee',
      'navigation',
      'none',
      'note',
      'presentation',
      'region',
      'search',
      'status',
      'tabpanel'
    ],
    section: [
      'alert',
      'alertdialog',
      'application',
      'banner',
      'complementary',
      'contentinfo',
      'dialog',
      'document',
      'feed',
      'group',
      'log',
      'main',
      'marquee',
      'navigation',
      'none',
      'note',
      'presentation',
      'search',
      'status',
      'tabpanel'
    ],
    ol: [
      'group',
      'listbox',
      'menu',
      'menubar',
      'radiogroup',
      'tablist',
      'toolbar',
      'tree',
      'presentation',
      'none'
    ],
    ul: [
      'group',
      'listbox',
      'menu',
      'menubar',
      'radiogroup',
      'tablist',
      'toolbar',
      'tree',
      'presentation',
      'none'
    ],
    // role="button" is only permitted when paired with aria-pressed (see
    // getElementRoleKey's checkbox[aria-pressed] split above).
    'input[type=checkbox][aria-pressed]': ['button', 'menuitemcheckbox', 'option', 'switch'],
    'input[type=checkbox]': ['menuitemcheckbox', 'option', 'switch'],
    'input[type=radio]': ['menuitemradio'],
    'input[type=image]': [
      'button',
      'link',
      'menuitem',
      'menuitemcheckbox',
      'menuitemradio',
      'radio',
      'switch'
    ],
    'input[type=text]': ['combobox', 'searchbox', 'spinbutton'],
    'input[type=search]': ['combobox', 'spinbutton'],
    'input[type=tel]': ['combobox', 'spinbutton'],
    'input[type=url]': ['combobox', 'spinbutton'],
    'input[type=email]': ['combobox', 'spinbutton'],
    select: ['menu'],
    // <select multiple> or <select size> 1>: native role is listbox, not
    // combobox (see NATIVE_ROLE_BY_ELEMENT_KEY below) — no override role
    // is permitted, but restating the native listbox role is always
    // allowed via the native-role fallback.
    'select[multiple]': [],
    // <table> permits any role. <td>/<th>/<tr> are spec'd as context-
    // dependent (restricted only when the ancestor <table> is exposed as
    // role=table/grid/treegrid); that conditional isn't implemented here,
    // so they're left unconstrained rather than guessing at ancestor-role
    // resolution.
    table: null,
    td: null,
    th: null,
    tr: null
  };

  // -------------------------------------------------------------------
  // H2) Native/implicit role per ALLOWED_ROLES_BY_ELEMENT key. Keeping an
  //     element's own native role (e.g. role="list" on <ul>, role="table"
  //     on <table>) is never a spec violation — the ARIA-in-HTML "allowed
  //     roles" tables enumerate roles you may override *to*, not the
  //     native default, which remains implicitly valid whether or not it
  //     is redundantly re-declared. isRoleAllowedOnElement always accepts
  //     this role in addition to whatever ALLOWED_ROLES_BY_ELEMENT lists.
  // -------------------------------------------------------------------
  const NATIVE_ROLE_BY_ELEMENT_KEY = {
    'a[href]': 'link',
    'area[href]': 'link',
    article: 'article',
    aside: 'complementary',
    button: 'button',
    form: 'form',
    // No entry for plain 'header' — a header nested in sectioning
    // content/<main> has no implicit role to restate.
    'header[toplevel]': 'banner',
    h1: 'heading',
    h2: 'heading',
    h3: 'heading',
    h4: 'heading',
    h5: 'heading',
    h6: 'heading',
    hr: 'separator',
    'img[alt]': 'img',
    img: 'img',
    li: 'listitem',
    nav: 'navigation',
    ol: 'list',
    ul: 'list',
    'input[type=checkbox][aria-pressed]': 'checkbox',
    'input[type=checkbox]': 'checkbox',
    'input[type=radio]': 'radio',
    'input[type=image]': 'button',
    'input[type=text]': 'textbox',
    'input[type=search]': 'searchbox',
    'input[type=tel]': 'textbox',
    'input[type=url]': 'textbox',
    'input[type=email]': 'textbox',
    select: 'combobox',
    'select[multiple]': 'listbox',
    table: 'table',
    td: 'cell',
    th: 'columnheader',
    tr: 'row'
  };

  // -------------------------------------------------------------------
  // I) Native HTML tag -> implicit "containment role" mapping, used only
  //    for aria-required-children / aria-required-parent ownership
  //    matching (getContainmentRole). Deliberately small and scoped to
  //    exactly the roles referenced by REQUIRED_OWNED_ROLES /
  //    REQUIRED_CONTEXT_ROLE above, so that adding an explicit container
  //    role (e.g. role="list" on a <ul>, a common CSS-reset workaround)
  //    does not produce a false positive against plain native children
  //    (e.g. <li> with no role attribute) — same scope-limiting rationale
  //    as ALLOWED_ROLES_BY_ELEMENT (see file header).
  // -------------------------------------------------------------------
  const NATIVE_CONTAINMENT_ROLE_BY_ELEMENT = {
    li: 'listitem',
    option: 'option',
    tr: 'row',
    td: 'cell',
    th: 'columnheader',
    thead: 'rowgroup',
    tbody: 'rowgroup',
    tfoot: 'rowgroup',
    ul: 'list',
    ol: 'list',
    table: 'table',
    select: 'listbox',
    'input[type=radio]': 'radio'
  };

  function isElement(el) {
    return !!(el && el.nodeType === 1);
  }

  function getAttr(el, name) {
    try {
      return el && el.getAttribute ? el.getAttribute(name) : null;
    } catch {
      return null;
    }
  }

  // -------------------------------------------------------------------
  // Public API
  // -------------------------------------------------------------------

  function getExplicitRole(el) {
    if (!isElement(el)) return '';
    const raw = trim(getAttr(el, 'role'));
    if (!raw) return '';
    // role attribute may be a space-separated fallback list; the first
    // token is the "primary" role used by the accessibility tree.
    const tokens = raw.split(/\s+/).filter(Boolean);
    return tokens.length ? lower(tokens[0]) : '';
  }

  function getAllRoleTokens(el) {
    if (!isElement(el)) return [];
    const raw = trim(getAttr(el, 'role'));
    if (!raw) return [];
    return raw.split(/\s+/).filter(Boolean).map(lower);
  }

  function isAbstractRole(role) {
    return ABSTRACT_ROLES.has(lower(role));
  }

  function isDeprecatedRole(role) {
    return DEPRECATED_ROLES.has(lower(role));
  }

  function isAuthorDiscouragedRole(role) {
    return AUTHOR_DISCOURAGED_ROLES.has(lower(role));
  }

  function isAuthorProhibitedRole(role) {
    return AUTHOR_PROHIBITED_ROLES.has(lower(role));
  }

  function isDeprecatedAttr(attr /* , role */) {
    return DEPRECATED_ATTRS.has(lower(attr));
  }

  function isKnownRole(role) {
    const r = lower(role);
    return ABSTRACT_ROLES.has(r) || CONCRETE_ROLES.has(r);
  }

  function isValidConcreteRole(role) {
    return CONCRETE_ROLES.has(lower(role));
  }

  function isValidAriaAttrName(name) {
    const n = lower(name);
    if (!n || n.slice(0, 5) !== 'aria-') return false;
    return Object.prototype.hasOwnProperty.call(ATTR_VALUE_TYPES, n);
  }

  function getAttrValueType(name) {
    return ATTR_VALUE_TYPES[lower(name)] || null;
  }

  // Validates a single attribute's raw string value against its declared
  // value type. Returns { valid, reason } — reason is a short machine
  // code, not a user-facing string (rules localize their own messages).
  function validateAttrValue(name, rawValue) {
    const type = getAttrValueType(name);
    if (!type) return { valid: true, reason: 'unknown-attr-skip' };

    const v = trim(rawValue);

    switch (type) {
      case 'boolean': {
        const ok = v === 'true' || v === 'false';
        return { valid: ok, reason: ok ? '' : 'expected-true-false' };
      }
      case 'boolean-undefined': {
        const ok = v === 'true' || v === 'false' || v === 'undefined' || v === '';
        return { valid: ok, reason: ok ? '' : 'expected-true-false-undefined' };
      }
      case 'tristate': {
        const ok = v === 'true' || v === 'false' || v === 'mixed';
        return { valid: ok, reason: ok ? '' : 'expected-true-false-mixed' };
      }
      case 'integer': {
        const ok = /^-?\d+$/.test(v);
        return { valid: ok, reason: ok ? '' : 'expected-integer' };
      }
      case 'number': {
        const ok = v !== '' && Number.isFinite(Number(v));
        return { valid: ok, reason: ok ? '' : 'expected-number' };
      }
      case 'token': {
        const set = ATTR_TOKEN_SETS[lower(name)];
        if (!set) return { valid: true, reason: 'no-token-set-defined' };
        const ok = set.has(lower(v));
        return { valid: ok, reason: ok ? '' : 'invalid-token' };
      }
      case 'token-list': {
        const set = ATTR_TOKEN_SETS[lower(name)];
        if (!set) return { valid: true, reason: 'no-token-set-defined' };
        const parts = v.split(/\s+/).filter(Boolean).map(lower);
        if (!parts.length) return { valid: false, reason: 'empty-token-list' };
        const ok = parts.every((p) => set.has(p));
        return { valid: ok, reason: ok ? '' : 'invalid-token' };
      }
      case 'idref': {
        // An explicitly-empty value is valid, not "expected-single-idref".
        // Empty idrefs are a common, deliberate pattern in templated
        // markup (e.g. `aria-describedby={hasError ? errorId : ''}`), so
        // flagging them would be a false positive.
        if (v.length === 0) return { valid: true, reason: '' };
        const formatOk = !/\s/.test(v);
        if (!formatOk) return { valid: false, reason: 'expected-single-idref' };
        if (!idExists(v)) return { valid: false, reason: 'idref-not-found' };
        return { valid: true, reason: '' };
      }
      case 'idref-list': {
        const parts = v.split(/\s+/).filter(Boolean);
        // Same allowEmpty rationale as the 'idref' case above — an
        // empty idref-list value is valid, not "empty-idref-list".
        if (!parts.length) return { valid: true, reason: '' };
        // Only flag when NONE of the referenced ids resolve — a
        // partially-dangling list (some ids exist, some don't) is left
        // unflagged.
        if (!parts.some((p) => idExists(p)))
          return { valid: false, reason: 'idref-list-none-found' };
        return { valid: true, reason: '' };
      }
      case 'string':
      default:
        return { valid: true, reason: '' };
    }
  }

  function getRequiredAttrsForRole(role) {
    return REQUIRED_PROPS_BY_ROLE[lower(role)] ? REQUIRED_PROPS_BY_ROLE[lower(role)].slice(0) : [];
  }

  function getRequiredOwnedRoles(role) {
    return REQUIRED_OWNED_ROLES[lower(role)] ? REQUIRED_OWNED_ROLES[lower(role)].slice(0) : null;
  }

  function getRequiredContextRoles(role) {
    return Object.prototype.hasOwnProperty.call(REQUIRED_CONTEXT_ROLE, lower(role))
      ? REQUIRED_CONTEXT_ROLE[lower(role)].slice(0)
      : null;
  }

  // Resolves the ALLOWED_ROLES_BY_ELEMENT / NATIVE_ROLE_BY_ELEMENT_KEY
  // lookup key for an element, accounting for the small set of
  // attribute-conditioned entries. Returns '' when no key applies.
  function getElementRoleKey(el) {
    if (!isElement(el)) return '';
    const tag = lower(el.tagName || '');

    if (tag === 'a' || tag === 'area') {
      const href = getAttr(el, 'href');
      if (href != null && trim(href) !== '') return tag + '[href]';
      // <area> without href has its own permitted-roles entry (see
      // ALLOWED_ROLES_BY_ELEMENT above); hrefless <a> is left
      // unconstrained, since WHATWG/HTML-AAM sources disagree on how
      // restrictive it is.
      return tag === 'area' ? 'area' : '';
    }

    if (tag === 'section') {
      // <section>'s own implicit role is conditional: "region" when it
      // has an accessible name, "generic" when it doesn't (W3C
      // ARIA-in-HTML). So role="region" is a permitted no-op restatement
      // only when a name is present; on an unnamed <section> it's a real
      // violation ('region' isn't in <section>'s allowedRoles array).
      return hasAccessibleNameHint(el) ? 'section[named]' : 'section';
    }

    if (tag === 'header') {
      // <header>'s own implicit role is conditional: "banner" when
      // top-level (not nested inside sectioning content/<main>),
      // generic/null when nested — see hasLandmarkScopingAncestor above
      // (includeMain: true, since <header>'s exclusion list includes
      // <main>). So role="banner" is a permitted no-op restatement only at
      // the top level; 'banner' isn't in <header>'s allowedRoles array and
      // is reached only via the native-role match, same shape as
      // <section>'s 'region'.
      return hasLandmarkScopingAncestor(el, { includeMain: true }) ? 'header' : 'header[toplevel]';
    }

    if (tag === 'label') {
      // A <label> permits no explicit role at all when associated with
      // a labelable form control (via `for` or wrapping); otherwise any
      // role is permitted. Uses the native `.control` API (resolves both
      // `for` and wrapping association) instead of reimplementing that
      // lookup.
      let associated = false;
      try {
        associated = !!el.control;
      } catch {}
      return associated ? 'label[associated]' : '';
    }

    if (tag === 'img') {
      // Permitted roles depend on whether the img has a non-empty alt
      // (see ALLOWED_ROLES_BY_ELEMENT above): with alt text it may take a
      // small set of widget roles; without it, only presentation/none
      // (plus its own native img role, always allowed via the native-role
      // fallback below).
      const alt = getAttr(el, 'alt');
      return alt != null && trim(alt) !== '' ? 'img[alt]' : 'img';
    }

    if (tag === 'input') {
      const type = lower(getAttr(el, 'type') || 'text');
      if (type === 'checkbox') {
        // role="button" is only permitted on a checkbox when paired
        // with aria-pressed (W3C ARIA-in-HTML).
        let hasAriaPressed = false;
        try {
          hasAriaPressed = !!(el.hasAttribute && el.hasAttribute('aria-pressed'));
        } catch {}
        return hasAriaPressed ? 'input[type=checkbox][aria-pressed]' : 'input[type=checkbox]';
      }
      return 'input[type=' + type + ']';
    }

    if (tag === 'select') {
      // <select multiple> or <select size> 1>: native role becomes
      // listbox instead of combobox (WHATWG HTML-AAM), a distinct
      // permitted-roles entry — see ALLOWED_ROLES_BY_ELEMENT/
      // NATIVE_ROLE_BY_ELEMENT_KEY above.
      let isMultiSelect;
      try {
        isMultiSelect = !!(el.hasAttribute && el.hasAttribute('multiple'));
        if (!isMultiSelect) {
          const sizeAttr = getAttr(el, 'size');
          const size = sizeAttr != null ? parseInt(sizeAttr, 10) : NaN;
          isMultiSelect = Number.isFinite(size) && size > 1;
        }
      } catch {
        isMultiSelect = false;
      }
      return isMultiSelect ? 'select[multiple]' : 'select';
    }

    return tag;
  }

  function getAllowedRolesForElement(el) {
    const key = getElementRoleKey(el);
    if (!key) return undefined;
    return Object.prototype.hasOwnProperty.call(ALLOWED_ROLES_BY_ELEMENT, key)
      ? ALLOWED_ROLES_BY_ELEMENT[key]
      : undefined;
  }

  function getNativeRoleForElement(el) {
    const key = getElementRoleKey(el);
    if (!key) return '';
    return Object.prototype.hasOwnProperty.call(NATIVE_ROLE_BY_ELEMENT_KEY, key)
      ? NATIVE_ROLE_BY_ELEMENT_KEY[key]
      : '';
  }

  // Returns { constrained, allowed } — constrained=false means this
  // element/role combination has no asserted constraint (rule should
  // not flag it), matching the deliberately-scoped table above. An
  // element's own native/implicit role (see NATIVE_ROLE_BY_ELEMENT_KEY)
  // is always allowed, even when not separately listed.
  function isRoleAllowedOnElement(el, role) {
    const allowed = getAllowedRolesForElement(el);
    if (allowed === undefined) return { constrained: false, allowed: true };
    if (allowed === null) return { constrained: true, allowed: true };
    const r = lower(role);
    if (r && r === getNativeRoleForElement(el)) return { constrained: true, allowed: true };
    return { constrained: true, allowed: allowed.indexOf(r) !== -1 };
  }

  // Effective role for ownership/context matching only (aria-required-
  // children / aria-required-parent): explicit role wins when present;
  // otherwise falls back to the small NATIVE_CONTAINMENT_ROLE_BY_ELEMENT
  // map above. Not a general-purpose implicit-role resolver — scoped
  // deliberately narrow, see the table's header comment.
  //
  // The explicit role must be a real, valid concrete ARIA role to count:
  // an invalid/unrecognized role="" token (e.g. a library's own
  // non-standard "columngroup") is ignored by browsers/AT, which fall back
  // to the implicit role. Without this, a bogus role token wrongly
  // "blocks" the ancestor/descendant containment-role search instead of
  // being transparent to it — e.g. role="columnheader" cells inside a
  // role="columngroup" wrapper (not a real ARIA role) that itself sits
  // inside the real role="row" ancestor should still resolve to "row".
  function getContainmentRole(el) {
    const explicit = getExplicitRole(el);
    if (explicit && isValidConcreteRole(explicit)) return explicit;

    if (!isElement(el)) return '';
    const tag = lower(el.tagName || '');

    if (tag === 'input') {
      const type = lower(getAttr(el, 'type') || 'text');
      const key = 'input[type=' + type + ']';
      return Object.prototype.hasOwnProperty.call(NATIVE_CONTAINMENT_ROLE_BY_ELEMENT, key)
        ? NATIVE_CONTAINMENT_ROLE_BY_ELEMENT[key]
        : '';
    }

    return Object.prototype.hasOwnProperty.call(NATIVE_CONTAINMENT_ROLE_BY_ELEMENT, tag)
      ? NATIVE_CONTAINMENT_ROLE_BY_ELEMENT[tag]
      : '';
  }

  return {
    isValidAriaAttrName,
    getAttrValueType,
    validateAttrValue,
    getExplicitRole,
    getAllRoleTokens,
    isAbstractRole,
    isDeprecatedRole,
    isAuthorDiscouragedRole,
    isAuthorProhibitedRole,
    isDeprecatedAttr,
    getDeprecatedRoleGuidance,
    isKnownRole,
    isValidConcreteRole,
    getRequiredAttrsForRole,
    getRequiredOwnedRoles,
    getRequiredContextRoles,
    isRoleAllowedOnElement,
    getContainmentRole,

    // An element's own native/implicit ARIA-in-HTML role (see
    // NATIVE_ROLE_BY_ELEMENT_KEY above) — previously internal-only
    // (used by isRoleAllowedOnElement), now also re-exported for
    // aria-prohibited-attr's roleless-element branch, which needs to
    // tell "no role at all" (e.g. a bare <span>/<div>) apart from "has
    // a real implicit role" (e.g. <button>, <a href>) without
    // over-flagging the latter.
    getNativeRoleForElement,

    // Shared "does this element have a landmark-scoping ancestor"
    // primitive — see its own header comment above. Re-exported at
    // helpers' top level too (src/core/dom-helpers.js), matching
    // getLandmarkNameInfo's precedent, for the manual landmark-check
    // files that used to each carry their own (buggy, tag-only) copy.
    hasLandmarkScopingAncestor
  };
});

// Inlined from src/core/dom-helpers.js
const normalizeSelectorList = (function normalizeSelectorList(value) {
  if (!value) return [];
  if (Array.isArray(value))
    return value
      .map(String)
      .map((s) => s.trim())
      .filter(Boolean);
  if (typeof value === 'string') {
    // allow "#a,#b" or "#a, #b"
    return value
      .split(',')
      .map((s) => s.trim())
      .filter(Boolean);
  }
  return [];
});
const resolveContextRoots = (function resolveContextRoots(document, contextSelector) {
  const ctxSelector = Array.isArray(contextSelector)
    ? (() => {
        const list = contextSelector
          .map((s) => (typeof s === 'string' ? s.trim() : ''))
          .filter(Boolean);
        return list.length ? list : null;
      })()
    : typeof contextSelector === 'string' && contextSelector.trim()
      ? contextSelector.trim()
      : null;

  let roots = [];
  {
    const selectorList = Array.isArray(ctxSelector)
      ? ctxSelector
      : ctxSelector
        ? [ctxSelector]
        : [];
    const seen = new Set();
    for (const sel of selectorList) {
      let matches;
      try {
        matches = document.querySelectorAll(sel);
      } catch {
        matches = [];
      }
      for (const el of matches) {
        if (el && !seen.has(el)) {
          seen.add(el);
          roots.push(el);
        }
      }
    }
  }
  if (!roots.length) {
    const fallback = document.documentElement || document.body || document.querySelector('html');
    if (fallback) roots = [fallback];
  }

  return { ctxSelector, roots };
});
const createDomHelpers = (function createDomHelpers(opts) {
  // <generated:language-subtags>
  const LANGUAGE_SUBTAGS =
    'aa aaa aab aac aad aae aaf aag aah aai aak aal aam aan aao aap aaq aas aat aau aav aaw aax aaz ab aba abb abc abd abe abf abg abh abi abj abl abm abn abo abp abq abr abs abt abu abv abw abx aby abz aca acb acd ace acf ach aci ack acl acm acn acp acq acr acs act acu acv acw acx acy acz ada adb add ade adf adg adh adi adj adl adn ado adp adq adr ads adt adu adw adx ady adz ae aea aeb aec aed aee aek ael aem aen aeq aer aes aeu aew aey aez af afa afb afd afe afg afh afi afk afn afo afp afs aft afu afz aga agb agc agd age agf agg agh agi agj agk agl agm agn ago agp agq agr ags agt agu agv agw agx agy agz aha ahb ahg ahh ahi ahk ahl ahm ahn aho ahp ahr ahs aht aia aib aic aid aie aif aig aih aii aij aik ail aim ain aio aip aiq air ais ait aiw aix aiy aja ajg aji ajn ajp ajs ajt aju ajw ajz ak akb akc akd ake akf akg akh aki akj akk akl akm ako akp akq akr aks akt aku akv akw akx aky akz ala alc ald ale alf alg alh ali alj alk all alm aln alo alp alq alr als alt alu alv alw alx aly alz am ama amb amc ame amf amg ami amj amk aml amm amn amo amp amq amr ams amt amu amv amw amx amy amz an ana anb anc and ane anf ang anh ani anj ank anl anm ann ano anp anq anr ans ant anu anv anw anx any anz aoa aob aoc aod aoe aof aog aoh aoi aoj aok aol aom aon aor aos aot aou aox aoz apa apb apc apd ape apf apg aph api apj apk apl apm apn apo app apq apr aps apt apu apv apw apx apy apz aqa aqc aqd aqg aqk aql aqm aqn aqp aqr aqt aqz ar arb arc ard are arh ari arj ark arl arn aro arp arq arr ars art aru arv arw arx ary arz as asa asb asc asd ase asf asg ash asi asj ask asl asn aso asp asq asr ass ast asu asv asw asx asy asz ata atb atc atd ate atg ath ati atj atk atl atm atn ato atp atq atr ats att atu atv atw atx aty atz aua aub auc aud aue auf aug auh aui auj auk aul aum aun auo aup auq aur aus aut auu auw aux auy auz av avb avd avi avk avl avm avn avo avs avt avu avv awa awb awc awd awe awg awh awi awk awm awn awo awr aws awt awu awv aww awx awy axb axe axg axk axl axm axx ay aya ayb ayc ayd aye ayg ayh ayi ayk ayl ayn ayo ayp ayq ayr ays ayt ayu ayx ayy ayz az aza azb azc azd azg azj azm azn azo azt azz ba baa bab bac bad bae baf bag bah bai baj bal ban bao bap bar bas bat bau bav baw bax bay baz bba bbb bbc bbd bbe bbf bbg bbh bbi bbj bbk bbl bbm bbn bbo bbp bbq bbr bbs bbt bbu bbv bbw bbx bby bbz bca bcb bcc bcd bce bcf bcg bch bci bcj bck bcl bcm bcn bco bcp bcq bcr bcs bct bcu bcv bcw bcy bcz bda bdb bdc bdd bde bdf bdg bdh bdi bdj bdk bdl bdm bdn bdo bdp bdq bdr bds bdt bdu bdv bdw bdx bdy bdz be bea beb bec bed bee bef beg beh bei bej bek bem beo bep beq ber bes bet beu bev bew bex bey bez bfa bfb bfc bfd bfe bff bfg bfh bfi bfj bfk bfl bfm bfn bfo bfp bfq bfr bfs bft bfu bfw bfx bfy bfz bg bga bgb bgc bgd bge bgf bgg bgi bgj bgk bgl bgm bgn bgo bgp bgq bgr bgs bgt bgu bgv bgw bgx bgy bgz bh bha bhb bhc bhd bhe bhf bhg bhh bhi bhj bhk bhl bhm bhn bho bhp bhq bhr bhs bht bhu bhv bhw bhx bhy bhz bi bia bib bic bid bie bif big bij bik bil bim bin bio bip biq bir bit biu biv biw bix biy biz bja bjb bjc bjd bje bjf bjg bjh bji bjj bjk bjl bjm bjn bjo bjp bjq bjr bjs bjt bju bjv bjw bjx bjy bjz bka bkb bkc bkd bkf bkg bkh bki bkj bkk bkl bkm bkn bko bkp bkq bkr bks bkt bku bkv bkw bkx bky bkz bla blb blc bld ble blf blg blh bli blj blk bll blm bln blo blp blq blr bls blt blv blw blx bly blz bm bma bmb bmc bmd bme bmf bmg bmh bmi bmj bmk bml bmm bmn bmo bmp bmq bmr bms bmt bmu bmv bmw bmx bmy bmz bn bna bnb bnc bnd bne bnf bng bni bnj bnk bnl bnm bnn bno bnp bnq bnr bns bnt bnu bnv bnw bnx bny bnz bo boa bob boe bof bog boh boi boj bok bol bom bon boo bop boq bor bot bou bov bow box boy boz bpa bpb bpc bpd bpe bpg bph bpi bpj bpk bpl bpm bpn bpo bpp bpq bpr bps bpt bpu bpv bpw bpx bpy bpz bqa bqb bqc bqd bqf bqg bqh bqi bqj bqk bql bqm bqn bqo bqp bqq bqr bqs bqt bqu bqv bqw bqx bqy bqz br bra brb brc brd brf brg brh bri brj brk brl brm brn bro brp brq brr brs brt bru brv brw brx bry brz bs bsa bsb bsc bse bsf bsg bsh bsi bsj bsk bsl bsm bsn bso bsp bsq bsr bss bst bsu bsv bsw bsx bsy bta btb btc btd bte btf btg bth bti btj btk btl btm btn bto btp btq btr bts btt btu btv btw btx bty btz bua bub buc bud bue buf bug buh bui buj buk bum bun buo bup buq bus but buu buv buw bux buy buz bva bvb bvc bvd bve bvf bvg bvh bvi bvj bvk bvl bvm bvn bvo bvp bvq bvr bvt bvu bvv bvw bvx bvy bvz bwa bwb bwc bwd bwe bwf bwg bwh bwi bwj bwk bwl bwm bwn bwo bwp bwq bwr bws bwt bwu bww bwx bwy bwz bxa bxb bxc bxd bxe bxf bxg bxh bxi bxj bxk bxl bxm bxn bxo bxp bxq bxr bxs bxu bxv bxw bxx bxz bya byb byc byd bye byf byg byh byi byj byk byl bym byn byo byp byq byr bys byt byv byw byx byy byz bza bzb bzc bzd bze bzf bzg bzh bzi bzj bzk bzl bzm bzn bzo bzp bzq bzr bzs bzt bzu bzv bzw bzx bzy bzz ca caa cab cac cad cae caf cag cah cai caj cak cal cam can cao cap caq car cas cau cav caw cax cay caz cba cbb cbc cbd cbe cbg cbh cbi cbj cbk cbl cbn cbo cbq cbr cbs cbt cbu cbv cbw cby cca ccc ccd cce ccg cch ccj ccl ccm ccn cco ccp ccq ccr ccs cda cdc cdd cde cdf cdg cdh cdi cdj cdm cdn cdo cdr cds cdy cdz ce cea ceb ceg cek cel cen cet cey cfa cfd cfg cfm cga cgc cgg cgk ch chb chc chd chf chg chh chj chk chl chm chn cho chp chq chr cht chw chx chy chz cia cib cic cid cie cih cik cim cin cip cir ciw ciy cja cje cjh cji cjk cjm cjn cjo cjp cjr cjs cjv cjy cka ckb ckh ckl ckm ckn cko ckq ckr cks ckt cku ckv ckx cky ckz cla clc cld cle clh cli clj clk cll clm clo cls clt clu clw cly cma cmc cme cmg cmi cmk cml cmm cmn cmo cmr cms cmt cna cnb cnc cng cnh cni cnk cnl cno cnp cnq cnr cns cnt cnu cnw cnx co coa cob coc cod coe cof cog coh coj cok col com con coo cop coq cot cou cov cow cox coy coz cpa cpb cpc cpe cpf cpg cpi cpn cpo cpp cps cpu cpx cpy cqd cqu cr cra crb crc crd crf crg crh cri crj crk crl crm crn cro crp crq crr crs crt crv crw crx cry crz cs csa csb csc csd cse csf csg csh csi csj csk csl csm csn cso csp csq csr css cst csu csv csw csx csy csz cta ctc ctd cte ctg cth ctl ctm ctn cto ctp cts ctt ctu cty ctz cu cua cub cuc cug cuh cui cuj cuk cul cum cuo cup cuq cur cus cut cuu cuv cuw cux cuy cv cvg cvn cwa cwb cwd cwe cwg cwt cxh cy cya cyb cyo czh czk czn czo czt da daa dac dad dae daf dag dah dai daj dak dal dam dao dap daq dar das dau dav daw dax day daz dba dbb dbd dbe dbf dbg dbi dbj dbl dbm dbn dbo dbp dbq dbr dbt dbu dbv dbw dby dcc dcr dda ddd dde ddg ddi ddj ddn ddo ddr dds ddw de dec ded dee def deg deh dei dek del dem den dep deq der des dev dez dga dgb dgc dgd dge dgg dgh dgi dgk dgl dgn dgo dgr dgs dgt dgu dgw dgx dgz dha dhd dhg dhi dhl dhm dhn dho dhr dhs dhu dhv dhw dhx dia dib dic did dif dig dih dii dij dik dil dim din dio dip diq dir dis dit diu diw dix diy diz dja djb djc djd dje djf dji djj djk djl djm djn djo djr dju djw dka dkg dkk dkl dkr dks dkx dlg dlk dlm dln dma dmb dmc dmd dme dmf dmg dmk dml dmm dmn dmo dmr dms dmu dmv dmw dmx dmy dna dnd dne dng dni dnj dnk dnn dno dnr dnt dnu dnv dnw dny doa dob doc doe dof doh doi dok dol don doo dop doq dor dos dot dov dow dox doy doz dpp dra drb drc drd dre drg drh dri drl drn dro drq drr drs drt dru drw dry dsb dse dsh dsi dsk dsl dsn dso dsq dsz dta dtb dtd dth dti dtk dtm dtn dto dtp dtr dts dtt dtu dty dua dub duc dud due duf dug duh dui duj duk dul dum dun duo dup duq dur dus duu duv duw dux duy duz dv dva dwa dwk dwl dwr dws dwu dww dwy dwz dya dyb dyd dyg dyi dym dyn dyo dyr dyu dyy dz dza dzd dze dzg dzl dzn eaa ebc ebg ebk ebo ebr ebu ecr ecs ecy ee eee efa efe efi ega egl egm ego egx egy ehs ehu eip eit eiv eja eka ekc eke ekg eki ekk ekl ekm eko ekp ekr eky el ele elh eli elk elm elo elp elu elx ema emb eme emg emi emk emm emn emo emp emq ems emu emw emx emy emz en ena enb enc end enf enh enl enm enn eno enq enr enu env enw enx eo eot epi era erg erh eri erk ero err ers ert erw es ese esg esh esi esk esl esm esn eso esq ess esu esx esy et etb etc eth etn eto etr ets ett etu etx etz eu eud euq eve evh evn ewo ext eya eyo eza eze fa faa fab fad faf fag fah fai faj fak fal fam fan fap far fat fau fax fay faz fbl fcs fer ff ffi ffm fgr fi fia fie fif fil fip fir fit fiu fiw fj fkk fkv fla flh fli fll fln flr fly fmp fmu fnb fng fni fo fod foi fom fon for fos fox fpe fqs fr frc frd frk frm fro frp frq frr frs frt fse fsl fss fub fuc fud fue fuf fuh fui fuj fum fun fuq fur fut fuu fuv fuy fvr fwa fwe fy ga gaa gab gac gad gae gaf gag gah gai gaj gak gal gam gan gao gap gaq gar gas gat gau gav gaw gax gay gaz gba gbb gbc gbd gbe gbf gbg gbh gbi gbj gbk gbl gbm gbn gbo gbp gbq gbr gbs gbu gbv gbw gbx gby gbz gcc gcd gce gcf gcl gcn gcr gct gd gda gdb gdc gdd gde gdf gdg gdh gdi gdj gdk gdl gdm gdn gdo gdq gdr gds gdt gdu gdx gea geb gec ged gef geg geh gei gej gek gel gem geq ges gev gew gex gey gez gfk gft gfx gga ggb ggd gge ggg ggk ggl ggn ggo ggr ggt ggu ggw gha ghc ghe ghh ghk ghl ghn gho ghr ghs ght gia gib gic gid gie gig gih gii gil gim gin gio gip giq gir gis git giu giw gix giy giz gji gjk gjm gjn gjr gju gka gkd gke gkn gko gkp gku gl glb glc gld glh gli glj glk gll glo glr glu glw gly gma gmb gmd gme gmg gmh gml gmm gmn gmq gmr gmu gmv gmw gmx gmy gmz gn gna gnb gnc gnd gne gng gnh gni gnj gnk gnl gnm gnn gno gnq gnr gnt gnu gnw gnz goa gob goc god goe gof gog goh goi goj gok gol gom gon goo gop goq gor gos got gou gov gow gox goy goz gpa gpe gpn gqa gqi gqn gqr gqu gra grb grc grd grg grh gri grj grk grm gro grq grr grs grt gru grv grw grx gry grz gse gsg gsl gsm gsn gso gsp gss gsw gta gti gtu gu gua gub guc gud gue guf gug guh gui guk gul gum gun guo gup guq gur gus gut guu guv guw gux guz gv gva gvc gve gvf gvj gvl gvm gvn gvo gvp gvr gvs gvy gwa gwb gwc gwd gwe gwf gwg gwi gwj gwm gwn gwr gwt gwu gww gwx gxx gya gyb gyd gye gyf gyg gyi gyl gym gyn gyo gyr gyy gyz gza gzi gzn ha haa hab hac had hae haf hag hah hai haj hak hal ham han hao hap haq har has hav haw hax hay haz hba hbb hbn hbo hbu hca hch hdn hds hdy he hea hed heg heh hei hem hgm hgw hhi hhr hhy hi hia hib hid hif hig hih hii hij hik hil him hio hir hit hiw hix hji hka hke hkh hkk hkn hks hla hlb hld hle hlt hlu hma hmb hmc hmd hme hmf hmg hmh hmi hmj hmk hml hmm hmn hmp hmq hmr hms hmt hmu hmv hmw hmx hmy hmz hna hnd hne hng hnh hni hnj hnm hnn hno hns hnu ho hoa hob hoc hod hoe hoh hoi hoj hok hol hom hoo hop hor hos hot hov how hoy hoz hpo hps hr hra hrc hre hrk hrm hro hrp hrr hrt hru hrw hrx hrz hsb hsh hsl hsn hss ht hti hto hts htu htx hu hub huc hud hue huf hug huh hui huj huk hul hum huo hup huq hur hus hut huu huv huw hux huy huz hvc hve hvk hvn hvv hwa hwc hwo hy hya hyw hyx hz ia iai ian iap iar iba ibb ibd ibe ibg ibh ibi ibl ibm ibn ibr ibu iby ica ich icl icr id ida idb idc idd ide idi idr ids idt idu ie ifa ifb ife iff ifk ifm ifu ify ig igb ige igg igl igm ign igo igs igw ihb ihi ihp ihw ii iin iir ijc ije ijj ijn ijo ijs ik ike ikh iki ikk ikl iko ikp ikr iks ikt ikv ikw ikx ikz ila ilb ilg ili ilk ill ilm ilo ilp ils ilu ilv ilw ima ime imi iml imn imo imr ims imt imy in inb inc ine ing inh inj inl inm inn ino inp ins int inz io ior iou iow ipi ipo iqu iqw ira ire irh iri irk irn iro irr iru irx iry is isa isc isd ise isg ish isi isk ism isn iso isr ist isu isv it itb itc itd ite iti itk itl itm ito itr its itt itv itw itx ity itz iu ium ivb ivv iw iwk iwm iwo iws ixc ixl iya iyo iyx izh izi izm izr izz ja jaa jab jac jad jae jaf jah jaj jak jal jam jan jao jaq jar jas jat jau jax jay jaz jbe jbi jbj jbk jbm jbn jbo jbr jbt jbu jbw jcs jct jda jdg jdt jeb jee jeg jeh jei jek jel jen jer jet jeu jgb jge jgk jgo jhi jhs ji jia jib jic jid jie jig jih jii jil jim jio jiq jit jiu jiv jiy jje jjr jka jkm jko jkp jkr jks jku jle jls jma jmb jmc jmd jmi jml jmn jmr jms jmw jmx jna jnd jng jni jnj jnl jns job jod jog jor jos jow jpa jpr jpx jqr jra jrb jrr jrt jru jsl jua jub juc jud juh jui juk jul jum jun juo jup jur jus jut juu juw juy jv jvd jvn jw jwi jya jye jyy ka kaa kab kac kad kae kaf kag kah kai kaj kak kam kao kap kaq kar kav kaw kax kay kba kbb kbc kbd kbe kbf kbg kbh kbi kbj kbk kbl kbm kbn kbo kbp kbq kbr kbs kbt kbu kbv kbw kbx kby kbz kca kcb kcc kcd kce kcf kcg kch kci kcj kck kcl kcm kcn kco kcp kcq kcr kcs kct kcu kcv kcw kcx kcy kcz kda kdc kdd kde kdf kdg kdh kdi kdj kdk kdl kdm kdn kdo kdp kdq kdr kdt kdu kdv kdw kdx kdy kdz kea keb kec ked kee kef keg keh kei kej kek kel kem ken keo kep keq ker kes ket keu kev kew kex key kez kfa kfb kfc kfd kfe kff kfg kfh kfi kfj kfk kfl kfm kfn kfo kfp kfq kfr kfs kft kfu kfv kfw kfx kfy kfz kg kga kgb kgc kgd kge kgf kgg kgh kgi kgj kgk kgl kgm kgn kgo kgp kgq kgr kgs kgt kgu kgv kgw kgx kgy kha khb khc khd khe khf khg khh khi khj khk khl khn kho khp khq khr khs kht khu khv khw khx khy khz ki kia kib kic kid kie kif kig kih kii kij kil kim kio kip kiq kis kit kiu kiv kiw kix kiy kiz kj kja kjb kjc kjd kje kjf kjg kjh kji kjj kjk kjl kjm kjn kjo kjp kjq kjr kjs kjt kju kjv kjx kjy kjz kk kka kkb kkc kkd kke kkf kkg kkh kki kkj kkk kkl kkm kkn kko kkp kkq kkr kks kkt kku kkv kkw kkx kky kkz kl kla klb klc kld kle klf klg klh kli klj klk kll klm kln klo klp klq klr kls klt klu klv klw klx kly klz km kma kmb kmc kmd kme kmf kmg kmh kmi kmj kmk kml kmm kmn kmo kmp kmq kmr kms kmt kmu kmv kmw kmx kmy kmz kn kna knb knc knd kne knf kng kni knj knk knl knm knn kno knp knq knr kns knt knu knv knw knx kny knz ko koa koc kod koe kof kog koh koi koj kok kol koo kop koq kos kot kou kov kow kox koy koz kpa kpb kpc kpd kpe kpf kpg kph kpi kpj kpk kpl kpm kpn kpo kpp kpq kpr kps kpt kpu kpv kpw kpx kpy kpz kqa kqb kqc kqd kqe kqf kqg kqh kqi kqj kqk kql kqm kqn kqo kqp kqq kqr kqs kqt kqu kqv kqw kqx kqy kqz kr kra krb krc krd kre krf krh kri krj krk krl krm krn kro krp krr krs krt kru krv krw krx kry krz ks ksa ksb ksc ksd kse ksf ksg ksh ksi ksj ksk ksl ksm ksn kso ksp ksq ksr kss kst ksu ksv ksw ksx ksy ksz kta ktb ktc ktd kte ktf ktg kth kti ktj ktk ktl ktm ktn kto ktp ktq ktr kts ktt ktu ktv ktw ktx kty ktz ku kub kuc kud kue kuf kug kuh kui kuj kuk kul kum kun kuo kup kuq kus kut kuu kuv kuw kux kuy kuz kv kva kvb kvc kvd kve kvf kvg kvh kvi kvj kvk kvl kvm kvn kvo kvp kvq kvr kvs kvt kvu kvv kvw kvx kvy kvz kw kwa kwb kwc kwd kwe kwf kwg kwh kwi kwj kwk kwl kwm kwn kwo kwp kwq kwr kws kwt kwu kwv kww kwx kwy kwz kxa kxb kxc kxd kxe kxf kxh kxi kxj kxk kxl kxm kxn kxo kxp kxq kxr kxs kxt kxu kxv kxw kxx kxy kxz ky kya kyb kyc kyd kye kyf kyg kyh kyi kyj kyk kyl kym kyn kyo kyp kyq kyr kys kyt kyu kyv kyw kyx kyy kyz kza kzb kzc kzd kze kzf kzg kzh kzi kzj kzk kzl kzm kzn kzo kzp kzq kzr kzs kzt kzu kzv kzw kzx kzy kzz la laa lab lac lad lae laf lag lah lai laj lak lal lam lan lap laq lar las lau law lax lay laz lb lba lbb lbc lbe lbf lbg lbi lbj lbk lbl lbm lbn lbo lbq lbr lbs lbt lbu lbv lbw lbx lby lbz lcc lcd lce lcf lch lcl lcm lcp lcq lcs lda ldb ldd ldg ldh ldi ldj ldk ldl ldm ldn ldo ldp ldq lea leb lec led lee lef leg leh lei lej lek lel lem len leo lep leq ler les let leu lev lew lex ley lez lfa lfn lg lga lgb lgg lgh lgi lgk lgl lgm lgn lgo lgq lgr lgs lgt lgu lgz lha lhh lhi lhl lhm lhn lhp lhs lht lhu li lia lib lic lid lie lif lig lih lii lij lik lil lio lip liq lir lis liu liv liw lix liy liz lja lje lji ljl ljp ljw ljx lka lkb lkc lkd lke lkh lki lkj lkl lkm lkn lko lkr lks lkt lku lky lla llb llc lld lle llf llg llh lli llj llk lll llm lln llo llp llq lls llu llx lma lmb lmc lmd lme lmf lmg lmh lmi lmj lmk lml lmm lmn lmo lmp lmq lmr lmu lmv lmw lmx lmy lmz ln lna lnb lnd lng lnh lni lnj lnl lnm lnn lno lns lnu lnw lnz lo loa lob loc loe lof log loh loi loj lok lol lom lon loo lop loq lor los lot lou lov low lox loy loz lpa lpe lpn lpo lpx lqr lra lrc lre lrg lri lrk lrl lrm lrn lro lrr lrt lrv lrz lsa lsb lsc lsd lse lsg lsh lsi lsl lsm lsn lso lsp lsr lss lst lsv lsw lsy lt ltc ltg lth lti ltn lto lts ltu lu lua luc lud lue luf luh lui luj luk lul lum lun luo lup luq lur lus lut luu luv luw luy luz lv lva lvi lvk lvl lvs lvu lwa lwe lwg lwh lwl lwm lwo lws lwt lwu lww lxm lya lyg lyn lzh lzl lzn lzz maa mab mad mae maf mag mai maj mak mam man map maq mas mat mau mav maw max maz mba mbb mbc mbd mbe mbf mbh mbi mbj mbk mbl mbm mbn mbo mbp mbq mbr mbs mbt mbu mbv mbw mbx mby mbz mca mcb mcc mcd mce mcf mcg mch mci mcj mck mcl mcm mcn mco mcp mcq mcr mcs mct mcu mcv mcw mcx mcy mcz mda mdb mdc mdd mde mdf mdg mdh mdi mdj mdk mdl mdm mdn mdp mdq mdr mds mdt mdu mdv mdw mdx mdy mdz mea meb mec med mee mef meg meh mei mej mek mel mem men meo mep meq mer mes met meu mev mew mey mez mfa mfb mfc mfd mfe mff mfg mfh mfi mfj mfk mfl mfm mfn mfo mfp mfq mfr mfs mft mfu mfv mfw mfx mfy mfz mg mga mgb mgc mgd mge mgf mgg mgh mgi mgj mgk mgl mgm mgn mgo mgp mgq mgr mgs mgt mgu mgv mgw mgx mgy mgz mh mha mhb mhc mhd mhe mhf mhg mhh mhi mhj mhk mhl mhm mhn mho mhp mhq mhr mhs mht mhu mhw mhx mhy mhz mi mia mib mic mid mie mif mig mih mii mij mik mil mim min mio mip miq mir mis mit miu miw mix miy miz mja mjb mjc mjd mje mjg mjh mji mjj mjk mjl mjm mjn mjo mjp mjq mjr mjs mjt mju mjv mjw mjx mjy mjz mk mka mkb mkc mke mkf mkg mkh mki mkj mkk mkl mkm mkn mko mkp mkq mkr mks mkt mku mkv mkw mkx mky mkz ml mla mlb mlc mld mle mlf mlh mli mlj mlk mll mlm mln mlo mlp mlq mlr mls mlu mlv mlw mlx mlz mma mmb mmc mmd mme mmf mmg mmh mmi mmj mmk mml mmm mmn mmo mmp mmq mmr mmt mmu mmv mmw mmx mmy mmz mn mna mnb mnc mnd mne mnf mng mnh mni mnj mnk mnl mnm mnn mno mnp mnq mnr mns mnt mnu mnv mnw mnx mny mnz mo moa moc mod moe mof mog moh moi moj mok mom moo mop moq mor mos mot mou mov mow mox moy moz mpa mpb mpc mpd mpe mpg mph mpi mpj mpk mpl mpm mpn mpo mpp mpq mpr mps mpt mpu mpv mpw mpx mpy mpz mqa mqb mqc mqe mqf mqg mqh mqi mqj mqk mql mqm mqn mqo mqp mqq mqr mqs mqt mqu mqv mqw mqx mqy mqz mr mra mrb mrc mrd mre mrf mrg mrh mrj mrk mrl mrm mrn mro mrp mrq mrr mrs mrt mru mrv mrw mrx mry mrz ms msb msc msd mse msf msg msh msi msj msk msl msm msn mso msp msq msr mss mst msu msv msw msx msy msz mt mta mtb mtc mtd mte mtf mtg mth mti mtj mtk mtl mtm mtn mto mtp mtq mtr mts mtt mtu mtv mtw mtx mty mua mub muc mud mue mug muh mui muj muk mul mum mun muo mup muq mur mus mut muu muv mux muy muz mva mvb mvd mve mvf mvg mvh mvi mvk mvl mvm mvn mvo mvp mvq mvr mvs mvt mvu mvv mvw mvx mvy mvz mwa mwb mwc mwd mwe mwf mwg mwh mwi mwj mwk mwl mwm mwn mwo mwp mwq mwr mws mwt mwu mwv mww mwx mwy mwz mxa mxb mxc mxd mxe mxf mxg mxh mxi mxj mxk mxl mxm mxn mxo mxp mxq mxr mxs mxt mxu mxv mxw mxx mxy mxz my myb myc myd mye myf myg myh myi myj myk myl mym myn myo myp myq myr mys myt myu myv myw myx myy myz mza mzb mzc mzd mze mzg mzh mzi mzj mzk mzl mzm mzn mzo mzp mzq mzr mzs mzt mzu mzv mzw mzx mzy mzz na naa nab nac nad nae naf nag nah nai naj nak nal nam nan nao nap naq nar nas nat naw nax nay naz nb nba nbb nbc nbd nbe nbf nbg nbh nbi nbj nbk nbm nbn nbo nbp nbq nbr nbs nbt nbu nbv nbw nbx nby nca ncb ncc ncd nce ncf ncg nch nci ncj nck ncl ncm ncn nco ncp ncq ncr ncs nct ncu ncx ncz nd nda ndb ndc ndd ndf ndg ndh ndi ndj ndk ndl ndm ndn ndp ndq ndr nds ndt ndu ndv ndw ndx ndy ndz ne nea neb nec ned nee nef neg neh nei nej nek nem nen neo neq ner nes net neu nev new nex ney nez nfa nfd nfl nfr nfu ng nga ngb ngc ngd nge ngf ngg ngh ngi ngj ngk ngl ngm ngn ngo ngp ngq ngr ngs ngt ngu ngv ngw ngx ngy ngz nha nhb nhc nhd nhe nhf nhg nhh nhi nhk nhm nhn nho nhp nhq nhr nht nhu nhv nhw nhx nhy nhz nia nib nic nid nie nif nig nih nii nij nik nil nim nin nio niq nir nis nit niu niv niw nix niy niz nja njb njd njh nji njj njl njm njn njo njr njs njt nju njx njy njz nka nkb nkc nkd nke nkf nkg nkh nki nkj nkk nkm nkn nko nkp nkq nkr nks nkt nku nkv nkw nkx nkz nl nla nlc nle nlg nli nlj nlk nll nlm nln nlo nlq nlr nlu nlv nlw nlx nly nlz nma nmb nmc nmd nme nmf nmg nmh nmi nmj nmk nml nmm nmn nmo nmp nmq nmr nms nmt nmu nmv nmw nmx nmy nmz nn nna nnb nnc nnd nne nnf nng nnh nni nnj nnk nnl nnm nnn nnp nnq nnr nns nnt nnu nnv nnw nnx nny nnz no noa noc nod noe nof nog noh noi noj nok nol nom non noo nop noq nos not nou nov now noy noz npa npb npg nph npi npl npn npo nps npu npx npy nqg nqk nql nqm nqn nqo nqq nqt nqy nr nra nrb nrc nre nrf nrg nri nrk nrl nrm nrn nrp nrr nrt nru nrx nrz nsa nsb nsc nsd nse nsf nsg nsh nsi nsk nsl nsm nsn nso nsp nsq nsr nss nst nsu nsv nsw nsx nsy nsz ntd nte ntg nti ntj ntk ntm nto ntp ntr nts ntu ntw ntx nty ntz nua nub nuc nud nue nuf nug nuh nui nuj nuk nul num nun nuo nup nuq nur nus nut nuu nuv nuw nux nuy nuz nv nvh nvm nvo nwa nwb nwc nwe nwg nwi nwm nwo nwr nww nwx nwy nxa nxd nxe nxg nxi nxk nxl nxm nxn nxo nxq nxr nxu nxx ny nyb nyc nyd nye nyf nyg nyh nyi nyj nyk nyl nym nyn nyo nyp nyq nyr nys nyt nyu nyv nyw nyx nyy nza nzb nzd nzi nzk nzm nzr nzs nzu nzy nzz oaa oac oak oar oav obi obk obl obm obo obr obt obu oc oca och ocm oco ocu oda odk odt odu ofo ofs ofu ogb ogc oge ogg ogo ogu oht ohu oia oie oin oj ojb ojc ojg ojp ojs ojv ojw oka okb okc okd oke okg okh oki okj okk okl okm okn oko okr oks oku okv okx okz ola old ole olk olm olo olr olt olu om oma omb omc ome omg omi omk oml omn omo omp omq omr omt omu omv omw omx omy ona onb one ong oni onj onk onn ono onp onr ons ont onu onw onx ood oog oon oor oos opa opk opm opo opt opy or ora orc ore org orh orn oro orr ors ort oru orv orw orx ory orz os osa osc osi osn oso osp ost osu osx ota otb otd ote oti otk otl otm otn oto otq otr ots ott otu otw otx oty otz oua oub oue oui oum oun ovd owi owl oyb oyd oym oyy ozm pa paa pab pac pad pae paf pag pah pai pak pal pam pao pap paq par pas pat pau pav paw pax pay paz pbb pbc pbe pbf pbg pbh pbi pbl pbm pbn pbo pbp pbr pbs pbt pbu pbv pby pbz pca pcb pcc pcd pce pcf pcg pch pci pcj pck pcl pcm pcn pcp pcr pcw pda pdc pdi pdn pdo pdt pdu pea peb ped pee pef peg peh pei pej pek pel pem peo pep peq pes pev pex pey pez pfa pfe pfl pga pgd pgg pgi pgk pgl pgn pgs pgu pgy pgz pha phd phg phh phi phj phk phl phm phn pho phq phr pht phu phv phw pi pia pib pic pid pie pif pig pih pii pij pil pim pin pio pip pir pis pit piu piv piw pix piy piz pjt pka pkb pkc pkg pkh pkn pko pkp pkr pks pkt pku pl pla plb plc pld ple plf plg plh plj plk pll pln plo plp plq plr pls plt plu plv plw ply plz pma pmb pmc pmd pme pmf pmh pmi pmj pmk pml pmm pmn pmo pmq pmr pms pmt pmu pmw pmx pmy pmz pna pnb pnc pnd pne png pnh pni pnj pnk pnl pnm pnn pno pnp pnq pnr pns pnt pnu pnv pnw pnx pny pnz poc pod poe pof pog poh poi pok pom pon poo pop poq pos pot pov pow pox poy poz ppa ppe ppi ppk ppl ppm ppn ppo ppp ppq ppr pps ppt ppu pqa pqe pqm pqw pra prb prc prd pre prf prg prh pri prk prl prm prn pro prp prq prr prs prt pru prw prx pry prz ps psa psc psd pse psg psh psi psl psm psn pso psp psq psr pss pst psu psw psy pt pta pth pti ptn pto ptp ptq ptr ptt ptu ptv ptw pty pua pub puc pud pue puf pug pui puj puk pum puo pup puq pur put puu puw pux puy puz pwa pwb pwg pwi pwm pwn pwo pwr pww pxm pye pym pyn pys pyu pyx pyy pze pzh pzn qaa..qtz qu qua qub quc qud quf qug quh qui quk qul qum qun qup quq qur qus quv quw qux quy quz qva qvc qve qvh qvi qvj qvl qvm qvn qvo qvp qvs qvw qvy qvz qwa qwc qwe qwh qwm qws qwt qxa qxc qxh qxl qxn qxo qxp qxq qxr qxs qxt qxu qxw qya qyp raa rab rac rad raf rag rah rai raj rak ral ram ran rao rap raq rar ras rat rau rav raw rax ray raz rbb rbk rbl rbp rcf rdb rea reb ree reg rei rej rel rem ren rer res ret rey rga rge rgk rgn rgr rgs rgu rhg rhp ria rib rie rif ril rim rin rir rit riu rjg rji rjs rka rkb rkh rki rkm rkt rkw rm rma rmb rmc rmd rme rmf rmg rmh rmi rmk rml rmm rmn rmo rmp rmq rmr rms rmt rmu rmv rmw rmx rmy rmz rn rna rnb rnd rng rnl rnn rnp rnr rnw ro roa rob roc rod roe rof rog rol rom roo rop ror rou row rpn rpt rri rrm rro rrt rsb rsi rsk rsl rsm rsn rsw rtc rth rtm rts rtw ru rub ruc rue ruf rug ruh rui ruk ruo rup ruq rut ruu ruy ruz rw rwa rwk rwl rwm rwo rwr rxd rxw ryn rys ryu rzh sa saa sab sac sad sae saf sah sai saj sak sal sam sao sap saq sar sas sat sau sav saw sax say saz sba sbb sbc sbd sbe sbf sbg sbh sbi sbj sbk sbl sbm sbn sbo sbp sbq sbr sbs sbt sbu sbv sbw sbx sby sbz sc sca scb sce scf scg sch sci sck scl scn sco scp scq scs sct scu scv scw scx sd sda sdb sdc sde sdf sdg sdh sdj sdk sdl sdm sdn sdo sdp sdq sdr sds sdt sdu sdv sdx sdz se sea seb sec sed see sef seg seh sei sej sek sel sem sen seo sep seq ser ses set seu sev sew sey sez sfb sfe sfm sfs sfw sg sga sgb sgc sgd sge sgg sgh sgi sgj sgk sgl sgm sgn sgo sgp sgr sgs sgt sgu sgw sgx sgy sgz sh sha shb shc shd she shg shh shi shj shk shl shm shn sho shp shq shr shs sht shu shv shw shx shy shz si sia sib sid sie sif sig sih sii sij sik sil sim sio sip siq sir sis sit siu siv siw six siy siz sja sjb sjc sjd sje sjg sjk sjl sjm sjn sjo sjp sjr sjs sjt sju sjw sk ska skb skc skd ske skf skg skh ski skj skk skm skn sko skp skq skr sks skt sku skv skw skx sky skz sl sla slc sld sle slf slg slh sli slj sll slm sln slp slq slr sls slt slu slw slx sly slz sm sma smb smc smd smf smg smh smi smj smk sml smm smn smp smq smr sms smt smu smv smw smx smy smz sn snb snc sne snf sng snh sni snj snk snl snm snn sno snp snq snr sns snu snv snw snx sny snz so soa sob soc sod soe sog soh soi soj sok sol son soo sop soq sor sos sou sov sow sox soy soz spb spc spd spe spg spi spk spl spm spn spo spp spq spr sps spt spu spv spx spy sq sqa sqh sqj sqk sqm sqn sqo sqq sqr sqs sqt squ sqx sr sra srb src sre srf srg srh sri srk srl srm srn sro srq srr srs srt sru srv srw srx sry srz ss ssa ssb ssc ssd sse ssf ssg ssh ssi ssj ssk ssl ssm ssn sso ssp ssq ssr sss sst ssu ssv ssx ssy ssz st sta stb std ste stf stg sth sti stj stk stl stm stn sto stp stq str sts stt stu stv stw sty su sua sub suc sue sug sui suj suk sul sum suo suq sur sus sut suv suw sux suy suz sv sva svb svc sve svk svm svr svs svx sw swb swc swf swg swh swi swj swk swl swm swn swo swp swq swr sws swt swu swv sww swx swy sxb sxc sxe sxg sxk sxl sxm sxn sxo sxr sxs sxu sxw sya syb syc syd syi syk syl sym syn syo syr sys syw syx syy sza szb szc szd sze szg szl szn szp szs szv szw szy ta taa tab tac tad tae taf tag tai taj tak tal tan tao tap taq tar tas tau tav taw tax tay taz tba tbb tbc tbd tbe tbf tbg tbh tbi tbj tbk tbl tbm tbn tbo tbp tbq tbr tbs tbt tbu tbv tbw tbx tby tbz tca tcb tcc tcd tce tcf tcg tch tci tck tcl tcm tcn tco tcp tcq tcs tct tcu tcw tcx tcy tcz tda tdb tdc tdd tde tdf tdg tdh tdi tdj tdk tdl tdm tdn tdo tdq tdr tds tdt tdu tdv tdx tdy te tea teb tec ted tee tef teg teh tei tek tem ten teo tep teq ter tes tet teu tev tew tex tey tez tfi tfn tfo tfr tft tg tga tgb tgc tgd tge tgf tgg tgh tgi tgj tgn tgo tgp tgq tgr tgs tgt tgu tgv tgw tgx tgy tgz th thc thd the thf thh thi thk thl thm thn thp thq thr ths tht thu thv thw thx thy thz ti tia tic tid tie tif tig tih tii tij tik til tim tin tio tip tiq tis tit tiu tiv tiw tix tiy tiz tja tjg tji tjj tjl tjm tjn tjo tjp tjs tju tjw tk tka tkb tkd tke tkf tkg tkk tkl tkm tkn tkp tkq tkr tks tkt tku tkv tkw tkx tkz tl tla tlb tlc tld tlf tlg tlh tli tlj tlk tll tlm tln tlo tlp tlq tlr tls tlt tlu tlv tlw tlx tly tma tmb tmc tmd tme tmf tmg tmh tmi tmj tmk tml tmm tmn tmo tmp tmq tmr tms tmt tmu tmv tmw tmy tmz tn tna tnb tnc tnd tne tnf tng tnh tni tnk tnl tnm tnn tno tnp tnq tnr tns tnt tnu tnv tnw tnx tny tnz to tob toc tod toe tof tog toh toi toj tok tol tom too top toq tor tos tou tov tow tox toy toz tpa tpc tpe tpf tpg tpi tpj tpk tpl tpm tpn tpo tpp tpq tpr tpt tpu tpv tpw tpx tpy tpz tqb tql tqm tqn tqo tqp tqq tqr tqt tqu tqw tr tra trb trc trd tre trf trg trh tri trj trk trl trm trn tro trp trq trr trs trt tru trv trw trx try trz ts tsa tsb tsc tsd tse tsf tsg tsh tsi tsj tsk tsl tsm tsp tsq tsr tss tst tsu tsv tsw tsx tsy tsz tt tta ttb ttc ttd tte ttf ttg tth tti ttj ttk ttl ttm ttn tto ttp ttq ttr tts ttt ttu ttv ttw tty ttz tua tub tuc tud tue tuf tug tuh tui tuj tul tum tun tuo tup tuq tus tut tuu tuv tuw tux tuy tuz tva tvd tve tvi tvk tvl tvm tvn tvo tvs tvt tvu tvw tvx tvy tw twa twb twc twd twe twf twg twh twl twm twn two twp twq twr twt twu tww twx twy txa txb txc txe txg txh txi txj txm txn txo txq txr txs txt txu txx txy ty tya tye tyh tyi tyj tyl tyn typ tyr tys tyt tyu tyv tyx tyy tyz tza tzh tzj tzl tzm tzn tzo tzx uam uan uar uba ubi ubl ubr ubu uby uda ude udg udi udj udl udm udu ues ufi ug uga ugb uge ugh ugn ugo ugy uha uhn uis uiv uji uk uka ukg ukh uki ukk ukl ukp ukq uks uku ukv ukw uky ula ulb ulc ule ulf uli ulk ull ulm uln ulu ulw uly uma umb umc umd umg umi umm umn umo ump umr ums umu una und une ung uni unk unm unn unp unr unu unx unz uok uon upi upv ur ura urb urc ure urf urg urh uri urj urk url urm urn uro urp urr urt uru urv urw urx ury urz usa ush usi usk usp uss usu uta ute uth utp utr utu uum uun uur uuu uve uvh uvl uwa uya uz uzn uzs vaa vae vaf vag vah vai vaj val vam van vao vap var vas vau vav vay vbb vbk ve vec ved vel vem veo vep ver vgr vgt vi vic vid vif vig vil vin vis vit viv vjk vka vki vkj vkk vkl vkm vkn vko vkp vkt vku vkz vlp vls vma vmb vmc vmd vme vmf vmg vmh vmi vmj vmk vml vmm vmp vmq vmr vms vmu vmv vmw vmx vmy vmz vnk vnm vnp vo vor vot vra vro vrs vrt vsi vsl vsn vsv vto vum vun vut vwa wa waa wab wac wad wae waf wag wah wai waj wak wal wam wan wao wap waq war was wat wau wav waw wax way waz wba wbb wbe wbf wbh wbi wbj wbk wbl wbm wbp wbq wbr wbs wbt wbv wbw wca wci wdd wdg wdj wdk wdt wdu wdy wea wec wed weg weh wei wem wen weo wep wer wes wet weu wew wfg wga wgb wgg wgi wgo wgu wgw wgy wha whg whk whu wib wic wie wif wig wih wii wij wik wil wim win wir wit wiu wiv wiw wiy wja wji wka wkb wkd wkl wkr wku wkw wky wla wlc wle wlg wlh wli wlk wll wlm wlo wlr wls wlu wlv wlw wlx wly wma wmb wmc wmd wme wmg wmh wmi wmm wmn wmo wms wmt wmw wmx wnb wnc wnd wne wng wni wnk wnm wnn wno wnp wnu wnw wny wo woa wob woc wod woe wof wog woi wok wom won woo wor wos wow woy wpc wra wrb wrd wrg wrh wri wrk wrl wrm wrn wro wrp wrr wrs wru wrv wrw wrx wry wrz wsa wsg wsi wsk wsr wss wsu wsv wtb wtf wth wti wtk wtm wtw wua wub wud wuh wul wum wun wur wut wuu wuv wux wuy wwa wwb wwo wwr www wxa wxw wya wyb wyi wym wyn wyr wyy xaa xab xac xad xae xag xai xaj xak xal xam xan xao xap xaq xar xas xat xau xav xaw xay xba xbb xbc xbd xbe xbg xbi xbj xbm xbn xbo xbp xbr xbw xbx xby xcb xcc xce xcg xch xcl xcm xcn xco xcr xct xcu xcv xcw xcy xda xdc xdk xdm xdo xdq xdy xeb xed xeg xel xem xep xer xes xet xeu xfa xga xgb xgd xgf xgg xgi xgl xgm xgn xgr xgu xgw xh xha xhc xhd xhe xhm xhr xht xhu xhv xia xib xii xil xin xip xir xis xiv xiy xjb xjt xka xkb xkc xkd xke xkf xkg xkh xki xkj xkk xkl xkn xko xkp xkq xkr xks xkt xku xkv xkw xkx xky xkz xla xlb xlc xld xle xlg xli xln xlo xlp xls xlu xly xma xmb xmc xmd xme xmf xmg xmh xmj xmk xml xmm xmn xmo xmp xmq xmr xms xmt xmu xmv xmw xmx xmy xmz xna xnb xnd xng xnh xni xnj xnk xnm xnn xno xnq xnr xns xnt xnu xny xnz xoc xod xog xoi xok xom xon xoo xop xor xow xpa xpb xpc xpd xpe xpf xpg xph xpi xpj xpk xpl xpm xpn xpo xpp xpq xpr xps xpt xpu xpv xpw xpx xpy xpz xqa xqt xra xrb xrd xre xrg xri xrm xrn xrq xrr xrt xru xrw xsa xsb xsc xsd xse xsh xsi xsj xsl xsm xsn xso xsp xsq xsr xss xsu xsv xsy xta xtb xtc xtd xte xtg xth xti xtj xtl xtm xtn xto xtp xtq xtr xts xtt xtu xtv xtw xty xtz xua xub xud xug xuj xul xum xun xuo xup xur xut xuu xve xvi xvn xvo xvs xwa xwc xwd xwe xwg xwj xwk xwl xwo xwr xwt xww xxb xxk xxm xxr xxt xya xyb xyj xyk xyl xyt xyy xzh xzm xzp yaa yab yac yad yae yaf yag yah yai yaj yak yal yam yan yao yap yaq yar yas yat yau yav yaw yax yay yaz yba ybb ybd ybe ybh ybi ybj ybk ybl ybm ybn ybo ybx yby ych ycl ycn ycp ycr yda ydd yde ydg ydk yds yea yec yee yei yej yel yen yer yes yet yeu yev yey yga ygi ygl ygm ygp ygr ygs ygu ygw yha yhd yhl yhs yi yia yif yig yih yii yij yik yil yim yin yip yiq yir yis yit yiu yiv yix yiy yiz yka ykg ykh yki ykk ykl ykm ykn yko ykr ykt yku yky yla ylb yle ylg yli yll ylm yln ylo ylr ylu yly yma ymb ymc ymd yme ymg ymh ymi ymk yml ymm ymn ymo ymp ymq ymr yms ymt ymx ymz yna ynb ynd yne yng ynh ynk ynl ynn yno ynq yns ynu yo yob yog yoi yok yol yom yon yos yot yox yoy ypa ypb ypg yph ypk ypm ypn ypo ypp ypz yra yrb yre yri yrk yrl yrm yrn yro yrs yrw yry ysc ysd ysg ysl ysm ysn yso ysp ysr yss ysy yta ytl ytp ytw yty yua yub yuc yud yue yuf yug yui yuj yuk yul yum yun yup yuq yur yut yuu yuw yux yuy yuz yva yvt ywa ywg ywl ywn ywq ywr ywt ywu yww yxa yxg yxl yxm yxu yxy yyr yyu yyz yzg yzk za zaa zab zac zad zae zaf zag zah zai zaj zak zal zam zao zap zaq zar zas zat zau zav zaw zax zay zaz zba zbc zbe zbl zbt zbu zbw zca zcd zch zdj zea zeg zeh zem zen zga zgb zgh zgm zgn zgr zh zhb zhd zhi zhn zhw zhx zia zib zik zil zim zin zir ziw ziz zka zkb zkd zkg zkh zkk zkn zko zkp zkr zkt zku zkv zkz zla zle zlj zlm zln zlq zls zlu zlw zma zmb zmc zmd zme zmf zmg zmh zmi zmj zmk zml zmm zmn zmo zmp zmq zmr zms zmt zmu zmv zmw zmx zmy zmz zna znd zne zng znk zns zoc zoh zom zoo zoq zor zos zpa zpb zpc zpd zpe zpf zpg zph zpi zpj zpk zpl zpm zpn zpo zpp zpq zpr zps zpt zpu zpv zpw zpx zpy zpz zqe zra zrg zrn zro zrp zrs zsa zsk zsl zsm zsr zsu zte ztg ztl ztm ztn ztp ztq zts ztt ztu ztx zty zu zua zuh zum zun zuy zwa zxx zyb zyg zyj zyn zyp zza zzj';
  // </generated:language-subtags>

  // BCP 47 well-formedness plus a registry check on the primary subtag. Shape
  // alone accepts "eng" and "em-US", which look like language tags but are not
  // registered: the IANA registry lists a three-letter subtag only when no
  // two-letter one exists, so "en" is registered and "eng" is not.
  let __languageSubtagSet = null;
  function isRegisteredLanguageSubtag(subtag) {
    if (!__languageSubtagSet) __languageSubtagSet = new Set(LANGUAGE_SUBTAGS.split(' '));
    return __languageSubtagSet.has(String(subtag || '').toLowerCase());
  }

  function isValidLanguageTag(value) {
    const raw = String(value == null ? '' : value).trim();
    if (!raw) return false;
    if (!/^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{1,8})*$/.test(raw)) return false;
    return isRegisteredLanguageSubtag(raw.split('-')[0]);
  }

  const document = opts && opts.document ? opts.document : null;
  const window = opts && opts.window ? opts.window : null;
  // Some engine paths may not pass opts.window; recover it from document when possible.
  const realmWindow = window || (document && document.defaultView) || null;
  // opts.root accepts either a single element (back-compat -- every
  // existing call site, including every test, passes one) or an array of
  // elements (multi-region contextSelector support, dom-runner.js). Every
  // internal consumer below works off `roots` (always an array, possibly
  // empty) rather than assuming a single element.
  const roots = (() => {
    const r = opts && opts.root;
    if (Array.isArray(r)) return r.filter((x) => x && typeof x === 'object');
    if (r && typeof r === 'object') return [r];
    return [];
  })();
  // Default on: opt OUT with `includeShadowDom: false`, not opt in.
  const includeShadowDom = !(opts && opts.includeShadowDom === false);
  // Default off: by default, helper queries skip structurally/CSS-hidden
  // subtrees (display:none, [hidden], closed <details>, etc.). Callers can
  // opt out with includeHiddenElements:true.
  const includeHiddenElements = !!(opts && opts.includeHiddenElements === true);
  const excludeSelectors = Array.isArray(opts && opts.excludeSelectors)
    ? opts.excludeSelectors
    : [];
  // Default off: explicit opt-in for "this scan target was never meant to
  // represent a real page" (e.g. a raw component fragment parsed on its
  // own), regardless of whether document.documentElement happens to be in
  // scope. See isWholeDocumentScope() below.
  const fragment = !!(opts && opts.fragment === true);

  // Rule-scoped excludes (engineOptions.rules[ruleId].excludeSelectors), set
  // by dom-runner.js immediately before invoking each rule's applicability/
  // run function via __setActiveRuleExcludeSelectors(). Safe as mutable
  // closure state because rule execution is synchronous and single-rule-
  // at-a-time: exactly one rule's excludes are ever "active" at once.
  var __activeRuleExcludeSelectors = [];

  function __getEffectiveExcludeSelectors() {
    return __activeRuleExcludeSelectors.length
      ? excludeSelectors.concat(__activeRuleExcludeSelectors)
      : excludeSelectors;
  }

  function __setActiveRuleExcludeSelectors(list) {
    __activeRuleExcludeSelectors = normalizeSelectorList(list);
  }

  // Selector-related caches (selector uniqueness index, per-element built
  // selector strings) depend on includeShadowDom/the effective exclude
  // list, since those change which elements are considered when checking
  // uniqueness. The underlying storage is shared across createDomHelpers()
  // calls on the same window/document (see __domSharedCache below), so a
  // run -- or a rule with its own rule-scoped excludes -- must not
  // read/write another run/rule's cached selectors. This key partitions
  // those caches per effective option set; recomputed per call (not a
  // constant) since the effective list changes as the active rule changes.
  function __getSelectorOptsKey() {
    return (
      (includeShadowDom ? 'sd1' : 'sd0') +
      '|' +
      __getEffectiveExcludeSelectors().slice().sort().join(',')
    );
  }

  // -------------------------------------------------------------------------
  // Per-run shared caches (DOM helpers)
  // Stored on the realm window when possible so multiple helper instances
  // within the same run share caches deterministically.
  // -------------------------------------------------------------------------
  var __domSharedCache = {};
  var __selectorCache = null;
  var __outerHtmlCache = null;
  var __idLookupDocCache = null; // Map<string, Element|null>
  var __idLookupRootCache = null; // Map<string, Element|null>
  var __idRefCacheByRoot = null; // WeakMap<object, Map<string, {refs, missing, flags, partsLen}>>
  var __idRefReverseIndexByScope = null; // WeakMap<object, Map<string, Set<Element>>>
  var __uniqIndexByScope = null; // WeakMap<object, object> (selector uniqueness index per scope)
  var __shadowRootsByRoot = null; // WeakMap<object, Array<object>> (cached open shadow roots per root)

  // Shared recursion-depth guard across the mutually-recursive naming
  // functions (computeIdRefTargetTextAlternative <-> getContentNameInfo <->
  // getAccessibleNameInfo, via aria-labelledby targets that themselves
  // contain elements with their own aria-labelledby). Each per-call
  // `visited` Set only guards against cycles *within* a single top-level
  // getTextFromIdRefs() invocation; a cross-function hop (e.g. resolving a
  // labelledby target's content, which contains a descendant with its own
  // aria-labelledby) starts a *fresh* visited Set and would defeat that
  // guard on a genuine circular reference. This counter bounds the total
  // combined call depth regardless of which function is on the stack.
  var __nameComputationDepth = 0;
  var __NAME_COMPUTATION_MAX_DEPTH = 40;

  // -------------------------------------------------------------------------
  // Optional per-run performance counters (debug/benchmark only)
  // -------------------------------------------------------------------------
  const __perfEnabled = !!(opts && opts.perfStats);
  const __perf = __perfEnabled ? { enabled: true, counters: Object.create(null) } : null;

  function __perfInc(key, n) {
    if (!__perfEnabled || !__perf) return;
    const k = String(key);
    const add = n == null ? 1 : Number(n) || 0;
    __perf.counters[k] = (__perf.counters[k] || 0) + add;
  }

  function getPerfStats() {
    if (!__perfEnabled || !__perf) return { enabled: false, counters: {} };
    // Return a shallow copy to prevent accidental mutation by callers
    return { enabled: true, counters: { ...__perf.counters } };
  }

  function resetPerfStats() {
    if (!__perfEnabled || !__perf) return;
    __perf.counters = Object.create(null);
  }

  // -------------------------------------------------------------------------
  // Shared escaping helpers (reduce per-call allocations, deterministic)
  // -------------------------------------------------------------------------
  const __w = realmWindow || window;
  const __cssEscapeSafe = (s) => {
    try {
      return __w && __w.CSS && typeof __w.CSS.escape === 'function'
        ? __w.CSS.escape(String(s))
        : String(s);
    } catch {
      return String(s);
    }
  };
  // Spec-accurate CSS.escape() fallback (CSSOM "serialize an identifier"
  // algorithm) for environments without a native window.CSS.escape, such
  // as jsdom. Must handle CSS identifiers that start with a digit or a
  // hyphen+digit (e.g. UUID-style element IDs): an unescaped leading digit
  // makes the selector fragment invalid CSS, so buildSelectorUncached's
  // el.matches(candidate) verification throws (silently caught) and the
  // selector degrades to a bare tag name, losing all positional/uniqueness
  // information.
  function __cssEscapeIdentFallback(value) {
    const string = String(value);
    const length = string.length;
    const firstCodeUnit = string.charCodeAt(0);
    if (length === 1 && firstCodeUnit === 0x002d) return '\\' + string;
    let result = '';
    for (let index = 0; index < length; index++) {
      const codeUnit = string.charCodeAt(index);
      if (codeUnit === 0x0000) {
        result += '\uFFFD';
        continue;
      }
      if (
        (codeUnit >= 0x0001 && codeUnit <= 0x001f) ||
        codeUnit === 0x007f ||
        (index === 0 && codeUnit >= 0x0030 && codeUnit <= 0x0039) ||
        (index === 1 && codeUnit >= 0x0030 && codeUnit <= 0x0039 && firstCodeUnit === 0x002d)
      ) {
        result += '\\' + codeUnit.toString(16) + ' ';
        continue;
      }
      if (
        codeUnit >= 0x0080 ||
        codeUnit === 0x002d ||
        codeUnit === 0x005f ||
        (codeUnit >= 0x0030 && codeUnit <= 0x0039) ||
        (codeUnit >= 0x0041 && codeUnit <= 0x005a) ||
        (codeUnit >= 0x0061 && codeUnit <= 0x007a)
      ) {
        result += string.charAt(index);
        continue;
      }
      result += '\\' + string.charAt(index);
    }
    return result;
  }
  const __cssEscapeIdent = (s) => {
    try {
      if (__w && __w.CSS && typeof __w.CSS.escape === 'function') return __w.CSS.escape(String(s));
    } catch {}
    return __cssEscapeIdentFallback(s);
  };
  const __escapeAttrValue = (s) => String(s).replace(/\\/g, '\\\\').replace(/"/g, '\\"');

  // --- eligibility utilities ---
  const isElement = (n) => !!n && n.nodeType === 1;
  const computedStyle = (el) => {
    // Per-run memoization scoped by *helper scope* (root/document), to ensure
    // style caching does not bleed across helper instances with different roots.
    // This aligns with eligibility cache scoping semantics locked by checks.
    const scope = __getScopeObj();

    let map = null;

    try {
      if (__computedStyleCacheByScope && scope && el && typeof el === 'object') {
        map = __computedStyleCacheByScope.get(scope) || null;
        if (map && map.has(el)) {
          __perfInc('computedStyle.hit');
          const c = map.get(el);
          return c && typeof c === 'object' ? c : {};
        }
      }
    } catch {
      /* ignore */
    }

    __perfInc('computedStyle.miss');
    let cs;
    try {
      const w = realmWindow || window;
      cs = w && w.getComputedStyle ? w.getComputedStyle(el) : (el && el.style) || {};
    } catch {
      cs = {};
    }

    try {
      if (__computedStyleCacheByScope && scope && el && typeof el === 'object') {
        if (!map) {
          map = __computedStyleCacheByScope.get(scope) || null;
          if (!map) {
            map = new WeakMap();
            __computedStyleCacheByScope.set(scope, map);
          }
        }
        map.set(el, cs);
      }
    } catch {
      __perfInc('computedStyle.nocache');
    }

    return cs && typeof cs === 'object' ? cs : {};
  };

  const getOpenModalDialogs = () => {
    // Per-run memoization of open modal dialogs (document-scoped).
    // Safe under engine constraints (no DOM mutation during a run); deterministic.
    if (!document || !document.querySelectorAll) return [];
    if (!__openModalDialogsByDoc) {
      __perfInc('modalDialogs.nocache');
    }
    try {
      if (__openModalDialogsByDoc) {
        const cached = __openModalDialogsByDoc.get(document);
        if (cached) {
          __perfInc('modalDialogs.hit');
          return cached;
        }
        __perfInc('modalDialogs.miss');
      }
    } catch {
      __perfInc('modalDialogs.nocache');
    }

    let list = [];
    try {
      const nl = document.querySelectorAll('dialog[open][aria-modal="true"]');
      // Preserve document order, avoid Array.from allocation where possible.
      for (const el of nl) list.push(el);
    } catch {
      list = [];
    }

    try {
      if (__openModalDialogsByDoc) __openModalDialogsByDoc.set(document, list);
    } catch {
      /* ignore */
    }

    return list;
  };

  // Flat-tree (composed) parent: a distributed/slotted node's real rendered
  // parent is the <slot> it's assigned to, NOT its own light-DOM parentNode
  // (parentNode is unaffected by slot assignment and stays truthy for any
  // normally-connected slotted element — checking it first, as an earlier
  // version of this helper did, means the assignedSlot branch never fires
  // for the common case of a real, connected slotted child, silently
  // treating it as if it rendered under its light-DOM parent instead of
  // the shadow-tree container it's actually distributed into). assignedSlot
  // must be checked first; parentNode only applies to nodes that aren't
  // currently distributed through a slot. Once climbing reaches a
  // ShadowRoot itself (parentNode is null there), `.host` is the shadow
  // host element directly — NOT `getRootNode({composed:true})`, which
  // resolves all the way to the top-level document, skipping past the
  // immediate shadow boundary this function is trying to climb out of one
  // level at a time.
  const composedParent = (n) => {
    if (!n) return null;
    if (n.assignedSlot) return n.assignedSlot;
    if (n.parentNode) return n.parentNode;
    return n.host || null;
  };
  const ancestorsIncludingSelf = (n) => {
    if (!n) return [];
    // Cache ancestor chains per node, per run, to avoid repeated composed-parent walks.
    // Deterministic: purely memoized within the current run, no cross-run persistence.
    try {
      if (
        __ancestorsIncludingSelfCache &&
        typeof __ancestorsIncludingSelfCache.get === 'function'
      ) {
        const cached = __ancestorsIncludingSelfCache.get(n);
        if (cached) {
          __perfInc('ancestorsIncludingSelf.hit');
          return cached;
        }
        __perfInc('ancestorsIncludingSelf.miss');
        const out = [];
        let cur = n,
          guard = 0;
        while (cur && guard++ < 200) {
          out.push(cur);
          cur = composedParent(cur);
        }
        __ancestorsIncludingSelfCache.set(n, out);
        return out;
      }
    } catch {
      /* fall through */
    }

    __perfInc('ancestorsIncludingSelf.nocache');
    const out = [];
    let cur = n,
      guard = 0;
    while (cur && guard++ < 200) {
      out.push(cur);
      cur = composedParent(cur);
    }
    return out;
  };

  function getClosestMap(el) {
    try {
      if (!isElement(el)) return null;
      return el.closest ? el.closest('map') : null;
    } catch {
      return null;
    }
  }

  function hasBlockingInert(node) {
    // Default behavior: inert anywhere in ancestorsIncludingSelf blocks.
    if (!isElement(node)) return false;

    const tag = (node.tagName || '').toLowerCase();
    const isArea = tag === 'area';

    let mapEl = null;
    if (isArea) mapEl = getClosestMap(node);

    const chain = ancestorsIncludingSelf(node);

    for (const a of chain) {
      if (!isElement(a)) continue;

      // Exception: for <area>, inert on itself or on its <map> does NOT block
      if (isArea) {
        if (a === node) continue; // ignore <area inert>
        if (mapEl && a === mapEl) continue; // ignore <map inert>
      }

      if (a.hasAttribute && a.hasAttribute('inert')) return true;
    }
    return false;
  }

  const trim = (v) => (v == null ? '' : String(v)).trim();

  const getAttr = (el, name) => {
    try {
      return el && el.getAttribute ? el.getAttribute(name) : null;
    } catch {
      return null;
    }
  };

  function parseTabIndex(el) {
    const raw = getAttr(el, 'tabindex');
    const t = trim(raw);
    if (raw == null || t === '') return { has: false, value: null, valid: false };
    const n = Number(t);
    if (Number.isNaN(n)) return { has: true, value: null, valid: false };
    return { has: true, value: n, valid: true };
  }

  function getPlatformFocusability(el) {
    // Per-run memoization (WeakMap<Element, Result>)
    try {
      if (__focusabilityCache && el && typeof el === 'object' && __focusabilityCache.has(el)) {
        __perfInc('focusability.hit');
        const c = __focusabilityCache.get(el);
        if (c && typeof c === 'object') {
          return {
            focusable: !!c.focusable,
            tabbable: !!c.tabbable,
            mechanism: c.mechanism || 'none',
            flags: Array.isArray(c.flags) ? c.flags.slice(0) : []
          };
        }
      }
    } catch {}

    __perfInc('focusability.miss');
    let result;

    if (!isElement(el)) {
      result = { focusable: false, tabbable: false, mechanism: 'none', flags: ['notElement'] };
    } else if (hasBlockingInert(el)) {
      result = { focusable: false, tabbable: false, mechanism: 'none', flags: ['inert'] };
    } else {
      const flags = [];
      const disabled = !!(el.matches && el.matches(':disabled'));
      if (disabled) {
        result = { focusable: false, tabbable: false, mechanism: 'disabled', flags: ['disabled'] };
      } else {
        const ti = parseTabIndex(el);
        if (ti.has) {
          if (!ti.valid)
            result = {
              focusable: false,
              tabbable: false,
              mechanism: 'tabindex',
              flags: ['tabindex-invalid']
            };
          else if (ti.value < 0)
            result = {
              focusable: true,
              tabbable: false,
              mechanism: 'tabindex',
              flags: ['tabindex-negative']
            };
          else
            result = {
              focusable: true,
              tabbable: true,
              mechanism: 'tabindex',
              flags: ['tabindex-nonnegative']
            };
        } else {
          // native focusability
          const native = isPlatformFocusable(el); // uses your existing boolean logic
          if (native) result = { focusable: true, tabbable: true, mechanism: 'native', flags };
          else result = { focusable: false, tabbable: false, mechanism: 'none', flags };
        }
      }
    }

    try {
      if (__focusabilityCache && el && typeof el === 'object') {
        __focusabilityCache.set(el, {
          focusable: !!result.focusable,
          tabbable: !!result.tabbable,
          mechanism: result.mechanism || 'none',
          flags: Array.isArray(result.flags) ? result.flags.slice(0) : []
        });
      }
    } catch {}

    return {
      focusable: !!result.focusable,
      tabbable: !!result.tabbable,
      mechanism: result.mechanism || 'none',
      flags: Array.isArray(result.flags) ? result.flags.slice(0) : []
    };
  }

  // --- attribute ---
  function getAttributeInfo(el, attr) {
    const flags = [];
    if (!isElement(el))
      return { present: false, value: '', mechanism: 'unsupported', flags: ['notElement'] };

    const attrValue = trim(getAttr(el, attr));
    if (!attrValue) return { present: false, value: '', mechanism: attr, flags: ['empty'] };

    return { present: true, value: attrValue, mechanism: attr, flags };
  }

  // --- ARIA name primitives (reusable across checks) ---
  function getAriaLabelInfo(el) {
    const flags = [];
    if (!isElement(el))
      return { present: false, value: '', mechanism: 'unsupported', flags: ['notElement'] };

    const ariaLabel = trim(getAttr(el, 'aria-label'));
    if (!ariaLabel) return { present: false, value: '', mechanism: 'aria-label', flags: ['empty'] };

    return { present: true, value: ariaLabel, mechanism: 'aria-label', flags };
  }

  function getAriaLabelledByInfo(el, _ctx, opts) {
    const flags = [];
    if (!isElement(el))
      return { present: false, value: '', mechanism: 'unsupported', flags: ['notElement'] };

    const ariaLabelledBy = trim(getAttr(el, 'aria-labelledby'));
    if (!ariaLabelledBy)
      return { present: false, value: '', mechanism: 'aria-labelledby', flags: ['missing'] };

    const t = getTextFromIdRefs(ariaLabelledBy, _ctx, opts);
    for (const f of t.flags) flags.push(f);

    if (!t.text) flags.push('empty');

    return {
      present: !!t.text,
      value: t.text || '',
      mechanism: 'aria-labelledby',
      refsCount: t.refsCount,
      missing: t.missing ? t.missing.slice(0) : [],
      flags
    };
  }

  /**
   * getAriaNameInfo: ARIA-only name, with correct precedence.
   * aria-labelledby (if non-empty) wins over aria-label.
   */
  function getAriaNameInfo(el, _ctx, opts) {
    const flags = [];
    if (!isElement(el))
      return { present: false, value: '', mechanism: 'unsupported', flags: ['notElement'] };

    const lb = getAriaLabelledByInfo(el, _ctx, opts);
    if (lb.present && lb.value)
      return {
        present: true,
        value: lb.value,
        mechanism: 'aria-labelledby',
        flags: flags.concat(lb.flags || [])
      };

    const al = getAriaLabelInfo(el);
    if (al.present && al.value)
      return {
        present: true,
        value: al.value,
        mechanism: 'aria-label',
        flags: flags.concat(al.flags || [])
      };

    // If aria-labelledby existed but was empty/unresolvable, preserve that info in flags.
    if (trim(getAttr(el, 'aria-labelledby'))) flags.push('aria-labelledby-empty-or-unresolvable');
    if (getAttr(el, 'aria-label') != null && !trim(getAttr(el, 'aria-label')))
      flags.push('aria-label-empty');

    return { present: false, value: '', mechanism: 'none', flags };
  }

  // Landmark-role naming (nav/main/region/banner/contentinfo/etc.): these
  // roles don't derive a name from content (unlike a button/link), so per
  // the accname spec their only sources are aria-label, aria-labelledby,
  // then a title-attribute fallback. Shared by the 7 landmark rule files
  // (landmark-unique, landmark-no-duplicate-banner/-contentinfo,
  // landmark-banner/-main/-contentinfo-is-top-level, region); title must
  // be included, otherwise e.g. two <nav>s distinguished only by
  // title="navigation" are both seen as unnamed and flagged as duplicates.
  function getLandmarkNameInfo(el, ctx) {
    if (!isElement(el))
      return { present: false, value: '', mechanism: 'unsupported', flags: ['notElement'] };

    const aria = getAriaNameInfo(el, ctx);
    if (aria.present && aria.value) return aria;

    const title = trim(getAttr(el, 'title'));
    if (title) return { present: true, value: title, mechanism: 'title', flags: aria.flags || [] };

    return {
      present: false,
      value: '',
      mechanism: 'none',
      flags: (aria.flags || []).concat(
        title === '' && getAttr(el, 'title') != null ? ['title-empty'] : []
      )
    };
  }

  const lower = (v) => trim(v).toLowerCase();

  const safeDocGetById = (id) => {
    const key = trim(id);
    if (!key) return null;

    // Shared cache (per run)
    try {
      if (__idLookupDocCache && __idLookupDocCache.has(key)) {
        __perfInc('idLookup.doc.hit');
        return __idLookupDocCache.get(key) || null;
      }
    } catch {}

    __perfInc('idLookup.doc.miss');
    let el = null;
    try {
      if (document && document.getElementById) el = document.getElementById(key);
    } catch {
      el = null;
    }

    try {
      if (__idLookupDocCache) __idLookupDocCache.set(key, el || null);
    } catch {}

    return el || null;
  };

  const safeRootQueryById = (id) => {
    // Best-effort for cases where root is not the document (e.g. shadow root-like, fragment roots).
    // Note: IDs are document-global in HTML, but test harnesses may use scoped roots.
    // With multi-region contextSelector, tries each root in turn and
    // returns the first match -- IDs are meant to be document-unique
    // anyway, so at most one root should ever actually contain it.
    const key = trim(id);
    if (!key) return null;
    if (!roots.length) return null;

    try {
      const cacheKey = '#' + key;
      if (__idLookupRootCache && __idLookupRootCache.has(cacheKey)) {
        __perfInc('idLookup.root.hit');
        return __idLookupRootCache.get(cacheKey) || null;
      }
    } catch {}

    __perfInc('idLookup.root.miss');
    let el = null;
    for (const r of roots) {
      if (!r || !r.querySelector) continue;
      try {
        el = r.querySelector('#' + key);
      } catch {
        el = null;
      }
      if (el) break;
    }

    try {
      const cacheKey = '#' + key;
      if (__idLookupRootCache) __idLookupRootCache.set(cacheKey, el || null);
    } catch {}

    return el || null;
  };

  function inClosedDetailsContent(node) {
    try {
      if (!isElement(node)) return false;
      const summary = node.closest && node.closest('summary');
      if (summary && summary.contains(node)) return false;
      const details = node.closest && node.closest('details');
      if (details && !details.hasAttribute('open')) return true;
    } catch {}
    return false;
  }

  function isPlatformFocusable(el) {
    if (!isElement(el) || hasBlockingInert(el)) return false;
    const tag = (el.tagName || '').toLowerCase();
    const type = (el.getAttribute && (el.getAttribute('type') || '').toLowerCase()) || '';
    const disabled = !!(el.matches && el.matches(':disabled'));
    if (disabled) return false;

    if (tag === 'a') {
      const href = el.getAttribute && el.getAttribute('href');
      if (href && href.trim()) return true;
    }
    if (tag === 'area') {
      // Engine policy: treat <area> as focusable when it's part of a *used* image map.
      const map = getClosestMap(el);
      if (map) {
        const rawName = (
          map.getAttribute &&
          (map.getAttribute('name') || map.getAttribute('id') || '')
        ).trim();
        if (rawName && document && document.querySelector) {
          const esc = __cssEscapeSafe;
          const n = esc(rawName);

          // Be practical: accept both "#name" and "name", and ignore case.
          const sels = [`img[usemap="#${n}" i]`, `img[usemap="${n}" i]`];

          for (const sel of sels) {
            try {
              if (document.querySelector(sel)) return true;
            } catch {}
          }
        }
      }
    }
    if (tag === 'input') {
      if (type !== 'hidden') return true;
    }
    if (tag === 'select' || tag === 'textarea' || tag === 'button' || tag === 'summary')
      return true;
    if (el.hasAttribute && el.hasAttribute('contenteditable')) {
      // contenteditable="false" explicitly disables the editing host
      // and does not by itself add the element to the tab order.
      const ceVal = lower(getAttr(el, 'contenteditable'));
      if (ceVal !== 'false') return true;
    }

    const tabindex = el.getAttribute && el.getAttribute('tabindex');
    if (tabindex != null && String(tabindex).trim() !== '' && !Number.isNaN(Number(tabindex)))
      return true;

    return false;
  }

  function getIdRefReverseIndex(scopeObj) {
    // Reverse index: id token -> referencing elements (aria-labelledby / aria-describedby)
    // Built once per scope per run. Deterministic: querySelectorAll order is document order.
    if (!scopeObj || !scopeObj.querySelectorAll) return null;

    if (!__idRefReverseIndexByScope) {
      __perfInc('idrefReverseIndex.nocache');
      return null;
    }

    try {
      const cached = __idRefReverseIndexByScope.get(scopeObj);
      if (cached) {
        __perfInc('idrefReverseIndex.hit');
        return cached;
      }
    } catch {
      __perfInc('idrefReverseIndex.nocache');
      return null;
    }

    __perfInc('idrefReverseIndex.miss');

    const idx = new Map();
    let refs;
    try {
      refs = Array.from(scopeObj.querySelectorAll('[aria-labelledby],[aria-describedby]'));
    } catch {
      refs = [];
    }

    for (const el of refs) {
      if (!isElement(el)) continue;

      // Parse tokens deterministically
      const lb = trim(getAttr(el, 'aria-labelledby'));
      const db = trim(getAttr(el, 'aria-describedby'));

      // Avoid pushing same element twice for the same token when both attrs contain it.
      const pushed = new Set();

      if (lb) {
        const parts = lb.split(/\s+/).filter(Boolean);
        for (const t of parts) {
          const tok = trim(t);
          if (!tok || pushed.has(tok)) continue;
          pushed.add(tok);
          const arr = idx.get(tok);
          if (arr) arr.push(el);
          else idx.set(tok, [el]);
        }
      }

      if (db) {
        const parts = db.split(/\s+/).filter(Boolean);
        for (const t of parts) {
          const tok = trim(t);
          if (!tok || pushed.has(tok)) continue;
          pushed.add(tok);
          const arr = idx.get(tok);
          if (arr) arr.push(el);
          else idx.set(tok, [el]);
        }
      }
    }

    try {
      __idRefReverseIndexByScope.set(scopeObj, idx);
      __perfInc('idrefReverseIndex.build');
    } catch {
      // ignore cache set errors
    }

    return idx;
  }

  function isReferencedByVisibleIdRef(node) {
    if (!document || !isElement(node)) return false;
    const id = node.getAttribute && node.getAttribute('id');
    const idTok = id && id.trim ? id.trim() : '';
    if (!idTok) return false;

    // Prefer reverse-index lookup (single build per run) over repeated querySelectorAll per node.
    const idx = getIdRefReverseIndex(document);
    if (idx && typeof idx.get === 'function') {
      let refs;
      try {
        refs = idx.get(idTok) || null;
      } catch {
        refs = null;
      }
      if (refs && refs.length) {
        for (const ref of refs) {
          if (!isElement(ref)) continue;
          const elig = isAccTreeEligible(ref); // safe recursion
          if (elig && elig.eligible) return true;
        }
        return false;
      }
      // If index exists but no references, short-circuit.
      return false;
    }

    // Fallback to querySelectorAll when cache is unavailable.
    const esc = __cssEscapeSafe;
    const idSel = esc(idTok);
    let refs;
    try {
      refs = [
        ...Array.from(document.querySelectorAll('[aria-labelledby~="' + idSel + '"]')),
        ...Array.from(document.querySelectorAll('[aria-describedby~="' + idSel + '"]'))
      ];
    } catch {
      refs = [];
    }
    for (const ref of refs) {
      if (!isElement(ref)) continue;
      const elig = isAccTreeEligible(ref); // safe recursion
      if (elig && elig.eligible) return true;
    }
    return false;
  }

  function isExcluded(el) {
    const eff = __getEffectiveExcludeSelectors();
    if (!eff.length || !el || !el.closest) return false;
    try {
      return eff.some((sel) => !!el.closest(sel));
    } catch {
      return false;
    }
  }

  function queryAll(sel) {
    if (!roots.length) return [];
    const out = [];
    const seen = new Set();
    // Per root: self-match first (matching the original single-root
    // ordering), then descendants, deduped across all roots -- matters
    // when multiple contextSelector regions overlap/nest, so an element
    // reachable from more than one root is only ever reported once.
    for (const r of roots) {
      if (!r) continue;
      // querySelectorAll never returns its own context node, only
      // descendants — so an attribute/role selector can never match `r`
      // itself this way. In the default (unscoped) case `r` is
      // `document.documentElement` (the <html> element), so without this
      // self-match every rule using this helper would be blind to an issue
      // asserted directly on <html> (e.g. `<html role="...">`, `[lang]`,
      // any `[aria-*]`).
      if (r.nodeType === 1 && typeof r.matches === 'function' && !seen.has(r)) {
        try {
          if (r.matches(sel)) {
            seen.add(r);
            out.push(r);
          }
        } catch {}
      }
      try {
        const list = r.querySelectorAll(sel);
        for (const el of list) {
          if (el && !seen.has(el)) {
            seen.add(el);
            out.push(el);
          }
        }
      } catch {
        // skip this root, keep results from the others
      }
    }
    return out;
  }

  function queryAllDeep(sel) {
    if (!roots.length) return [];
    // Performance note:
    // Avoid the old "querySelectorAll('*')" approach which is O(N) per shadow host
    // and explodes on huge DOMs. Instead, walk shadow roots only and run the selector
    // in each root once. This keeps work proportional to the number of shadow roots.
    const results = [];
    const seen = new Set();
    const visitedRoots = new Set();

    const pushMatches = (scope) => {
      if (!scope || !scope.querySelectorAll) return;
      let els;
      try {
        els = scope.querySelectorAll(sel);
      } catch {
        els = [];
      }
      // NodeList is iterable; avoid Array.from to reduce allocations.
      for (const el of els) {
        if (el && !seen.has(el) && !isExcluded(el)) {
          seen.add(el);
          results.push(el);
        }
      }
      // Same root-self-match gap as queryAll above: querySelectorAll
      // never returns `scope` itself, so a shadow-root host element
      // (or the top-level <html> root) matching `sel` directly would
      // otherwise be invisible here too.
      if (
        scope.nodeType === 1 &&
        typeof scope.matches === 'function' &&
        !seen.has(scope) &&
        !isExcluded(scope)
      ) {
        try {
          if (scope.matches(sel)) {
            seen.add(scope);
            results.push(scope);
          }
        } catch {}
      }
    };

    const collectShadowRoots = (scope) => {
      if (!scope || !scope.querySelectorAll) return [];

      // Cache shadow root discovery per root to avoid repeated querySelectorAll('*') walks.
      // IMPORTANT: do not cache when the effective exclude list (global
      // ∪ active rule-scoped excludes) is non-empty -- different rules
      // may have different effective lists and must not share results.
      if (!__getEffectiveExcludeSelectors().length && __shadowRootsByRoot) {
        try {
          const cached = __shadowRootsByRoot.get(scope);
          if (cached) {
            __perfInc('shadowRoots.hit');
            return cached;
          }
          __perfInc('shadowRoots.miss');

          let hosts = [];
          try {
            hosts = scope.querySelectorAll('*');
          } catch {
            hosts = [];
          }

          const roots = [];
          for (const el of hosts) {
            if (!el || el.nodeType !== 1) continue;
            const sr = el.shadowRoot;
            if (sr) roots.push(sr);
          }

          try {
            __shadowRootsByRoot.set(scope, roots);
          } catch {
            __perfInc('shadowRoots.nocache');
          }
          return roots;
        } catch {
          __perfInc('shadowRoots.nocache');
          // fall through to uncached path
        }
      } else {
        __perfInc('shadowRoots.nocache');
      }

      // Uncached path (preserves excludeSelectors filtering semantics).
      let hosts;
      try {
        hosts = scope.querySelectorAll('*');
      } catch {
        hosts = [];
      }
      const roots = [];
      for (const el of hosts) {
        if (!el || el.nodeType !== 1) continue;
        if (isExcluded(el)) continue;
        const sr = el.shadowRoot;
        if (sr) roots.push(sr);
      }
      return roots;
    };

    // Seed the BFS queue with every resolved context root (not just one)
    // -- the existing shadow-root discovery loop below already
    // generalizes to multiple starting points without further changes,
    // since it was already a growing queue, not a single fixed root.
    const q = roots.slice();
    for (let qi = 0; qi < q.length; qi++) {
      const curRoot = q[qi];
      if (!curRoot || visitedRoots.has(curRoot)) continue;
      visitedRoots.add(curRoot);

      pushMatches(curRoot);

      const childShadowRoots = collectShadowRoots(curRoot);
      for (const sr of childShadowRoots) q.push(sr);
    }

    return results;
  }

  const HARD_HIDDEN_REASONS = new Set([
    'displayNone',
    'hiddenAttr',
    'detailsClosed',
    'templateContent',
    'nonRenderedElement',
    'inputHidden',
    'visibilityHidden',
    'contentVisibilityHidden'
  ]);

  function queryAllSmart(sel) {
    let list = includeShadowDom ? queryAllDeep(sel) : queryAll(sel);

    // Global hidden-content policy: skip nodes that are fully excluded from
    // rendered visibility by default (unless includeHiddenElements:true).
    if (!includeHiddenElements) {
      list = list.filter((el) => {
        try {
          const vis = isAccTreeEligible(el);
          if (!vis || vis.eligible !== false) return true;
          const reasons = Array.isArray(vis.reasons) ? vis.reasons : [];
          for (const r of reasons) {
            if (HARD_HIDDEN_REASONS.has(r)) return false;
          }

          // `isAccTreeEligible` can short-circuit on an inert ancestor
          // before it reaches an outer hard-hidden ancestor (e.g.
          // display:none wrapper). In that case the node is still
          // structurally hidden and should be excluded by the default
          // hidden-content policy.
          if (reasons.includes('inert')) {
            const domVis = isDomVisibleEligible(el, null, {
              visibilityMode: 'styleOnly',
              disableGeometry: true,
              ignoreOpacity: true
            });
            const domReasons = Array.isArray(domVis && domVis.reasons) ? domVis.reasons : [];
            for (const r of domReasons) {
              if (HARD_HIDDEN_REASONS.has(r)) return false;
            }
          }
          return true;
        } catch {
          return true;
        }
      });
    }

    return __getEffectiveExcludeSelectors().length ? list.filter((el) => !isExcluded(el)) : list;
  }

  // -------------------------------------------------------------------------
  // Per-run shared caches (DOM helpers)
  // -------------------------------------------------------------------------
  try {
    const w =
      realmWindow ||
      (document && document.defaultView) ||
      (typeof global !== 'undefined' && global.window ? global.window : null);

    if (w) {
      if (!w.__a11ycoreSharedCache) w.__a11ycoreSharedCache = {};
      if (!w.__a11ycoreSharedCache.dom) w.__a11ycoreSharedCache.dom = {};
      __domSharedCache = w.__a11ycoreSharedCache.dom;
    }
  } catch {
    __domSharedCache = {};
  }

  // Selector cache (per element), partitioned by __selectorOptsKey since
  // built selectors depend on includeShadowDom/excludeSelectors.
  try {
    __selectorCache =
      __domSharedCache.selectorCache instanceof Map
        ? __domSharedCache.selectorCache
        : (__domSharedCache.selectorCache = new Map());
  } catch {
    __selectorCache = null;
  }

  function __getSelectorCacheForOpts() {
    if (!__selectorCache) return null;
    try {
      const key = __getSelectorOptsKey();
      let wm = __selectorCache.get(key);
      if (!(wm instanceof WeakMap)) {
        wm = new WeakMap();
        __selectorCache.set(key, wm);
      }
      return wm;
    } catch {
      return null;
    }
  }

  try {
    __outerHtmlCache =
      __domSharedCache.outerHtmlCache instanceof WeakMap
        ? __domSharedCache.outerHtmlCache
        : (__domSharedCache.outerHtmlCache = new WeakMap());
  } catch {
    __outerHtmlCache = null;
  }

  // ID lookups: cache getElementById / root.querySelector(#id) results within a run
  try {
    __idLookupDocCache =
      __domSharedCache.idLookupDocCache instanceof Map
        ? __domSharedCache.idLookupDocCache
        : (__domSharedCache.idLookupDocCache = new Map());
  } catch {
    __idLookupDocCache = null;
  }

  try {
    __idLookupRootCache =
      __domSharedCache.idLookupRootCache instanceof Map
        ? __domSharedCache.idLookupRootCache
        : (__domSharedCache.idLookupRootCache = new Map());
  } catch {
    __idLookupRootCache = null;
  }

  // IDREF resolution: cache resolveIdRefs results (root-scoped) within a run
  try {
    __idRefCacheByRoot =
      __domSharedCache.idRefCacheByRoot instanceof WeakMap
        ? __domSharedCache.idRefCacheByRoot
        : (__domSharedCache.idRefCacheByRoot = new WeakMap());
  } catch {
    __idRefCacheByRoot = null;
  }

  // Reverse index for aria-labelledby/aria-describedby -> id token
  try {
    __idRefReverseIndexByScope =
      __domSharedCache.idRefReverseIndexByScope instanceof WeakMap
        ? __domSharedCache.idRefReverseIndexByScope
        : (__domSharedCache.idRefReverseIndexByScope = new WeakMap());
  } catch {
    __idRefReverseIndexByScope = null;
  }

  // Selector uniqueness index (per scope) within a run
  try {
    __uniqIndexByScope =
      __domSharedCache.uniqIndexByScope instanceof WeakMap
        ? __domSharedCache.uniqIndexByScope
        : (__domSharedCache.uniqIndexByScope = new WeakMap());
  } catch {
    __uniqIndexByScope = null;
  }

  // Shadow root discovery cache (per root) within a run.
  // Only used when excludeSelectors is empty to avoid cross-helper bleed.
  try {
    __shadowRootsByRoot =
      __domSharedCache.shadowRootsByRoot instanceof WeakMap
        ? __domSharedCache.shadowRootsByRoot
        : (__domSharedCache.shadowRootsByRoot = new WeakMap());
  } catch {
    __shadowRootsByRoot = null;
  }

  // -------------------------------------------------------------------------
  // Additional per-run caches (eligibility / focusability / labeling)
  // -------------------------------------------------------------------------
  let __ancestorsIncludingSelfCache = null;
  let __eligibilityAccCache = null;
  let __eligibilityDomCacheByMode = null; // Map<string, WeakMap<Element, Result>>
  let __focusabilityCache = null;
  let __visibilityHintsCache = null; // WeakMap<Element, {hints:Array<string>, metrics:object}>
  let __computedStyleCacheByScope = null; // WeakMap<object, WeakMap<Element, CSSStyleDeclaration|object>>
  let __openModalDialogsByDoc = null; // WeakMap<Document, Array<Element>>
  let __ancestorBlockerAccByScope = null; // WeakMap<object, WeakMap<Element, {struct:string|null, css:string|null}>>
  let __ancestorBlockerDomByScope = null; // WeakMap<object, WeakMap<Element, {struct, css, cssKnown, visibility, contentVisHidden, opacity}>>
  let __ancestorBlockerDomStructFinalByScope = null; // WeakMap<object, WeakMap<Element, string|null>> (final structural blocker per element per scope)
  let __labelAssociationCache = null;
  let __labelMethodCache = null;
  let __labelForIndexByDoc = null; // WeakMap<Document, Map<string, {exists:boolean, text:string}>> (label[for] by id cache)
  let __accessibleNameCacheByKey = null; // Map<string, WeakMap<Element, Info>>
  let __accessibleDescCacheByKey = null; // Map<string, WeakMap<Element, Info>>

  try {
    __ancestorsIncludingSelfCache =
      __domSharedCache.ancestorsIncludingSelfCache instanceof WeakMap
        ? __domSharedCache.ancestorsIncludingSelfCache
        : (__domSharedCache.ancestorsIncludingSelfCache = new WeakMap());
  } catch {
    __ancestorsIncludingSelfCache = null;
  }

  try {
    __eligibilityAccCache =
      __domSharedCache.eligibilityAccCache instanceof WeakMap
        ? __domSharedCache.eligibilityAccCache
        : (__domSharedCache.eligibilityAccCache = new WeakMap());
  } catch {
    __eligibilityAccCache = null;
  }

  try {
    __eligibilityDomCacheByMode =
      __domSharedCache.eligibilityDomCacheByMode instanceof Map
        ? __domSharedCache.eligibilityDomCacheByMode
        : (__domSharedCache.eligibilityDomCacheByMode = new Map());
  } catch {
    __eligibilityDomCacheByMode = null;
  }

  try {
    __focusabilityCache =
      __domSharedCache.focusabilityCache instanceof WeakMap
        ? __domSharedCache.focusabilityCache
        : (__domSharedCache.focusabilityCache = new WeakMap());
  } catch {
    __focusabilityCache = null;
  }

  try {
    __visibilityHintsCache =
      __domSharedCache.visibilityHintsCache instanceof WeakMap
        ? __domSharedCache.visibilityHintsCache
        : (__domSharedCache.visibilityHintsCache = new WeakMap());
  } catch {
    __visibilityHintsCache = null;
  }

  try {
    __computedStyleCacheByScope =
      __domSharedCache.computedStyleCacheByScope instanceof WeakMap
        ? __domSharedCache.computedStyleCacheByScope
        : (__domSharedCache.computedStyleCacheByScope = new WeakMap());
  } catch {
    __computedStyleCacheByScope = null;
  }

  try {
    __openModalDialogsByDoc =
      __domSharedCache.openModalDialogsByDoc instanceof WeakMap
        ? __domSharedCache.openModalDialogsByDoc
        : (__domSharedCache.openModalDialogsByDoc = new WeakMap());
  } catch {
    __openModalDialogsByDoc = null;
  }

  try {
    __ancestorBlockerAccByScope =
      __domSharedCache.ancestorBlockerAccByScope instanceof WeakMap
        ? __domSharedCache.ancestorBlockerAccByScope
        : (__domSharedCache.ancestorBlockerAccByScope = new WeakMap());
  } catch {
    __ancestorBlockerAccByScope = null;
  }

  try {
    __ancestorBlockerDomByScope =
      __domSharedCache.ancestorBlockerDomByScope instanceof WeakMap
        ? __domSharedCache.ancestorBlockerDomByScope
        : (__domSharedCache.ancestorBlockerDomByScope = new WeakMap());
  } catch {
    __ancestorBlockerDomByScope = null;
  }

  try {
    __ancestorBlockerDomStructFinalByScope =
      __domSharedCache.ancestorBlockerDomStructFinalByScope instanceof WeakMap
        ? __domSharedCache.ancestorBlockerDomStructFinalByScope
        : (__domSharedCache.ancestorBlockerDomStructFinalByScope = new WeakMap());
  } catch {
    __ancestorBlockerDomStructFinalByScope = null;
  }

  try {
    __labelAssociationCache =
      __domSharedCache.labelAssociationCache instanceof WeakMap
        ? __domSharedCache.labelAssociationCache
        : (__domSharedCache.labelAssociationCache = new WeakMap());
  } catch {
    __labelAssociationCache = null;
  }

  try {
    __labelMethodCache =
      __domSharedCache.labelMethodCache instanceof WeakMap
        ? __domSharedCache.labelMethodCache
        : (__domSharedCache.labelMethodCache = new WeakMap());
  } catch {
    __labelMethodCache = null;
  }

  try {
    __labelForIndexByDoc =
      __domSharedCache.labelForIndexByDoc instanceof WeakMap
        ? __domSharedCache.labelForIndexByDoc
        : (__domSharedCache.labelForIndexByDoc = new WeakMap());
  } catch {
    __labelForIndexByDoc = null;
  }

  try {
    __accessibleNameCacheByKey =
      __domSharedCache.accessibleNameCacheByKey instanceof Map
        ? __domSharedCache.accessibleNameCacheByKey
        : (__domSharedCache.accessibleNameCacheByKey = new Map());
  } catch {
    __accessibleNameCacheByKey = null;
  }

  try {
    __accessibleDescCacheByKey =
      __domSharedCache.accessibleDescCacheByKey instanceof Map
        ? __domSharedCache.accessibleDescCacheByKey
        : (__domSharedCache.accessibleDescCacheByKey = new Map());
  } catch {
    __accessibleDescCacheByKey = null;
  }

  function __getScopeObj() {
    // Purely a cache-partition key -- doesn't need to BE a real scan
    // scope, just a value that's stable for this run and distinct across
    // runs with a different root set. `roots` itself (the array) is a
    // stable reference for the whole run when there's more than one.
    if (roots.length === 1) return roots[0];
    if (roots.length > 1) return roots;
    return document && typeof document === 'object' ? document : null;
  }

  function __getLabelForByIdCache(nameKey) {
    // Document-scoped cache for `document.querySelector('label[for="..."]')`.
    // Keeps test semantics (first lookup uses querySelector) while eliminating repeated lookups.
    if (!document || !document.querySelector) return null;
    if (!__labelForIndexByDoc) {
      __perfInc('labelForById.nocache');
      return null;
    }

    try {
      const nk = nameKey == null ? '__default__' : String(nameKey);
      let byKey = __labelForIndexByDoc.get(document);
      if (!(byKey instanceof Map)) {
        __perfInc('labelForById.miss');
        byKey = new Map();
        __labelForIndexByDoc.set(document, byKey);
        __perfInc('labelForById.build');
      }
      const existing = byKey.get(nk);
      if (existing && existing instanceof Map) {
        __perfInc('labelForById.hit');
        return existing;
      }
      __perfInc('labelForById.miss');
      const map = new Map();
      byKey.set(nk, map);
      __perfInc('labelForById.build');
      return map;
    } catch {
      __perfInc('labelForById.nocache');
      return null;
    }
  }

  function __lookupLabelForId(id, nameKey) {
    const key = trim(id);
    if (!key) return null;

    const map = __getLabelForByIdCache(nameKey);
    if (map) {
      if (map.has(key)) return map.get(key) || null;
      // compute and store
      let entry;
      try {
        const sel = 'label[for="' + key.replace(/\\/g, '\\\\').replace(/"/g, '\\"') + '"]';
        const label = document.querySelector(sel);
        if (label && isElement(label)) {
          let t = '';
          try {
            t = trim(label.textContent);
          } catch {
            t = '';
          }
          entry = { exists: true, text: t };
        } else {
          entry = { exists: false, text: '' };
        }
      } catch {
        entry = { exists: false, text: '' };
      }
      try {
        map.set(key, entry);
      } catch {}
      return entry && entry.exists ? entry : null;
    }

    // No cache available: fallback to direct querySelector
    try {
      const sel = 'label[for="' + key.replace(/\\/g, '\\\\').replace(/"/g, '\\"') + '"]';
      const label = document.querySelector(sel);
      if (label && isElement(label)) {
        let t = '';
        try {
          t = trim(label.textContent);
        } catch {
          t = '';
        }
        return { exists: true, text: t };
      }
    } catch {}
    return null;
  }

  function __getEligibilityAccCacheForScope() {
    const scopeObj = __getScopeObj();
    if (!scopeObj || !__domSharedCache) return null;
    try {
      const wmByScope =
        __domSharedCache.eligibilityAccCacheByScope instanceof WeakMap
          ? __domSharedCache.eligibilityAccCacheByScope
          : (__domSharedCache.eligibilityAccCacheByScope = new WeakMap());

      let perScope = wmByScope.get(scopeObj);
      if (!(perScope instanceof WeakMap)) {
        perScope = new WeakMap();
        wmByScope.set(scopeObj, perScope);
      }
      return perScope;
    } catch {
      return null;
    }
  }

  function __getEligibilityDomCacheForScope(modeKey) {
    const scopeObj = __getScopeObj();
    if (!scopeObj || !__domSharedCache) return null;
    try {
      const wmByScope =
        __domSharedCache.eligibilityDomCacheByScope instanceof WeakMap
          ? __domSharedCache.eligibilityDomCacheByScope
          : (__domSharedCache.eligibilityDomCacheByScope = new WeakMap());

      let perScopeMap = wmByScope.get(scopeObj);
      if (!(perScopeMap instanceof Map)) {
        perScopeMap = new Map();
        wmByScope.set(scopeObj, perScopeMap);
      }

      let perMode = perScopeMap.get(modeKey);
      if (!(perMode instanceof WeakMap)) {
        perMode = new WeakMap();
        perScopeMap.set(modeKey, perMode);
      }
      return perMode;
    } catch {
      return null;
    }
  }

  function __getAncestorBlockerAccCacheForScope() {
    const scopeObj = __getScopeObj();
    if (!scopeObj || !__ancestorBlockerAccByScope) return null;
    try {
      let perScope = __ancestorBlockerAccByScope.get(scopeObj);
      if (!(perScope instanceof WeakMap)) {
        perScope = new WeakMap();
        __ancestorBlockerAccByScope.set(scopeObj, perScope);
      }
      return perScope;
    } catch {
      return null;
    }
  }

  function __getAncestorBlockerDomCacheForScope() {
    const scopeObj = __getScopeObj();
    if (!scopeObj || !__ancestorBlockerDomByScope) return null;
    try {
      let perScope = __ancestorBlockerDomByScope.get(scopeObj);
      if (!(perScope instanceof WeakMap)) {
        perScope = new WeakMap();
        __ancestorBlockerDomByScope.set(scopeObj, perScope);
      }
      return perScope;
    } catch {
      return null;
    }
  }

  function __getAncestorBlockerDomStructFinalCacheForScope() {
    const scopeObj = __getScopeObj();
    if (!scopeObj || !__ancestorBlockerDomStructFinalByScope) return null;
    try {
      let perScope = __ancestorBlockerDomStructFinalByScope.get(scopeObj);
      if (!(perScope instanceof WeakMap)) {
        perScope = new WeakMap();
        __ancestorBlockerDomStructFinalByScope.set(scopeObj, perScope);
      }
      return perScope;
    } catch {
      return null;
    }
  }

  function __getDomEligibilityModeKey(opts) {
    const mode =
      opts && opts.visibilityMode === 'styleAndGeometry' ? 'styleAndGeometry' : 'styleOnly';
    const disableGeometry = !!(opts && opts.disableGeometry === true);
    return mode + '|' + (disableGeometry ? 'dg1' : 'dg0');
  }

  function __getNameOptsKey(opts) {
    // Only include options that affect this helper's output.
    const disallowContents = !!(opts && opts.disallowContents === true);
    const maxRefs = opts && opts.maxRefs != null ? Number(opts.maxRefs) | 0 : -1;
    return (disallowContents ? 'dc1' : 'dc0') + '|mr' + String(maxRefs);
  }

  function __getDescOptsKey(opts) {
    const allowTitle = !!(opts && opts.allowTitle === true);
    const maxRefs = opts && opts.maxRefs != null ? Number(opts.maxRefs) | 0 : -1;
    return (allowTitle ? 'at1' : 'at0') + '|mr' + String(maxRefs);
  }

  function getOuterHtmlSnippet(el) {
    if (!el || typeof el !== 'object') return '';
    try {
      if (__outerHtmlCache && __outerHtmlCache.has(el)) {
        __perfInc('outerHtml.hit');
        return __outerHtmlCache.get(el) || '';
      }
    } catch {}

    __perfInc('outerHtml.miss');

    let out;
    try {
      const html = el.outerHTML || '';
      if (html.length > 2000) out = html.slice(0, 2000) + '…';
      else out = html;
    } catch {
      out = '';
    }

    try {
      if (__outerHtmlCache && el && typeof el === 'object') __outerHtmlCache.set(el, out);
    } catch {}
    return out;
  }

  // --- Accessibility-tree eligibility (ordered checks) ---
  function isAccTreeEligible(node) {
    // Cache is per-scope (root/document) to avoid cross-run leakage.
    const __accCache = __getEligibilityAccCacheForScope();
    const __ancBlockCache = __getAncestorBlockerAccCacheForScope();

    if (!isElement(node)) {
      return { eligible: false, reasons: ['notElement'] };
    }

    try {
      if (__accCache && node && typeof node === 'object' && __accCache.has(node)) {
        const c = __accCache.get(node);
        if (c && typeof c === 'object') {
          return {
            eligible: !!c.eligible,
            reasons: Array.isArray(c.reasons) ? c.reasons.slice(0) : []
          };
        }
      }
    } catch {}

    const reasons = [];

    function __cacheAndReturn(res) {
      const out = {
        eligible: !!(res && res.eligible),
        reasons: res && Array.isArray(res.reasons) ? res.reasons.slice(0) : []
      };
      try {
        if (__accCache && node && typeof node === 'object') {
          __accCache.set(node, { eligible: out.eligible, reasons: out.reasons.slice(0) });
        }
      } catch {}
      return out;
    }

    const chain = ancestorsIncludingSelf(node);

    // 1) HTML/DOM hiding
    for (const a of chain) {
      if (!isElement(a)) continue;

      // Ancestor structural blockers are scope-cached (per run) to avoid repeated checks.
      let struct = null;
      try {
        if (__ancBlockCache && __ancBlockCache.has(a)) {
          __perfInc('ancestorBlockerAcc.struct.hit');
          const cached = __ancBlockCache.get(a);
          struct = cached && cached.struct ? String(cached.struct) : null;
        } else {
          __perfInc('ancestorBlockerAcc.struct.miss');
          const tn = (a.tagName || '').toLowerCase();
          if (a.hasAttribute && a.hasAttribute('hidden')) struct = 'hiddenAttr';
          else if (tn === 'template') struct = 'templateContent';
          else if (
            tn === 'script' ||
            tn === 'style' ||
            tn === 'meta' ||
            tn === 'link' ||
            tn === 'noscript'
          )
            struct = 'nonRenderedElement';
          else if (tn === 'input') {
            const t = (a.getAttribute && (a.getAttribute('type') || '').toLowerCase()) || '';
            if (t === 'hidden') struct = 'inputHidden';
          }
          try {
            try {
              if (__ancBlockCache) {
                const prev = __ancBlockCache.has(a) ? __ancBlockCache.get(a) || null : null;
                __ancBlockCache.set(a, {
                  struct,
                  css: prev && prev.css ? prev.css : null,
                  cssKnown: prev && prev.cssKnown === true ? true : false
                });
              }
            } catch {
              __perfInc('ancestorBlockerAcc.struct.nocache');
            }
          } catch {
            __perfInc('ancestorBlockerAcc.struct.nocache');
          }
        }
      } catch {
        /* ignore */
      }

      // `hidden="until-found"` is a distinct state from a plain `hidden`
      // attribute (HTML spec: the UA stylesheet applies `content-
      // visibility: hidden` for `until-found`, vs. `display: none` for any
      // other value/bare `hidden`). content-visibility:hidden hides an
      // element's DESCENDANTS from rendering, not the element itself. The
      // `struct` lookup above is cached per-element and shared across every
      // node that walks through `a` as an ancestor, where "this ancestor
      // hides its descendants" is the right answer for `until-found`; this
      // self-only override corrects it for the case where `a` IS `node`
      // itself, without touching the cached value descendants rely on.
      // Without it, an `until-found` panel would be excluded even from
      // rules checking its own attributes.
      if (struct === 'hiddenAttr' && a === node) {
        const hiddenVal = String((a.getAttribute && a.getAttribute('hidden')) || '')
          .trim()
          .toLowerCase();
        if (hiddenVal === 'until-found') struct = null;
      }

      if (struct) return __cacheAndReturn({ eligible: false, reasons: [struct] });
    }
    if (inClosedDetailsContent(node))
      return __cacheAndReturn({ eligible: false, reasons: ['detailsClosed'] });

    // 2) Inertness / modality
    if (hasBlockingInert(node)) {
      return __cacheAndReturn({ eligible: false, reasons: ['inert'] });
    }
    // Modal dialog (best effort)
    try {
      const openModals = getOpenModalDialogs();
      if (openModals.length) {
        let inside = false;
        for (const d of openModals) {
          if (d && d.contains && d.contains(node)) {
            inside = true;
            break;
          }
        }
        if (!inside) return __cacheAndReturn({ eligible: false, reasons: ['modalInert'] });
      }
    } catch {}

    // 3) CSS rendering suppression
    // display:none is NOT inherited: if ANY ancestor (or self) has
    // display:none, the whole subtree is unrendered no matter what a
    // descendant's own display is, so this must be resolved via an
    // ancestor walk that breaks on the first blocker found.
    for (const a of chain) {
      if (!isElement(a)) continue;

      // <area> is a non-rendered element; some DOMs report display:none for it.
      // Don’t treat the *area itself* as ineligible based on computed style.
      if (a === node) {
        const tn = (a.tagName || '').toLowerCase();
        if (tn === 'area') continue;
      }

      // Cache ancestor CSS blockers (display) per scope.
      let cssBlock = null;
      let cssKnown = false;
      try {
        if (__ancBlockCache && __ancBlockCache.has(a)) {
          const cached = __ancBlockCache.get(a);
          if (cached && cached.cssKnown === true) {
            __perfInc('ancestorBlockerAcc.css.hit');
            cssKnown = true;
            cssBlock = cached.css ? String(cached.css) : null;
          } else {
            __perfInc('ancestorBlockerAcc.css.miss');
          }
        } else {
          __perfInc('ancestorBlockerAcc.css.miss');
        }
      } catch {}

      if (!cssKnown) {
        const cs = computedStyle(a);
        cssBlock = cs && cs.display === 'none' ? 'displayNone' : null;

        try {
          if (__ancBlockCache) {
            const prev = __ancBlockCache.has(a) ? __ancBlockCache.get(a) || null : null;
            __ancBlockCache.set(a, {
              struct: prev && prev.struct ? prev.struct : null,
              css: cssBlock || null,
              cssKnown: true
            });
          }
        } catch {
          __perfInc('ancestorBlockerAcc.css.nocache');
        }
      }

      if (cssBlock === 'displayNone')
        return __cacheAndReturn({ eligible: false, reasons: ['displayNone'] });
    }

    // visibility IS inherited (and thus invertible): a descendant with an
    // explicit visibility:visible re-renders even under a
    // visibility:hidden ancestor. The fully resolved, post-inheritance
    // value is already reflected in the target node's own computed
    // style, so this is checked on `node` directly rather than by
    // walking ancestors (which would incorrectly treat visibility like
    // the non-inherited `display` property above).
    {
      const tn = (node.tagName || '').toLowerCase();
      if (tn !== 'area') {
        const cs = computedStyle(node);
        if (cs && (cs.visibility === 'hidden' || cs.visibility === 'collapse')) {
          return __cacheAndReturn({ eligible: false, reasons: ['visibilityHidden'] });
        }
      }
    }

    // 4) ARIA subtree hiding with exceptions with exceptions
    let ariaHidden = false;
    for (const a of chain) {
      if (!isElement(a)) continue;
      const v = a.getAttribute && a.getAttribute('aria-hidden');
      if (v != null && String(v).trim().toLowerCase() === 'true') {
        ariaHidden = true;
        break;
      }
    }
    if (ariaHidden) {
      const idref = isReferencedByVisibleIdRef(node);

      // IDREF exception stays
      if (idref)
        return __cacheAndReturn({ eligible: true, reasons: ['ariaHiddenOverriddenIdref'] });

      // Only *explicit* tabbable focus (tabindex >= 0) overrides aria-hidden by default.
      // Native focusability alone does not override aria-hidden EXCEPT for specific
      // mechanisms where the engine must still evaluate (e.g. <area> in a *used* map,
      // and <input type="image">).
      const ti = parseTabIndex(node);
      if (ti.has && ti.valid && ti.value >= 0) {
        return __cacheAndReturn({ eligible: true, reasons: ['ariaHiddenOverriddenTabbable'] });
      }

      // Programmatic focus (explicit tabindex < 0) does NOT override eligibility.
      if (ti.has && ti.valid && ti.value < 0) {
        return __cacheAndReturn({
          eligible: false,
          reasons: ['ariaHiddenProgrammaticFocusExcluded']
        });
      }

      // Exception: allow aria-hidden override for mechanisms where the engine must
      // still evaluate required labeling/alt checks. Keep this narrowly scoped.
      const tag = (node.tagName || '').toLowerCase();
      const type =
        tag === 'input'
          ? (node.getAttribute && (node.getAttribute('type') || '').toLowerCase()) || ''
          : '';

      // Native form controls are tabbable by default (even without tabindex)
      // and are targeted by labeling checks.
      const isNativeFormControl =
        tag === 'select' || tag === 'textarea' || (tag === 'input' && type !== 'hidden'); // includes type=image

      // Other elements that are natively tabbable by default (no explicit
      // tabindex required): <button>, <summary>, and <a>/<area> with a
      // non-empty href. Real browsers keep these in the tab order
      // regardless of aria-hidden — this is exactly the "aria-hidden on a
      // focusable element" anti-pattern that aria-hidden-focus.js itself
      // detects as a violation, so the eligibility model must evaluate
      // these too rather than silently excluding them. getPlatformFocusability
      // (via isPlatformFocusable) already checks the href/disabled/inert
      // conditions correctly for each of these tags.
      const isOtherNativelyFocusable = tag === 'button' || tag === 'summary' || tag === 'a';

      if (tag === 'area' || isNativeFormControl || isOtherNativelyFocusable) {
        const f2 = getPlatformFocusability(node);
        if (f2 && f2.tabbable) {
          return __cacheAndReturn({ eligible: true, reasons: ['ariaHiddenOverriddenTabbable'] });
        }
      }

      return __cacheAndReturn({ eligible: false, reasons: ['ariaHidden'] });
    }

    // 5/6 handled implicitly; 7 already covered
    return __cacheAndReturn({ eligible: true, reasons });
  }

  function isDomVisibleEligible(node, _ctx, opts) {
    const reasons = [];
    const out = (visible, reasonsArr, metrics) => ({
      eligible: !!visible,
      reasons: reasonsArr.slice(0),
      metrics: metrics && typeof metrics === 'object' ? { ...metrics } : {}
    });

    if (!isElement(node)) return out(false, ['notElement'], {});

    const __modeKey = __getDomEligibilityModeKey(opts);
    const __domCache = __getEligibilityDomCacheForScope(__modeKey);

    const __ancBlockDomCache = __getAncestorBlockerDomCacheForScope();
    const __ancBlockStructFinalCache = __getAncestorBlockerDomStructFinalCacheForScope();

    try {
      if (__domCache && node && typeof node === 'object' && __domCache.has(node)) {
        const c = __domCache.get(node);
        if (c && typeof c === 'object') {
          return {
            eligible: !!c.eligible,
            reasons: Array.isArray(c.reasons) ? c.reasons.slice(0) : [],
            metrics: c.metrics && typeof c.metrics === 'object' ? { ...c.metrics } : {}
          };
        }
      }
    } catch {}

    function __cacheAndReturn(res) {
      const outRes = {
        eligible: !!(res && res.eligible),
        reasons: res && Array.isArray(res.reasons) ? res.reasons.slice(0) : [],
        metrics: res && res.metrics && typeof res.metrics === 'object' ? { ...res.metrics } : {}
      };
      try {
        if (__domCache && node && typeof node === 'object') {
          __domCache.set(node, {
            eligible: outRes.eligible,
            reasons: outRes.reasons.slice(0),
            metrics: { ...outRes.metrics }
          });
        }
      } catch {}
      return outRes;
    }

    // 1) HTML hiding
    // Final short-circuit: reuse structural blocker result for this node when already known.
    try {
      if (__ancBlockStructFinalCache && __ancBlockStructFinalCache.has(node)) {
        __perfInc('ancestorBlockerDom.structFinal.hit');
        const r = __ancBlockStructFinalCache.get(node);
        const rr = r != null && r !== '' ? String(r) : null;
        if (rr) return __cacheAndReturn(out(false, [rr], {}));
      } else {
        __perfInc('ancestorBlockerDom.structFinal.miss');
      }
    } catch {}

    const chain = ancestorsIncludingSelf(node);
    const __domStructSeen = [];
    for (const a of chain) {
      if (!isElement(a)) continue;

      __domStructSeen.push(a);

      // If an ancestor already has a final structural blocker cached,
      // short-circuit immediately (this is what the test expects).
      try {
        if (__ancBlockStructFinalCache && __ancBlockStructFinalCache.has(a)) {
          __perfInc('ancestorBlockerDom.structFinal.hit');
          const r = __ancBlockStructFinalCache.get(a);
          const rr = r != null && r !== '' ? String(r) : null;
          if (rr) {
            // Propagate to nodes we've seen on this walk (including `node`)
            try {
              for (const s of __domStructSeen) {
                if (!__ancBlockStructFinalCache.has(s)) __ancBlockStructFinalCache.set(s, rr);
              }
            } catch {}
            return __cacheAndReturn(out(false, [rr], {}));
          }
        }
      } catch {}

      // Cached structural blockers (per scope) to short-circuit shared ancestor checks.
      let struct = null;
      try {
        if (__ancBlockDomCache && __ancBlockDomCache.has(a)) {
          __perfInc('ancestorBlockerDom.struct.hit');
          const cached = __ancBlockDomCache.get(a);
          struct = cached && cached.struct ? String(cached.struct) : null;
        } else {
          __perfInc('ancestorBlockerDom.struct.miss');
          const tn = (a.tagName || '').toLowerCase();
          if (a.hasAttribute && a.hasAttribute('hidden')) struct = 'hiddenAttr';
          else if (tn === 'template') struct = 'templateContent';
          else if (
            tn === 'script' ||
            tn === 'style' ||
            tn === 'meta' ||
            tn === 'link' ||
            tn === 'noscript'
          )
            struct = 'nonRenderedElement';
          else if (tn === 'input') {
            const t = (a.getAttribute && (a.getAttribute('type') || '').toLowerCase()) || '';
            if (t === 'hidden') struct = 'inputHidden';
          }
          try {
            if (__ancBlockDomCache) {
              const prev = __ancBlockDomCache.has(a) ? __ancBlockDomCache.get(a) || null : null;
              __ancBlockDomCache.set(a, {
                struct,
                css: prev && prev.css ? prev.css : null,
                cssKnown: prev && prev.cssKnown === true ? true : false,
                visibility: prev && prev.visibility ? prev.visibility : null,
                contentVisHidden: prev && prev.contentVisHidden === true ? true : null,
                opacity: prev && typeof prev.opacity === 'number' ? prev.opacity : null
              });
            }
          } catch {
            __perfInc('ancestorBlockerDom.struct.nocache');
          }
        }
      } catch {}

      if (struct) {
        try {
          if (__ancBlockStructFinalCache) {
            for (const s of __domStructSeen) {
              if (!__ancBlockStructFinalCache.has(s)) __ancBlockStructFinalCache.set(s, struct);
            }
          }
        } catch {}
        return __cacheAndReturn(out(false, [struct], {}));
      }
    }

    try {
      if (__ancBlockStructFinalCache) {
        for (const s of __domStructSeen) {
          if (!__ancBlockStructFinalCache.has(s)) __ancBlockStructFinalCache.set(s, null);
        }
      }
    } catch {}

    // Closed <details> hides content visually
    if (inClosedDetailsContent(node)) return __cacheAndReturn(out(false, ['detailsClosed'], {}));

    const visibilityMode =
      opts && opts.visibilityMode === 'pointer'
        ? 'pointer'
        : opts && opts.visibilityMode === 'styleAndGeometry'
          ? 'styleAndGeometry'
          : 'styleOnly';

    // CSS visibility is inherited, so the target node's own computed
    // style already reflects the fully-resolved (post-inheritance)
    // value. Checked here, before the opacity accumulation walk below,
    // so an element that is BOTH opacity:0 AND visibility:hidden (a
    // common hover/JS-reveal dropdown pattern — confirmed on a real
    // site, Getty's global nav dropdowns) is correctly reported as
    // 'visibilityHidden' rather than only 'opacityZero'. Reporting only
    // 'opacityZero' matters because callers that deliberately treat
    // opacity:0 as "still in-scope" (e.g. aria-hidden-focus, which must
    // not exclude opacity-based hiding) would otherwise see no other
    // blocking reason and wrongly conclude the element is focusable,
    // even though visibility:hidden alone already removes it from the
    // tab order in real browsers.
    {
      const nodeCs = computedStyle(node);
      if (nodeCs && (nodeCs.visibility === 'hidden' || nodeCs.visibility === 'collapse')) {
        return __cacheAndReturn(
          out(false, ['visibilityHidden'], { visibility: nodeCs.visibility })
        );
      }
    }

    // 2) CSS visibility suppression + opacity chain
    //
    // Two passes over the SAME ancestor chain, deliberately NOT
    // interleaved: display:none (and content-visibility:hidden) are
    // absolute, un-overridable blocks — there is no CSS mechanism for a
    // descendant to un-hide itself from a display:none ancestor, unlike
    // visibility:hidden (invertible) or opacity (never a hard block by
    // this function's own design — see callers like aria-hidden-focus
    // that deliberately keep opacity:0 in-scope). A single interleaved
    // loop returning on the FIRST blocking condition would let a closer
    // ancestor's opacity:0 short-circuit before a farther ancestor's
    // display:none is reached, hiding the stronger, unconditional block
    // behind the weaker, filterable one. So pass 1 checks every ancestor
    // for a hard structural CSS block first, with no early exit for
    // opacity; pass 2 (below) computes the accumulated opacity only once no
    // hard block was found anywhere in the chain.
    const __cssInfoByAncestor = new Map();

    for (const a of chain) {
      if (!isElement(a)) continue;

      let cssBlock = null;
      let cssKnown = false;

      let cachedVisibility = null;
      let cachedContentVisHidden = null;
      let cachedOpacity = null;
      let cachedPointerEventsNone = null;
      let cachedPointerEventsKnown = false;
      let cs;

      try {
        if (__ancBlockDomCache && __ancBlockDomCache.has(a)) {
          const cached = __ancBlockDomCache.get(a);
          if (cached) {
            // cssKnown means "we already computed display/visibility/content-visibility once"
            if (cached.cssKnown === true) {
              __perfInc('ancestorBlockerDom.css.hit');
              cssKnown = true;
              cssBlock = cached.css ? String(cached.css) : null;
            } else {
              __perfInc('ancestorBlockerDom.css.miss');
            }

            cachedVisibility = cached.visibility != null ? String(cached.visibility) : null;
            cachedContentVisHidden = cached.contentVisHidden === true ? true : null;
            cachedOpacity =
              cached && typeof cached.opacity === 'number' && Number.isFinite(cached.opacity)
                ? cached.opacity
                : null;

            cachedPointerEventsNone = cached.pointerEventsNone === true ? true : null;
            cachedPointerEventsKnown = cached.pointerEventsKnown === true ? true : false;
          }
        } else {
          __perfInc('ancestorBlockerDom.css.miss');
        }
      } catch {}

      // Compute CSS blockers (and maybe opacity) only when needed
      if (!cssKnown && cachedContentVisHidden !== true) {
        cs = computedStyle(a);

        if (cs && cs.display === 'none') cssBlock = 'displayNone';
        else if (cs && (cs.visibility === 'hidden' || cs.visibility === 'collapse')) {
          cssBlock = 'visibilityHidden';
          cachedVisibility = cs.visibility;
        } else if (cs && cs.contentVisibility === 'hidden') {
          cssBlock = 'contentVisibilityHidden';
          cachedContentVisHidden = true;
        }

        // NEW: parse opacity once and cache it (even if cssBlock is null)
        if (cachedOpacity == null) {
          try {
            const raw = cs && cs.opacity != null ? String(cs.opacity).trim() : '';
            const parsed = Number.parseFloat(raw);
            if (Number.isFinite(parsed)) cachedOpacity = parsed;
          } catch {}
        }

        // Pointer reachability: pointer-events:none blocks hit-testing
        if (visibilityMode === 'pointer' && !cachedPointerEventsKnown) {
          try {
            const pe = cs && cs.pointerEvents != null ? String(cs.pointerEvents).trim() : '';
            cachedPointerEventsKnown = true;
            if (pe === 'none') cachedPointerEventsNone = true;
          } catch {}
        }

        try {
          if (__ancBlockDomCache) {
            const prev = __ancBlockDomCache.has(a) ? __ancBlockDomCache.get(a) || null : null;
            __ancBlockDomCache.set(a, {
              struct: prev && prev.struct ? prev.struct : null,
              css: cssBlock || null,
              cssKnown: true,
              visibility: cachedVisibility || (prev && prev.visibility ? prev.visibility : null),
              contentVisHidden:
                cachedContentVisHidden === true
                  ? true
                  : prev && prev.contentVisHidden === true
                    ? true
                    : null,
              opacity:
                cachedOpacity == null
                  ? prev && typeof prev.opacity === 'number'
                    ? prev.opacity
                    : null
                  : cachedOpacity,
              pointerEventsNone:
                cachedPointerEventsNone === true
                  ? true
                  : prev && prev.pointerEventsNone === true
                    ? true
                    : null,
              pointerEventsKnown:
                cachedPointerEventsKnown === true
                  ? true
                  : prev && prev.pointerEventsKnown === true
                    ? true
                    : false
            });
          }
        } catch {
          __perfInc('ancestorBlockerDom.css.nocache');
        }
      }

      __cssInfoByAncestor.set(a, {
        cssBlock,
        cachedOpacity,
        cachedPointerEventsNone,
        cachedPointerEventsKnown
      });

      if (cssBlock === 'displayNone') return __cacheAndReturn(out(false, ['displayNone'], {}));
      // NOTE: unlike display:none, CSS visibility is inherited and thus
      // invertible — a descendant with an explicit visibility:visible
      // re-renders even under a visibility:hidden ancestor. So an
      // ancestor's visibility:hidden must NOT short-circuit this walk;
      // the target node's own fully-resolved visibility is checked
      // once, after the loop (see below).
      if (cssBlock === 'contentVisibilityHidden') {
        return __cacheAndReturn(out(false, ['contentVisibilityHidden'], {}));
      }
    }

    let opacityProduct = 1;
    for (const a of chain) {
      if (!isElement(a)) continue;

      const info = __cssInfoByAncestor.get(a) || {};
      let cachedOpacity = info.cachedOpacity;
      let cachedPointerEventsNone = info.cachedPointerEventsNone;
      let cachedPointerEventsKnown = info.cachedPointerEventsKnown;
      let cs = null;

      if (visibilityMode === 'pointer') {
        // pointer-events:none prevents the element from receiving pointer interactions
        if (cachedPointerEventsKnown === true && cachedPointerEventsNone === true) {
          return __cacheAndReturn(out(false, ['pointerEventsNone'], {}));
        }

        if (cachedPointerEventsKnown !== true) {
          try {
            if (!cs) cs = computedStyle(a);
            const pe = cs && cs.pointerEvents != null ? String(cs.pointerEvents).trim() : '';
            cachedPointerEventsKnown = true;
            if (pe === 'none') cachedPointerEventsNone = true;

            // Write back pointer-events status without disturbing other fields
            try {
              if (__ancBlockDomCache) {
                const prev = __ancBlockDomCache.has(a) ? __ancBlockDomCache.get(a) || null : null;
                if (prev) {
                  __ancBlockDomCache.set(a, {
                    struct: prev.struct || null,
                    css: prev.css || null,
                    cssKnown: prev.cssKnown === true ? true : false,
                    visibility: prev.visibility || null,
                    contentVisHidden: prev.contentVisHidden === true ? true : null,
                    opacity: typeof prev.opacity === 'number' ? prev.opacity : null,
                    pointerEventsNone: cachedPointerEventsNone === true ? true : null,
                    pointerEventsKnown: cachedPointerEventsKnown === true ? true : false
                  });
                } else {
                  __ancBlockDomCache.set(a, {
                    struct: null,
                    css: null,
                    cssKnown: false,
                    visibility: null,
                    contentVisHidden: null,
                    opacity: null,
                    pointerEventsNone: cachedPointerEventsNone === true ? true : null,
                    pointerEventsKnown: cachedPointerEventsKnown === true ? true : false
                  });
                }
              }
            } catch {}
          } catch {}
        }

        if (cachedPointerEventsNone === true) {
          return __cacheAndReturn(out(false, ['pointerEventsNone'], {}));
        }
      }

      // If opacity isn't cached yet, compute once and write it back even when cssBlock was cached.
      // This prevents repeated computedStyle(a) calls across many isDomVisibleEligible() invocations.
      if (cachedOpacity == null) {
        try {
          if (!cs) cs = computedStyle(a);
          const raw = cs && cs.opacity != null ? String(cs.opacity).trim() : '';
          const parsed = Number.parseFloat(raw);
          if (Number.isFinite(parsed)) {
            cachedOpacity = parsed;

            // Write back to cache without disturbing other fields
            try {
              if (__ancBlockDomCache) {
                const prev = __ancBlockDomCache.has(a) ? __ancBlockDomCache.get(a) || null : null;
                if (prev) {
                  __ancBlockDomCache.set(a, {
                    struct: prev.struct || null,
                    css: prev.css || null,
                    visibility: prev.visibility || null,
                    contentVisHidden: prev.contentVisHidden === true ? true : null,
                    opacity: cachedOpacity,
                    pointerEventsNone: prev.pointerEventsNone === true ? true : null,
                    pointerEventsKnown: prev.pointerEventsKnown === true ? true : false
                  });
                } else {
                  // No prior cache entry for this ancestor and no hard
                  // structural block was found for it in pass 1 above
                  // (pass 1 would have returned early otherwise), so
                  // struct/css/visibility/contentVisHidden are all
                  // known-null here.
                  __ancBlockDomCache.set(a, {
                    struct: null,
                    css: null,
                    visibility: null,
                    contentVisHidden: null,
                    opacity: cachedOpacity,
                    pointerEventsNone: null,
                    pointerEventsKnown: false
                  });
                }
              }
            } catch {}
          }
        } catch {}
      }

      // opacity handling (visual)
      const op = cachedOpacity != null ? cachedOpacity : 1;
      opacityProduct *= op;
      // Allow callers to ignore opacity-based invisibility (still focusable).
      const ignoreOpacity = !!(opts && opts.ignoreOpacity === true);

      if (!ignoreOpacity && visibilityMode !== 'pointer' && opacityProduct <= 0.0001) {
        return __cacheAndReturn(out(false, ['opacityZero'], { opacity: opacityProduct }));
      }
    }

    // 3) Layout/geometry (optional)
    const useGeometry =
      visibilityMode === 'pointer'
        ? !(opts && opts.disableGeometry === true)
        : visibilityMode === 'styleAndGeometry' && !(opts && opts.disableGeometry === true);

    if (useGeometry) {
      try {
        if (node.getClientRects) {
          const rects = node.getClientRects();
          const rectCount = rects ? rects.length : 0;

          if (!rectCount) {
            return __cacheAndReturn(out(false, ['noClientRects'], { rectCount: 0 }));
          }

          const r = node.getBoundingClientRect ? node.getBoundingClientRect() : null;
          const w = r && Number.isFinite(r.width) ? r.width : 0;
          const h = r && Number.isFinite(r.height) ? r.height : 0;

          if (w <= 0 || h <= 0) {
            return __cacheAndReturn(out(false, ['zeroArea'], { rectCount, width: w, height: h }));
          }

          return __cacheAndReturn(
            out(true, reasons, {
              rectCount,
              width: w,
              height: h,
              opacity: opacityProduct
            })
          );
        }
      } catch {
        // ignore geometry failures; fall back to style-only eligibility
      }
    }

    return __cacheAndReturn(out(true, reasons, { opacity: opacityProduct }));
  }

  function getEligibilityInfo(node, _ctx, opts) {
    const targetSet =
      opts && (opts.targetSet === 'acc' || opts.targetSet === 'dom') ? opts.targetSet : 'dom';
    const r =
      targetSet === 'dom' ? isDomVisibleEligible(node, _ctx, opts) : isAccTreeEligible(node);
    return {
      eligible: !!(r && r.eligible),
      reasons: r && Array.isArray(r.reasons) ? r.reasons.slice(0) : [],
      targetSet,
      accEligible: targetSet === 'acc' ? !!(r && r.eligible) : null
    };
  }

  // E) IDREF helpers
  function resolveIdRefs(idrefString, _ctx, opts) {
    const raw = trim(idrefString);
    if (!raw) return { refs: [], missing: [], flags: ['empty'] };

    // Normalize whitespace for stable cache keys
    const parts = raw.split(/\s+/).filter(Boolean);
    const normKey = parts.join(' ');

    // Root-scoped cache map
    let cacheMap = null;
    if (__idRefCacheByRoot) {
      const scopeObj = __getScopeObj();
      if (scopeObj) {
        try {
          cacheMap = __idRefCacheByRoot.get(scopeObj) || null;
          if (!cacheMap) {
            cacheMap = new Map();
            __idRefCacheByRoot.set(scopeObj, cacheMap);
          }
        } catch {
          cacheMap = null;
        }
      }
    }

    // Cached base result is *untruncated* (opts.maxRefs applied per call)
    if (cacheMap) {
      try {
        const cached = cacheMap.get(normKey);
        if (cached && cached.refs && cached.missing && cached.flags) {
          const baseRefs = Array.isArray(cached.refs) ? cached.refs.slice(0) : [];
          const baseMissing = Array.isArray(cached.missing) ? cached.missing.slice(0) : [];
          const baseFlags = Array.isArray(cached.flags) ? cached.flags.slice(0) : [];

          // Apply deterministic truncation if requested
          if (opts && opts.maxRefs && baseRefs.length > opts.maxRefs) {
            baseRefs.length = Math.max(0, Number(opts.maxRefs) | 0);
            baseFlags.push('truncated');
          }

          __perfInc('idref.resolve.hit');
          return { refs: baseRefs, missing: baseMissing, flags: baseFlags };
        }
      } catch {
        // cache read errors should never throw
      }
    }

    __perfInc(cacheMap ? 'idref.resolve.miss' : 'idref.resolve.nocache');
    // Compute base result
    const refs = [];
    const missing = [];
    const seen = new Set();

    for (const id of parts) {
      const key = trim(id);
      if (!key) continue;

      let el = safeDocGetById(key);
      if (!el) el = safeRootQueryById(key);

      if (!el || !isElement(el)) {
        missing.push(key);
        continue;
      }
      if (seen.has(el)) continue;
      seen.add(el);
      refs.push(el);
    }

    const flags = [];
    if (missing.length) flags.push('idref-missing');
    if (parts.length !== refs.length + missing.length) flags.push('deduped'); // indicates repeats

    // Store untruncated base result
    if (cacheMap) {
      try {
        cacheMap.set(normKey, {
          refs: refs.slice(0),
          missing: missing.slice(0),
          flags: flags.slice(0),
          partsLen: parts.length
        });
      } catch {
        // ignore cache write errors
      }
    }

    // Apply deterministic truncation per call
    if (opts && opts.maxRefs && refs.length > opts.maxRefs) {
      refs.length = Math.max(0, Number(opts.maxRefs) | 0);
      flags.push('truncated');
    }

    return { refs, missing, flags };
  }

  // Native "name is derived from value/alt" mechanisms, used when resolving
  // an IDREF *target*'s own text alternative (see computeIdRefTargetTextAlternative).
  function __getElementValueLikeName(el) {
    if (!isElement(el)) return '';
    const tag = (el.tagName || '').toLowerCase();

    if (tag === 'img' || tag === 'area') {
      const alt = getAttr(el, 'alt');
      if (alt != null) {
        const t = trim(alt);
        if (t) return t;
      }
      return '';
    }

    if (tag === 'input') {
      const type = lower(getAttr(el, 'type') || 'text');
      if (type === 'button' || type === 'submit' || type === 'reset' || type === 'image') {
        const v = getAttr(el, 'value');
        if (v != null) {
          const t = trim(v);
          if (t) return t;
        }
        if (type === 'submit') return 'Submit';
        if (type === 'reset') return 'Reset';
      }
    }

    return '';
  }

  // Recursively computes an IDREF-referenced node's own text alternative,
  // per the Accessible Name and Description Computation spec (resolving a
  // reference re-applies the name-computation algorithm to the target, it
  // does not just read raw textContent — see getContentNameInfo for why
  // raw textContent misses image alt text and other attribute-sourced
  // names on descendants). `visited` guards against cycles reachable via
  // direct aria-labelledby chains (e.g. two elements labelling each
  // other); `__nameComputationDepth` additionally bounds the combined
  // depth across this function and getContentNameInfo/
  // getAccessibleNameInfo, since a hop through a target's *content* (a
  // descendant with its own aria-labelledby) starts a fresh `visited` Set
  // and would otherwise defeat that per-call guard on a genuine cycle.
  function computeIdRefTargetTextAlternative(el, visited, _ctx, opts) {
    if (!isElement(el)) return '';
    if (visited.has(el)) return '';
    visited.add(el);
    if (__nameComputationDepth >= __NAME_COMPUTATION_MAX_DEPTH) return '';

    // Establish opts.includeHidden exactly once per aria-labelledby/
    // aria-describedby traversal, from the top-level referenced target's
    // own hidden state, and never overwrite it on recursive calls, so the
    // whole referenced subtree (nested labelledby chains included) shares
    // one decision. See getContentNameInfo's collect() for what this
    // bypasses and why.
    let effOpts = opts;
    if (!opts || opts.includeHidden === undefined) {
      let hidden;
      try {
        const elig = isAccTreeEligible(el);
        hidden = !(elig && elig.eligible);
      } catch {
        hidden = false;
      }
      effOpts = Object.assign({}, opts, { includeHidden: hidden });
    }

    // Share this call's own `visited` guard with getTextFromIdRefs/
    // getTextFromIdRefsIdrefEligible (both otherwise start a fresh Set of
    // their own), so the native-<label> lookup below can't re-enter a cycle
    // undetected: a label whose content contains a descendant
    // aria-labelledby'd back to the very control it labels (e.g.
    // <label id="lbl">Custom <span aria-labelledby="x">t</span>
    // label<input id="x"></label>) resolves that descendant back into this
    // function for the SAME `el`; without the shared guard it re-walks the
    // whole label, bounded only by the blunt depth counter.
    effOpts = Object.assign({}, effOpts, { __idrefVisited: visited });

    __nameComputationDepth += 1;
    try {
      // aria-labelledby outranks aria-label per the accname spec (2A before
      // 2B), matching getAriaNameInfo's own precedence.
      const labelledBy = trim(getAttr(el, 'aria-labelledby'));
      if (labelledBy) {
        const parts = labelledBy.split(/\s+/).filter(Boolean);
        const texts = [];
        for (const id of parts) {
          let ref = safeDocGetById(id);
          if (!ref) ref = safeRootQueryById(id);
          if (ref && isElement(ref)) {
            const t = computeIdRefTargetTextAlternative(ref, visited, _ctx, effOpts);
            if (t) texts.push(t);
          }
        }
        const joined = trim(texts.join(' '));
        if (joined) return joined;
      }

      const ariaLabel = trim(getAttr(el, 'aria-label'));
      if (ariaLabel) return ariaLabel;

      // Native <label> association, same two-step lookup (.labels API,
      // then id-based `label[for]` fallback) and same priority slot
      // (before value-like/content) as getAccessibleNameInfo's own
      // resolution of a standalone element. Without it, an aria-labelledby
      // target that is itself a labeled form control (e.g. an <input>
      // named only by a <label for>) would resolve to empty text.
      try {
        if (el.labels && el.labels.length) {
          for (const labelEl of Array.from(el.labels)) {
            const info = getLabelSubtreeNameInfo(labelEl, el, _ctx, effOpts);
            if (info.present && info.value) return info.value;
          }
        }
      } catch {}
      const elId = trim(getAttr(el, 'id'));
      if (elId) {
        const entry = __lookupLabelForId(elId, __getNameOptsKey(effOpts));
        if (entry && entry.exists && entry.text) return entry.text;
      }

      const valueLike = __getElementValueLikeName(el);
      if (valueLike) return valueLike;

      const contentInfo = getContentNameInfo(el, _ctx, effOpts);
      if (contentInfo && contentInfo.present && contentInfo.value) return contentInfo.value;

      const title = trim(getAttr(el, 'title'));
      if (title) return title;

      return '';
    } finally {
      __nameComputationDepth -= 1;
    }
  }

  function getTextFromIdRefs(idrefString, _ctx, opts) {
    const r = resolveIdRefs(idrefString, _ctx, opts);
    const texts = [];
    // Reuse an in-flight cycle guard when one was threaded in via
    // opts.__idrefVisited (see computeIdRefTargetTextAlternative's own
    // comment on why this matters) rather than always starting fresh --
    // a fresh Set here can't see a target already being resolved higher
    // up the SAME chain, letting a cycle slip past this function's own
    // guard undetected.
    const visited = opts && opts.__idrefVisited instanceof Set ? opts.__idrefVisited : new Set();
    for (const el of r.refs) {
      try {
        const t = computeIdRefTargetTextAlternative(el, visited, _ctx, opts);
        if (t) texts.push(t);
      } catch {}
    }
    const text = trim(texts.join(' '));
    const flags = r.flags.slice(0);
    if (!text && r.refs.length) flags.push('resolved-empty-text');
    return { text, refsCount: r.refs.length, missing: r.missing.slice(0), flags };
  }

  function isIdRefEligibleTarget(node) {
    // IDREF policy: include hidden/aria-hidden/collapsed targets,
    // exclude only inertness or non-composed.
    if (!isElement(node)) return { eligible: false, reasons: ['notElement'] };

    // NOTE: `root` is not an eligibility boundary for IDREF targets.

    if (hasBlockingInert(node)) return { eligible: false, reasons: ['inert'] };

    return { eligible: true, reasons: [] };
  }

  function getTextFromIdRefsIdrefEligible(idrefString, _ctx, opts) {
    const r = resolveIdRefs(idrefString, _ctx, opts);

    const texts = [];
    const excluded = []; // [{ id, reasons }]
    // Same shared-cycle-guard reuse as getTextFromIdRefs above.
    const visited = opts && opts.__idrefVisited instanceof Set ? opts.__idrefVisited : new Set();
    for (const el of r.refs) {
      const elig = isIdRefEligibleTarget(el);
      if (!elig.eligible) {
        const id = trim(el.getAttribute && el.getAttribute('id'));
        excluded.push({ id: id || null, reasons: elig.reasons.slice(0) });
        continue;
      }
      try {
        const t = computeIdRefTargetTextAlternative(el, visited, _ctx, opts);
        if (t) texts.push(t);
      } catch {}
    }

    const text = trim(texts.join(' '));
    const flags = r.flags.slice(0);
    if (!text && r.refs.length) flags.push('resolved-empty-text');

    if (excluded.length) flags.push('idref-excluded');

    return {
      text,
      refsCount: r.refs.length,
      missing: r.missing.slice(0),
      excluded,
      flags
    };
  }

  // B) Accessible name / description helpers (mechanism-first, but scoped & deterministic)
  // Computes a wrapping/explicit <label>'s own text for the purpose of
  // naming ONE specific control inside it, excluding that control's own
  // subtree (matches HTML-AAM's "label text minus embedded control
  // content"). Deliberately does NOT call back into
  // getAccessibleNameInfo/getContentNameInfo for descendants — only img
  // alt (getTextAlternativeInfo), aria-label (getAriaLabelInfo), and
  // aria-labelledby (getAriaLabelledByInfo) on descendants, all of which
  // are leaf-safe with respect to <label> lookups. This is intentional:
  // getAccessibleNameInfo calls this function, and getContentNameInfo's
  // own descendant walk calls getAccessibleNameInfo — if this function
  // routed back through either of those instead, a control nested inside
  // its own naming <label> (the exact case this exists to handle) would
  // recurse forever between "what's my name" and "what's my label's
  // content."
  function getLabelSubtreeNameInfo(labelEl, excludeEl, _ctx, opts) {
    if (!isElement(labelEl)) return { present: false, value: '', mechanism: 'none', flags: [] };

    const parts = [];
    let guardCount = 0;

    function isImageLikeNode(node) {
      const tag = lower(node.tagName);
      const type = tag === 'input' ? lower(getAttr(node, 'type')) : '';
      return tag === 'img' || tag === 'area' || (tag === 'input' && type === 'image');
    }

    function walk(node) {
      if (node === excludeEl) return; // exclude the target control's own subtree
      guardCount += 1;
      if (guardCount > 5000) return;

      if (node.nodeType === 3) {
        const t = trim(node.nodeValue);
        if (t) parts.push(t);
        return;
      }
      if (!isElement(node)) return;

      let eligible;
      try {
        const r = isAccTreeEligible(node);
        eligible = !!(r && r.eligible);
      } catch {
        eligible = true;
      }
      if (!eligible) return;

      const al = getAriaLabelInfo(node);
      if (al && al.present && al.value) {
        parts.push(al.value);
        return;
      }
      const alb = getAriaLabelledByInfo(node, _ctx, opts);
      if (alb && alb.present && alb.value) {
        parts.push(alb.value);
        return;
      }

      if (isImageLikeNode(node)) {
        const alt = getTextAlternativeInfo(node, _ctx, opts);
        if (alt && alt.present && alt.value) parts.push(alt.value);
        return;
      }

      const kids = node.childNodes ? Array.from(node.childNodes) : [];
      for (const kid of kids) walk(kid);
    }

    try {
      const kids = labelEl.childNodes ? Array.from(labelEl.childNodes) : [];
      for (const kid of kids) walk(kid);
    } catch {}

    const value = trim(parts.join(' ').replace(/\s+/g, ' '));
    return { present: !!value, value, mechanism: 'label', flags: value ? [] : ['empty'] };
  }

  // ACT scopes every accessible-name rule to elements "included in the
  // accessibility tree" (c487ae link-name, 97a4e1 button-name and siblings),
  // and its glossary puts focusable aria-hidden content outside that set:
  // "Because they are hidden, these elements are considered not included in
  // the accessibility tree", even where a browser leaves them in it. The
  // defect they do represent, aria-hidden over content in the focus order,
  // belongs to aria-hidden-focus (ACT 6cfa84, WCAG 4.1.2).
  //
  // isAccTreeEligible keeps them eligible on purpose so aria-hidden-focus can
  // reach them, so naming rules need this narrower question instead.
  function isIncludedInAccessibilityTree(el) {
    const r = isAccTreeEligible(el);
    if (!r || !r.eligible) return false;
    const reasons = Array.isArray(r.reasons) ? r.reasons : [];
    return !reasons.some((x) => typeof x === 'string' && x.indexOf('ariaHiddenOverridden') === 0);
  }

  function getAccessibleNameInfo(el, _ctx, opts) {
    const flags = [];
    if (!isElement(el))
      return { present: false, value: '', mechanism: 'unsupported', flags: ['notElement'] };

    const key = __getNameOptsKey(opts);
    try {
      if (__accessibleNameCacheByKey && __accessibleNameCacheByKey.has(key)) {
        const wm = __accessibleNameCacheByKey.get(key);
        if (wm && wm instanceof WeakMap && wm.has(el)) {
          const c = wm.get(el);
          if (c && typeof c === 'object') {
            __perfInc('accessibleName.hit');
            return {
              present: !!c.present,
              value: c.value == null ? '' : String(c.value),
              mechanism: c.mechanism || 'none',
              flags: Array.isArray(c.flags) ? c.flags.slice(0) : []
            };
          }
        }
      }
    } catch {}
    __perfInc('accessibleName.miss');

    const aria = getAriaNameInfo(el, _ctx, opts);
    if (aria && aria.present && aria.value) {
      const out = {
        present: true,
        value: aria.value,
        mechanism: aria.mechanism,
        flags: flags.concat(aria.flags || [])
      };
      try {
        if (__accessibleNameCacheByKey) {
          const wm =
            __accessibleNameCacheByKey.get(key) ||
            (__accessibleNameCacheByKey.set(key, new WeakMap()),
            __accessibleNameCacheByKey.get(key));
          if (wm && wm instanceof WeakMap)
            wm.set(el, {
              present: !!out.present,
              value: out.value,
              mechanism: out.mechanism,
              flags: out.flags.slice(0)
            });
        }
      } catch {}
      return out;
    }
    if (aria && aria.flags && aria.flags.length) {
      for (const f of aria.flags) flags.push(f);
    }

    // Native <label> association via the HTML `.labels` API, which
    // resolves BOTH `<label for="...">` and wrapping `<label>...</label>`
    // in one call, for any genuinely labelable element (button, input,
    // meter, output, progress, select, textarea — `.labels` is simply
    // absent/undefined on anything else, so this never over-triggers).
    // Catches e.g. an unlabeled icon-only <button> wrapped in a <label>,
    // which the id-based lookup below misses (it only handles explicit
    // for="" and such a button has no id).
    try {
      if (el.labels && el.labels.length) {
        // Seed the IDREF cycle guard with `el` itself before walking its
        // own label: a label whose content contains a descendant
        // aria-labelledby'd back to `el` (the very control being named --
        // self-contradictory, but not invalid markup) would otherwise
        // resolve that descendant via a brand-new getTextFromIdRefs Set
        // that has no idea `el`'s own name is already mid-computation,
        // round-tripping back through this exact label once before the
        // (correctly guarded) inner resolution stops it -- doubling every
        // part of the label's text. See computeIdRefTargetTextAlternative's
        // own __idrefVisited comment for the general mechanism this reuses.
        const labelOpts = Object.assign({}, opts, { __idrefVisited: new Set([el]) });
        for (const labelEl of Array.from(el.labels)) {
          const info = getLabelSubtreeNameInfo(labelEl, el, _ctx, labelOpts);
          if (info.present && info.value) {
            const out = { present: true, value: info.value, mechanism: 'label', flags };
            try {
              if (__accessibleNameCacheByKey) {
                const wm =
                  __accessibleNameCacheByKey.get(key) ||
                  (__accessibleNameCacheByKey.set(key, new WeakMap()),
                  __accessibleNameCacheByKey.get(key));
                if (wm && wm instanceof WeakMap)
                  wm.set(el, {
                    present: true,
                    value: out.value,
                    mechanism: out.mechanism,
                    flags: out.flags.slice(0)
                  });
              }
            } catch {}
            return out;
          }
        }
      }
    } catch {}

    // Explicit <label for="..."> or wrapping <label> (common and deterministic for form controls)
    // (fallback for elements where `.labels` isn't natively available,
    // e.g. a non-native-labelable element like <div role="button" id="x">
    // still explicitly pointed at by <label for="x">.)
    const id = trim(getAttr(el, 'id'));
    if (id) {
      // Prefer indexed lookup (1 build per run) over repeated querySelector per element.
      const entry = __lookupLabelForId(id, key);
      if (entry && entry.exists) {
        const lt = entry.text || '';
        if (lt) {
          const out = { present: true, value: lt, mechanism: 'label', flags };
          try {
            if (__accessibleNameCacheByKey) {
              const wm =
                __accessibleNameCacheByKey.get(key) ||
                (__accessibleNameCacheByKey.set(key, new WeakMap()),
                __accessibleNameCacheByKey.get(key));
              if (wm && wm instanceof WeakMap)
                wm.set(el, {
                  present: true,
                  value: out.value,
                  mechanism: out.mechanism,
                  flags: out.flags.slice(0)
                });
            }
          } catch {}
          return out;
        }
        // If label exists but is empty, fall through (matches prior behavior: empty label doesn't produce a name).
      }
    }

    // POLICY NOTE (revisit if ever reconsidered): title is accepted here as
    // a last-resort accessible-name source, matching HTML-AAM/accname. This
    // is a deliberate, spec-compliant choice -- but title is a genuinely
    // weak mechanism in practice (no touch/mobile exposure, inconsistent
    // screen-reader support, no visible affordance for sighted users), and
    // this is the shared function nearly every accessible-name-dependent
    // rule in the engine goes through. Flagged here so it isn't silently
    // load-bearing.
    const title = trim(getAttr(el, 'title'));
    if (title) {
      flags.push('title-used');
      const out = { present: true, value: title, mechanism: 'title', flags };
      try {
        if (__accessibleNameCacheByKey) {
          const wm =
            __accessibleNameCacheByKey.get(key) ||
            (__accessibleNameCacheByKey.set(key, new WeakMap()),
            __accessibleNameCacheByKey.get(key));
          if (wm && wm instanceof WeakMap)
            wm.set(el, {
              present: true,
              value: out.value,
              mechanism: out.mechanism,
              flags: out.flags.slice(0)
            });
        }
      } catch {}
      return out;
    }

    const out = { present: false, value: '', mechanism: 'none', flags };
    try {
      if (__accessibleNameCacheByKey) {
        const wm =
          __accessibleNameCacheByKey.get(key) ||
          (__accessibleNameCacheByKey.set(key, new WeakMap()), __accessibleNameCacheByKey.get(key));
        if (wm && wm instanceof WeakMap)
          wm.set(el, {
            present: false,
            value: '',
            mechanism: 'none',
            flags: out.flags.slice(0)
          });
      }
    } catch {}
    return out;
  }

  function getAccessibleDescriptionInfo(el, _ctx, opts) {
    const flags = [];
    if (!isElement(el))
      return { present: false, value: '', mechanism: 'unsupported', flags: ['notElement'] };

    const key = __getDescOptsKey(opts);
    try {
      if (__accessibleDescCacheByKey && __accessibleDescCacheByKey.has(key)) {
        const wm = __accessibleDescCacheByKey.get(key);
        if (wm && wm instanceof WeakMap && wm.has(el)) {
          const c = wm.get(el);
          if (c && typeof c === 'object') {
            __perfInc('accessibleDesc.hit');
            return {
              present: !!c.present,
              value: c.value == null ? '' : String(c.value),
              mechanism: c.mechanism || 'none',
              flags: Array.isArray(c.flags) ? c.flags.slice(0) : []
            };
          }
        }
      }
    } catch {}
    __perfInc('accessibleDesc.miss');

    const describedBy = trim(getAttr(el, 'aria-describedby'));
    if (describedBy) {
      const t = getTextFromIdRefs(describedBy, _ctx, opts);
      for (const f of t.flags) flags.push(f);
      if (t.text) {
        const out = { present: true, value: t.text, mechanism: 'aria-describedby', flags };
        try {
          if (__accessibleDescCacheByKey) {
            const wm =
              __accessibleDescCacheByKey.get(key) ||
              (__accessibleDescCacheByKey.set(key, new WeakMap()),
              __accessibleDescCacheByKey.get(key));
            if (wm && wm instanceof WeakMap)
              wm.set(el, {
                present: true,
                value: out.value,
                mechanism: out.mechanism,
                flags: out.flags.slice(0)
              });
          }
        } catch {}
        return out;
      }
      flags.push('empty');
    }

    const allowTitle = !!(opts && opts.allowTitle === true);
    if (allowTitle) {
      const title = trim(getAttr(el, 'title'));
      if (title) {
        flags.push('title-used');
        const out = { present: true, value: title, mechanism: 'title', flags };
        try {
          if (__accessibleDescCacheByKey) {
            const wm =
              __accessibleDescCacheByKey.get(key) ||
              (__accessibleDescCacheByKey.set(key, new WeakMap()),
              __accessibleDescCacheByKey.get(key));
            if (wm && wm instanceof WeakMap)
              wm.set(el, {
                present: true,
                value: out.value,
                mechanism: out.mechanism,
                flags: out.flags.slice(0)
              });
          }
        } catch {}
        return out;
      }
    }

    const out = { present: false, value: '', mechanism: 'none', flags };
    try {
      if (__accessibleDescCacheByKey) {
        const wm =
          __accessibleDescCacheByKey.get(key) ||
          (__accessibleDescCacheByKey.set(key, new WeakMap()), __accessibleDescCacheByKey.get(key));
        if (wm && wm instanceof WeakMap)
          wm.set(el, {
            present: false,
            value: '',
            mechanism: 'none',
            flags: out.flags.slice(0)
          });
      }
    } catch {}
    return out;
  }

  // <canvas> fallback content is the element's *children*, not just its
  // rendered text — a documented HTML5 technique is an equivalent <img
  // alt="..."> (or similarly self-describing element) inside <canvas>.
  // textContent alone misses that, since alt text isn't part of it.
  function __hasMeaningfulCanvasFallbackDescendant(container) {
    try {
      if (!container || !container.querySelectorAll) return false;

      const imgs = container.querySelectorAll('img[alt]');
      for (const img of imgs) {
        if (trim(img.getAttribute && img.getAttribute('alt'))) return true;
      }

      const areas = container.querySelectorAll('area[alt]');
      for (const area of areas) {
        if (trim(area.getAttribute && area.getAttribute('alt'))) return true;
      }

      const named = container.querySelectorAll('[aria-label]');
      for (const n of named) {
        if (trim(n.getAttribute && n.getAttribute('aria-label'))) return true;
      }

      return false;
    } catch {
      return false;
    }
  }

  // C) Text alternative helper (mechanism-aware by element/type)
  function getTextAlternativeInfo(el, _ctx, opts) {
    const flags = [];
    if (!isElement(el)) {
      return {
        present: false,
        value: '',
        mechanism: 'unsupported',
        requiredMechanism: 'unknown',
        flags: ['notElement']
      };
    }

    const tag = lower(el.tagName);
    const type = tag === 'input' ? lower(getAttr(el, 'type')) : '';

    const isImageLike = tag === 'img' || tag === 'area' || (tag === 'input' && type === 'image');

    if (isImageLike) {
      const altRaw = getAttr(el, 'alt');
      const altPresent = altRaw != null;
      const altText = trim(altRaw);

      if (altPresent) {
        if (!altText) flags.push('alt-empty');
        return {
          present: true,
          value: altText,
          mechanism: 'alt',
          requiredMechanism: 'alt',
          flags
        };
      }

      // Missing alt is a real issue even if an accessible name exists.
      const name = getAccessibleNameInfo(el, _ctx, opts);
      if (name && name.present && name.value) flags.push('name-present-but-alt-missing');
      flags.push('alt-missing');

      return {
        present: false,
        value: name && name.present ? name.value || '' : '',
        mechanism: name && name.present ? 'accessible-name' : 'none',
        requiredMechanism: 'alt',
        flags: flags.concat(name && name.flags ? name.flags.slice(0) : [])
      };
    }

    if (tag === 'canvas') {
      const fallbackText = trim(el.textContent || '');
      if (fallbackText || __hasMeaningfulCanvasFallbackDescendant(el)) {
        return {
          present: true,
          value: fallbackText || 'fallback-content',
          mechanism: 'canvas-fallback',
          requiredMechanism: 'fallback-or-name',
          flags
        };
      }

      // <canvas> is not a labelable element (no browser computes an
      // accessible name from <label for="...">), so only ARIA naming
      // (and title, as a generic last-resort accname source) count —
      // unlike getAccessibleNameInfo, which also accepts native
      // <label> associations.
      const aria = getAriaNameInfo(el, _ctx, opts);
      if (aria && aria.present && aria.value) {
        return {
          present: true,
          value: aria.value,
          mechanism: aria.mechanism || 'aria',
          requiredMechanism: 'fallback-or-name',
          flags: flags.concat(aria.flags ? aria.flags.slice(0) : [])
        };
      }

      const title = trim(getAttr(el, 'title'));
      if (title) {
        return {
          present: true,
          value: title,
          mechanism: 'title',
          requiredMechanism: 'fallback-or-name',
          flags: flags.concat(['title-used'])
        };
      }

      return {
        present: false,
        value: '',
        mechanism: 'none',
        requiredMechanism: 'fallback-or-name',
        flags: flags.concat(aria && aria.flags ? aria.flags.slice(0) : [])
      };
    }

    return {
      present: false,
      value: '',
      mechanism: 'unsupported',
      requiredMechanism: 'unknown',
      flags: ['unsupported-element']
    };
  }

  // C.1) "Name from content" — recursive accname-aligned content-name computation.
  //
  // Rationale: the accname spec's "name from content" step (2F) is recursive —
  // for each child node, use that CHILD's own accessible name if it has one
  // (aria-label/aria-labelledby/native <label>/title, or `alt` for image-like
  // elements) rather than only concatenating literal text nodes. A naive
  // TreeWalker(SHOW_TEXT)-only walk misses any descendant that gets its
  // name from an attribute rather than visible text, e.g. a logo link
  // `<a href="..."><img alt="Company Name"></a>` or an icon-only button
  // `<button><span role="img" aria-label="Close"></span></button>`.
  function getContentNameInfo(el, _ctx, opts) {
    const flags = [];
    if (!isElement(el))
      return { present: false, value: '', mechanism: 'unsupported', flags: ['notElement'] };

    // Shares __nameComputationDepth with computeIdRefTargetTextAlternative
    // — see that function's header comment for why a single per-call
    // guard isn't enough on its own.
    if (__nameComputationDepth >= __NAME_COMPUTATION_MAX_DEPTH) {
      return { present: false, value: '', mechanism: 'none', flags: ['depth-limit'] };
    }

    const maxNodes =
      opts && Number.isFinite(opts.maxContentNodes) ? Math.max(1, opts.maxContentNodes) : 5000;
    let visitedCount = 0;
    let truncated = false;

    function isImageLikeNode(node) {
      const tag = lower(node.tagName);
      const type = tag === 'input' ? lower(getAttr(node, 'type')) : '';
      return tag === 'img' || tag === 'area' || (tag === 'input' && type === 'image');
    }

    function collect(node, parts) {
      if (truncated) return;
      visitedCount += 1;
      if (visitedCount > maxNodes) {
        truncated = true;
        if (flags.indexOf('truncated') === -1) flags.push('truncated');
        return;
      }

      if (node.nodeType === 3) {
        const t = trim(node.nodeValue);
        if (t) parts.push(t);
        return;
      }

      if (!isElement(node)) return;

      // Skip anything not exposed to the accessibility tree (hidden,
      // aria-hidden, display:none, inert, etc.) — same scope as
      // isAccTreeEligible, so a hidden descendant never contributes.
      //
      // Exception: opts.includeHidden (set by computeIdRefTargetTextAlternative
      // when the aria-labelledby/aria-describedby TARGET itself is hidden)
      // skips this check entirely except for genuinely non-rendered tags.
      // Per the accname spec, a directly-referenced target's own hidden
      // state doesn't block name computation, and that bypass covers the
      // target's whole subtree, not just the target element. Without it,
      // several elements labelled by distinct-but-hidden targets would all
      // resolve to unnamed and collapse into one false "not unique" cluster
      // (landmark-unique, dialog/tab/menuitem-name-present, etc.).
      if (opts && opts.includeHidden) {
        const tag = lower(node.tagName);
        if (tag === 'script' || tag === 'style' || tag === 'noscript' || tag === 'template') return;
      } else {
        let eligible;
        try {
          const eligRes = isAccTreeEligible(node);
          eligible = !!(eligRes && eligRes.eligible);
        } catch {
          eligible = true;
        }
        if (!eligible) return;
      }

      if (isImageLikeNode(node)) {
        // aria-labelledby/aria-label take priority over alt per the
        // accname spec (HTML-AAM), so they're checked first: an
        // <img alt="" aria-labelledby="..."> must contribute the
        // referenced text, not nothing, to its parent's content name.
        //
        // Deliberately uses getAriaNameInfo (aria only), NOT the general
        // getAccessibleNameInfo — the latter falls back to a native
        // <label>/title, which for an image-like descendant must rank
        // BELOW alt, not above it (otherwise an image's title tooltip would
        // win over its real alt text).
        const ariaName = getAriaNameInfo(node, _ctx, opts);
        if (ariaName && ariaName.present && ariaName.value) {
          parts.push(ariaName.value);
          if (flags.indexOf('descendant-name-used:image-aria') === -1)
            flags.push('descendant-name-used:image-aria');
          return;
        }

        // input[type=image] (unlike img/area) is a genuinely
        // labelable form control -- a native <label> association
        // still outranks its alt attribute per accname's
        // element-specific name mapping, so it's checked here,
        // ahead of alt/title, same relative order getAccessibleNameInfo
        // itself uses for every other labelable control.
        if (lower(node.tagName) === 'input') {
          try {
            if (node.labels && node.labels.length) {
              for (const labelEl of Array.from(node.labels)) {
                const labelInfo = getLabelSubtreeNameInfo(labelEl, node, _ctx, opts);
                if (labelInfo.present && labelInfo.value) {
                  parts.push(labelInfo.value);
                  if (flags.indexOf('descendant-name-used:image-label') === -1)
                    flags.push('descendant-name-used:image-label');
                  return;
                }
              }
            }
          } catch {}
        }

        // alt (even an explicit alt="", a deliberate "decorative,
        // contributes nothing" marker) always outranks title.
        // getTextAlternativeInfo already encodes exactly that
        // precedence: `present` distinguishes a real (possibly
        // empty) alt attribute -- terminal, contributes its
        // (possibly empty) value and nothing else -- from one
        // that's structurally absent, where `value` already
        // carries getTextAlternativeInfo's own title fallback.
        const alt = getTextAlternativeInfo(node, _ctx, opts);
        if (alt && alt.value) {
          const usedFlag = alt.present
            ? 'descendant-alt-used'
            : 'descendant-name-used:image-title-fallback';
          parts.push(alt.value);
          if (flags.indexOf(usedFlag) === -1) flags.push(usedFlag);
        }
        return; // image-like elements have no meaningful children to recurse into
      }

      const ownName = getAccessibleNameInfo(node, _ctx, opts);
      // A descendant's own name outranks its content for real naming
      // mechanisms (aria-label/aria-labelledby, a native <label>), but not for
      // a bare title: accname orders name-from-content ahead of the title
      // fallback, so a descendant's title may only speak for it when its
      // content is empty. Same precedence the image-like branch above applies
      // to alt; without it, a tooltip silently replaces the visible text of
      // anything named from its content.
      const titleOnlyName = !!(
        ownName &&
        ownName.present &&
        ownName.value &&
        ownName.mechanism === 'title'
      );

      if (ownName && ownName.present && ownName.value && !titleOnlyName) {
        parts.push(ownName.value);
        const tag = `descendant-name-used:${ownName.mechanism || 'unknown'}`;
        if (flags.indexOf(tag) === -1) flags.push(tag);
        return; // this descendant speaks for itself; don't also use its content
      }

      if (titleOnlyName) {
        // Content first; keep the title only if the content contributed
        // nothing. Compared on the trimmed join rather than parts.length,
        // since a whitespace-only text node pushes a part but no name text.
        const before = parts.length;
        walkChildren(node, parts);
        if (!trim(parts.slice(before).join(' '))) {
          parts.length = before;
          parts.push(ownName.value);
          if (flags.indexOf('descendant-name-used:title-fallback') === -1)
            flags.push('descendant-name-used:title-fallback');
        } else if (flags.indexOf('descendant-title-superseded-by-content') === -1) {
          flags.push('descendant-title-superseded-by-content');
        }
        return;
      }

      walkChildren(node, parts);
    }

    // Extracted from collect() so the title-only branch above can walk a
    // descendant's children and then decide whether the title was needed,
    // without duplicating the <slot> handling.
    function walkChildren(node, parts) {
      // A <slot>'s own childNodes are its FALLBACK content only —
      // rendered solely when nothing is assigned to it. When real content
      // IS distributed into it, that's what's exposed to the accessibility
      // tree, and it lives elsewhere in the light DOM, not as this node's
      // children — so prefer assignedNodes() and fall back to childNodes.
      if (lower(node.tagName) === 'slot' && typeof node.assignedNodes === 'function') {
        let assigned;
        try {
          assigned = node.assignedNodes({ flatten: true }) || [];
        } catch {
          assigned = [];
        }
        const kids = assigned.length
          ? assigned
          : node.childNodes
            ? Array.from(node.childNodes)
            : [];
        for (const kid of kids) {
          collect(kid, parts);
          if (truncated) break;
        }
        return;
      }

      const kids = node.childNodes ? Array.from(node.childNodes) : [];
      for (const kid of kids) {
        collect(kid, parts);
        if (truncated) break;
      }
    }

    const parts = [];
    __nameComputationDepth += 1;
    try {
      const topKids = el.childNodes ? Array.from(el.childNodes) : [];
      for (const kid of topKids) {
        collect(kid, parts);
        if (truncated) break;
      }
    } finally {
      __nameComputationDepth -= 1;
    }

    const value = trim(parts.join(' ').replace(/\s+/g, ' '));
    return {
      present: !!value,
      value,
      mechanism: value ? 'content' : 'none',
      flags
    };
  }

  // D) Role + focusability helpers
  function getRoleInfo(el, _ctx, opts) {
    const flags = [];
    if (!isElement(el)) return { role: '', source: 'none', flags: ['notElement'] };

    const explicit = trim(getAttr(el, 'role'));
    if (explicit) {
      const v = explicit;
      const low = v.toLowerCase();
      if (low === 'presentation' || low === 'none') flags.push('presentation');
      // Minimal sanity: role token should not contain spaces beyond role list; keep deterministic
      if (/\s/.test(v)) flags.push('multiple-roles');
      return { role: v, source: 'explicit', flags };
    }

    const allowImplicit = !(opts && opts.disallowImplicit === true);
    if (!allowImplicit) return { role: '', source: 'none', flags };

    const tag = lower(el.tagName);
    const type = tag === 'input' ? lower(getAttr(el, 'type')) : '';
    const href = tag === 'a' || tag === 'area' ? trim(getAttr(el, 'href')) : '';

    // Minimal implicit mapping (expand later if needed, but keep stable and small).
    let role = '';
    if ((tag === 'a' || tag === 'area') && href) role = 'link';
    else if (tag === 'button') role = 'button';
    else if (tag === 'summary') role = 'button';
    else if (tag === 'input') {
      if (type === 'checkbox') role = 'checkbox';
      else if (type === 'radio') role = 'radio';
      else if (type === 'range') role = 'slider';
      else if (type === 'button' || type === 'submit' || type === 'reset' || type === 'image')
        role = 'button';
      else if (type !== 'hidden') role = 'textbox';
    } else if (tag === 'select') role = 'combobox';
    else if (tag === 'textarea') role = 'textbox';

    if (role) return { role, source: 'implicit', flags };
    return { role: '', source: 'none', flags };
  }

  function getFocusableInfo(el, _ctx, opts) {
    // Allocation-minimal merge: avoid chained concat() which creates intermediate arrays.
    if (!isElement(el))
      return { focusable: false, tabbable: false, mechanism: 'none', flags: ['notElement'] };

    const pf = getPlatformFocusability(el); // returns focusable + tabbable + mechanism + flags

    // Merge flags deterministically (stable order: local flags, then pf.flags)
    const outFlags = [];
    // (No local flags today; keep structure for forward compatibility without extra allocations.)
    if (pf && Array.isArray(pf.flags) && pf.flags.length) {
      for (let i = 0; i < pf.flags.length; i++) outFlags.push(pf.flags[i]);
    }

    return {
      focusable: !!(pf && pf.focusable),
      tabbable: !!(pf && pf.tabbable),
      mechanism: (pf && pf.mechanism) || 'none',
      flags: outFlags
    };
  }

  function getVisibilityHintsInfo(el, _ctx, opts) {
    // Deterministic, style-only visibility hints for triage.
    // Does NOT decide eligibility; checks decide outcomes.
    // Uses computedStyle() which is already scope-cached.

    if (!isElement(el)) return { hints: [], metrics: {}, flags: ['notElement'] };

    // Cache per element per run
    try {
      if (__visibilityHintsCache && __visibilityHintsCache.has(el)) {
        __perfInc('visibilityHints.hit');
        const c = __visibilityHintsCache.get(el);
        if (c && typeof c === 'object') {
          return {
            hints: Array.isArray(c.hints) ? c.hints.slice(0) : [],
            metrics: c.metrics && typeof c.metrics === 'object' ? { ...c.metrics } : {},
            flags: Array.isArray(c.flags) ? c.flags.slice(0) : []
          };
        }
      }
    } catch {
      // ignore
    }

    __perfInc('visibilityHints.miss');

    const hints = [];
    const metrics = {};
    const flags = [];

    const cs = computedStyle(el) || {};

    // opacity:0
    try {
      const raw = cs.opacity != null ? String(cs.opacity).trim() : '';
      const op = raw ? Number.parseFloat(raw) : 1;
      if (Number.isFinite(op)) metrics.opacity = op;
      if (Number.isFinite(op) && op <= 0.0001) hints.push('opacityZero');
    } catch {
      flags.push('opacity-parse-failed');
    }

    // clip / clip-path
    try {
      const clip = cs.clip != null ? String(cs.clip).trim() : '';
      const clipPath = cs.clipPath != null ? String(cs.clipPath).trim() : '';

      const clipLow = clip.toLowerCase();
      const clipPathLow = clipPath.toLowerCase();

      if (clipLow && clipLow !== 'auto') {
        // Detect common visually-hidden: rect(0,0,0,0)
        const norm = clipLow.replace(/\s+/g, '');
        if (norm.indexOf('rect(') !== -1 && norm.indexOf('rect(0') !== -1) hints.push('clipped');
      }

      if (clipPathLow && clipPathLow !== 'none') {
        // Detect common visually-hidden: inset(50%) / inset(100%)
        if (
          clipPathLow.indexOf('inset(') !== -1 &&
          (clipPathLow.indexOf('50%') !== -1 || clipPathLow.indexOf('100%') !== -1)
        ) {
          hints.push('clipped');
        }
      }

      if (clip) metrics.clip = clip;
      if (clipPath) metrics.clipPath = clipPath;
    } catch {
      flags.push('clip-parse-failed');
    }

    // zero-size + overflow hidden/clip
    try {
      const wv = cs.width != null ? String(cs.width).trim() : '';
      const hv = cs.height != null ? String(cs.height).trim() : '';
      const ov = cs.overflow != null ? String(cs.overflow).trim().toLowerCase() : '';

      metrics.width = wv || null;
      metrics.height = hv || null;
      metrics.overflow = ov || null;

      const isZeroW = wv === '0px' || wv === '0';
      const isZeroH = hv === '0px' || hv === '0';
      const hidesOverflow = ov === 'hidden' || ov === 'clip';
      if ((isZeroW || isZeroH) && hidesOverflow) hints.push('zeroSizeOverflowHidden');
    } catch {
      flags.push('size-parse-failed');
    }

    // offscreen heuristic (string-based; no geometry)
    try {
      const pos = cs.position != null ? String(cs.position).trim().toLowerCase() : '';
      const left = cs.left != null ? String(cs.left).trim().toLowerCase() : '';
      const top = cs.top != null ? String(cs.top).trim().toLowerCase() : '';
      const ti = cs.textIndent != null ? String(cs.textIndent).trim().toLowerCase() : '';

      metrics.position = pos || null;
      metrics.left = left || null;
      metrics.top = top || null;
      metrics.textIndent = ti || null;

      const parsePx = (s) => {
        if (!s || s === 'auto') return null;
        const m = String(s).match(/-?\d+(\.\d+)?/);
        if (!m) return null;
        const n = Number.parseFloat(m[0]);
        return Number.isFinite(n) ? n : null;
      };

      const l = parsePx(left);
      const t = parsePx(top);
      const ind = parsePx(ti);

      if (pos === 'absolute' || pos === 'fixed') {
        if ((l != null && l <= -5000) || (t != null && t <= -5000)) hints.push('offscreen');
      }
      if (ind != null && ind <= -5000) hints.push('offscreen');
    } catch {
      flags.push('offscreen-parse-failed');
    }

    // Dedupe hints, stable order
    const order = ['opacityZero', 'offscreen', 'clipped', 'zeroSizeOverflowHidden'];
    const seen = new Set();
    const stable = [];
    for (const k of order) {
      if (hints.indexOf(k) !== -1 && !seen.has(k)) {
        seen.add(k);
        stable.push(k);
      }
    }

    const out = { hints: stable, metrics, flags };

    try {
      if (__visibilityHintsCache) {
        __visibilityHintsCache.set(el, {
          hints: stable.slice(0),
          metrics: { ...metrics },
          flags: flags.slice(0)
        });
      }
    } catch {
      __perfInc('visibilityHints.nocache');
    }

    return out;
  }

  // Back-compat: keep existing helper but implement via new name helper.
  function hasAccessibleName(el) {
    const info = getAccessibleNameInfo(el);
    return !!(info && info.present && trim(info.value));
  }

  function createSelectorUniqIndex() {
    const idCount = new Map();
    const testIdCount = new Map(); // data-testid + data-test + data-cy + data-qa
    const nameCount = new Map(); // key: tag|name
    const ariaLabelCount = new Map(); // key: tag|aria-label
    const roleAriaLabelCount = new Map(); // key: role|aria-label

    const sel = '[id],[data-testid],[data-test],[data-cy],[data-qa],[name],[aria-label],[role]';
    let nodes;
    if (typeof queryAllSmart === 'function') {
      nodes = queryAllSmart(sel) || [];
    } else {
      // Defensive fallback (queryAllSmart is always defined in this
      // module, so this branch is not expected to run) -- loop every
      // resolved root rather than assuming a single scope element.
      nodes = [];
      const seen = new Set();
      for (const r of roots) {
        if (!r || !r.querySelectorAll) continue;
        for (const el of r.querySelectorAll(sel)) {
          if (el && !seen.has(el)) {
            seen.add(el);
            nodes.push(el);
          }
        }
      }
      if (!nodes.length && !roots.length && document) {
        nodes = Array.from(document.querySelectorAll(sel));
      }
    }

    const inc = (map, key) => map.set(key, (map.get(key) || 0) + 1);

    for (const el of nodes) {
      if (!el || el.nodeType !== 1) continue;

      const tag = (el.tagName || '').toLowerCase();

      const elementId = el.getAttribute('id');
      if (elementId && elementId.trim()) inc(idCount, elementId.trim());

      for (const a of ['data-testid', 'data-test', 'data-cy', 'data-qa']) {
        const v = el.getAttribute(a);
        if (v && v.trim()) inc(testIdCount, a + '=' + v.trim());
      }

      const name = el.getAttribute('name');
      if (name && name.trim() && tag) inc(nameCount, tag + '|' + name.trim());

      const aria = el.getAttribute('aria-label');
      if (aria && aria.trim() && tag) inc(ariaLabelCount, tag + '|' + aria.trim());

      const role = el.getAttribute('role');
      if (role && role.trim() && aria && aria.trim()) {
        inc(roleAriaLabelCount, role.trim() + '|' + aria.trim());
      }
    }

    return { idCount, testIdCount, nameCount, ariaLabelCount, roleAriaLabelCount };
  }

  function buildSimpleSelector(el, fallbackTag) {
    try {
      if (!el || el.nodeType !== 1) return fallbackTag || 'html';

      const tag = (el.tagName || fallbackTag || 'html').toLowerCase();

      const cssEscapeIdent = __cssEscapeIdent;

      const escapeAttrValue = __escapeAttrValue;

      // Same trimmed-check / raw-value-embed split as buildSelectorUncached's
      // anchor builders (see that function's header comment): a CSS
      // attribute/ID selector must match the DOM attribute's real,
      // untrimmed value exactly, so only the truthiness check may trim.
      const elementId = el.getAttribute && el.getAttribute('id');
      if (elementId && elementId.trim()) return '#' + cssEscapeIdent(elementId);

      for (const a of ['data-testid', 'data-test', 'data-cy', 'data-qa']) {
        const v = el.getAttribute && el.getAttribute(a);
        if (v && v.trim()) return '[' + a + '="' + escapeAttrValue(v) + '"]';
      }

      const name = el.getAttribute && el.getAttribute('name');
      if (name && name.trim()) return tag + '[name="' + escapeAttrValue(name) + '"]';

      return tag;
    } catch {
      return fallbackTag || 'html';
    }
  }

  function getUniqIndex() {
    const scopeObj = __getScopeObj();
    if (!scopeObj || !__uniqIndexByScope) {
      __perfInc('uniqIndex.nocache');
      // Fallback: build per call (should be rare; determinism preserved)
      return createSelectorUniqIndex();
    }

    // Partitioned by __selectorOptsKey: the index's counts depend on
    // includeShadowDom/excludeSelectors (via queryAllSmart), so a scope
    // reused across runs with different options must not share indices.
    let perScope;
    try {
      perScope = __uniqIndexByScope.get(scopeObj);
      if (!(perScope instanceof Map)) {
        perScope = new Map();
        __uniqIndexByScope.set(scopeObj, perScope);
      }
    } catch {
      __perfInc('uniqIndex.nocache');
      return createSelectorUniqIndex();
    }

    const key = __getSelectorOptsKey();
    const cached = perScope.get(key);
    if (cached) {
      __perfInc('uniqIndex.hit');
      return cached;
    }

    __perfInc('uniqIndex.miss');
    const idx = createSelectorUniqIndex();
    try {
      perScope.set(key, idx);
    } catch {
      /* ignore */
    }
    __perfInc('uniqIndex.build');
    return idx;
  }

  function buildSelectorUncached(el) {
    const escapeAttrValue = __escapeAttrValue;
    try {
      if (!el || el.nodeType !== 1) return 'html';

      const cssEscape = __cssEscapeIdent;

      const idx = getUniqIndex();
      const tag = (el.tagName || '').toLowerCase();

      // NOTE: every anchor builder below keys its uniqueness-index lookup on
      // the *trimmed* attribute value (matching how the index itself was
      // built in createSelectorUniqIndex, so whitespace-only differences
      // don't spuriously count as "different" values) but embeds the *raw,
      // untrimmed* attribute value in the actual CSS selector string. A CSS
      // attribute selector (`[attr="..."]`) requires an exact match against
      // the real DOM attribute -- trimming the embedded value while the
      // real attribute keeps its whitespace produces a selector that can
      // never match -- e.g. on Slack's real homepage, several
      // `role="region"` promo cards have a templated `aria-label` ending
      // in a trailing ", " (a
      // string-concatenation artifact, not a typo), which made every one of
      // these anchor builders silently construct a non-matching selector,
      // falling through to the document-wide non-unique `buildSimpleSelector`
      // tag-only fallback (e.g. plain "header") for elements that actually
      // had a perfectly good, unique aria-label anchor available -- and any
      // downstream consumer re-resolving that bare-tag selector via
      // `querySelector` (this comparisons project's own tooling included)
      // silently gets the *wrong* element instead of an error.
      const uniqueIdSel = () => {
        const elementId = el.getAttribute('id');
        if (!elementId || !elementId.trim()) return null;
        if (idx && (idx.idCount.get(elementId.trim()) || 0) === 1)
          return '#' + cssEscape(elementId);
        return null;
      };

      const uniqueTestSel = () => {
        for (const a of ['data-testid', 'data-test', 'data-cy', 'data-qa']) {
          const v = el.getAttribute(a);
          if (!v || !v.trim()) continue;
          const key = a + '=' + v.trim();
          if (idx && (idx.testIdCount.get(key) || 0) === 1) {
            return '[' + a + '="' + escapeAttrValue(v) + '"]';
          }
        }
        return null;
      };

      const uniqueNameSel = () => {
        const v = el.getAttribute('name');
        if (!v || !v.trim() || !tag) return null;
        const key = tag + '|' + v.trim();
        if (idx && (idx.nameCount.get(key) || 0) === 1)
          return tag + '[name="' + escapeAttrValue(v) + '"]';
        return null;
      };

      const uniqueAriaSel = () => {
        const v = el.getAttribute('aria-label');
        if (!v || !v.trim() || !tag) return null;
        const key = tag + '|' + v.trim();
        if (idx && (idx.ariaLabelCount.get(key) || 0) === 1)
          return tag + '[aria-label="' + escapeAttrValue(v) + '"]';
        return null;
      };

      const uniqueRoleAriaSel = () => {
        const role = el.getAttribute('role');
        const aria = el.getAttribute('aria-label');
        if (!role || !role.trim() || !aria || !aria.trim()) return null;
        const key = role.trim() + '|' + aria.trim();
        if (idx && (idx.roleAriaLabelCount.get(key) || 0) === 1) {
          return (
            '[role="' + escapeAttrValue(role) + '"][aria-label="' + escapeAttrValue(aria) + '"]'
          );
        }
        return null;
      };

      const direct =
        uniqueIdSel() ||
        uniqueTestSel() ||
        uniqueRoleAriaSel() ||
        uniqueNameSel() ||
        uniqueAriaSel();

      if (direct) return direct;

      const parts = [];

      function nthOfType(node) {
        const t = (node.tagName || '').toLowerCase() || '*';
        const p = node.parentElement;
        if (!p) return t;

        let i = 1;
        let sib = node.previousElementSibling;
        while (sib) {
          if ((sib.tagName || '').toLowerCase() === t) i++;
          sib = sib.previousElementSibling;
        }

        // A same-tag sibling before this node (i > 1) already means
        // an unqualified tag selector would be ambiguous — no need
        // to also scan forward in that case. Only scan
        // nextElementSibling when this node is the first of its tag
        // among its siblings, to catch the case where the
        // disambiguating sibling comes after it instead.
        let hasSame = i > 1;
        if (!hasSame) {
          sib = node.nextElementSibling;
          while (sib) {
            if ((sib.tagName || '').toLowerCase() === t) {
              hasSame = true;
              break;
            }
            sib = sib.nextElementSibling;
          }
        }
        return hasSame ? t + ':nth-of-type(' + i + ')' : t;
      }

      let node = el;
      let safety = 0;

      // Only apply the "stop climbing once we reach a contextSelector-
      // matched root" shortcut when there's a single (or no) matched
      // root -- resolveContextRoots() falls back to `[documentElement]`
      // when no contextSelector is given, so this is the overwhelmingly
      // common case and behaves exactly as before.
      //
      // With MULTIPLE matched roots (multi-region contextSelector
      // scans), stopping there without recording anything about which
      // root produced an ambiguous, non-unique selector string for two
      // structurally-identical regions is a real bug -- e.g. two
      // wrapper <div>s, each containing two identical ".widget"
      // sections scanned via `contextSelector: '.widget'`, produced the
      // *same* selector string ("section:nth-of-type(1) > div > div >
      // button") for the equivalent button in each wrapper --
      // resolving to 2 elements instead of 1 when queried, and pointing
      // at the wrong one for at least one of the two occurrences. The
      // existing `el.matches(candidate)` safety check below couldn't
      // catch this: it only verifies THIS element matches the string,
      // never that the string is unique document-wide.
      //
      // Fix: when multiple roots are in play, don't stop early --
      // keep climbing (same as the always-correct no-contextSelector
      // path) until finding a genuinely unique anchor or reaching the
      // true document root, which is always singular. That restores
      // the invariant the final safety-check comment below relies on,
      // rather than needing a separate (more expensive) document-wide
      // uniqueness re-check.
      const stopAtMatchedRoot = roots.length <= 1;

      while (node && node.nodeType === 1 && safety++ < 20) {
        let anchor = null;

        if (node !== el) {
          const t = (node.tagName || '').toLowerCase();
          // Same trimmed-key-lookup / raw-value-embed split as the direct
          // anchor builders above -- see this function's header comment.
          const id = node.getAttribute('id');
          if (id && id.trim() && idx && (idx.idCount.get(id.trim()) || 0) === 1)
            anchor = '#' + cssEscape(id);
          if (!anchor) {
            for (const a of ['data-testid', 'data-test', 'data-cy', 'data-qa']) {
              const v = node.getAttribute(a);
              if (v && v.trim() && idx && (idx.testIdCount.get(a + '=' + v.trim()) || 0) === 1) {
                anchor = '[' + a + '="' + escapeAttrValue(v) + '"]';
                break;
              }
            }
          }
          if (!anchor) {
            const name = node.getAttribute('name');
            if (
              name &&
              name.trim() &&
              t &&
              idx &&
              (idx.nameCount.get(t + '|' + name.trim()) || 0) === 1
            ) {
              anchor = t + '[name="' + escapeAttrValue(name) + '"]';
            }
          }
          if (!anchor) {
            const aria = node.getAttribute('aria-label');
            if (
              aria &&
              aria.trim() &&
              t &&
              idx &&
              (idx.ariaLabelCount.get(t + '|' + aria.trim()) || 0) === 1
            ) {
              anchor = t + '[aria-label="' + escapeAttrValue(aria) + '"]';
            }
          }
        }

        if (node === el) {
          parts.unshift(nthOfType(node));
        } else if (anchor) {
          parts.unshift(anchor);
          break;
        } else {
          parts.unshift(nthOfType(node));
        }

        if (!node.parentElement || (stopAtMatchedRoot && roots.includes(node))) break;
        node = node.parentElement;
      }

      const candidate = parts.join(' > ') || tag || 'html';

      // Verify the constructed selector string actually resolves to
      // `el` per the CSS engine's own semantics — a real safety net,
      // since some selector engines (observed in jsdom) disagree with
      // this function's own :nth-of-type sibling counting in edge
      // cases. `el.matches(candidate)` checks exactly that (does the
      // engine agree this element satisfies the string we built) at a
      // cost bounded by el's own ancestor-chain depth.
      //
      // This intentionally does NOT re-verify global uniqueness via a
      // whole-document query: every path segment above pins an exact
      // position relative to its own parent via `>` (child, not
      // descendant) combinators, so a correctly-matching chain can
      // only resolve to one element short of a malformed document
      // (e.g. two <html> roots) -- true as long as the walk above
      // never stops short of a genuinely unique anchor/root, which is
      // exactly what `stopAtMatchedRoot` now guarantees (see its own
      // comment above; a multi-root contextSelector scan stopping
      // early used to violate this invariant silently). Re-deriving
      // that guarantee via a
      // document-wide :nth-of-type scan was measured to cost O(total
      // same-tag siblings) per call — pathological on pages with many
      // flat, unidentified siblings (e.g. hundreds of unlabeled
      // <img>s), while contributing no realistic additional safety.
      try {
        if (el && typeof el.matches === 'function' && el.matches(candidate)) return candidate;
      } catch {}

      return buildSimpleSelector(el, tag || 'html');
    } catch {
      return 'html';
    }
  }

  function buildSelector(el) {
    const cache = __getSelectorCacheForOpts();
    try {
      if (cache && el && typeof el === 'object' && cache.has(el)) {
        __perfInc('selector.hit');
        return cache.get(el) || 'html';
      }
    } catch {}
    __perfInc('selector.miss');
    const sel = buildSelectorUncached(el);
    try {
      if (cache && el && typeof el === 'object') cache.set(el, sel);
    } catch {}
    return sel;
  }

  // Sibling-index path from documentElement's descendants down to `el`
  // ([] if `el` IS the documentElement); null if `el` is falsy or detached
  // in a way that makes indexing impossible. A more robust element-identity
  // mechanism than a CSS selector string alone (survives some DOM changes
  // a selector wouldn't -- e.g. an id/class rename), at the cost of not
  // being usable as a real CSS selector itself. Deliberately mirrors the
  // same algorithm used by this project's (external) cross-engine
  // result-matching tooling exactly, rather than requiring it --
  // this file must stay self-contained (embedded into the generated
  // runtime via .toString(), no module requires survive that), so a
  // correctness fix to the algorithm must be applied to both copies.
  function structuralPath(el) {
    if (!el || typeof el !== 'object') return null;
    const path = [];
    let node = el;
    try {
      while (node && node.parentElement) {
        const parent = node.parentElement;
        const idx = Array.prototype.indexOf.call(parent.children, node);
        if (idx < 0) return null;
        path.unshift(idx);
        node = parent;
      }
    } catch {
      return null;
    }
    return path;
  }

  // Occurrence-level structural path: prefers the actual element reference
  // (exact, no re-resolution risk) and only falls back to re-resolving via
  // the occurrence's own selector when no element reference was kept --
  // the same technique the cross-engine live-DOM adapters already use to
  // recover an element from a reported selector, with the same accepted
  // caveat (a non-unique selector could resolve to a different element
  // than originally intended -- already documented as "structural-path
  // collisions" for the cross-engine tooling).
  function buildStructuralPath(node, selector) {
    if (node && typeof node === 'object') {
      const p = structuralPath(node);
      if (p) return p;
    }
    if (
      selector &&
      typeof selector === 'string' &&
      document &&
      typeof document.querySelector === 'function'
    ) {
      let el;
      try {
        el = document.querySelector(selector);
      } catch {
        el = null;
      }
      if (el) return structuralPath(el);
    }
    return null;
  }

  function getNonEmptyTitle(el) {
    if (!getAttributeInfo) return null;
    try {
      const info = getAttributeInfo(el, 'title');
      const v = info && info.present ? trim(info.value) : '';
      return v ? v : null;
    } catch {
      return null;
    }
  }

  function isPlaceholderCapable(el) {
    // Per HTML, `placeholder` is only a name/hint source for text-entry
    // input types and <textarea> — browsers/AT ignore it on other input
    // types (checkbox, radio, range, color, date, file, ...) and on
    // <select>, so it must not be treated as an accessible-name source
    // for those.
    try {
      if (!isElement(el)) return false;
      const tag = (el.tagName || '').toLowerCase();
      if (tag === 'textarea') return true;
      if (tag !== 'input') return false;
      const type = ((el.getAttribute && (el.getAttribute('type') || 'text')) || 'text')
        .toLowerCase()
        .trim();
      const t = type || 'text';
      return (
        t === 'text' ||
        t === 'search' ||
        t === 'tel' ||
        t === 'url' ||
        t === 'email' ||
        t === 'password' ||
        t === 'number'
      );
    } catch {
      return false;
    }
  }

  function getNonEmptyPlaceholder(el) {
    if (!getAttributeInfo) return null;
    if (!isPlaceholderCapable(el)) return null;
    try {
      const info = getAttributeInfo(el, 'placeholder');
      const v = info && info.present ? trim(info.value) : '';
      return v ? v : null;
    } catch {
      return null;
    }
  }

  // A <label> contributes a name to its associated control either via its
  // own aria-label/aria-labelledby (checked first, the usual ARIA-over-
  // content precedence — so <label aria-label="Toggle Navigation"> names
  // its control even when its only child content is aria-hidden), or,
  // failing that, its rendered content (getContentNameInfo, which excludes
  // aria-hidden/display:none/inert descendants — so a label whose only
  // text is aria-hidden gives the control no name despite the association).
  function labelContributesAccessibleName(lab) {
    try {
      const aria = getAriaNameInfo(lab, null, {});
      if (aria && aria.present && trim(aria.value)) return true;
    } catch {}
    try {
      const info = getContentNameInfo(lab, null, {});
      if (info && info.present && trim(info.value)) return true;
    } catch {
      return true; // conservative on error: don't newly fail
    }
    // A <label> with no aria-name and empty own content can still
    // contribute a name via its own title attribute -- accname's title-
    // fallback step applies to any element, the label itself included, so
    // `<label for="..." title="Search"></label>` with empty content is an
    // intentional PASS. Without this, form-control-programmatic-label-present
    // would wrongly fail it.
    try {
      return !!getNonEmptyTitle(lab);
    } catch {
      return false;
    }
  }

  function hasLabelAssociation(el) {
    // Deterministic, stable subset:
    // - <label for="id">
    // - wrapping <label> ... <input> ...
    // A structural association alone isn't enough — see
    // labelContributesAccessibleName above for what counts.
    if (!isElement(el)) return false;

    try {
      if (
        __labelAssociationCache &&
        el &&
        typeof el === 'object' &&
        __labelAssociationCache.has(el)
      ) {
        __perfInc('labelAssociation.hit');
        return !!__labelAssociationCache.get(el);
      }
    } catch {}

    __perfInc('labelAssociation.miss');
    let out = false;
    let associatedLabels = [];

    // Prefer the native `.labels` API — resolves both wrapping <label>
    // and <label for="id"> association in one call, as real elements.
    try {
      if (el && 'labels' in el && el.labels && el.labels.length) {
        associatedLabels = Array.prototype.slice.call(el.labels);
      }
    } catch {}

    if (!associatedLabels.length) {
      // Fallback for environments without a working `.labels` API:
      // structural-only (pre-existing behavior, no content check —
      // __lookupLabelForId's cache doesn't retain an element ref).
      const id = trim(getAttr(el, 'id'));
      if (id) {
        const entry = __lookupLabelForId(id, '__default__');
        if (entry && entry.exists) out = true;
      }

      if (!out && el.closest) {
        try {
          const wrap = el.closest('label');
          if (wrap && isElement(wrap)) associatedLabels = [wrap];
        } catch {}
      }
    }

    if (!out && associatedLabels.length) {
      out = associatedLabels.some(labelContributesAccessibleName);
    }

    try {
      if (__labelAssociationCache && el && typeof el === 'object')
        __labelAssociationCache.set(el, !!out);
    } catch {}

    return out;
  }

  function getLabelMethod(el, _ctx, opts) {
    // returns { method, value } where value is best-effort text, deterministically trimmed
    if (!isElement(el)) return { method: 'none', value: null };

    try {
      if (__labelMethodCache && el && typeof el === 'object' && __labelMethodCache.has(el)) {
        __perfInc('labelMethod.hit');
        const c = __labelMethodCache.get(el);
        if (c && typeof c === 'object') {
          return { method: c.method || 'none', value: c.value == null ? null : String(c.value) };
        }
      }
    } catch {}

    __perfInc('labelMethod.miss');
    let out = { method: 'none', value: null };

    if (hasLabelAssociation(el)) out = { method: 'label', value: null };
    else if (getAriaLabelledByInfo) {
      try {
        const info = getAriaLabelledByInfo(el, _ctx, { maxRefs: 8 });
        const v = info && info.present ? trim(info.value) : '';
        if (v) out = { method: 'aria-labelledby', value: v };
      } catch {}
    }

    if (out.method === 'none' && getAriaLabelInfo) {
      try {
        const info = getAriaLabelInfo(el);
        const v = info && info.present ? trim(info.value) : '';
        if (v) out = { method: 'aria-label', value: v };
      } catch {}
    }

    if (out.method === 'none') {
      const titleV = getNonEmptyTitle(el);
      if (titleV) out = { method: 'title', value: titleV };
    }

    if (out.method === 'none') {
      const phV = getNonEmptyPlaceholder(el);
      if (phV) out = { method: 'placeholder', value: phV };
    }

    try {
      if (__labelMethodCache && el && typeof el === 'object') {
        __labelMethodCache.set(el, { method: out.method, value: out.value });
      }
    } catch {}

    return out;
  }

  function getLabelStrength(method) {
    // policy choice; this is deterministic and tweakable
    if (method === 'label' || method === 'aria-labelledby') return 'strong';
    if (method === 'aria-label') return 'medium';
    if (method === 'title' || method === 'placeholder') return 'weak';
    return 'none';
  }

  function reportOccurrence(node, partial) {
    const o =
      partial && typeof partial === 'object' && !Array.isArray(partial) ? { ...partial } : {};
    // Attach the node for engine-side finalization. This must be removed later before returning results.
    o.__node = node || null;
    return o;
  }

  // Resolves the final {outcome, severity, occurrences} for a rule that
  // collects two independent confidence tiers during one run — some
  // findings are confident enough for a hard `fail`, others only warrant
  // `cantTell` (e.g. "this needs human review"). The naive approach
  // (`if (failOccurrences.length) return fail(failOccurrences); else if
  // (cantTellOccurrences.length) return cantTell(cantTellOccurrences);`)
  // silently drops every cantTell-tier finding whenever at least one
  // fail-tier finding also exists on the same page — a real information
  // loss for a real scan, not just a test artifact: a page with one
  // confident violation and five "needs review" ones would report only
  // the one. This is a recurring shape across automatic rules with a
  // fail/cantTell split (e.g. aria-prohibited-attr's roleless-naming
  // branch, aria-hidden-focus's runtime-redirect downgrade), which is why
  // it's factored into this one shared helper rather than reimplemented
  // per rule.
  // The correct behavior when a fail-tier finding exists: the overall
  // outcome is still `fail` (a real, confident violation must still gate
  // CI), but BOTH buckets' occurrences are returned together, not just
  // the fail ones — each occurrence already carries its own
  // distinguishing `data.details.reasonCode`/summary/hint, so nothing
  // about which findings were confident vs. which need review is lost;
  // only the single aggregate outcome label stays singular, which was
  // already this engine's accepted one-outcome-per-rule-run schema
  // constraint (changing that is a separate, much larger, cross-cutting
  // decision spanning report.js/baseline.js/explain.js/WCAG rollups —
  // out of scope for this helper).
  function resolveTieredOutcome(failOccurrences, cantTellOccurrences, severity) {
    function withOccurrenceTier(items, tier) {
      return items.map((occ) => {
        if (!occ || typeof occ !== 'object' || Array.isArray(occ)) return occ;
        if (occ.occurrenceOutcome === 'fail' || occ.occurrenceOutcome === 'cantTell') return occ;
        return { ...occ, occurrenceOutcome: tier };
      });
    }

    const fails = Array.isArray(failOccurrences) ? failOccurrences : [];
    const cantTells = Array.isArray(cantTellOccurrences) ? cantTellOccurrences : [];
    const failTier = withOccurrenceTier(fails, 'fail');
    const cantTellTier = withOccurrenceTier(cantTells, 'cantTell');
    if (fails.length) {
      return { outcome: 'fail', severity, occurrences: failTier.concat(cantTellTier) };
    }
    if (cantTells.length) {
      return { outcome: 'cantTell', severity, occurrences: cantTellTier };
    }
    return { outcome: 'pass', severity: 'minor', occurrences: [] };
  }

  let __contrastSharedCache = {};
  try {
    // In Node/JSDOM checks, the harness sets global.window/global.document.
    // The engine may instantiate helpers per rule without passing opts.window,
    // so we must be able to recover the stable realm window to share caches.
    const w =
      realmWindow ||
      (document && document.defaultView) ||
      (typeof global !== 'undefined' && global.window ? global.window : null);

    if (w) {
      if (!w.__a11ycoreSharedCache) w.__a11ycoreSharedCache = {};
      if (!w.__a11ycoreSharedCache.contrast) w.__a11ycoreSharedCache.contrast = {};
      __contrastSharedCache = w.__a11ycoreSharedCache.contrast;
    }
  } catch {
    __contrastSharedCache = {};
  }

  const __contrastShared = {
    trim,
    computedStyle,
    composedParent,
    buildSimpleSelector,
    __contrastSharedCache
  };

  const contrast = createContrastHelpers(
    { window: realmWindow || window, document, root: roots, includeShadowDom, excludeSelectors },
    __contrastShared
  );

  // Expose shared cache to checks (deterministic, in-memory only)
  contrast.sharedCache = __contrastShared.__contrastSharedCache;

  const aria = createAriaHelpers(
    { window: realmWindow || window, document, root: roots },
    { trim }
  );

  // For rules whose check is inherently about the WHOLE page (does the
  // page have a title? a declared language? a way to skip repeated
  // blocks?) rather than about elements found within a scanned subtree --
  // these can't be answered correctly by scoping via queryAllSmart/ctx.root
  // the way per-element checks can, since a subtree that never had (and
  // was never meant to have) e.g. its own <title> shouldn't be faulted for
  // lacking one. `false` when `fragment:true` was explicitly set, or when
  // `contextSelector` scoped this run narrower than the whole document
  // (roots doesn't include document.documentElement); `true` in the
  // default/unscoped case, so this is a no-op for the overwhelming
  // majority of existing (whole-page) scans.
  function isWholeDocumentScope() {
    if (fragment) return false;
    return roots.includes(document.documentElement);
  }

  return {
    isValidLanguageTag,
    isRegisteredLanguageSubtag,

    // Existing query/snippet utilities
    queryAll,
    queryAllDeep,
    queryAllSmart,
    getOuterHtmlSnippet,
    buildSimpleSelector,
    buildSelector,
    buildStructuralPath,

    // Existing (back-compat)
    hasAccessibleName,
    isExcluded,
    isAccTreeEligible,
    isIncludedInAccessibilityTree,
    isDomVisibleEligible,
    isWholeDocumentScope,

    // Engine-internal: sets which rule's rule-scoped excludeSelectors
    // (engineOptions.rules[ruleId].excludeSelectors) are currently in
    // effect. Called by dom-runner.js before each rule invocation, not
    // intended for use by rule implementations.
    __setActiveRuleExcludeSelectors,

    // Eligibility info wrapper
    getEligibilityInfo,

    // IDREF primitives
    resolveIdRefs,
    getTextFromIdRefs,
    getTextFromIdRefsIdrefEligible,

    // ARIA-only name primitives (new)
    getAriaLabelInfo,
    getAriaLabelledByInfo,
    getAriaNameInfo,

    // Landmark-role naming (aria-label -> aria-labelledby -> title; no content fallback --
    // see getLandmarkNameInfo's own header comment for why this replaced 7 duplicated copies)
    getLandmarkNameInfo,

    // "Does this element have a landmark-scoping ancestor" (role-aware
    // sectioning-content/<main> check backing <header>/<footer>/<aside>'s
    // conditional implicit roles) -- re-exported from aria helpers at
    // this top level, matching getLandmarkNameInfo just above, so the
    // manual landmark-check files that used to each carry their own
    // (buggy, tag-only) copy can call helpers.hasLandmarkScopingAncestor
    // directly. See aria.hasLandmarkScopingAncestor's own header comment
    // in src/core/aria-helpers.js for the full algorithm and rationale.
    hasLandmarkScopingAncestor: aria.hasLandmarkScopingAncestor,

    // Name / description
    getAccessibleNameInfo,
    getAccessibleDescriptionInfo,

    // Text alternatives
    getTextAlternativeInfo,

    // Recursive "name from content" (accname-aligned; see getContentNameInfo header comment)
    getContentNameInfo,

    // Role / focusability
    getRoleInfo,
    getFocusableInfo,
    getVisibilityHintsInfo,

    getAttributeInfo,

    getLabelMethod,
    getLabelStrength,

    // Whether a <label> carries text that names its associated control
    // (own aria-name, else rendered content, else title). Shared so
    // form-control-single-label and form-control-programmatic-label-present
    // agree on what a label is worth.
    labelContributesAccessibleName,

    // Flat-tree ancestor walk (assignedSlot-aware, then shadow host) —
    // see this function's own definition above for why assignedSlot
    // must win over parentNode.
    composedParent,

    // Perf counters (only populated when opts.perfStats === true)
    getPerfStats,
    resetPerfStats,

    reportOccurrence,
    resolveTieredOutcome,

    contrast,
    aria
  };
});

// Inlined from src/core/rule-meta.js (also used at build time by loadRuleModules
// above -- single source of truth -- and here so runtime-registered custom
// rules via engineOptions.customRules get identical meta defaulting/validation
// to build-time rules; see runCore's own customRules handling)
const normalizeRuleMeta = (function normalizeRuleMeta(ruleId, id, meta, engineTag) {
  function normalizeStringArray(value) {
    if (!Array.isArray(value)) return [];
    return value
      .map(String)
      .map((s) => s.trim())
      .filter(Boolean);
  }

  function normalizeObjectArray(value) {
    if (!Array.isArray(value)) return [];
    return value
      .filter((v) => v && typeof v === 'object' && !Array.isArray(v))
      .map((v) => ({ ...v }));
  }

  function deriveWcagScFromNormativeMappings(normativeMappings) {
    const nm = Array.isArray(normativeMappings) ? normativeMappings : [];
    const out = new Set();
    for (const m of nm) {
      if (!m || typeof m !== 'object') continue;
      if (String(m.standard || '').toUpperCase() !== 'WCAG') continue;
      const req = String(m.requirement || '').trim();
      if (req) out.add(req);
    }
    return Array.from(out).sort();
  }

  const m = meta && typeof meta === 'object' ? meta : {};

  const title = typeof m.title === 'string' && m.title.trim() ? m.title.trim() : id;
  const description = typeof m.description === 'string' ? m.description : '';
  const helpUrl = typeof m.helpUrl === 'string' ? m.helpUrl : '';

  const i18n =
    m.i18n && typeof m.i18n === 'object' && !Array.isArray(m.i18n) ? { ...m.i18n } : null;

  const tags = normalizeStringArray(m.tags).map((t) => t.toLowerCase());
  if (!tags.includes(engineTag)) tags.push(engineTag);

  const normativeMappings = normalizeObjectArray(m.normativeMappings);
  const wcagSc = deriveWcagScFromNormativeMappings(normativeMappings);
  const informativeReferences = normalizeObjectArray(m.informativeReferences);

  const defaultSeverity =
    typeof m.defaultSeverity === 'string' && m.defaultSeverity.trim()
      ? m.defaultSeverity.trim()
      : 'moderate';

  const defaultConfidence =
    typeof m.defaultConfidence === 'string' && m.defaultConfidence.trim()
      ? m.defaultConfidence.trim()
      : 'medium';

  const type = m.type === 'manual' || m.type === 'automatic' ? m.type : 'automatic';

  const coverage =
    m.coverage === null || typeof m.coverage === 'string' || typeof m.coverage === 'object'
      ? m.coverage
      : null;

  const ruleInterfaceVersion =
    typeof m.ruleInterfaceVersion === 'string' && m.ruleInterfaceVersion.trim()
      ? m.ruleInterfaceVersion.trim()
      : '1.0.0';

  const ruleVersion =
    typeof m.ruleVersion === 'string' && m.ruleVersion.trim() ? m.ruleVersion.trim() : '0.0.0';

  const normative = typeof m.normative === 'boolean' ? m.normative : true;
  const atomic = typeof m.atomic === 'boolean' ? m.atomic : true;

  // Deprecation signal for the rule catalog (see docs/API_STABILITY.md).
  // Purely informational -- a deprecated rule still runs and produces
  // results completely normally; this is a catalog-level migration signal
  // for integrators, not an automatic exclusion.
  const deprecated = typeof m.deprecated === 'boolean' ? m.deprecated : false;
  const deprecation =
    deprecated &&
    m.deprecation &&
    typeof m.deprecation === 'object' &&
    !Array.isArray(m.deprecation)
      ? {
          replacedBy:
            typeof m.deprecation.replacedBy === 'string' && m.deprecation.replacedBy.trim()
              ? m.deprecation.replacedBy.trim()
              : null,
          reason: typeof m.deprecation.reason === 'string' ? m.deprecation.reason.trim() : '',
          sinceVersion:
            typeof m.deprecation.sinceVersion === 'string' ? m.deprecation.sinceVersion.trim() : ''
        }
      : null;

  if (deprecated && (!deprecation || !deprecation.reason || !deprecation.sinceVersion)) {
    throw new Error(
      `Rule ${ruleId}: meta.deprecated:true requires meta.deprecation.reason and meta.deprecation.sinceVersion`
    );
  }

  const category = typeof m.category === 'string' && m.category.trim() ? m.category.trim() : null;
  const standard = typeof m.standard === 'string' && m.standard.trim() ? m.standard.trim() : null;

  const applicability = typeof m.applicability === 'string' ? m.applicability : '';
  const expectation = typeof m.expectation === 'string' ? m.expectation : '';

  const references = Array.isArray(m.references) ? m.references.slice() : [];
  const requirements =
    m.requirements === null ||
    typeof m.requirements === 'string' ||
    typeof m.requirements === 'object'
      ? m.requirements
      : null;

  const mappings =
    m.mappings === null || typeof m.mappings === 'string' || typeof m.mappings === 'object'
      ? m.mappings
      : null;

  if (i18n) {
    if (typeof i18n.titleKey !== 'string' || !i18n.titleKey.trim()) {
      throw new Error(`Rule ${ruleId}: meta.i18n.titleKey must be a non-empty string`);
    }
    if (
      i18n.descriptionKey != null &&
      (typeof i18n.descriptionKey !== 'string' || !i18n.descriptionKey.trim())
    ) {
      throw new Error(
        `Rule ${ruleId}: meta.i18n.descriptionKey must be a non-empty string when provided`
      );
    }
  }

  return {
    title,
    description,
    i18n,
    helpUrl,
    tags,
    wcagSc,
    normativeMappings,
    informativeReferences,
    defaultSeverity,
    defaultConfidence,
    type,
    coverage,

    ruleInterfaceVersion,
    ruleVersion,
    normative,
    atomic,
    deprecated,
    deprecation,
    category,
    standard,
    applicability,
    expectation,
    references,
    requirements,
    mappings
  };
});

// Inlined from src/core/dom-runner.js
const runCore = (function runCore(
  pageUrl,
  contextSelector,
  engineOptions,
  runOnly,
  CHECK_DEFS,
  RULE_IMPLS,
  ENGINE_TAG,
  SCHEMA_VERSION,
  COMPOSITE_RULES
) {
  // Normalize contrast options without mutating caller-provided engineOptions.
  function __normalizeContrastOptions(engineOptions2) {
    const eo = engineOptions2 && typeof engineOptions2 === 'object' ? engineOptions2 : {};
    const c = eo.contrast && typeof eo.contrast === 'object' ? eo.contrast : {};
    const mode = c.mode === 'auditorAssist' ? 'auditorAssist' : 'strictConformance';
    const rootCanvasFallback =
      typeof c.rootCanvasFallback === 'string' && c.rootCanvasFallback.trim()
        ? c.rootCanvasFallback.trim()
        : '#ffffff';
    return { mode, rootCanvasFallback };
  }

  const engineOptionsResolved =
    engineOptions && typeof engineOptions === 'object'
      ? { ...engineOptions, contrast: __normalizeContrastOptions(engineOptions) }
      : { contrast: __normalizeContrastOptions(null) };

  const policy = resolvePolicy(POLICY_CONTRACTS, engineOptionsResolved);

  // contextSelector accepts a single selector string (which may itself be a
  // comma-separated selector list -- ordinary CSS union semantics) OR an
  // array of selector strings for scanning multiple, possibly disjoint
  // regions in one run. Both forms resolve via querySelectorAll, not
  // querySelector, so "matches this selector" means all matches, not just
  // the first. Shared with frame-scan.js (same resolution used to discover
  // which child <iframe>/<frame> elements fall within the same scan scope).
  const { ctxSelector, roots } = resolveContextRoots(document, contextSelector);

  // Default on: opt OUT with `includeShadowDom: false`, not opt in.
  const includeShadowDom = !(
    engineOptionsResolved && engineOptionsResolved.includeShadowDom === false
  );
  // Default off: hidden/collapsed content is excluded from rule evaluation
  // unless the caller explicitly opts in.
  const includeHiddenElements = !!(
    engineOptionsResolved && engineOptionsResolved.includeHiddenElements === true
  );
  const excludeSelectors = normalizeSelectorList(
    engineOptionsResolved && engineOptionsResolved.excludeSelectors
  );
  // Default off: explicit opt-in for "this scan target was never meant to
  // represent a real page" -- see helpers.isWholeDocumentScope().
  const fragment = !!(engineOptionsResolved && engineOptionsResolved.fragment === true);

  const url = pageUrl || (document.location && document.location.href) || null;
  const title = document.title || null;
  // Deterministic timestamp: only use host-provided value (no time-based logic).
  const timestamp =
    engineOptionsResolved &&
    typeof engineOptionsResolved.timestamp === 'string' &&
    engineOptionsResolved.timestamp.trim()
      ? engineOptionsResolved.timestamp.trim()
      : null;

  // createDomHelpers()/createContrastHelpers() persist their element-keyed
  // caches (outerHtmlCache, selectorCache, etc.) on window.__a11ycoreSharedCache
  // so multiple helper instances created *within this run* can share them
  // deterministically. But a window/document is frequently reused across
  // SEPARATE runs -- e.g. Jest's jsdom environment creates one window per
  // test file, and mutating document.body between it() blocks is standard.
  // Those caches are keyed by element reference, not content, so a run that
  // reuses an already-cached element (document.body never changes identity)
  // would otherwise read stale data cached by an earlier, unrelated run on
  // the same window. Clearing at the start of every run keeps sharing scoped
  // to "this run" as intended, without leaking across runs.
  try {
    if (window && window.__a11ycoreSharedCache) window.__a11ycoreSharedCache = {};
  } catch {}

  const sharedHelpers = createDomHelpers({
    document,
    window,
    root: roots,
    includeShadowDom,
    includeHiddenElements,
    excludeSelectors,
    fragment,
    // Optional perf counters (bench/debug only). Deterministic and per-run.
    perfStats: !!(engineOptionsResolved && engineOptionsResolved.perfStats)
  });

  const profileRules = !!(engineOptionsResolved && engineOptionsResolved.profileRules);
  const ruleTimings = profileRules ? Object.create(null) : null;

  function nowMs() {
    // performance.now() if available, else Date.now()
    try {
      if (
        typeof performance !== 'undefined' &&
        performance &&
        typeof performance.now === 'function'
      ) {
        return performance.now();
      }
    } catch (e) {}
    return Date.now();
  }

  // =========================
  // Probes (optional evidence fed by the host app)
  // Keep deterministic + serializable + no-throws.
  // =========================
  function sanitizeProbeValue(v, depth) {
    // depth-bounded, JSON-safe sanitizer
    if (depth <= 0) return null;
    if (v == null) return null;

    const t = typeof v;
    if (t === 'string') return v.length > 2000 ? v.slice(0, 2000) : v;
    if (t === 'number') return Number.isFinite(v) ? v : null;
    if (t === 'boolean') return v;
    if (t === 'function') return null;

    if (Array.isArray(v)) {
      // cap arrays to avoid huge payloads
      const out = [];
      const n = Math.min(v.length, 200);
      for (let i = 0; i < n; i++) out.push(sanitizeProbeValue(v[i], depth - 1));
      return out;
    }

    if (t === 'object') {
      const out = {};
      const keys = Object.keys(v).sort();
      // cap object keys
      const n = Math.min(keys.length, 50);
      for (let i = 0; i < n; i++) {
        const k = keys[i];
        // only allow string keys
        if (typeof k !== 'string') continue;
        out[k] = sanitizeProbeValue(v[k], depth - 1);
      }
      return out;
    }

    return null;
  }

  let probes;
  try {
    const rawProbes =
      engineOptionsResolved && typeof engineOptionsResolved.probes === 'object'
        ? engineOptionsResolved.probes
        : null;
    probes = rawProbes ? sanitizeProbeValue(rawProbes, 6) : null;
    if (!probes || typeof probes !== 'object' || Array.isArray(probes)) probes = null;
  } catch (e) {
    probes = null;
  }

  // =========================
  // Runtime custom rules (engineOptions.customRules)
  // =========================
  // Same module shape as an internal rule file: { id, meta, runInPage, applicability?, data? }.
  // runInPage/applicability may be a real function (fine for same-realm/Node/jsdom callers)
  // or a function-source string (required for cross-realm callers, e.g. a Playwright
  // page.evaluate(runa11yCoreInPage, { engineOptions }) call -- engineOptions crosses a
  // structured-clone/JSON boundary there, so a live Function reference can't survive it,
  // but a string can). Reconstructed via `new Function`, matching exactly how build-core.js
  // already embeds each built-in rule's own runInPage source into the in-page runner.
  // Scan-scoped only (not added to the static CHECK_DEFS/getRulesCatalog()
  // catalog) -- matches surea11y's "fresh engineOptions per call, no
  // mutable global config" design (see ROADMAP.md).
  let effectiveCheckDefs = CHECK_DEFS;
  let effectiveRuleImpls = RULE_IMPLS;
  let overriddenBuiltinIds = [];
  const rawCustomRules = Array.isArray(engineOptionsResolved.customRules)
    ? engineOptionsResolved.customRules
    : [];
  if (rawCustomRules.length) {
    function reviveRuleFn(value) {
      if (typeof value === 'function') return value;
      if (typeof value === 'string' && value.trim()) {
        try {
          const fn = new Function('return (' + value + ')')();
          if (typeof fn === 'function') return fn;
        } catch (e) {
          return null;
        }
      }
      return null;
    }

    const extraDefsById = new Map();
    const extraImpls = {};
    for (const c of rawCustomRules) {
      if (!c || typeof c !== 'object') continue;
      const ruleId = typeof c.id === 'string' ? c.id.trim() : '';
      if (!ruleId) continue;
      const runFn = reviveRuleFn(c.runInPage);
      if (typeof runFn !== 'function') continue; // invalid custom rule: skipped, not a crash

      const applicabilityFn = reviveRuleFn(c.applicability);
      const normalizedMeta = normalizeRuleMeta(ruleId, ruleId, c.meta, ENGINE_TAG);

      // Overriding a built-in rule id is supported (see docs/ENGINE_OPTIONS.md),
      // but a same-named custom rule is just as likely to be an accidental
      // collision (a generic name like "region" or "tabindex" picked without
      // realizing it's already a built-in id) as a deliberate override -- so
      // surface it either way rather than silently swapping the rule out.
      if (CHECK_DEFS.some((d) => d && d.ruleId === ruleId)) {
        overriddenBuiltinIds.push(ruleId);
      }

      extraDefsById.set(ruleId, {
        ruleId,
        title: normalizedMeta.title,
        description: normalizedMeta.description,
        i18n: normalizedMeta.i18n,
        helpUrl: normalizedMeta.helpUrl,
        tags: normalizedMeta.tags,
        wcagSc: normalizedMeta.wcagSc,
        normativeMappings: normalizedMeta.normativeMappings,
        defaultSeverity: normalizedMeta.defaultSeverity,
        defaultConfidence: normalizedMeta.defaultConfidence,
        type: normalizedMeta.type,
        coverage: normalizedMeta.coverage,
        data: c.data === undefined ? null : c.data,
        ruleInterfaceVersion: normalizedMeta.ruleInterfaceVersion,
        ruleVersion: normalizedMeta.ruleVersion,
        normative: normalizedMeta.normative,
        atomic: normalizedMeta.atomic,
        deprecated: normalizedMeta.deprecated,
        deprecation: normalizedMeta.deprecation,
        category: normalizedMeta.category,
        standard: normalizedMeta.standard,
        applicability: normalizedMeta.applicability,
        expectation: normalizedMeta.expectation,
        references: normalizedMeta.references,
        requirements: normalizedMeta.requirements,
        mappings: normalizedMeta.mappings
      });
      extraImpls[ruleId] = { run: runFn, applicability: applicabilityFn || null };
    }

    if (extraDefsById.size) {
      effectiveCheckDefs = CHECK_DEFS.filter((d) => !extraDefsById.has(d.ruleId)).concat(
        Array.from(extraDefsById.values())
      );
      effectiveRuleImpls = { ...RULE_IMPLS, ...extraImpls };
    }

    if (overriddenBuiltinIds.length) {
      try {
        console.warn(
          '[surea11y] customRules overriding built-in rule id(s) for this scan: ' +
            overriddenBuiltinIds.join(', ')
        );
      } catch (e) {}
    }
  }

  const checksResults = [];

  for (const def of effectiveCheckDefs) {
    const t0 = ruleTimings ? nowMs() : 0;
    const defResolved = resolveRuleDefI18n(def, engineOptionsResolved);
    if (!ruleMatchesRunOnly(defResolved, runOnly, ENGINE_TAG)) continue;

    const implEntry = effectiveRuleImpls[defResolved.ruleId];
    const impl = implEntry && typeof implEntry.run === 'function' ? implEntry.run : null;
    const applicabilityFn =
      implEntry && typeof implEntry.applicability === 'function' ? implEntry.applicability : null;
    if (typeof impl !== 'function') continue;

    const ruleConfig =
      engineOptionsResolved &&
      engineOptionsResolved.rules &&
      engineOptionsResolved.rules[defResolved.ruleId]
        ? engineOptionsResolved.rules[defResolved.ruleId]
        : null;

    // Rule-scoped excludeSelectors (engineOptions.rules[ruleId].excludeSelectors)
    // apply on top of the global excludeSelectors for exactly this rule's
    // applicability check + run, then are cleared once this rule is done.
    // Safe because rule execution below is synchronous and one rule at a
    // time -- sharedHelpers is reused across all rules in this loop.
    if (typeof sharedHelpers.__setActiveRuleExcludeSelectors === 'function') {
      sharedHelpers.__setActiveRuleExcludeSelectors(ruleConfig && ruleConfig.excludeSelectors);
    }

    const ctx = {
      document,
      window,
      root: roots,
      rule: defResolved,
      config: ruleConfig,
      helpers: sharedHelpers,
      engineTag: ENGINE_TAG,
      contextSelector: ctxSelector,
      engineOptions:
        engineOptionsResolved && typeof engineOptionsResolved === 'object'
          ? engineOptionsResolved
          : {},

      // Optional evidence channel provided by host app
      inputs: {
        probes
      }
    };

    if (typeof applicabilityFn === 'function') {
      let applicable = true;
      try {
        const res = applicabilityFn(ctx);
        if (typeof res === 'boolean') applicable = res;
        else if (res && typeof res === 'object' && typeof res.applicable === 'boolean')
          applicable = res.applicable;
      } catch (err) {
        const raw = {
          outcome: 'cantTell',
          occurrences: [],
          error: String(err && err.message ? err.message : err),
          engineOptions: {
            ...(ctx.engineOptions || {}),
            locale: normalizeLocale(engineOptionsResolved && engineOptionsResolved.locale)
          }
        };
        checksResults.push(
          normalizeRuleResult(defResolved, raw, SCHEMA_VERSION, policy, sharedHelpers)
        );
        if (ruleTimings)
          ruleTimings[defResolved.ruleId] = (ruleTimings[defResolved.ruleId] || 0) + (nowMs() - t0);
        continue;
      }

      if (!applicable) {
        const raw = {
          outcome: 'notApplicable',
          occurrences: [],
          engineOptions: {
            ...(ctx.engineOptions || {}),
            locale: normalizeLocale(engineOptionsResolved && engineOptionsResolved.locale)
          }
        };
        checksResults.push(
          normalizeRuleResult(defResolved, raw, SCHEMA_VERSION, policy, sharedHelpers)
        );
        if (ruleTimings)
          ruleTimings[defResolved.ruleId] = (ruleTimings[defResolved.ruleId] || 0) + (nowMs() - t0);
        continue;
      }
    }

    let result;
    try {
      result = impl(ctx);
    } catch (err) {
      result = {
        outcome: 'cantTell',
        occurrences: [],
        error: String(err && err.message ? err.message : err),
        engineOptions: {
          ...(ctx.engineOptions || {}),
          locale: normalizeLocale(engineOptionsResolved && engineOptionsResolved.locale)
        }
      };
    }

    if (!result || typeof result !== 'object') {
      if (ruleTimings)
        ruleTimings[defResolved.ruleId] = (ruleTimings[defResolved.ruleId] || 0) + (nowMs() - t0);
      continue;
    }
    if (!result.engineOptions) {
      result.engineOptions = {
        ...(ctx.engineOptions || {}),
        locale: normalizeLocale(engineOptionsResolved && engineOptionsResolved.locale)
      };
    }
    checksResults.push(
      normalizeRuleResult(defResolved, result, SCHEMA_VERSION, policy, sharedHelpers)
    );
    if (ruleTimings)
      ruleTimings[defResolved.ruleId] = (ruleTimings[defResolved.ruleId] || 0) + (nowMs() - t0);
  }

  // Composite rollups below carry no occurrences/nodes of their own, so
  // they never exercise rule-scoped excludes -- but clear the "active
  // rule" state on sharedHelpers regardless, so nothing after this point
  // (composite aggregation, perf stats) can observe a stale rule's excludes.
  if (typeof sharedHelpers.__setActiveRuleExcludeSelectors === 'function') {
    sharedHelpers.__setActiveRuleExcludeSelectors(null);
  }

  // =========================
  // Composite rule aggregation (data-only rollups)
  // =========================
  const rulesResults = [];
  try {
    const composites = Array.isArray(COMPOSITE_RULES) ? COMPOSITE_RULES : [];

    // Determine target conformance level from runOnly.tags (already normalized by caller)
    const LEVEL_RANK = { A: 1, AA: 2, AAA: 3 };

    function inferTargetLevelFromRunOnly(runOnly2) {
      const tags = runOnly2 && Array.isArray(runOnly2.tags) ? runOnly2.tags : [];
      // tags are already lowercase
      if (tags.includes('wcag2aaa') || tags.includes('wcag22aaa') || tags.includes('wcag21aaa'))
        return 'AAA';
      if (tags.includes('wcag2aa') || tags.includes('wcag22aa') || tags.includes('wcag21aa'))
        return 'AA';
      if (tags.includes('wcag2a') || tags.includes('wcag22a') || tags.includes('wcag21a'))
        return 'A';
      return null; // if not specified, don't filter composites (back-compat)
    }

    function normalizeLevel(s) {
      const v = typeof s === 'string' ? s.trim().toUpperCase() : '';
      return v === 'A' || v === 'AA' || v === 'AAA' ? v : null;
    }

    function isAllowedByTargetLevel(compositeLevel, targetLevel) {
      if (!targetLevel) return true;
      const c = LEVEL_RANK[compositeLevel];
      const t = LEVEL_RANK[targetLevel];
      if (!c || !t) return false; // unknown level => safest: exclude
      return c <= t;
    }

    const targetLevel = inferTargetLevelFromRunOnly(runOnly);

    // Severity rollup (deterministic)
    const SEVERITY_RANK = { minor: 1, moderate: 2, serious: 3, critical: 4 };

    function normalizeSeverity(s) {
      const v = typeof s === 'string' ? s.trim().toLowerCase() : '';
      return SEVERITY_RANK[v] ? v : null;
    }

    function maxSeverity(a, b) {
      if (!a) return b || null;
      if (!b) return a || null;
      return SEVERITY_RANK[b] > SEVERITY_RANK[a] ? b : a;
    }

    // Index atomic results by ruleId (deterministic)
    const byRuleId = Object.create(null);
    for (const rr of checksResults) {
      if (rr && typeof rr === 'object' && typeof rr.ruleId === 'string' && rr.ruleId) {
        byRuleId[rr.ruleId] = rr;
      }
    }

    function isNonEmptyString(s) {
      return typeof s === 'string' && !!s.trim();
    }

    function buildCompositeDef(entry) {
      if (!entry || typeof entry !== 'object') return null;

      const ruleId = isNonEmptyString(entry.id) ? entry.id.trim() : String(entry.id || '').trim();
      if (!ruleId) return null;

      const metaIn =
        entry.meta && typeof entry.meta === 'object' && !Array.isArray(entry.meta)
          ? entry.meta
          : {};

      const titleKey =
        typeof metaIn.titleKey === 'string' && metaIn.titleKey.trim() ? metaIn.titleKey.trim() : '';
      const descriptionKey =
        typeof metaIn.descriptionKey === 'string' && metaIn.descriptionKey.trim()
          ? metaIn.descriptionKey.trim()
          : '';

      const wcagSc = Array.isArray(metaIn.wcagSc)
        ? metaIn.wcagSc
            .map(String)
            .map((s) => s.trim())
            .filter(Boolean)
        : [];

      const tags = [];
      tags.push(String(ENGINE_TAG || 'a11ycore').toLowerCase());
      tags.push('composite');

      // Fixed WCAG-version-introduction lists (2.1 and 2.2 additions only -- every other
      // SC, including all pre-2.1 ones, is WCAG 2.0 baseline). Keep in sync with
      // src/coverage/wcag-version-map.js (the canonical copy the rule-authoring
      // consistency test checks against) -- this one has to stay a self-contained
      // literal since runCore is inlined via .toString() with no module access at runtime.
      const WCAG21_NEW_SCS = [
        '1.3.4',
        '1.3.5',
        '1.3.6',
        '1.4.10',
        '1.4.11',
        '1.4.12',
        '1.4.13',
        '2.1.4',
        '2.2.6',
        '2.3.3',
        '2.5.1',
        '2.5.2',
        '2.5.3',
        '2.5.4',
        '2.5.5',
        '2.5.6',
        '4.1.3'
      ];
      const WCAG22_NEW_SCS = [
        '2.4.11',
        '2.4.12',
        '2.4.13',
        '2.5.7',
        '2.5.8',
        '3.2.6',
        '3.3.7',
        '3.3.8',
        '3.3.9'
      ];
      const isWcag22Sc = wcagSc.some((sc) => WCAG22_NEW_SCS.includes(sc));
      const isWcag21Sc = !isWcag22Sc && wcagSc.some((sc) => WCAG21_NEW_SCS.includes(sc));
      const versionTagPrefix = isWcag22Sc ? 'wcag22' : isWcag21Sc ? 'wcag21' : 'wcag2';

      const lvl = typeof metaIn.level === 'string' ? metaIn.level.trim().toUpperCase() : '';
      if (lvl === 'A') {
        tags.push(versionTagPrefix + 'a');
      } else if (lvl === 'AA') {
        tags.push(versionTagPrefix + 'a', versionTagPrefix + 'aa');
      } else if (lvl === 'AAA') {
        tags.push(versionTagPrefix + 'a', versionTagPrefix + 'aa', versionTagPrefix + 'aaa');
      }

      // Build normativeMappings so downstream consumers (like adapters) can derive WCAG SC/level
      const normativeMappingsFromMeta = wcagSc.map((sc) => {
        const m = { standard: 'WCAG', requirement: sc };
        if (lvl === 'A' || lvl === 'AA' || lvl === 'AAA') m.level = lvl;
        return m;
      });

      const checksIds = Array.isArray(entry.checksIds)
        ? entry.checksIds
            .map(String)
            .map((s) => s.trim())
            .filter(Boolean)
        : [];

      return {
        ruleId,
        title: metaIn.title || ruleId,
        description: metaIn.description || '',

        i18n:
          titleKey || descriptionKey
            ? { titleKey: titleKey || '', descriptionKey: descriptionKey || '' }
            : null,

        helpUrl: '',
        tags,

        normativeMappings: normativeMappingsFromMeta,

        defaultSeverity: 'serious',
        defaultConfidence: 'medium',
        type: 'automatic',
        coverage: null,

        ruleInterfaceVersion: '1.0.0',
        ruleVersion: '0.0.0',
        normative: true,
        atomic: false,
        deprecated: false,
        deprecation: null,
        category: null,
        standard: null,
        applicability: '',
        expectation: '',
        references: [],
        requirements: null,
        mappings: null,

        // optional catalog meta passthrough
        data: {
          details: {
            kind: 'compositeRule',
            wcagSc,
            level:
              typeof metaIn.level === 'string' && metaIn.level.trim() ? metaIn.level.trim() : null
          }
        },

        __checksIds: checksIds
      };
    }

    for (let i = 0; i < composites.length; i++) {
      const entry = composites[i];
      const cDef0 = buildCompositeDef(entry);
      if (!cDef0) continue;

      // Conformance-level gate: if scan is AA, suppress AAA composites even if tags match
      const compositeLevel =
        cDef0 && cDef0.data && cDef0.data.details && normalizeLevel(cDef0.data.details.level);

      if (!isAllowedByTargetLevel(compositeLevel, targetLevel)) continue;

      // Localize title/description (uses def.i18n.* keys)
      const cDefResolved = resolveRuleDefI18n(cDef0, engineOptionsResolved);

      // Apply same selection logic to composites
      if (!ruleMatchesRunOnly(cDefResolved, runOnly, ENGINE_TAG)) continue;

      const checksIds = Array.isArray(cDef0.__checksIds) ? cDef0.__checksIds : [];

      // rollup metrics (stable order)
      let failCount = 0;
      let cantTellCount = 0;
      let notApplicableCount = 0;
      let passCount = 0;
      let missingCount = 0;

      const contributors = [];

      let rolledFailSeverity = null; // max severity among FAIL contributors
      let rolledCantTellSeverity = null; // max severity among CANTTELL contributors (optional)

      for (let j = 0; j < checksIds.length; j++) {
        const tid = checksIds[j];
        const child = tid ? byRuleId[tid] : null;

        if (!child) {
          missingCount += 1;
          contributors.push({ testId: tid, outcome: 'missing' });
          continue;
        }

        const out = child.outcome;
        const childSev = normalizeSeverity(child && child.severity);

        contributors.push({ testId: tid, outcome: out, severity: childSev || null });

        if (out === 'fail' && childSev) {
          rolledFailSeverity = maxSeverity(rolledFailSeverity, childSev);
        } else if (out === 'cantTell' && childSev) {
          rolledCantTellSeverity = maxSeverity(rolledCantTellSeverity, childSev);
        }

        if (out === 'fail') failCount += 1;
        else if (out === 'cantTell') cantTellCount += 1;
        else if (out === 'notApplicable') notApplicableCount += 1;
        else if (out === 'pass') passCount += 1;
      }

      // outcome precedence:
      // fail if any fail
      // cantTell if any cantTell OR missing and none fail
      // notApplicable if all notApplicable (and there is at least one test)
      // pass otherwise
      let outcome = 'pass';
      let reasonCode = 'composite.rollup.pass.otherwise';

      if (failCount > 0) {
        outcome = 'fail';
        reasonCode = 'composite.rollup.fail.anyFail';
      } else if (cantTellCount > 0) {
        outcome = 'cantTell';
        reasonCode = 'composite.rollup.cantTell.anyCantTell';
      } else if (missingCount > 0) {
        outcome = 'cantTell';
        reasonCode = 'composite.rollup.cantTell.missingChild';
      } else if (checksIds.length > 0 && notApplicableCount === checksIds.length) {
        outcome = 'notApplicable';
        reasonCode = 'composite.rollup.notApplicable.allInapplicable';
      } else if (checksIds.length === 0) {
        outcome = 'cantTell';
        reasonCode = 'composite.rollup.cantTell.emptyComposite';
      }

      const raw = {
        outcome,
        occurrences: [],

        // REQUIRED by your reporting schema (top-level)
        summaryKey: 'Composite rule rollup',
        i18nKey: 'composite_rollup_summary',
        i18nParams: { reasonCode, testCount: String(checksIds.length) },

        // REQUIRED by your reporting schema (machine-readable payload)
        data: {
          details: {
            reasonCode,
            checksIds: checksIds.slice(),
            contributors,
            metrics: {
              failCount,
              cantTellCount,
              notApplicableCount,
              passCount,
              missingCount
            }
          }
        },

        engineOptions: {
          ...(engineOptionsResolved || {}),
          locale: normalizeLocale(engineOptionsResolved && engineOptionsResolved.locale)
        }
      };

      // Promote composite severity based on contributors (deterministic).
      // - If composite fails: use max severity among failing children.
      // - If composite cantTell: use max severity among cantTell children (fallback to failing if you prefer).
      if (outcome === 'fail' && rolledFailSeverity) {
        raw.severity = rolledFailSeverity;
      } else if (outcome === 'cantTell' && rolledCantTellSeverity) {
        raw.severity = rolledCantTellSeverity;
      }

      rulesResults.push(
        normalizeRuleResult(cDefResolved, raw, SCHEMA_VERSION, policy, sharedHelpers)
      );
    }
  } catch (e) {
    // no-throws: omit rulesResults if anything goes wrong
  }

  // Optional perf counters passthrough (only when enabled). Deterministic.
  let perfStats = null;
  try {
    if (
      engineOptionsResolved &&
      engineOptionsResolved.perfStats &&
      sharedHelpers &&
      typeof sharedHelpers.getPerfStats === 'function'
    ) {
      perfStats = sharedHelpers.getPerfStats();
    }
  } catch (e) {
    perfStats = null;
  }

  if (ruleTimings) {
    if (perfStats && engineOptionsResolved && engineOptionsResolved.profileRules) {
      perfStats.ruleTimings = ruleTimings; // (whatever your timing map is)
    }
  }

  return {
    engine: { tag: ENGINE_TAG, schemaVersion: SCHEMA_VERSION },
    url,
    title,
    timestamp,
    perfStats,
    contextSelector: ctxSelector,
    checksResults,
    rulesResults,
    overriddenBuiltinIds
  };
});

// Inlined from src/core/frame-messaging.js -- postMessage RPC used by
// runa11yCoreAcrossFrames/a11yCoreEnableFrameResponder below (browser-only
// cross-frame scanning for the "plain script injection" consumption mode).
const FRAME_RPC_CHANNEL = "__frame_rpc_v1__";
const getFrameRpcRegistry = (function getFrameRpcRegistry(win) {
  if (!win.__a11yCoreFrameRpc__) {
    win.__a11yCoreFrameRpc__ = {
      pending: new Map(),
      seq: 0,
      responder: null,
      listening: false
    };
  }
  return win.__a11yCoreFrameRpc__;
});
const installFrameRpcListener = (function installFrameRpcListener(win, channel) {
  const registry = getFrameRpcRegistry(win);
  if (registry.listening) return registry;

  win.addEventListener('message', function a11yCoreFrameRpcListener(event) {
    const data = event && event.data;
    if (!data || data.__a11ycore !== true || data.channel !== channel) return;

    if (data.type === 'ping') {
      try {
        event.source &&
          event.source.postMessage(
            { __a11ycore: true, channel: channel, requestId: data.requestId, type: 'pong' },
            '*'
          );
      } catch (e) {
        /* target gone/closed -- nothing to do */
      }
      return;
    }

    if (data.type === 'run') {
      const responder = registry.responder;
      if (typeof responder !== 'function') return; // no responder enabled here: unreachable
      Promise.resolve()
        .then(function () {
          return responder(data.payload);
        })
        .then(function (result) {
          try {
            event.source &&
              event.source.postMessage(
                {
                  __a11ycore: true,
                  channel: channel,
                  requestId: data.requestId,
                  type: 'result',
                  payload: result
                },
                '*'
              );
          } catch (e) {
            /* target gone/closed */
          }
        })
        .catch(function (err) {
          try {
            event.source &&
              event.source.postMessage(
                {
                  __a11ycore: true,
                  channel: channel,
                  requestId: data.requestId,
                  type: 'error',
                  payload: String(err && err.message ? err.message : err)
                },
                '*'
              );
          } catch (e) {
            /* target gone/closed */
          }
        });
      return;
    }

    // 'pong' | 'result' | 'error' -- resolve whichever pending request this replies to.
    const pending = registry.pending.get(data.requestId);
    if (!pending) return;
    if (data.type === 'pong') {
      pending.onPong();
      return;
    }
    registry.pending.delete(data.requestId);
    if (data.type === 'result') pending.resolve(data.payload);
    else
      pending.reject(
        new Error(typeof data.payload === 'string' ? data.payload : 'surea11y frame RPC error')
      );
  });

  registry.listening = true;
  return registry;
});
const nextFrameRpcRequestId = (function nextFrameRpcRequestId(win) {
  const registry = getFrameRpcRegistry(win);
  registry.seq += 1;
  return (
    'req_' +
    Date.now().toString(36) +
    '_' +
    registry.seq +
    '_' +
    Math.random().toString(36).slice(2, 8)
  );
});
const pingFrame = (function pingFrame(win, targetWindow, pingWaitTime) {
  installFrameRpcListener(win, FRAME_RPC_CHANNEL);
  const registry = getFrameRpcRegistry(win);
  const requestId = nextFrameRpcRequestId(win);
  const waitMs = typeof pingWaitTime === 'number' ? pingWaitTime : 500;

  return new Promise(function (resolve) {
    let settled = false;
    const timeout = setTimeout(function () {
      if (settled) return;
      settled = true;
      registry.pending.delete(requestId);
      resolve(false);
    }, waitMs);

    registry.pending.set(requestId, {
      onPong: function () {
        if (settled) return;
        settled = true;
        clearTimeout(timeout);
        registry.pending.delete(requestId);
        resolve(true);
      },
      resolve: function () {},
      reject: function () {}
    });

    try {
      targetWindow.postMessage(
        { __a11ycore: true, channel: FRAME_RPC_CHANNEL, requestId: requestId, type: 'ping' },
        '*'
      );
    } catch (e) {
      if (!settled) {
        settled = true;
        clearTimeout(timeout);
        registry.pending.delete(requestId);
        resolve(false);
      }
    }
  });
});
const sendFrameRunCommand = (function sendFrameRunCommand(win, targetWindow, payload, frameWaitTime) {
  installFrameRpcListener(win, FRAME_RPC_CHANNEL);
  const registry = getFrameRpcRegistry(win);
  const requestId = nextFrameRpcRequestId(win);
  const waitMs = typeof frameWaitTime === 'number' ? frameWaitTime : 60000;

  return new Promise(function (resolve, reject) {
    const timeout = setTimeout(function () {
      registry.pending.delete(requestId);
      reject(new Error('surea11y frame RPC timed out waiting for a run result'));
    }, waitMs);

    registry.pending.set(requestId, {
      onPong: function () {},
      resolve: function (result) {
        clearTimeout(timeout);
        resolve(result);
      },
      reject: function (err) {
        clearTimeout(timeout);
        reject(err);
      }
    });

    try {
      targetWindow.postMessage(
        {
          __a11ycore: true,
          channel: FRAME_RPC_CHANNEL,
          requestId: requestId,
          type: 'run',
          payload: payload
        },
        '*'
      );
    } catch (e) {
      clearTimeout(timeout);
      registry.pending.delete(requestId);
      reject(e);
    }
  });
});
const enableFrameRpcResponder = (function enableFrameRpcResponder(win, handler) {
  installFrameRpcListener(win, FRAME_RPC_CHANNEL);
  const registry = getFrameRpcRegistry(win);
  registry.responder = handler;
  return function disable() {
    if (registry.responder === handler) registry.responder = null;
  };
});

  return runCore(
    pageUrl,
    contextSelector,
    engineOptions,
    resolveEffectiveRunOnly(engineOptions, runOnly),
    CHECK_DEFS,
    RULE_IMPLS,
    ENGINE_TAG,
    SCHEMA_VERSION,
    COMPOSITE_RULES
  );
}

  global.a11ycore = {
    ENGINE_TAG: "a11ycore",
    SCHEMA_VERSION: "1.0.0",
    runa11yCoreInPage: runa11yCoreInPage
  };
})(typeof window !== 'undefined' ? window : this);
